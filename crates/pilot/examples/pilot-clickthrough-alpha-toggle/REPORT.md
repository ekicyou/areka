# REPORT: pilot-clickthrough-alpha-toggle 検証結果

> 本 REPORT は T1〜T8 の機械的な合否・証跡の詳細台帳である（根拠）。結論（go／違う／直す ＋ 学び）は README の「検証結果」に記す。
> 検証手順（R9.1）: 人間の準備確認 → エージェントが `cargo run -p pilot --example pilot-clickthrough-alpha-toggle` を起動 → 結果のヒアリング。
> go 判定は開発者（人間）が下す。Claude Code 単独で合格判定して次フェーズに進まない（R9.6）。

- 検証日: YYYY-MM-DD
- 実行コマンド: `cargo run -p pilot --example pilot-clickthrough-alpha-toggle`
- 環境: OS（例: Windows 11 26200）/ DPI 倍率（例: 100% ＋ 150%）/ モニタ構成（例: シングル / マルチ・配置）

## T1〜T8 合否台帳

| # | 試験項目 | 期待結果 | 合否 | 証跡（観測内容・ログ抜粋・スクショ参照） |
|---|---------|---------|------|----------------------------------------|
| T1 | 起動確認 | 透過トップモスト窓＋中央の不透明円が表示される | ✅/❌/条件付き | |
| T2 | 円外でのクリック透過 | 円外クリックで背面アプリ（デスクトップアイコン等）が反応 | | |
| T3 | 円内でのクリック受領 | 円内クリックで WndProc に `WM_LBUTTONDOWN`（受領ログ＋色トグル） | | |
| T4 | 状態切替の発火 | 円境界をまたぐ瞬間に `[applier] ON→OFF`／`OFF→ON` ログ | | |
| T5 | 状態変化なし時の非発火 | 円内／円外に留まる間は `SetWindowPos` 非呼び出し（切替ログ無し） | | |
| T6 | マルチプロセス透過 | 背面ブラウザのリンクが円外クリックで開く（別プロセスへ透過） | | |
| T7 | DPI 環境での座標一致 | 高 DPI（150% 等）でも円判定が見た目の円と一致（※ client≈window の中心一致も確認） | | |
| T8 | 終了処理 | 窓を閉じるとプロセス・ワーカスレッドが正常終了（残留スレッド無し） | | |

> 検証注記（境界チャタリング対策）: T4／T5 はカーソルを**円境界でゆっくり一度だけまたぐ**こと。高速往復は 16ms 周期で ON↔OFF が連発し、特に T5 の負の証跡が濁る。ヒステリシスは先進坑では実装しない（YAGNI）。
> 検証注記（T7 中心一致）: 判定円・描画円ともに `GetWindowRect` 中心基準。ボーダーレス窓では client≈window で一致するが、見えている円と当たり判定円がズレないか一度目視確認すること（ズレた場合のみ本坑でクライアント矩形基準へ補正）。

## 必須合格基準（T1・T2・T3・T4・T6）の充足

- すべて ✅ か: はい / いいえ（いいえの場合は不足項目を明記）
- 条件付き可（T5・T7・T8）: ✅ または軽微な条件付き合格（理由を明記）

## 総合判定（人間が記入）

- go / 違う / 直す: ____
- 理由・学び: ____
