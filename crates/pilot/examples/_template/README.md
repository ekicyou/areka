# 先進坑: _template（雛形・コピーして使う）

このフォルダ（`examples/_template/`）は新しい先進坑を即座に着手するための README 雛形である。
`_` 前置で実 spec フォルダ（`examples/<spec-name>/`）と区別する。実際の先進坑を掘るときは、
この `README.md` と隣の `main.rs` を `examples/<spec-name>/` へコピーし、以下 3 幕の各欄を埋めること。

> この README は先進坑の**一次記録（正本）**である。本坑 spec の design はここの検証結果を参照し、
> 同じ結果を二重化しない。

## 動機（なぜ掘るか）

- 対応する本坑 spec: `<main-spec-name>`   ← 必ず名指しする（先進坑⟷本坑の traceability）
- 確認したい方向 / 実現可能性 / 手順:
  - （何が怪しくて掘るのか。よく分かっている所は掘らず直に本坑へ）

## 概要（何を作ったか）

- 実装内容:
  - （この先進坑で何を作って試したか）
- 実行法: `cargo run -p pilot --example <spec-name>`

## 検証結果

- 判定: go / 違う / 直す   ← いずれかを残す
- 学び:
  - （得られた知見。本坑をクリーンに掘り直すための材料。コピペ donor にはしない）
- 日付: YYYY-MM-DD
