//! 差し替え可能な時刻供給コンポーネント（ticker）。
//!
//! OS時計の絶対グリッド（既定 50ms・1000ms の2周期）に整列して時刻の刻みを配り、
//! 処理遅延が蓄積しないようにする。大幅な遅延で複数の境界を跨いだ場合は各系統
//! 1回のみ配り、次の境界へスナップする。決定論テストでは起動せず外部からの
//! 直接投函に道を譲れるようにする（design.md「ghost::ticker」）。task 2.2 で実装する。
