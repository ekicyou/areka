//! lexer — 構文層。
//!
//! 後続タスク 3.1 で `char_indices` 手書き線形スキャナを実装する。
//! ブロック/行/ドット付きキー/行指向 CSV/`[id,...]` 配列を `pub(crate)`
//! トークンへ区切る（構文区切りのみを担い、意味正規化は decode の領分）。
//! 未閉じ境界は `Raw` として吸収し走査を中断しない寛容方針を採る。
