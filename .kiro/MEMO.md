# LLM参照を禁じる。このドキュメントは人間用です。LLMは参考にせず、書き込みも禁止


/kiro-discovery
# メッセージループとウィンドウ起動を**wintf-winmsg-executor**にベースに置き換える検討

現在、メッセージループはほぼ自作であるが、非同期関数の実行ができるメッセージループ作成クレート`wintf-winmsg-executor`を用意したので置き換えを実施せよ。

## 置き換えの同期

フォーク元である`winmsg-executor`は、Windows低レベルレイヤーに関する深い知見に基づいた洗練されたコードであり、私たち開発者の思い付きのようなコードより正しい挙動が得られると判断する。

また、UIスレッド上などで簡便に非同期コードが実行可能であり、利用上のトラブル余地も極めて小さい。wintfのコードベースがこれ以上膨らむ前に、置き換えを行うべきであると判断した。


## 必要と思われる仕様

+ `wintf-winmsg-executor`クレートの技術概要を`wintf-winmsg-executor`の実装ソースコードレベルで知見調査・記録し、置き換え可能か検討、置き換え時にどこを修正すべきかを判断する仕様
+ 置き換え可能と判断したら、実際に置き換えを行う仕様

## `wintf-winmsg-executor`の主な調査領域
+ doc.rsより
  + https://docs.rs/wintf-winmsg-executor/latest/wintf_winmsg_executor/
  + https://docs.rs/wintf-winmsg-executor/latest/wintf_winmsg_executor/util/struct.Window.html
    + fn new_ex（実装ソースコードも調査対象）
    + fn new_checked_ex（実装ソースコードも調査対象）
    + ソースコードの内部関数「get_instance_handle」この関数は現在未公開だが、次のバージョンで pub fn util::get_instance_handle()に変更します。
    + Windowを作るとき、`util::Window::new_ex`または`new_checked_ex`を使うこと。
  + https://docs.rs/wintf-winmsg-executor/latest/wintf_winmsg_executor/fn.block_on.html
  + https://docs.rs/wintf-winmsg-executor/latest/wintf_winmsg_executor/fn.spawn_local.html

## 検討すべき内容
+ ECSのtick起動をどうすべきか。ecs ticksを非同期タスクとして予約し、60Hz起動スレッドからウェイクアップしてもらう？多分、現在のやりかた（起動メッセージをpopする）方法より洗練されるはず。rust非同期関数のウェイクアップ作法ってあるのか？C#ならTaskCompletionSourceを使うべきシチュエーション。tokio依存は避けたい。event_listenerクレートがよさそうですね。






/kiro-discovery
# 「αマスク連動クリックスルー」実証試験と導入

## 0. 背景と前提知識（必読）

このタスクは Windows 10/11 上で動作する **Rust 製デスクトップマスコット** に「キャラクター描画領域だけクリックを受け、それ以外の透明領域は背面アプリ（他プロセス）にクリックを透過させる」機能を実装するものです。

過去の検討で以下が確定しています。**この知見を前提として作業してください。再調査・再検討は不要です。**

### 0-1. 採用しない手法と理由

- **`WM_NCHITTEST` で `HTTRANSPARENT` を返す方式は採用しない**。
  - MSDN仕様上、HTTRANSPARENTのカスケードは「同一スレッド内」でしか機能せず、他プロセス（Explorer/ブラウザ等）には届かないため、デスクトップマスコット用途では役に立たない。
- **Layered Window (`UpdateLayeredWindow` + GDI) も採用しない**。
  - GPU→CPU転送が毎フレーム発生し、3D描画では重い。今回はモダンな構成にする。

### 0-2. 採用する手法

「**`WS_EX_TRANSPARENT` を αマスクに応じて動的に付け外しする**」方式。

- 既定状態：`WS_EX_TRANSPARENT` は立っていない（クリックを受ける）
- 別スレッドのワーカが定期的にカーソル位置を取得し、αマスク関数に問い合わせる
- 透明領域なら `WS_EX_TRANSPARENT` を立てる、不透明領域なら外す
- 状態変化したフレームでのみ `SetWindowLongPtr` + `SetWindowPos(SWP_FRAMECHANGED)` を呼ぶ
- Windows 10以降は **`WS_EX_LAYERED` は併用不要**（他プロセスへのクリック透過は WS_EX_TRANSPARENT 単独で成立）

### 0-3. 参考にする既存実装（Rustポートではない）

- **kirurobo/UniWindowController**（Unity, C# + C++, MIT）
  - 本リポジトリの `VisualStudio/LibUniWinC/libuniwinc.cpp` 内 `SetClickThrough()` がコア
  - C#側 `UniWindowController.cs` の HitTestType::Opacity 判定（毎フレーム `Texture2D.ReadPixels` で1px読み）を参考
- これらの設計を Rust + `windows` クレートに置き換えるのが今回の作業

### 0-4. 同等のRustクレートは現時点で存在しない

調査済み。`winit::Window::set_cursor_hittest(bool)` や `tauri::set_ignore_cursor_events(bool)` は **API としては存在する** が、「αマスクに応じた自動切替」を行うクレートは見つかっていない。自前実装する。

---

## 1. タスクの全体構成

このタスクは **2フェーズ**（2仕様）で進めてください。フェーズ1の合否判定を必ず人間（依頼者）にレポートし、合格判定を得てからフェーズ2に進むこと。**勝手に統合しないこと。**

```
[フェーズ1] 実証試験（PoC）
    ↓ 合否レポート → 依頼者の承認
[フェーズ2] 本体プロジェクトへの導入
```

---

## 2. フェーズ1：実証試験（PoC）

### 2-1. 成果物

独立したexample 先進坑プロジェクトを作成。

### 2-2. PoCの仕様

最小構成で以下を実証する：

1. 透過したトップモストのウィンドウを作成
2. ウィンドウ全域は透明。中央に「不透明な四角領域」を定義
3. 別スレッドが16ms周期でカーソル位置を見て、αマスク関数（仮想）に問い合わせる
4. 円内ならクリックスルーOFF、円外ならクリックスルーONを動的に切り替える
5. 切替が起こるたびでログ出力
6. 四角領域のクリックでもログ出力＆四角領域の色をトグル変更

重要なのは「クリックスルーの状態切替が正しく動くこと」「実際に四角がクリックできること」。

αマスク関数は以下のような仮実装でよい。座標とかは適当だが、あえて丸判定する方がクリックスルーが分かりやすいかもしれない。

```rust
let mask: HitMaskFn = Arc::new(|x: i32, y: i32| -> bool {
    // 仮：画面中央(960, 540)を中心とした半径200pxの円の「外」を透明扱い
    let dx = x - 960;
    let dy = y - 540;
    let dist_sq = dx * dx + dy * dy;
    dist_sq > 200 * 200  // true = 透明 = クリックスルーON
});
```

### 2-4. 必須の実装上の注意

- **DPI対応**: `main` 冒頭で `SetProcessDpiAwarenessContext(DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2)` を必ず呼ぶ。これを忘れるとマルチモニタ環境で GetCursorPos の座標がズレる。
- **状態変化最適化**: 毎フレーム SetWindowLongPtr を呼ぶのは禁止。前回状態と比較し、変化したときだけ呼ぶ。
- **HWND の Send**: HWND は `!Send` だが、`unsafe impl Send for AppState` でラップしてよい。Win32の慣例。
- **HTTRANSPARENT は使わない**。WM_NCHITTEST を自前ハンドルしない。
- **WS_EX_LAYERED は付けない**。WS_EX_TRANSPARENT 単独で十分。

### 2-5. 実証試験項目（必ず全項目を実施しレポートに記載）

PoCをビルド・実行した上で、以下を **すべて手動検証** し、`REPORT.md` に結果を記載すること。手動検証は人間とともに実装フェーズで行う。人間の準備を確認⇒エージェントがプログラム起動⇒結果のヒアリングを行え。

| # | 試験項目 | 期待結果 | 検証方法 |
|---|---|---|---|
| T1 | 起動確認 | 透過トップモストウィンドウが表示される | `cargo run --release` で実行 |
| T2 | 円外でのクリック透過 | 円外でクリックすると **背面アプリ**（例：デスクトップアイコン、エクスプローラ）が反応する | デスクトップアイコンをダブルクリックして起動できることを確認 |
| T3 | 円内でのクリック受領 | 円内でクリックすると、PoCのWndProcに WM_LBUTTONDOWN が届く | ログ出力で確認 |
| T4 | 状態切替の発火 | カーソルが円の境界をまたぐ瞬間に「ON→OFF」「OFF→ON」のログが出る | カーソルをゆっくり移動してログ確認 |
| T5 | 状態変化なし時の非発火 | 円内に留まっている間や円外に留まっている間は SetWindowPos が呼ばれない | ログで余計な呼び出しが出ないことを確認 |
| T6 | マルチプロセス透過 | ブラウザを背面に置き、円外でブラウザ内のリンクをクリックすると、ブラウザのリンクが開く | Edge/Chrome等で確認 |
| T7 | DPI環境での座標一致 | 高DPIディスプレイ（150% など）でも円判定が見た目と一致する | 設定 > ディスプレイ > スケーリングを変更して再起動・検証 |
| T8 | 終了処理 | ウィンドウを閉じるとプロセスが正常終了する（ワーカスレッドも回収される） | タスクマネージャで確認 |

### 2-6. REPORT.md のフォーマット

```markdown
# 実証試験レポート

## 環境
- OS: Windows 11 24H2（など具体的に）
- ディスプレイ構成: メイン1920x1080 100% + サブ2560x1440 150%（など）
- Rust: rustc 1.xx.x
- windows クレート: 0.58.x

## 試験結果

| # | 試験項目 | 結果 | 備考 |
|---|---|---|---|
| T1 | 起動確認 | ✅ / ❌ | |
| ... | | | |

## 判明した問題点
（あれば記載。なければ「なし」と明記）

## 結論
- 採用判定: 推奨 / 条件付き推奨 / 非推奨
- 推奨の理由：
- 条件付きの場合：要対処事項一覧
```

### 2-7. フェーズ1完了の合否判定基準

以下を **すべて満たした場合のみ「合格」** とする：

- T1 〜 T8 のうち、T1, T2, T3, T4, T6 が ✅
- T5, T7, T8 は ✅ または「軽微な条件付き合格」（理由を明記）

合格・不合格を問わず、必ずレポートを作成し、依頼者の判断を仰ぐこと。**Claude Code 単独で「合格判定」して次フェーズに進まないこと。**

---

## 3. フェーズ2：本体プロジェクトへの導入

**フェーズ1で依頼者から明示的に「導入を進めてよい」と承認を得てから着手すること。**

### 3-1. 導入時の追加検証

導入後、本体プロジェクトのビルドが通り、以下が動作することを確認：

- 既存機能が壊れていないこと
- 本体のαマスク関数（実描画αバッファの参照）と接続して、キャラクター領域のみクリック可能になっていること
- 既存のリリースビルドフラグ・最適化設定と互換であること

### 3-2. ドキュメント

`docs/click_through.md` を新規作成し、以下を記載：

- 仕組みの概要（WS_EX_TRANSPARENT トグル方式である旨）
- なぜ HTTRANSPARENT や Layered Window ではないか（0節の要約）
- API使用例
- 既知の制約（DPI、αマスク関数の呼び出し頻度、など）

---

## 4. 禁止事項・注意事項

- **`tokio` の使用は禁止**。`event_listener` + `std::thread` で組むこと（依頼者の方針）。
- **`WS_EX_LAYERED` を勝手に追加しないこと**。必要だと判断したら必ず理由とともに依頼者に確認。
- **`WM_NCHITTEST` ハンドラを書かないこと**。HTTRANSPARENTは他プロセスに届かないため無意味。
- **既存の本体コードを推測で書き換えないこと**。フェーズ2で既存コードに触る前に、対象ファイルと変更内容を依頼者に提示して確認を取ること。
- **Cargo.toml の依存追加は最小限**。新しい大きなクレート（winit, tauriなど）は提案のみ行い、勝手に追加しないこと。
- **マルチモニタ・高DPI環境を仮定すること**。プライマリディスプレイのみを前提にした実装はしない。
- **クレートやWin32 APIの仕様で不確実な点があれば、推測で進めず質問すること**。

