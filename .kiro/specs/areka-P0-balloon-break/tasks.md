# Implementation Plan

> 本計画は `.kiro/specs/areka-P0-balloon-break/design.md` の File Structure Plan・Requirements Traceability・Testing Strategy に対応する。行数は着手時に `wc -l` で引き直す。新しい crate・新しい外部依存・`Cargo.toml` の変更は **0**。
> テストは実装と同じディレクトリの兄弟ファイルへ置く（要件 7.6）。既に決定論テストで固定されている配線（閉じ指示 → 再生停止 →「中断で終わった」を返す経路）は再テストしない（要件 7.7）。

- [ ] 1. 基盤: 欄と入口の宣言を置き、以降のタスクが宣言行で衝突しないようにする
- [x] 1.1 横断改変: 2 つの欄と、中断要求の入口の宣言を一度に足す
  - 再生の完了通知に「止めた時点で台本が終了を予約していたか」の真偽の欄を足し、組み立てている全箇所（製品コードの構築点 2 つ・残りはテストと模擬）を偽で追随させる
  - 運行の状態に「利用者の中断を出した相手のトーク」の欄を足し、直に組み立てている全箇所を「無し」で追随させる
  - 運行への指示と状態機械の入力に「利用者の中断（スコープ付き）」を 1 種ずつ足し、殻の写しに 1 腕を足して状態機械の横断の腕へ渡す（この時点の受理側は何もせず状態も指示も変えない空の腕でよい。中身は 4.1 が埋める）
  - 以降のタスクが宣言行で衝突しないよう、本仕様が新設する実装 3 ファイルと兄弟テスト 4 ファイルの module 宣言と空の雛形を、このタスクで置く（以降のタスクは中身を埋めるだけ）
  - 欄も入口も、この時点では振る舞いを 1 つも変えない
  - 完了状態: ワークスペースのテストが緑のまま、両方の欄と中断の指示・入力が型に在り、7 つの新規ファイルが空の雛形として存在する（以降のタスクは UI 側・運行側のどちらからでも中断の指示を組み立てられる）
  - _Requirements: 3.3, 7.6_

- [x] 1.2 再生の側が「終了の予約」を報告する
  - 閉じ指示を受けて再生を止める関数が、再生中の状態が持ち回っている終わり方を読み、「終了を予約していた」を完了通知に載せる。自然に終わったときの通知は常に偽
  - 止める意味論（何を捨てるか・どこで止めるか）と、返す終わり方の種類（3 値）は変えない
  - 完了状態: 兄弟テストで「`\-` を含む台本を閉じると予約あり・含まない台本は予約なし」が緑（判断分岐 ⑺ の再生側）
  - _Requirements: 3.3, 3.8, 7.1_
  - _Depends: 1.1_

- [ ] 2. 表示の側: 隠す理由と、旗を運ぶ受け口
- [x] 2.1 (P) 「中断された」で全バルーンを隠し、次のトークまで出し直さない
  - 表示の合図の語彙と、バルーンを隠す理由の語彙に「利用者が中断した」を 1 つずつ足す（既存 4 理由の意味・タイムアウト計測・抑止の規則は変えない）
  - 判断中核に「中断の非表示」の段を、合図の畳み込みの直後・内容による表示の前に置く。現に出ているスコープだけを昇順で 1 件の指示に載せ、ドラッグ・滞在・選択肢表示中の抑止は見ない
  - 「中断で隠した」記憶を 1 本持ち、内容による表示をトークが始まるまで見送る。見送りでは記録を 1 行も出さない。トークが始まった合図で記憶を解く
  - タイムアウトの段が見る可視の集合からも隠したスコープを除き、本仕様として新しい記録を 1 行も足さない
  - 隠す理由が「4 種」と書いている説明と、「中断のみを理由とする即時非表示の経路は無い」と書いている 2 か所のコメントを新しい規則に合わせる
  - 完了状態: 兄弟テストで「2 スコープ可視 → 1 件の指示で両方隠れ、不可視は載らない」「隠した後に見える文字が増えても出し直さない／トークの始まりの後に増えたら出る」「続く巡で期限が来ても行動 0 件」が緑。配線層は変更 0 行
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6, 4.7, 4.8, 7.1, 7.4_
  - _Depends: 1.1_
  - _Boundary: balloon visibility core, talk lifecycle signals_

- [x] 2.2 (P) 中断の無効化の合図を台本から UI へ運ぶ 7 本目の受け口
  - 汎用コマンド運搬から「名前＋第 1 引数」で `enter,nouserbreakmode`／`leave,nouserbreakmode` の 2 組だけを自己選別し、担当外（`onlinemode` など・非運搬）は記録して読み飛ばす。cue の占有時間には触れない
  - 複製で状態を初期化し、複製後の最初の指示で「このトークが始まった」を 1 回だけ同じ線へ流す（旗とトークの境界を 1 本の線で運ぶ）
  - 受け口は旗の状態を持たない（入れ子・区間外の判定は UI が下す）。送出の失敗は記録して台本を続ける
  - 消費者台帳に 2 行を足し、件数を固定しているテストの 6 を 8 へ直す。正典 URL のコメント 2 行を消費者の定義側に置く
  - 完了状態: 兄弟テストで「2 組だけを拾う・`onlinemode` は拾わない・複製ごとに開始が 1 回だけ先頭に出る」が緑、台帳の一意性と件数のテストが 8 で緑
  - _Requirements: 5.1, 5.2, 5.4, 7.3, 8.4, 9.2, 9.7_
  - _Depends: 1.1_
  - _Boundary: emo2_boot 運搬 cue の受け口, 消費者台帳_

- [ ] 3. UI 入力: 押下 1 回を結論へ写し、2 つの線へ送る
- [x] 3.1 押下の判定と旗の畳み込みを純関数で置く
  - 押下 1 回を 5 つの結論（左ダブルクリックでない・選択の確定に消費された・バルーンが出ていない・無効化の区間・受理）のどれかへ写す純関数を、上から順に最初に当たった結論を返す形で置く
  - 旗の畳み込みを純関数で置く: 入る・出る・トークの始まりで解く・入れ子は数えない・区間外の「出る」は記録だけ
  - 中断の合図は左ダブルクリックだけから作る。右・中・単押し・3 打目以降からは作らない。SSTP に関する判定は 1 つも置かない
  - 完了状態: 兄弟テストで判断分岐 ⑷（無効化の区間 → 退ける）⑸（選択の確定の続きの 2 打目 → 作らない）、バルーンが出ていない・観測できないの 2 通り、旗の 4 通り＋入れ子が緑
  - _Requirements: 1.2, 1.5, 1.6, 1.9, 2.3, 5.3, 5.5, 5.6, 5.8, 7.1, 7.3, 9.3, 9.5_
  - _Depends: 2.2_
  - _Boundary: areka input_events/user_break（純関数）_

- [x] 3.2 押下ハンドラから中断を送り出す
  - UI の持ち物を 1 つ置く: 旗・旗の受信端・表示の合図の送出端・運行への送出端・直前の左押下が選択の確定だったかの記憶。旗の読み口を外へ開けておく（読む者はまだ居ないので通知は作らない）
  - 旗の取り出しを毎巡走らせ、旗が変わったこと・区間外の「出る」を記録する
  - バルーン窓の押下ハンドラの末尾から入口を呼ぶ。入口は直前の押下の記憶を読んでから今回で上書きし、左ダブルクリックだけを検出として記録し、バルーンが出ているかを表示層へ照会して判定へ渡す
  - 受理なら表示の合図の線へ「中断された」を、運行への線へスコープ付きの中断要求を、この順に 1 件ずつ送る。どちらの失敗も記録し、もう一方の送出はやめない
  - 持ち物が無い（結線前）は記録して何もしない。キャラクター窓のダブルクリックとバルーン窓の右ボタンには触れない
  - 完了状態: 兄弟テストで「受理のとき 2 本の線にちょうど 1 件ずつ届く」「受信端を落とした線では記録が出て、もう一方は届く」「結線前は 0 件」が緑。既存の押下ハンドラのテストが緑のまま
  - _Requirements: 1.1, 1.3, 1.4, 1.7, 1.8, 2.3, 2.8, 4.2, 5.7, 6.3, 6.4, 6.5, 6.6, 9.8_
  - _Depends: 2.1, 3.1_

- [x] 3.3 結線: 旗の線を敷き、押下より前に取り出す
  - 起動の結線で旗の線を 1 本作り、送出端を 7 本目の受け口として運搬の受け手の列の末尾に足し、受信端・表示の合図の送出端の複製・運行への送出端で UI の持ち物を据える
  - 旗の取り出しを入力の段の、ポインタの配送より**前**に登録する（同じ巡に届いた「入る」が同じ巡の押下の判定に効くため）
  - これで台本の `\![enter,nouserbreakmode]` が UI の旗へ届く経路が繋がる（実機での確かめは 5.3）
  - 完了状態: 登録順を確かめるテストが緑（同じ巡に届いた「入る」で押下が退けられる）。既存の起動の結線テスト・背骨テストが緑のまま
  - _Requirements: 4.5, 5.1, 5.4_
  - _Depends: 2.2, 3.2_

- [ ] 4. 運行の側: 受理の規則と、終了の予約の効かせ方
- [x] 4.1 (P) 中断の受理の規則
  - 1.1 が置いた空の横断の腕に中身を入れる。場面を問わずに受け、現行のトークが在れば相手を帳簿に控えて既存の停止経路の指示を 1 件返す。同じ相手への 2 件目は何も返さない。現行のトークが無ければ状態を変えず何も返さない
  - 受理の時点で選択の待ちの帳簿を消す（掃除点を 1 つ足す）。完了通知を待つと、その間に期限を過ぎた刻みで時間切れの通知が SHIORI へ出てしまうため
  - 返す指示は停止の 1 種だけで、SHIORI への要求は 1 件も積まない。場面（起動の挨拶・定常・終了の握手）でもスコープ番号でも結論を変えない。面は戻さない
  - 受理を 1 行・止める再生が無いを 1 行記録する。停止の指示を送れないときは既存の記録に任せ、足す記録は 0。既存の停止指示の説明を、発行点が 2 つになったことに合わせて直す
  - 完了状態: 兄弟テストで判断分岐 ⑴（3 つの場面のそれぞれで停止 1 件だけ・SHIORI 要求 0 件）⑵（何も返さない・状態不変）⑶（選択待ちで受理 → 完了通知の前でも後でも、期限後の刻みで時間切れが出ない）と 2 件目の無視が緑
  - _Requirements: 2.1, 2.2, 2.4, 2.5, 2.6, 2.7, 2.8, 3.1, 3.2, 3.5, 3.9, 6.1, 6.2, 6.5, 7.1, 9.1, 9.4_
  - _Depends: 1.1_
  - _Boundary: areka-kanade schedule/user_break, schedule/mod の横断の腕と帳簿_

- [x] 4.2 終了の予約を利用者の中断のときだけ終了へ結ぶ
  - 現行トークの完了を受けた時点で帳簿を必ず空にし、「利用者の中断で終わり、かつ終了の予約があった」ときだけ真を返す判定を置く
  - 完了通知の「中断で終わった」の腕でその判定を先に呼び、真なら `\-` に辿り着いたのと同じ終了の遷移へ進めて 1 行記録する。偽なら既存どおり定常へ戻る
  - 差し替え・選択肢の時間切れの解除で止まったときは終了しない（帳簿が空なので偽になる）
  - 完了状態: 兄弟テストで判断分岐 ⑺ の 3 通り（中断＋予約あり → 終了／中断＋予約なし → 定常／予約ありでも中断を出していない → 定常）と、中断の後に定常へ戻って次の刻みで普通に次のトークの要求が出ることが緑
  - _Requirements: 3.4, 3.8, 7.1, 7.2_
  - _Depends: 1.2, 4.1_

- [ ] 4.3 別れの台詞はどの終わり方でも終了で終わる
  - 終了の握手の待ちで完了通知を受けたときの腕を「終了を拒んで定常へ戻る」から「終了へ進む」へ改める。最後まで終わったときも中断されたときも同じ腕
  - 拒否の記録を廃し、終了へ進んだことの記録に置き換える（既存ログ行の変更はこの 1 行）。期限超過の扱いは変えない
  - 旧規則を固定している既存テスト 3 本（運行の 2 本＋統合の 1 本）を、新規則を固定する形へ改める
  - 旧規則を説明している既存コメント（背骨側 3 か所・運行側 3 か所・モジュール doc）を新規則に合わせる
  - 完了状態: 判断分岐 ⑻ が緑（最後まで／中断 のどちらでも終了の遷移と終了の要求が出る）。終了の握手から定常へ戻る経路が 0 本であることが、書き換えた 3 本で示される
  - _Requirements: 3.6, 3.7, 7.1, 7.7, 9.6_
  - _Depends: 4.2_

- [ ] 5. 仕上げ: 摂動・台帳・実機
- [ ] 5.1 檻が赤になることを 1 つの分岐で示す
  - 判断分岐 ⑺ の「中断を出した相手か」の条件を常に真へ置き換える（平行移動ではなく経路から外す形）と、「予約ありでも中断を出していない → 定常へ戻る」のテストが赤になることを示す
  - 摂動は戻し、赤になったテスト名と 2 回の走行の結果を完了記録に残す
  - 完了状態: 摂動で赤・復帰で緑の 2 回の走行の記録が残っている
  - _Requirements: 7.5_
  - _Depends: 4.2_

- [ ] 5.2 網羅台帳の担当を登記し、互換の対応表を追随させる
  - さくらスクリプトの台帳の 2 項目（`enter,nouserbreakmode`・`leave,nouserbreakmode`）の担当欄に本仕様を書き、着地した実装に合わせて状態を実装済みへ改める
  - 担当にしないもの（中断イベント・`\t`・`onlinemode` などの他の名前・中断の操作そのもの＝登記できる台帳の行が 0 行）は登記しない
  - 同じ変更で調査の下書きの本仕様の行・件数・本文の手書きの数を**数え直した値**で直し、報告を作り直す
  - 互換の対応表に 3 行（中断の操作・無効化の閉じ忘れ・別れの台詞は必ず終了）を足し、「中断のみを理由とする即時非表示の経路は無い」の 1 行を直す
  - 完了状態: 調査の検査（`cargo test -p ukadoc-survey`）が緑で、報告の数と台帳が一致している
  - _Requirements: 8.1, 8.2, 8.3_
  - _Depends: 3.3, 4.3_

- [ ] 5.3 実機サインオフと行数の番人
  - 触った全ファイルが 1 ファイル 1,000 行以内であることを番人のテストで確かめる（入力ハンドラと運行の 2 ファイルは残りが 100 行を切っているので、着手時と着地時に引き直す）
  - emo2 を絶対パスで起動し、長い台詞の途中でバルーンを左ダブルクリックして ⑴ 文字送りがその場で止まる ⑵ バルーンが即座に消える（30 秒待たない）⑶ その後に次のランダムトークが普通に出る を確かめる
  - 別れの台詞を返すゴーストで右クリックメニューの「終了」を選び、⑷ 台詞の途中の左ダブルクリックでただちに終了する を確かめる
  - 確認項目と結果（ログの検索語の当たり）を完了記録に残す。裁定を覆す必要が出たときは、要件・設計・境界の節を同じ変更で直す
  - 完了状態: 4 点すべてに結果が付き、番人のテストとワークスペースのテストが緑
  - _Requirements: 7.8, 7.9, 9.9_
  - _Depends: 5.1, 5.2_

## Implementation Notes
- 3.2: GPU 無しのテストでは可視のバルーンを作れない（表示が一度も確立していない target は `show_target` が失敗する）。そのため `on_left_press` は表示層への照会だけをして、非公開の `press_with_visibility(.., balloon_visible)` へ任せる形にした。照会先が `balloon_target(scope)` であることと、押下ハンドラの末尾から入口が呼ばれることは、実機サインオフ（5.3）で確かめる。
- 3.2: `UserBreakWiring` を World に据えるテストは `insert_non_send_resource(UserBreakWiring::new(..))`。ログの有無は `crate::placement::test_support::capture_logs` で見る（「無い」は同じ捕捉の中の「有る」と対にする）。
- 3.3: 順序指定を外しても振る舞いのテストは実行器の並び次第で緑になりうる。そのため登録順のテストは、スケジュールの依存グラフに「取り出し → ポインタの配送」の辺が在ることも併せて確かめている（bevy_ecs の `Schedule::graph()`・最初の `run_schedule` より前に読む）。`wire_emo2_boot` が `wire_user_break` を実際に呼ぶことは、受け口の並びのソース照合（`t_zwi05_…`）と実機サインオフ（5.3）で確かめる。
- 4.2: `TalkDone` は場面を問わず `schedule::step` の横断の腕から `fn on_talk_done` を通る（`fn current_talk_id` が `Some` を返す `Steady{Some}`・`BootVersion{Some}`・`CloseTalkWait` の 3 場面すべて）。したがって「利用者の中断＋終了の予約」は別れの台詞の最中でも `fn on_talk_done` の中で先に終了へ進み、そのときの記録は `talk_done_break_quit` になる（`close.rs` へは届かない）。
