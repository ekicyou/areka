# roadmap 追記の草案（`areka-P0-bindoption-exclusivity` 完了時に `.kiro/steering/roadmap.md` へ載せる）

> **本ファイルは草案である。** `.kiro/steering/roadmap.md` への実際の追記は spec 完了時（`/kiro-complete`）が担当する。本ファイル自体は roadmap ではない。
>
> **プレースホルダの扱い**:
> - `追記(NN)` の番号は完了時に確定する（現行 roadmap の最新は追記(62)＝2026-08-11 棚卸⑦）。
> - `YYYY-MM-DD` は完了日（PR squash マージ日）を入れる。
> - `PR#NNN` はマージ時に確定する PR 番号を入れる。
> - **【実機】** で始まる文は、タスク 7.2（emo2 実機実走・目視サインオフ）の完了後に実測値へ差し替える。7.2 が未完了のまま完了登記に進むことはない（要件 5.6）。
> - 完了 spec 総数は完了時の実数へ更新する。**計数規約＝`.kiro/specs/completed/` の直下エントリ数**（現在 152＝ディレクトリ 151 ＋ 単体ファイル `graphics-rendering-stability.md` 1）。**ディレクトリ数 151 を採ると 1 ずれる**。本 spec 着地後の正しい値は **153**（詳細と stale の事実は §3 の該当行）。
>
> 根拠: 要件 6.2／6.3／6.4（`requirements.md:154-156`）。覆しの記録は `design.md:144-152`、2026-08-11 裁定の記録は `design.md:110-113`。

---

## 1. 追記本体（roadmap 末尾の追記群へ追加する）

**YYYY-MM-DD 追記(NN)（`bindoption-exclusivity` 完了＝表情固着の根治と `mayuna-compose` R4.5/D11 の覆し）**: W6 の挙動バグ 1 本が着地（PR#NNN）。`bindoption` を ukadoc 正典の **3 値意味論**（`mustselect`＝ちょうど 1 個・解除不可／**非宣言（既定）＝高々 1 個・解除可**／`multiple`＝複数可）へ是正した。**採取層が `multiple` 宣言を捨てていた**（非宣言と明示 multiple を下流で区別できない情報欠落の根）ため、判定は「`mustselect` か、さもなくば加算」の 2 値に縮退しており、非宣言カテゴリで bind 集合が単調に肥大していた。⑴採取層は `+` 区切り複数オプション（正典「オプションは+区切りで複数可」）を含めてオプション語ごとに認識・スコープ別に転記、⑵判定は 3 値 enum＋単一アクセサへ一本化（旧 2 値述語は退役）、⑶適用は「複数可と宣言されていないカテゴリの着衣＝排他置換」へ反転、⑷`mustselect` への脱衣指示は正典「解除不可」どおり集合を変えず読み流し `warn!` で痕跡を残す（**無言の握り潰しを作らない**）。

**⚠️ `completed/areka-P0-mayuna-compose` の R4.5／D11 を覆した**（完了済み spec の判断の訂正）。覆された判断＝**「`multiple`（紅等・非宣言＝既定）はスクリプト明示 on/off で従来どおり成立ゆえ語彙保持のまま」「非宣言は既定＝非排他で無視」**（`completed/areka-P0-mayuna-compose/requirements.md:85` の R4.5、`同/design.md:68` の 3 分類表と `:142` の D11）。覆しの根拠は 2 系統:

- **正典**: ukadoc `descript_shell` の `bindoption` 既定値は「選択解除可能、**複数選択不可**」。非宣言＝非排他（加算）は正典に反する。
- **実機証拠**: 2026-08-11 の emo2 実走直接観測——非宣言のまばたきカテゴリで bind 集合が **{1403}→{1403,1400}→{1403,1400,1402}** と単調肥大し、飽和後にゴーストが送った**是正指示 28 件がすべて「無変化」として握り潰され**、表情が非可逆に固着した。ゴーストは正典作法（on のみ送る）で完全にシロ。

`mayuna-compose` は `mustselect` についても同型の誤仮定（「ゴーストが明示 off を送るはず」）を 2026-07-23 に実機で反証された前例があり、**同じ穴の 2 度目**である。完了済み spec の文書は改変しない規律ゆえ現物は無改変とし、覆しの根拠は本 spec の設計文書（`§mayuna-compose 覆しの記録`）に、2026-08-11 の裁定（`mustselect` 解除不可を本 spec で拾う・警告水準の選定根拠）は同 `D1` に記録した。

**⚠️ 本番挙動の変化（次の読み手が驚く点）**: 既定の意味反転で、実 emo2 で**新たに排他になるのは非宣言かつ複数パーツの 3 カテゴリ——まばたき（1400-1403）・キラリ（1700/1701）・髪飾り（1800/1801）**。とくに**髪飾りは `bindgroup1800.default,1` で起動時オン（リボン）**のため、今後 `\![bind,髪飾り,ボンボン,1]` を送ると**既定オンのリボンが自動で外れる**。これは正典どおりの意図した是正であり、退行ではない。

**先送りの登記（残る正典乖離・完全語彙）**: 正典の `mustselect` は「必ず 1 つ選択」（**ちょうど 1 個**）だが、本 spec が実装したのは **off 無視（解除不可）まで**である。**起動時の充足は shell の `default,1` 宣言へ委譲する（既存縮退の維持）**。したがって **`mustselect` カテゴリに `default` 宣言が 1 つも無い shell では、最初の on 指示が届くまで当該カテゴリは起動時ゼロ個のまま**になる。emo2 は既定集合 `{1100,1207,1302,1500,1800}` が全 `mustselect` カテゴリ（腕 1100・口 1207・目 1302・眉 1500）を被覆するため**実害なし**。**起動時自動充足（先頭パーツの自動選択等）は実装しない**——縮退シーム＝ポリシー判定の一元アクセサ（この 1 点を変えれば起動時充足を後付けできる）。追跡は本追記が担う（M1 では起票しない・実害が観測されたら `/kiro-discovery` で起票）。あわせて **`char*.bindoption*.group`（char2 以降）の走査は既存縮退のまま**（採取層は `sakura.`/`kero.` 接頭辞のみ走査）——`bindoption` 固有の新規乖離ではなく `bindgroup` 系全体が持つ既存の縮退で、本 spec は形を揃えただけである。従来の引受先だった **M-dual は退役済み**（e2e 適合 #10 へ吸収）ゆえ、受け皿としてここに登記する。

**【実機】サインオフ**: 実 emo2（実 pasta.dll・辞書込みフルゴースト・絶対パス起動・有界自動終了）で判定 J1（同一時間帯に複数まばたきパーツが並行発火する痕跡の不在）・J2（片側カテゴリの恒久沈黙＝飽和パターンの不在）・J3（ジト目からの表情復帰の目視）を実施。是正前の保全ログでは J1・J2 とも必ず赤になることを既知ケース較正として確認済み（是正前実測: J1 違反 109/169・J2 は変更回数差 22／末尾時刻差 316.8 秒）。判定手順は `signoff-scan.py`＋`signoff-procedure.md` として再実行可能な形で残した。**（実測値・PASS/FAIL は 7.2 完了後に差し替える）**

**申し送り（別途裁定・任意で載せる）**: bind 経路のコード内に**出所が単一 spec に定まらない裸の設計 ID 引用**（`D1`・`D4`・`D7`・`D8` 等）が残っている。本 spec の設計 ID と番号が正面衝突しているが、実測すると複数 spec 由来が混在しており、**誤った spec 名を冠するのは裸で残すより有害**なため本 spec では無改変とした（本 spec の一掃対象は `D11`／`R4.5`／`Req 4.5` の 3 文字列に明示限定・受入条件は充足）。出所の全数特定と接頭辞付与は別途裁定すること。

---

## 2. 追記台帳は**完了時には触らない**（次回棚卸へ送る 1 行要約の控え）

**`/kiro-complete` は `roadmap.md:136` の「追記台帳（要約・全文は history）」を編集しないこと。** 実運用は「**追記の全文を history へ退避する棚卸のときに、台帳へ 1 行要約を足す**」であり、完了時に足すと §1 の全文と重複する。実測: 現行台帳が保持しているのは **(51)〜(57) ＋ (59)**（(55) は欠番・**(58) は全文が `roadmap.md:144` に現存するため台帳に不在**）で、**(60)(61)(62) も台帳に載っていない**——全文が `roadmap.md:138`／`:140`／`:142` に現存するためである。つまり「**全文が本文にある追記は台帳に載らない**」が実運用則で、(58) はその追加実例にあたる。本 spec の追記(NN) も同じ扱い＝**全文を §1 として本文へ置くだけ**とし、台帳行は追記 **(58)** および **(60)〜(62)** とまとめて次回棚卸で history 退避されるときに足す（(58) を取りこぼさないこと）。

以下は**その次回棚卸のための控え**（本 spec 完了時には roadmap へ入れない）。表記は `roadmap.md:136` の既存形式に揃えてある（番号は半角 `(NN)`・日付は全角括弧の `（MM-DD）`）:

> (NN) `bindoption-exclusivity` 完了＝`bindoption` 3 値正典（mustselect／非宣言=高々 1／multiple）実装・**`mayuna-compose` R4.5/D11 の覆し**（正典＋実機の 2 系統・同じ穴の 2 度目）・非宣言かつ複数パーツの 3 カテゴリ（まばたき/キラリ/髪飾り）が新たに排他化・`mustselect` の起動時充足は shell の `default,1` 委譲のまま先送り登記（MM-DD）

---

## 3. 完了時に併せて更新する既存箇所（`/kiro-complete` 向けの反映先メモ）

| 反映先 | 現在の記述 | 完了時の更新 |
|---|---|---|
| `roadmap.md:66`（M1 残工程ゴール表） | `挙動バグ｜表情固着解消＝bindoption 3値正典…｜bindoption-exclusivity｜**W6**` | 取り消し線＋`completed/bindoption-exclusivity`・`✅ **完了**（YYYY-MM-DD）` へ（W5 完了行 `:63-64` と同じ形） |
| `roadmap.md:87`（ウェーブ編成 W6 行） | `W6（残 4本…）… ∥ bindoption-exclusivity ∥ …` | `bindoption-exclusivity` に取り消し線＋✅・残本数を 3 本へ。編成条件⑵「bind は `BindResolver::empty()` 署名維持」は**維持したまま着地した**旨を記す（`empty()` の署名は不変・呼出元は無傷） |
| `roadmap.md:57`（完了サマリ） | `完了 spec = **151**` | **本 spec 着地時点で 153** へ更新。⑴計数規約は `.kiro/specs/completed/` の**直下エントリ数**（ディレクトリ 151 ＋ 単体ファイル `graphics-rendering-stability.md` 1 ＝ **現在 152**）——**ディレクトリ数 151 を採ると 1 ずれる**。⑵**roadmap:57 の現行値 151 は stale**——`file-slimming` マージ `7df461b`（PR#103）が completed へ 1 本足しながら roadmap:57 の更新を落としたため（実測: `ec9687c` エントリ 150／roadmap 150、`ce86995` 151／151、`7df461b` **152／roadmap 151**）。⑶よって「現行 151 に +1」ではなく**実数 152 に +1 ＝ 153**を書く（完了時に `ls .kiro/specs/completed/ \| wc -l` で再確認すること） |
| `roadmap.md:105`（干渉台帳・因果のみ） | `bind→budget（CPU 上昇の切り分け）` | bind 着地により `recompose-budget`（W6.75）の CPU 単調上昇について (a)bind 同根／(b)活性集合 の切り分けが**可能になった**旨へ更新（W6.75 行 `:90` も同趣旨） |

> 上表は反映先の**所在メモ**であり、追記本文ではない。実際の編集は `/kiro-complete` が行う。
