# Implementation Plan

> 期待値・件数・file:line は **2026-09-18 の実測**（`design.md`・`research.md` §8）。着手時に必ず引き直し、差は `verification/signoff-record.md` に記す。

- [ ] 1. 基準値の採取・資産の保管・出典の記録
- [x] 1.1 着手前の基準値を採る
  - i686 helper を既存手順で既定の出力先に揃えたうえで `cargo test --workspace` を単独で（他の cargo と並走させずに）走らせ、`Running` 行の本数と test result の合計本数を控える
  - 着手前の HEAD を基準コミットとして控え、非回帰の差分がこの基準から採れることを確かめる
  - 台帳の対象項目の現在値（状態・宛先・優先度）と、ロードマップ草案の件数、ブリーフィングの当該ページの分布 2 数値を控える
  - 完了状態: 着手前のテスト本数・基準コミット・台帳側の 4 値が作業メモに揃い、後段の非回帰検査がこの値と突き合わせられる
  - _Requirements: 8.4_

- [x] 1.2 上流を取得し、29 ファイルを無改変で保管する
  - 上流リポジトリを一時領域へ取得し、設計が固定したコミットで固定する
  - 配布物の中身（ファイル一覧・定義ファイルの要点・ライセンスの種別）を要件書の記述と突合し、食い違いがあれば要件書を実測へ是正してから保管する
  - 上流の作業木の 29 ファイルだけを `vendors/sample_ghost/StayseeBalloon/` へ複写する（サブフォルダ・バージョン管理のメタデータ・自前の告知ファイルは置かない。`.nar` は作らない）
  - 完了状態: 保管フォルダ直下に 29 ファイルがちょうど存在し、readme・ライセンス・インストール指示を含め areka が読まないファイルも 1 つも欠けていない
  - _Requirements: 2.1, 2.2, 2.6, 5.4_

- [x] 1.3 保管の機械検査を通し、出典記録を書く
  - 保管後に 29 ファイルのハッシュを採り直し、取得時の一覧と一致することを確かめる
  - 保管フォルダを索引へ追加してから索引側の改行表示を採る（索引に入るまでこの照会は何も返さない）
  - 改行変換が掛かっていないことを属性照会と索引の改行表示の 2 通りで確かめ、ファイル名をリポジトリ直下の除外規則と大小無視で照合して当たる名前が 0 件であることを確かめる
  - 既定バルーンの id とフォルダ名（インストール指示の配置先）が同じ綴りであることを実測する
  - `verification/provenance.md` に、資産名・id・作者・ライセンス・出典 URL・取得コミットと日付・上流の版・ハッシュ一覧・上の 3 検査の結果・食い違い突合の判定を書く
  - 完了状態: この記録を読めば取得元・保管内容・改行と除外の検査結果・既定バルーン id が第三者に再検証できる
  - _Requirements: 2.2, 2.3, 2.4, 2.5, 2.6, 5.1, 7.1_

- [x] 1.4 裁定と選定根拠の記録を起こす
  - `verification/signoff-record.md` を作り、既定バルーンを確定した開発者裁定（日付・結論・根拠 3 点）を書く
  - 候補を選んだ根拠（再配布条件がライセンス本文と readme の両方で確認できる・専用指定が無い・半透明前提が areka の固定の扱いと一致する）を書く
  - 他候補を採らない理由（再配布条件が公開されていない 2 件・既存の検証用バルーンが既定に向かないという裁定）を書く
  - 正典既定の書体のまま受け入れる旨（areka 側の既定書体を変えない・バルーン側に書体宣言を書き足さない）を書く
  - 完了状態: 採否の判断とその理由が、後から読む人に追加の会話なしで伝わる状態で 1 ファイルに揃っている
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ] 2. 決定論テスト——起動時の焼き込みと同じ公開 API 経路で値を固定する
  - 檻 A〜I はすべて 1 本の新規テストファイルに入る。同一ファイルを共有するため 2.1〜2.8 に `(P)` は付けない（順に積む）
- [x] 2.1 テストの土台と、名前集合・定義の読み取りを固定する
  - 新規テストファイルを 1 本だけ作り、検体フォルダのパスを 1 か所の定数だけが持つ形にする（畳み込み側が共有ヘルパへ寄せるとき 1 行の付け替えで済む）
  - 保管フォルダ直下のエントリ名の集合が 29 本の固定集合と一致し、サブフォルダが 0 であることを固定する。枠画像 6 枚の原寸も固定する
  - 定義ファイルを宣言された文字コードで復号し、種別・id・名前・描画範囲・文字の高さと色を宣言どおりに読み取ること、および縦書き・折返し基準・書体名が未指定として扱われることを固定する。配置先の綴りが id と同じであることも固定する
  - 完了状態: 新規テストファイルだけを対象にテストを走らせると檻 A・B が緑になり、既存のテスト・example は 1 行も変わっていない
  - _Requirements: 2.2, 3.1, 3.2, 3.3, 5.4, 7.1_

- [x] 2.2 面の系列解決と記録を固定する
  - 本体側と相方側の 2 つの scope で面の解決を呼び、解決された面の並び（面 id・接頭辞・ファイル名）を固定する
  - 記録の捕捉窓を解決の 2 関数に限り、失敗の記録が両 scope で 0 件、縮退の記録が本体側 0 件・相方側ちょうど 2 件（欄の面 id と接頭辞まで）であることを固定する。捕捉が空のまま緑にならないよう番兵を置く
  - areka が使わない資産（装飾枠・矢印・オンライン印・目印・通信枠・サムネイル・テキスト類）が解決の列挙に載らないことを固定する
  - 完了状態: 檻 C が緑で、縮退の記録件数を 1 つずらすと赤くなる（恒真でないことを確かめた）
  - _Requirements: 3.4, 3.8_

- [x] 2.3 焼き込みと半透明の保持を固定する
  - 画像復号器をテストスレッドで初期化し（多重初期化は許容）、両 scope の面を起動時と同じ固定の透過設定で焼き込む
  - 焼き込みが成功し、面 0 の原寸が保管した画像と一致し、割り当て結果が得られることを固定する
  - 焼き上がりの面 0 の領域内に、完全な不透明でも完全な透明でもない画素が 1 つ以上あることを固定する（半透明が潰れていない）
  - 完了状態: 檻 D が緑で、両 scope の焼き込みに失敗の記録が 0 件
  - _Requirements: 3.4_

- [x] 2.4 文字の領域解決と既定書体の門を固定する
  - 本番の描画入口と同じ関数で文字の領域を解き、描画範囲の 4 辺・描画開始点・折返し基準・遠辺・画像寸法を両 scope で固定する（未宣言の折返し基準が遠辺へ縮退していること）
  - 解決された書体名が正典既定であり、かつ areka 側の既定書体の定数と一致することを固定する。文字の高さ・全角と半角の送り幅・行ボックス・行送りも固定する
  - 期待値は実装時に実測して較正し、動いた値とその理由を 2.8 の記録へ回す
  - 完了状態: 檻 E・F が緑で、既定書体が別の書体へ落ちていたら赤になる
  - _Requirements: 1.3, 3.3, 3.5_

- [x] 2.5 折返しと描画範囲への内包を固定する
  - 全角のみ・半角のみ・全角半角混在の 3 本文を流し、各行の文字数・行の上端・行数を固定する
  - すべてのグリフ矩形が描画範囲の内側に収まり、1 画素も超えないことを固定する。混在では貪欲充填の性質（次の 1 文字を足すと遠辺を超える）まで固定する
  - 相方側の低い枠で最終行の下端が境界ちょうどのときにあふれ扱いにならないことを固定する
  - 完了状態: 檻 G が緑で、描画範囲を 1 画素狭めると赤になる
  - _Requirements: 3.5_

- [x] 2.6 選択肢と遅延座標指定の位置を固定する
  - 遅延座標指定と選択肢 2 つを含む台本を解析・翻訳し、既存の選択肢テストと同じ経路で状態へ適用してから配置する
  - 遅延座標指定の直後のグリフ左上が、描画開始点からの相対位置として指定どおりに置かれることを固定する
  - 選択肢 2 行のグリフ矩形と強調帯がすべて描画範囲の内側に収まること、および定義の指示から選択肢の見た目が実際に導かれていることを固定する
  - 完了状態: 檻 H が緑で、遅延座標指定の値を変えるとグリフ位置が追随して赤になる
  - _Requirements: 3.6_

- [x] 2.7 表示スケールの扱いを固定する
  - 表示スケール 3 通りで枠の配置を作り、画像寸法がスケールに依らず原寸のままであること、保持されるスケール値が渡した値であることを固定する
  - 物理寸法への変換が遠辺・左辺のそれぞれでスケール倍になることを固定する
  - 文字の領域解決の入力が物理寸法ではなく画像寸法であること（＝領域がスケールに依らず檻 E と同値）を固定する。配置計算自体はスケールを引数に取らないので「スケールごとに解き直して同一」は恒真であり、檻に置かない
  - 完了状態: 檻 I が緑で、領域解決へ物理寸法を渡す実装へ変わったら赤になる
  - _Requirements: 3.7_

- [x] 2.8 較正差を記録し、番人と既存期待値の非後退を確かめる
  - 檻 F・G の導出値（送り幅・行ボックス・行送り・1 行の文字数）を実測で較正し、動いた値と理由（書体の版など）を 1.4 で起こした記録のテスト節へ書く。既定書体から落ちていないことは檻 F の名前検査で保証する
  - 領域座標・系列・名前集合・半透明のいずれかが食い違った場合は期待値を緩めず、保管フォルダか areka 側の欠陥として扱う。areka 側の崩れなら本仕様内で直す（その場合は本番コード変更 0 行の例外として何を・なぜ変えたかを記録し、同じ崩れを再現する檻を足す）か、引受先の spec が実在し完了済みでないことを確かめてから先送りし、どちらにしたかを記録する
  - 新規テストファイルが 1,000 行以下であることを番人で確かめ、番人の例外表には触れない。超える見込みなら規約どおりテーマ別ファイルへ分割し、検体パス定数は親ファイルに残す
  - 既存の検体で固定している期待値を 1 つも緩めていないこと（既存テストが赤なら期待値を書き換えず原因を直したこと）を確かめる
  - 完了状態: 新規テスト全体と 1,000 行の番人が緑で、較正で動いた値とその理由が記録に残っている
  - _Requirements: 3.9, 3.10, 3.11_

- [ ] 3. 透過の扱いの裁量を登記する
- [x] 3.1 (P) 正典沈黙箇所の対応表に 1 行足す
  - 沈黙ルール対応表の**末尾**に 1 行を足す（既存行の間に挟まない。他 spec が別節を触るため）
  - 4 列に、項目（バルーンの透過 3 鍵と透過マスク）・裁量（宣言を読まず常に半透明尊重で焼く・透過マスクは読まない）・根拠（開発者裁定の日付・固定値を渡している実在の 2 か所・既知の制限）・出典 spec を書く。根拠に書く関数名は実在を確かめてから書く
  - 完了状態: 表の末尾に本仕様の 1 行が在り、その節以外の差分が 0
  - _Requirements: 6.1_
  - _Boundary: C4 沈黙ルール対応表_

- [ ] 3.2 (P) 網羅台帳を登記し、報告を作り直して番人を通す
  - 台帳の対象 1 項目を「動くが正典どおりではない」状態へ改め、宛先に本仕様を書き、備考に「どう違うか・記録は出ない・裁量は対応表に登記」を書く。優先度・値・リンク・導入は変えない。隣の 2 項目は一切変えない
  - 順序を守る: 台帳 → ロードマップ草案（本仕様の行を末尾へ・件数を 1 増やす・表の直前の散文に 1 文・束表の依存 spec 欄に本仕様を足す） → ブリーフィングの当該ページの分布 2 数値（縮退 +1・未対応 −1。他の数は触らない） → 報告 2 本の再生成（道具で作り直す・手で編集しない） → 番人
  - 常設の検査（宛先・件数・数え直し・束の実在・分布の突合）が緑であることを確かめ、結果を 1.4 で起こした記録の台帳節へ書く
  - 完了状態: 台帳の検査一式が緑で、分布の突合が台帳の数え直しと一致している
  - _Requirements: 6.2, 6.3, 6.4, 6.5_
  - _Boundary: C5 台帳文書_

- [ ] 4. 実機で見え方を目視する
  - 決定論テストが緑であること、および i686 helper が既定の出力先に在ることを確かめてから、既存の起動引数の経路で本番ゴーストと保管フォルダを**絶対パス**で指して起動する（コード変更 0）
  - 表示スケール既定と既定以外の 2 通りで、半角・全角・選択肢（ダブルクリックメニュー）・枠の透け・文字の欠けを 1 度ずつ見る
  - 日付・ゴースト・バルーンの絶対パス・表示スケールの値・観察項目・所見を記録の目視節へ書く（GPU 合成窓のため読み戻し画像が無ければ観察記録を証跡とする）
  - 崩れが見つかった場合は 2.8 と同じ処理（本仕様内で直す／引受先を実在確認して先送り）に回し、直したなら同じ崩れを再現する檻を足す
  - 完了状態: 2 通りのスケールの観察記録が記録に残り、崩れの有無とその行き先が明記されている
  - _Depends: 2.8_
  - _Requirements: 4.1, 4.2, 4.3_

- [ ] 5. 記録の完成と下流への申し送り
- [ ] 5.1 記録を完成させる
  - 決定論テストの結果と着手前後のワークスペース本数、既知の縮退の事実（枠の原点色の白縮退・折返し基準未宣言の記録・相方側の縮退 2 件）、崩れの処理の発動有無と行き先を書く
  - 第三者向け README にそのまま写せる形の出典文（資産名・作者・ライセンス・出典 URL・既知の制限「半透明前提のバルーンだけが正しく表示される」）を 1 節にまとめる
  - 完了状態: 記録の全節が埋まり、README 用の文が中身をそのまま持ち出せる状態になっている
  - _Depends: 2.8, 3.2, 4_
  - _Requirements: 3.9, 4.2, 4.3, 5.2, 6.5_

- [ ] 5.2 下流 3 spec と本仕様の brief へ申し送る
  - 追記先 3 件が仕様ディレクトリ直下に brief として実在することを先に確かめる。既に完了済みへ移っていればその spec には追記せず、正本ロードマップの当該行へ書く
  - 畳み込み側へ「保管済みのフォルダと 29 ファイル・ハッシュの在り処・畳む対象と共有ヘルパの検体名・付け替える定数 1 行の場所」を 1 段
  - 根の解決順側へ「既定バルーンの id（定義の id ＝配置先の綴り）・定数と配線はそちらが足す」を 1 段
  - 配布側へ「zip に入れる保管フォルダ・README の出典文は本仕様の記録の該当節をそのまま写す」を 1 段。本仕様の brief にも「既定バルーン id」を 1 行
  - 完了状態: 4 つの brief（または不在時のロードマップ行）に追記が入り、追記の実施記録が記録に残っている
  - _Depends: 5.1_
  - _Requirements: 5.2, 7.1, 7.3, 7.4_

- [ ] 6. 触っていないことを機械で証明する
  - 基準コミットからの差分で、本番コード（各 crate の src 配下の非テストファイル）・すべての依存記述とロックファイル・自動生成の第三者告知・開発者向け README・既存の検体参照ファイル群・既存検体 3 つが **0 行**であることを確かめる（本番コードに正典 URL のコメントも既定バルーン id の定数も足していないことを同時に示す）
  - 台帳の隣 2 項目の行が差分に現れないことを確かめる
  - 新規テストのディレクトリの差分が新規追加ファイルのみ（2.8 でテーマ分割した場合はその一式）で、既存ファイルの変更が 0 行であること、および並走 2 spec の接触面に触れていないことを確かめる
  - 作業木の変更ファイル全行が設計のファイル一覧に含まれることを確かめる。2.6 が発動して要件書を是正した場合は、その 1 ファイルを一覧外の例外として記録に明記する
  - i686 helper が既定の出力先に在ることを確かめてから（ワークスペースのビルドが x64 で上書きするので毎回置き直す）`cargo test --workspace` を単独で走らせ、1.1 の本数＋新規檻の分で緑になること、整形検査と 1,000 行の番人が緑であることを確かめ、本数を記録へ書く
  - 完了状態: 上の差分検査がすべて 0 で、ワークスペース全体が緑。結果が記録に本数付きで残っている
  - _Depends: 5.2_
  - _Requirements: 2.7, 3.1, 3.10, 5.3, 6.3, 6.6, 7.2, 8.1, 8.2, 8.3, 8.4, 8.5_

## Implementation Notes

- 1.2: この機械は `core.autocrlf=true`（system 設定）。素の `git clone` で上流を取り直すと `LICENSE`（上流 blob は LF のみ）が CRLF へ壊れ、保管先とハッシュが食い違う。採り直しは必ず `git -c core.autocrlf=false -c core.eol=lf clone` で行うこと。`descript.txt`／`install.txt`／`readme.txt` は blob 自体が CRLF なのでどちらでも同値。
- 1.2: `research.md` §8.2 の「`LICENSE` は…（CRLF）」は実測と逆（実測は CR 0・LF 121）。`provenance.md` はこの誤記を引き写さないこと（実装者・レビュアーの双方が独立に実測）。
- preflight: この worktree は `vendors/pasta` サブモジュールが未初期化だった。`cargo` は `[patch.crates-io]` の `pasta_core` を解決できず即座に落ちる。`git submodule update --init --recursive` が要る。
- 1.1: `cargo test --workspace` は i686 の成果物を **2 つ**要求する。helper（`target/debug/shiori-host32-helper.exe`）だけでなく **testdll**（`cargo build -p shiori-host32-testdll --target i686-pc-windows-msvc` → `target/i686-pc-windows-msvc/debug/shiori.dll`）も要る。testdll が無いと `shiori-host32-host::lifecycle_cyclic_e2e::cyclic_run_and_clean_shutdown` が赤になり、そこで走行が打ち切られて 81→60 ターゲット・7,668→5,411 本しか採れない（＝部分的な緑を全体と誤認する罠）。
- 1.1: 基準値は `--no-fail-fast` 付きで採った。タスク 6 も旗をそろえること（全緑のときだけ素の `cargo test --workspace` と同本数になる）。基準: passed 7,668 / failed 0 / ignored 40・`Running` 81 ＋ `Doc-tests` 22 ＝ `test result:` 103 行。基準コミット `082379b3`。
- 1.3: Git Bash の `grep` は入力の CR を自分で落とす。**改行の判定に `grep` を使わないこと**（LF のみの `LICENSE` に対し `grep -c $'\r$'` が 121 という嘘を返した）。`od -An -tu1`／`tr -cd '\r'` で数えること。
- 1.3: `git check-ignore` は `-v` を付けると打ち消し規則（`vendors/sample_ghost/.gitignore` の `!*_test.txt`）への一致も報告して exit 0 を返す。除外の判定に使うのは `-v` なしの形。
- 1.3: 設計 C1 の 2 か所が実測と合わない（記録は実測側を採用済み）。⑴「`git ls-files --eol` で `i/-text` を確認」→ 実測は `i/-text` 25・`i/crlf` 3・`i/lf` 1（`i/` 欄は属性でなく索引に入ったバイト列の改行を映す）。⑵「取得時の一覧（`research.md` §8.2 で採取済み）」→ §8.2 にハッシュ一覧は**無い**（64 桁 16 進 0 件）。基準は上流 blob に置いた。
- 1.4: `verification/signoff-record.md` は §1 だけが埋まっている。残りの担当は §2・§3・§4＝2.8、§4 の目視分と §5＝4、§6＝5.1、§7＝3.2、§8＝5.2。完了ゲートで §2〜§8 の充足を必ず確認すること。プレースホルダには TODO / TBD / FIXME の語を使わないこと（レビューが欠陥として検出する）。
- 2.1: 「未宣言であること」（`None`）の assert は、読み口が常に `None` を返す実装なら恒真になる。較正は**期待値をずらす**だけでは足りず、**検体パス定数を `font.name`／`vertical`／`wordwrappoint` を実際に宣言している別の検体**（`crates/areka-emo-text/examples/fixtures/emo2-vertical-canon` など）へ向け直して `Some` が返ることを見せること。レビュアーが実測で裏取り済み。
- 2.1: 設計 C2 の B 行は `validrect` を**宣言された生値**（負値のまま）で固定する担当。原寸を掛けて解決した座標（scope 0 の right 309・bottom 158 等）は E 行＝タスク 2.4 の担当。重複させないこと。
- 2.1: 較正で動いた期待値は 0 件（設計 Data Models と A/B 行の値がすべて実測と一致）。`signoff-record.md` §2 の「較正で動いた値」欄は檻 A・B については「差なし」と書けばよい。
- 2.2: ログの「0 件であること」は**捕捉窓が空でも緑になる恒真**。レビュアーが実測で確認済み（対照 assert を外して窓を空にすると `error!` 0・`warn!` 0 が緑で通った）。`log_capture_kit::capture` 自身の番兵は「捕捉先が生きている」ことしか示さず、「被験コードが窓の中で走った」ことは示さない。**同じ窓で必ず出る記録（対照）を先に固定すること。**
- 2.2: `prefix = %face.prefix` は `Display` 経由で載るので `field_str` は `None` を返す。判定には `field`（Debug 表現）を使うこと。
- 2.2: 要件 3.8 が名指しする非面資産は 6 種＝19 本。設計 C2 の檻 C 行はこれに `LICENSE`・`*.txt` 4 本を加えた 23 本（＝29−6）を除外集合としている。19 と 23 は数え落としではない。
- 2.2 → 2.3 への申し送り: 本ファイルは 692 行で残余 308 行。檻 D〜I（5 タスク分）は入りきらない見込み。**タスク 2.8 の分割規定を前倒しで発動する**。規約は `.kiro/steering/structure.md`「Test Naming Conventions」＝ `<stem>_<テーマ名>.rs` ＋ 親ファイル内の `#[cfg(test)] #[path = "…"] mod <テーマ名>;`（`tests/` 配下でも `#[cfg(test)]` は冗長でも付ける）。テーマ間で共有するヘルパは `<stem>_test_support.rs` へ**集約**する（1 件でも複製しない）。**検体パス定数 `STAYSEE_BALLOON_DIR` は親ファイルに残す**（設計 C2・要件 3.2）。
- 2.3: **テーマ分割を前倒しで実施**（タスク 2.8 の規定）。入口は `tests/staysee_balloon_fixture_test.rs`（83 行・検体パス定数と `#[path]` の mod 宣言だけ）、実体は `tests/staysee_balloon_fixture/` 配下。⚠ **`tests/` 直下の平置き（`<stem>_<テーマ>.rs`）は成立しない**——親から `#[path]` で繋いでも cargo が独立テストターゲットとしても自動収集し、同じテストが 2 か所で走る（実装者・レビュアーが独立に再現）。`Cargo.toml` の `autotests` で止める手は要件 8.2 が封じている。design.md C2 と Directory Structure は 2026-09-18 に実測へ追随済み。
- 2.3 → 2.4〜2.7 への申し送り: 追加先は `tests/staysee_balloon_fixture/` に新規ファイルを作り、**親ファイルに `#[cfg(test)] #[path = "staysee_balloon_fixture/<名>.rs"] mod <名>;` を 1 行足す**。想定: 2.4 → `region.rs`（檻 E・F）、2.5 → `wrapping.rs`（檻 G）、2.6 → `script.rs`（檻 H）、2.7 → `scale.rs`（檻 I）。2 つ目のテーマが要り始めたヘルパは `test_support.rs` へ集約する（複製しない）。現在の共有項目: `staysee_root` / `expected_frame_size` / `resolve_faces` / `EXPECTED_FACE_COUNT` / `read_decoded` / `staysee_model`。
- 2.3: 焼き上がりの走査矩形はトリム後 333×203（原寸 335×205 から完全透明の縁が 1 画素ずつ落ちる）。`research.md` §8.2 の α 別画素数は**原画像**の値なので、透明の数だけ 9,617 → 8,541 に減る（差 1,076 ＝ 335×205 − 333×203 ＝ 縁 1 周 2·335+2·203）。半透明 2,445・不透明 56,613 はトリムで動かない。
- 2.4: **較正で動いた期待値は 0 件**。設計 E 行の領域座標（left 22・top 20・right 309・bottom 158／88・start (22,20)・折返し基準と遠辺 309・`image_size`）と F 行の導出値（半角送り 6.0・全角 12.0・行ボックス 12.0・行送り 14.0）がすべて実測と一致した。2.8 の記録には「檻 A〜F は較正差なし」と書けばよい。
- 2.4: `font.name == DEFAULT_FONT_NAME` の比較は**この検体に対しては構造上恒真**（`draw.rs` の `ResolvedFont::resolve` の未宣言腕が定数そのものを返す）。恒真性を破っているのは⑴逐語リテラル `"ＭＳ ゴシック"` との比較と⑵文字寸法の門の 2 つ。設計 F 行が両方の比較を明記しているので恒真な側も残す。
- 2.4: 恒真化を防ぐ**常設の対照**を `region.rs` に置いた（`declared_balloon_returns_a_different_font_and_a_split_wrap_threshold`）。対照検体は `crates/areka-emo-text/examples/fixtures/emo2-vertical-canon`（読むだけ）で、`font.name,Yu Gothic UI`・折返し基準 145 と遠辺 149 が分かれることを固定する。対照検体のパス定数は `DECLARED_BALLOON_DIR`＝検体の `STAYSEE_BALLOON_DIR` とは別名で区別。
- 2.4: `ＭＳ ゴシック` 不在の門は**名前の assert ではなく文字寸法の assert** が担う（名前は解決文字列を見るだけなので不在を捕まえない・設計 DD4 ⑶ どおり）。
- 2.5: **較正で動いた期待値は 0 件**（檻 G も設計 G 行・Domain Model の導出どおり）。混在の行の文字数 [31,31,28]（半角 45・全角 45）は設計が数値で書いていなかったので実測で固定した。全角 [23,23,14]・半角 [47,47,26]・行の上端 20／34／48。
- 2.5: **描画範囲の右辺には原理的にグリフが接しない**（幅 287 は送り幅 6 でも 12 でも割り切れず、詰められるのは半角 282〔余り 5〕・全角 276〔余り 11〕）。よって右辺側は「グリフ内包」ではなく⑴描画範囲そのものの逐語固定と⑵貪欲充填の検査（早すぎる折返しを捕まえる）が守る。完了状態「1 画素狭めると赤」は 4 辺すべてで成立することをレビュアーが独立の較正 6 本で確認済み。
- 2.5: 共有項目を `region.rs` → `test_support.rs` へ集約（65 行の移動）。**値も assert も 1 つも変えていない**ことをレビュアーが機械で検算済み（`assert` を含む削除行 0 本・`region.rs` の assert 総数 30 → 30）。
- 2.6: **較正で動いた期待値は 0 件**（檻 H も設計 H 行どおり）。実測: `\_l[60,42]` 直後のグリフ左上 (82,62)・行の上端 62／76／90・強調帯 丈 12.0 寄せ 0.0・選択肢の帯の矩形 「はい」(22,76)-(46,88)／「いいえ」(22,90)-(58,102)・`choice_style` = SquareFill { fill: (192,214,242), text: (0,20,50) }。
- 2.6: ⚠ **設計 H 行の「`cursor.style,square` → SquareFill 実導出」はそれ自体が恒真**——`choice.rs` の `match cursor.style()` は `_ => square_fill_from(cursor)` で、square 明示も未宣言も同じ SquareFill を返す。恒真性を破っているのは恒久の対照 2 本（`cursor.style,none` → NoMarker ／ `cursor.brush.color.r` 192→7 で fill が追随）。対照は**読み込んだ文字列の写しの上で差し替える**形（検体は 1 バイトも変えない）。
- 2.6: 遅延座標指定の対は **2 本**置く（`\_l[60,42]`→(82,62) と `\_l[100,14]`→(122,34)）。1 本だけなら位置を決め打ちした実装でも緑になる。
- 2.6: 報告に貼る RED 出力は**最終形のコードで採り直すこと**。下書き段階の出力を貼ると、実物と食い違ってレビュアーの検算を余計に増やす（今回、帯の失敗文言が「A ではなく A」に見える下書き出力が貼られていた。現行コードは正しく分離する）。
- 2.7: **較正で動いた期待値は 0 件**（檻 I も設計 I 行どおり。`ceil(287×1.25)=359`・`ceil(287×2)=574`・`22×1.25=27.5`）。**檻 A〜I は本タスクで全 9 種そろった**（テスト 30 本。A=assets 2／B=definition 4／C=faces 1／D=bake 1／E・F=region 5／G=wrapping 6／H=script 6／I=scale 5）。2.8 へ持ち越す被覆の抜けは無い。
- 2.7: ⚠ **k=1.0 では物理寸法と画像寸法が一致する**ので、k=1.0 だけの観測では「領域解決へ物理寸法を渡す取り違え」を検出できない（実測: 入力を供給面の寸へ差し替えると k=1.0 は黙って通り k=1.25 で初めて赤）。k≠1 を必ず含めること。恒久の対照 `resolving_from_the_physical_surface_moves_the_far_edges` がこの性質自体をテストとして残している。
- 2.7: ⚠ **期待値を「入力と同じ定数」へ寄せると恒真化する。** k=1.0 の行の `287`／`22.0`／`158.0` は**独立した外部オラクル**として直書きのまま残すこと（一度 `EXPECTED_INLINE_EXTENT as u32` へ寄せかけたが、`EXPECTED_RIGHT` を 309→310 へ動かす較正で k=1.0 行が黙って緑になると判明し差し戻した）。「複製を避ける」規律と「恒真を避ける」規律がぶつかる場面では後者が優先。
- 2.8: ⚠⚠ **`git diff -- 'crates/*/src'` 形のパス指定は判定になっていない**——変更が実在しても**無出力・exit 0**（実測で確認）。真因は「既定のパス指定は**道の全体に一致**する必要があり、`crates/*/src` は末尾が `src` で終わる道にしか当たらない」こと。`*` が `/` を跨がないからではない（`-- 'crates/*region.rs'` は 3 段下のファイルに当たる）。正しい形は `git diff --numstat <base> | grep '/src/'` か `-- ':(glob)crates/*/src/**'`。**design.md C8 と Requirements Traceability の 8.1 行、`baseline.md` §1 を是正済み。タスク 6 は是正後の形で採り直すこと。**
- 2.8: ワークスペース本数 **着手前 7,668 → 着手後 7,698**（＋30）。`Running` 81→82（増えたのは `staysee_balloon_fixture_test` の 1 本だけ＝テーマ別サブディレクトリは二重にターゲットを作っていない）・`test result:` 103→104・failed 0・ignored 40。命令は `cargo test --workspace -j 4 --no-fail-fast`。
- 2.8: 決定論テストは新規 10 ファイル・合計 2,862 行（最長 `wrapping.rs` 488 行）。1,000 行の番人の例外表は無変更。番人の較正（`wrapping.rs` を 1,008 行にすると 6 本中 3 本が赤で当該ファイルを名指し）を実装者・レビュアーの双方が再現済み。
- 2.8: 実行形式の対象機械の欄は **PE 見出しの位置が実行形式ごとに違う**（helper は 240・`areka.exe` は 232）。`0x3C` から位置を読み → 署名 `P E \0 \0` を確かめ → ＋4 バイト目を読む、の 3 手順を毎回踏むこと。位置を流用すると別のバイト（`areka.exe` の 244 は `00 00`）を読んで「確かめたつもり」になる。
- 2.8 → 最終検証への申し送り: `signoff-record.md` §2.2 の突合（設計 Data Models の表と C2 の A〜I 行の述語欄の数値 ↔ テストの期待値定数）は**手作業の掃き出し**で、常設の検査が無い。読む範囲は記録に逐語で書いてあるので、`kiro-validate-impl` で全数を機械で数え直すこと。
- 3.1: ⚠ **要件 6.1／設計 C4 の「`.pna` は読まない」は実測と違う。** 実測では**同名 `.pna` の存在だけは見ている**（`areka-emo-atlas` の bake 本体が `probe_pna` を呼ぶ）。画素は使われない——α を持つ PNG では α が勝ち、**α の無い PNG に `.pna` を添えた組合せは理由を載せた失敗になる**（`normalize.rs` の実装腕は `UseSelfAlpha::On` × α チャンネルの 1 本だけ）。§8 に足した行は実測どおりに書き、要件 6.1 にも是正の註を入れた。
- 3.1: ⚠ **設計 C4 が根拠として挙げた `crates/areka/src/emo2_boot/assets.rs` はシェルの焼き付け経路であって「バルーンの 2 か所目」ではない**（バルーン側は同ファイルが `build_balloon_target_from_faces` へ委譲）。§8 の行では「シェル側の同じ固定」と明示して誤読を防いだ。なお同じ縮退のシームでもシェル側は記録を出して継続し、失敗を返すのはバルーン経路だけ。
- 3.1: §8 の行は 1,005 文字。表 116 行の中央値 651・平均 778・最大 2,798 で、隣のバルーン系の既存行も 1,206／1,256 文字あるので分布の内側。
