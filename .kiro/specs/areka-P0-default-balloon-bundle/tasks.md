# Implementation Plan

> 期待値・件数・file:line は **2026-09-18 の実測**（`design.md`・`research.md` §8）。着手時に必ず引き直し、差は `verification/signoff-record.md` に記す。

- [ ] 1. 基準値の採取・資産の保管・出典の記録
- [x] 1.1 着手前の基準値を採る
  - i686 helper を既存手順で既定の出力先に揃えたうえで `cargo test --workspace` を単独で（他の cargo と並走させずに）走らせ、`Running` 行の本数と test result の合計本数を控える
  - 着手前の HEAD を基準コミットとして控え、非回帰の差分がこの基準から採れることを確かめる
  - 台帳の対象項目の現在値（状態・宛先・優先度）と、ロードマップ草案の件数、ブリーフィングの当該ページの分布 2 数値を控える
  - 完了状態: 着手前のテスト本数・基準コミット・台帳側の 4 値が作業メモに揃い、後段の非回帰検査がこの値と突き合わせられる
  - _Requirements: 8.4_

- [x] 1.2 上流を取得し、29 ファイルを無改変で保管する
  - 上流リポジトリを一時領域へ取得し、設計が固定したコミットで固定する
  - 配布物の中身（ファイル一覧・定義ファイルの要点・ライセンスの種別）を要件書の記述と突合し、食い違いがあれば要件書を実測へ是正してから保管する
  - 上流の作業木の 29 ファイルだけを `vendors/sample_ghost/StayseeBalloon/` へ複写する（サブフォルダ・バージョン管理のメタデータ・自前の告知ファイルは置かない。`.nar` は作らない）
  - 完了状態: 保管フォルダ直下に 29 ファイルがちょうど存在し、readme・ライセンス・インストール指示を含め areka が読まないファイルも 1 つも欠けていない
  - _Requirements: 2.1, 2.2, 2.6, 5.4_

- [ ] 1.3 保管の機械検査を通し、出典記録を書く
  - 保管後に 29 ファイルのハッシュを採り直し、取得時の一覧と一致することを確かめる
  - 保管フォルダを索引へ追加してから索引側の改行表示を採る（索引に入るまでこの照会は何も返さない）
  - 改行変換が掛かっていないことを属性照会と索引の改行表示の 2 通りで確かめ、ファイル名をリポジトリ直下の除外規則と大小無視で照合して当たる名前が 0 件であることを確かめる
  - 既定バルーンの id とフォルダ名（インストール指示の配置先）が同じ綴りであることを実測する
  - `verification/provenance.md` に、資産名・id・作者・ライセンス・出典 URL・取得コミットと日付・上流の版・ハッシュ一覧・上の 3 検査の結果・食い違い突合の判定を書く
  - 完了状態: この記録を読めば取得元・保管内容・改行と除外の検査結果・既定バルーン id が第三者に再検証できる
  - _Requirements: 2.2, 2.3, 2.4, 2.5, 2.6, 5.1, 7.1_

- [ ] 1.4 裁定と選定根拠の記録を起こす
  - `verification/signoff-record.md` を作り、既定バルーンを確定した開発者裁定（日付・結論・根拠 3 点）を書く
  - 候補を選んだ根拠（再配布条件がライセンス本文と readme の両方で確認できる・専用指定が無い・半透明前提が areka の固定の扱いと一致する）を書く
  - 他候補を採らない理由（再配布条件が公開されていない 2 件・既存の検証用バルーンが既定に向かないという裁定）を書く
  - 正典既定の書体のまま受け入れる旨（areka 側の既定書体を変えない・バルーン側に書体宣言を書き足さない）を書く
  - 完了状態: 採否の判断とその理由が、後から読む人に追加の会話なしで伝わる状態で 1 ファイルに揃っている
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ] 2. 決定論テスト——起動時の焼き込みと同じ公開 API 経路で値を固定する
  - 檻 A〜I はすべて 1 本の新規テストファイルに入る。同一ファイルを共有するため 2.1〜2.8 に `(P)` は付けない（順に積む）
- [ ] 2.1 テストの土台と、名前集合・定義の読み取りを固定する
  - 新規テストファイルを 1 本だけ作り、検体フォルダのパスを 1 か所の定数だけが持つ形にする（畳み込み側が共有ヘルパへ寄せるとき 1 行の付け替えで済む）
  - 保管フォルダ直下のエントリ名の集合が 29 本の固定集合と一致し、サブフォルダが 0 であることを固定する。枠画像 6 枚の原寸も固定する
  - 定義ファイルを宣言された文字コードで復号し、種別・id・名前・描画範囲・文字の高さと色を宣言どおりに読み取ること、および縦書き・折返し基準・書体名が未指定として扱われることを固定する。配置先の綴りが id と同じであることも固定する
  - 完了状態: 新規テストファイルだけを対象にテストを走らせると檻 A・B が緑になり、既存のテスト・example は 1 行も変わっていない
  - _Requirements: 2.2, 3.1, 3.2, 3.3, 5.4, 7.1_

- [ ] 2.2 面の系列解決と記録を固定する
  - 本体側と相方側の 2 つの scope で面の解決を呼び、解決された面の並び（面 id・接頭辞・ファイル名）を固定する
  - 記録の捕捉窓を解決の 2 関数に限り、失敗の記録が両 scope で 0 件、縮退の記録が本体側 0 件・相方側ちょうど 2 件（欄の面 id と接頭辞まで）であることを固定する。捕捉が空のまま緑にならないよう番兵を置く
  - areka が使わない資産（装飾枠・矢印・オンライン印・目印・通信枠・サムネイル・テキスト類）が解決の列挙に載らないことを固定する
  - 完了状態: 檻 C が緑で、縮退の記録件数を 1 つずらすと赤くなる（恒真でないことを確かめた）
  - _Requirements: 3.4, 3.8_

- [ ] 2.3 焼き込みと半透明の保持を固定する
  - 画像復号器をテストスレッドで初期化し（多重初期化は許容）、両 scope の面を起動時と同じ固定の透過設定で焼き込む
  - 焼き込みが成功し、面 0 の原寸が保管した画像と一致し、割り当て結果が得られることを固定する
  - 焼き上がりの面 0 の領域内に、完全な不透明でも完全な透明でもない画素が 1 つ以上あることを固定する（半透明が潰れていない）
  - 完了状態: 檻 D が緑で、両 scope の焼き込みに失敗の記録が 0 件
  - _Requirements: 3.4_

- [ ] 2.4 文字の領域解決と既定書体の門を固定する
  - 本番の描画入口と同じ関数で文字の領域を解き、描画範囲の 4 辺・描画開始点・折返し基準・遠辺・画像寸法を両 scope で固定する（未宣言の折返し基準が遠辺へ縮退していること）
  - 解決された書体名が正典既定であり、かつ areka 側の既定書体の定数と一致することを固定する。文字の高さ・全角と半角の送り幅・行ボックス・行送りも固定する
  - 期待値は実装時に実測して較正し、動いた値とその理由を 2.8 の記録へ回す
  - 完了状態: 檻 E・F が緑で、既定書体が別の書体へ落ちていたら赤になる
  - _Requirements: 1.3, 3.3, 3.5_

- [ ] 2.5 折返しと描画範囲への内包を固定する
  - 全角のみ・半角のみ・全角半角混在の 3 本文を流し、各行の文字数・行の上端・行数を固定する
  - すべてのグリフ矩形が描画範囲の内側に収まり、1 画素も超えないことを固定する。混在では貪欲充填の性質（次の 1 文字を足すと遠辺を超える）まで固定する
  - 相方側の低い枠で最終行の下端が境界ちょうどのときにあふれ扱いにならないことを固定する
  - 完了状態: 檻 G が緑で、描画範囲を 1 画素狭めると赤になる
  - _Requirements: 3.5_

- [ ] 2.6 選択肢と遅延座標指定の位置を固定する
  - 遅延座標指定と選択肢 2 つを含む台本を解析・翻訳し、既存の選択肢テストと同じ経路で状態へ適用してから配置する
  - 遅延座標指定の直後のグリフ左上が、描画開始点からの相対位置として指定どおりに置かれることを固定する
  - 選択肢 2 行のグリフ矩形と強調帯がすべて描画範囲の内側に収まること、および定義の指示から選択肢の見た目が実際に導かれていることを固定する
  - 完了状態: 檻 H が緑で、遅延座標指定の値を変えるとグリフ位置が追随して赤になる
  - _Requirements: 3.6_

- [ ] 2.7 表示スケールの扱いを固定する
  - 表示スケール 3 通りで枠の配置を作り、画像寸法がスケールに依らず原寸のままであること、保持されるスケール値が渡した値であることを固定する
  - 物理寸法への変換が遠辺・左辺のそれぞれでスケール倍になることを固定する
  - 文字の領域解決の入力が物理寸法ではなく画像寸法であること（＝領域がスケールに依らず檻 E と同値）を固定する。配置計算自体はスケールを引数に取らないので「スケールごとに解き直して同一」は恒真であり、檻に置かない
  - 完了状態: 檻 I が緑で、領域解決へ物理寸法を渡す実装へ変わったら赤になる
  - _Requirements: 3.7_

- [ ] 2.8 較正差を記録し、番人と既存期待値の非後退を確かめる
  - 檻 F・G の導出値（送り幅・行ボックス・行送り・1 行の文字数）を実測で較正し、動いた値と理由（書体の版など）を 1.4 で起こした記録のテスト節へ書く。既定書体から落ちていないことは檻 F の名前検査で保証する
  - 領域座標・系列・名前集合・半透明のいずれかが食い違った場合は期待値を緩めず、保管フォルダか areka 側の欠陥として扱う。areka 側の崩れなら本仕様内で直す（その場合は本番コード変更 0 行の例外として何を・なぜ変えたかを記録し、同じ崩れを再現する檻を足す）か、引受先の spec が実在し完了済みでないことを確かめてから先送りし、どちらにしたかを記録する
  - 新規テストファイルが 1,000 行以下であることを番人で確かめ、番人の例外表には触れない。超える見込みなら規約どおりテーマ別ファイルへ分割し、検体パス定数は親ファイルに残す
  - 既存の検体で固定している期待値を 1 つも緩めていないこと（既存テストが赤なら期待値を書き換えず原因を直したこと）を確かめる
  - 完了状態: 新規テスト全体と 1,000 行の番人が緑で、較正で動いた値とその理由が記録に残っている
  - _Requirements: 3.9, 3.10, 3.11_

- [ ] 3. 透過の扱いの裁量を登記する
- [ ] 3.1 (P) 正典沈黙箇所の対応表に 1 行足す
  - 沈黙ルール対応表の**末尾**に 1 行を足す（既存行の間に挟まない。他 spec が別節を触るため）
  - 4 列に、項目（バルーンの透過 3 鍵と透過マスク）・裁量（宣言を読まず常に半透明尊重で焼く・透過マスクは読まない）・根拠（開発者裁定の日付・固定値を渡している実在の 2 か所・既知の制限）・出典 spec を書く。根拠に書く関数名は実在を確かめてから書く
  - 完了状態: 表の末尾に本仕様の 1 行が在り、その節以外の差分が 0
  - _Requirements: 6.1_
  - _Boundary: C4 沈黙ルール対応表_

- [ ] 3.2 (P) 網羅台帳を登記し、報告を作り直して番人を通す
  - 台帳の対象 1 項目を「動くが正典どおりではない」状態へ改め、宛先に本仕様を書き、備考に「どう違うか・記録は出ない・裁量は対応表に登記」を書く。優先度・値・リンク・導入は変えない。隣の 2 項目は一切変えない
  - 順序を守る: 台帳 → ロードマップ草案（本仕様の行を末尾へ・件数を 1 増やす・表の直前の散文に 1 文・束表の依存 spec 欄に本仕様を足す） → ブリーフィングの当該ページの分布 2 数値（縮退 +1・未対応 −1。他の数は触らない） → 報告 2 本の再生成（道具で作り直す・手で編集しない） → 番人
  - 常設の検査（宛先・件数・数え直し・束の実在・分布の突合）が緑であることを確かめ、結果を 1.4 で起こした記録の台帳節へ書く
  - 完了状態: 台帳の検査一式が緑で、分布の突合が台帳の数え直しと一致している
  - _Requirements: 6.2, 6.3, 6.4, 6.5_
  - _Boundary: C5 台帳文書_

- [ ] 4. 実機で見え方を目視する
  - 決定論テストが緑であること、および i686 helper が既定の出力先に在ることを確かめてから、既存の起動引数の経路で本番ゴーストと保管フォルダを**絶対パス**で指して起動する（コード変更 0）
  - 表示スケール既定と既定以外の 2 通りで、半角・全角・選択肢（ダブルクリックメニュー）・枠の透け・文字の欠けを 1 度ずつ見る
  - 日付・ゴースト・バルーンの絶対パス・表示スケールの値・観察項目・所見を記録の目視節へ書く（GPU 合成窓のため読み戻し画像が無ければ観察記録を証跡とする）
  - 崩れが見つかった場合は 2.8 と同じ処理（本仕様内で直す／引受先を実在確認して先送り）に回し、直したなら同じ崩れを再現する檻を足す
  - 完了状態: 2 通りのスケールの観察記録が記録に残り、崩れの有無とその行き先が明記されている
  - _Depends: 2.8_
  - _Requirements: 4.1, 4.2, 4.3_

- [ ] 5. 記録の完成と下流への申し送り
- [ ] 5.1 記録を完成させる
  - 決定論テストの結果と着手前後のワークスペース本数、既知の縮退の事実（枠の原点色の白縮退・折返し基準未宣言の記録・相方側の縮退 2 件）、崩れの処理の発動有無と行き先を書く
  - 第三者向け README にそのまま写せる形の出典文（資産名・作者・ライセンス・出典 URL・既知の制限「半透明前提のバルーンだけが正しく表示される」）を 1 節にまとめる
  - 完了状態: 記録の全節が埋まり、README 用の文が中身をそのまま持ち出せる状態になっている
  - _Depends: 2.8, 3.2, 4_
  - _Requirements: 3.9, 4.2, 4.3, 5.2, 6.5_

- [ ] 5.2 下流 3 spec と本仕様の brief へ申し送る
  - 追記先 3 件が仕様ディレクトリ直下に brief として実在することを先に確かめる。既に完了済みへ移っていればその spec には追記せず、正本ロードマップの当該行へ書く
  - 畳み込み側へ「保管済みのフォルダと 29 ファイル・ハッシュの在り処・畳む対象と共有ヘルパの検体名・付け替える定数 1 行の場所」を 1 段
  - 根の解決順側へ「既定バルーンの id（定義の id ＝配置先の綴り）・定数と配線はそちらが足す」を 1 段
  - 配布側へ「zip に入れる保管フォルダ・README の出典文は本仕様の記録の該当節をそのまま写す」を 1 段。本仕様の brief にも「既定バルーン id」を 1 行
  - 完了状態: 4 つの brief（または不在時のロードマップ行）に追記が入り、追記の実施記録が記録に残っている
  - _Depends: 5.1_
  - _Requirements: 5.2, 7.1, 7.3, 7.4_

- [ ] 6. 触っていないことを機械で証明する
  - 基準コミットからの差分で、本番コード（各 crate の src 配下の非テストファイル）・すべての依存記述とロックファイル・自動生成の第三者告知・開発者向け README・既存の検体参照ファイル群・既存検体 3 つが **0 行**であることを確かめる（本番コードに正典 URL のコメントも既定バルーン id の定数も足していないことを同時に示す）
  - 台帳の隣 2 項目の行が差分に現れないことを確かめる
  - 新規テストのディレクトリの差分が新規追加ファイルのみ（2.8 でテーマ分割した場合はその一式）で、既存ファイルの変更が 0 行であること、および並走 2 spec の接触面に触れていないことを確かめる
  - 作業木の変更ファイル全行が設計のファイル一覧に含まれることを確かめる。2.6 が発動して要件書を是正した場合は、その 1 ファイルを一覧外の例外として記録に明記する
  - i686 helper が既定の出力先に在ることを確かめてから（ワークスペースのビルドが x64 で上書きするので毎回置き直す）`cargo test --workspace` を単独で走らせ、1.1 の本数＋新規檻の分で緑になること、整形検査と 1,000 行の番人が緑であることを確かめ、本数を記録へ書く
  - 完了状態: 上の差分検査がすべて 0 で、ワークスペース全体が緑。結果が記録に本数付きで残っている
  - _Depends: 5.2_
  - _Requirements: 2.7, 3.1, 3.10, 5.3, 6.3, 6.6, 7.2, 8.1, 8.2, 8.3, 8.4, 8.5_

## Implementation Notes

- 1.2: この機械は `core.autocrlf=true`（system 設定）。素の `git clone` で上流を取り直すと `LICENSE`（上流 blob は LF のみ）が CRLF へ壊れ、保管先とハッシュが食い違う。採り直しは必ず `git -c core.autocrlf=false -c core.eol=lf clone` で行うこと。`descript.txt`／`install.txt`／`readme.txt` は blob 自体が CRLF なのでどちらでも同値。
- 1.2: `research.md` §8.2 の「`LICENSE` は…（CRLF）」は実測と逆（実測は CR 0・LF 121）。`provenance.md` はこの誤記を引き写さないこと（実装者・レビュアーの双方が独立に実測）。
- preflight: この worktree は `vendors/pasta` サブモジュールが未初期化だった。`cargo` は `[patch.crates-io]` の `pasta_core` を解決できず即座に落ちる。`git submodule update --init --recursive` が要る。
- 1.1: `cargo test --workspace` は i686 の成果物を **2 つ**要求する。helper（`target/debug/shiori-host32-helper.exe`）だけでなく **testdll**（`cargo build -p shiori-host32-testdll --target i686-pc-windows-msvc` → `target/i686-pc-windows-msvc/debug/shiori.dll`）も要る。testdll が無いと `shiori-host32-host::lifecycle_cyclic_e2e::cyclic_run_and_clean_shutdown` が赤になり、そこで走行が打ち切られて 81→60 ターゲット・7,668→5,411 本しか採れない（＝部分的な緑を全体と誤認する罠）。
- 1.1: 基準値は `--no-fail-fast` 付きで採った。タスク 6 も旗をそろえること（全緑のときだけ素の `cargo test --workspace` と同本数になる）。基準: passed 7,668 / failed 0 / ignored 40・`Running` 81 ＋ `Doc-tests` 22 ＝ `test result:` 103 行。基準コミット `082379b3`。
