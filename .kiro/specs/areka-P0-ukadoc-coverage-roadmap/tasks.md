# Implementation Plan

> 段 0〜6 は design.md「作業の段」に対応する。数はすべて 2026-09-11 の写真であり、各タスクで数え直す（要件 1.5）。

- [x] 1. 段 0: 着手条件の確認と道具の土台
- [x] 1.1 着手条件 3 つを確かめ、3 文書の器を置き、辞書の配布元を確認する
  - 4 台帳の未分類 0 件・`doc/ukadoc-coverage/report/summary.md` の実在・`emo2-conformance-e2e` が `completed/` にあり `verification/m1-completion.md` に開発者の署名があることを確かめ、確かめた日付と方法を `briefing.md` 冒頭の「着手条件の確認」節に書く
  - 欠けている物があれば絶対パスを添えて止まり、代替の値を推測で書かない
  - `linkage.md`・`briefing.md`・`roadmap-draft.md` を、以降の判定が読み込みに失敗しないだけの空の骨組み（`[tally]`・`[stage.A]`〜`[stage.E]`・`[briefs]` の 0 値と `snapshot_on`）付きで新規に作る
  - 里々「ポストと狛犬」・YAYA「はろーYAYAわーるど」（無ければ「SimpleYAYA」）の配布元 URL を開発者へ確認し、答えを待つ間も後続の段が進むように、確認した URL（または未回答である事実）を記録しておく
  - 完了状態: 3 文書が実在し、`briefing.md` の冒頭に確認日と手順が書かれ、着手時の実測値をすべて数え直した値で記録している
  - _Requirements: 1.1, 1.4, 1.5_

- [x] 1.2 3 文書の骨組みを読む純粋層 `documents::parse` を作る
  - ```toml の囲みの取り出し・引用符の id・逆引用符の id・裸の `ukadoc:` の 4 つの口を置き、`examples.rs` の重複した実装をこちらへ寄せる
  - 1 文書の囲みを連結して 1 度だけ TOML として読み、鍵の重複・欄の欠落・語彙外の値を文書名と表の鍵を添えて落とす（行番号は添えない）
  - `[[rank]]` は `bundle` と `singles` のちょうど一方を持つこと・`[stage.*]` が 5 つ揃うことを形として確かめる
  - 3 文書の場所を既存の場所の定義へ足す
  - 完了状態: 在中テストが「囲みの連結」「鍵の重複で落ちる」「欄の欠落で鍵を名指す」「3 種の id の口の振り分け」で緑になり、`examples.rs` が新しい口を使って従来どおり緑
  - _Requirements: 4.1, 4.2, 5.6, 6.2, 10.2, 11.1_
  - _Depends: 1.1_
  - _Boundary: documents::parse_

- [x] 1.3 帰属と順位から機械で決まる値を導く `documents::derive` を作る
  - 束ごとのテーマの和集合・跨ぐドメイン・資産の広さ・基盤共有度を導き、4 つの根拠の比較鍵を固定序列（壊れ方 ＞ テーマ数 ＞ 資産 ＞ 共有度）で組む
  - 全項目の優先度を「段階 1 文字＋段階内の束の順位（密な順位 1,2,2,3）」で導き、別名・対象外は空文字にする
  - どの束にも属さない対象項目・順位表に無い束・帰属に無い束名は id と文書名を添えて失敗にする
  - 完了状態: 在中テストが「導出の鍵が 4 台帳の全 id と一致」「帰属の無い id で落ちる」「比較鍵の序列」「共有度が自分を含む」で緑
  - _Requirements: 6.1, 6.2, 6.4, 6.5, 6.7, 7.1, 7.2_
  - _Depends: 1.2_
  - _Boundary: documents::derive_

- [x] 1.4 (P) 台帳の優先度 1 行だけを置き換える純粋層を作る
  - 既存の塊の切り分けを使い、塊の中で行頭が優先度の欄で始まる行 1 つだけを差し替え、前置き・他の欄・備考の複数行文字列はバイト列のまま写す
  - 置換の必要が 0 件なら入力と 1 バイトも違わない本文を返し、優先度の行が無い・2 行ある塊は id を挙げて落ちる
  - 完了状態: 在中テストが「冪等」「他の欄のバイト不変」「備考の中の字下げ行を触らない」「置換後に台帳の読み取りが通る」で緑
  - _Requirements: 7.1, 12.2_
  - _Boundary: ledger::patch_

- [x] 1.5 書き戻しの副手続きを 9 つ目として足す
  - 帰属と順位の 2 文書と台帳 4 本を読み、導出して 4 本を書き直す副手続きを置き、振り分け表・使い方・「8 つ」の釘付けを 9 つへ改める
  - 1 本でも読めなければ探した絶対パスを添えて止まり、導出に失敗する id があれば挙げて止まり、どちらの場合も 1 バイトも書かない
  - 読み手と書き手を引数で受ける形を併せて置き、4 本の本文が全部決まってから書く順番を確かめる。実行体テストに「引数を取らない」の腕を 1 つ足す
  - 完了状態: 在中テストが「2 回目は変更 0 項目で本文が変わらない」「導出失敗で 1 本も書かない」「副手続きの名前が 9 つ目に並ぶ」で緑
  - _Requirements: 7.1, 7.2, 7.6, 1.4, 12.2_
  - _Depends: 1.3, 1.4_

- [x] 1.6 全体報告を台帳とカタログだけで決まる本文にする
  - 末尾の「ドメインごとの証拠あり件数」の節を落とし、描画の入力から証拠の索引を外す
  - 冒頭 2 行目の「常時検査の合否に入れません」を「カタログと台帳から決まり、常時検査が新しさを判定する。証拠の件数は `evidence` 副手続きで読む」に改め、その文言を逐語で釘付けしている在中テストを同時に直す
  - 報告を作る副手続きから証拠の走査を外す（1.5 と同じ入口のファイルを触るので直列に行う）
  - 完了状態: 在中テストが「出力に証拠の表が無い」「冒頭 2 行目が新しい文言」で緑
  - _Requirements: 11.2_
  - _Depends: 1.5_

- [x] 1.7 判定の土台（読み込み・数え上げ・写しを壊す道具・母数の器）を置く
  - 3 文書・全体報告・spec ディレクトリの一覧（本 spec 自身のディレクトリ名と `completed` を除く直下の brief 持ち・`completed/` の直下）を 1 か所で読む道具を、統合テスト側に置く
  - 写しを壊す道具（id を 1 文字変える・構成 id を 1 つ抜く・件数を 1 ずらす・束名を 1 つ消す）を本文の写しの上だけで働く小さな関数として置き、リポジトリのファイルには触らない
  - 母数の下限を集める器と、判定ファイル 4 つの登録を済ませる。自 spec はディレクトリ名の定数だけを持ち、パスは書かない
  - 完了状態: 統合テストから 3 文書と spec ディレクトリの一覧が読め、壊す道具の単体の確認が緑で、`cargo test -p ukadoc-survey` が従来どおり緑
  - _Requirements: 11.3, 11.4, 12.8_
  - _Depends: 1.2_
  - _Boundary: tests/consistency/documents.rs, documents_non_vacuity.rs, mod.rs_

- [x] 2. 段 1: 跨ぐ連鎖の骨格を補修し、報告 5 本を作り直す
  - 重なり順の連鎖にプロパティ側から資産側へ 1 本、インストールの連鎖にさくらスクリプト側からイベントへ 2 本を、相手 id をカタログから写して足す（時刻の連鎖は既に 1 つの束なので 0 本）
  - 同じ繋がりを往復で 2 度書かず、種別は 6 つに限り、帰属を表すためだけの関連は書かない。既存の向きの流儀が正典の種別定義と逆であることを帰属の文書の「補修した関連」節に 1 行書く
  - 関連を足すとドメイン別報告が古くなるので、続けて報告 5 本を各々の副手続きで作り直す（手で編集しない・改行の違いは手で直さない）
  - 完了状態: 台帳と報告 5 本が 1 つのコミットに入り、足した本数がドメインごとに（0 本のドメインも 0 と）記録され、報告を作り直した後に `cargo test -p ukadoc-survey` が緑
  - _Requirements: 3.1, 3.2, 3.4, 3.5, 3.6, 2.1, 2.2, 2.5_
  - _Depends: 1.6_

- [x] 3. 段 2: 束の名付けと帰属（`linkage.md`）
- [x] 3.1 機械の束の過剰な繋がりを解き、分割の表を書く
  - 53 件を 1 つに繋いでいる 13 のページ単位 id とその相互参照の関連を洗い出し、ページの相互参照であって機能の繋がりでないことを示す
  - 13 のページ id 全部の行き先と、過剰だった関連（種別と両端の id）を「機械の束の分割」節に表で書く
  - 台帳の関連は削らず、分けた各束の由来欄に同じ機械の束 id を書く
  - 完了状態: 13 件すべてに行き先が付き、由来する機械の束 id と過剰な関連の一覧が表として読める
  - _Requirements: 3.1, 3.3_
  - _Depends: 2_

- [x] 3.2 段階 A・B 相当の名前付き束を書く
  - 束ごとに 8 つ（名前・由来する機械の束 id・構成 id の全列挙・跨ぐドメイン・成立に要る最小の基盤・欠けると壊れる振る舞い・壊れ方の最悪値・テーマの集合）を 1 か所に書く
  - 人手で足した id には人手の印を付け、推量の語を使わず、壊れる振る舞いを書けない束は名付けない
  - 例示の 3 連鎖（時刻の刻み・重なり順・インストール）がそれぞれ 1 つの束に収まることを束 id と構成 id で示す
  - 帰属の正本がこの文書であることと、id は必ず引用符か逆引用符で囲む書き方の規律を冒頭に書く
  - 完了状態: 起動・会話・撫で・メニュー・終了・自発発話・名前の記憶と、更新・インストール・切替・消滅の各束が、8 項目すべて埋まった形で読める
  - _Requirements: 3.1, 4.1, 4.4_
  - _Depends: 3.1_

- [x] 3.3 段階 C 相当の名前付き束を書く
  - 環境の察知（スリープ復帰・バッテリー・スクリーンセーバー・フルスクリーン退避・最小化・ディスプレイ変化・サウンド）と照会系の各束を 8 項目で書く
  - 完了状態: 段階 C 相当の各束が 8 項目すべて埋まった形で読め、推量の語を含まない
  - _Requirements: 4.1, 4.4_
  - _Depends: 3.2_

- [x] 3.4 段階 D・E 相当の名前付き束を書く
  - 多重ゴースト・コミュニケート・呼び出し・SSTP・FMO・PLUGIN・リンクと、外部アプリ・開発者機能・トランスレータ・ヘッドラインの各束を 8 項目で書く
  - 完了状態: 段階 D・E 相当の各束が 8 項目すべて埋まった形で読め、推量の語を含まない
  - _Requirements: 4.1, 4.4_
  - _Depends: 3.3_

- [x] 3.5 関連 0 本の項目を人手で束へ入れる
  - 台帳の関連を 1 本も持たない項目（着手時の実測で 1,127 件が順位の対象）を、台帳の関連を足さずに既存の名前付き束へ人手で入れ、人手の印を付ける
  - 束に入れられない項目は次のタスクで単独項目に落とす候補として印を残す
  - 完了状態: 関連 0 本の対象項目のうち束に入れたものの件数と、単独項目に落とす候補の件数が数え直した値で記録され、合計が実測と一致する
  - _Requirements: 3.6, 4.1_
  - _Depends: 3.4_

- [x] 3.6 単独項目・合計・SAORI 節・別軸 節を書く
  - 束に入らない項目を単独項目として同じ表に置き、束に入れられない理由を書き、ドメインごとの件数を（0 のドメインも 0 と）書く
  - 合計の表に対象の全数・機械の束から来た数・人手で足した数・単独項目の数・別名と対象外の除外数を置き、3 つの恒等式が成り立つ値にする
  - 台帳に行が 0 件である SAORI を成立条件 3 つとともに専用の節へ置いて実装項目としてどの束にも入れず、技術選定は「別軸」として 1 節に置く
  - 束の解説をこの文書にだけ書き、報告 5 本へ説明を書き足さない
  - 完了状態: 対象 4 状態の全項目がちょうど 1 つの束か単独項目に属し、合計の各数と恒等式が数え直しと一致する
  - _Requirements: 4.2, 4.3, 4.5, 4.6, 4.7, 11.5_
  - _Depends: 3.5_

- [x] 3.7 id と機械の束 id の実在を判定する
  - 3 文書が引用する id の全数がカタログに実在すること・裸の `ukadoc:` が 0 件であること・実在しない綴りの報告ファイル名が 3 文書のどこにも無いことを判定に加える
  - 引用された機械の束 id の全数が報告 5 本の束の一覧に在ることを判定に加える
  - 各判定について、写しの id を 1 文字変える／由来の id を 1 文字変える摂動でちょうどその 1 件が赤になることを確かめる
  - 対象が 0 件でないこと（引用 id 数の下限・報告から読めた束 id の下限。下限の値は段 1 で数え直した実測を注釈に残す）を確かめる
  - 完了状態: `cargo test -p ukadoc-survey` に判定 2 種・摂動 2 本・母数の下限が加わって緑で、摂動を有効にすると失敗の本文がファイル名と id を名指す
  - _Requirements: 11.1, 11.3, 11.4, 1.2_
  - _Depends: 3.6, 1.7_

- [x] 3.8 帰属の分割を判定する
  - 束の構成 id が互いに素であること・和集合が対象 4 状態の全項目と過不足なく一致すること・人手の印が構成 id の部分集合であること・機械由来の id が引用した機械の束の構成 id に含まれること・別名と対象外の id が 1 つも現れないことを判定する
  - テーマと跨ぐドメインが導出と一致し、壊れ方が「該当なし」の束は全構成 id が実装済みであることを判定する
  - 合計の各数と 3 つの恒等式を数え直しと突き合わせる
  - id を 1 つ抜く／2 束に入れる／合計を 1 ずらす摂動でちょうどその 1 件が赤になり、名前付き束と人手の数の下限で母数 0 を許さない
  - 完了状態: 判定 1 種と摂動 3 本と母数の下限が標準のテスト実行で緑になり、失敗時に束名と id を名指す
  - _Requirements: 11.1, 11.3, 11.4, 4.2_
  - _Depends: 3.7, 1.3_

- [x] 4. 段 3: 段階と順位の第一段（`briefing.md`）と台帳への書き戻し
- [x] 4.1 テンプレート辞書の語彙をカタログの id へ写す
  - 1.1 で確認した配布元から配布物を取得し、辞書ファイルを静的に読む（実走しない・リポジトリに入れない）
  - イベント名・タグの綴り・プロパティ名・設定キーを取り出し、カタログの題との一致で id に写す。写せなかった語彙は件数と例を本文に書く
  - 取得できなければ wiki が名指しする語彙だけを参照値にし、退路を使った事実と取得できなかった配布元を書く
  - 完了状態: 配布元・取得日・辞書ファイル名・写し方・写した id の一覧が骨組みに書かれ、判定 ⑴ が id の実在を確かめられる形になっている
  - _Requirements: 6.3, 6.8_
  - _Depends: 3.8_

- [x] 4.2 段階 A〜E の定義・初期配置・写像の規則・裁定候補を書く
  - 5 つの節目の定義表と、brief が定めた束の初期配置を冒頭に置く
  - 写像の規則 3 つ（テーマ 1 つ＝先頭群／2 つ以上＝繰り上げ可／0 かつ見た目の差以下＝段階 E 候補）を固定して書く
  - 台帳の根拠が初期配置と食い違う束を、初期配置・台帳が示す配置・差の理由（id 付き）の 3 点で裁定候補の節に並べ、黙って変えない
  - 完了状態: 定義表・初期配置・規則 3 つ・裁定候補の一覧が読め、「更新」を段階 B の先頭に・照会系を段階 C の末尾に置く方針が明記されている
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  - _Depends: 3.6_

- [x] 4.3 順位表と段階ごとの件数の骨組みを書く
  - 束ごとに段階・順位・資産の広さ・基盤共有度を置き、順位は固定序列の比較鍵の降順・密な順位（1,2,2,3）で振る
  - 4 つの根拠と由来 id は帰属の文書の欄名で指し、同じ値を 2 か所に書かない。推量の語を書かず、根拠が空の束は順位表に載せず「根拠不足」の一覧に置く
  - 規則と両立しない行と退路を使った行には、型の付いた例外の印（種別と根拠の参照先）を書いて順序の主張から外す
  - 段階ごとの束数・単独項目数・項目数を 5 段階すべて（0 も）書く
  - 完了状態: すべての名前付き束と単独項目がちょうど 1 度だけ順位表に現れ、段階ごとの 3 つの数が埋まっている
  - _Requirements: 5.6, 6.1, 6.2, 6.4, 6.5, 6.6, 6.7, 11.5_
  - _Depends: 4.1, 4.2_

- [x] 4.4 台帳へ確定値を書き戻し、前後の分布を記録する
  - 書き戻しの副手続きを走らせ、対象 4 状態の全項目に「段階 1 文字＋段階内の束の順位」を入れ、別名と対象外は空にする
  - 書き戻し前のドメイン別段階分布を手順付きの写真として本文に残し、後の分布と空欄の件数を骨組みに置く
  - 台帳と報告 5 本を同じコミットに入れ、改行の差は手で直さない
  - 完了状態: 副手続きを 2 回続けて走らせて 2 回目が 0 項目変更となり、`cargo test -p ukadoc-survey` が緑で、赤になった所見の種別と直し方を作業記録に残している
  - _Requirements: 7.1, 7.2, 7.3, 7.6, 2.1, 2.5_
  - _Depends: 1.5, 4.3_

- [x] 4.5 宛先の欄を 4 つの規則で整える
  - 進行中 spec 宛ての宛先は保ち、完了済み spec 宛ては実装済みか縮退なら記録として保ち、未対応か語彙のみなら空にして理由を備考に書く
  - 保った完了済み spec の名前と件数を骨組みに列挙し、brief がまだ無い候補 spec の名前を宛先に書かない
  - 規則で変えた件数を（0 も）本文に書き、備考へ書き足すときは既存の記述を消さず行番号を書かない
  - 台帳を触るので報告 5 本を作り直して同じコミットに入れる
  - 完了状態: 非空の宛先がすべて進行中 spec か列挙した完了済み spec のいずれかになり、列挙した spec を宛先に持つ項目の状態が実装済みか縮退だけになっている
  - _Requirements: 7.4, 7.5_
  - _Depends: 4.4_

- [x] 4.6 主障壁と根拠表の参照を書く
  - 段階 A の主障壁をページ別の状態分布として台帳から数え直して骨組みに置く
  - テーマ別の状態分布は全体報告を、SSP 世代別はドメイン別報告 4 本を参照で指し、写しを作らない
  - 文書の 8 つの節（着手条件・段階の定義と規則・順序付き束一覧・主障壁・根拠表・処分台帳・第二段の改訂記録・是正候補への参照）の骨格を揃える
  - 完了状態: 主障壁の各数が台帳の数え直しと一致し、8 つの節が（後段で埋める節は見出しだけでも）揃っている
  - _Requirements: 8.1, 2.3, 2.4_
  - _Depends: 4.5_

- [x] 4.7 段階と順位を判定する
  - 束と単独項目がちょうど 1 度だけ順位表に現れること・順位表の束名が帰属の文書に在ること・段階ごとの 3 つの数が数え直しと一致することを判定する
  - 台帳の優先度が導出と一致すること・資産と共有度が導出と一致すること・例外の印を持たない行が比較鍵の降順で密な順位に並ぶこと・例外の印の種別と参照先が受け付け形に収まることを判定する
  - 主障壁・書き戻し後の分布・空欄の件数が台帳の数え直しと一致すること、「更新」が段階 B の先頭・照会系が段階 C の末尾にあることを判定する
  - 束名を 1 つ消す／件数を 1 ずらす／優先度を 1 件書き換える摂動で赤になり、順位表の行数と段階 A の項目数の下限で母数 0 を許さない
  - 完了状態: 判定 1 種と摂動 3 本と母数の下限が標準のテスト実行で緑になり、失敗時にファイル名と束名を名指す
  - _Requirements: 11.1, 11.3, 11.4_
  - _Depends: 4.6, 1.3_

- [x] 4.8 全体報告の新しさを判定する
  - 全体報告の本文（復帰文字を落としたもの）が描画の出力と全文一致することを判定に加える
  - 本文の数字を 1 つ変える摂動で赤になり、出力が空でなく束の表が 1 行以上あることで母数 0 を許さない
  - 上流が全体報告を常時検査から外した理由（並走 4 本が同じファイルを取り合う）が消えたこと、証拠の表を外して `evidence` 副手続きに一本化したことを、本 spec の文書の側に書く
  - 完了状態: 報告を手で 1 文字変えると標準のテスト実行が赤になり、実データでは緑
  - _Requirements: 11.1, 11.2, 2.2, 11.3, 11.4_
  - _Depends: 4.7, 1.6_

- [x] 5. 段 4: 第二段——M1 完成の実物で順位を直す
- [x] 5.1 適合検証 20 項目と持ち越し 8 行を 1 行ずつ読んで改訂を決める
  - 第一段の順位を「草案」と明記したうえで、20 項目の各行と持ち越し 8 行を 1 行ずつ読み、関わる束と順位を動かすかを表にする
  - 動かす行は変更前・変更後・理由（項目番号または持ち越し行の見出し）を書き、動かさない場合は「いずれも順位を動かす根拠にならなかった」と明記して確かめた項目番号を列挙する
  - 既に spec が起票済みの 4 件は新たな束にせず、次段の spec 表に載せる印を付ける
  - 完了状態: 28 行すべてに判断が付き、改訂記録の表が読める
  - _Requirements: 9.1, 9.2, 9.3, 9.4_
  - _Depends: 4.8_

- [x] 5.2 段階 A の温度感と一般化で壊れる項目を id で示す
  - 段階 A の温度感を「当面 emo2 が動けばよい」とし、テンプレート辞書の語彙と段階 A の構成 id の重なりのうち未対応・語彙のみ・縮退の項目を id で列挙する
  - 外部から要望が来た時点で参照元と検証対象を切り替える旨と、現時点では切り替え先が無いことを書く
  - 完了状態: 一般化で壊れる項目が id 単位で列挙され、切り替え方針が本文に書かれている
  - _Requirements: 9.5, 9.6_
  - _Depends: 5.1_

- [x] 5.3 改訂を順位表へ反映して書き戻しを再実行する
  - 動かすと決めた行の段階・順位を書き換え、例外の印に第二段の根拠（項目番号または持ち越し行の見出し）を書く
  - 書き戻しの副手続きを再実行し、報告 5 本を作り直して同じコミットに入れる
  - 完了状態: 判定 ⑷ を含む標準のテスト実行が緑になり、改訂記録の表の内容と順位表が一致する
  - _Requirements: 9.2, 7.1, 2.1_
  - _Depends: 5.2_

- [x] 6. 段 5: ロードマップ草案・申し送りの処分・README
- [x] 6.1 ロードマップ草案の本体を書く
  - 冒頭に「草案であり、反映は棚卸セッションで一括裁定する」旨を書く
  - 段階ごとに束→候補 spec 名の案→依存する既存 spec→ウェーブ案を並べ、先頭ウェーブの束だけ構成 id の全列挙と 3 行の要約（問題・現状・何が変わるか）を持たせ、それより後は brief を作らない
  - ウェーブ編成は入力として扱い、並べ替えと同順位の解消は現在のウェーブ・提案・理由（id 付き）の裁定候補として書く。技術選定は「別軸」の節へ置いて順位を付けない
  - M3 の受入基準の候補（テーマ 8 つすべてで代表束が実装済み）を登記し、決定は階梯の議論（棚卸セッション）に委ねる旨を添える
  - 完了状態: 段階 A〜E の各節と先頭ウェーブ・別軸・M3 候補・裁定候補の節が揃い、先頭ウェーブの束が `/kiro-discovery` 再入の入力としてそのまま使える
  - _Requirements: 10.1, 10.3, 10.4, 10.5, 10.6, 10.9, 5.7, 6.7_
  - _Depends: 5.3_

- [x] 6.2 既存 brief の位置づけと予約群の対応表を書く
  - brief 済み未完了 spec を、本 spec 自身のディレクトリ名を除いて数え、その数え方を本文に書く（パスは書かない）
  - 各 spec について段階・束・宛先に持つ id の数・ウェーブを 1 表に書き、どの束にも属さない spec はそう書く（0 本なら 0 と書く）
  - 表が着手時の写真であること（撮った日付を添える）と、生きた総数との一致は主張しないことを書く
  - 予約群の各項目がどの束に写ったかの対応表を書き、写らなかった項目は名前と理由を書く（0 件なら 0 と書く）
  - 台帳 id と brief の所有宣言が食い違うものを spec 名・食い違う id・直し方の案の 3 列で列挙し、brief 本体は書き換えない
  - 完了状態: 表の行数と冒頭の数が一致し、表の各 spec 名が実在するディレクトリ名になっている
  - _Requirements: 10.2, 10.7, 10.8, 1.3_
  - _Depends: 6.1_

- [x] 6.3 (P) 申し送りの処分台帳を書く
  - 調査 4 本のブリーフィングと完了 spec 5 本の `tasks.md` を 4 つの語で検索して申し送りを全数拾い、宛先で仕分けた件数を冒頭に書く
  - 1 件ごとに出典（文書名と節名）・要約・処分・理由・反映先を書き、名指しの項目（shiori 系・assets 系・sakura-script 系・property 系の各件）をすべて含める
  - 採用は反映先を台帳の id か 3 文書の節名で示し、裁定候補には 3 行の前置きを添える。答えで作業が変わらないものは自分で決めて理由を書く
  - 「10 刻み」の提案の却下理由と、全体報告から証拠の表を外して上流の要件を覆した旨をこの台帳に書く
  - 平易な語で書き、上流ブリーフィングの本文を写さず参照で済ませる
  - 完了状態: 拾った件数・処分済み件数・裁定候補の件数が冒頭に並び、全件に処分と理由が付いている
  - _Requirements: 8.2, 8.3, 8.4, 8.5, 8.7, 8.8, 8.1_
  - _Depends: 5.3_
  - _Boundary: briefing.md_

- [x] 6.4 裁定待ち 2 件を裁定して台帳へ反映する
  - タグの分担に関する裁定待ち 2 件を統合担当として裁定し、調査側の案を採るか採らない理由を書く
  - 結果を該当 id の宛先と備考に書き、処分台帳にも記す。宛先に書く名前は spec 表か完了済みの列挙に在る名前に限る。備考は既存の記述を消さず行番号を書かない
  - 完了状態: 2 件の id の宛先と備考に裁定の結果が入り、台帳を触ったので報告 5 本を作り直して標準のテスト実行が緑
  - _Requirements: 8.6, 7.5_
  - _Depends: 6.2, 6.3_

- [x] 6.5 (P) README の限定編集
  - 「誰が何を作り直すか」の表の全体報告の行を常時検査に入る形へ改め、その直下の 1 段落を証拠の表だけを外す理由に書き換える
  - 「報告の扱い」節と「全体報告は黙って古くなる」節のうち、事実でなくなる文を揃え、上流の要件を本 spec が覆した旨を 1 行書く
  - 「この一式に入っているもの」に新規 3 文書を追記する。ほかは触らない
  - 完了状態: README に「常時の検査に入っていない」と述べる文が残っておらず、編集箇所が上記の 3 か所に限られている
  - _Requirements: 11.2, 12.4_
  - _Depends: 5.3_
  - _Boundary: doc/ukadoc-coverage/README.md_

- [x] 6.6 spec ディレクトリと宛先の整合を判定する
  - 冒頭の数が表の行数と一致すること・表の各 spec 名が `.kiro/specs/` 直下か `completed/` に実在すること・宛先に持つ id の数が台帳の数え直しと一致することを判定する
  - 表と予約群が指す束名が帰属の文書に在ること・列挙した完了済み spec が実在しその名前を宛先に持つ項目の状態がすべて実装済みか縮退であること・台帳の非空の宛先がすべて表か列挙のいずれかに在ることを判定する
  - 他 spec の起票・完了では赤にならず、表の spec の改名・削除で赤になる形にし、テストには本 spec 自身のパスを書かずディレクトリ名だけを使う
  - 数を 1 ずらす／spec 名を 1 文字変える／宛先を 1 件書き換える摂動で赤になり、表の行数・列挙の行数・完了済みディレクトリ数の下限で母数 0 を許さない
  - 完了状態: 判定 1 種と摂動 3 本と母数の下限が標準のテスト実行で緑になり、他 spec のディレクトリを増やしても赤にならないことを確かめている
  - _Requirements: 11.1, 11.3, 11.4, 12.8_
  - _Depends: 6.4_

- [ ] 7. 段 6: 最終検証と完了報告
- [x] 7.1 main へ rebase して数え直す
  - 既定ブランチへ rebase し、spec ディレクトリと完了済みディレクトリを数え直す
  - 改名・削除があれば spec 表と完了済み宛先の列挙を直し、数え直した値で冒頭の数を更新する
  - 完了状態: rebase 後に標準のテスト実行が緑で、表の各名前が実在している
  - _Requirements: 1.3, 10.2, 11.7_
  - _Depends: 6.5, 6.6_

- [ ] 7.2 判定の全数と非接触を確かめる
  - 判定 6 種がすべて実データで緑・摂動がそれぞれちょうど 1 件で赤・母数の下限がすべて有効であることを一度に確かめ、判定を緩めて緑にしていないことを示す
  - 判定が標準のテスト実行の外（作業用スクリプト）に無いことと、関連の種別が 6 つに限られ別名・後継の欄だけの対が束になっていないことを確かめる
  - 触ったファイルの一覧を取り、道具の crate 以外の実行時コード・steering・既存 brief・調査 4 本のブリーフィング・テーマ定義・カタログに 1 行も差分が無いこと、3 文書と台帳の備考に行番号が無いこと、実機比較や実走を行っていないことを確かめる
  - 完了状態: `cargo test -p ukadoc-survey` が緑で、差分の一覧が境界の内側だけに収まっている
  - _Requirements: 11.1, 11.3, 11.4, 11.6, 11.7, 3.4, 12.1, 12.3, 12.5, 12.6, 12.8_
  - _Depends: 7.1_

- [ ] 7.3 完了報告の 6 項目を書く
  - 作った文書 3 本の絶対パスと行数・台帳の編集件数（関連／テーマ／優先度／宛先／備考ごと、0 も）・判定の種別数と摂動の本数・裁定候補の一覧・先頭ウェーブの束名と候補 spec 名・ロードマップの改訂候補の 6 つを書く
  - 完了状態: 6 項目すべてが数え直した値で埋まっている
  - _Requirements: 12.7_
  - _Depends: 7.2_

## Implementation Notes

- 1.1: 3 文書を読む判定はこの時点でリポジトリに 1 つも無い（`crates/ukadoc-survey/{src,tests}` に `linkage`／`briefing`／`roadmap-draft` を読むコードは無く、当たるのは `src/report/bundle.rs` と `bundle_tests.rs` の散文コメント 2 件だけ）。骨組みの正しさは常時テストがまだ何も主張していない。較正（写しを壊して名前の付いたテストが赤になることの確認）はタスク 1.2 の `documents::parse` の責務。
- 1.1: 骨組みに 0 行の配列表（`[[rank]]`・`[[barrier]]`・`[[after]]`・`[[owner_completed]]`・`[[template]]`・`[[spec]]`・`[[reserved]]`）は置いていない。TOML は配列表の見出しだけを空で置けず、`rank = []` にすると後続が `[[rank]]` を書いた瞬間に「値を上書きできない」で落ちるため。**1.2 の `documents::parse` はこれらの欄を「欠落＝0 行」として受け取れる形（省略可）で作ること。**
- 1.1: `roadmap-draft.md` の `[briefs].count = 0`・`snapshot_on = 2026-09-12` は仮値。**タスク 6.2 で数え直し、`snapshot_on` も打ち直すこと。**
- 1.1: テンプレート辞書の配布元は 2026-09-12 の開発者回答で「実装側（Claude）が公式配布元を探して取得する」。記録済み URL は 0 個。**タスク 4.1 が実際に配布元を特定・取得し、`[[template]]` の `url`・`fetched_on`・`files` を埋める**（取得できなければ要件 6.8 の退路へ落とし、開発者の追加指示は待たない）。経緯は `doc/ukadoc-coverage/briefing.md` §1-5 が唯一の記録。
- 全般: `grep -c` は 0 件のとき終了コード 1 を返す。0 件を「検査が失敗した」と読み違えないこと。
- 1.2: **`crates/ukadoc-survey/src/documents/parse.rs` は 997 行**。repo の 1,000 行制限まで余裕 3 行しかない。**次にこのファイルへ 1 行でも足す前に、共通ヘルパ節（`document_file`〜`theme_array_field` の約 270 行）を `documents/fields.rs` へ割ること。**
- 1.2: `documents::parse` が D-2 の明言を超えて決めた厳しさ 5 つ——名前付き束は `foundation` 必須・`reason` 禁止／単独項目は `reason` 必須・`machine`/`hand`/`foundation` 禁止・`members` ちょうど 1 件／`hand ⊆ members`／`[[rank]]` の `assets`・`shared` は省略可（既定 0）／`[[spec]]`・`[[reserved]]` は `bundle` と `none = true`＋`reason` のちょうど一方。**段 2〜4 で文書を書く担当がこれに当たったら、読み手を緩めるか文書を合わせるかを裁定すること。** 特に「単独項目に `foundation` を禁じた」のは設計に無い上乗せで、単独項目の `shared` が構造的に 0 に固定される（要件 6.5 ⑷）。
- 1.2: 設計 `documents::parse`「読み取り時の形の検査」のうち **「`bundle` は `single = true` でない束の名前・`singles` の各 id は `single = true` の束の名前」は `read_briefing(&str)` 単体では判定できない**（`linkage.md` を要る跨ぎの検査）。**引受先は `documents::derive`（1.3）かタスク 3.8 の判定。段 2 以降で明示すること。**
- 1.2: 欄取り出しの補助 5 本（`malformed`・`reject_unknown_keys`・`field`・`string_field`・`string_array_field`）が `src/ledger/read.rs` の私有補助とほぼ同形（約 120 行の重複）。共通化は境界外のため今回は割った。**3 つ目の読み手が出た時点で括ること。**
- 1.2: `toml_blocks` は行頭からの ```` ```toml ```` だけを囲みの始まりと見るが、`prose_lines` は字下げされた ```` ``` ```` も囲みと見る。**3 文書で囲みを字下げしないこと**（字下げすると骨組みとしても地の文としても読まれない）。
- 1.3: `documents::derive` は **順位を振り直さない**（`[[rank]]` に書かれた `rank` をそのまま写す）。密な順位 1,2,2,3（要件 7.1）を強制するのは**判定 ⑷-e の持ち場**（設計 D-9）。**判定 ⑷-e を実装するタスク 4.7 まで、密な順位はどこからも強制されない。** 4.7 の受け入れで必ず確かめること。
- 1.3: **`unclassified` の項目は `derive::priorities` が `Err` で落とす。** 要件 7.1（対象 4 状態）にも 7.2（別名・対象外）にも行き先が無いため。2026-09-11／2026-09-12 の実測ではどの台帳も 0 件。要件側で行き先を決めるなら 7.2 に 1 行足すのが素直。
- 1.3: 1.2 から持ち越された跨ぎの検査（`[[rank]]` の `bundle` は `single = true` でない束・`singles` の各 id は単独項目）は `derive::priority_by_id` が引き受けた。**両方向とも試験済み。持ち越しは消化済み。**
- 1.3: `error.rs` に `SurveyError::DeriveMismatch { file, reason }` を追加（本文「{file} から導けない: {reason}」）。`parse.rs` は `LINKAGE_FILE`・`BRIEFING_FILE`・`document_file` を `pub(super)` にしただけで **997 行のまま**。
- 1.3: 同じ id が 2 本の台帳に現れると `priorities` の `insert` が黙って上書きする。4 台帳がカタログを分割することは consistency の 677/542/342/188 テストが実データで見張っているので実害なし。
- 1.4: `ledger::patch::replace_priority` は **`wanted` の有無によらず全塊**に「行頭の `priority = ` 行がちょうど 1 本」を要求する（付録 A.2 が `priority` を必須の欄としているため）。レビューが実台帳 4 本（1,749 塊）で素振りし、`wanted` を空にした出力が**バイト一致**で `Ok` になることを確認済み。
- 1.4: 失敗の変種は 1.3 で足した `SurveyError::DeriveMismatch` を再利用（本文「…から導けない: 項目 <id> の priority の行が無い／2 本以上ある」）。書き戻しの失敗に「導けない」を使うのはやや語感が合わない。**1.5 で専用変種が要るなら併せて裁定すること。**
- 1.4: `patch.rs` は `toml::` を一切呼ばず `tomlout::basic_string` だけを使う（設計 D-9・付録 A.3 が凍結した書き方を壊さないため）。**この層で TOML の読み書きを持ち込まないこと。**
- 1.4: `LEDGER_FILE`（置き場の綴り）が `blocks.rs` の private 定数と `patch.rs` で重複している。1 行なので共有化はしていない。
- 1.5: 9 つ目の副手続き `priority-apply` を追加。**crate 内の「8 つ」の逐語の釘付けはすべて 9 つへ直した**（`SUBCOMMANDS`・`usage()`・`cli_tests.rs`）。残る「8 つ」は歴史記述のみ（`cli_tests.rs:216`「当時の 8 つ」・`cli_streams.rs:27,102`）。
- 1.5: **⚠ `doc/ukadoc-coverage/README.md:36`「副手続きは 8 つで、いずれも引数を取らない。」が陳腐化した。** ところが**要件 12.4 は README の編集を 3 か所に「限る」と書いており、副手続きの数はその 3 か所に含まれない**。タスク 6.5 の完了状態も「編集箇所が上記の 3 か所に限られている」なので、**6.5 の担当者は 12.4 に阻まれてこの 1 文を直せない。**
  - **親コントローラの裁定（2026-09-12）**: 9 つあるものを 8 つと書いた文を残すのは正典文書の誤りであり、12.4 の趣旨（README の膨張を防ぐ）に反しない。**タスク 6.5 で「副手続きの数」の是正を 4 か所目として行い、同時に要件 12.4 とタスク 6.5 の完了状態に 4 か所目を明記して追随させること**（要件を改訂したら design の境界節まで追随させる、が repo の規律）。完了報告の裁定候補一覧にもこの改訂を 1 件として載せること。
- 1.5: 書き手の引数は `FnMut(&Path, &str, usize)`（3 つ目は変更件数）。設計本文に明記は無いが、冪等（2 回目は変更 0 項目）を標準出力を捕まえずに主張するために必要。
- 1.5: 専用のエラー変種は足さず `SurveyError::DeriveMismatch` を継続。裁定で足すことになれば `patch.rs` の `undecidable` 1 か所と `error.rs` だけで済む。
- 1.5: `crates/ukadoc-survey/src/cli/generate_tests.rs` が **948 行**（上限 1,000 まで 52 行）。**次にこのファイルへ追記するなら先に分割すること。**
- 1.5: 現時点で `priority-apply` を実データに掛けると「どの束にも属さない対象項目」で終了コード 1 になるのが正しい姿（3 文書の `[[bundle]]` がまだ 0 行）。**実台帳を実際に書き換えるのはタスク 4.4 の担当。それまで実データへ `priority-apply` を掛けて書き込まないこと。**
- 1.6: 冒頭 2 行目の綴りは **design が書き分けている**——`summary.md` は敬体（`render_summary`（純関数化）節）、`README.md` は常体（File Structure Plan の README 行・主語「全体報告は」付き）。tasks.md 1.6 が引いた常体は README 向けの綴りだった。**タスク 6.5 で README を直すときは常体・主語付きの方を使うこと。**
- 1.6: **⚠ タスク 6.5 への申し送り 2 件。** ⑴ `doc/ukadoc-coverage/README.md` の「⚠ 全体報告は黙って古くなる」節（466 行付近）が丸ごと事実でなくなった。⑵ **design.md の README の行は「証拠の表だけを外す理由に書き換えよ」と書いているが、これは表を丸ごと外した裁定より前の文言で、design のその指示自体が陳腐化している。** 6.5 の担当者は design を鵜呑みにしないこと。
- 1.6: 廃止したテスト 2 本（`the_evidence_section_gives_counts_per_domain`・`the_report_never_shows_where_the_evidence_is_written`）は陳腐化による除去。後者の残余の主張（本文に URL やパスが出ない）は `the_body_is_pinned_verbatim` の全文一致がより強く引き受けている。
- 1.6: `report_summary` を参照するテストは `generate_tests.rs` ではなく `cli_tests.rs` にあり、副手続き名と使い方の一覧だけを見ている。`generate_tests.rs` は 948 行のまま。
- 1.6: design.md が「作らない」と明記した `render_summary_judged` は作っていない（research.md にその旧案が残っているが design が覆している）。
- 1.7: **⚠ 判定ファイルの登記は 2 本だけ**（`documents.rs`・`documents_non_vacuity.rs`）。design.md の File Structure Plan が挙げる残り 2 本 `documents_checks.rs`・`linkage_checks.rs` は本タスクの境界外なので `mod.rs` 冒頭の申し送りだけ。**タスク 3.7 以降が作成と `mod` 登記を行うこと。**
- 1.7: **⚠ 判定 6 種それぞれの母数の下限はまだ 1 つも置いていない**（`linkage.md` の引用 id 1,552 以上・報告の束 id の下限・`[[rank]]` 1 行以上・`[[spec]]` 20 行以上など）。3 文書が骨組み（配列表 0 行）なので今置くと即赤になるため。**タスク 3.7 以降が置くこと。それまで新しい判定は恒真の緑になりうる。**
- 1.7: **⚠ design.md「入口 / tests/consistency」の署名 `Documents::load(repo: &RepoData)` は実装の `load()` と食い違ったまま**（`repo` は値を運ばない添え物。`Documents` の欄に `RepoData` 由来が 1 つも無く、読むファイルも重ならない）。**設計側の追随（引数を落とす）を最終検証（タスク 7.2／7.3）で処理すること。**
- 1.7: **⚠ `crates/ukadoc-survey/src/io/paths.rs` の `summary_report_path()` の doc「常時検査の対象外（要件 7.6）」が事実でなくなった**（要件 11.2 が覆した）。境界外のため 1.6・1.7 とも触っていない。**判定 ⑹ を置くタスク（3.7 以降で `paths.rs` を境界に持つもの）で必ず直すこと。**
- 1.7: 3 文書の実測（復帰文字を落とした後の `chars().count()`・2026-09-12）: `linkage.md` 1,336／`roadmap-draft.md` 1,416／`briefing.md` 4,618／`report/summary.md` 33,326。**バイト長（linkage.md は 2,874）と取り違えないこと。**
- 1.7: 壊す道具 4 つのうち実データの写しに掛かっているのは `shift_count` 1 つだけ。3 文書に `ukadoc:` も `members = ` も `bundle = ` も 0 件だから（骨組みのため）。**3 文書が育ったら残る 3 つも実データの写しへ寄せてよい。**
- 2: 足した関連 3 本——`property.toml` に `configures` → `ukadoc:descript_shell:seriko.zorder_…:1` 1 本、`sakura-script.toml` に `triggers` → `ukadoc:list_shiori_event:OnInstallComplete:1` 2 本。ドメイン別 assets 0／property 1／sakura-script 2／shiori 0。
- 2: **⚠ タスク 3.1 への申し送り。** インストール連鎖に足した `triggers` 2 本は**束の構成を変えていない**（3 id は既にページ単位の `same-feature` 経由で同じ機械の束に居た）。**「機械の束の分割」節で、この 2 本が過剰な `same-feature` に代わる「インストール」束の核である旨を 1 行書くこと。**
- 2: 向きの流儀の実測（2026-09-12・レビューが独立に数え直して一致）: `configures` の終点ページ別は property 25（うち新規 1）・shiori 7。**assets の設定キーを終点にする既存 `configures` は 29 本**（property 24・shiori 5）で、**assets→property の `configures` は 0 本**。正典の定義（設定キー → 挙動・タグ・イベント）とは向きが逆。
- 2: 束の総数の数え直し（2026-09-12・判定 ⑵ の下限の材料）: 跨ぐ束 75 ＋ ドメイン内で閉じた束 shiori 25・sakura-script 23・assets 0・property 0 ＝ **合計 123**。補修の前後で変化なし。
- 2: 3 連鎖の着地——時刻＝`ukadoc:descript_plugin:secondchangeinterval_2c_79d2_6570:1`（3 件）／重なり順＝`ukadoc:descript_shell:char_2a.menu_2cauto_307e_305f_306fhidden:1`（48→**49** 件）／インストール＝`ukadoc:descript_ghost:makoto_2c_30d5_30a1_30a4_30eb_540d:1`（53 件）。
- 2: `report/{assets,property,sakura-script,shiori}.md` は `git status` に M と出ても **blob は同一**（作業ツリー LF・index CRLF の stat 差）。**改行の違いを手で直さないこと**（要件 2.5）。
- 3.1: 機械の束（makoto・53 件）を繋いでいたページ単位 id は 13 件、それを端に持つ `same-feature` は 46 本（ページ id どうし 26 ＋ ページ id と項目 id 20）。**過剰と判定した 19 本（17 対）だけを外すと 53 件が 9 つに割れる**（更新 30／インストール 9／開発者機能 4／切替 3／メニュー 2／トランスレータ 2／単独項目 1×3）。レビューが独立に再現済み。
- 3.1: **⚠ 設計 D-7 の「13 のページ id どうしの `same-feature` 46 本」は実データとずれている**（46 ＝ ページ id どうし 26 ＋ ページ id と項目 id 20。research.md §10.1 の方が正確）。**設計の誤記として最終検証（7.2／7.3）で記録すること。**
- 3.1: **⚠ タスク 4.2 への申し送り（名指しで確かめること）。** 要件 5.2 の初期配置は「メニュー」を段階 A・「オーナードローメニュー」を段階 B に分けているが、3.1 は機構で切って **1 束「メニュー」**にした。`linkage.md` に裁定候補として明記済み。**4.2 のレビューで、この 1 件が裁定候補の一覧から落ちていないかを名指しで確かめること**（4.2 の本文は一般文なので落ちやすい）。
- 3.1: **⚠ タスク 3.8 への申し送り。** 「機械の束の分割」節の数（53・13・46・26・20・19・17）を**数え直す判定は存在しない**。13 件の行き先は 3.8 の判定が id 単位で見張るが、過剰な関連 19 行は誰も数え直さない。**3.8 で「ページ単位の id を端に持つ `same-feature` のうち両端の束が異なるものの本数」を数え直す判定を足すか、足さない理由を書くこと。**
- 3.1: design D-7 の束名 3 つ（「nar の作成」「オーナードローメニュー」「配布物の構造」）は使わず、tasks.md 3.2〜3.4 の一覧の名前（「開発者機能」「メニュー」＋単独項目 3 件）に寄せた。**設計違反ではない**（D-7 自身が「最終の帰属は実装で決め」と明示・区切り方は D-7 の案と同一）とレビューが認定。
- 3.1: 既存の assets 台帳に**往復で書かれた `same-feature` が 5 対**ある（うち過剰 2 対）。要件 3.2 の「往復で 2 度書かない」と読み方が競合しうるが、既存行は触っていない。
- 3.2: 段階 A・B 相当の 11 束を書いた（起動と挨拶 16／会話 39／撫で 27／メニュー 69／終了 4／自発発話 8／名前の記憶 22／更新 39／インストール 36／切替 26／消滅 9＝**構成 id 延べ 295**・互いに素・機械 108＋人手 187）。`linkage.md` は 242 → **1,032 行**。
- 3.2: **⚠ `linkage.md` は最終的に 3,000 行を超える見込み**（3.3〜3.6 が段階 C・D・E の束と関連 0 本 約 1,127 件と単独項目を同じファイルへ足す）。分割は設計 D-1 が明示的に却下している。1 束あたり `members` と `hand` を 1 id 1 行で二重に並べる書き方が行数の主因で、`hand` を 1 行に詰めれば約 4 割減らせる。**3.3 以降の担当は書き方を統一すること**（途中で変えると読みにくい）。
- 3.2: **⚠ タスク 3.3／3.4 への申し送り。** **重なり順の 3 id**（`descript_shell:seriko.zorder…`・`\![set,zorder,…]`・`currentghost.seriko.zorder`）はまだ名前付き束が無い。**3.4 が終わった時点で名前付き束が付いたことを確かめること**（要件 3.1 は機械の束 1 つでも満たすが、例示 3 連鎖なので名前が要る）。
- 3.2: makoto 束 53 件のうち 3.2 が取ったのは **44**（更新 30・インストール 9・切替 3・メニュー 2）。残り 9 件——`descript_ghost:makoto`・`manual_translator`（トランスレータ・**3.4**）／`dev_nar`・`\![execute,createnar]`・`OnNarCreated`・`OnNarCreating`（開発者機能・**3.4**）／`manual_balloon`・`manual_directory`・`manual_ghost`（単独項目・**3.6**）。
- 3.2: 推量で入れずに **3.5 へ回した id**: `OnCacheRestore`・`OnCacheSuspend`・`\4`・`\5`・`\6`（いずれも台帳の `links` 0 本）。`\-` は備考の逐語に基づき「終了」へ入れた。
- 3.2: **⚠ 判定 ⑶-a（構成 id が互いに素）はまだ機械化されていない**（重複 id を仕込んでも赤にならない）。**タスク 3.8 の持ち場。** 3.2 時点では手作業の走査で 0 重複を確認しただけ。
- 3.3: 段階 C 相当の 8 束を書いた（スリープ復帰・バッテリー・スクリーンセーバー・フルスクリーン退避・最小化・ディスプレイ変化・サウンド・環境の照会＝構成 id 80 件）。`linkage.md` 1,032 → **1,368 行**。累計 **19 束・構成 id 375 件**（既存 295 との重なり 0）。
- 3.3: **⚠ タスク 3.5／3.6 への申し送り（最重要の数）。** 対象 4 状態 **1,552 件**のうち **未帰属は 1,177 件**（1,552 − 375）。そのうち「察知」形の SHIORI イベント **27 件**＋`\![set,tasktrayicon,…]` 1 件 ＝ **28 件**が段階 C の初期配置に名前が無いまま残っている（`OnCPULoadHigh`／`Low`・`OnDarkTheme`・`OnDeviceArrival`／`Remove`・`OnLanguageChange`・`OnMediaPlayerOpen`・`OnMemoryLoadHigh`／`Low`・`OnMusicPlay`／`Ex`・`OnNetworkStatusChange`・`OnOffscreen`・`OnRecycleBinEmpty`／`EmptyFromOther`／`StatusUpdate`・`OnSessionDisconnect`／`Lock`／`Reconnect`／`Unlock`・`OnSystemDialog`／`Cancel`・`OnTabletMode`・`OnTrayBalloonClick`／`Timeout`・`OnVirtualDesktopChanged`・`OnWallpaperChange`）。**3.5 のレビューでこの 28 件が束か単独項目に着地したかを名指しで突き合わせること。**
- 3.3: **⚠ 3.5 へ回した id**（3.2 の 5 件に加えて）: `OnCacheSuspend`・`OnCacheRestore`（ukadoc の逐語「ゴーストキャッシュに入った／出た際に発生」＝ゴースト入れ替えの退避であって電源・画面・音ではない）／`property.get`・`property.set`（ukadoc 2.7.85 の `activeghostlist(…).ext.拡張プロパティ名` の口＝多重ゴースト側の機構）。
- 3.3: `サウンド`（33 件）と`環境の照会`（28 件）は **テーマ 0 件**（構成 id の `values` の和集合が空。台帳から全数読んで確認済み）。壊れ方はどちらも「黙って壊れる」なので要件 5.4 の規則 ⑶（段階 E 候補）には掛からないが、**4.2／4.3 は要件 5.5 の裁定候補に当たるかを見ること。**
- 3.3: `foundation` を意図的に共有した——「OS の状態変化の通知を受け取って SHIORI へ送る経路」4 束・「ゴースト窓の最小化と復帰の状態遷移とその通知」2 束。**基盤共有度（要件 6.5 ⑷）がここで初めて 1 を超える。**
- 3.3: 3.2 が書いた「例示の 3 連鎖」節の 1 行を陳腐化のため直した（`上の 11 束` → `上の 19 束`・担当タスクの記述）。**3.4 も同じ行を直す必要がある**（束の数が増えるため）。
- 3.4: 段階 D 相当 7 束＋段階 E 相当 4 束を書いた。累計 **30 束・構成 id 延べ 621 件**（互いに素）。`linkage.md` 1,368 → **2,122 行**。未帰属は **931 件**（対象 4 状態 1,552 − 621）。
- 3.4: **⚠⚠ タスク 3.5 の表題の「1,127 件」は着手時 main の数。今の台帳では 1,126 件。** 減った 1 件は `ukadoc:descript_shell:seriko.zorder…` で、段 1 が `configures` を 1 本足して端が付いたため。**3.5 の完了判定は 1,127 ではなく 1,126 を基準に数え直すこと。** 出向き 0 本・全状態の方も 1,494 → 1,492（段 1 が `links` を書き足した 3 行のうち、それまで空だった 2 行のぶん）。
- 3.4: **親コントローラの裁定（2026-09-12）**: 要件 4.2 は「関連 0 本」を 2 通りの定義で並べている（出向き 0 本・全状態 ＝ 1,492／無向 0 本・対象 4 状態 ＝ 1,126）。**本 spec では「関連 0 本」＝ 無向 0 本・対象 4 状態 と読む**（tasks.md 3.5 の表題の数が無向側だから）。`linkage.md` に定義を明記済み。
- 3.4: **⚠ タスク 3.5 への申し送り（最重要）。** **重なり順の 4 件**（`descript_shell:seriko.zorder…` 端 1／`\![set,zorder,…]` 端 1／`currentghost.seriko.zorder` 端 3／`\![reset,zorder]` 端 0）を **1 つの名前付き束にまとめること**。3.5 の母集団（無向 0 本）に入るのは `\![reset,zorder]` の 1 件だけだが、残る 3 件も機械の束 `ukadoc:descript_shell:char_2a.menu_2cauto_307e_305f_306fhidden:1` の構成 id として引き渡されている。**3.6 の単独項目へ落とすと要件 3.1 が例示した重なり順の連鎖が 4 つに割れる。** 詳細は `linkage.md` の「このタスクへ回された id の始末」⑶。
- 3.4: **⚠ design.md D-7 の丸括弧の数字は要件番号であってタスク番号ではない**（「`links` は削らない（3.3）」「名前付き束へは人手で入れる（3.6）」）。design が書かれた時点で tasks.md はまだ生成されていない。**設計を読むときに取り違えないこと。**
- 3.4: 3.5 へ回した id（3.2 の 5 件・3.3 の 4 件に加えて）: `hwnd`・`%lastghostname`（正典が FMO／SSTP／多重ゴーストのどれとも結び付けていない）／`\![effect,…]`・`\![effect2,…]`・`\![filter,…]`（正典「サーフェスをプラグインにより変化させる。」＝絵の加工で PLUGIN/2.0 の相手ではない）／ごみ箱・壁紙・書庫・スケジュール系（「外部アプリ」束の粒度の裁定が要る）。
- 3.4: **⚠ ヘッドライン束は brief の前提（テーマ 0）と食い違う**（26 件中 3 件が「装い」を持つ）。配置は変えず事実を id 付きで書き、**引受先を `briefing.md`＝タスク 4.2 と名指し済み**。4.2 のレビューで落ちていないか確かめること。
- 3.4: `サウンド`・`環境の照会`（3.3）に加え、3.4 の束にもテーマ 0 件のものがありうる。**4.2／4.3 は要件 5.4 の規則 ⑶（テーマ 0 かつ壊れ方が見た目の差以下＝段階 E 候補）の当てはめを全束で確かめること。**
- 3.4: 「段 1 が 2 つの行に `links` を書き足した」の記述は、20 行上の「補修した関連」の表（3 行）と単独で読むと食い違って見える。**次に同じ節を触るときに「3 行のうちそれまで空だった 2 行」と 1 語足すこと**（非ブロッキング）。
- 3.5: 関連 0 本の項目を人手で束へ入れた。`linkage.md` 2,152 → **4,770 行**。id を **927 件**追加（新設 33 束へ 797・既存 13 束へ 130）。**累計 63 束・帰属済み 1,548 件・未帰属 4 件**。完了状態の 3 数: 関連 0 本の対象項目 **1,126** ＝ 3.2〜3.4 が既に入れていた **415** ＋ 3.5 が入れた **710** ＋ 単独項目候補 **1**。
- 3.5: **⚠⚠ タスク 4.2 への申し送り（最重要・穴が空いている）。** 要件 5.5／tasks.md 4.2 の本文が扱うのは「台帳の根拠が 5.2 の初期配置と**食い違う**束」だけで、**5.2 に最初から名前が無い束**は文言に入っていない。**3.5 が新設した 33 束はこの隙間に落ちる。** 4.2 を字句どおり実行すると 33 束が段階を持たないまま 4.3 へ渡り、**要件 5.6（全束がちょうど 1 つの段階を持つ）が赤になる。** **段 3 の開始時に 4.2 の対象を「5.2 と食い違う束 ＋ 5.2 に無い 33 束」へ広げること。** 33 束の `foundation` は 53 通りで 6 組が共有されており、写像の規則 3 つ（テーマ 1／2 以上／0 かつ見た目の差以下）はそのまま適用できる。
- 3.5: **⚠ タスク 4.3 への申し送り。** **63 束すべての `breakage` が「黙って壊れる」**（台帳の備考から機械で裏取り済み）。**要件 6.1 の固定序列の第 1 根拠が全束で同値＝順位の弁別力が 0。** 順位は実質 ⑵ テーマ数 → ⑶ 資産の広さ → ⑷ 共有度 の 3 段で決まる。**同順位が大量に出る前提で 4.3 を組むこと**（要件 6.7 の同順位解消が効く場面が増える）。
- 3.5: 新設 33 束は逸脱ではないとレビューが認定。根拠——design D-7 が既に 5.2 に無い束（「nar の作成」「着せ替え」「配布物の構造」）を名指ししている／931 件を 30 束へ押し込むと要件 4.4 が壊れる／新設束の構成 id は中央値 15・平均 24 で既存束（2 件の束が 2 本ある）より粗い／**切れ目は実装者の発明ではなく台帳の逐語**（assets の `束: …（先に要る仕組み: X）` 542 件中 538 件・sakura-script の `[群: X]` 342 件中 286 件をそのまま境界にした。property・shiori にその欄は 0 件）。
- 3.5: 単独項目候補の引き渡し口は `### 単独項目に落とす候補` 節（行頭 `- \`id\` — 束へ入れない理由:` の 1 行 1 件形式）。候補は **`ukadoc:memo` の 1 件**（assets 1・property 0・sakura-script 0・shiori 0）。**3.6 はこれに 3.1 の表が単独項目と決めた 3 件（`manual_balloon`・`manual_directory`・`manual_ghost`）を加えて計 4 件を扱うこと。**
- 3.5: 既存 30 束で変わったのは `themes` 4 件と `domains` 2 件のみ（構成 id 追加に伴う導出欄の数え直し）。構成 id・`hand` の削除、`machine`／`foundation`／`breakage` の変更は 0 件。
- 3.5: **⚠ レビュアーが自分の検出台本の欠陥を 2 つ発掘した**——⑴ ドメイン別報告の束の表が 2 列（summary は 3 列）なのに 3 列を要求して機械の束 144 本中 69 本を取り落とし**偽の赤**を出した ⑵ 注入文字列が実在せず空振り（`### SAORI` は実際は `## SAORI`）。**報告の表の列数はファイルによって違う。後段の判定を書くときに注意すること。**
- 3.6: 単独項目 4 件（`manual_balloon`・`manual_directory`・`manual_ghost`・`memo`）を `single = true` で置き、`## 合計`・`## SAORI`・`## 別軸` を追加。`linkage.md` **4,900 行**。**対象 4 状態 1,552 件がちょうど 1 つの束か単独項目に属する（欠 0・余 0）。** `[tally]`: `target = 1552`／`from_machine = 195`／`by_hand = 1353`／`singles = 4`／`alias_excluded = 27`／`not_applicable_excluded = 170`、`singles_by_domain` assets 4・property 0・sakura-script 0・shiori 0。恒等式 3 本成立（1552 = 195+1353+4／4 = 4／1552+27+170 = 1749）。
- 3.6: **⚠⚠ タスク 3.8 への申し送り（罠・必ず読むこと）。** `tests/consistency/documents.rs` の `shift_count` は `only_occurrence` で「`鍵 = ` が本文にちょうど 1 度」を assert する。**`linkage.md` の散文に半角の `target = ` を書いた瞬間に `the_breaking_tools_do_not_touch_the_repository_files` が赤になる。** 3.6 は恒等式を**全角の ＝／＋**で書いて解消した（理由は本文に 1 文残してある）。**`briefing.md` は設計 D-2 により `singles = ` を最低 5 回持つので、`shift_count(briefing_text, "singles")` は散文の有無に関わらず必ず落ちる。** 3.8 は**摂動の錨を `[tally]`／`[stage.X]` の囲みに限る（あるいは出現位置を指定する）**よう `documents.rs` を直すこと。
- 3.6: **⚠ タスク 3.8 への申し送り。** `tests/consistency/documents_non_vacuity.rs:385-390` の doc コメントの実測（3 文書の `ukadoc:` 0 件・`linkage.md` の `members = ` 0 件）が**陳腐化した**（現状 `ukadoc:` 3,080 件・`members = ` 67 件）。**3.8 で直すこと。**
- 3.6: 単独項目の表の鍵は**項目 id そのもの**（設計 D-2 の `[[rank]]` の検査「`singles` の各 id は `single = true` の束の名前」と `RankTarget::Singles(Vec<EntryId>)` が強制する）。**人の名前を付けると 4.3 が指せなくなる。**
- 3.6: SAORI はカタログ側 0 件・台帳側 0 件（`saori` の部分一致で数えた）。成立条件 3 つ（32bit 同一プロセスの同居／作業ディレクトリ `ghost/master`／DLL 探索パス）は要件 4.5 の綴りどおり。SAORI に触れる唯一の台帳の行 `ukadoc:spec_dll` は名前付き束「SHIORI の要求と応答」の構成 id であって SAORI の実装項目ではない。
- 3.6: 「別軸」節は束にしていない（```toml 囲み無し・`rank`／`stage`／`wave` の欄 0 件）。M2 技術選定 4 つ（pasta の native x64・`IShiori` の in-proc 化・ベクトル描画・AI）。
- 3.7: 判定 ⑴（引用 id の実在・裸の `ukadoc:` 0 件・`report.md` の綴り 0 件）と判定 ⑵（機械の束 id が報告 5 本の一覧に在る）を機械化。consistency **53 → 69**。摂動 4 本・母数の下限 3 本（`MIN_LINKAGE_CITED_IDS` 1,552／`MIN_REPORT_BUNDLE_IDS` 100／`MIN_CITED_BUNDLE_IDS` 1）。レビューが 4 種の赤と下限の赤を自分の手で再現済み。
- 3.7: **⚠⚠ 報告の束の一覧は `| 束 id |` の見出しで拾うこと。** `| ukadoc:` で始まる行の総数は 150 だが束 id は **123 行・122 種**で、差の **27 行は別名の表**（sakura 20・shiori 3・assets 4）。**列数決め打ちは 48 行または 75 行を落とし、行の綴り決め打ちは別名 27 件を混ぜる**（summary は 3 列・ドメイン別は 2 列・assets.md は束の表 0 で別名 4 行だけ）。
- 3.7: **⚠ design.md「判定の一覧」⑵ の記述が実装と食い違ったまま**——design は「`| ukadoc:` で始まる行の 1 列目」と定めるが、それだと**別名 27 件が紛れて偽の緑**になる。実装は `| 束 id |` 見出し走査（判定を厳しくする向きの差・コード冒頭の doc に明示）。**最終検証（7.2／7.3）で design.md の当該行を追随させること。**
- 3.7: **⚠ タスク 3.8 への申し送り。** `doc/ukadoc-coverage/linkage.md` の「3 つの恒等式」直前の 1 文（「半角の `鍵 = 数` の形を本文に書かないのは…」）が**陳腐化した**（3.7 が `shift_count` の錨を表の見出しで絞ったので全角回避はもう不要）。**3.8 で半角へ戻すのと同時にこの 1 文を落とすこと。**
- 3.7: `shift_count(text, table, key)` は表の見出しの行から次に行頭 `[` が来るまでを探す形になった。`[tally]` の `target` も `briefing.md` の `[stage.A]`〜`[stage.E]` 5 つも 1 つずつ狙える。**表の外の同綴りには巻き込まれない。**
- 3.7: 判定 ⑵ は**地の文の「…を束 id とする」型を拾わない**（束 id はそれ自体が項目 id で綴りでは見分けられず、拾えば構成 id を束 id と名指して偽の赤になる）。今日の地の文の 3 件は 3 件とも表の列で拾えているので穴は 0。**段 4・段 5 が新しい表を足すなら見出しを「…束 id」で終わらせれば自動で判定に入る。**
- 3.7: 引用 id の実測は 1,553 だが下限は **1,552**（＝`members` の和集合＝`[tally].target`）。差の 1 件は地の文だけに現れる別名 `ukadoc:list_sakura_script:_5c7:1` で、散文の推敲で増減するため釘付けしない。
- 3.7: `briefing.md`・`roadmap-draft.md` の引用 id は今も 0 件なので下限を置いていない。**引受先は段 4・段 5。**
- 3.8: 判定 ⑶（帰属の分割）を 7 つの腕（互いに素・過不足なし・由来・除外・導出／該当なし・合計・3 恒等式）で機械化。consistency **69 → 81**。摂動 3 本・腕の較正 5 本・母数の下限 2 本（`MIN_NAMED_BUNDLES` 1／`MIN_BY_HAND` 1・実測 63／1,353）。導出は `documents::derive::derive_bundle` を呼ぶだけ（設計 D-4「導出の実体を 1 つに」）。
- 3.8: **⚠⚠ タスク 4.7 への申し送り（着手前に設計を直すこと）。** design.md の File Structure Plan（167 行）と「1,000 行の番人」（641 行）は**判定 ⑶⑷ を `linkage_checks.rs` 1 本に置く**と書いているが、**⑶ だけで 944 行**（上限 1,000 まで 56 行）。**判定 ⑷ は別ファイル（例 `briefing_checks.rs`）にするほかない。着手前に design.md の該当 2 行を直すこと。**
- 3.8: 台帳 4 本の状態の内訳（2026-09-12 実測）: 全 1,749 件 ＝ implemented 88／vocabulary-only 440／degraded 22／absent 1,002／alias 27／not-applicable 170。対象 4 状態 **1,552**。
- 3.8: **63 束すべての `breakage` が「黙って壊れる」なので「該当なし」の腕は実データで母数 0。** 恒真を避けるため、写しで 1 束を「該当なし」にして 7 件の赤が出ることを較正し、さらに**テスト冒頭の `assert!(全束 != NotApplicable, ...)` が「実データに『該当なし』の束が 1 束でも増えたら赤にする」見張り**を置いてある。**この構造を後続タスクで壊さないこと。**
- 3.8: 3.7 の申し送りを消化——`linkage.md` の恒等式を半角へ戻し、陳腐化した 1 文を削除（4 挿入 5 削除）。`shift_count` の錨が表名に絞られているので散文の半角 `=` は届かない。
- 3.8: `documents_checks.rs` に `bundle_rows_in_report`（束 id ＋最終列の構成 id）を切り出した（腕 c が要る「機械の束の構成 id」の走査を報告の表の読み方 1 つに寄せるため）。`bundle_ids_in_report` の振る舞いは不変（123 行・122 種・別名 27 行不採取をレビューが数え直して確認）。
- 3.8: 摂動 3 と腕 g の失敗本文は id でも束名でもなく**鍵**（`[tally] の singles`／`singles_by_domain`）を名指す。⑶-f は数の主張なので許容とレビューが裁定。
- 4.1: テンプレート辞書 2 本を公式配布元から取得して静的に読み、`briefing.md` §5 に `[[template]]` 2 行を書いた。**配布物はリポジトリに入れず読了後に削除・実走 0 回。** 取得先——里々「ポストと狛犬」`http://ukgk.s34.xrea.com/poskoma/post124.zip`（作者サイト）／YAYA「はろーYAYAわーるど」`https://github.com/YAYA-shiori/konnoyayame/releases/download/23584055353/yayame.nar`（整備班が名指す Releases）。`fallback = false`（要件 6.8 の退路は使っていない）。
- 4.1: **写した id の和集合は 216 件**（里々 122・ヤヤ 188・重なり 94）。全件カタログに実在（レビューが独立に照合）。**タスク 4.3 が使う値は 束の `assets` ＝ `members ∩ (216 件)`。**
- 4.1: **⚠ タスク 4.3 への申し送り（本文を 1〜2 文直すこと）。** ⑴ ヤヤの「族で当てた 2 件」の件数が縮約規則の再構成と 1 件合わない（`\c` と `\C` を大小同一と見た突き合わせが 1 件混じっている疑い）。⑵ **「語彙 41 → 項目 60」の増分の主因は族ではなく「同一ページの複数の題が引数を落とすと同じ綴りになる」こと**（`\_b`×6・`\q`×6・`\b`×2・`\i`×2・`\n`×2・`\p`×2・`\s`×2・`\_s`×2）。本文の理由の列挙がこの主因を挙げていない。**どちらも下流の `assets` に影響しないが、4.3 で本文を触るときに直すこと。**
- 4.1: 里々テンプレートは**プロパティを 1 件も使っていない**（写せた 0 件・拾った 6 件はすべて SHIORI 資源名かファイル名）。ヤヤは 3 件のみ。**根拠 ⑶ は実質「イベント＋タグ＋descript キー」の広さになる。**
- 4.1: 写せなかった語彙 里々 11・ヤヤ 94。ヤヤの 94 件のうち **63 件はテンプレート内部の自前イベント名**（`On_name` 等・正典の語彙ではない）、**29 件は辞書の文字列中の逃がし文字やファイルの通り道**（`\[`・`\ghost` 等）の誤検出。
- 4.2: `briefing.md` §2（段階の定義と写像の規則）を書いた。**67 束すべてに段階が付いた（取りこぼし 0）。内訳 A 25・B 12・C 14・D 7・E 9 ＝ 67**（レビューが独立に数え直して一致）。**要件 5.2 に名前がある 30 束の配置は 1 つも動かしていない。** 規則 ⑶（テーマ 0 かつ見た目の差以下）に掛かる束は **0**（67 束すべての `breakage` が「黙って壊れる」ため）。
- 4.2: **⚠ タスク 4.3 への引き渡し値**: 段階ごとの束の数 A 25・B 12・C 14・D 7・E 9。**ただし裁定候補 10 件が動けばこの内訳も動く**（うち 6 件は「仮の段階」）。
- 4.2: **⚠⚠ タスク 6.3（申し送りの処分台帳）への申し送り。** `briefing.md` §2-5 の**裁定候補 10 件が §6 の処分台帳に未登記**。**6.3 でこの 10 件を拾うこと。** 内訳——⑴ メニュー束の段階のまたがり ⑵ ヘッドラインのテーマが brief の前提と食い違う ⑶ サウンドと環境の照会のテーマ 0 ⑷ **「投げ込み」の移動理由が成り立たない**（brief は「触れ合い＋更新の 2 テーマ」と書くが構成 id 10 件はすべて「触れ合い」だけで「更新」を持つ id は 0 件）⑸〜⑽ 節目の判定が割れた 6 束（キーとゲームパッド・好感度の絵柄・作り付けの窓・薦める場所・通知領域・読み上げと聞き取り）に**仮の段階**を付けたもの。
- 4.2: **⚠ tasks.md の申し送りの件数が陳腐化している**——サウンド 33 → **34**・ヘッドライン 26 → **29**（環境の照会 28 は一致）。**4.3 がこの数を引き写さないこと。その場で数え直すこと。**
- 4.2: 要件の明文ではない判断が 2 件（本文に理由あり）——⑴ 「基準は、初期配置がある束は初期配置、名前が無い束は 2-1 の節目」という基準の置き方 ⑵ 「一覧と汎用プロパティの照会」を `foundation` の同一性を理由に段階 C の末尾へ並べたこと。**開発者へ上げる価値がある（完了報告の裁定候補一覧に載せること）。**
- 4.2: 本文の細かな誤り 2 件（非ブロッキング・次に §2 を触るときに直す）——⑴ 2-2 の「数え方: 表の 5 行の中黒で区切られた名前を数えた」を字句どおり実行すると 32 になる（括弧内の中黒を拾う。値 30 は正しい）⑵ 2-4 冒頭の「差の 37」の引き算が 2 つのずれを混ぜている（結果は正しく、同じ節の末尾で正しく分解されている）。
- 4.2: 67 束すべての `themes` が台帳 `values` の和集合と一致（不一致 0 件・レビューが確認）。
- 4.3: `briefing.md` §3（順位表と段階ごとの件数）を書いた。`[[rank]]` **66 行**（`bundle` 63＋`singles` 3）で **67 の束と単独項目がちょうど 1 度ずつ**。段階ごとの数 **A(25,0,874) B(9,3,210) C(14,0,169) D(7,0,133) E(8,1,166)**、**items の和 1,552** が `[tally].target` と一致。同順位 13 組・29 束。根拠不足 **0 束**・`insufficient` **0 行**。`assets`・`shared` は `derive` の導出と全 66 行一致（レビューが独立に再計算）。
- 4.3: `override` は **2 行**（どちらも `kind = "stage-rule"`, `ref = "要件 5.3"`）——「更新」を段階 B の先頭へ（根拠の順では 4 番目）、「環境の照会」を段階 C の末尾へ（根拠の順では 9 番目）。
- 4.3: **⚠⚠ タスク 4.7（判定 ⑷ の実装）への申し送り。** **段階 B は `rank` 1 の行が 2 行ある**（「インストール」＝対象行・「更新」＝`override` 行）。判定 ⑷-g は「段階 B の `rank` 1 の束の `themes` に『更新』が含まれる」だが、**どちらの行を採っても真になる**（インストールの `themes` にも「更新」がある）。**「rank 1 の行が複数ある」ことを前提に、「いずれか／すべて」を明示して実装すること。**
- 4.3: 「一覧と汎用プロパティの照会」の `members` 37 件に `system.` を含む id は **0 件**（台帳全体で 25 件あり全部「環境の照会」に属する）。よって要件 5.3 の対象外として印を付けず根拠の順（8 位）に置いた。
- 4.3: 段階が仮の 6 行は TOML コメント `# 段階は仮（2-5 の裁定候補 N）` で注記。**裁定で段階が動けば、動いた先の段階の順位表を組み直す必要がある。**
- 4.3: **⚠ 要件 6.5 の「数えた束の名前を書く」の解釈**——`briefing.md` は `shared` の数だけを持ち、名前は `linkage.md` の `foundation` の綴りで引く形にした（「同じ値を 2 か所に書かない」に従った結果）。**最終検証（7.2／7.3）で解釈を 1 度確認すること。**
- 4.3: 4.3 の実装者は `priority-apply` を実際に走らせて台帳を書き換え、事前の写しで戻した。**レビューが md5 4 本一致と `git diff` の空を確認済み。台帳の書き戻しはタスク 4.4 の担当。**
- 4.4: 台帳 4 本へ確定値を書き戻した（`priority` 行 **1,445 本**のみ変更・他の欄は 1 バイトも動いていない）。対象 4 状態 **1,552 件**に「段階 1 文字＋順位」が入り、別名 27・対象外 170 は空。**2 回目以降は「変更 0 項目」**（冪等）。後分布 **A 874・B 210・C 169・D 133・E 166**（和 1,552）・空 197。**報告 5 本は副手続きを走らせたが内容差分 0 本**（報告は `priority` 欄を載せない）。
- 4.4: **⚠ 親コントローラの前提が誤っていた**——「書き戻し前は全項目の `priority` が空」ではなく、4 本の仮置きで埋まっており空は 240 件だけだった。前分布 shiori A58/B61/C180/D160/E25/空193・sakura-script C301/空41・assets A112/B92/C103/D127/E104/空4・property C186/空2。**要件 7.3 が求める「4 本の作り方の違いの解消」はまさにこの非自明な前分布が 1 つの作り方に揃うことで示される。**
- 4.4: **⚠⚠ タスク 6.3 への申し送り（`briefing.md` §6 の HTML コメント内に記録済み）。** 書き戻しで **`ledger/shiori.toml` の冒頭コメント**（`briefing-shiori.md`「群の索引」の写し）の優先度に関する記述が事実でなくなった——⑴ 「484 記入済み／193 空」→ 実測 **505／172** ⑵ 空の内訳から `implemented` 21 が消え **`alias` 3・`not-applicable` 169 の 172 だけ** ⑶ 「優先度 `""` の 7 つの群」のうち**群 1（送出しているイベント）はもう空ではない** ⑷ 「仮置き」に触れる行 **14 行**（うち 9 行が群ごとの既定値 `C1`・`C2`・`D1`・`D2`・`E1` を名指し・2 行が「テーマから決まる」）。**正本は `briefing-shiori.md` で要件 12.3 が編集を禁じ、要件 12.2 が台帳の接触を `priority`・`owner` に限るので本 spec では直さない。6.3 で処分を付けること。6.3 の拾い方（4 つの語の検索）ではこの件は掛からないので、§6 の囲みの中の記録を必ず読むこと。**
- 4.4: **⚠ タスク 5.3／6.x への申し送り。** `README.md` の「仮置き」に触れる記述（欄の定義の表の `priority` の行と「段階（A〜E）の最終決定はここでは行わない」節・`grep -c` では 3 行）は、**要件 9.1 が第一段の順位を「草案」と位置づけているので第二段（タスク 5.1〜5.3）の完了後に見直す**。要件 12.4 が README の編集を 11.2 まわりに限っているので 4.4 では触っていない。
- 4.4: **赤 0 件**（書き戻し後の `cargo test` は 600/6/81/5 緑）。ただしこの 0 は**判定 ⑷（4.7）と判定 ⑹（4.8）が未実装**の状態での 0 で、段階と順位の正しさの証拠ではない。**ただしレビューが 1,552 件を `linkage.md` の `members` と `briefing.md` の `[[rank]]` から独立に組み立てて全数照合し不一致 0 を確認済み。**
- 4.5: `owner` 欄を要件 7.4 の 4 規則で整えた。規則 ⑴ 進行中 spec 宛て **372 件・13 spec** → 変更 **0**／規則 ⑵ 完了済み spec 宛て **75 件** → 保った **41**（実装済み 33・縮退 8）・空にした **34**（未対応 32・語彙のみ 2）／規則 ⑶ brief の無い候補 spec 名 → 前も後も **0**／合計 **34**。整理後の非空 **413**・空 **1,336**。`[[owner_completed]]` は **14 行**（和 41）。**変更は `ledger/assets.toml` と `briefing.md` の 2 本のみ。`priority` の変更行 0。報告 5 本は差分 0**（報告は `owner` 欄を載せない）。
- 4.5: 要件 12.2 が限定するのは `crates/ukadoc-survey` への接触で、`note` への書き足しは禁じていない（設計 D-4 が `owner`／`note`／`links` を手編集と定める）。**`note` の編集件数の報告は要件 12.7 ⑵ が求める——完了報告（7.3）で忘れないこと。**
- 4.5: **⚠ タスク 6.6 への申し送り。** 判定 ⑸-c（`owner_count` が数え直しと一致）・⑸-e（列挙した spec を `owner` に持つ項目の状態が `implemented`／`degraded` だけ）・⑸-f（非空の `owner` はすべて `[[spec]]` か `[[owner_completed]]` の名前）は**まだ Rust の標準テストに入っていない**。**6.6 が実装すること**（tasks.md 275 行付近に実在確認済み）。
- 4.5: 空にした 34 件の備考には元の「担当 spec は areka-P0-… 。」が残る（要件 7.5 が削除を禁じる）。**足した 1 行が同じ spec 名を名指しして「完了して封じられた spec で引き受けられない」「上の記述は当時の経緯として消さずに置く」と打ち消している。** レビューが 3 件読んで打ち消しの成立を確認済み。
- 4.5: 全件空になって列挙から落ちた完了済み spec は 2 本——`areka-P0-window-placement`（16 件）・`areka-P0-emo-atlas`（4 件）。
- 4.5: spec の数の実測（2026-09-13）: `.kiro/specs/` 直下 28 ディレクトリ全部が `brief.md` を持ち、自 spec を除いて **27**。`completed/` は 175 エントリ中 1 つが `.md` ファイルなのでディレクトリ **174**。
- 4.6: `briefing.md` §4（段階 A の主障壁・`[[barrier]]` 6 行）と §5（根拠表への参照）を書いた。**8 つの節が揃った**（§1 着手条件／§2 段階の定義と規則／§3 順序付き束一覧／§4 主障壁／§5 根拠表／§6 処分台帳／§7 第二段の改訂記録／§8 是正候補への参照）。テーマ別・SSP 世代別は参照で指し、**数の写しは 0 件**。
- 4.6: **⚠⚠ タスク 4.7 への申し送り（最重要）。** **`[[barrier]]` の 6 行は「ページの全項目」の分布（合計 924）であって「段階 A の項目だけ」（874）ではない。** 根拠 3 点——要件 8.1 ⑷ の「着手時の実測」9 の数がページの全項目の合計・`documents/mod.rs` の `Barrier` の doc が「ページ 1 つの状態分布」・design D-2 の注記も「ページ別の状態分布」。**判定 ⑷-f はページの全項目で数えること。段階 A の 874 件で数えると 6 行とも赤になる。** 段階 A の項目数は `[stage.A].items = 874` が別に持っている。
- 4.6: `[[barrier]]` の実測（レビューが独立に数え直して全一致・implemented/vocabulary_only/degraded/absent/alias/not_applicable）: `list_shiori_event` 11/3/0/273/3/0（290）・`list_shiori_resource` 1/158/0/0/0/0（159）・`descript_ghost` 7/1/0/66/0/0（74）・`descript_balloon` 20/5/4/133/0/0（162）・`descript_shell` 11/2/0/89/0/0（102）・`descript_shell_surfaces` 4/57/4/68/4/0（137）。
- 4.6: **⚠ 判定 ⑴ の罠。** 初稿で §5 に **`report.md` の綴りを逐語で書いた**ら consistency が 3 本赤になった（「実在しない報告のファイル名」）。**「実在しない綴り」に言及するときは綴りそのものを書かないこと。**
- 4.6: 文書冒頭の「まだ 1 行も無い表（主障壁・完了済み spec 宛ての宛先）は書いていない・読み手は『その表が無い』を『0 行』として扱う」の段落を削除した（4.5 と 4.6 で両方とも行を持ったので偽になった）。**design D-2 の 7 種の表は今すべて行を持つ。**
- 4.6: 台帳の `not-applicable` 170 件の所在は 3 ページだけ——`list_shiori_event_ex` 168・`memo_shiorievent` 1・`list_sakura_script` 1。`[priority_blank].not_applicable = 170` と一致。
- 4.7: 判定 ⑷（段階と順位）を腕 a〜g で機械化。consistency **81 → 94**。摂動 3 本（束名を消す／件数を 1 ずらす／優先度を 1 件書き換える）＋腕の較正 7 本＋母数の下限（`MIN_RANK_ROWS`・`MIN_STAGE_A_ITEMS`・新設 `MIN_BRIEFING_CITED_IDS = 100`）。導出は `documents::derive` を呼ぶだけ（D-4）。腕は `briefing_arms.rs`（736 行・テスト本体なし）、テスト本体は `briefing_checks.rs`（541 行）に分けた（1 本だと 1,207 行で制限超過）。**design.md の ⑷ の帰属を全数是正済み（`grep` で残 0）。**
- 4.7: 腕 f（`[[barrier]]`）は**ページの全項目**で数えている（段階 A の 874 ではない）。腕 g は**「いずれか」で判定**（設計 ⑷-g の逐語に合致とレビューが認定）。ただし**「更新」束が rank 2 へ落ちても「インストール」が「更新」テーマを持つ間は緑のままという盲点**があり doc に明記済み。
- 4.7: `carryover_headings()` を遅延化（`OnceCell`）——`override` の `second-stage` の腕に入ったときだけ `.kiro/specs/completed/areka-P0-emo2-conformance-e2e/verification/m1-completion.md` を読む。**実データに `second-stage` の行が 0 行の間はよその spec の文書を 1 度も開かない。** 効果の実測: 同文書を消すと落ちるのは受け付け形の較正 1 本だけ（遅延化前は 5 本が落ちた）。
- 4.7: **⚠ タスク 7.1 への申し送り。** `documents_non_vacuity.rs` の 789 行付近の段落は**2 度陳腐化した**（初版は骨組みのころ・2026-09-12 の版は段 1 のまま・どちらも `briefing.md` を 0 件と書いていた）。**直し方は「判定を足す」ことではなく「生の数を段落から消して『腕 b が数え直す』『`MIN_BRIEFING_CITED_IDS` が見張る』と欄名で指すこと」**（設計 D-1「数の置き方の規則」と同じ流儀・足すのでなく減らす）。**7.1 で採り直すか消すかを決めること。**
- 4.7: 実測（2026-09-13・レビューが独立に採り直して全一致）: `briefing.md` `ukadoc:` 337／引用符付き 延べ 314・異なり 220／`cited_ids` **237**／`bundle = ` 63／`[[rank]]` 66 行（`singles` 3 行）。`linkage.md` `ukadoc:` 3,135／`cited_ids` 1,553／`members = ` 67。`roadmap-draft.md` すべて 0。
- 4.7: `MIN_BRIEFING_CITED_IDS = 100`（実測 237 に対して緩め）。**理由: `briefing.md` は段 5（6.1〜6.5）でまだ増減するので実測に密着させると赤くなる。** 兄弟の `MIN_LINKAGE_CITED_IDS = 1_552` が密着なのは `linkage.md` が段 3 で書き終わっているから。
- 4.8: 判定 ⑹（全体報告の新しさ）を `documents_checks.rs` に置いた。`report/summary.md` の本文（復帰文字を落としたもの）が `render_summary` の出力と**全文一致**することを主張。consistency **94 → 101**。摂動 1 本＋道具の較正 3 本＋母数の下限（`MIN_SUMMARY_BUNDLE_ROWS = 1`・実測 75）。**報告を手で 1 文字変えると赤・戻すと緑**をレビューが独立に再現（数字でない散文の 1 文字でも赤になる）。
- 4.8: **⚠⚠ タスク 6.5（README の限定編集）への申し送り。** 要件 11.2 は「除外を覆す旨を **`README.md` の表と本 spec の文書に書く**」と両方を求めるが、README は 6.5 の境界。**`README.md` にはいまも「全体報告は…常時の検査に入っていない」「常時の検査には入っていないので何も失敗しない」が 264・427・430・466〜467 行付近に残り、4.8 で事実でなくなった。** 6.5 の完了状態が「README に『常時の検査に入っていない』と述べる文が残っていない」なので消化可能。**あわせて 1.5 の申し送り（「副手続きは 8 つ」→ 9 つ）と 1.6 の申し送り（「⚠ 全体報告は黙って古くなる」節が丸ごと事実でない・design の README 指示自体が陳腐化）も 6.5 で処理すること。**
- 4.8: **⚠ タスク 6.3 への申し送り。** 要件 11.2 と design D-5 は証拠の表の覆しを「本 spec の**処分台帳**」（`briefing.md` §6）に書けと名指すが、§6 はまだ 6.3 の骨組みなので**本文は §5 の新しい小節に書いた**。**6.3 は §5 を参照する形にして二重に書かないこと。**
- 4.8: `MIN_SUMMARY_BUNDLE_ROWS = 1`（実測 75）は design「判定の一覧」⑹ とタスク本文が逐語で「1 行以上」と定めているのでそのまま。「1 行だけ吐く壊れ方」は判定 ⑹ 本体（全文一致）が先に赤くする。
- 4.8: **⚠ タスク 6.6 への申し送り。** `documents_non_vacuity.rs` が **976 行**（1,000 まで残り 24 行）。**判定 ⑸ の母数を足す前にファイルを分けること。**
- 4.8: 摂動の道具 `shift_row_count`（行頭が `| ラベル |` の行の右端の数を 1 増やす）を `documents.rs` に追加。狙いが 1 行でなければ止まる。
- 5.1: `briefing.md` §7（第二段の改訂記録）を書いた。**適合検証 20 項目と持ち越し 8 行の 28 行すべてに判断が付き、動かす行は 0 行。** 要件 9.3 の逐語「いずれも順位を動かす根拠にならなかった」を明記し、確かめた項目番号 20 件と持ち越し見出し 8 件を省略なく列挙。起票済み 4 件（`present-gpu-transform-scale` W13・`kanade-boot-talkdone-drop` W13・`host32-window-thread-pump` W13・`dpi-transition-two-tick-bounce` W14）に「次段の spec 表に載せる」印を付け、**新たな束は 0 束**。
- 5.1: 「0 行」が真面目な読みの結果であることをレビューが独立に検証済み——項目名 20/20 逐語一致・持ち越し見出し 8/8 一致・抜き取り 11 行が `acceptance-record.md` §8.1 の実測と行ごとに異なる形で一致。**構造的な理由**: 順位の 4 軸のうち ⑵`themes`・⑶`assets`・⑷`shared` は実機走行が機械的に動かす経路を持たず、⑴`breakage` は **67 束すべてが「黙って壊れる」で同値なのでそもそも順位を分けない。**
- 5.1: **⚠⚠ タスク 5.3 への申し送り（宙に浮いている・必ず処理すること）。** **design D-11 の第 1 項「『第一段＝草案』を `briefing.md` の順位の節（§3）の冒頭に明記する」が未達**（§3 冒頭に「草案」の語は 0 件）。5.1 の境界は §7 のみで直せず、**タスク 5.3 の箇条書き 2 本もこれを含まないので、放っておくと誰にも拾われない。** **5.3 で §3 の冒頭（3-1 の直前）へ「第一段の順位は草案である」を 1 行足すこと。** あわせて §3-4 の「第二段の改訂の印は**まだ** 0 行である」の「まだ」を落とすかも同じ場で決めること。
- 5.1: **⚠ タスク 5.3 への申し送り。** §7 末尾の HTML コメント（「段 4 の残り: タスク 5.2 …と 5.3 …が続く」）を **5.3 の完了時に消すこと。**
- 5.1: **⚠ タスク 5.2 への申し送り。** 5.2 の成果物（段階 A の温度感・一般化で壊れる項目の id 列挙）の**置き場が spec に明示されていない**。**作業指示の側で節番号を決めて渡すこと。**
- 5.1: 5.3 が書き換える順位の行は **0 行**・`override` の追加も **0 行**なので、5.3 の実質は「§3 冒頭の 1 行追加＋書き戻しと報告の作り直しを回して緑を確かめる」だけになる。
- 5.2: `briefing.md` §7-7 に段階 A の温度感（「当面 emo2 が動けばよい」）と一般化で壊れる項目を書いた。**テンプレート辞書の語彙 216 ∩ 段階 A の構成 id 874 ＝ 105 件**、うち **未対応 67・語彙のみ 6・縮退 2 ＝ 75 件を id で全列挙**（実装済み 30・別名 0・対象外 0）。テンプレート別は里々 30・ヤヤ 74・両方 29。段階 A の 25 束のうち **16 束に散らばり 9 束が 0**（9 束の名前も列挙）。要件 9.6 の切り替え方針を書き、**切り替え先は 0 件**（「どっとさくら」は候補であって要望ではないので別行）。レビューが全数を独立に数え直して一致。
- 5.2: **⚠⚠ 最終検証（7.2／7.3）への申し送り＝開発者裁定候補。** **§7-7 の数（67／6／2／75／30）を見張る常設の判定が無い。** 要件 11.1 の判定は ⑴〜⑹ で閉じており 9.5 用の判定を求めておらず、**要件 12.2 が `crates/ukadoc-survey` への接触を「要件 11 の判定」と「要件 7 の書き戻し」に限っているので、7 番目の判定を足すこと自体が境界違反になる。** 既存の判定 ⑴ は id の実在を、⑷ は段階ごとの束数・項目数を見張るが、**台帳の `status` が動いたときに §7-7 の数が古びることはどの判定も検知しない。** 引受先は本 spec 内では名指しできず、**要件 11.1 に ⑺ を足す開発者裁定が要る。完了報告の裁定候補一覧に載せること。**
- 5.2: 本文に `216`・`874`・`25` のリテラルは 0 件（欄の名前で指し、16＋9 の表で表した。設計 D-8 の「数は骨組みに重ねて書かない」に従った）。
- 5.3: 動かす行 0 行なので順位表の書き換えは発生せず、実質は 4 件——⑴ **design D-11 第 1 項の充足**（§3 冒頭に「この節の順位は第一段の草案である（要件 9.1）」を追加）⑵ §3-4 の「まだ」を落として「7-5 の判断で確定した値である」に改め、§7-5 の引用も同じ綴りへ追随 ⑶ §7 末尾の工程コメントを除去（§8 の段 5 用コメントは残置）⑷ 書き戻しと報告 5 本の作り直しを再実行（`priority-apply` 4 本とも「変更 0 項目」・報告の内容差分 0）。判定 ⑷ の 11 本を含め consistency 101 緑。
- 5.3: 一致の確認（レビューが独立に再現）——§7 の判断表 **28 行**すべて「いいえ」・「はい」0 件、§3 の囲みの `second-stage` **0 行**・`override` **2 行**（どちらも `stage-rule`）。
- 5.3: **⚠⚠ 親コントローラの裁定（2026-09-13）＝タスク 6.5 の 5 か所目。** 4.4 の申し送り「`README.md` の『仮置き』を第二段の完了後に見直す」の引き金が 5.3 の着地で引かれたが、**引受先のタスク 6.5 は要件 12.4 と 6.5 の完了状態（「編集箇所が上記の 3 か所に限られている」）に阻まれてこれを拾えない。** 「仮置き」は `README.md:115`（欄の定義の表）と `:303`・`:306`（「段階（A〜E）の最終決定はここでは行わない」節）にあり、12.4 が許す 3 か所のいずれにも属さない。
  - **裁定**: 1.5 の申し送り（「副手続きは 8 つ」→ 9 つ・4 か所目）と**同じ形**で解く。**タスク 6.5 で「仮置き」の是正を 5 か所目として行い、要件 12.4・タスク 6.5 の完了状態・design D-11 の README 行に追随させること。** **完了報告（7.3）の裁定候補一覧に 1 件として載せること。**
  - **⚠ したがってタスク 6.5 は README の編集を 5 か所行う**——⑴ 11.2 の表とその直下の段落 ⑵ 「報告の扱い」「全体報告は黙って古くなる」の「常時の検査に入っていない」の文 ⑶ 「この一式に入っているもの」への 3 文書追記 ⑷ **副手続きの数（8 → 9）** ⑸ **「仮置き」3 行**。
- 5.3: **⚠ タスク 6.3 への申し送り。** `briefing.md` の §6 直前の HTML コメントが「この 2 か所は第二段（5.1〜5.3）が終わるまでは**まだ正しい**」と書いているが、**5.3 の着地でこの前提は満了した。** 6.3 は `briefing.md` を境界に持つので、**確定した引受先の綴りへ書き換えること。**
- 6.1: `roadmap-draft.md` の本体を書いた（69 → **736 行**）。段階 A 25・B 12・C 14・D 7・E 9 ＝ **67 行**（`linkage.md` の 67 束と 1 対 1）。**先頭ウェーブ ＝ 段階 A の順位 1〜6 の 6 束**（会話 46・窓の配置と重なり 126・名前の記憶 22・起動と挨拶 20・バルーンの文字 63・サーフェスアニメーション 47 ＝ **324 件**）だけが構成 id の全列挙と 3 行の要約を持つ。引用 id 328 件すべてカタログに実在。同順位 13 組・29 束（`briefing.md` §3-5 と一致・先頭ウェーブに掛かる組 0）。別軸 4 つは順位なし。M3 候補は「テーマ 8 つすべてで代表束が実装済み」。裁定候補 W-1／W-2／W-3。
- 6.1: **先頭ウェーブの切り方の規則**——「段階 A の順位表で同順位が現れる手前まで」（順位 7 で初めて同順位が出る）。**要件 10.4 は束数を定めていないので本タスクの判断**だが、規則を 2 か所に明記し理由を要件 6.7 由来で述べているのでレビューが「恣意的ではない」と認定。**棚卸セッションが別の切り方を採る余地は文書自身が残している。**
- 6.1: **⚠ 先頭ウェーブの節の状態の数は判定が数え直さない写真**（`roadmap-draft.md` の骨組みは `[briefs]`・`[[spec]]`・`[[reserved]]` の 3 表しか受け付けないので新しい囲みを足せない）。2026-09-13 の写真であること・生きた値の正本が台帳であることを本文に明記済み。**台帳が動けば黙って古びる。完了報告の申し送りに残すこと。**
- 6.1: **⚠ 候補 spec 名 63 個は案であり実在しない綴り**（既存 spec 名の再利用は `areka-P0-anchor-tag-canon` の 1 行のみ）。**判定はこれらを見張っていない。起票時に名前が変わりうる。**
- 6.1: **⚠ タスク 6.2 への申し送り。** `[briefs].count`・`snapshot_on`・`[[spec]]`・`[[reserved]]`・是正候補の 3 列表は 6.2 の持ち物で、6.1 は触っていない。**1.1 が置いた 6.2 宛ての HTML コメント 3 つ（既存 brief の位置づけ・M2 予約群・是正候補）が残っている。**
- 6.1: **⚠ 裁定候補 W-2 は他の 2 件より優先度が高い**——`areka-P0-anchor-tag-canon`（W17）が持つ 1 件のために第 1 波の開始が 1 ウェーブ遅れる。
- 6.1: 非ブロッキング所見——**裁定候補 W-1 の理由に逐語の id が 1 つも無い**（要件 10.3 は「理由（id 付き）」と定める。W-2 は 1 件・W-3 は 2 件の id を名指す）。代表 id を 1〜2 件添えると逐語に完全に沿う。
- 6.2: `roadmap-draft.md` に既存 brief の位置づけと予約群の対応表を書いた（736 → **1,176 行**）。`[briefs].count = 27`・`snapshot_on = "2026-09-13"`・`[[spec]]` **27 行**（束を持つ 13・`none` 14）・`[[reserved]]` **18 行**（予約群 17 項目のうち写った 10 項目＝11 行・写らなかった 7 項目）・是正候補 **15 件**を 3 列で列挙（brief 本体は無改変）。1.1 の HTML コメント 3 つは置き換え済み（残 0）。判定 ⑸ の a〜d・f をレビューが独立に再現して全一致。
- 6.2: **⚠⚠ タスク 6.6 への申し送り 2 件。** ⑴ **判定 ⑸-a は単独では母数 0 で恒真**（着手前は `count = 0` ＝ `[[spec]]` 0 行で緑だった）。**設計の判定表が定める下限「`[[spec]]` 20 行以上・`[[owner_completed]]` 1 行以上・`completed_specs` 100 以上」を必ず併せて置くこと。** ⑵ **`[[spec]]` の `stage` は必須欄だが、宛先 0 件の 14 行には台帳由来の段階が無いので `A` を「置き字」として置いた**（本文で太字で明示・`none = true`＋`reason` つき）。`stage` を読む判定・下流は 0 なので実害は無いが、`stage = "A"` が 27 行中 23 行（本物 9・置き字 14）になり grep で段階分布を数えると誤る。**恒久策は `SpecRow.stage` を `Option<Stage>` にして `none = true` の行では欄ごと省略すること。要件 12.2 の「判定に要る読み込み」に収まるので 6.6 の境界内で可能。6.6 で裁定すること。**
- 6.2: **⚠ 親コントローラの裁定（2026-09-13）＝分割 3 本由来の宛先 42 件は移さない。** 2026-09-11 の 3 本分割で生まれた 6 本の説明書が割る前の宛先のままになっている件（`balloon-font-descript-keys` 14 件・`text-align-shadow-canon` 5 件・`balloon-lifecycle-events` 5 件・`sylphya-set-ledger` 18 件 ＝ 計 **42 件**）は、レビューが台帳で全数裏取りした実在の問題。
  - **裁定**: **移さない。** 理由——⑴ **要件 10.7 はこの種の食い違いを「spec 名・食い違う id・直し方の案の 3 列で列挙し、brief 本体は書き換えない」と定めており、surface する設計であって fix する設計ではない。** 6.2 は既に 15 件の是正候補として列挙済み。⑵ 要件 7.4 の 4 規則（4.5）は分割後継への付け替えを求めていない。⑶ 移すと 6.2 の `owner_count`（`text-decoration-canon` 32→13・`balloon-canon-residue` 26→21・`property-catalog-lists` 120→102・移った先 4 本が 0→14/5/5/18）と段階 A・C の依存表が同時に変わり、検証済みの表を作り直すことになる。**要件が求めていない作業のために検証済みの成果を崩さない。**
  - **完了報告（7.3）の裁定候補一覧に 1 件として載せること**（「分割 3 本由来の宛先 42 件を移すか、是正候補のまま各 spec の実装時に直すか」）。
- 6.2: **⚠ タスク 7.1 への申し送り。** `snapshot_on = "2026-09-13"` は design D-6・D-2 の literal（`"2026-09-11"`）と違うが、**要件 10.2「撮った日付を添える」・1.5「写した時点で数え直す」に従った側の逸脱**。**7.1 が main へ rebase して数え直すときに同じ規律で日付も打ち直すこと。**
- 6.2: 予約群は `.kiro/steering/roadmap.md`「M2 以降」の段落を「中黒と全角斜線で切る・半角斜線は名前の内側」で **17 項目**。写らなかった 4 件（`DirectSSTP`・`SAORI`・里々/YAYA・回転）はカタログに 0 ヒットであることを機械で確認済み。
- 6.2: `[[spec]]` の束は「宛先 id をいちばん多く含む束」で決めた。**先頭と 2 番目の差が 1 件しかない行が 4 行**（`currentghost-property-tree` 16 対 15・`property-query-channels` 3 対 2・`charset-canon` 2 対 1・`status-execution-states` 2 対 1）——宛先が 1 件動くと束と段階が入れ替わる。
- 6.3: `briefing.md` §6（申し送りの処分台帳）を 222 行で書いた。4 語（統合担当／裁定案／是正候補／申し送り）で 9 文書を引いて **191 行**を読み、**拾った 37・処分を付けた 37・裁定候補 12**（表の 2 ＋ §2-5 の 10）。内訳 採用 26・却下 9・裁定候補 2。仕分け 統合担当 36／検索外 1／brief の持ち主 18／調査 spec 自身 12 ＝ 67。**レビューが 9 文書を独立に引き直して 45 セル全一致・拾い漏れ 0・要件 8.3 の名指し 17 項目すべて登記・37 行全数に 5 欄・上流本文の写し 0** を確認。
- 6.3: **⚠⚠ タスク 6.4 への申し送り（必須）。** 申-21／申-22（`\![embed,…]`・`\![move]` の分担）は**処分「採用」で登記したが裁定の中身は未記載**（要件 8.6 は 6.3 の `_Requirements:` に無いので範囲外）。**6.4 がこの 2 行の下に裁定本文と該当 id の `owner`／`note` を書かないと要件 8.6 が未達のまま残る。**
- 6.3: **⚠ タスク 6.5 への申し送り。** **`README.md` の「仮置き」は実測 3 行**（欄の定義の表 1 行と「段階の最終決定はここでは行わない」節 2 行）。骨組みのコメントが言う「2 か所」は誤りだった。**6.5 は 3 行を数え直してから改めること。**
- 6.3: **⚠ 棚卸セッションへの入力（申-35）。** 三重所有の食い違いを 1 件発掘——`briefing-property.md` の案 甲は書き込み一覧の 1 行の持ち主を `areka-P0-property-query-channels` とするが、`.kiro/steering/roadmap.md`「棚卸⑬の仮裁定」1 は同じ行を `sylphya-set-ledger` と登記している。**要件 12.3 が roadmap.md の編集を禁じ 10.9 が反映を棚卸セッションへ委ねるので、食い違いを記録して該当 2 件の宛先は空のままにした。**
- 6.3: 非ブロッキング所見 2 件——⑴ **`ukadoc-survey-toolkit` の `tasks.md` の 25 行はすべて spec 内のタスク間申し送りで 37／18／12 のどの群にも登記されない**（要件 8.2 が求めるのは「4 spec の `tasks.md`」なので違反ではないが、6-1 の当たり表に toolkit 行が載る一方で仕分け表がその行方に触れていない。1 文足すと閉じる）。⑵ 数値 **172** が §5 と §6 申-37 の 2 か所に出る（片方だけ古びる形）。
- 6.3: 「10 刻み」の却下理由 3 つ（要件 7.1 が 1 からの密な順位を定める／10 刻みの利点は機械で導く作り方では要らない／数値の意味が 4 台帳で 1 つに揃った）＋「提案が誤っていたのではなく前提が変わった」の 1 段落。レビューが出典を読んで筋が通ることを確認。
- 6.4: 裁定待ち 2 件を統合担当として裁定した。**申-21 `\![embed,イベント名,r0,r1,r2...]`**——調査側の案を採る。**この項目は 2 本の着地を要し宛先の欄 1 つには収まらない**（`COMPAT_ARCHITECTURE.md` は M1 を「綴りを受け取って覚えるだけで意味は実装しない」と定め実装を `sakura-time-directives` へ申し送り／同 brief は本タグを「台本を途中で分けて進み方を組み直す必要がある」と分類）。宛先は **`areka-P0-property-query-channels`**（照会の 4 経路の 4 つ目として「`\![embed]` の対」を逐語で担当範囲に挙げ、返す側の道を作る 1 本）。**申-22 `\![move]`**——案を半分だけ採る（時間の側は `sakura-time-directives`・基準の側の `surfaces-basepos` は採らない）。宛先は **`areka-P0-sakura-time-directives`**。基準 4 語（`screen`・`primaryscreen`・`me`・`global`）はどちらの brief の範囲にも入らず、brief が無いので宛先に書かない（要件 7.4 ⑶）。
- 6.4: 台帳の変更行は**ちょうど 4 本**（`owner` 2 行・裁定行 2 行）で `priority` の変更行 **0**。報告 5 本の差分 0。
- 6.4: **⚠⚠ タスク 6.6 への申し送り（`briefing.md` §6 の表に実数付きで記録済み）。** この裁定で **`roadmap-draft.md` の 4 か所が古びた**——⑴ `[[spec]].owner_count` `areka-P0-property-query-channels` **5 → 6** ⑵ 同 `areka-P0-sakura-time-directives` **10 → 11** ⑶ 束 2「窓の配置と重なり」の依存 `sakura-time-directives`（W16）**1 件 → 2 件**（**`## 段階 A` の表の行と `## 先頭ウェーブ` の束の節の 2 か所に同じ記載**）⑷ 束 14「イベントの呼び起こし」の依存 **0 本 → 1 本**（`areka-P0-property-query-channels`・1 件）。**⑴⑵ は判定 ⑸-c が実装された時点で機械が必ず赤にする。⑶⑷ はどの判定も見張らないので 6.6 で手で直すこと。** とくに ⑷ は**明示して書いた 0 が偽になった**点で件数のずれより重い。
- 6.4: **⚠ タスク 7.1 への申し送り。** `briefing.md` §5-3 の「非空 413・空 1,336」が事実でなくなった（実測 **非空 415・空 1,334**・和 1,749）。§6 に数え直した実数と数え方を書いて食い違いは封じてあるが、**5-3 本文の是正は 7.1 の数え直しが拾うこと。**
- 6.4: **⚠ 要件 8.7（平易な語）の罠。** 是正の過程で `allowlist`・`compile`・`lowering`・`決定論檻`・`Barrier シーム`・`Scope In`・`オーケストレーター`・`非実導出`・`実導出`・`意味論` の 10 語を持ち込んで 2 度差し戻された。**上流 brief の内部様式の名前（`Scope In` 等）や設計の術語をそのまま引くと 8.7 に反する。** 「上流 4 本を読んでいない開発者の目」で全数走査（節の本文からコード span を除き、`briefing.md` の他の本文に 1 度も出ない語を列挙）するのが確実。**この走査は 7.3 の完了報告を書くときにも掛けること。**
- 6.5: `README.md` を **6 か所**直した——⑴ 「誰が何を作り直すか」の表の全体報告の行＋直下の 1 段落 ⑵ 事実でなくなった「常時の検査に入っていない」系の文（**編集前 3 件 → 0 件**）＋上流の要件を覆した旨 2 件 ⑶ 「この一式に入っているもの」への新規 3 文書（2 列目を既存行に倣い「人が書く・機械が検査する」に揃えた）⑷ 副手続き **8 → 9**（`SUBCOMMANDS` の実数と名前の `diff` 差 0）⑸ 「仮置き」**3 → 0 件** ⑹ 5 章「どちらの順でも共通に守ること」の 2 箇条（`summary.md` を含める形へ）。
- 6.5: **⚠ 要件 12.4 とタスク 6.5 の完了状態は「3 か所に限る」だが、実際は 6 か所編集した**（親コントローラが 4〜6 か所目を裁定で許可: 4 ＝ 1.5 の「副手続き 8 つ」・5 ＝ 4.4／5.3 の「仮置き」・6 ＝ 6.5 のレビューが見つけた 5 章の矛盾）。**完了報告（7.3）の裁定候補一覧に「要件 12.4 とタスク 6.5 の完了状態を 6 か所へ改訂する」を 1 件として載せること。**
- 6.5: **⚠ 統合検証への申し送り。** `briefing.md` の **2-5 の裁定候補の件数と束の数がずれる**——**候補は 10 件だが束は 11**（候補 3 が「サウンド」と「環境の照会」の 2 束を 1 行にまとめている。2-4 の表では 223／224 行の別々の 2 行）。README には「裁定待ちの束が 11 ある…候補は 10 件だが候補 3 が 2 束を 1 行にまとめている」と書いた。**`briefing.md` 側の書き方を直すかは統合検証で決めること。**
- 6.5: **⚠ 段階は「決め終えている」ではない。** 裁定待ちの束が **11**（候補 1〜10）。うち 6 束は順位表の囲みに「段階は仮」の注記が付く（候補 5〜10）が、**残る 5 束（候補 1〜4）は注記が無いのに裁定は済んでいない**。注記の有無は「順位表に仮のまま載せたか」の印であって段階の確定の印ではない。**台帳の頭 1 文字を確定値として扱わないこと。**
- 6.5: **⚠ spec 文書に旧見出しの参照が 8 か所残る**（境界外なので不接触）——旧「段階（A〜E）の最終決定はここでは行わない」が `requirements.md:49`・`tasks.md:428`・`tasks.md:465`／旧「⚠ 全体報告は黙って古くなる」が `requirements.md:239`・`design.md:181`・`tasks.md:268`・`tasks.md:335`・`design-validation.md:53`。**最終形の綴りは「段階（A〜E）は統合担当が付け、台帳へ書き戻してある」と「証拠の件数は全体報告に載せず、副手続きで読む」。7.2／7.3 で直すか記録すること。**
- 6.5: **⚠ 要件 12.4 の括弧書きの節名が実データとずれている**——是正した「常時の検査に入っていない」の文のうち 2 つは「4. 報告の扱い」節ではなく **2 章の「⚠ ドメインを跨ぐ `links` は、機械が 1 つも検証しない」節**にある（design.md の Modified Files も同じ取り違え）。**7.2 で記録すること。**
- 6.5: 3 度差し戻された（⑴「決め終えている」の言い切りが `briefing.md` と食い違う ⑵「6 束」に絞りすぎ ⑶ 束の数が 10 ではなく 11）。**最後の数値訂正は親コントローラが直接適用した。**
- 6.6: 判定 ⑸ の 6 腕（a 冒頭の数＝表の行数／b spec 名の実在／c `owner_count` ＝台帳の数え直し／d `bundle` が `linkage.md` に在る／e `[[owner_completed]]` の実在・状態・`items`／f 台帳の非空の宛先が表か列挙に在る）を機械化。consistency **101 → 112**・unit **600 → 601**。摂動 3 本・腕の較正 5 本・母数の下限 3 つ（`MIN_SPEC_ROWS = 20`・`MIN_OWNER_COMPLETED_ROWS = 1`・`MIN_COMPLETED_SPECS = 100`）。**他 spec のディレクトリを増やしても緑・表の spec を消すと赤**を実ファイルで両側確認（一時ディレクトリは削除済み）。
- 6.6: **⚠⚠ 検証コマンドの穴（親コントローラの不備）。** **1,000 行の番人は別 crate に居る**——`cargo test -p log-capture-kit --test file_length_guard_test`。**`cargo test -p ukadoc-survey` では走らない。** 6.6 が `parse.rs` を 997 → 1,018 行にして番人を赤にしたのを、実装者もレビュアーの最初の走査も見逃した（レビュアーが 2 度目に気づいた）。**以後の検証セットに必ず含めること。最終検証（7.2）でも走らせること。**
- 6.6: 是正——`parse.rs` の共通ヘルパ節を新規 `documents/fields.rs`（294 行）へ**純粋な移動**（`parse.rs` **1,018 → 755 行**）。1.2 の申し送りどおりの案 A。**`OVER_LIMIT_ALLOWED` には 1 行も足していない。** `crates/ukadoc-survey` に 1,000 行超は **0 本**。
- 6.6: 是正——`SpecRow.stage` を **`Option<Stage>`** にして置き字の `A` 14 個を消した。読み手が**束と段階の一蓮托生**を強制する（束を持つ行は段階必須・`none = true` の行は段階を書くと落ちる）。`roadmap-draft.md` の `stage = ` は 27 行中 **13 行**（束を持つ行だけ）。
- 6.6: 是正——6.4 が古びさせた `roadmap-draft.md` の **5 か所**を直した（`owner_count` 5→**6**・10→**11**／束 2 の依存 1 件→**2 件**〔2 か所〕／束 14 の依存 0 本→**1 本**／**「0 本の束は 37」→ 36**。最後の 1 つは 6.4 の申し送りの表に載っていなかったものを 6.6 が見つけた）。
- 6.6: **⚠ 同一コミット内で実測値が食い違う罠。** `MIN_OWNER_COMPLETED_ROWS` の注釈「合計 37 件」（実測 **41**）と `spec_checks.rs` の「`completed/` 175 本」（実測 **174**・`ls` の行数は単独の `.md` を 1 本数える）が兄弟ファイルと食い違っていた。**注釈に数を書くときは兄弟ファイルの同じ数と突き合わせること。**
- 6.6: **⚠ FYI（設計の範囲外・修正不要）。** `SpecRow.stage` の**値そのもの**（束を持つ 13 行の A×9・C×2・E×2）が `briefing.md` の順位表でその束が置かれている段階と一致するかを見る腕は無い。design の判定 ⑸ の a〜f に含まれないため。
- 6.6: **⚠ 次に `parse.rs` を触るときの整理候補。** `DOCUMENT_DIR` の宣言は `parse.rs` にあるのに消費するのは `fields.rs` の `document_file` だけ。移せば相互参照が片道（`fields → parse::toml_blocks`）に減る。
- 7.1: `origin/main` へ rebase（0 behind / 46 ahead）。競合 2 件を解決——`report/summary.md` は再生成される生成物なので適用側を採り、`ledger/shiori.toml` の `ukadoc:spec_shiori3:Charset:2` は **`owner` を上流の主張（`areka-P0-charset-canon`）・`priority` を本 spec の導出値（`A18`）**とした。**競合マーカーはリポジトリ全体で 0。**
- 7.1: **判定が期待どおり働いて 5 件の食い違いを検出した**（`[[barrier]]` の 4 つと `[[spec]].owner_count`）。**判定は 1 つも緩めず文書の数を実測へ直した**（レビューが `MIN_*` 定数のバイト一致と、Rust 2 ファイルの差分が 100% コメントであることを確認）。
- 7.1: 上流の変化——`charset-canon` と `present-gpu-transform-scale` が `completed/` へ（174 → **176**）、`nar-install` と `shell-implicit-surface` が新規起票。**live spec dir は 27（自 spec を除く）で数は変わらないが集合が入れ替わった。**
- 7.1: **裁定 A（`[[spec]]` の表を入れ替えない）**——要件 10.2 が表を「着手時の写真」と定め、判定 ⑸-b は「直下 ∪ `completed/`」に照らすので完了した 2 本が残っても赤にならない設計。design の「⑸ の数え方」が W13「共有ファイル 0」を理由に起票・完了で赤にすることを退けている。**live 27・表 27 行・`count = 27` の三者一致。`snapshot_on = 2026-09-13`。**
- 7.1: **裁定 B（`[[owner_completed]]` に `charset-canon` を足さない）**——同 spec は 5 件を宛先に持ち **5 件とも `implemented`** なので規則 ⑵ を当て直しても空にする件数は 0。判定 ⑸-f は「いずれか」なので `[[spec]]` 側で満たされる。**15 本目を足すと本文が釘付けする「14 本の和は 41」の恒等式が偽になる。** 代わりに §5-3 へ段落を置いた。`present-gpu-transform-scale` は宛先 0 件。
- 7.1: 直した数（レビューが全数を独立に数え直して一致）: `[[barrier]]` `descript_ghost` implemented **9**・absent **64**／`descript_shell_surfaces` implemented **5**・absent **67**／`[[spec]] charset-canon.owner_count` **5**／§5-3 の非空 **416**・空 **1,333**（差 3 ＝ 6.4 の裁定 2 ＋ 上流 1）／`completed/` **176**／束「SHIORI の要求と応答」の依存 `charset-canon` **2 件**／束「起動と挨拶」実装済み **7**・未対応 **9**／**「差 1 件の行」4 → 3 行・「同数で並んだ行」0 → 1 行**（`charset-canon` が 2 対 2）／`MIN_COMPLETED_SPECS` の実測 **176**／`MIN_DOCUMENT_CHARS` の 4 実測 **268,349／95,695／52,007／33,420**（最小は `report/summary.md`）／`briefing.md` の `cited_ids` union **251**（消した 237 は陳腐化していた）。
- 7.1: `priority-apply` は **1 回目から「変更 0 項目」**（上流の `status` 変更 5 件は対象 4 状態の中の移動）。報告 5 本は差分 0。
- 7.1: **⚠ 検証コマンドは 4 本**——`cargo test -p ukadoc-survey`／**`cargo test -p log-capture-kit --test file_length_guard_test`**／`cargo fmt --check`／`cargo clippy -p ukadoc-survey --all-targets`。
