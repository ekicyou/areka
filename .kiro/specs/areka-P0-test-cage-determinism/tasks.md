# Implementation Plan

> **着手条件**: 本仕様の実装は、開発者が別セッションで回す改善ループの成果が `main` へマージされた**後**に開始する（2026-08-22 要件ディスカッションの取り決め）。タスク 1 がこの確認と全面再計測を兼ねる。
> **2026-08-23: 着手条件は満たされた** — 改善ループ（PR#118・`draw-load-parity` の全成果）がマージされ、本ブランチへ取り込み済み（`76384c83`）。取り込み時の実測では**捕捉サイトの集合・1,000 行超の 11 ファイル・待機 2 箇所・表示更新の失敗点はいずれも無傷**で、変わったのは要件 7 の前提（下記）だけ。タスク 1 は「確認済みの再確認＋着手日時点の値でインベントリを固定する」作業として実施する。
> **要件 7 は分岐⒝（着地）で確定** — 自発書込カウンタはスレッド局所になり、錠を取らずに並列実行しても緑であることを並走 spec が固定済み。タスク 7.1 の判定は「着地」、**7.2 は実施対象**。
> **新しい制約（要件 11.7・11.8）**: 共有の起床旗に触る／tick 実行判定へ到達するテストは既存の**唯一の錠**を取る（2 本目を作らない）。共有の旗の上で「旗が立っていない」という不在主張は書かず、注入口で行う。**タスク 4 の待機 2 箇所は旗を観測しないので現状のまま抵触しない**。
> **2026-08-27 開発者裁定（範囲の拡張・3 件）**: ⑴ **要件 7.4 を改訂**——錠の定義の削除を本仕様で行う（引受先が実在せず、`draw-load-parity` と `dpi-transition-atomicity` の 2 度にわたり同じ項目が落ちていたため。呼出を 0 にしたのは本仕様のタスク 7.2）。⑵ **要件 12 を新設**——タスク 8.2 が掘り当てた「プロセス間で共有される固定の一時パス」による赤を本仕様で全解決する（書込を行う 20 ファイルの移行＋迂回検知の見張り）。⑶ **要件 13 を新設**——常時化の費用を同一テスト集合で測り直す（8.2 の移行前後比較は分離不能に終わった）。**群 9・10・11 がこの 3 件で、8.3 は最後尾へ移した。**
> **並行の読み方**: `(P)` は直前の同階層タスクと同時に進められることを表す。移行タスク（群 3）は原則として crate ごとに編集ファイル集合が交わらないため互いに `(P)`。**ただし 3.6 と 3.7 は同じ crate を触るため直列**（3.7 に `(P)` は付かず 3.6 に依存する）。同一ツリーで同時に走らせた場合に競合するのはビルドの排他だけで、編集するファイルは重ならない。

- [x] 1. 着手前の前提確認と全面再計測
  - 改善ループの成果が `main` へマージ済みであることを確認し、本ブランチへ取り込む（**2026-08-23 に実施済み＝`76384c83`**。着手時にさらに新しい `main` が無いかだけ再確認する）
  - `crates/**` を走査して現在値を採り直す: 捕捉 subscriber を差すファイルと硬化の印の有無・派生ヘルパの定義位置・反復回数固定の待機・表示更新の失敗点の数・テスト直列化の錠の呼出数・1,000 行超のファイル
  - 採取に使ったコマンドを `verification/remeasure.md` に残し、次回以降も同じ手順で再現できるようにする
  - requirements.md の Introduction のインベントリ（2026-08-22 時点の 24 ファイル・待機 2 箇所・失敗点 7・錠 21 呼出・超過 11 ファイル）を現在値へ更新し、増減した項目は旧値を併記する
  - 完了状態: requirements.md のインベントリが着手日の実測値になっており、`verification/remeasure.md` の手順を再実行すると同じ数値が再現する
  - _Requirements: 2.1, 4.1_

- [ ] 2. 共有ログ捕捉 crate の新設
- [x] 2.1 crate の骨格を作り、消費 10 crate へテスト専用の依存として配線する
  - ワークスペース内 crate への依存を持たない新 crate を作る（公開しない・必須依存はログ基盤のみ・絞り込み用の追加依存はフィーチャ有効時だけ）
  - 消費 10 crate のテスト専用依存に 1 行ずつ追加する（絞り込みを使う窓基盤 crate だけフィーチャを有効にする）
  - 製品側の依存には一切追加しない（依存方向の規律。機械的な検査はタスク 6.2）
  - 完了状態: `cargo test --workspace` が新 crate を含めて緑のまま通り、窓基盤 crate の依存木に製品 crate への新しい依存が現れない
  - _Requirements: 1.1, 1.3, 1.4_

- [x] 2.2 取りこぼさない捕捉窓を実装する
  - ログ有効判定の焼き付きを防ぐ常駐の仕掛けをプロセス寿命で 1 度だけ確立する冪等な処理を置き、既存の原典にある機序の説明を移設する
  - 捕捉窓は現在のスレッドに捕捉先を差し、窓の内側で有効判定を作り直してから対象の処理を走らせる
  - 窓の内側で対照イベント（専用の宛先の最下位レベル 1 件）を発火し、捕捉できなければ失敗を宣告する。返却前に対照イベントを取り除くので呼出側の戻り値と主張は変わらない
  - 最下位を含む全レベルを捕捉対象とし、窓の外・他スレッドで発火したイベントは混入させない
  - 捕捉結果は窓を抜けた後に取り出す（共有参照の解放タイミングに依存しない形にする）
  - 完了状態: 別スレッドが同じ発行点を先に踏んだ状況でも窓内のイベントを取りこぼさない自己テストが緑になり、対照イベントが捕まらない捕捉先を差した場合は失敗が宣告される
  - _Requirements: 1.1, 3.1, 3.2, 3.3, 3.5, 3.6, 6.3_

- [x] 2.3 正準イベント型と、既存の文字列形を再現する整形・件数の窓口を実装する
  - イベント 1 件はレベル・宛先・フィールド列（**訪問順を保持**）で表す。フィールドの値はデバッグ表現と、文字列として渡された場合の生値の**両方**を持つ
  - 現行の 2 つの行形式（宛先を含む形と含まない形）を 1 バイトも違わずに再現する整形と、レベル別件数の集計を提供する
  - メッセージ・フィールド取得（デバッグ表現と生値の両方）・フィールド名の整列列・マップ化の補助を揃え、各 crate 側で文字列を再解釈せずに済むようにする
  - 完了状態: 移行対象 crate の現行出力からコピーした逐語の見本に対し、整形結果が 1 バイトも違わないことを確かめる自己テストが緑になる
  - _Requirements: 1.1, 2.3_

- [x] 2.4 (P) 絞り込み指示を通した捕捉を提供する
  - 環境変数と同じ書式の指示文字列で実際に絞り込んだうえで整形出力を返す形を、フィーチャ有効時のみ提供する
  - 対照イベントの宛先を絞り込み指示に内部で足し、返す文字列からは対照イベントの行を取り除く
  - 窓基盤 crate の既存 96 呼出の契約（引数・戻り値の意味）を変えない
  - 窓口の公開は crate の入口へ 1 行足すだけにし、2.3・2.5 と同時に進めても行の追加が競合しない形にする
  - 完了状態: 既存の指示文字列の例で絞り込みが効き、対照イベントの行が戻り値に現れないことを確かめる自己テストが緑になる
  - _Requirements: 1.2_
  - _Boundary: 共有ログ捕捉 crate（絞り込み）_
  - _Depends: 2.2_

- [x] 2.5 (P) 全スレッド横断の一回限り捕捉の窓口を分けて提供する
  - プロセス全体の既定の捕捉先を置く処理を本 crate の 1 箇所だけに閉じ込め、別スレッドで発火するログを捕える用途専用の窓口として公開する
  - 既に別の全体設定がある場合は黙って縮退せず、両立条件を示して明示的に失敗させる
  - この窓口を使うと同じテストバイナリ内でログ有効判定が全スレッドで真になることを、両立条件として利用手順に明記する
  - 既定のスレッド局所の捕捉窓とは名前で明確に区別し、取り違えられない形にする
  - 窓口の公開は crate の入口へ 1 行足すだけにし、2.3・2.4 と同時に進めても行の追加が競合しない形にする
  - 完了状態: 2 度呼んでも同じ蓄積先を返し、先に別の全体設定がある場合は明示的な失敗になることを確かめる自己テストが緑になる
  - _Requirements: 1.6_
  - _Boundary: 共有ログ捕捉 crate（全スレッド捕捉）_
  - _Depends: 2.2_

- [x] 2.6 機構の自己テストと利用手順を揃える
  - 自己テスト: 窓内で他スレッドが先に発行点を踏んでも捕捉できる／対照イベントが無ければ失敗を宣告する／最下位レベルが捕れる／他スレッド発火を混入させない／文字列として渡したフィールドを生値でもデバッグ表現でも取り出せる／件数集計が合う
  - crate の説明を**利用手順**として書く: どこから引くか、存在を主張するテストの書き方、不在を主張するテストで対照が自動で入ること、全スレッド捕捉の使いどころと両立条件、統合テストからの引き方
  - 「スレッドローカルだから並行実行でも干渉しない」という誤った説明を明確に否定し、正しい機序（有効判定はプロセス共有・先着が焼き付く・本機構が何を保証するか）を書く
  - 完了状態: 共有 crate の単体テストが緑で、利用手順を読んだだけで別 crate から捕捉テストが書ける状態になっている
  - _Requirements: 3.4, 3.5, 3.6, 11.6_

- [x] 2.7 硬化していない捕捉では取りこぼすことを別プロセスで示す較正テストを置く
  - 親テストが自身のテストバイナリを 2 通り（硬化なし／硬化あり）で子プロセス起動する形にし、区別は所定の接頭辞を持つ環境変数で渡す
  - 子テストは通常実行から除外し、環境変数が無ければ即座に終了する（親の実行順に依存しない）
  - 場面は「窓の内側で別スレッドが先に同じ発行点を登録する」に固定し、硬化なしでは捕捉 0 件・硬化ありでは 1 件を各子が自分で確かめる
  - 親は子の出力に「1 件成功」が現れることを要求し、0 件実行で終了コードだけ 0 になる空振りを弾く
  - 完了状態: 較正テストが緑で、硬化ありの経路を意図的に外すと子が赤になる（毎回赤も作れる）ことを 1 度確認している
  - _Requirements: 3.4, 9.6_

- [ ] 3. 捕捉サイトの移行（crate 単位）
- [x] 3.1 (P) 面表と再生ループの crate を共有機構へ寄せる
  - 捕捉ヘルパ 5 箇所の本体を共有機構への委譲に置き換え、戻り値の形（改行で連結した文字列）を保つ
  - 常駐の仕掛けを集めた専用モジュールをファイルごと削除し、モジュール宣言も外す
  - 統合テストの全スレッド捕捉を共有の窓口へ寄せる
  - 誤った説明文と誤った由来表示（「別 crate の流儀に倣った」等）を正しい機序の説明へ置き換える
  - 完了状態: この crate のテストが移行前と同じ本数・同じ合否で緑になり、捕捉先を自前で差す箇所が 1 つも残っていない
  - _Requirements: 1.5, 1.6, 1.7, 2.2, 2.3, 2.4, 2.5, 6.1_
  - _Boundary: areka-seriko_
  - _Depends: 2.3, 2.5_

- [x] 3.2 (P) 全体設定を常駐させていた 3 crate を共有機構へ寄せる
  - 3 crate の常駐処理と捕捉層を削除し、共有機構の捕捉窓へ置き換える
  - 各 crate のイベント型（宛先・レベル・メッセージ・種別・結果・フィールド）は正準型からの変換で組み立て、種別と結果は**生値**で取り出して完全一致の判定を変えない
  - 同じ crate 内で硬化されていない捕捉が別に残っている箇所も同じ窓口へ寄せる
  - 統合テストの全スレッド捕捉を共有の窓口へ寄せる
  - 完了状態: 3 crate それぞれのテストが移行前と同じ合否で緑になり、プロセス全体の設定を置く処理がこれら 3 crate から無くなっている
  - _Requirements: 1.5, 1.6, 1.7, 2.2, 2.3, 2.4, 6.1_
  - _Boundary: areka-sylphya, areka-kanade, areka-ghost_
  - _Depends: 2.3, 2.5_

- [x] 3.3 (P) 画像合成の 2 crate を共有機構へ寄せる
  - 2 crate の常駐の仕掛けと捕捉層を削除し、行の列を返す形を保ったまま共有機構へ委譲する
  - モジュール説明の「この方式でないと取りこぼす」の記述を、共有機構を指す短い形へ改める
  - 完了状態: 2 crate のテストが移行前と同じ合否で緑になる
  - _Requirements: 1.5, 1.7, 2.2, 2.3, 2.4, 6.1_
  - _Boundary: areka-emo-atlas, areka-emo-compose_
  - _Depends: 2.3_

- [x] 3.4 (P) 表示 crate の捕捉を共有機構へ寄せる
  - 常駐の仕掛けを持つヘルパ 2 種と、共有の捕捉先を自前で持つ 2 定義を削除し、共有機構へ委譲する
  - 捕捉先を直接差していた 4 ファイル 21 呼出を捕捉窓の呼出へ置き換える。フィールド名の整列列を返す補助は同じ意味のまま保つ
  - 「スレッドローカルなので他テストのイベントを取り込まない」という混入だけに触れた説明を、取りこぼしにも触れた正しい機序へ改める
  - 不在を主張するテストが自前で用意していた対照観測は、共有機構の内蔵対照と重複しても判定が変わらないことを確認する
  - 完了状態: この crate のテストが移行前と同じ本数・同じ合否で緑になる
  - _Requirements: 1.5, 1.7, 2.2, 2.3, 2.4, 6.1_
  - _Boundary: areka-emo-present_
  - _Depends: 2.3_

- [x] 3.5 (P) 文字描画 crate の捕捉と件数集計を共有機構へ寄せる
  - 別名の捕捉ヘルパ 3 種と、件数カウンタを直接差していた 2 ファイルを共有機構の件数集計へ置き換え、戻り値の組を保つ
  - 統合テスト側に複製されていた常駐の仕掛けと捕捉先を削除し、共有機構へ寄せる（統合テストからも同じ引き方ができることの実例になる）
  - 説明文の「スレッドの範囲に閉じる」の記述を、有効判定の焼き付きにも触れた形へ改める
  - 完了状態: この crate のテスト（統合テストを含む）が移行前と同じ合否で緑になる
  - _Requirements: 1.5, 1.7, 2.2, 2.3, 2.4, 6.1_
  - _Boundary: areka-emo-text_
  - _Depends: 2.3_

- [x] 3.6 (P) 本体 crate の起動系の捕捉を共有機構へ寄せる
  - 起動系 6 ファイルの捕捉ヘルパ本体を共有機構への委譲に置き換え、行形式を保つ
  - 多フレーム駆動のハーネスは捕捉層だけを差し替え、ハーネス本体とその逐語検査が見張る項目は動かさない
  - 誤った説明文（「スレッドローカルゆえ並行実行でも干渉しない」類）を正しい機序へ置き換える
  - 待機の共通処理を足すファイルと同じファイルを 1 つ含むので、タスク 4.1 とは同時に進めない
  - 完了状態: この crate のテストが移行前と同じ本数・同じ合否で緑になり、多フレーム駆動ハーネスの逐語検査も緑のまま
  - _Requirements: 1.2, 1.5, 1.7, 2.2, 2.3, 2.4, 6.1, 11.3_
  - _Boundary: areka（起動系）_
  - _Depends: 2.3_

- [x] 3.7 本体 crate の入力系・デモ・常駐の原典を共有機構へ寄せる
  - 入力系 2 ファイル・デモ 1 ファイルの捕捉ヘルパ本体と直接の捕捉を共有機構への委譲に置き換え、行形式を保つ
  - 常駐の仕掛けの原典ファイルは捕捉機構の本体を削除して判定用の補助だけを残し、イベント型は正準型の別名にする。原典を消費していた 3 ファイルの有効判定の作り直しの直接呼出も削除する
  - 「最小複製」の自認と誤った説明文を正しい機序へ置き換える
  - 3.6 と同じ crate のため並行しない（順序どおり 3.6 の後に実施する）
  - 完了状態: この crate のテストが移行前と同じ合否で緑になり、常駐の仕掛けの定義がこの crate から無くなっている
  - _Requirements: 1.5, 1.7, 2.2, 2.3, 2.4, 6.1_
  - _Depends: 2.3, 3.6_

- [x] 3.8 (P) 窓基盤 crate の捕捉を薄い再輸出へ縮める
  - 常駐の仕掛けと書き込み先・絞り込み捕捉の本体を削除し、共有機構の絞り込み捕捉を薄く再輸出する形にする（96 呼出は無改変）
  - モジュール説明の「上位 crate に依存できないので同型を持つ」を「共有 crate を引く」へ改める
  - 同じ crate 内で共有機構を使わずに捕捉していた表示倍率まわりのテストも同じ窓口へ寄せる
  - 遷移の観測に使う語彙（窓種別のフィールド名・1 行に同名フィールドを 2 度出さない）は増やさず、既存の逐語検査を緑のまま保つ。窓書込の回数を数える判定は追加しない
  - 完了状態: この crate のテストが移行前と同じ合否で緑になり、遷移観測の逐語検査も緑のまま
  - _Requirements: 1.2, 1.3, 1.5, 1.7, 2.2, 2.3, 2.4, 6.1, 11.1, 11.2_
  - _Boundary: wintf_
  - _Depends: 2.3, 2.4_

- [ ] 4. 待機の有界化
- [x] 4.1 (P) 「尽きるのが正常」の回収待ちを壁時計と観測量で有界化する共通処理を置く
  - 1 反復分の回収を行って件数を返す処理を受け取り、**最小の持続時間を満たし、かつ連続して空だった回数が既定に達する**まで繰り返す形にする。全体は既存の上限時間で必ず戻る
  - 反復の間は既存の短い休止（有界な待ちの譲歩）だけを使い、時刻を進めるための休止は入れない
  - この共通処理は失敗を宣告しない（合否の判定は呼出側の既存の主張のまま）
  - モジュール説明の「回収待ちだけは従来どおり譲歩のみ」の記述を本処理の説明へ更新する
  - 共通処理を足すファイルは 3.6 が捕捉ヘルパを差し替えるファイルと同一なので、3.6 の完了後に着手する
  - 完了状態: 共通処理を単独で駆動する検査が緑になり、最小の持続時間と連続空回数の両方が満たされるまで戻らないことが確かめられる
  - _Requirements: 4.2, 4.4_
  - _Boundary: areka（起動系の待機）_
  - _Depends: 3.6_

- [x] 4.2 反復回数固定の待機 2 箇所を差し替える
  - 時刻注入を兼ねていた側は、**注入を前段へ完全に分離**する。旧範囲の時刻を毎回すべて決定論的に注入し（各注入の直後に回収）、そのうえで回収だけを行う待機へ移す
  - もう一方（外側で別系統の時刻を注入している側）は、内側の回収ループだけを共通処理へ置き換える
  - どちらも既存の主張・期待値・テスト本数を変えない。テストの削除もしない
  - 共有の起床旗には触らない（2 テストとも旗を観測しない）。もし観測が必要になったら、唯一の既存の錠を取り、不在の主張は共有の旗ではなく注入口で行う
  - 完了状態: 2 テストが緑で、注入する時刻の範囲が環境の速さに関わらず旧テストと同一であることがコード上で読み取れる
  - _Requirements: 4.3, 4.5, 4.6, 6.1, 6.4, 11.7, 11.8_

- [ ] 5. 表示更新の失敗注入と前状態の保持
- [x] 5.1 (P) 失敗の注入点を置き、内部状態の更新を最後に一括で行う順序へ改める
  - 失敗し得る 7 点（外形変更 3・資源取得 3・提示 1）の直前に、テストビルドでのみ実体を持つ注入点を置く。通常ビルドでは何もしない空の処理になる
  - 注入された失敗は既存の失敗経路と同じ形（記録を残してから失敗を返す）で返す
  - 内部状態の一括更新を**画素の書き込み直前**まで遅らせ、失敗し得る操作がすべてその前に終わるようにする
  - 表示側の呼出元（早期に戻る分岐）・保持側は一切触らない。成功時のグラフィクス呼出の種類と回数は現行と同じに保つ
  - 完了状態: 通常ビルドが通り、既存の定常状態の確保 0 件を見る検査と、呼出元の本文を逐語で見る検査がどちらも緑のまま
  - _Requirements: 5.1, 5.3, 5.5, 5.6, 5.7_
  - _Boundary: areka-emo-present（表示面の更新）_

- [x] 5.2 失敗注入で「前の状態を保つ」を実行テストで固定する
  - 失敗点 × 経路（外形が変わる／変わらない）の意味のある 11 組について、内部状態の 4 項目・読み戻し内容・寸法の期待値を表として固定する
  - 既知の残余 2 件（最終段の提示で失敗した場合と、外形変更が成功した後に後段で失敗した場合）は是正せず、現状の挙動を期待値として書く
  - どの失敗の後でも、次の成功で新しい内容・新しい寸法へ回復することを確かめる
  - 既存のグラフィクステストと同じ前提（窓なし・実デバイス）で走り、実機の障害再現を必要としない
  - 是正が表示面の更新ファイルの外へ波及しないと成立しないと判明した場合は、是正せず申し送りへ回す（タスク 8.3）
  - 完了状態: 11 組の検査が緑になり、注入点を意図的に無効化すると当該検査が赤になることを 1 度確認している
  - _Requirements: 5.1, 5.2, 5.4, 5.5, 5.8, 5.9_

- [x] 5.3 表示側から見た前状態の保持を実行テストで固定する
  - 表示を一度確立したうえで失敗を注入し、可視状態・適用済み情報・実寸・現在の面・配置の指示が**いずれも書き換わらない**こと、呼出元へ失敗が返ることを確かめる
  - 観測点である早期に戻る分岐の位置と意味は動かさず、その分岐を見張っている既存の逐語検査の被覆も縮めない
  - 完了状態: 表示側の失敗テストが緑になり、既存の逐語検査も緑のまま
  - _Requirements: 5.2, 5.4, 5.6_

- [ ] 6. 再発防止の見張り
- [x] 6.1 (P) ワークスペース走査の部品を純粋な関数として用意し、自己較正する
  - ワークスペース配下のソースを列挙する処理（本番・テスト・実行例・兄弟ファイルを含み、生成物と外部取り込みは除く）を用意する
  - コメント行と行末コメントを取り除いてから語を探す処理、行数を測って上限超過を返す処理を、いずれも入出力だけで完結する純粋な関数として切り出す
  - 既知の陽性例（自前で捕捉先を差す文字列）で 1 件返し、コメントだけの文字列では 0 件になること、例外表から 1 件外すと当該ファイルが返ることを、文字列の見本で確かめる
  - 完了状態: 純粋な関数の検査が緑で、列挙処理が実行例・統合テスト・兄弟ファイルの既知ファイルを実際に含むことも確かめられる
  - _Requirements: 8.3, 8.4, 10.3_
  - _Boundary: 共有ログ捕捉 crate（走査）_
  - _Depends: 2.1_

- [x] 6.2 共有機構を迂回する捕捉の新設を検知する見張りを有効にする
  - 走査語（捕捉先を直接差す呼出・プロセス全体の設定を置く呼出）を持つ箇所が、共有 crate の内側と例外表以外に無いことを検査する
  - 例外表（直接呼出）は**実測の 4 件**で開始する（移行不能な 3 件＋較正 1 件。起草時の「既定で空」は 2026-08-24 に訂正・requirements.md 8.1 と design.md `#### C6` も追随済み）。全スレッド捕捉を使うファイルだけを理由付きの別表に載せる（着手時点の 2 件）
  - 各 crate の製品側依存に共有 crate が現れていないことも同じ見張りで検査する
  - 例外表への追加は明示的な編集としてのみ通し、暗黙に増えない形にする
  - 完了状態: 移行完了後にこの見張りが緑になり、既知の陽性例を 1 件戻すと赤になることを 1 度確認している
  - _Requirements: 1.3, 1.6, 2.6, 8.1, 8.2, 11.5_
  - _Depends: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 6.1_

- [x] 6.3 (P) 1 ファイルの行数の見張りを置く
  - 上限を超え例外表に無いファイルがあれば失敗する検査を 1 本置く。行数の数え方は既存の測り方に合わせる
  - 例外表は着手時点の超過ファイル（タスク 1 の再計測値・2026-08-22 時点で 11 件）で開始する。項目の追加は明示的な編集としてのみ許し、削除だけが自然な方向になるようにする
  - 既存の超過ファイルの分割・縮小は**行わない**（見張りを作るだけ）
  - 例外表から既知の 1 件を外すと赤になることを自己検査で固定する
  - 完了状態: 見張りが緑で、例外表の件数と中身が着手時の実測と一致している
  - _Requirements: 10.1, 10.2, 10.3, 10.4_
  - _Boundary: 共有ログ捕捉 crate（走査）_
  - _Depends: 2.1, 6.1_

- [ ] 7. テスト間の共有状態による直列化の解消
- [x] 7.1 並走 spec の着地状況を判定して分岐を確定する
  - 並走している性能 spec が自発書込カウンタのスレッド局所化を着地させたかを、対象ファイルの実際の型と当該 spec の状態で判定する（**2026-08-23 時点の実測は「着地」＝スレッド局所の型・並列で緑であることを固定する新テストも同梱済み**。着手時に型を再確認して裏を取る）
  - 未着地なら対象ファイルには一切触れず、錠を現状のまま保つ。見送りが確定していれば申し送りへ登記し、本仕様は錠を温存したまま完了できるようにする
  - 錠の**定義**の削除は、着地の有無に関わらず本仕様では行わず所有者へ申し送る
  - 完了状態: 判定結果（着地／未着地／見送り）と根拠（実測した型と参照した spec の状態）が申し送り台帳に記録されている
  - _Requirements: 7.1, 7.3, 7.4_

- [x] 7.2 着地していれば錠の取得を退役させる
  - 兄弟テストファイル 4 本の呼出（19 箇所）を外す。並走の衝突が解消していれば対象ファイル内の 2 呼出も外す
  - **同じ塊で説明文の陳腐化も是正する**——兄弟テスト 4 本の冒頭・注記に「プロセス共有のカウンタ」と書かれたまま残っている記述を、スレッド局所になった現在の機序へ書き換える（**2026-08-24 訂正**: 起草時は「要件 2.4 の対象」としていたが、2.4 は捕捉サイトの `with_default` 誤説明に固有で置換先も別物。根拠は design `#### C5` の「同じ塊で是正する陳腐化」。並走 spec からの名指しの申し送り）
  - 退役後に当該 crate の単体テストを負荷下で 30 回以上繰り返し、失敗が出ないことを示す（証跡はタスク 8.2 の記録へ合流させる）
  - 未着地・見送りの場合は本タスクを実施せず、その旨を申し送りへ残す
  - 完了状態: 錠の取得が 0 箇所になり、「プロセス共有」と述べる説明文も 0 件になり、当該 crate のテストが規定回数の反復で 1 度も赤にならない
  - _Requirements: 7.2_（**2026-08-24 訂正**: 2.4 は本タスクの根拠ではない。上記参照）
  - _Depends: 7.1_

- [ ] 8. 反復検証と申し送り
- [x] 8.1 反復実行と記録の仕組みを用意する
  - 対象と回数を受け取って繰り返し実行し、各回の出力をファイルへ保存する仕組みを `verification/` に置く
  - 赤が出た回はテスト名と失敗内容を抜き出して要約へ登記する（名前の分からない赤を残さない）
  - 負荷をかけた状態を再現する手順（同時に別のテストを走らせる等）を手順書に固定する
  - 完了状態: 仕組みを 1 回試走させ、保存されたログと要約から「何回走らせて何回赤だったか」が読み取れる
  - _Requirements: 9.4_

- [x] 8.2 反復検証を実施して証跡を残す
  - ワークスペース全体を負荷下で 10 回以上、ログの存在を主張する面表 crate のテストを 30 回以上、待機を直した 2 テストを 30 回以上繰り返す（前提となる 32bit 側の成果物を先にビルドしてから実行する）
  - 錠を退役させた場合（タスク 7.2 を実施した場合）は、窓基盤 crate の単体テストの 30 回以上の反復も同じ仕組みで実行して記録に含める。未着地・見送りの場合はその旨を記録に残す
  - 移行前後で全体の所要時間を比較して記録する（常時の有効判定になることの影響）
  - 正体不明だった 1 件の赤が硬化後に再現しなくなったか、別系統として残るかを記録する
  - 較正の結果（硬化なしで取りこぼすこと・見張りが既知の陽性で赤になること）を並べて報告し、緑が道具の故障で出ていないことを示す
  - 完了状態: 規定回数の反復で捕捉・待機・錠に起因する失敗が 0 件であることが、保存されたログで裏付けられている
  - _Requirements: 3.7, 7.2, 9.1, 9.2, 9.3, 9.5, 9.6_
  - _Depends: 7.1, 8.1_

- [x] 9. 錠の定義の削除（2026-08-27 裁定・改訂後の要件 7.4）
- [x] 9.1 呼出 0 になった錠の定義と、裁定待ちを告げる説明文を削除する
  - `crates/wintf/src/ecs/window/command.rs` の錠の項目を丸ごと削除する。関数本体の中のプロセス共有の静的値もこれで消える
    - **起草時の範囲 `:88-102`（doc コメント 11 行＋関数 5 行）は実物と食い違っていた**。実測した項目の実体は **`:73-102`**（説明文 25 行＝`:73-97`／`#[cfg(test)]` 1 行／関数 4 行）で、直後の空行 `:103` を含めて **31 行の純削除**が正しい。`:88` から切ると説明文の前半 `:73-87` が項目に付かない孤児となり `error: found a documentation comment that doesn't document anything` でコンパイルが落ちる
  - 同じ変更で説明文の「定義そのものの扱いは同仕様のタスク 8.3 で開発者が裁定する」を消す（裁定が済んだ後に残ると存在しない判断を指し続けるため）
  - 削除前に呼出が本当に 0 であることを実測で確かめる（走査式は 7.2 で較正済み。`git show HEAD:<file>` に同じ式を当てると当時の件数が返ることも併せて確認する）
  - この crate の説明文で錠に言及している箇所（過去形で正しい記述を含む）が壊れていないことを確認する。**語で数えると正しい説明を壊す**ので、`(ファイル, 行内容)` の対で前後を比較する
  - 削除で壊れる説明文を同じ変更で直す。`command_threadlocal_tests.rs:19`（当時）が削除対象へ**説明文内リンク**を張っており、放置すると `cargo doc` だけが壊れる＝テストにも clippy にも映らない陳腐化が残る。節の主張（このファイルは錠に頼らず並列で緑になることを固定している）は真であり続けるので**消さずに過去形へ改める**
  - 完了状態: `cargo test -p wintf --lib` が移行前と同じ本数・同じ合否で緑になり、`cargo clippy -p wintf --all-targets` の未使用関数の警告が 1 件減る
  - _Requirements: 7.4_
  - _Boundary: wintf（`command.rs` と、削除により壊れる同 crate の説明文）_

- [ ] 10. テスト用一時パスのプロセス間衝突の解消（2026-08-27 裁定・要件 12）
- [x] 10.1 プロセス間で一意な一時パスの窓口を新設し、自己テストを置く
  - 依存を 1 つも持たない（標準ライブラリのみ）テスト専用の新 crate を作る。公開しない
  - 型は既存の正解型の移植とする（`crates/areka/src/placement/placement_shared_test_support.rs:41-68`＝単調増加の連番＋プロセス識別子＋破棄時の再帰削除）。**発明しない**
  - ディレクトリを配る形に統一し、単一ファイルの宛先はその下に置く（宛先の種類を増やさない）
  - 自己テスト: 同一プロセス内で 2 度作っても名前が衝突しない／破棄で実体が消える／プロセス識別子が違えば別名になる（識別子を引数で受ける内部関数を用意して決定論的に確かめる）
  - 完了状態: 新 crate の単体テストが緑で、`cargo test --workspace` の合計本数が新 crate のぶんだけ増える
  - _Requirements: 12.1_
  - _Boundary: 一時パスの共通窓口 crate_

- [x] 10.2 (P) 本体 crate の 6 ファイルを窓口へ寄せる
  - 対象は `std::env::temp_dir()` を使い書込・削除を行う 6 ファイル（実測で落ちている `placement/transition_signoff_tests.rs`・`main_restore_seam_tests.rs` を含む）
  - テスト専用依存に 1 行足す（既に共有ログ捕捉 crate の配線がある節へ並べる）
  - 既存の主張・期待値・テスト本数を変えない。本番コード（テスト属性の外）は 1 行も変えない
  - 完了状態: `cargo test -p areka` が移行前と同じ本数・同じ合否で緑になる
  - _Requirements: 12.2, 12.3_
  - _Boundary: areka（テストコードのみ）_
  - _Depends: 10.1_

- [ ] 10.3 (P) ゴースト crate の 12 ファイルを窓口へ寄せる
  - 対象は統合テスト 10 本と本番ファイル内のテストモジュール 2 本。**本番ファイルは中のテストモジュールだけを触る**
  - 実プロセスを起こす統合テストが含まれるので、宛先が変わっても子プロセスへの受け渡し（環境変数・引数）が壊れていないことを確かめる
  - 完了状態: `cargo test -p areka-ghost` が移行前と同じ本数・同じ合否で緑になる
  - _Requirements: 12.2, 12.3_
  - _Boundary: areka-ghost（テストコードのみ）_
  - _Depends: 10.1_

- [ ] 10.4 (P) 構文解析 crate の 2 ファイルを窓口へ寄せる
  - この crate だけ共有 crate の配線が無いので、テスト専用依存の節を新設する（製品側の依存には置かない）
  - 対象 2 ファイルのうち 1 本は本番ファイル内のテストモジュール
  - 完了状態: `cargo test -p areka-parsers` が移行前と同じ本数・同じ合否で緑になる
  - _Requirements: 12.2, 12.3_
  - _Boundary: areka-parsers（テストコードのみ）_
  - _Depends: 10.1_

- [ ] 10.5 迂回した新設を検知する見張りを置き、較正する
  - 見張りは共有ログ捕捉 crate の統合テスト側へ置く（走査の部品を複製しないため。**窓口と見張りが別 crate に分かれるのは意図的**）
  - 一時パスの入口から固定名を組み立てる箇所を検知する。コメントは必ず除去してから走る（**コメント中の語で判定が反転した実例が本仕様の調査中に実際に起きている**）
  - 例外表は移行後の実測値で開始し、追加は明示的な編集としてのみ許す。既にプロセス識別子で一意化している 16 ファイルを例外表に載せるか窓口へ寄せるかを決め、根拠を記録する
  - 既知の陽性例を 1 件戻すと赤になることを自己テストで固定する
  - 完了状態: 見張りが緑で、例外表の件数と中身が移行後の実測と一致している
  - _Requirements: 12.4, 12.5, 12.7_
  - _Boundary: 共有ログ捕捉 crate（走査）_
  - _Depends: 10.2, 10.3, 10.4_

- [ ] 10.6 同時走行での 0 失敗を反復で示す
  - 反復の仕組みで `cargo test -p areka` を同時 4 プロセスで 30 回以上回し、一時パスの衝突に起因する失敗が 0 件であることを示す
  - タスク 8.2 が記録した赤 3 回（`summary.md` の該当節）と同じ条件で回し、**同じ条件で赤が消えたこと**が読み取れる形で記録する
  - 走らせる前に期待件数を採り直す（8.2 の申し送り。`--list` の行数を期待件数にしない）
  - 完了状態: 30 回以上の反復が全緑で、保存された記録から回数と各回の件数が読み取れる
  - _Requirements: 12.6_
  - _Depends: 10.5_

- [ ] 11. ログ有効判定の常時化の費用の測定（2026-08-27 裁定・要件 13）
- [ ] 11.1 常駐の仕掛けの切替点を置く
  - 常駐を確立する処理の中で環境変数を 1 度だけ読み、無効指定なら登録せずに戻る形にする（プロセス寿命で 1 度の判定なので測定対象への上乗せは無い）
  - 変数名はワークスペースの規約（`AREKA_` 名前空間）に従い、既存の較正用変数と同じ流儀にする
  - 既定（変数なし）の挙動は現状と 1 ビットも変えない。既存の全テストが緑のまま
  - 無効指定で走らせたとき、取りこぼした捕捉窓が黙って空を返さず失敗を宣告することを確かめる（対照イベントの性質＝赤の集合が決定論的に特定できることの担保）
  - 完了状態: 既定で `cargo test --workspace` が変化なく緑、無効指定では捕捉テスト群だけが赤になり、その集合が 2 度走らせても同一である
  - _Requirements: 13.1, 13.2_
  - _Boundary: 共有ログ捕捉 crate（常駐の仕掛け）_
  - _Depends: 10.6_

- [ ] 11.2 同一集合の比較を実施して記録する
  - 無効側で赤になるテストの集合を確定し、**両側から同じフィルタで除外**して比較する。除外後に両側とも全緑でなければ比較値として採らない
  - 反復して中央値と散らばりの両方を採る。**差が散らばりに埋没する場合はそれをそのまま結論とする**（差が出たことにしない）
  - 反復の仕組みで実行し、記録を要約へ残す。環境変数を渡す口が無ければ仕組みに引数を 1 つ足す
  - 得られた数字と、**その数字が何を測っていて何を測っていないか**を書く。「測った、問題なかった」とは書かない
  - 完了状態: 両側全緑の比較値が中央値と散らばりつきで記録されており、除外したテスト集合とその根拠も併記されている
  - _Requirements: 13.3, 13.4, 13.5_
  - _Depends: 11.1_

- [ ] 12. 申し送りの決着
- [ ] 12.1 申し送り台帳を登記して完了条件を揃える（旧 8.3・2026-08-27 に最後尾へ移動）
  - 観測が正しくなった結果として恒常的に落ちたテストがあれば、緩めずに欠陥候補として起票し台帳へ登記する
  - 表示更新の是正が対象ファイルの外へ波及した場合の起票、並走 spec 宛の申し送り（合流後は新規の 1,000 行超が赤になること）を登記する。**錠の定義削除は 2026-08-27 の裁定で群 9 が実施したので申し送りではなく実施記録として登記する**
  - 再表示時の重なり順の再確認は、決定論的なテストで固定できる範囲が無い理由（再表示の経路に再指示が無く、挿入点は確立時の 1 箇所のみ・実際の隣接確認には実窓が要る）を添えて、後続の実機適合 spec へ申し送る
  - リポジトリ構成の登記（steering の crate 一覧）へ**共有ログ捕捉 crate と一時パスの窓口 crate の 2 本**を加え、実在する crate 集合と一致させる
  - 共有ログ捕捉 crate の説明文を、統合テスト側がワークスペース全体の見張りの置き場になっている実態へ合わせて 1 行更新する（設計 `#### C6` の決定）
  - タスク 8.2 が残した項目を決着させる: 記録の正本の宣言（`summary.md`）・錠の退役の安全性の主張の正しい形。**一時パス共有の赤と所要時間の分離は 2026-08-27 の裁定で群 10・11 が実施したので、実施結果として登記する**
  - タスク 3.6 の残余（行整形の選択がどのテストにも縛られていないこと）を登記する
  - 完了状態（追加）: steering の crate 一覧に 2 本が載っており、一覧と実際の `crates/` の内容が食い違わない
  - 完了状態: 台帳に未解決の項目が 1 つも「担当先未定」で残っておらず、各項目に引受先または起票先、あるいは本仕様での実施記録が書かれている
  - _Requirements: 5.8, 6.2, 7.3, 7.4, 9.5, 10.5, 11.4, 12.7, 13.5_
  - _Depends: 8.2, 9.1, 10.6, 11.2_

## Implementation Notes

- **タスク番号の移動（2026-08-27）**: 旧 **8.3**（申し送り台帳）は最後尾の **12.1** へ移した。2026-08-27 の裁定で群 9・10・11 が新設され、台帳はそれらの結果も登記するので最後でなければ閉じられないため。**本節および requirements.md / design.md に既にある「8.3 宛」「8.3 で裁定」の記述はすべて 12.1 を指す**（記述そのものは当時の判断の記録なので書き換えない）。なお 8.3 が抱えていた 3 件の裁定事項は 2026-08-27 に決着済みで、⑴ 錠の定義削除＝群 9 が実施、⑵ 一時パス共有の赤＝群 10 が実施、⑶ 所要時間の分離＝群 11 が実施。12.1 はそれらを「申し送り」ではなく「実施記録」として登記する。

- **タスク 1**: `rg -l` は Windows で `\` 区切り、`git grep -l` は `/` 区切りを出す。両者を突き合わせるコマンドは必ず正規化する（`| tr '\134' '/'`。`--path-separator /` は MSYS がパス展開して失敗するので使えない）。詳細は `verification/remeasure.md` §0。
- **タスク 1**: 「空を返せば合格」型の検査は、道具が不在でも空になり得る。左辺を意図的に 1 件削ると赤になることを併記する（較正）。`verification/remeasure.md` §1-b がその形。
- **タスク 1**: `crates/areka/src/emo2_boot/adapter.rs` の行番号が design.md 執筆時から **+5** ずれている（`fn capture_logs` :383→:388、説明文 :358-359→:363-364）。**design.md:155 の引用も同じずれを持つ**ので、群 3 の移行時は行番号を鵜呑みにせず実測すること。
- **タスク 2.1**: フィーチャは workspace 全体で統一されるので、`wintf` が `env-filter` を有効にすると他 9 crate からも `filter.rs` の API が届く。「有効にするのは wintf のみ」は**宣言**であってコンパイラは強制しない。**タスク 6.2 に「`features = ["env-filter"]` を宣言してよいのは wintf のみ（他 9 crate の `[dev-dependencies]` 行に `features` が現れたら赤）」の検査を足すこと**（6.2 の現行本文は製品側依存の有無しか見ていない）。
- **（番外・開発者裁定 2026-08-23）**: `crates/wintf/src/ecs/world/tick_diag_tests.rs` の `try_tick_world_body` が `include_str!("mod.rs")` に対し LF 固定の needle `"\n    }\n"` を掛けており、`core.autocrlf=true`・`.gitattributes` 無しの本リポジトリでは **CRLF チェックアウトで必ず赤**だった（着手前から赤・2 件）。所有は完了済みの `draw-load-parity` で引受不能、かつ「チェックアウト次第で赤緑が変わるテスト」は本仕様の主題そのもののため、**開発者裁定により本仕様で是正**。`indent4_close_brace_at` を新設し行末の `\r` を落として照合する形にした。以後の `cargo test --workspace` の基準線は **失敗 0 件**。**タスク 8.3 の申し送り台帳へ登記すること**（同型の LF 固定 needle が他にも無いかの掃除は本仕様の範囲外）。
- **タスク 2.2 → 2.7 宛**: 硬化を外して赤になる檻は `captures_event_whose_callsite_another_thread_registers_inside_the_window` **1 本だけ**で、しかも **`--test-threads=1` か `--exact` のときだけ**赤になる。既定の並列実行では他 7 本の `with_default` が同時に走って `has_just_one` が偽のままになり、焼き付きが起きず 20/20 緑のまま。**タスク 2.7 の子プロセス較正はこの穴を塞ぐためのもので、省略できない**。
- **タスク 2.2 → 2.7 または 8.3 宛（引受先未定）**: 窓の内側の `rebuild_interest_cache()`（`capture.rs`）は**どのテストにも縛られていない**。これだけ外しても 8/8 緑（並列・直列とも）。probe 常駐時の `Dispatch::new` と `with_default` 内部の `Dispatch::new` がいずれも窓が開く前に全発行点を再計算するため、プロセス内の変異では露出しない。要件 3.2 の機序が赤にできる檻を持たない状態なので、2.7 の子プロセス較正へ足すか、意図的な二重防御として台帳へ記録するかを決めること。
- **タスク 2.2**: 不在を主張する檻（`does_not_capture_events_from_outside_the_window_or_other_threads`）は `count == 0` しか見ないので、発行点が焼き付いていても空振りで緑になる。番兵は**窓**が生きていることは示すが**その発行点**が生きていることは示さない。同一スレッド・窓内の別 target を `== 1` で見る対照を足すと閉じる（群 3 の移行で同型の檻を書くときも同じ）。
- **タスク 2.3 → 群 3 宛（重要）**: `field_str()` は `record_str` 経路の生値だけを返すので、`event`／`outcome` が `?expr`／`%expr` で渡された場合は `None` になる。`areka-kanade/src/schedule/log_capture.rs` と `areka-ghost/src/test_log_capture.rs` はどちらもその経路のために `format!("{value:?}").trim_matches('"')` の保険を持っている。**移行アダプタを `field_str` だけで書くとこの経路が黙って落ちる**。`field()`（Debug 表現）からの復旧経路を残すこと。
- **タスク 2.3**: 行整形の出所は実測で `LevelTargetFields` が 13 サイト・`LevelFields` が 4 サイトの計 17（`grep -rn 'format!("level='`）。17 すべてで書式文字列が 1 文字も違わないので 2 形で足りる。
- **タスク 2.4（罠・記録）**: `EnvFilter` の既定指令（`LevelFilter::ERROR`）は**解釈後の指令集合が完全に空のときだけ**足される（`tracing-subscriber-0.3.23/src/filter/env/builder.rs:322-339`・`mod.rs:350-354`）。よって `format!("{directives},...")` の文字列連結で番兵指令を足すと、`directives` が空文字列のときに既定の ERROR が黙って消え、**呼出側の `error!` が出力から失われる**。正解は `EnvFilter::new(directives).add_directive(...)`（呼出側の文字列を先に素で解釈させる）。wintf の 96 呼出はすべて非空（全部 `info` 始まり）なので現時点では潜在的な罠だが、是正は無償。
- **タスク 2.4 → 2.6 宛**: 番兵行の判定は「行が番兵の宛先文字列を含む」なので、呼出側のログ本文が `log_capture_kit::sentinel` を字面で含むとその行ごと消える（wintf には該当なしを実測確認済み）。利用手順の注意書きへ 1 行入れること。
- **タスク 2.5 → 3.1・3.2 宛（根拠の訂正）**: kit の `CaptureSubscriber` は素の `Subscriber`（span id 固定・enter/exit は no-op）で、移行元 2 者が今使っている `tracing_subscriber::registry().with(layer)` とは形が違う。**「移行対象に span は無い」は誤り**——`crates/areka-actor/src/spawn.rs` と `ui.rs` が `info_span!("actor", …)` を張り、areka-ghost／areka-seriko／areka-kanade はいずれも areka-actor に依存しているので、capture-all が観測したいまさにそのスレッドで span が流れる。**それでも無害**な理由は「移行元 2 者の layer が span の文脈を一切読まず、`event.metadata()` とイベント自身のフィールドしか見ないから」。3.1/3.2 で根拠を書くときはこちらを引くこと。
- **タスク 2.5**: `install_into` 内の `ensure_interest_probes()` と `rebuild_interest_cache()` は保険であり、**どのテストにも縛られていない**（両方外しても緑）。`set_global_default` 自身が `Dispatch::new` 経由で全発行点を再計算し、以後どのスレッドも `NoSubscriber` に落ちないため、プロセス内の変異では露出しない。
- **タスク 2.7 → 6.1・6.2 宛（重要）**: 較正テスト `crates/log-capture-kit/tests/capture_calibration_test.rs` は**わざと素の `tracing::subscriber::with_default` を直接呼ぶ**（硬化なし側の経路。1 件だけ）。要件 8.3 は `tests/` も走査対象にするので、**この 1 ファイルを理由付きで例外表に載せること**。載せ忘れて「是正」すると較正が空振りになり、本仕様の中心的な主張が無証跡に戻る。
- **タスク 2.7 → 8.3 宛（決着）**: 窓の内側の `rebuild_interest_cache()`（`capture.rs`）は**証明可能に冗長**で、正直な檻は作れない。実測: ⑴ rebuild だけを外しても窓前の焼き付きの場面は緑（`with_default` 自身の `Dispatch::new` → `register_dispatch` が全発行点を再計算するため。`tracing-core-0.1.36/src/callsite.rs:483-488`）、⑵ probe と rebuild の両方を外しても窓前の場面は緑、⑶ その状態で窓**内**の場面は赤。つまり窓前の焼き付きは rebuild の有無に関わらず治り、窓内の焼き付きは rebuild では治せない。**「引受先未定」ではなく「意図的な二重防御・檻は作れない」として台帳へ記録する**。要件 3.2 の機序自体は 2.7 の硬化あり子テストが縛っている。
- **タスク 2.7（記録の訂正）**: `AREKA_` 接頭辞の実行時環境変数は実測 13 件（本タスクの追加で 14 件）。また接頭辞は例外なしではない——`HOST32_*`（7 件）・`GHOSTDIR`・`PARENT_HWND`・`WUC_SPIKE_HOLD_MS`・`WINTF_CROSSHAIR` が別系統で存在する。
- **タスク 3.1 → 8.3 宛（群 3 全体で再発する）**: 移行の結果、`crates/areka-seriko/Cargo.toml:28` の `[dev-dependencies] tracing-subscriber` が**未使用**になった（crate 全域で参照 0）。要件 11.5 が Cargo.toml の変更を dev-dependency の**追加**に限っているので撤去は本仕様の裁量外で、**6.2 の Cargo.toml 検査も拾わない**（6.2 が見るのは「製品側依存に共有 crate が現れていないこと」だけ）。**8.3 の台帳へ引受先付きで登記すること**。同じ残渣が 3.2〜3.8 でも出る見込み。併せて同ファイル `:26-27` の存在理由コメントも陳腐化する。
- **タスク 3.2 → 8.3 宛**: `crates/areka-ghost/src/test_log_capture.rs` の `event` ラベルの Debug 復旧経路は**どのテストにも縛られていない**（現行の ghost 側 `event = …` が全件文字列リテラルのため、外しても 107/107 緑）。移行前の visitor も同じ保険を持っていたので保険は残した。規則そのものは kit 側の `field_str_is_none_for_values_that_did_not_come_through_record_str` が縛っている。ghost へ新しい檻を足すと本数の同一性が崩れるので、**台帳に書くのが正しい引受先**。
- **タスク 3.2（design.md 訂正済み）**: `#### C2` の「keeper 3 crate は `field_str("event")`／`field_str("outcome")` から組み立てる」は**誤り**だったので 2026-08-24 に訂正した。シジル形が `field_str` を通らないため、生値・Debug 表現の二経路が要る。
- **タスク 3.4 → 群 3 の残り宛（重要・design.md 訂正済み）**: kit の `CapturedEvent` は**固有メソッド `field()` を持つ**。取り出しアダプタを `field` と同名の拡張トレイトで足すと**固有メソッドが黙って優先される**（Rust の解決順）。emo-present では balloon 側の引用符剥がしが消えて**緑のまま**になり、しかも当該フィールドは全件シジル渡しで trim が実質 no-op なので誰も気付かない。別名（`field_unquoted`／`expect_field` 等）で分けること。
- **タスク 3.6 → 3.7 宛（必達）**: `crates/areka/src/emo2_boot/frame_harness_tests.rs` は `crate::placement::test_support::capture_logs` と `LogEvent::message()` を呼んでおり、これは**要件 11.3 の逐語検査の一部**。3.7 が `placement/test_support.rs` を書き換えるとき、`capture_logs` が `(R, Vec<LogEvent>)` を返し `LogEvent::message()` が使えることを保つこと。壊すと 11.3 の検査が落ちる。
- **タスク 3.6 → 8.3 宛（残余）**: 行整形の**variant 選択**（`LevelTargetFields` か `LevelFields` か）は**どのテストにも縛られていない**。起動系 6 サイトを `LevelFields` へ変異させても 1234/0 で緑のまま（assert が `target=` を 1 箇所も見ていない）。正しさは ⑴ design.md が当該 6 ファイルへ `LevelTargetFields` を明示していること ⑵ 移行前 6 ファイルの整形が逐語で `format!("level={} target={}", …)` だったことからの導出で担保した。kit の逐語 fixture は「各 variant が何を出すか」は縛るが「呼出側がどの variant を選ぶか」は構造上縛れない。6.2（走査語は迂回呼出）・6.3（行数）・8.2（反復）のいずれも引受先にならないので**台帳へ残余として記録する**。
- **道具の較正（全タスク共通・重要）**: `git diff | grep -c $'\r'` は**信用できない**。`core.autocrlf=true` のため git が diff を LF へ正規化するので常に 0 を返す。MSYS の `grep -c $'\r$'` もテキストモードで誤報する。改行の実態は **PowerShell の生バイト走査**でしか確認できない。編集ツールが CRLF ファイルを黙って bare LF に落とす事故が 3.6 で実装側・レビュー側の**両方**で起きた（いずれも検出・復元済み）。改行を含む逐語検査があれば静かに壊れる。
- **タスク 3.7 → 6.2 宛（必達・design.md 訂正済み）**: `ALLOWED_DIRECT_CALLS` は**初期値 空にできない**。`crates/areka/src/placement/{diag_tests.rs, follow_transition_diag_tests.rs, follow_window_move_diag_tests.rs}` の 3 件は実濾過に `EnvFilter` が要り、`capture_under_filter` は kit の `env-filter` feature 下で `areka` は当該 feature を有効にしない（実測 `cargo tree -p areka -e dev -i log-capture-kit -f "{p} FEATURES={f}"` → `FEATURES=` 空）。有効化は design の「wintf のみ」と要件 11.5 に反するので移行不能。2.7 の較正 1 件と合わせて**初期 4 件**。
- **タスク 3.7 → 6.1・6.2 宛（見落とすと静かに壊れる）**: 上記 3 ファイルは移行後も窓の直前で `ensure_interest_probes()`（kit の再輸出）を呼んでおり、それによって `has_just_one` が偽に固定され `never` が焼き付かない＝硬化は保たれている。**この 3 呼出を「未使用」として外すと 3 つの窓が静かに硬化を失う**。番人の例外表の理由欄に明記すること。
- **タスク 3.7（3.6 と同一の残余・8.3 宛）**: 行整形の variant 選択は areka 側でも縛られていない（`LevelFields`→`LevelTargetFields` の変異で 1234/0 のまま緑）。ワークスペース全域で同じ穴。
- **タスク 3.8 → 6.1・6.2 宛（必須）**: 走査語 `set_default(` は**アンカーしないと偽陽性を出す**。`crates/wintf/src/ecs/types.rs` の `fn test_offset_default() {` が `test_off|set_default(` で部分一致する。`(^|[^A-Za-z0-9_])set_default\(` の形にすれば wintf のヒットは 0 件になる（実測）。`with_default(`／`set_global_default(` も同型の危険があるので同じくアンカーすること。
- **タスク 3.8（残余・8.3 宛の 3 件目）**: 行整形の variant 選択が縛られていない件は wintf でも同じ（`LevelFields`→`LevelTargetFields` の変異で 22/22 緑）。**群 3 の 8 本を通じてワークスペース全域で同型**。8.3 の台帳へは 1 項目として（3.6・3.7・3.8 の 3 実例を挙げて）登記すれば足りる。
- **群 3 完了時点の状態（2026-08-24）**: 自前で捕捉先を差す箇所はワークスペース全体で **4 箇所**のみ（`areka/src/placement/` の diag 系 3＋kit の較正 1）。いずれも移行不能または意図的で、6.2 の例外表の初期値になる。捕捉機構の定義（probe／`CaptureSubscriber`／捕捉 Layer）は kit の外に 0 件。
- **道具の較正（追加）**: PowerShell の**二重引用符**ヒアストリング `@"…"@` はバックティックをエスケープ文字として食う。doc コメントを書くと `` [`X`] `` が `[X]` へ黙って変わり、コンパイルもテストも通るので目視でしか気付けない。ブロックは bash ヒアドキュメントでファイルへ書き、PowerShell では**置換だけ**する。`#[cfg(test)]` のモジュールは rustdoc が処理しないので `cargo doc` も backstop にならない。
- **タスク 4.2 → 8.3 宛（残余）**: 差し替えた 2 テストはどちらも**不在の主張**なので、注入列を短くする変異では**原理的に赤にできない**（弱めた刺激は主張を通りやすくするだけ）。一般に、負検証を赤にできる変異は「禁止された物を持ち込む」もの＝陽性対照そのものになる。注入列の同一性を守っているのはコードの逐語（リテラル範囲）と陽性対照の実験であって、テストの赤ではない。要件 4.3 の完了条件が「コード上で読み取れること」なのはこの性質と整合する。
- **タスク 5.1 → 5.2 宛（3 点・必須）**: ⑴ `chain.rs` の `arm_upload_fault`／`clear_upload_fault` に付いた `#[allow(dead_code, reason = …)]` **2 件を削除すること**（5.2 が消費し始めるので不要になる。残したままなら差し戻し対象）。⑵ `fault_point` は**一致した点でしか武装を解かない**ので、届かなかった注入（例: `CreateSourceTex` を武装して外形不変の upload をする）は同一スレッドの後続 upload で発火する。**全経路で（失敗が返った後も）`clear_upload_fault()` を呼ぶこと**。⑶ design.md の `device_err("<injected:{at:?}>")` は訂正済み（`injected_context(at)`）。
- **道具の較正（追加）**: 「通常ビルドに `#[cfg(test)]` のコードが残っていないこと」を **rlib の名前表走査で確かめてはいけない**。`#[cfg(test)] fn zzz_probe_marker_fn` を置いた較正で、非テスト rlib にもその名前が現れた。正しい道具は **release の LLVM IR**（`cargo rustc -p <crate> --lib --release -- --emit=llvm-ir` の `.ll` を grep）。ただし「0 件」が有意であることを示すため、必ず**非 0 になるはずの識別子**も同時に数えて較正すること。
- **タスク 5.2 → 8.3 宛（残余 ⒝ の補足）**: 回復の検査は毎回 `(7,3)` を使うため必ず `ResizeBuffers` 経路を通る。残余 ⒝ の後（`self.size` は旧寸・swap chain の buffer だけ新寸）に**同じ寸法の upload** が来ると `ResizeBuffers` を通らず、`CopyResource` が寸法不一致で黙って no-op になり、表示は凍ったまま `read_back` だけ正しく見える。Flow 3 の機序の記述（「`self.size` 不一致で `ResizeBuffers` を再度通り回復する」）は正確だが、その手前の散文「次回 `upload` が回復する」はそれより広い。残余 ⒝ は「実デバイスでは実質起こらない経路」として記録のみと裁定済みなので是正はしないが、台帳に 1 行添えること。

- **タスク 5.3 → 8.3 宛（残余）**: `set_bounds` のもう一方の効果＝`mount.rs:259-266` の `SpriteVisual::SetSize` は `presenter_upload_failure_tests.rs` では観測していない。`mount.rs:241` が `Arrangement` を権威と明記し、`SetSize` の失敗は `tracing::warn!` のみの最善努力なので是正はせず、台帳へ記録のみ（起票不要）。
- **タスク 5.3（檻の識別能・重要）**: 失敗経路でも `show.rs` は `mount.set_bounds(world, chain.size())` を**実際に呼ぶ**（`chain.rs:327-329` の commit が失敗し得る全操作より後ろなので `chain.size()` が旧寸のまま）。ゆえに寸法の一致だけでは design ⒝ の「未呼出」を縛れず、初版は `bounds` がどの変異でも赤にならなかった。**区別を与えるのは `mount.rs:244` の無条件書込 `arr.offset = Offset { x: 0.0, y: 0.0 }`** で、スナップショット直前に番兵 offset `(99.0, 77.0)` を植えると本番コードを 1 行も変えずに呼出そのものが観測できる。**「値が変わらないこと」と「呼ばれないこと」を同一視する主張は、書く前に本番側の無条件書込を洗うこと。**
- **道具の較正（追加）**: **復元した原本を、前の変異のまま残ったバイナリで測ってしまう。** `Copy-Item $backup $src` は元ファイルの `LastWriteTime` を保つので、変異を戻しても cargo が mtime を見て再ビルドを省き、**変異版の実体をそのまま再実行する**。このとき `git status` は clean・`git hash-object` も清浄 blob と一致するので、blob 一致は「次の走行が復元後のコードを測る」ことの根拠にならない。復元後は mtime を進める（または再ビルドされたことを実体の mtime で確かめる）こと。既知の「glob で拾う stale バイナリ」と同型で、別経路から到達する 2 例目。
- **タスク 6.1 → 6.2 宛（必須・2 件）**: ⑴ `design.md` `#### C6` の「kit 自身のディレクトリは検知走査から除外」は、同じ design が `ALLOWED_DIRECT_CALLS` に `crates/log-capture-kit/tests/capture_calibration_test.rs` を載せていることと**矛盾する**。実測（kit の `src/{capture,filter,global}.rs` が 3 件・`tests/capture_calibration_test.rs` が 1 件）と整合する読みは「**kit の `src/` は除外・`tests/` は走査する**」だけ。**この一文の訂正は 6.2 の担当**。6.1 の walker が kit を除外していないのは意図どおり（要件 10 の見張りは kit のファイルも測る）。⑵ 走査語を**開き括弧まで含めた逐語で書くと**、⒜ 見張りが自分自身を拾い ⒝ `rg -l 'with_default\('` 系インベントリの母数が動く（コメントは見張りが除去するが `rg` は除去しない）。6.1 は `concat!` と「開き括弧を除いた表記」で回避し `mod.rs:21-23` に約束として明記した。**6.2 の見張りファイルでも同じ約束を守ること。**
- **タスク 6.1（較正の空振り・重要）**: **見本を「縛るべき定数からの相対」で書くと、その定数は縛られない。** 初版は `LINE_LIMIT + 618` の形で書いていたため、`LINE_LIMIT` を 1000 → 100000 にしても 15 本すべて緑だった（＝ 6.3 の見張りが例外表 11 件を掲げたまま恒真で緑になる穴に直結した）。**閾値を縛るには見本をリテラルの実値で書き、定数そのものも別途 assert する。** 同型で、`scan_tokens` の最長一致優先の規則も「規則が発火しない語集合」でしか試しておらず、実装行を丸ごと消しても**その規則を名乗るテストごと緑**だった。**性質を名乗るテストは、その性質を実装から消すと赤になることを毎回実測すること。**
- **タスク 6.1（doc の事実誤認・4 度目）**: 最長一致規則の根拠として書かれていた「`set_global_default(` を `set_default(` で二重に数えないため」は**原理的に成立しない**（`set_g…` は `set_d…` で始まらない）。本番の語集合 3 語は 6 通りの順序対すべてで接頭辞関係も内部一致も持たないので、**どちらの重なり規則も現行では発火しない**。規則は語表が増えたときに黙って壊れないための備えであり、doc にもそう書き直した。
- **タスク 6.2 → 8.3 宛（残余 3 件）**: ⑴ `with_default_guard_test.rs:288-290` の `mentions_kit` のアンダースコア分岐（`log_capture_kit`）はどのテストにも縛られていない。ただし唯一の現実的な迂回形（`[dependencies.log_capture_kit]` 下位表＋内側の `package = "log-capture-kit"`）は本体行の分岐で拾えることを実測済みで、Cargo はハイフン package に対する裸のアンダースコア鍵を拒否する。**隙間ではなく冗長**。⑵ `:352-354` の `if name == KIT_PACKAGE { return None; }` も同様に縛られていない（kit 自身の manifest に該当行が無く `[features] env-filter = [...]` にも引用符つきの `"env-filter"` が無いので今日は挙動が変わらない）。⑶ `EXCLUDED_DIRS` が `:334` で重複している（6.1 の写しが private なため）。**2 つの除外表が黙って乖離し得る**。
- **タスク 6.2（見張りを見張る者・重要）**: 「走査語を逐語で書かない」約束を守らせているのは `scan_tokens` だけで、**`scan_tokens` はコメントを除去する**。見張りファイルの**コメント内**へ走査語を植えると、23 本すべて緑のまま `rg -l 'with_default\('` のインベントリ母数が **6 → 7 へ動いた**。是正として**生テキスト走査**（コメント除去なし・`rg` と同じ見方）の見張りを 1 本足し、同一テスト内に「コメント行の見本で必ず 1 件当たる」陽性側を置いて恒真化を塞いだ。**約束を doc に書くだけでは約束は縛られない。**
- **道具の較正（追加・改行の罠の 3 例目）**: `crates/*/Cargo.toml` は **CRLF** なのに MSYS の `sed`／`cat -A` は **LF に見せる**（`$` が出て `^M` が出ない）。`[dependencies]\n` を狙った変異スクリプトが黙って空振りし、依存方向の検査が「赤にできない」と一度**誤判定**された。真偽は `git ls-files --eol` か PowerShell の生バイト走査でしか付かない。既知の `git diff | grep -c $'\r'` と同型・別経路。
- **タスク 6.2（起草時の見積り誤りを 3 文書へ反映）**: 直接呼出の例外表は「既定で空」ではなく**実測 4 件**（移行不能 3＋較正 1）で始まる。design.md `#### C6` はタスク 3.7 で、requirements.md 8.1 と tasks.md 6.2 の本文はタスク 6.2 で訂正した。「着手時点の 2 件」は**全スレッド捕捉の別表**にかかる数字で、そちらは実測と一致する。
- **タスク 6.3 → 8.3 宛（残余）**: **例外表に載ったファイルが太っても誰も捕まえない**（例: `areka-seriko/src/bind.rs` が 1,043 → 1,500 行）。要件 10.1〜10.4 のどこにも増加を抑える条項は無く、ラチェット（例外ファイルは増やせない）を入れると**要件 10.4 で「触らない」と決めたファイルへの編集圧力**になるので、入れないのが 10.4 と整合する。ラチェットが要るなら別 spec の起票が要る。
- **タスク 6.3（例外表に行数を持たせない裁定）**: 表が表すのは「上限を超えている」という事実だけで、超過の程度ではない。行数を書くと当該ファイルへの無関係な編集のたびに表が陳腐化し、触らないと決めたファイルを触らせることになる。
- **タスク 6.3（見張りが閾値に縛られているかの確認）**: 「閾値の較正は 6.1 が持つので 6.3 では重複させない」という分業は**穴ではない**ことを実測で確認した——`LINE_LIMIT` を 100000 にすると 6.3 側でも 3 本が赤（測定側が空になり陽性側の相棒 2 本が落ちる）、500 にすると本検査を含む 3 本が赤。走査器を壊す 8 経路（`over_limit` 常時空／`measure_workspace_sources` が 1 件だけ／`line_count` 常時 0／walker が空／walker が `examples/` を落とす／例外表フィルタが常に許可 ほか）のいずれでも 6 本すべてが緑にはならない。
- **タスク 6.3（6.2 の約束表へ登録した）**: `FILES_THAT_MUST_NOT_SPELL_THE_TOKENS` へ `file_length_guard_test.rs` を追加した。当初は「行数しか見ないファイルなので対象外」と判断されたが、表の doc が「見張りと走査器」と定義しており**本ファイルも見張り**なので、code と doc が食い違っていた。実測でも、本ファイルのコメントへ走査語を植てると `rg -l` の母数が 6 → 7 へ動くのに 79 本すべてが緑のままだった。登録後は同じ変異で赤になることを確認済み（復元後は母数 6・緑）。
- **タスク 7.1（判定＝着地・分岐⒝）**: カウンタは実測でスレッド局所（`crates/wintf/src/ecs/window/command.rs:49` の `thread_local!`・`:70` の `Cell<i32>`）。並走 spec `areka-P0-draw-load-parity` は completed・アーカイブ済みで、着地コミットは PR#118 squash `327e7fd3`（HEAD の祖先）。並列で緑を固定する檻は `command_threadlocal_tests.rs:37,:90,:127` に実在し 3 passed。**よって 7.2 は実施対象**。錠の実呼出は **21 / 5 ファイル**（兄弟 4 本ぶんが 19）で 2026-08-23 から増減なし。錠の定義は `command.rs:104`（本仕様では削除しない）。
- **タスク 7.1 → 8.3 宛（開発者の判断が要る・重要）**: **要件 7.4 の引受先が実在しない。** 錠の定義の削除を `draw-load-parity` へ委ねているが、当該 spec は完了・アーカイブ済みで引受不能。しかも `completed/areka-P0-draw-load-parity/design.md:226` は逆に「退役候補（cage が受ける）」と書いており、**両者が互いに相手へ委ねる閉ループで、片端がアーカイブ済み**。進行中 spec で `command.rs` の所有を主張するものは 0（`present-write-coherence/brief.md:154`・`emo2-conformance-e2e/brief.md:42,171` の 3 件はいずれも本番側の観測点一覧であって所有ではない）。**さらにこれは 2 度目**——`completed/areka-P0-dpi-transition-atomicity/tasks.md:308` が同じ `SELF_INITIATED_DEPTH` を「台帳へ登記して申し送る」と書き、同 spec の `mechanism-ledger.md:769` が「**台帳への登記が行われておらず、引受先も 0 だった**」と最終ゲートの実測で記録している。8.3 で ⒜ 進行中 spec へ引受け ⒝ 新規起票 ⒞ 残置裁定 のいずれかを開発者が決めること。**2 度落ちた項目なので、もう一度申し送るのは選択肢に入らない。** 残置が安全な根拠は記録済み（`#[cfg(test)]`・`.cargo/` 不在・root と `wintf` に `[lints]` 無し・`crates` 全域に `deny(warnings)`/`forbid(warnings)` 無し＝呼出 0 でも警告どまりで赤にならない）。
- **タスク 7.1 → 7.2 宛（帰属の訂正）**: 「プロセス共有」のまま**現在形で誤っている**説明文は **4 件**（`command_batch_tests.rs:25`／`command_transition_tests.rs:28`／`window_pos_tests.rs:40`／`window_pos_transition_tests.rs:21`）。語の総ヒットは 30 行 / 21 ファイルだが、21 件は**別の共有物**（起床旗・スレッド名簿・spawn フック・`OnceLock` の `PatternState`・書込コマンド待ち行列）の正しい説明、5 件のうち 3 件（`command.rs:77`「だった頃」・`command_threadlocal_tests.rs:35`「是正前」・`areka/src/emo2_boot/frame_harness_tests.rs:10`「既に一度…出している」）は**過去形で正しい**。**語で数えると 26 件の正しい説明を壊す。** なお**この 4 件は要件 2.4 の対象ではない**——要件 2.4（`requirements.md:78`）は捕捉サイトの「`with_default` はスレッドローカルゆえ…」型の誤説明に固有で、置換先も interest cache の話。`SELF_INITIATED_DEPTH` の説明文に固有の要件は無く、根拠は design `#### C5` の「同じ塊で是正する陳腐化」の行。**7.2 は C5 の権限で直し、要件 2.4 の被覆を主張しないこと。**
- **タスク 7.1 → 8.3 宛（design 上の問い）**: 要件文書（`requirements.md`）が実測の記録（申し送り台帳）を持つ形になった。`design.md:40` と `:582` がまさにその置き場所を指定しており規範的な行は 1 つも変えていない（46 挿入 / 0 削除）ので実装は design どおりだが、「要件は規範的な記述だけに保ちたい」なら台帳を別ファイルへ移すのは**design の判断**であって実装の欠陥ではない。開発者の裁定を仰ぐこと。
- **タスク 7.2（錠の退役・実施済み）**: 実呼出 21 → **0**（`command.rs:961,973`／`command_batch_tests.rs` 5／`command_transition_tests.rs` 4／`window_proc/window_pos_tests.rs` 5／`window_proc/window_pos_transition_tests.rs` 5）。定義は残置（`command.rs:99`・`:104` から移動）。**走査式は較正済み**——同じ式を `git show HEAD:<file>` に当てると 21 が返り、作業ツリーでは 0。「プロセス共有」の現在形の誤り 4 件を是正し、残る 26 行は `(ファイル, 行内容)` の対比較で**1 行も動いていない**ことを確認。負荷下の反復は実装 36 回＋レビュー独立 156 回がすべて緑（各回 `842 passed` を確認。**終了コードだけでは空振りを弾けない**——フィルタの綴り誤りは `0 passed; 842 filtered out` で終了コード 0 になる）。clippy は 151 → 152（未使用の定義 1 件のみ）。
- **タスク 7.2 → 8.2 宛（必須）**: **反復の証跡がリポジトリに無い**。`verification/` には `remeasure.md` しか無く、30 回超の反復が行われたことを記録した成果物が未コミット。タスク記述が「証跡は 8.2 の記録へ合流」と定めているので範囲違反ではないが、**現時点では完了条件の第 3 項が実装者の申告にしか立っていない**。8.2 は ⑴ 負荷の定義 ⑵ **各回の passed 件数**（終了コードだけでは不可）を記録すること。
- **タスク 7.2 → 8.2／8.3 宛（安全性の主張を実証の広さに合わせる）**: 錠が付随的に守り得た状態の列挙は 3 つの thread_local を挙げたが、`command_batch_tests.rs` が `with_forced_batch_begin_failure` で実際に動かす 4 つ目（`command.rs:361` の `FORCE_BATCH_BEGIN_FAILURE`）が漏れていた。これも thread_local なので結論は変わらないが、正しい形は「**`command.rs` と `transition_diag.rs` のすべての static が thread_local であり、例外は退役した錠の内側の `LOCK`（`command.rs:100`）だけ**」。この形で記録すること。
- **タスク 7.2（帰属の訂正を全文書へ反映）**: 説明文 4 件の是正は**要件 2.4 の対象ではない**（2.4 は捕捉サイトの `with_default` 誤説明に固有で置換先も interest cache の話）。根拠は design `#### C5` の「同じ塊で是正する陳腐化」。design は 7.2 で訂正済み、tasks.md の `_Requirements:_` 行も同時に訂正した。**片肺の訂正は「GO が矛盾文書の上に立つ」形になるので、裁定で帰属を変えたら参照側も同じコミットで直すこと。**
- **道具の較正（追加・バッククォートの罠の 2 例目）**: **bash の二重引用符の中でもバッククォートは食われる**（コマンド置換）。`python -c "..."` の中に doc の逐語（`` `with_default` `` や `` `#### C5` ``）を書いたところ、当該部分が**空文字へ黙って置き換わった**まま tasks.md へ書き込まれた。エラーは 1 行出るが処理は続行し、`ok` も表示されるので**成功したように見える**。既知の PowerShell 二重引用符ヒアストリング `@"…"@` の罠と同型で、シェルが違うだけ。**逐語を含む本文は、必ず引用符つきヒアドキュメント（`<<'EOF'`）でファイルへ書き出してから読み込むこと。** インラインの `-c "..."` に本文を埋めない。
- **タスク 7.2（自分の走査器が嘘をついた実例）**: 実装側が最初に使った PowerShell 走査は「LF の直前が CR か」しか見ておらず、**全行が `\r\r\n` になっているのに「CRLF で正常」と報告し続けた**。気付けたのは `cargo fmt` の `bare CR not allowed in doc-comment` だけ。**改行を数える道具は bare CR も独立に数える形へ較正すること**（CR 総数・LF 総数・CRLF・bare CR・bare LF の 5 つを別々に採る）。レビュー側が独立にバイトを数え直し、最終状態は 5 ファイルとも bare CR 0・混在 0 で着手前の `git ls-files --eol` と一致することを確認した。
- **タスク 8.1（反復ハーネス）**: `verification/repeat-tests.ps1`（対象と回数を受けて反復・各回の出力を保存・要約へ追記）＋ `repeat-tests.md`（手順書）＋ `summary.md`（記録の正本）。**判定は 6 値**（緑／赤／空振り／件数不一致／ビルド失敗／打ち切り）で、緑と数えるのは「緑」だけ。レビューが独立に 6 値すべてを再現し、**緑に化ける経路は 1 つも構成できなかった**。実行体は `cargo test --no-run --message-format=json` から解決し、パス・サイズ・更新時刻を刻む（刻印 72 本と走行 92 本の差はドックテスト 20 本＝算術一致を確認済み）。
- **タスク 8.1（検証ハーネス自身の待機が無界だった）**: 初版の `WaitForExit()` に上限が無く、**要件 4 が「待機の有界化」そのものである spec で、検証側の待機が無界**という形になっていた。緑に化ける穴ではないが、8.2 は約 100 回を無人で回すので 1 回のハングで全体が止まり記録も残らない。`-TimeoutSec`（既定は `max(120, ceil(単独実測秒 × 同時数 × 10))`）を入れ、上限に達した回はプロセス木を停止して 6 つ目の判定「打ち切り」として記録する形へ是正。**上限が効くことは、意図的にハングする対象（`loop { sleep(3600s) }`）で実測済み**。**規律は検証の道具にも当てること。**
- **タスク 8.1（打ち切りが掘り当てた副次の欠陥）**: 打ち切り直後は止めたプロセスのリダイレクト先ハンドルが OS 側でまだ開いており、ログの読み出しが `being used by another process` で例外になって**ハーネス自体が要約を書かずに落ちた**。有界（10 回 × 200ms）の読み直しへ是正。読めなかった場合は黙って空にせず、その旨を要約へ書く。
- **タスク 8.1 → 8.2 宛（必須・3 件）**: ⑴ **100 回を回す前に対象表の期待件数を採り直すこと**（`workspace 5865 / wintf 842 / seriko 200 / wait 2 / kit 79` は時点の実測。テストが増減していると全回「件数不一致」になり全部やり直しになる。**`-- --list` の行数を期待件数にしないこと**——`--list` は ignored も数える）。⑵ **7.2 の実測（実装 36 回＋レビュー独立 156 回）を本ハーネスで採り直して記録に合流させること**（現状リポジトリに証跡が無い）。⑶ `areka` はライブラリを持たないので待機 2 テストは `--bins` で当てる。
- **タスク 8.1 → 8.2／8.3 宛（記録の正本をどこに置くか）**: `logs/` は非追跡（試走だけで 2.5MB・本走行は桁が 1 つ増える）なので、squash-merge 後は `summary.md` だけが残り、そこに書かれたログ名の実体が無くなる。要件 9.4 の義務（赤が出たらログを保存）は `red/` が追跡なので果たされているが、**8.2 の完了条件「保存されたログで裏付けられている」が派生記録の上に立つ**。8.2 が縮約版（各回の `test result:` 行）を追跡するか、8.3 が「`summary.md` を正本とする」と明記するかを決めること。
- **タスク 8.1（環境固有パスの伏せ字）**: 追跡ファイルへ OS アカウント名とセッション ID を含む絶対パスが入っていた。較正用の使い捨てクレートを一時ディレクトリ直下の固定名へ移し、`TEMP`／`TMP`／`USERPROFILE` を伏せる形へ是正。**無加工の生ログは非追跡の `logs/` に残る**ので診断は可能。**伏せ字だけ手で書き換えると偽の記録になる**ので、最終版のコードで採り直した節に差し替えてある。走査で追跡側 0 件・非追跡側 1 件＝空が道具の不在でないことも較正済み。

- **タスク 8.2（本走行の結果）**: 規定 4 対象を本ハーネスで回して **合計 100 回・緑 100**（`r91-workspace` 10 回 @同時 2・`r92-seriko` 30 回・`r93-wait` 30 回・`r72-wintf` 30 回 @同時 4）。赤・空振り・件数不一致・ビルド失敗・打ち切りはいずれも 0。**走らせる前に期待件数を採り直した**結果 5 対象とも増減なし（`workspace 5865 / wintf 842 / seriko 200 / wait 2 / kit 79`）。`r72-wintf` の 30 回が**タスク 7.2 の欠けていた証跡**（実装者の申告にしか立っていなかったもの）を初めてリポジトリへ載せた。レビューが要約の合計欄ではなく `logs/` の `test result:` 行から独立に集計して全数値を再現している。
- **タスク 8.2（要件 9.5 の決着＝別系統）**: 規定 4 対象は 1 度も赤にならなかったので `cargo test -p areka` を追加で 30 回（同時 4）回したところ **30 回中 3 回が赤**。2 系統・3 テストで、原因はどちらも**プロセス内では一意だがプロセス間では共有される固定の一時パス**（`crates/areka/src/placement/transition_signoff_tests.rs:102` の `areka-transition-signoff-empty.log`／`crates/areka/src/main_restore_seam_tests.rs:16-20` の `unique_temp_dir`）で、同時走行が互いの前提ファイルを消し合う。**捕捉でも待機でも退役した錠でもない。** 同じコマンドを同時 1 で 10 回回すと全緑。**この切り分けは 10 回の緑だけに寄りかかっていない**——`unique_temp_dir` の札は 3 テストで全て別、当該ログの利用者はワークスペース全域で 1 テストのみ、という静的構造から単独走行の安全性が出る（10 回だけなら偶然の余地が 3 割強残る）。**2026-08-05 の 553/1 との同一性は主張しない**（当時テスト名が採られておらず同定不能）。是正は `crates/` の変更なので 8.2 の範囲外＝8.3 へ**引受先未定**で残置。
- **タスク 8.2（R-3＝所要時間の移行前後比較・否定的な結果）**: 移行前（分岐点 `327e7fd3`）の中央値 39.6 秒 対 移行後 41.7 秒＝**+2.1 秒 / +5.3%**。ただし**この差を「常時の有効判定になった費用」とは読めない**——移行後はテストが 111 件・実行体が 6 本多く、移行前側の揺れ（34.6〜77.8 秒）が差の 20 倍ある。分離には同一テスト集合で硬化の有無だけを切り替える測り方が要る（8.3 へ **引受先未定**で残置）。**比較の途中で移行前ツリーが素の `cargo test --workspace` で完走しないことが判明**（赤が出て cargo が 92 本中 58 本で打ち切る）ため両側とも `--no-fail-fast` で採り直した。その赤 2 件は本ブランチが `40ee8460` で既に是正した LF 固定 needle の件。比較対象は `origin/main` ではなく分岐点（main は無関係な 3 コミット先行）。
- **タスク 8.2（判定が恒真になる節がある・記録のみ）**: `-ExpectPassed` を指定せずに回した節（`r95-areka`・`r95-areka-serial`・`r3-*`）は `repeat-tests.ps1:347` の `$ExpectPassed -ge 0` ガードで**「件数不一致」の判定式そのものが働かない**ので、それらの節の「件数不一致 0」は恒真。規定 4 対象には期待件数が入っているので要件 9.1-9.3 の主張は影響を受けない（レビューの独立集計でも緑 27 回すべて 1241 passed で一致）。**「0 と書いてあるのに何も測っていない」は本仕様が退治している形そのものなので、次に対象を足すときは期待件数を必ず持たせること。**
- **タスク 8.2（赤ログの間引き＝規則の例外を自分で作った）**: `r3-before` の赤は 5 回とも同一原因の決定論的な赤なので代表 1 組だけ `red/` へ追跡し 6 組（約 3.6MB）を落とした。手順書 §4 の「赤は再生成できないから追跡する」は再現しない赤に向けた趣旨と読んだもの。**追跡済みのファイルは 1 件も消えていない**ことをレビューが `git log --name-only -- verification/red/` で確認済み（8.1 の 6 ファイルのみが履歴にある）。無加工の 7 組は非追跡 `logs/` に現存。
- **タスク 8.2 → 8.3 宛（受け渡し表は `summary.md` §8・4 項目）**: ⑴ 一時パス共有による赤の起票先（**引受先未定**）⑵ 所要時間の分離の要否（**引受先未定**）⑶ `summary.md` を記録の正本とする宣言の台帳への登記 ⑷ 錠の退役の安全性の主張の正しい形。⑷ は `tasks.md:331` の申し送りがどこにも記録されていなかったのをレビュー指摘（FINDING 11）で拾ったもの——**本仕様は受け渡しの穴で既に 2 度取りこぼしている**（要件 7.4）ので、転記ではなく数え直して載せた: `command.rs` と `transition_diag.rs` の static は **6 個**、うち 5 個が `thread_local!` の中、**例外は退役した錠の内側の `LOCK`（`command.rs:100`）1 個だけ**。素の `grep "static "` は 12 件当たるが 6 件は `&'static str` の型注釈＝**走査語がそのまま使えない実例**。
- **タスク 9.1（起草した file:line が実物とずれていた・この spec で 6 度目）**: 削除範囲は tasks.md が `:88-102`、requirements.md 7.4 の条文が `:98-102`、design.md C5 が `:88-102` と**三者三様に書いてあり、三つとも実物と違った**。この関数の説明文は `:73` から始まる 25 行で、どの範囲で切っても説明文の一部が項目に付かない孤児 doc コメントとして残り `error: found a documentation comment that doesn't document anything` でコンパイルが落ちる。正しい実体は `:73-102`（＋直後の空行）＝**31 行の純削除**。3 文書とも実測値へ訂正した。**教訓は「1 つの文書を直せば済むと思わないこと」**——同じ数字が 3 箇所に写されていた。
- **タスク 9.1（削除は「消す行」だけでなく「消える参照」を数える）**: `command_threadlocal_tests.rs:19`（当時）が削除対象へ `[\`lock_self_initiated_for_test\`](super::lock_self_initiated_for_test)` という**説明文内リンク**を張っていた。放置しても `cargo test` も `cargo clippy` も緑のまま通り、**`cargo doc` だけが壊れる**。完了条件に `cargo doc` が無いので**この spec の檻には原理的に映らない**種類の陳腐化だった。境界を「`command.rs` と、削除により壊れる同 crate の説明文」へ広げて同じ変更で解消した。**次に何かを削除するときは、識別子の全ヒットを実行行と説明文内参照に分けて数えること**（実行行 0 件でも説明文内参照は残り得る）。
- **タスク 9.1（「1 件減った」は総数だけでは証明できない）**: clippy の診断総数が 131 → 130 になっても、別の警告が 1 件増えて 1 件減れば同じ数になる。決着させたのは**行番号を除いた突合**——削除後に残る `command.rs` の警告 2 件は `:298`／`:906` で、削除前の `:329`／`:937` と**行内容が逐語一致し差はちょうど 31**（純削除の行数）。純削除以外に何も動いていないことが構造で言える。**なお件数は数え方を明示しないと食い違う**: 7.2 の記録「151 → 152」は同じ走行に同居した `dola` の 21 件を含む数で、wintf 単独では `152 − 21 = 131` → `130`。
- **タスク 9.1（道具の罠・9 種目＝末尾に改行の無いファイルへの追記は前の行と黙って融合する）**: 本節へ 3 行を追記した際、`cat >> file` の 1 行目が既存の最終行の**末尾に連結**された（当時この `tasks.md` は末尾改行を持っていなかった）。エラーも警告も出ず、`tail` で見ると 3 行のうち 2 行しか見えないという形で初めて気づいた。**追記の前に末尾バイトが改行かを確かめること。** 併せて、行末の検査は `grep -c $'\r'` のような シェル依存の式ではなく**バイト単位で数える**こと——同じセッション内で同じ式が 0 と 442（全行）の両方を返し、正しかったのはバイト単位の `CR 0 / LF 442 / CRLF 0` だった。
- **タスク 10.1（窓口 crate の新設）**: `crates/temp-path-kit/`（`Cargo.toml` 10 行／`src/lib.rs` 120 行／`src/lib_tests.rs` 133 行）。**依存 0**（`[dependencies]` 節そのものが無く、`cargo tree -p temp-path-kit` の出力はノード 1 行）。root `Cargo.toml` は `members = ["crates/*"]` なので**無改変で自動加入**する。名前は `areka-{札}-{プロセス識別子}-{連番}` を組む唯一の内部関数 `compose_name(label, process_id, serial)`（`lib.rs:73-75`）で作り、実際に作る `TempPath::new`（`lib.rs:90-96`）が**その関数を通る**。宛先の種類は増やさず、単一ファイルは `child(name)` でディレクトリの下に取る。本数は unit 8＋doctest 1 の **9 本**で、ワークスペース合計は **5,865 → 5,874**（基準線は 8.2 と逐語一致）。既存ファイルは 1 つも変更していない（`git status --porcelain` が `?? crates/temp-path-kit/` の 1 行だけ）。
- **タスク 10.1（レビュアが変異検査を 4 種実施した——これが本仕様の作法の見本）**: 「内部関数を作ったのに本番が別の式で組む」なら自己テストは何も測らない。レビュアは⒜本番の名前式を末尾 1 文字違いへ⒝識別子を無力化⒞`Drop` を no-op 化＋連番を定数化——と実際に壊して、狙ったテストがそれぞれ赤になることを確かめ、`sha256` 一致で復元した。**ただし限界も 1 つ判明**: `compose_name` を呼ばずに**完全に等価な**インライン式へ書き換えると 8 本とも緑のまま通る（唯一の信号はコンパイラの未使用関数警告）。固定されているのは「本番が組む名前の**値**が内部関数の式と一致すること」であって「呼出そのもの」ではない。本タスクが潰す欠陥（本番が**別の**名前を組む）は⒜⒝で確実に赤になるので合格としたが、**同型の設計をするときは「値の一致」と「呼出の存在」が別物であることを憶えておくこと**。
- **タスク 10.1（境界外で間欠赤を 1 件掘り当てた・要件 9.5 の記録対象）**: `cargo test --workspace` の 1 回目で `crates/areka-ghost/tests/ghost/spine_e2e_test_s3_helper_liveness_detected.rs:180-184` の `assert_eq!(boot_prefix_len, 5, …)` が `left: 4 / right: 5` で赤になった（2 回目は全緑・単独 5 連走も全緑）。**本仕様の要件 4 と同型の構造欠陥**——有界スピン（`:144-160`。`while` は `:145`）が待つのは記録が非空になることだけで、その直後の 5 呼出の数え上げ（`:173-179`）と判定には**待機が 1 つも無い単発のスナップショット**である。`:132-141` のコメントは「5 本は先に完了しているはず」と論じるが、それは**仮定であって強制ではない**。負荷下で 5 本目が間に合わないと 4 のまま読まれる。`verification/logs/` の既存全ログではこのテストは常に緑で、**赤の記録は今回が初**。10.1 の境界外なので触っていない。**引受先は 12.1 で決めること。**
- **タスク 10.2（本体 crate 6 ファイルの移行）**: 対象の選定は実測で確定した——`crates` 全域で `env::temp_dir()` を使うのは 39 ファイルだが、うち 17 は既にプロセス識別子を持つ（10.1 で足した窓口自身を含む）。**候補 22**、読み出しのみの 2 件（`placement_monitor_tests.rs:250`・`shiori-host32-host/tests/error_paths.rs`）を除いて **20**＝areka 6／areka-ghost 12／areka-parsers 2 で、要件 12.2 の記録と逐語一致。`cargo test -p areka` は移行の前後とも **1238 passed / 0 failed / 1 ignored**（＋ `emo2_real_run` 1・`smoke_boot_loop_exit` 2）。**4 プロセス同時 × 3 巡＝12/12 緑**をレビュアが独立に再現し、走行後に自分の札の残骸が 0 件であること（破棄が効いていること）も確認した。
- **タスク 10.2（移植元 `TempDir` を窓口の薄い包みにした・設計 C8 の「実装時に決める」の決着）**: `placement_shared_test_support.rs` は「既に正しい 17」の 1 つだが、対象 6 の `placement_prepare_tests.rs` がその消費者だった。2 系統を並存させると 10.5 の見張りが「迂回」と見分けられなくなるので、**窓口へ寄せて `new()` の署名は無引数のまま**にした。おかげで対象外の消費者 2 ファイル（`placement_windowposition_tests.rs`・`placement_windowposition_vocab_tests.rs`）は 1 行も触っていない。**ただしレビュアが記録の食い違いを 1 件見つけた**——同 crate の `measure_tests.rs:34-72` に**同型の自前実装がまだ残っている**ので、「crate 内に 2 系統を残さない」という説明文は事実として成り立たない。**`measure_tests.rs:44` を例外表へ載せる根拠の記録は 10.5 の宿題**（設計 C8 が「根拠を記録する」と定めた当の項目）。
- **タスク 10.2（要件が引く根拠の行を、その要件の実施が消した）**: 要件 12.7 が較正の根拠として引いていた `main_restore_seam_tests.rs:15` の「外部 tempfile 非依存」というコメントは、**まさにこのタスクの移行で消えた**。要件側に「再現したいときは 10.2 の親コミットを見よ」と注記した。**自分が引く証拠を自分の作業が消す形**は今後も起きるので、`file:line` を根拠に挙げるときは「消える側かどうか」を一度考えること。
- **タスク 10.2（境界外で観測・10.3 と 12.1 へ）**: ⑴ `%TEMP%` に `areka-ghost-shiori-wiring-test-{pid}-ThreadId(n)` が **1,934 件**残留していた。`crates/areka-ghost/src/shiori_wiring.rs:102-110` が作る一方、同ファイルに `remove_dir_all`／`remove_file`／`Drop` が **1 つも無い**（レビュアが独立に確認）＝**後始末が存在しない**。10.3 が窓口へ寄せれば `Drop` で解消する。⑵ 移植した型は `create_dir_all` なので、**プロセス識別子が再利用され連番も一致すると既存ディレクトリを黙って再利用する**。要件 12 が狙う失敗クラスそのものだが移植元からの既存の性質であり、10.2 の欠陥ではない。**12.1 の台帳向け。**
