# Implementation Plan

> **性格**: 文書＋検証仕様（2026-08-14 裁定による縮小後）。製品コードの式・署名・既存テストの期待値は 1 つも変更しない。
> **着手順の要点**: 実測（タスク 1）→ 登記（タスク 2）の順を守る。裁定の根拠として登記へ書き込む数値は、先にテストで再現してから記す。

- [x] 1. 裁定の前提を固定する決定論テスト
- [x] 1.1 テスト基盤（比集合導出・整数オラクル・本番同一経路ヘルパ）と較正
  - 到達比を DPI 格子（作者 {72,96,120,144} × モニタ {96,120,144,168,192,216,240,288}）から gcd 約分・重複排除で導出するヘルパを置く
  - 真値オラクルを整数演算のみ（u64 中間の切り上げ除算）で組み、浮動小数を一切用いない
  - 本番と同一経路（既約有理→f32 出口ビュー→拡大契約→供給面寸導出）で寸を得るヘルパを置く
  - ファイル冒頭 doc に裁定日・出典 spec 名・計測日、およびこの檻の到達範囲は f32 出口ビュー以降の算術であり提示段から文字層への搬送は素通し前提である旨を記録する
  - 観測可能な完了: 較正 assert が緑（到達比がちょうど 23 個・6/5 と 12/5 を含む／既知値の逐語再現＝比 6/5・寸 25 で本番経路が 31・整数オラクルが 30 を返し両ヘルパが同時に動作確認される）
  - _Requirements: 3.6, 3.7_

- [x] 1.2 全到達比・全寸の上下界テスト
  - 23 比 × 寸 1..=1200 の全組で、導出結果が真値以上かつ真値との差が 1 以内であることを検証する
  - 失敗メッセージに分子・分母・寸・実測値・真値を含め、単独で再現条件が読める形にする
  - 実 DPI モニタ・GPU・実窓を用いず、拡大率を注入するだけで走ること、および全域で桁溢れ・パニックが起きないことを確認する
  - 観測可能な完了: 27,600 評価すべてで違反ゼロ（真値未満または差 2 以上が 1 件でもあれば赤）
  - _Requirements: 3.1, 3.2, 3.3, 3.5, 3.6, 3.7_

- [x] 1.3 誤り件数と代表例の固定
  - 誤りが出る 2 比について件数をちょうど 81 件ずつに固定し、代表例（寸 25 → 31・正 30／寸 25 → 61・正 60）を個別に検証する
  - 残る 21 比は誤りゼロであることを検証する
  - 是正しない判断を期待値として固定する旨と計測日を doc コメントへ記録する
  - 同一入力に対し常に同一結果であること（浮動小数演算の決定論）を前提として明記する
  - 観測可能な完了: 件数・代表例・ゼロ件集合の全 assert が緑（件数が動けば赤＝裁定の再審トリガ）
  - _Requirements: 3.4, 3.6, 3.7_

- [x] 2. 既知欠陥登記を裁定済みへ書き換え、例外文面の正典を確定する
  - 見出しの「未是正・担当 spec 不在」を裁定済み・許容である旨へ差し替える
  - 本文に残る「開発者裁定待ちとして本コメントに登記する」および「能動的な担当 spec は実在しない」の一文を、担当 spec が本仕様として実在し裁定が下りた旨へ書き換える（先送り規律の参照は残す）
  - 裁定の事実・裁定日（2026-08-14）・4 根拠（誤差は +1 側のみで文字が切れない／不可視／救える範囲が「1.2 の f32 表現」一点／署名追随 112 箇所と釣り合わない）を本文へ記す
  - タスク 1.3 で実測した 23 比総当たりの結果を要約し、既存の 2026-07-30 実測表は根拠として保持する
  - 出典として本 spec 名と、前提を固定するテストへの参照を記す
  - 他 4 箇所へ転記する例外文面の正典（例外は文字供給面の確保寸ただ 1 点・他用途へ拡大禁止・裁定への参照）をこの登記内に確定させ、以降のタスクはこれを転記するだけにする
  - 供給面寸の導出式そのものには 1 文字も触れない
  - 観測可能な完了: 当該 doc に「裁定待ち」「担当 spec 不在」系の語が見出し・本文とも 1 つも残らず、裁定日・4 根拠・両実測・出典参照・転記用の正典文面がすべて揃っている
  - _Depends: 1.3_
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [ ] 3. 浮動小数禁止宣言への例外明記
- [ ] 3.1 (P) 拡大率の数学層の宣言へ例外を追記
  - モジュール冒頭の「画素・寸法演算に浮動小数を持ち込まない」宣言へ、タスク 2 で確定した正典文面を 1 項として転記する
  - f32 出口ビューの「寸法・画素演算に使ってはならない」宣言へ、裁定済みの唯一の例外である旨を接続する（禁止の主文と丸め権威への誘導は削除しない）
  - 観測可能な完了: 2 箇所の宣言が正典文面どおりの例外を含み、規則本文は削除されず残っている
  - _Depends: 2_
  - _Requirements: 2.1, 2.2, 2.4, 2.5_
  - _Boundary: C2 例外明記（拡大率の数学層）_

- [ ] 3.2 (P) 提示段の警告 doc へ例外の所在と参照先を追記
  - 文字スロットの現況公開に付された 2 つの警告へ、タスク 2 で確定した正典文面に沿って例外の所在（文字層の供給面寸のみ・裁定済み）と参照先を 1〜2 文で転記する
  - 窓 client 寸を拡大率から掛け算で復元することの禁止は例外化せず、従来どおり禁止のまま据え置くことを明示する
  - 観測可能な完了: 2 つの警告が例外の所在と参照先を含み、窓寸復元禁止は無傷で残っている
  - _Depends: 2_
  - _Requirements: 2.3, 2.4, 2.5_
  - _Boundary: C2 例外明記（提示段）_

- [ ] 4. 下流仕様とロードマップへの申し送り
- [ ] 4.1 (P) 適合 e2e への +1 許容の申し送り
  - 供給面寸の判定は絶対値一致では書けず期待値 +1 の許容が必要であること、窓 client 寸は従来どおり絶対値で書けることを追記する
  - 「画素演算の有理数化により絶対値で書ける」とする既存記述へ失効注記を付す（対象行は着手時に現物で再確認する）
  - 根拠は再説明せず本 spec 名の参照で済ませる
  - 観測可能な完了: 当該 brief に +1 許容の要否と失効注記が入り、絶対値前提の記述が有効なまま生き残っていない
  - _Depends: 2_
  - _Requirements: 4.1, 4.4_
  - _Boundary: C4 申し送り（適合 e2e）_

- [ ] 4.2 (P) バルーン位置 DPI 追従 spec への前提申し送り
  - 有理数の文字層配管は行われないこと、既存の「配管を前提にする」記述が失効することを追記する
  - 丸め権威そのものは既存のまま利用可能であり、供給面寸の例外は文字層の 1 点に限られ当該 spec へは適用されないことを明記する
  - 観測可能な完了: 当該 brief に配管失効と丸め権威存続の区別が入っている
  - _Depends: 2_
  - _Requirements: 4.2, 4.4_
  - _Boundary: C4 申し送り（バルーン位置 DPI）_

- [ ] 4.3 (P) ロードマップの該当 3 箇所を縮小後の実態へ改訂
  - 改訂対象は設計が名指しする 3 箇所のみ（基盤ゴール表の当該行・W6.5 のウェーブ行・因果台帳の当該行）
  - ゴール表の当該行を「裁定の登記＋前提の決定論テスト（有理数配管は 2026-08-14 却下）」へ改訂する
  - W6.5 行の記述と因果台帳の下流注記を「申し送りのみ・丸め権威は既存充足」へ追随させる
  - ウェーブ編成上の着地スロット自体は維持する（登記・テスト・申し送りという着地物があるため）
  - 他ウェーブ行・過去の追記履歴に現れる当該 spec への言及は縮小後も真であるため変更しない
  - 観測可能な完了: 指定 3 箇所が縮小後の実態を指し、かつ他ウェーブ行・追記履歴の言及は差分ゼロ
  - _Depends: 2_
  - _Requirements: 4.3, 4.4_
  - _Boundary: C4 申し送り（ロードマップ）_

- [ ] 5. 非回帰の確認
  - 文字層・拡大率の数学層・提示段の 3 crate のテストを実行し全緑を確認する
  - 既存テストの期待値が 1 つも変更されていないことを確認する
  - 製品コード側の差分が doc コメント行のみであること（式・署名・use・属性に差分がないこと）を差分目視で確認する
  - 観測可能な完了: 3 crate 全緑（新規テスト 3 本を含む）かつ製品コードの差分が doc 行のみ＝供給面寸・連続量・レイアウトの結果不変が構造的に成立している
  - _Depends: 1.2, 1.3, 2, 3.1, 3.2_
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_

## Implementation Notes

- 1.2 レビュー時、検証者が独立実装（Python float32・Fraction 由来の比集合）で 23 比 × 1..=1200 の全 27,600 組を再走査し、下界違反 0・上界違反 0・差の分布 {0: 27438, 1: 162}・比別誤り件数 6/5=81・12/5=81・残り 21 比=0 を再現した。タスク 1.3 が固定する 81/81/0 は独立の道具で事前確認済み。
- 1.3 レビュー時、検証者がさらに 2 つ目の独立実装（standalone rustc・BTreeSet 由来の比集合・u128 オラクル）でも同一の数値を再現した。**タスク 2 の登記へ書き込む 2026-08-14 実測の確定値**: 到達比 23・評価 23×1,200＝27,600 組・差は常に 0 か 1（−1 は 1 件も出ない）・差 1 は 162 件（6/5 で 81・12/5 で 81）・残る 21 比は 0 件・代表例は 6/5 の寸 25 → 31（真値 30）と 12/5 の寸 25 → 61（真値 60）。
- 決定論テストの現物は `crates/areka-emo-text/tests/physical_extent_arbitration_test.rs`（テスト 4 本）。タスク 2 以降の登記・例外文面はこのパスを参照先とする。
- **タスク 3.1／3.2 が転記する正典文面**（タスク 2 で `crates/areka-emo-text/src/region.rs` の「## 転記用の正典文面」節に確定。design.md C2 共通文面と逐語一致）:
  > 唯一の既知の例外は emo-text `ScaleContract::physical_extent`（文字供給面の確保寸）であり、2026-08-14 の裁定（spec `areka-P0-scale-exact-rational`）に基づく。誤差は +1 側のみで不可視。**この例外を他の用途へ拡大してはならない**。
- 2 レビュー時、「112 箇所」を今日再計測すると 113 になることが判明した。差分 1 件の正体は**本仕様のタスク 1.3 が追加したテストファイル自身の `ScaleContract::new(` 呼び出し**であり、裁定時点の 112（本番 3・テスト 109）は正しい。登記側に「2026-08-14 裁定時点の計測」の注記を足して将来の読み手が陳腐化と誤読しないようにした。
