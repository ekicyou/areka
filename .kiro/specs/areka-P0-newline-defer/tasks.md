# Implementation Plan

- [x] 1. layout.rs 本番改訂: 改行を「文字書き込み位置を次行先頭へ動かす予約」として保留・実体化する意味論への変更
- [x] 1.1 改行マーカー到着時に即座の行送りをやめ、走査ローカルの保留値へ行送り量（ratio）を累算する処理に変更する
  - 改行マーカーの到着では行を閉じず・block 位置も前進させず、保留値（未保留なら新規保持・保留中なら加算）へ ratio を積む
  - 連続する複数の改行マーカーは単一の累算保留として扱う（中間で個別の行を開かない）
  - 既存の ratio 正準表（行送り量 = line_pitch × ratio）の解釈自体は変更しない
  - 観測可能な完了条件: 改行マーカーのみが連続する入力を与えても、可視グリフを1つも含まない限り出力行数が変化しないことをその場で確認できる
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - _Boundary: LayoutEngine::layout_
- [x] 1.2 可視グリフ配置の判定順序を「可視上限への到達判定 → 保留の実体化 → 折返し判定 → グリフ配置」の順へ再編し、行の確定を担っていた既存の開始済みフラグを撤去する
  - 可視上限（可視グリフ数の打切り）判定を保留の実体化より前に置き、可視上限に到達した時点で残っている保留は実体化せずに走査を終える
  - 保留が存在する状態で次の可視グリフを配置する直前に、現在行を確定し・累算された保留量ぶん block 位置を前進させ・行内位置を行頭へ戻し・保留を空に戻す（その後で折返し判定とグリフ配置を行う）
  - 行の確定条件として使われていた既存の「行が開始済みか」を示すフラグを撤去し、現在行が空でないことによる判定へ置き換える（改行マーカー単独では現在行を開始済み扱いにしない）
  - 関数のドキュメントコメントを、改行の即時反映を前提にした記述から本 spec の遅延（保留・実体化・蒸発）意味論の説明へ更新する
  - 観測可能な完了条件: 「改行マーカーの後ろに可視グリフがまだ無い」入力では行送りが起きず、可視グリフが追加された時点でのみ累算された行送りが一括で反映されることを確認できる
  - _Requirements: 2.1, 2.2, 2.3, 4.1, 4.2, 4.3_
  - _Boundary: LayoutEngine::layout_
- [x] 1.3 viewbox 後方縮退ガードの拡張: 遅延化で新たに到達可能になる「行内縮小」フレーム列（後方時刻ジャンプ・un-reveal）で退避インクが未クリアになる既存欠陥を、既存の全域ダーティ縮退の同型拡張で正規に塞ぐ（DD-9・実装中に発見・開発者承認 2026-07-18 のスコープ追加）
  - `plan_with_overhangs` の後方縮退条件を「行数減少」から「同一 index 行の被覆不能な指紋変化（block 位置移動・extent 縮小＝新 extent の変化行矩形が旧インクを覆えない）」まで拡張する
  - 前方 typewriter（text prefix 伸長・extent 増加・block 不動）では不発であることを檻で固定する（増分ホットパスの維持）
  - 改行を一切含まない行内縮小の oracle vs viewbox byte 等価檻を恒久追加する（欠陥が改行非依存＝本 spec の意味変更と直交であることの固定）
  - 観測可能な完了条件: `diag_line_boundary_dropout_vs_oracle`（後方時刻ジャンプ検分を含む）が無改変のまま緑・新規檻（全域縮退・増分維持・byte 等価）が緑
  - _Requirements: 3.3, 7.2_
  - _Boundary: ScrollPlanner::plan_with_overhangs（viewbox.rs）＋viewbox.rs／viewbox_draw.rs テストモジュール_
  - _Depends: 1.2_

- [x] 2. 既存3檻の更新: 即時意味論を前提にしていた既存テスト（中間改行檻・末尾改行檻・末尾あふれ参加檻）を遅延意味論の期待値へ更新する
  - 可視 prefix 内の改行マーカーが次の可視グリフより前に来る既存檻を、「次の可視グリフが無い間は行を開かず保留を維持し、可視グリフ到達時にのみ行送りが反映される」期待値へ更新する
  - 末尾（走査終了まで後続の可視グリフを持たない）改行マーカーの既存檻を、「空行を開かず、保留は実体化されずに蒸発する」期待値へ更新する
  - 末尾の保留改行がバルーン満杯付近のあふれ判定に参加していた既存檻を、「保留のみでは内容ビューボックスも最新行集合も変化せず、あふれが発火しない」期待値へ更新する
  - 観測可能な完了条件: 上記3つの既存テストが更新後の遅延意味論の期待値でグリーンになる
  - _Requirements: 1.1, 1.2, 3.1, 5.2, 5.3, 7.2_
  - _Boundary: LayoutEngine::layout_

- [ ] 3. 新規決定論檻の追加: 遅延意味論の判断分岐を FixedMetrics（実描画非依存）で全網羅する新規テスト群
- [ ] 3.1 連続する複数の改行マーカーが単一の累算保留として実体化され、中間に空行が生じず行間が ratio 合計になることを検証する
  - _Requirements: 1.3, 2.1, 2.2, 2.3, 7.1_
  - _Boundary: LayoutEngine::layout_
- [ ] 3.2 先頭改行（可視グリフ配置前の保留）が空行を作らず前進のみで実体化すること、ratio 0 の改行が「送りゼロで行を替える」縮退挙動を保つこと、改行マーカーのみの入力（可視グリフなし）が 0 行を返し内容ビューボックスを変化させないことを検証する
  - _Requirements: 1.2, 1.4, 1.5, 7.1_
  - _Boundary: LayoutEngine::layout_
- [ ] 3.3 typewriter リビール進行との整合を検証する: 同一の入力列に対し可視グリフ数を段階的に増やしたとき、改行マーカーの実体化がその改行より後ろの可視グリフが現れた時点でのみ起きることを確認する
  - _Requirements: 4.1, 4.2, 7.1_
  - _Boundary: LayoutEngine::layout_
- [ ] 3.4 バルーンが満杯付近の状態で保留改行が実体化された直後にあふれ判定が従来どおり発火することを検証する（保留のみで不発火であることは既存檻（タスク2）が担保する対の片側）
  - _Requirements: 3.2, 7.3_
  - _Boundary: LayoutEngine::layout_
- [ ] 3.5 縦書き（vertical_rl／vertical_lr）でも横書きと同一の遅延・累算・実体化・蒸発規則が成立し、行送りが軸読み替え正準表に従うことを検証する
  - _Requirements: 6.1, 6.2, 7.1_
  - _Boundary: LayoutEngine::layout_
- [ ] 3.6 既存の決定論（同一入力から同一出力）検証に、連続改行・末尾改行を含む新しい入力パターンを追加し、遅延意味論の全分岐が同一入力で同一出力を返すことを確認する
  - 観測可能な完了条件: タスク3の全新規檻とタスク2の更新檻が `cargo test -p areka-emo-text` でグリーンになる
  - _Requirements: 7.1, 7.2, 7.3_
  - _Boundary: LayoutEngine::layout_

- [x] 4. (P) canvas.rs 檻の更新: 末尾改行のみの入力で行に対応する住人が1個になり、行と住人の1:1写像が遅延意味論のもとでも維持されることを検証する既存檻を更新する
  - 観測可能な完了条件: 更新後の檻が、改行由来の空行が入力に現れなくなった状態でも layout 出力の行と canvas の住人が1:1で対応することを確認してグリーンになる
  - _Requirements: 3.3, 7.2_
  - _Boundary: ContentCanvas::from_layout_
  - _Depends: 1.2_

- [ ] 5. (P) state.rs ドキュメントコメント更新: NewLine の追記に関する説明へ、行送りの可視化は layout 側の遅延解釈である旨の注記を加える（挙動・既存檻は無変更）
  - 観測可能な完了条件: state.rs のコメントを読むだけで「追記は即時・行送りの可視化は layout が遅延解釈する」という役割分担が分かる
  - _Requirements: 4.3, 7.2_
  - _Boundary: TextLayerState_
  - _Depends: 1.2_

- [ ] 6. (P) viewbox_draw.rs コメント整合更新: 「幽霊空行」に言及する既存コメントを、遅延化によりその現象が発生しなくなった旨へ更新し、live-diff 系シナリオのうち発火タイミングの後退が予期される箇所にその旨のコメントを追記する
  - 観測可能な完了条件: 該当コメントが現行コードの挙動（幽霊空行は生じない・一部シナリオで発火タイミングが後退しうる）と一致した記述になっている
  - _Requirements: 3.2, 7.2_
  - _Boundary: viewbox_draw.rs（live-diff オラクル檻）_
  - _Depends: 1.2_

- [ ] 7. 回帰確認
- [ ] 7.1 crate 単体の全テストスイートを実行し、draw.rs／viewbox.rs／state.rs／sink／actor 系の既存檻が非影響のまま緑であることを確認する。落ちた檻があれば意味変更に伴う更新か陳腐化かを個別判定する
  - 観測可能な完了条件: `cargo test -p areka-emo-text` が全緑（更新檻・新規檻・非影響確認対象の既存檻すべて含む）
  - _Requirements: 2.4, 3.3, 5.1, 7.2_
  - _Depends: 3.6, 4, 5, 6_
- [ ] 7.2 ワークスペース全体のテストスイートを実行し全体回帰を確認する（i686 host-32 成果物の事前ビルドが前提）
  - 観測可能な完了条件: `cargo test --workspace` が全緑
  - _Requirements: 7.1, 7.3_

- [ ] 8. 実機サインオフ（R8・emo2 fixture・pasta SHIORI）
- [ ] 8.1 有界 auto-exit（`AREKA_APP_SMOKE_EXIT_MS=180000`・`RUST_LOG=info,areka_emo_text=debug`）で実機を起動し、pasta の spot_newlines 指紋（`ratio=1.5` の NewLine cue 適用）がログに1件以上存在することを grep 確認する（シナリオ不在なら再実行）
  - 観測可能な完了条件: ログから `ratio=1.5` を伴う NewLine 適用が1件以上確認できる
- [ ] 8.2 実走全体で「あふれ発火」marker（ANSI 色コード耐性のため固定文字列を別々に grep）が0件であることを確認し、A→B→A 再登壇時に段落区切りが維持され A→B 単純切替時に A のバルーンが動かないことを人間目視で確認する
  - 混入時（正当な長文あふれが同一実走に含まれた場合）は機械的な窓判定を行わず、再実行または人間目視＋タスク3の決定論檻を最終根拠とする
  - 観測可能な完了条件: 「あふれ発火」marker が全走行で0件、かつ目視で A→B 切替時のスクロール消失と A→B→A の段落区切り維持の両方を確認できる
  - _Requirements: 8.1, 8.2, 8.3_

## Implementation Notes

- タスク1/2/4 は分離してグリーンにできない一体の意味変更（本番改訂が DD-7 の名指し4檻をちょうど落とす）ゆえ、タスク1.1/1.2/1.3/2/4 を1コミットの原子単位として実装した（独立レビュアー APPROVED・`cargo test -p areka-emo-text` 全緑）。
- **DD-9 の発見（実装中）**: 遅延化で trailing 空行が消えると、旧・即時意味論では行数減少ガードで偶然マスクされていた viewbox の「行内縮小で退避インク未クリア」欠陥が後方時刻ジャンプ（`diag_line_boundary_dropout_vs_oracle`）で露出。fresh-context デバッグ調査が改行ゼロの縮小列で同一 diverge を実証（改行非依存＝本 spec と直交）。開発者承認でスコープ追加（タスク1.3）。修正は既存の全域ダーティ縮退の同型拡張（`is_backward_shrink`）で viewbox.rs 1点のみ・前方 typewriter は不発（ホットパス維持）。
- **pipeline_test.rs の結合更新（DD-7 未列挙）**: 統合檻 `typewriter_reveal_drives_layout_and_window_through_the_pipeline` と `vertical_fixture_pipeline_scrolls_horizontally_and_is_deterministic` は即時意味論の行数/あふれ期待を持つため、タスク2と同型の「意味の変更に伴う更新」として遅延意味論の再計算値へ更新した（幽霊空行の消失・あふれ発火の実体化時刻への後退を反映）。タスク7.1 の非影響確認対象に含める。
