# Implementation Plan

- [ ] 1. 基盤: 参照実装の観測と単位空間契約の純関数
- [x] 1.1 参照実装の拡大率跨ぎ挙動を実機で観測する
  - 拡大率の異なるモニタ間で参照実装のバルーン相対位置を DPI 対応プロセスとして実測する
  - 「何もしない」という結果も観測結果として記録する
  - 参照挙動が存在する場合は実測を正として採り、以降の変換規則をその実測に合わせて実装する。存在しない場合、または採ると表示経路が拡大率に追従するという設計原則と両立しない場合は、areka 自身の設計原則から導いた判断を採る
  - 本タスクは spec 全体の前提である——観測が実施できない場合はここで停止し、後続を先行させない（先行させると 1.3 と 6.5 の逐語期待値を後から書き直すことになる）
  - 完了状態: 観測結果と採った腕が記録として残り、腕の選択が根拠つきで読める
  - _Requirements: 6.1, 6.2, 6.3_

- [x] 1.2 オフセットの単位空間契約と変換規則を純関数として定義する
  - 契約の定義元となる新規モジュールを置き、そのモジュール doc を「実行時の合流欄と追従オフセットは現在の表示 DPI における物理 px である」という契約の唯一の権威にする
  - 基準対（基準値とその値が属する表示 DPI・未係留を表す欠損を含む）、換算 1 軸ぶんの結果（飽和の有無つき）、拡大率を解決できない理由、追随の判定結果 4 腕を型として定義する
  - 遷移時の変換規則を純関数として実装する——基準対と現在の表示 DPI だけを入力に取り、前回の結果を入力にしない。比は表示 DPI の整数 2 つから直接組む
  - 作者空間のオフセットを合流欄の空間へ換算する純関数を実装する。丸めは既存の単一権威へ委譲し、新しい丸め規約を 1 つも導入しない
  - 供給元ごとの換算軸の割り当て（descript のバルーンオフセット＝シェル軸・windowposition 由来＝バルーン軸・移動台本＝シェル軸）と、永続値が契約の明示の例外であることを doc に記録する
  - 完了状態: モジュールが `World`・`Entity`・ログ機構のいずれにも触れずにコンパイルが通り、変換規則が値として判定結果を返す（警告の発行は呼び手の責務）
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.4, 2.5, 3.1, 3.3, 4.2, 4.4, 5.3, 9.3_
  - _Depends: 1.1_

- [x] 1.3 純関数の判断分岐を決定論テストで全網羅する
  - 往復の bit 同一を固定する（同じ表示 DPI へ戻るたびに初回と bit 同一・基準が不変であることも同時に主張）
  - 恒等の素通しを固定する（基準 DPI と現在 DPI が同一のとき値が 1 bit も動かない・供給側は恒等比で生値が素通し）
  - 作者基準 DPI の既定値の倍数でない表示 DPI を分子・分母の両側に置いた行列で、丸めどおりの値を逐語で固定する。負値・下限近傍・飽和の各腕を含む
  - シェルとバルーンの作者基準 DPI が異なる場合を純関数で踏み、供給時は軸ごとに換算されること、遷移の追随は軸に依らず同一結果になることを固定する
  - 追随の判定結果 4 腕と縮退 3 腕すべてに到達し、いずれも値を変えないことを固定する
  - 完了状態: 新規テストファイルが緑で、判定結果の全腕に到達している
  - _Requirements: 7.5, 7.6, 7.7, 7.8, 7.9, 3.3, 3.6, 2.2_

- [ ] 2. 基準対の状態を配置層へ通す
- [x] 2.1 採寸に使う表示 DPI を単一の決定点にし、配置解決の出力へ基準対を載せる
  - 採寸の拡大率を組む関数が実際に採用した表示 DPI（主モニタの値・取得できない場合は既存の縮退値）を、内部で決めて捨てるのではなく返す形にし、単一の決定点を作る
  - 配置解決の出力へ基準対の欄を追加し、配置式が出した既定のオフセットに採寸 DPI を刻んで代入する
  - 欄の追加は配置解決出力の構造体リテラル構築点を全て壊す——本番の構築点・テスト支援の構築点・テストファイル群（実測で 18 ファイル・37 か所）へ既定の基準対を補い、コンパイルを回復させる
  - 配置式そのものは 1 行も変えない
  - 完了状態: ワークスペースがコンパイルでき既存テストが緑のまま、配置解決の出力に基準対が載る。採寸 DPI が拡大率の分子側に採られた値と食い違わないことをテストが固定する
  - _Requirements: 1.1, 1.5, 3.1_

- [x] 2.2 保存値を採用した腕の基準を未係留にする
  - 保存値を採用する腕で基準対の DPI を未係留にし、保存値をそのまま基準値として置く
  - 保存値が無い腕は配置式が出した基準対をそのまま運ぶ
  - 保存表現・採否の優先順位・補正を焼き付けない規約はいずれも変更しない
  - 完了状態: 復元経路を通した配置解決の出力が、保存値と bit 同一の基準値と未係留の基準 DPI を持つ
  - _Requirements: 5.1, 5.2, 5.5, 5.6_

- [ ] 3. 追従コンポーネントを移設し、オフセットの書込口を閉じる
- [x] 3.1 追従コンポーネントの定義を契約の定義元モジュールへ移す
  - 定義を移すだけで欄構成も可視性も変えない（挙動の変化ゼロ）
  - ファサードの再輸出先を差し替え、外部からの参照がファサード経由のまま壊れないようにする
  - 追随相から呼ぶ必要のある随伴追従の関数と関連する引き金の型を、私有再束縛から crate 内公開へ格上げする（外部公開面は増やさない）
  - 完了状態: ワークスペースがコンパイルでき既存テストが緑で、実行時の挙動が 1 つも変わっていない
  - _Requirements: 9.1_

- [x] 3.2 オフセットを私有化し、書込を確立と追随の 4 本へ閉じる
  - オフセットと基準対をいずれも私有欄にし、読取はアクセサに限る
  - 書込を確立（構築・再確立）と追随相専用（係留・追随適用）の 4 本に分ける。後者 2 本は基準を変えない
  - 既存の書込 2 か所（バルーン単独ドラッグ・キーワード再導出）を再確立の呼出へ置き換える。ドラッグ側は表示 DPI を先に読んでから借りる順序にする
  - キーワード再導出側の発火条件と「経路で絞らない」設計判断の doc は 1 文字も変えない
  - 構築点は基準対を受け取る形へ改める。参照側の読取（実測で 9 ファイル・38 か所）をアクセサ呼出へ機械的に置き換える
  - 既に分量規律の例外表に載るファイルへは行を増やさない（呼び替えのみに留める）
  - 完了状態: ワークスペースがコンパイルでき既存テストが緑で、定義モジュールの外から私有欄へ直接代入するとコンパイルエラーになる
  - _Requirements: 1.1, 3.3, 3.5, 4.5, 5.2, 9.1, 9.6_

- [ ] 4. 供給層: descript のバルーンオフセットへの拡大率適用
- [x] 4.1 供給段階で作者空間のオフセットを物理 px へ換算する
  - 供給の準備段階で、windowposition 由来の調整量を合流させる直前に新しい換算ステップを挟み、descript 由来のオフセットをシェル軸の拡大率で物理 px へ置き換える
  - 両軸が揃ったときのみ採用する既存の受理規約を変更しない
  - 飽和したときは回り込ませず飽和値を採り、スコープ・軸・生値・飽和後の値を警告として記録する
  - 拡大率を解決できない場合は既存の単一縮退点の記録に相乗りし、重複した警告を新設しない（記録の無い縮退経路を作らない）
  - 完了状態: 拡大率が 1 でない環境で宣言済みのオフセットが換算後の値として合流し、拡大率 1 では従来と同一の出力になる
  - _Requirements: 1.2, 1.5, 2.1, 2.2, 2.3, 2.5, 2.6, 9.4_

- [x] 4.2 (P) 単位空間混在の暫定記述を確定契約の記述へ置き換える
  - 「単位空間の混在は意図的な暫定である」旨の見出し doc と実装コメントを、確定した契約（合流欄は物理 px・供給元ごとの換算軸の割り当て）の記述へ置き換え、契約の定義元モジュールを指す
  - 換算部品の署名と挙動は変更しない
  - 完了状態: 暫定を宣言する記述がリポジトリから消え、契約の記述だけが残る
  - _Requirements: 1.3, 1.4_
  - _Boundary: windowposition の doc コメント_
  - _Depends: 1.2_

- [x] 4.3 供給結線の決定論テストを追加する
  - 換算ステップが windowposition の合流より前に走ること、合流後の値が両者を同一空間で加算した結果になることを固定する
  - 飽和の記録が出ることを固定する
  - 完了状態: 新規テストファイルが緑で、是正前は換算されない生値のままとなって落ちる主張を持つ
  - _Requirements: 2.3, 2.5, 7.1, 7.4_
  - _Depends: 4.1_

- [ ] 5. 追随の観測と共有パーサの語彙
- [x] 5.1 遷移観測チャネルへ追随の種別を 1 つ追加する
  - 既定 OFF の構造化観測チャネルへ、追随専用の種別・欄名・判定語 6 値・全数配列・行の組み立て純関数・前置ガード付きの発行口を追加する
  - 種別の全数表へ新種別を足す
  - 完了状態: 観測を点灯した状態で 1 遷移 1 スコープにつき高々 1 行が出て、既定運転では文字列の確保も行わない
  - _Requirements: 3.7, 8.3_

- [x] 5.2 共有パーサの語彙表へ新種別を教える
  - 実機ログの共有パーサが持つ必須フィールド表へ新種別のアームと欄名定数を足し、未知種別として欠陥に数えられないようにする
  - 判定ロジックは足さない（判定は専用モジュールが持つ）
  - 再観測用の埋め込みログは更新しない——新種別は新規採取のログにのみ現れるため
  - 完了状態: 新種別を含むログを共有パーサに通しても整形性検査が緑のままで、既存の遷移判定が赤にならない
  - _Requirements: 8.3, 9.6_
  - _Depends: 5.1_

- [ ] 6. 拡大率遷移でのオフセット追随
- [x] 6.1 遷移相へ追随ステップを挿入し、基準から引き直した値を反映する
  - 追随の適用相を新規モジュールとして置き、追従コンポーネントと表示 DPI を読み、純関数へ委ね、結果を係留または追随適用として書き、観測を出す
  - 遷移相の第 2 巡で、寸法の再取得より前にキャラ窓に対して呼び出す。相順・待ち札の関門・寸法調整の署名はいずれも変えない
  - 縮退腕はすべて警告を伴わせる。追従先が無い場合は縮退ではないので記録しない
  - 拡大率遷移で追従オフセットが不変であることを主張する既存テスト 1 本を、同じコミットで「表示 DPI 比で追随する」へ書き換える。書込前に読んだ値と突合する構造と空振り防止の証人は保つ
  - 完了状態: 表示 DPI が変わったとき追従オフセットが表示 DPI 比の値へ更新され、書き換えた既存テストが緑になる（是正前は同テストが落ちる）
  - _Requirements: 3.1, 3.2, 3.6, 3.7, 7.4, 9.4, 9.8_

- [x] 6.2 窓書込が起きなかった腕でバルーンを収束させる
  - 位置と寸法がともに同一で寸法調整が早期に抜けた場合に限り、追随ステップが随伴追従を 1 度だけ呼んでバルーンを新しいオフセットの位置へ収束させる
  - 遷移 1 回あたりの窓書込の予算（キャラ 1 以下・バルーン 1 以下・別経路 0）を超えないこと、中間位置を提示しないことを守る
  - 完了状態: 早期に抜ける状況を作った決定論テストで、バルーンが同一フレームで新しい位置へ 1 度だけ書かれ、書込回数がキャラ 0・バルーン 1 になる
  - _Requirements: 3.1, 3.4_
  - _Depends: 6.1_

- [x] 6.3 キーワード由来の基本位置との排他の門を入れる
  - キーワードの再導出素材が残っているあいだ、追随ステップはオフセットも基準対も 1 bit も触らず、見送りの判定語を記録して抜ける
  - 再導出側の発火条件と「経路で絞らない」設計判断は 1 文字も変えない（分岐は本仕様の新規コード側に置く）
  - 素材未消費のまま寸法が据え置きの遷移を迎えた腕では追随も再導出も走らず、揃えの更新が次の寸法変化まで取り残される——これを受容残余として doc に記録する
  - 完了状態: 素材が残る遷移で追随が見送られ、再導出だけがオフセットを書く。門を外すと揃えが二重に動いて落ちる対を持つ
  - _Requirements: 4.1, 4.3, 4.4, 4.5_
  - _Depends: 6.1_

- [x] 6.4 (P) 追随の証拠にならない既存テストへ区分の注記を入れる
  - 書込後に読んで恒等式のみを主張する 3 本へ、追随が入っても緑のまま通る＝追随の証拠にならない旨を doc に明記する（**2026-08-28 訂正**: 当初 4 本としていたうち遷移原子性テストは前読み構造を持つ非空虚な檻だったため task 6.1 が書き換え済み。**同ファイルには 6.1 が訂正注記を書いてあるので、6.4 は当該ファイルへ矛盾する注記を足さないこと**）
  - 遷移を起こさないまま追従の恒等式を主張する 1 本を「寸法変化に対する不変」群へ区分し、遷移を起こさない旨を doc に明記する（主張は書き換えない）
  - 完了状態: 対象 4 本の doc に区分が明記され、「全部緑だから壊していない」の根拠に使えないことが読める
  - _Requirements: 7.4, 7.6_
  - _Boundary: 再射影テスト・再射影なしテスト・遷移分岐テスト・追従可視性結線テスト の doc（遷移原子性テストは task 6.1 が書き換え済みのため対象外）_
  - _Depends: 6.1_

- [x] 6.5 遷移相の結合テスト行列を組む
  - 表示 DPI 遷移 3 組 × 全アンカー × 全スコープで、書込前に読んだ値と書込後を突合し表示 DPI 比で追随していることを主張する。空振り防止の証人（比が 1 でない・オフセットが非ゼロ・バルーンが実際に動いた）を持つ
  - キーワード素材が残る腕・消費後の腕・素材未消費かつ寸据え置きの受容残余の腕をそれぞれ固定する。素材消費後の遷移では中央揃えの残差が許容量以内であることも固定する
  - 待ち札で見送られた窓が札の解除後に追随を取り戻すことを固定する
  - 拡大率が変わらない寸法変化（面の切替・作業領域の再スナップ）でオフセットが 1 bit も動かないことを固定する
  - バルーン単独ドラッグで決めた相対位置にも同一の追随規則が適用され、確立時の基準 DPI がその時点の表示 DPI になることを固定する
  - 完了状態: 新規結合テストファイルが緑で、行列の全組み合わせに到達している
  - _Requirements: 3.2, 3.5, 4.2, 7.1, 7.3, 9.8_
  - _Depends: 6.2, 6.3_

- [x] 7. 保存・復元の往復を固定する
  - 保存時と復元時で拡大率が異なる行列で、復元直後のオフセットが保存値と bit 同一であることを固定し、この裁定が黙って反転しないための非回帰テストである旨をテスト doc へ明記する
  - 復元（未係留）→ 最初の観測で係留（値は不変）→ 次の遷移で追随、の 3 段が順に成立することを固定する
  - 採否の優先順位と生値保存が変わっていないことを固定する
  - 完了状態: 新規テストファイルが緑で、係留前後の挙動の違いが観測できる
  - _Requirements: 5.2, 5.4, 5.5, 5.6, 7.1, 7.2_
  - _Depends: 6.1_

- [ ] 8. 実機サインオフの判定器と手順書
- [x] 8.1 追随レコードの機械判定器を新規モジュールとして実装する
  - 遷移の起点となる既存レコードを基準に遷移ごとに切り出し、追随レコードを集計する
  - 往復の前後で反映後の値が bit 同一に戻ること、遷移ごとの判定語が期待の腕であること、低い拡大率側で追随の判定語が出ていること、キーワード指定スコープの揃えの残差が許容量以内であることを判定する
  - 判定語は発行側の定数を参照するだけとし、リテラルを書かない
  - 完了状態: 既知の合格ログと不合格ログの両方を食わせて、それぞれ緑と赤が出る
  - _Requirements: 8.2, 8.3, 8.4, 8.5_
  - _Depends: 5.1, 5.2_

- [x] 8.2 実機ログを食わせるランナーと手順書を置く
  - 環境変数でログの絶対パスを受け取り、決定論テストと同一の判定器を回す無視属性つきランナーを置く
  - 環境変数の未設定・パス不達・観測行 0 行はいずれも失敗させ、無言のスキップで緑を偽装しない
  - 手順・点灯方法・判定語・合否条件を逐語で持つ手順書をモジュール doc として置く。先行仕様の手順書ファイルは書き換えない
  - 完了状態: ランナーが手順書どおりの手順で起動し、ログ不在では確実に赤になる
  - _Requirements: 8.1, 8.3_
  - _Depends: 8.1_

- [ ] 9. 互換記録へ 3 行を登記する
  - 単位空間契約・遷移時の変換規則・保存往復の意味論の 3 点を、項目・裁量・根拠・出典の 4 欄で登記する
  - 同種の作者空間オフセットの既登記 2 例と矛盾しない判断であることを根拠欄に示す。1.1 で参照挙動が観測された場合はその実測を、観測されなかった場合は areka 裁量である旨を根拠欄に書き分ける
  - 下流の適合検証が持つ「随伴バルーンの追従オフセットは変わらない」期待を 2 つに区別して記録する
  - 他仕様が所有する行は 1 文字も書き換えない
  - 完了状態: 互換記録に 3 行が増え、既存行の差分がゼロである
  - _Requirements: 5.3, 5.7, 6.3, 6.4, 6.5, 6.6, 9.7_
  - _Depends: 1.1_

- [ ] 10. 統合検証
- [ ] 10.1 実機サインオフを実施する
  - 拡大率の異なる 2 台以上のモニタを備えた環境で、観測を点灯して実行する
  - ゴーストをモニタ間で往復させ、バルーンとキャラの位置関係が往復の前後で見た目に変わらないことを判定項目として通す
  - 先行仕様の実機観測で残った所見「低い拡大率の側で定常的にバルーンがずれる」が解消したことを判定項目として通す
  - キーワード指定のバルーンについて遷移後の揃えを判定項目として通す
  - 宣言済みのバルーンオフセットを持つ資産で、拡大率適用の是正が効いていることを確認する
  - 完了状態: 判定器の機械判定が合格を出し、判定語と手順が文書として残る
  - _Requirements: 2.1, 8.1, 8.2, 8.3, 8.4, 8.5_
  - _Depends: 8.2, 9_

- [ ] 10.2 非回帰と分量規律を確認する
  - ワークスペース全体のテストを走らせ、既存の「拡大率が混入しないこと」を固定する群と「寸法変化に対する不変」を主張する群がいずれも緑であることを確認する
  - 分量規律の番人が緑であること、例外表を 1 行も書き換えていないことを確認する
  - 位置書込の共通経路の署名が 1 つも変わっていないことを確認する
  - 追従の基準・画面内維持の関門・補正を焼き付けない規約が変わっていないことを確認する
  - 完了状態: ワークスペースのテストが全て緑で、共通経路の署名と例外表の差分がゼロである
  - _Requirements: 7.6, 9.1, 9.2, 9.3, 9.5, 9.6_
  - _Depends: 10.1_

---

## Implementation Notes

- **1.1**: SSP は拡大率遷移で追従オフセットも窓寸法も変えない（腕 B 確定）。証跡 `ssp-dpi-crossing-observation-2026-08-27.log`・research.md §13。design D1〜D5 は見直し不要で、Open Questions の危険「観測が腕 A になる」は解消済み。
- **1.2**: tasks.md 1.3 の「縮退 3 腕」は enum `UnresolvedScale` の腕数（2）ではなく、design.md Testing Strategy の Unit Tests 項目 5 が挙げる **`ZeroBaseDpi` / `ZeroCurrentDpi` / 未係留** の 3 経路を指す（レビュー裁定）。1.3 はこの 3 つに到達させること。
- **1.2**: `offset_space.rs` 冒頭の `#![allow(dead_code)]` は消費者不在ゆえの暫定。**task 3.1 / 4.1 で結線が入った時点で撤去すること**（既存先例 `emo2_boot/move_cue.rs` に倣った形）。
- **1.2 → 1.3 申し送り**: ⑴ 飽和の檻は正側 `i32::MAX` の 1 例しか踏んでいない——負側・`i32::MIN` 近傍・96 の倍数でない DPI を分母側に置いた行列は 1.3 が埋める。⑵ `rescale_follow_offset` は同一 DPI の判定を 0 検査より前に置くため、`base_dpi == current == DPI{0,0}` の退化入力は `Unresolved` でなく `Unchanged` を返す（値は動かないので要件 3.6 とは矛盾しないが、呼び手が記録する判定語が変わる）。実値 0 の経路は現状存在しない（`DPI::default()` は 96）。
- **1.3**: design D8 の残差上限 3px は **実測で妥当**（research §12 の宿題を消化）。作者基準 DPI 3×3 × 表示 DPI 遷移 20 × 作者寸 4×4 ＝ 2,880 組で **実測最大 2px**（3px は 0 組）。ただし全数なのは DPI 側だけで作者寸は 16 通りの手選び——`worst` を逐語固定してあるので、寸法集合を広げて 3px が出れば赤くなり再評価が強制される。
- **1.3 → 4.1 申し送り**: 供給側の恒等比は **`i32::MIN` でだけ生値が素通しにならない**（`-i32::MAX` へ飽和し `saturated: true`）。要件 2.2 の字面に対する唯一の例外。出所は既存権威 `scale_signed` で `windowposition` 経路は今日すでに同じ挙動、回り込みは起きず値として報告されるので製品欠陥ではない。**4.1 が生値の受理範囲を決めるときにこの 1 点を見えるようにすること**（檻 `supply_identity_saturates_only_at_i32_min`）。
- **1.3 → 6.x 申し送り**: `base_dpi == current == DPI{0,0}` は `Unresolved` ではなく **`Unchanged`** を返す（腕の順序を裁定つきで確定・檻 `zero_on_both_sides_is_unchanged_not_unresolved`）。値は動かないので要件 3.6 の規範部は満たすが、**この腕だけ縮退の警告が出ない**。6.x は「DPI{0,0} は縮退ではなく無遷移として記録する」ことを設計判断として明示的に持つこと（要件 3.6 記録部・9.4 との関係）。
- **1.3 → 4.1 申し送り**: design Testing Strategy 項目 4 の後半「加算後の合流値が期待どおりになること」は純関数の外側（`descript` 由来＋`windowposition` 由来の合流）ゆえ **4.1 の引受け**。1.3 の箇条書きは軸ごとの換算と遷移の軸非依存に絞られている。
- **1.3**: design.md の file:line が 3 件ずれていた（`measure.rs:69-74`→71-75・`mod.rs:207-213`→208-214・`keyword_base.rs:71-78`→69-78）。結論に影響は無いが、**アンカーは使う前に必ず現物と突合すること**（本プロジェクト通算 8 度目の陳腐化）。
- **1.3**: `follow_offset_space_tests.rs` は 877 行で 1,000 行まで余裕 123 行。後続がここへ足すなら例外表を触らず兄弟ファイルへ割ること。
- **2.1 → 9 申し送り（必須）**: `resolver.rs` の **U5／DD1「resolver 署名に DPI・論理 DIP を持ち込まない」**（出典 `completed/areka-P0-window-placement/design.md:50,160`）は、本仕様の `measure_dpi` 引数追加で**緩和された**。本仕様の design は `### Allowed Dependencies` と `### Modified Files` で明示的に承認しており静かな上書きではないが、**完了済み spec の設計文書は書き換えられない**ため、task 9 の互換記録へ緩和の 1 行を登記すること（登記が無いと将来の読み手が resolver.rs の doc だけを頼りにする）。幾何は `measure_dpi` を 1 度も読まないので U5 の実質は保たれている。
- **2.1**: design の「構築点 18 ファイル・37 か所」は **素の `grep "ScopePlacement {"` の値**で、struct 宣言・引数型・戻り値型を含む数え方の誤り。実際に欄を補ったリテラルは **22 か所 / 16 ファイル**（＋構造体更新構文で自動追随 4 か所）。取りこぼしではない（`ScopePlacement` は areka crate 外に存在せず `--all-targets` も緑）。**設計が数えていなかった波及**として `resolve_placement` の引数追加で呼出点 38 か所 / 7 ファイルが壊れた。
- **2.1**: `offset_space.rs` の `#![allow(dead_code)]` はまだ外せない（実測で未使用 7 項目・消費者が付いたのは `OffsetBase` 本体のみ）。撤去は引き続き task 3.1／4.1／6.x の結線後。
- **2.2（design の表記についての裁定）**: design.md `## Requirements Traceability` の行 5.1/5.5/5.6 にある「persist.rs（**無改変**）」は、**ファイル非接触ではなく「規則の無改変」**の意である（レビュー裁定）。根拠＝⒜ 同じ表の行 5.2 が「persist（腕の分岐）」を接地点に挙げており、ファイル非接触と読むと表が自己矛盾する ⒝ `### Modified Files` の persist.rs 行が無改変の対象を「保存表現・採否の優先順位・`ScopePlacement` の他の欄」と**規則として列挙**している。**task 9／10.2 の担当者は字面で受け取らないこと**——本タスクの差分を回帰と誤認する形の罠である。
- **3.1**: design が書いていない付随変更が 1 つ要った——ファサードを `pub(crate) use` にする以上、`drag_follow.rs` 側の項目宣言も `pub(super)` → `pub(crate)` へ上げないと **E0365／E0364 でコンパイルできない**（レビューが摂動で実証）。より狭い代替は存在しない。外部公開面は `areka` が bin crate で `mod placement;` が私有ゆえ構造的に不変。
- **3.1 → 3.2 申し送り（3 件・いずれも 3.2 の作業範囲へ引き継ぎ）**:
  1. **`drag_follow.rs` の行番号が一律 −18 ずれた**。design D14 が書込点として挙げる `drag_follow.rs:534` は**現在 516-519**。古い行番号を頼ると空振りする。`keyword_base.rs:144` は現行と一致。
  2. `offset_space.rs:54` の scaffold コメント「残る未使用は **7** 項目」は**誤りで真値は 6**（`OffsetRescale`／`UnresolvedScale`／`ScaledAxis`／`rescale_follow_offset`／`scale_author_offset`／`scale_axis`）。HEAD 時点で既に陳腐化していたもので 3.1 由来ではない。3.2 が同ファイルを書き換える際に訂正すること。
  3. 移設した doc 内の intra-doc link `[`on_balloon_drag`]`／`[`move_window_to`]` は新スコープで解決しない。**3.1 で直すとバイト同一という挙動不変の証跡が崩れる**ため据え置いた。3.2 が doc を書き換える際に `[`super::…`]` へ直すこと。
- **3.2（差し戻し 1 回・本物の欠陥 2 件）**: レビューが「新設した縮退腕 2 か所を誰も実行していない」を **`panic!` 差し替えで実証**した（両腕を panic にしても全緑）。design が要求した「3 つの確立点それぞれの基準対の檻」も 0 本だった。いずれも是正済み。
- **3.2 ⚠ 道具が分岐を隠していた（後続タスクも同型を踏み得る）**: 檻で `DPI` を持たないキャラ窓を作れないのは、`crates/wintf/src/ecs/window/window_handle.rs` の `on_window_handle_add` が **`fake_handle()` を挿すだけで `DPI` を刻む**ため（偽 HWND では `GetDpiForWindow` が 0 を返し `DPI::default()`＝96 になる）。**縮退腕へ到達するには檻側で明示的に `remove::<DPI>()` が要る**。本番のキャラ窓は必ず `DPI` を持つので、この腕は本番では走らない。
- **3.2**: 実測は **読取 41 か所／20 ファイル・`BalloonFollow::new` 51 か所／13 ファイル**で、design の「9 ファイル・38 か所」より多い。集合はコンパイラが決めるので過剰接触ではなく design 側の過小計上。
- **3.2**: `window_move.rs` は **1,227 行を維持**（`debug_assert!` を 1 行短縮して増行と相殺。述語は論理的に同一・診断価値も不変とレビューが確認）。
- **3.2 → 5.2／8.1 申し送り（引受け不要の確認）**: 新設した `[balloon-drag] BaseUnpinned`／`[balloon-keyword] BaseUnpinned` は**平文 `warn!` の系統**であり、5.2 が教える `wintf::transition` の構造化 `kind` 語彙とは別系統。**5.2／8.1 が考慮する必要は無い**（レビュー裁定）。実機 grep 判定語 `[balloon-keyword] Unresolved` を流用しなかったのも正しい。
- **3.2 ⚠ 道具の罠**: python のテキストモード読み書きが CRLF のファイルを丸ごと LF へ変換する（repo は `core.autocrlf=true`）。`cargo fmt` も書き戻し時に LF にする。**全ファイル書換えの差分が本物の変更を覆い隠す**ので、編集後は行末を確認すること。
- **4.1**: フラグ手順を完走（OFF で 5 赤／無変化ピン 4 本は緑のまま → ON で 9 緑 → フラグ撤去して 9 緑）。レビューが早期 return を自分で注入して同じ 4 緑 5 赤を再現済み。
- **4.1（要件 2.2 の裏取り）**: 恒等比は**全 i32 で素通し**であり、例外は**ちょうど `i32::MIN` の 1 点のみ**。実効受理域 `i32::MIN + 1 ..= i32::MAX` はレビューが `scale_len` → `scale_signed`（`unsigned_abs` → `i32::try_from`）を辿って検証済み。恒等比でも `i32::MIN` は飽和フラグが立ち警告に出る。
- **4.1 → 4.3 申し送り**: ⑴ `BALLOON_OFFSET_SATURATED_TAG` の**綴りを逐語で固定する檻がまだ無い**（参照は `mod.rs` 内 3 か所のみ）。4.3 が入れること。⑵ 「加算後の合流値」は 4.1 が純関数 2 ステップを直に並べて踏んだが（`shell_axis_and_balloon_axis_sum_in_the_same_space`・現行フィクスチャは `seriko.dpi`／balloon `dpi` を宣言しないので 2 軸は今日つねに一致し、この分岐はフィクスチャでは踏めない）、**`prepare_stages` 越しに同じ主張を観測するのは 4.3 の所有**。
- **4.1（アンカーの正しい値・下流はこちらを使うこと）**: `ScaleRatio::new(dpi, author)` は design の `mod.rs:302` ではなく **`mod.rs:318`**。D2 が引く「windowposition はバルーン作者の空間ゆえバルーン軸」の裁定 doc は design の `mod.rs:395-397` でも（実装者が申告した）414-416 でもなく、**本タスクの +98 行が押し下げた結果 `mod.rs:509-510`**。現在の 414-416 は新設のタグ定数を指すので、古い数値は読み手を確実に誤誘導する。
- **4.1**: `mod.rs` は 713 → **811 行**（上限まで残り 189）。design D17 の見積り約 715 を超えたが内訳は doc/コメント 55・空行 3・**コード 40** で、契約の権威は `offset_space.rs` へ明示的に委譲しており要件 1.3 の「契約は 1 箇所」は破っていない（レビュー裁定）。
- **4.1 ⚠ 道具の較正**: 親（Claudia）が使った `grep -c $'$'` による行末検査が**壊れていた**（全行 CRLF と誤報告）。実際は `mod.rs` と `balloon_offset_supply_tests.rs` が全行 LF。`core.autocrlf=true` で index は LF 正規化されるため diff は汚れず実害は無いが、**行末を数えるなら `python -c "open(f,'rb').read().count(b'
')"` のようにバイトで数えること**。
- **4.2**: 暫定宣言はリポジトリから消えた（境界内 3 か所 → 0）。レビューが repo 全域 grep を自前で走らせ、残るヒットは歴史的参照（本 spec 文書群・完了アーカイブ）と無関係な「Out of scope」のみと分類済み。
- **4.2（裏取り済みの事実・下流も使える）**: `ScopeConfig.balloon_offset` への**本番の書込点は全域で 3 か所だけ**——`config.rs:290`（生値）・`mod.rs:483`（シェル軸で換算後）・`windowposition.rs:221`（バルーン軸で換算後）。`scope_ids` は `cfg.scopes.keys()`（`mod.rs:367`）と厳密に一致するので、生値が換算を素通りする scope は原理的に存在しない。永続値とキーワード由来基本位置は `ScopePlacement` 側で別欄。
- **4.2（非ブロッキング・spec 完了時の文書整理で扱う）**: `offset_space.rs:13` が「『単位空間の混在は意図的な暫定』という従来の記述は本契約が置き換える」と**もう存在しない文章を引用**している。レビュー裁定は **(i) 現状維持**——暫定の「宣言」ではなく「廃止の宣言」であり完了条件に違反せず、旧文言で grep した読み手の着地点としてはむしろ最良（契約の権威モジュール内）。任意の改善として死文の引用を落とす言い換えは可能。
- **4.3**: 群 4 完了。順序は**二重に**固定した——値による三択の排除（逆順なら windowposition にもシェル軸が二重に掛かり (643,−295) が (1086,−510) になる）と、**算術に依存しないログの並び**（換算の warn が windowposition の info より前）。後者は丸めや軸の割り当てが将来変わっても順序だけを見張り続ける。
- **4.3（強い非回帰の証拠）**: 換算を素通しへ戻すと**ちょうど 10 本だけ**が赤になり、すべて `balloon_offset_supply_tests.rs` の中（新設 5＋4.1 の純関数 5）。**他のどの既存の檻も換算に依存していない**＝供給の是正が既存の主張を 1 つも壊していないことの構造的な裏取り。レビューが `--no-fail-fast` で独立に再現済み。areka への逆依存 crate は 0。
- **4.3**: 1.3→4.1→4.3 と引き継いだ宿題を 2 件とも消化——`BALLOON_OFFSET_SATURATED_TAG` の綴りの檻（literal 比較で非循環）と、合流値を `prepare_ghost_windows_with_work_area` → `ScopePlacement` の実配置経路で観測する檻。
- **4.3（アンカーの現在値・下流はこちらを使うこと）**: D2 が引く「windowposition はバルーン作者の空間ゆえバルーン軸」の裁定 doc は **`mod.rs:510-511`**（design の `mod.rs:395-397` は陳腐化。現在の 395 行目は 4.1 が置いた新ステップの呼出そのもので、古い数値は確実に誤誘導する）。D3 の合流点 `windowposition.rs:216-219` は現在 **`windowposition.rs:221`**。
- **4.3**: 2 軸を異ならせるには合成構築が要る（`seriko.dpi,120` ／ balloon `dpi,144` ／ primary 240 ⇒ k_shell=2/1・k_balloon=5/3）。現行フィクスチャ emo2 からは到達しないが、要件 7.7 が明示的に要求し design も「フィクスチャでは踏めないので合成で踏む」と裁定済み。なお `sakura.balloon.offsetx` 自体は `hello-pasta` が実宣言する製品到達可能な構成である。
- **5.1+5.2（1 コミットで着地）**: 種別を 1 つ足すと**構造的な全数番人が 2 本赤くなる**ため、5.1 は単独では緑にできない。1 本は 5.2 の所有（共有パーサの必須フィールド表）、もう 1 本は**引受先の無い design の穴**だった。
- **5.1（判定語 6 値・8.1 はこれを参照すること）**: `rescaled` / `anchored` / `unchanged` / `keyword-pending` / `unresolved` / `saturated`。出所は design `##### Event Contract` の逐語列挙で、System Flows の 5 終端＋飽和分岐と Error Handling 表 4 行が独立に裏取りする。`UnresolvedScale` の 2 腕は 1 語 `unresolved` へ畳む（Error Handling 表どおり・腕の取りこぼしではない）。「`DPI` component が無い」腕には**語を発明していない**（design は `warn!` のみを指定）。
- **⚠ 5.1+5.2 の裁定（Claudia・開発者が覆せる）**: 番人 `the_procedure_names_every_record_kind_the_emitters_produce` は**完了アーカイブ** `completed/areka-P0-dpi-transition-atomicity/signoff-procedure.md` を読み、発行側の全種別が `kind=` で名指しされていることを要求する。種別を足すと赤くなるが、task 8.2 と要件 6.6 が先行仕様の手順書の書き換えを禁じている。**裁定＝完了アーカイブは触らず、areka 自身の番人を「出所集合」へ一般化する**。守るべき不変は「採取者が grep すべき語が**どこかの**手順書に載っている」であって「atom の手順書に載っている」ではない。`offset` は `PENDING_PROCEDURE_KINDS` へ明示的に保留し、`missing ⊆ pending` と **`pending ∩ covered = ∅`** の両方を主張して**自ら消える**形にした（8.2 が出所を足したのに保留を残すと赤）。レビューが 5 通りの摂動で自消え性まで実証済み。
- **5.1+5.2（裁定の代償・正直に登記）**: 一般化で逆向きの不変が**1 段ゆるくなった**。番人が見るのは「`kind=offset` の字面が出所集合のどれかに在る」だけで、その出所が本当に「サインオフ手順書」である保証は `PROCEDURE_SOURCES` を編集する人間の規律しかない（レビューは**発行側自身のモジュール doc** を出所に足して緑を作れることを実証した）。atom 時代は出所が 1 本固定だったのでこの穴は無かった。
- **5.1+5.2 → 8.2 申し送り（4 点・⑷を書かないと黙って空振りする）**: ⑴ 本仕様の手順書に `kind=offset` を**字面で**名指しする ⑵ その出所を `PROCEDURE_SOURCES` へ足す ⑶ `PENDING_PROCEDURE_KINDS` から `KIND_OFFSET` を消す（消し忘れは赤が教える） ⑷ **順方向の検査 `the_procedure_only_quotes_record_lines_the_emitters_can_produce` の入力を `procedure_text()` から `procedure_sources_text()` へ切り替える**——今は atom の手順書 1 本しか見ないので、新出所が引用する観測行の整形性を**誰も検査しない**まま緑になる。
- **5.1+5.2 → 6.1 申し送り**: 「1 遷移 1 スコープにつき高々 1 行」の**上半分は発行口では保証できない**（発行口が保証するのは 1 呼出 1 行のみ）。6.1 が ⑴ `BalloonFollow` を持たない窓では**呼ばない** ⑵ 待ち札で見送られた窓の再合流で同一遷移の 2 行目を出さない ⑶ 判定を窓 1 つにつき 1 度だけ下し腕ごとに別行にしない、を保証すること。
- **5.1+5.2（行数の余裕・後続は新規ファイルへ）**: `transition_diag.rs` 617→**800**（余裕 200・design D17 の見積り 680 を doc で 120 超過）／`transition_judge.rs` **931**（余裕 **69**＝これ以上の追記は入らない）／`transition_diag_tests.rs` 793／`transition_signoff_procedure_tests.rs` 731。
- **5.1**: 既定 OFF の無確保は**濾過テストでは原理的に検出できない**（`debug!` は既定で濾過されるのでガードを失っても出力が変わらない）。代替として本文走査の檻を置き、ガードを組立の後ろへ移す摂動で赤くなることを較正済み。発行口の引数は全て Copy スカラーで、呼出側に確保を強いない。
- **6.1（本仕様の心臓部・差し戻し 1 回＝文書のみ）**: 追随の適用相 `emo2_boot/frame/balloon_offset_follow.rs` を新設し、`dpi_phase_with` 第 2 巡の `refresh_scale_report` **直前**へキャラ窓限定で挿入（D6）。相順・待ち札の関門・`reconcile_window_size` / `resize_window_to` / `enqueue_window_set_pos` の署名はいずれも無改変。新モジュールは**値を 1 つも計算しない**（比の構成も丸めも無し・毎回 `base()` から引き直すので前回結果が入力へ戻る経路が構造的に存在しない）。
- **6.1 ⚠ design の分類が誤っていた（本タスク最大の発見）**: design D13 と Testing Strategy は `frame_transition_atomicity_tests.rs` を「**書込後に読んで恒等式のみを主張する**＝追随が入っても緑のまま」の群へ数えていたが、実物は**書込前にオフセットを読み**（`:301-311`）**拡大率遷移で再スケールされないことを突合して主張**していた（`:414-435`）＝非空虚な檻で、是正前は赤になる。**書き換えは 1 本ではなく 2 本**だった。
  - **原因**: `research.md` の既存檻の棚卸しが前読み構造を持つ本ファイルを取りこぼし、かつ引いた `:285` が**関数頭**であって主張の位置ではなかった。**行番号ではなく構造で見分けること**（design・research に教訓として登記済み）。
  - **権威**: 要件 3.1 が規範を反転させ、要件 9.7 が上書きされる側を「atom 時点の契約の写し」と名指しする。不変が残るのは **9.7 の作業領域再スナップと 9.8 の面の切替だけ**で、拡大率遷移を守る条項は要件のどこにも無い。9.7 の射程にリポジトリ内の写しも入ることを requirements.md へ**裁定として登記**（本文は改めていない）。
  - **予定された欠陥を 1 つ潰した**: tasks.md 6.4 が誤った名簿（4 本／対象 5 本／境界に遷移原子性テスト）の上に立っており、書かれたとおり実行すると **6.1 が同じファイルへ書いた訂正注記と矛盾する注記を足す**ところだった。6.4 を訂正済み（3 本／対象 4 本／境界から除外＋注意書き）。
- **6.1（巻き添えが無いことの構造的証拠）**: 追随ステップを無効化すると**落ちるのはちょうど 3 本**（書き換えた 2 本＋その片割れ）で、他の檻は 1 本も落ちない——レビューが自分で摂動して再現済み。原子性・窓ごとの書込数・経路 A 0 件・接地点 diff・非空虚性の証人はいずれも無傷。
- **6.1（縮退の全数）**: 終端は 6 つ。`BalloonFollow` 不在＝早期 return・**記録も警告もなし**（縮退ではない）／`DPI` component 不在＝`warn!` ＋ `unresolved`／`Anchored`・`Unchanged`＝警告なし／`Rescaled{saturated}`＝`warn!` ＋ `saturated`／`Unresolved`＝`warn!`。**縮退した腕はすべて警告を伴う**（要件 9.4）。`DPI{0,0}` は `verdict=unchanged`・警告なしで、**縮退ではなく無遷移として記録する**——design の Error Handling へ行を分けて登記済み。
- **6.1**: `offset_space.rs` の `#![allow(dead_code)]` を**撤去**（未使用 0 を再測して確認）。1.2 から引きずった足場がここで外れた。
- **6.1 → 6.2 / 6.3 の継ぎ目**: `OffsetFollowOutcome` を定義して返し、`dpi.rs` が `let _offset_outcome = …` で束ねて D16 の `wrote == false` 条件を名指しするコメントを置いてある（6.2）。`BalloonFollow` 読取と `DPI` 読取の間に `BalloonKeywordBase` の門の位置を平文コメントで確保してある（6.3）。いずれもスタブでも TODO でもない。
- **6.2**: 追随でオフセットが動いたのに窓書込が起きなかった腕（`resize_window_to` 手順 4 のべき等 skip）でだけ `follow_balloon` を 1 度呼ぶ（D16 の 2 条件＝`Changed` かつ `!wrote`）。放置すると**オフセットは直ったのにバルーンは古い位置に居座る**＝本仕様が消しに来た欠陥そのものが残る腕。
- **6.2（探針が本物であることの二重の裏取り）**: 早期 skip の状況は「拡大率だけ 192・作業領域は 96 のまま」のモニタ表で作った。⑴ 檻の主張ではなく**本番関数 `reproject_char_window_at_current_size` の戻り値そのもの**が `false` であることを専用テストで固定。⑵ **この状態対は本番で到達可能**——タスクバーの自動非表示や別モニタ配置では作業領域＝モニタ矩形で拡大率に依らず不動なので、その台での拡大率変更は作業領域を 1px も動かさない（レビュー裁定）。あり得ない状態の捏造ではない。
- **6.2（予算）**: skip 腕でキャラ 0・バルーン 1、通常腕でキャラ 1・バルーン 1、別経路 0。計数は新設せず `FrameHarness::drain_writes()` ＋ `frame_dpi_sync_hold_tests.rs` と同型の `writes_for` を再利用。書込の `(x,y)` が「確定済みキャラ窓位置＋**新しい** offset」と bit 等価であることまで固定（旧 offset で書いて直す形なら赤）。
- **6.2 ⚠ 檻の帰属を 1 件書き違えていた（是正済み）**: `the_normal_arm_still_writes_the_character_once_and_the_balloon_once` の doc が「収束が二重書込を足していないこと」を守ると称していたが、**D16 の `!wrote` 条件を落としても緑のまま通る**（通常腕ではバルーンが既に目的座標に居るので余分な `follow_balloon` が指令を積まない）。実際に二重書込を捕まえるのは `frame_dpi_reproject_none_tests.rs` の `s2_none_report_path_reprojects_position_without_touching_size`。doc を訂正し、誤った安心の根拠に使われないよう明記した。**「このテストが緑だから X は無い」の X は、実際に赤にしてみるまで信じないこと。**
- **6.2（引き金）**: 収束は `BalloonFollowTrigger::Placement(PlacementRoute::DpiReproject)` を渡す（通常腕の手順 6 と同一）。書込に載る経路語は `PlacementRoute::BalloonFollow` のままで観測上の誤帰属は生じない。
- **6.3**: 門は `BalloonFollow` 読取の直後・`DPI` 読取の直前に置き、`BalloonKeywordBase` が在るあいだはオフセットも基準対（係留を含む）も 1 bit も触らず `keyword-pending` を 1 行記録して抜ける（D7）。**`keyword_base.rs` は差分ゼロ**（sha256 一致・発火条件と「経路で絞らない」明文とも無改変＝要件 4.5）。分岐は本仕様の新規コード側にある。
- **6.3（檻の実効性を 3 種の摂動で確認）**: ⑴ 門を外すと 6 本中 4 本が赤・**バルーン窓の実書込が 2 件出て揃えが実際に動く** ⑵ 門の中で基準を係留すると「基準対を触った」で赤 ⑶ 門の中へ `warn!` を注入すると赤（＝WARN 0 の主張が空虚でないことの較正）。いずれもレビューが自分で実行。
- **6.3 ⚠ 帰属の注意（実装者が自ら申告・doc に明記済み）**: 寸が変わる腕では**門の有無にかかわらず最終オフセットは同じ**（再導出が絶対値で上書きするため）。その檻が赤になるのは**判定語の側**であって値の側ではない。値・書込レベルの証拠は別の 2 本が持つ。6.2 で掴まえた誤帰属と同型の罠。
- **6.3 → 8.1 申し送り（4 点・実機ログの読み方）**: 門は `DPI` 読取の**前**に立つので `keyword-pending` 行は **`new_dpi=0`** を運ぶ（必須欄検査は通るので特別扱いは不要）。⒜ `new_dpi=0` は**縮退（`unresolved`）と `keyword-pending` の両方**で出るため、**腕の切り分けは `verdict` のみで行い、`new_dpi` の値から腕を推定しない** ⒝ `keyword-pending` 行から遷移後の表示 DPI を読まない——要件 8.5 の比が要るなら `kind=monitor` の起点レコードから取る ⒞ 要件 8.4 の「低い拡大率側で `rescaled` が出る」期待は、キーワードスコープでは**素材消費後の遷移に限る**規則にしないと偽陽性の赤になる ⒟ 受容残余の腕は「`keyword-pending` かつ前後の offset が bit 同一」として現れる——これを「追随が壊れている」と読まないこと。
- **6.3（受容残余の登記先）**: `balloon_offset_follow.rs` のモジュール doc と檻ファイルの doc の両方（要件 4.4 の記録に含む）。檻 2 本は退化排除の証人を持ち恒真ではない。
- **6.4（書く前に実測した）**: 「この檻は追随の証拠にならない」という主張自体が本仕様で既に 2 度外れていたので、**追随の適用を止めた走行で 4 本が本当に緑のまま残るか**を先に確かめてから doc を書いた。結果＝追随側の檻 **13 件が赤・対象 4 本は全て緑**（レビューも独立に再現）。分類の誤りはゼロ。
- **6.4 ⚠ 記述の誤り 3 件目（結論は同じだが理由が違う）**: `research.md` §2.8 (b) は遷移分岐テストを「offset を書込**後**に読んで `balloon − char` と突合するため空振り」と説明していたが、実物は**遷移そのものは起こしており**（`set_scope_dpi`）、`BalloonFollow.offset` について**恒等式すら 1 つも主張していない**から反応しないのだった。空振りという結論は同じでも構造が違う。doc に食い違いを明記済み。**「行番号ではなく構造で見分けよ」は同じ棚卸しの別の行にも当てはまっていた。**
- **6.4（分類の内訳）**: ① 再射影テスト＝空振り（書込後読みの恒等式）／② 再射影なしテスト＝空振り（同上・本来の主張は寸と位置の分離）／③ 遷移分岐テスト＝空振り（遷移は起こすが offset について何も主張しない）／④ 追従可視性結線テスト＝**そもそも遷移を起こさない**（`for dpi in DPIS` で世界を組み直して `resize_window_to` を直接呼ぶだけで `Changed<DPI>` のエッジが立たない）。④ の既存主張（2026-07-31 実機 SSP 裁定の窓相対契約）は 1 文字も書き換えていない。
- **6.4（分量規律の確認）**: `frame_transition_branch_tests.rs` が 1,255→1,271 行と増えたが、例外表 `file_length_guard_test.rs` は**パスと理由文字列だけを登録し行数を固定していない**（表の doc が「行数は理由欄に書かない——書けば無関係な編集で表が陳腐化する」と明記）。よって例外表の編集は不要で要件 9.6 違反にならない（レビュー裁定・番人も緑）。
- **6.5（群 6 完了）**: 行列は `TRANSITIONS[3] × ANCHORS[5] × scopes[2]` を三重に回し、内側で到達数を数えて `assert_eq!(reached, 30)` で突合する（「ループが途中で回っていなかったが全部緑」という最も静かな失敗を検出するため）。比は 5/4・2/1・8/5 で 96 の倍数でない DPI を分子・分母の両側に置く。**期待値は全て `ScaleRatio::scale_len` から紙の上で導出**し、レビューも独立に再導出して一致。
- **6.5 ⚠ 空振り防止の実演で「どこを潰すか」が効いた**: 追随の相を**まるごと**潰すと 7 本すべて赤くなるが、**落ちるのは主張の側ではなく `settle_at` の係留自己検査**（同じ関数が係留も担うため）。正しい是正対は **`apply_rescaled` だけ**を潰す形で、そのとき **4 赤（追随を主張する側）／3 緑（据置きを主張する側）**になる。両方をモジュール doc に記録済み。**「全部赤になった」は「主張が効いている」の証拠にならない。**
- **6.5（行列が掘り当てた本物の相互作用・欠陥ではない）**: `Anchor::Left` × scope 0 では、キャラ窓が作業領域左端に固定されるため追随先がどの作業領域とも交差せず、可視性ガードが **X を引き戻す**（Y は一切触らない＝ガードの明文の事後条件）。ゆえに「バルーン ＝ キャラ窓位置 ＋ 新 offset」の恒等式は**表示位置の上では**成立しない——この恒等式は追従計算の出力時点の事後条件であって表示位置の恒久不変量ではない。檻の側で ⑴ Y は無条件に主張 ⑵ X は交差する組だけ ⑶ **免除した組の数を 3 と逐語で固定**（免除が黙って広がると「バルーンがどこに居ても通る」檻へ退化する）⑷ 免除組でも offset の値・基準対・窓書込・移動は主張、とした。レビューが免除 3 組の同一性まで実測で確認済み。
- **6.5（レビュー所見への是正）**: `ANCHORS` は配列なので `Anchor` の腕が増えてもコンパイルエラーにならず「全アンカー」が黙って縮み得た——`match` による**網羅の証人** `anchor_enumeration_is_exhaustive` を追加して構造で保つようにした。あわせて design Integration 項目 2 へ置き場所の相互参照を追記（素材残存・受容残余・自己回復は 6.3 の兄弟ファイルが先着で所有・カバレッジ欠落は無い）。
- **7**: 保存 → 復元（未係留）→ 係留（値は不変）→ 追随 の 3 段を 1 本に貫く檻を新設（本番差分は `#[cfg(test)]` の mod 宣言 3 行のみ）。**中段の「値は不変」を明示のアサーションにした**——端点だけ見る檻では係留がこっそり再スケールしても通ってしまう。摂動②（`anchor_base_dpi` が係留時に換算する＝最も起こりやすい静かな反転）で実際にその 2 本が赤くなることをレビューが再現済み。
- **7（要件 7.2 の doc）**: 裁定の日付（2026-08-14）・主体（開発者）・出所（windowposition-limit の実機検証）・承知の代償（初回起動で一度崩れる）・**費用の前提が変わってもなお 2026-08-27 に踏襲が選ばれた経緯**・覆す場合の順序まで書いた。後日「復元時に換算すれば直る」と親切に直しに来る保守者を止めるのが目的。
- **7 ⚠⚠ 本仕様の実装に本物の欠陥を掘り当てた（CONFIRMED・別タスクで是正）**: `emo2_boot/frame/balloon_offset_follow.rs` の `OffsetRescale::Unchanged` 腕が**基準値ではなく現在の派生値を据え置く**。`apply_rescaled` は派生値だけを動かし基準は据え置くので、**144 → 192 → 144 の往復でオフセットが `(-683,-64)` に取り残される**（基準は `(-512,-48)@144`）。**要件 3.3（往復で元の相対位置へ戻す）に直接違反**し、design D4（前回の結果を入力にしない）にも違反、要件 8.2 は実機で落ちる。
  - **なぜ誰も捕まえなかったか**: ⑴ 純関数の往復檻 3 本は**不変の base を毎回渡して判定語だけを比較**し、適用後の Component を一度も読まない ⑵ 6.5 の行列 `[(96,120),(96,192),(120,192)]` は**全て単調増加で復帰遷移が皆無**、しかも各組が harness を作り直して遷移 1 回だけ回すため「`Rescaled` の後に `Unchanged`」という腕の並びが World 上で一度も発生しない。**「全部緑」が十分性の証拠にならない実例がまた 1 つ。**
  - **是正の形（レビュー推奨・採用）**: (a) 系＝`Unchanged` 腕で `base.offset` を書く。D4 の「毎回引き直す」と字義一致（恒等比の引き直し結果は `base.offset` そのもの）、純関数も 6 語彙も無改変、末尾の `Changed` 判定経由で D16 の収束が既存のまま働きバルーンが実際に戻る。**ただし語の正直さのため、判定語は `base.offset` と `old_offset` の一致で `unchanged`／`rescaled` を選び分ける。** (b) 案（純関数が恒等でも `Rescaled` を返す）は `unchanged` の語が消えて 8.1 の機械判定と `DPI{0,0}` の既存判断を巻き添えにするので**採らない**。
- **7（写し取りの限界・修正時に doc を限定すること）**: 本ファイルの `observe` は本番の腕→書込口対応を**写して**いる（`rescale_balloon_follow_offset` が `pub(super)` で placement 層から呼べないため）。module doc は「その対応は World 檻が固定している」と書いたが、**`Unchanged` 腕についてはその固定が存在しない**（6.5 に復帰遷移が無いため）——写しの穴と上記欠陥の在処が正確に一致する。
- **✅ 欠陥是正（task 7 が掘り当てた・6.1 のコードの誤り）**: 恒等比の腕を「現在値の据え置き」から「**基準からの引き直し**」へ是正した。`base.offset != old_offset` のときだけ `apply_rescaled(base.offset)` を書き、判定語と `OffsetFollowOutcome` はいずれも**値が実際に動いたか**で決める。純関数も 6 語彙も無改変。是正のみを戻すと**新設 3 本だけ**が赤くなることをレビューが再現済み。
- **⚠ 欠陥是正 → 8.1 申し送り（判定器の前提が変わる・重要）**: ⑴ **往復は `rescaled → rescaled` を出す**（従来想定の `rescaled → unchanged` ではない）。**判定語の列で往復を認定する規則を書くと必ず外す。** ⑵ 是正後 `unchanged` は「値が 1 bit も動かない」と**同値**になった（逆向きの嘘は構造上作れない）。⑶ 一方 `rescaled` は**値が動いた保証ではない**——丸めで動かない腕が既存としてあり得る。⑷ ゆえに**要件 8.2 の「往復の前後で bit 同一」は判定語ではなく観測行の値の欄**（`base_offset=` / `old_offset=` / `new_offset=`）を鍵にすること。
- **欠陥是正（檻の穴も塞いだ）**: 純関数の層ではこの盲点を**表現できない**（`rescale_follow_offset` は Component の現在値を一度も見ない）。ゆえに新規兄弟ファイル `frame_balloon_offset_roundtrip_tests.rs` が 2 層で担う——⑴ 適用相を直接呼び**着地した Component の値**を読む単体檻 ⑵ `FrameHarness` で `96→192→96` と `96→120→192→120→96` を走らせる相結合檻。6.5 の行列は全て単調増加で戻りの遷移が無かった。
- **欠陥是正 ⚠ 檻の中の「写し」が壊れた挙動を写していた**: `balloon_offset_persist_roundtrip_tests.rs` の `observe` が `Unchanged` を no-op として写しており、**写しの穴と本番欠陥の在処が正確に一致**していた（前レビューの予言どおり）。本番に合わせて更新（assert は 1 つも変えていない）。over-broad だった doc 文も「恒等比の腕だけは 2026-08-28 までどの World 檻も固定していなかった」と狭めた。**本番の挙動を写す檻は、本番が壊れていれば一緒に壊れる。**
- **欠陥是正（文書の追随）**: design.md へ実装時訂正（2026-08-28）を追記し、「追随の判断」の流れ図を `Identity → Moved{yes/no}` の 3 ノードへ改訂、`offset_space` の事後条件に「**`Unchanged` は『比が恒等』であって『現在値が正しい』ではない**」の警句、Responsibilities に 1 項、`DPI{0,0}` 行に影響なしの注記。旧読みの残骸をレビューが全文 grep して生きた矛盾ゼロを確認。
- **8.1**: 判定器 `transition_judge_offset.rs` は `kind=monitor` を起点に遷移を切り出し `kind=offset` 行を集計する純関数。判定語は発行側の `pub const` のみ参照（リテラル注入で赤になる檻つき・非循環）。**沈黙は合格にしない**——追随レコード 0 行／往復 0 回／低い側の追随 0 件／キーワード揃えの実測 0 件はすべて違反。
- **8.1（往復の判定の作り）**: 鍵は **`(scope, base_offset, base_dpi, new_dpi)` の 4 つ組で判定語を 1 つも読まない**。「同じ基準対から同じ表示 DPI へ引き直したら必ず同じ値」という **D4 の純関数性そのもの**を突く形なので、往復が `rescaled → rescaled` を出すことにも `rescaled` が値の移動を保証しないことにも影響されない。基準の再確立で鍵が変わり区間が切れるので、利用者のドラッグを往復の破れと読み違えない。
- **8.1（較正を再現）**: レビューが 4 つの判定を 1 つずつ無力化して、狙ったテストだけが赤（1／5／1／2 本）になり合格 fixture は緑のままであることを自分で再現。各不合格 fixture が**意図した違反の種別**で赤くなることも確認済み。
- **8.1（残差の許容量）**: **D8 の契約上限 3px/軸**を採用（決定論の実測最大 2px ではない）。実測値を実機判定に持ち込むと契約が許した 2→3px の悪化で正しいビルドが落ちる。上限内の悪化の監視は実値を逐語で固定している決定論側の仕事。合否経路は完全に整数（浮動小数は表示のみ）。**両軸を単一の DPI 比で測るのは design D5 の明文どおり**（遷移比では作者基準 DPI が約分で消え軸の選択が生じない）——実装者の「`dpi_y` が無いので代用」という説明は誤りだがコードは正しい。
- **⚠ 8.1 → 10.1 申し送り（判定器が見逃す実機の失敗形・正直な限界）**: ⒜ **素の追従スコープの値が一貫して間違っている形**は検出できない——残差の検査はキーワード指定スコープにしか掛からず、素の追従が誤った比で拡縮していても決定論的でありさえすれば全検査を通過する（要件 8.4 の元の所見「低い側でバルーンがずれる」が「語は出ているのに値がずれている」形なら見えない）。⒝ **中央揃えそのもののずれ**は見えない——測っているのは丸め残差の 3 出所のうち 1 つだけで、`char_w`／`balloon_w` の丸めと中点の整数除算は寸法がログに無いので原理的に不可視。しかも 3 出所ぶんの 3px を 1 出所（真の残差 ≤0.5px）へ当てているので**網は約 6 倍粗い**。⒞ offset 以外に由来する見た目の破れ（キャラ窓位置・Z 順・可視状態・バルーン寸）。⒟ 一部スコープだけ基準対が毎遷移で作り直される欠陥（沈黙の番人がスコープ横断の 1 本なので他スコープの往復で埋もれる）と、最初の `kind=monitor` より前に出た追随行。**目視のサインオフ項目はこれらを補う位置づけで読むこと。**
- **⚠ 8.1 → 8.2 申し送り（手順書に必須で書くこと・書き漏らすと正しいビルドが赤になる）**: ⑴ **キーワード指定バルーンを必ず一度は素材消費させたうえで低い拡大率側へ遷移させる**（点灯しないと `NoKeywordAlignmentMeasured` で赤）。⑵ **素の追従スコープを必ず 1 つ含める**（`last_pending` はログ全体の最後の門を採るため、全スコープがキーワード指定で末尾が門で終わると偽の赤になり得る）。⑶ 継ぎ目は `judge_offset_log` / `OffsetReport` の 1 本。⑷ 手順書は `.md` として起こし `PROCEDURE_SOURCES` へ足す（`transition_judge_offset_tests.rs` は 712 行で残り 288 行・`#[ignore]` ランナーは兄弟ファイルへ）。
- **8.2（群 8 完了）**: `#[ignore]` ランナーは既存の `TRANSITION_LOG_ENV`（`AREKA_TRANSITION_LOG`）を再利用し、**新しい環境変数を増やさない**——同一の実機走行が両判定器の材料になり、操作者が同じログを 2 度採らずに済む。未設定・空白・不達パス・ディレクトリ・観測行 0 行はすべて失敗（レビューが 4 経路とも自分で赤を確認）。既定で走る檻 5 本がその失敗経路を固定するので、`#[ignore]` の本体が無効化されても失敗経路の検査は残る。
- **8.2 ✅ 自ら消える保留欄が回収された**: 手順書を `PROCEDURE_SOURCES` へ足し、`PENDING_PROCEDURE_KINDS` を空にした。5.1/5.2 で仕込んだ `pending ∩ covered = ∅` が実際に機能し、保留を残すと赤になることをレビューが再現済み。
- **8.2 ⚠ 前向き検査の空振りは実在した（申し送りが効いた）**: `the_procedure_only_quotes_record_lines_the_emitters_can_produce` を `procedure_sources_text()` へ切り替える**前**は、新しい手順書の観測行例を壊しても**緑のまま通った**（レビューが実測）。切り替えなければ、新出所が引用する観測行を誰も検査しないままだった。5.1/5.2 のレビューが予言した穴がそのとおり空いていた。
- **8.2（判定語の検査は分離が正しい）**: `the_procedure_names_the_runner_vocabulary_verbatim` は**先行仕様 1 本のまま据え置く**——集合化すると片方の文書の欠落を他方が埋めて空振りになる。本仕様ぶんは新設の専用検査が `OFFSET_VERDICT_ALL` から起こして見る。
- **8.2（申し送りの誤りを実装者が訂正）**: 低い拡大率側の母数制限は「ログ全体の最後の門より後」ではなく「**そのスコープごとの**最後の門より後」。実物（`last_pending: BTreeMap<u32, usize>`）を読んで訂正し、手順書もその正確な形で書かれている。**手順書に誤った規則を書き込むのは手順書が無いより悪い。**
- **8.2（design を実装へ追随させた）**: ランナーと手順書の器を `transition_judge_offset_tests.rs` から兄弟 `transition_judge_offset_signoff_tests.rs` へ移した理由（同ファイルが 712 行で分量規律に触れる）と、環境変数を新設せず再利用した理由、番人の 3 点の作業（出所登録・保留欄の解消・前向き検査の切替）、手順書に必須の内容（偽の赤を生む 2 形の禁止・判定器が見ないもの 4 件の配置）を design.md へ実装時訂正として登記。**なおレビューが「design が新環境変数 `AREKA_TRANSITION_OFFSET_LOG` を指定している」と述べた点は事実ではない**（design のどこにも無い）——追随が要ったのはファイル名の 1 点だけ。
