# SSP 実測オラクル 作業ノート（R1・進行中）

> 計測ツール: `tools/measure-ssp-rects.ps1`（DPI aware Per-Monitor v2・読み取り専用ポーリング）
> 証跡ログ: `ssp-rects-steady-dpi96.log`（定常スナップショット）／`ssp-rects-boot-dpi96.log`（再起動時系列）

## セッション記録（2026-08-11・DPI 96 = 100%・work area 2880×1752）

### セッション1（10:35 起動・プロファイル削除済み初回起動・開発者操作）
- OnFirstBoot 実行の確証あり（スクリーンショットの会話が boot.pasta:78-89 の 8 往復と一致）。
- **起動直後の観測なし**（ポーリング開始が 10:38 のため初期配置は未捕捉）。
- 定常値（10:37-10:41 で不変）:
  - むらさき (scope0) `Tmainform`: L=2498, T=1205, **W=382, H=547**
  - エモ (scope1) `Tkeroform`: L=2142, T=1352, **W=336, H=400**
  - scope0 右端 2880 ＝ work area 右端（右端密着）／二体の窓間隙間 = 2498 − (2142+336) = **20px**

### セッション2（10:42 再起動・プロファイル削除の有無 **要確認**）
- 10:41:28 旧プロセス消滅 → 10:42:04.828 キャラ窓出現。**出現時点で既に L=2498 / L=2142（定常と同値）**。
- 以後ポーリング終了（10:43:06）まで両キャラ窓の移動イベント **0 件**（バルーン出現は 10:42:07.773）。

## 確定事実（プロファイル状態に依存しない）
1. SSP の scope0 は work area 右端密着（defaultx=0 の右端基準解釈は現行 areka と一致）。
2. SSP の窓寸は shell PNG 原寸と不一致: surface0.png=434×687 だが むらさき窓=382×547／surface10.png=427×463 だが エモ窓=336×400。→ SSP の窓は「現在表示中の合成サーフェス寸」（areka 実機ログの scope1 char_w=420@DPI120 ÷1.25=336 と一致し、エモ寸の解釈は SSP/areka で合意）。
3. 現行 areka 式（前スコープ幅を引く）の予測隙間 = 幅差 46px（382−336）。観測 20px と不一致 → **現行式は SSP と不一致（R1.5 判定: 不一致）**。
4. H3（各スコープ独立右端密着）なら重なりが出るはず → 観測と不一致・**棄却**。

## 未確定（プロファイル削除の有無と move 汚染の切り分け待ち）
- 観測値 kero L=2142 の正体は次の 3 候補のどれか:
  - (a) SSP 既定配置そのもの（= H2: 隣接＋マージン 20px。2142 = 2498 − 336 − 20 が厳密成立）
  - (b) OnFirstBoot の `\![move,-353,,,0,base,base]`（boot.pasta:79）の適用結果（scope0 L 基準の素朴計算では 2498−353=2145 で観測と 3px 差＝アンカー解釈が別の可能性）
  - (c) セッション2がプロファイル未削除だった場合の前回終了位置の復元
- move はゴースト（emo2）自身の演出であり SSP 既定規則ではない。**既定規則の単離には move 前の初期矩形が必要**（requirements R1.6）。

## 切り分け実験の結果（同日追記）

### プロファイル削除の確認（開発者回答）
セッション2は**プロファイル・シェルプロファイルとも削除済み・OnFirstBoot 発動**。→ (c) 復元説は排除。
セッション2の初出現 tick（10:42:04.828）はトーク開始（バルーン出現 10:42:07.773）の 3 秒前 ⇒ **DPI 96 の既定配置 = エモ窓 L=2142（窓間隙間 20px）で確定**。move 適用後も不変（着地点＝既定位置と一致）。

### 表示サーフェスの正体（窓寸 382×547 / 336×400 の出所）
- surface0.png (434×687)・surface10.png (427×463) は**全面不透明**（透明余白ゼロ・刈り込み説は棄却）。
- surfaces.txt: \0 実表示は **surface1000**（bind 合成・ベース `purple/0/base1.png` = **382×547**）、\1 実表示はエイリアス通常→**surface2100**（`CityPop/surface0100.png` = **336×400**）。
- ⇒ SSP の窓寸＝現在表示サーフェスの合成寸。窓寸の解釈に SSP/areka の相違なし。

### セッション3（10:55 起動・DPI 192 = 200%・プロファイル削除済み初回起動）
証跡: `ssp-rects-boot-dpi192.log`。work area 2880×1704。
- むらさき: L=2116, W=764(=382×2), H=1094 → 右端 2880・下端 1704 **＝ work area へ両密着 ✓（既定規則が k=2 でも維持）**
- エモ: L=1757, W=672(=336×2) → 右端 2429 vs むらさき左端 2116 ＝ **313px の重なり**（開発者も目視で「崩れた」と確認）
- **ただし初出現 tick（10:55:23.599）に むらさきバルーンが同時出現** ⇒ トーク既開始 ⇒ この値は `\![move]` 適用後。**DPI 192 のエモ既定配置は未観測**（96 のときと違い窓出現→トーク開始が 100ms 未満で、move 前を捕捉できず）。

### `\![move,-353,,,0,base,base]` の意味論解読（両 DPI で厳密一致）
エモの観測中心 X（=L+W/2）が次式で両 DPI とも**誤差 0** で説明できる:
```
エモ中心X = (work_right − 217k) − 353     ※k=DPI/96、217 = surface0幅434の半分
  DPI 96 : (2880−217) −353 = 2310 = 2142+168 ✓
  DPI 192: (2880−434) −353 = 2093 = 1757+336 ✓
```
読み: 基準 = scope0 の**ロード時既定基準点**（代表面 surface0 (434k) を右端密着に置いたときの足元中央）。オフセット **−353 は DPI スケールされず物理 px のまま適用**される。
- ⇒ 開発者所見「SSP の座標補正は 100% でしか正しく動かない」の精密化: **窓寸は k 倍される（シェルサイズは 200% ✓）が、スクリプト座標（move オフセット）は無スケール**。96 前提で書かれたゴーストの move は高 DPI で幾何が崩れる（今回の 313px 重なりの正体）。
- 96 では既定配置と move 着地が**厳密に同値**（中心間距離 353）——ゴースト作者が 96 の既定値を move に焼き込んだと解される（emo-gs 忠実）。

### 座標モデル（開発者仮説の検証状況）
- **足元中央（サーフェス中央下）基準の座標モデルは確定**: ukadoc property `currentghost.scope(ID).x`（基準点＝通常サーフェス中央下）＋ 上記 move 解読の厳密一致。
- **既定配置規則の「シェル幅非依存」はまだ未確定**: DPI 96 の一点観測では
  (i) 窓隣接＋マージン20（幅依存: L = L0 − W1 − 20）と
  (ii) 中心間固定距離 353（幅非依存）
  が同一の一点を通り判別不能。**幅の異なる別シェルの観測が必要**。

## 残る未観測・次の実験
1. **【本命】幅の異なる別ゴースト（Emily/4 等・boot move なし）の初回起動観測**——100% と 200% の両方。
   - 幅依存 (i) vs 幅非依存 (ii) の判別、および **SSP 自身の既定規則が DPI≠96 で正しくスケールするか**（scope0 は右端密着維持を確認済み・scope1 側が未知）の両方が一度に確定する。
2. （代替）emo2 DPI192 を 10ms ポーリングで再走し move 前の初期矩形を競争捕捉（不確実）。
3. **要件への波及（保留・要ディスカッション）**: SSP の既定配置自体が DPI≠96 で無スケール（壊れている）と判明した場合、R2.3/R6.4 の「実 DPI で SSP 実測と一致」は成立不能となる。その場合オラクルは「96 で規則を確定 → areka は自前の DPI 追従設計で k 倍」へ再定義が必要（areka は DPI 追従が基本設計・SSP の DPI バグは互換対象外、の裁定候補）。
