# SSP 実測オラクル 作業ノート（R1・進行中）

> 計測ツール: `tools/measure-ssp-rects.ps1`（DPI aware Per-Monitor v2・読み取り専用ポーリング）
> 証跡ログ: `ssp-rects-steady-dpi96.log`（定常スナップショット）／`ssp-rects-boot-dpi96.log`（再起動時系列）

## セッション記録（2026-08-11・DPI 96 = 100%・work area 2880×1752）

### セッション1（10:35 起動・プロファイル削除済み初回起動・開発者操作）
- OnFirstBoot 実行の確証あり（スクリーンショットの会話が boot.pasta:78-89 の 8 往復と一致）。
- **起動直後の観測なし**（ポーリング開始が 10:38 のため初期配置は未捕捉）。
- 定常値（10:37-10:41 で不変）:
  - むらさき (scope0) `Tmainform`: L=2498, T=1205, **W=382, H=547**
  - エモ (scope1) `Tkeroform`: L=2142, T=1352, **W=336, H=400**
  - scope0 右端 2880 ＝ work area 右端（右端密着）／二体の窓間隙間 = 2498 − (2142+336) = **20px**

### セッション2（10:42 再起動・プロファイル削除の有無 **要確認**）
- 10:41:28 旧プロセス消滅 → 10:42:04.828 キャラ窓出現。**出現時点で既に L=2498 / L=2142（定常と同値）**。
- 以後ポーリング終了（10:43:06）まで両キャラ窓の移動イベント **0 件**（バルーン出現は 10:42:07.773）。

## 確定事実（プロファイル状態に依存しない）
1. SSP の scope0 は work area 右端密着（defaultx=0 の右端基準解釈は現行 areka と一致）。
2. SSP の窓寸は shell PNG 原寸と不一致: surface0.png=434×687 だが むらさき窓=382×547／surface10.png=427×463 だが エモ窓=336×400。→ SSP の窓は「現在表示中の合成サーフェス寸」（areka 実機ログの scope1 char_w=420@DPI120 ÷1.25=336 と一致し、エモ寸の解釈は SSP/areka で合意）。
3. 現行 areka 式（前スコープ幅を引く）の予測隙間 = 幅差 46px（382−336）。観測 20px と不一致 → **現行式は SSP と不一致（R1.5 判定: 不一致）**。
4. H3（各スコープ独立右端密着）なら重なりが出るはず → 観測と不一致・**棄却**。

## 未確定（プロファイル削除の有無と move 汚染の切り分け待ち）
- 観測値 kero L=2142 の正体は次の 3 候補のどれか:
  - (a) SSP 既定配置そのもの（= H2: 隣接＋マージン 20px。2142 = 2498 − 336 − 20 が厳密成立）
  - (b) OnFirstBoot の `\![move,-353,,,0,base,base]`（boot.pasta:79）の適用結果（scope0 L 基準の素朴計算では 2498−353=2145 で観測と 3px 差＝アンカー解釈が別の可能性）
  - (c) セッション2がプロファイル未削除だった場合の前回終了位置の復元
- move はゴースト（emo2）自身の演出であり SSP 既定規則ではない。**既定規則の単離には move 前の初期矩形が必要**（requirements R1.6）。

## 切り分け実験（計画）
1. **セッション2のプロファイル削除有無を開発者に確認**（(c) の排除）。
2. プロファイル削除 → ポーリング起動 → SSP 起動、の順で再実施。窓出現の瞬間から OnFirstBoot トーク完了まで観測すれば「既定配置 → move」の時系列が完全に取れる。
3. move の着地点の単離: エモを手動ドラッグで別位置へ→ menu.pasta:65 の同一 move を発火→ 着地 X を観測（= move 目標値。既定配置と分離して確定できる）。
4. DPI 125%（=120, k=5/4）で 2. を再実施（R1.2 の実 DPI ≠96 必達の本命観測・スケール則の確認）。
