# Implementation Plan

> 前提: x64 の決定論テスト（1〜4）は i686 の成果物なしで回る。i686 の 2 成果物（`shiori-host32-helper`・`shiori-host32-testdll-loadu`）は 5 の smoke と 6.2 の実機でだけ要り、5.1 で建てる。各タスクの完了時に `cargo build --workspace --tests`（x64）が通る状態を保つ（途中でワークスペースを壊したまま次へ渡さない）。
>
> 要件 8.7（裁定 1〜6 を覆す必要が出たら開発者へ議題として上げ、確定を待つ）は成果物を持たない進め方の決まりなので、専用のタスクは置かない。全タスクの実装中に守る。

- [x] 1. kanade: 失敗の種類と理由を停止通知まで運ぶ
- [x] 1.1 失敗の種類の語彙と写しを足す
  - 失敗の種類 5 値・種類＋理由・死活報告の種類 2 値を kanade の公開語彙として足し、crate の外へ再輸出する（既存の型の形はまだ変えない＝追加だけ）
  - 呼出失敗からの写し（接続・期限切れ・通信・内部の 4 腕と、送出点で写されて届かない契約のエラー応答の腕＝内部の失敗へ写す）、死活報告からの写し（2 腕）、原因不明の値を、wildcard なしの純関数で持つ。エラー応答の腕には「届かない契約・届いたら内部の失敗」をコメントで記す
  - 兄弟のテストファイルで、届く 4 腕・死活報告 2 腕・原因不明の種類と理由を固定し、緑になっている（エラー応答の腕は判断のテストにしない）
  - _Requirements: 1.4, 2.2, 7.3_

- [x] 1.2 死活報告に種類を持たせる
  - 死活報告のメッセージを「種類＋理由」にし、接続失敗の送出は「接続できなかった」、helper の予期しない終了は「通信が切れた」の種類で送る（理由の綴りで判別しない）
  - 運行表の死活報告の腕はこの段では種類を受け取るだけ（`kind: _` で受けるだけ・判断・`error!(event="shiori_down")` は不変）
  - 死活報告を綴る kanade のテスト（運行表・real・結合テストの共通部品）を機械的に追随させ、`cargo build --workspace --tests` と kanade の既存テストが緑
  - _Requirements: 2.2_

- [x] 1.3 停止原因の Fault を種類＋理由つきにし、Fault へ倒す腕に載せる
  - 停止原因の Fault を種類＋理由つきにし、停止原因・停止通知は Copy を外して Clone にする。内部の終了原因（Clone を足す）と Fault へ倒す関数も種類＋理由を受け取る
  - 死活報告の腕は死活報告からの写し、応答待ちの横断の腕は呼出失敗からの写しを渡す。どの腕が Fault へ倒すかの判断・既存の `error!` は変えない
  - 停止通知の写しは終了原因を clone して写し、原因を控えられなかったときは原因不明の Fault を送る
  - Copy を失ったことでコンパイルが指す箇所を追随させる: 内部の終了原因の Fault を綴る kanade のテスト（`Fault(_)` へ）・停止原因を綴る areka-ghost／areka のテスト・areka の終了の出所（Copy → Clone・4 呼び手は値渡しのまま）
  - `cargo build --workspace --tests` が通り、kanade・areka-ghost・areka の既存テストが緑
  - _Requirements: 2.1, 2.2, 2.3, 8.2_

- [x] 1.4 エラー応答を送出点で「返事なし」に写して会話を続ける
  - 送出点の往復の末尾で、エラー応答の失敗だけを GET なら返事なし（204 相当）、NOTIFY なら通知済みに写して再投入し、警告の記録 `shiori_error_response` を 1 往復に 1 件残す。他の 4 種は Failed のまま（回数の閾値は置かない）
  - 起動時の `username` 照会・選択肢の往復中の腕は無改変（前者はエラー応答なら返事なしとして sink へ渡る）
  - kanade の失敗テストで、起動時（`OnInitialize`＝NOTIFY）と会話中（`OnSecondChange`＝GET）の両方にエラー応答を注入し、kanade が止まらず次の呼出が記録され `shiori_error_response` が 1 件であることを固定する。既存の「失敗の語彙ごとに終了する」テストからエラー応答を外す
  - 選択肢の往復中の失敗テスト（`choice_test_stage_failure_tests.rs` の 2 本）が注入しているエラー応答を通信の失敗（または内部の失敗）へ替え、「選択肢の往復中は 204 扱い」と「選択肢以外の失敗は Fault」の分岐を引き続き踏ませる（エラー応答のままだと前者は分岐を踏まず、後者は停止せず赤になる）
  - 上記のテストが緑で、エラー応答では停止通知が出ないことが観測できる
  - _Requirements: 6.1, 6.2, 7.3, 8.3_

- [x] 1.5 失敗の入口ごとに停止通知の中身を固定する
  - kanade のテスト用ハーネスに、失敗を注入しつつ停止通知の投函端を持つ派生を 1 つ足す（既存の停止通知つきハーネスと同型）
  - 呼出失敗 4 種（接続・期限切れ・通信・内部）と死活報告 2 種のそれぞれで、停止通知が Fault の種類と理由（対応する `error!` と同じ文言）で届くことを固定する
  - 停止通知の写しの単体テストを中身つきの 5 値に更新し、原因不明が原因不明の種類の Fault になることを固定する
  - 入口 6 種＋原因不明のテストが緑
  - _Requirements: 2.1, 2.2, 7.3_

- [x] 2. 判定・告知・x64 の接続失敗を固定する
- [x] 2.1 (P) x64 の接続失敗で停止通知に種類と理由が載ることを固定する
  - 接続に失敗する SHIORI（`ShioriWiring::Custom` が Err を返す）での起動を停止通知の投函端つきに替え、停止通知が「接続できなかった」と接続失敗の理由で届くことを足す
  - ゴースト起動の失敗型の doc にある古い「ダミー窓」の 1 行を今の挙動に直す（コード無改変）
  - areka-ghost の接続失敗の e2e テストが緑
  - _Requirements: 2.4, 7.5_
  - _Boundary: areka-ghost, areka-kanade（shiori/real.rs）_

- [x] 2.2 (P) 最初の終了の出所を残し、告知と終了コードを 1 つの判定で決める
  - 終了の指示が最初の出所を World の資源に 1 度だけ残し、2 度目以降は debug の記録 `app_exit_again` で流す（最初が勝つ）
  - 出所が「kanade の停止で原因が Fault」のときだけ失敗の中身を返し、他の停止原因 4 値・強制退避・smoke の自動終了・OS の閉鎖要求では何も返さない判定を 1 つ置く（告知の有無と終了コードの両方がこれを見る）
  - 終了の記録 `app_exit` の出所に種類と理由が載る（既存の Debug 出力のまま）
  - 判定の表 8 行と「2 度呼んでも最初の出所のまま・`app_exit_again` 1 件」のテストが緑
  - _Requirements: 1.7, 1.8, 1.12, 2.5, 3.3, 4.5, 7.2_
  - _Boundary: app_exit_

- [x] 2.3 (P) 告知の部品に「SHIORI が動かなくなった」場面と場面ごとの題名を足す
  - 告知の場面に SHIORI の失敗（ゴースト名の有無・置き場所・失敗の中身）を足し、題名を場面ごとに返す（既存 4 場面は「areka を起動できません」のまま、新場面は「SHIORI が動かなくなりました」）
  - 本文は 3 行（ゴースト名と置き場所／名前が無ければ置き場所だけ・失敗の種類の 5 語・理由の一行）で、起動時か会話中かは載せない。種類から 5 語への写しは純関数
  - 告知の出し方（記録 1 件・抑止で箱を出さない・OK のみ・戻り値なし）は変えず、message の文言だけ場面に依らないものにする
  - 既存 9 本は無改変で緑、5 種類 × 名前の有無の 10 本が緑
  - _Requirements: 1.3, 1.4, 1.5, 1.6, 1.9, 3.5, 5.1, 5.2, 5.3, 5.4, 7.1_
  - _Boundary: alert_

- [x] 3. 停止通知の受け口を起動の 2 経路で共有する
- [x] 3.1 受け口を World の資源にし、終了相が World から読む形にする
  - 停止通知の受け口を World の NonSend 資源として持ち、終了相は World から受け口を読む（資源が無ければ何もしない）。判断（全件取り出し・最初の 1 件で 1 度だけ終了の指示・窓 0 は正常系・原因による分岐なし）は不変。終了相を呼ぶだけの独立 system を用意する（登録は 3.2）
  - この段ではフレームの system からの終了相の呼び出しを残し、実 sink 結線は受け取った受信端を設定口の代わりに World の資源として挿す（実 sink 結線の起動が途中で終了相を失わない。呼び手を失った設定口は 3.2 で退役するまで警告を抑えて残す）
  - 閉じる相のハーネス（`emo2_boot/spine.rs` の受信端の設定）と `spine_close_wiring_tests.rs` の終了相の呼び出しを新しい形へ追随させる（判断なし）
  - 終了相のテストを World の資源で組み直し（既存 4 本の判断は不変）、Fault の場合（`ghost_quit`／`app_exit` の記録に種類と理由・最初の出所が Fault）を 1 本足す。既存 4 本＋Fault の 1 本が緑、ワークスペースが通る
  - _Depends: 2.2_
  - _Requirements: 1.2, 2.5, 6.5, 7.4_

- [x] 3.2 統合: 受け口の据え付けを 1 か所にし、実 sink 結線と fn main をつなぎ替える
  - 受け口を World に挿し、終了相の system を「フレームの system より前」で Update に登録する据え付け関数を 1 つ置く（起動の 2 経路で共通・1 回だけ）。フレームの system から終了相の呼び出しを外し、実 sink 結線の受け口の欄と設定口を退役させる
  - 実 sink 結線は内部で channel を作らず、引数で受け取った送出端を起動へ渡す。既存の結線テストは引数の追随だけ
  - `fn main` に最小の結線を入れる: 停止通知の channel を 1 本作り、実 sink 結線へ送出端を渡し、起動の分岐の後・run の前に据え付けを 1 回呼ぶ（LogSink 側の起動への配り・run の後の処理は 4.2）
  - フレームの system を登録しない素の World で同じ登録を行い、停止通知 1 件 → Update 1 回で終了が指示され最初の出所が残ることを固定するテストを 1 本足す（bevy がこの登録の形を拒むようになったら赤）
  - 上記のテストが緑、`cargo build --workspace --tests` と areka の既存テストが緑
  - _Depends: 2.2_
  - _Requirements: 6.4_

- [x] 3.3 実 sink 結線なしの起動でも停止通知が終了の指示に届くことを固定する
  - 実 sink 結線の無い World に受け口を挿し、接続に失敗する SHIORI（`ShioriWiring::Custom`）で停止通知の送出端つき起動を行い、有界に終了相を回して終了が指示され、最初の出所が「接続できなかった」の Fault であることを確かめる兄弟テストを足す（検体は 2.1 の接続失敗の e2e と同じ最小の一時フォルダの組み方に倣う）
  - テストが有界の時間内で緑
  - _Requirements: 6.4, 7.6, 8.5_

- [x] 4. fn main: 後始末を必ず通し、告知して終了コードを決める
- [x] 4.1 run の成否によらず後始末を通して終了コードを決める判断を関数に切り出す
  - run の結果・Fault の有無・後始末の閉包を受け取り、後始末を必ず 1 回通し、run の失敗・後始末の失敗・Fault のどれか 1 つでもあれば失敗（終了コード 1）、なければ成功（0）を返す関数を置く
  - 兄弟のテストで、Ok／Err の両方で後始末がちょうど 1 回走ること・失敗を返す 3 条件・すべて無ければ成功を固定し、緑
  - _Requirements: 3.1, 3.2, 3.4, 6.3, 7.6, 8.6_

- [x] 4.2 fn main の LogSink 側の起動と run の後を結線する
  - LogSink 側の起動（`else` 腕）を停止通知の送出端つきの起動に替え、3.2 の channel の送出端を配る（`main` 自身の送出端は落とす）
  - run の `?` を外し、最初の出所を World から読み、Fault なら告知の場面（ゴースト名は実行環境の mount から・置き場所・失敗の中身）を組む。4.1 の関数に、後始末 ①（再生ループの停止）の直後・② の前に告知を出す形で ①〜④ を渡す（①〜④ の中身と順序は無改変・②③ の失敗は今日どおり記録して止める）
  - x64 で `cargo build -p areka` と areka の全テストが緑。Fault 以外の終了経路の終了コード 0 は変わらない
  - _Requirements: 1.1, 1.2, 1.6, 1.10, 1.11, 1.12, 3.1, 3.2, 3.3, 6.3, 6.4, 8.1, 8.4_

- [x] 5. 常設 smoke で無人の走行を確かめる
- [x] 5.1 i686 の 2 成果物を建てる
  - PowerShell で `cargo build -p shiori-host32-helper -p shiori-host32-testdll-loadu --target i686-pc-windows-msvc` を走らせる（smoke ①②④ と実機が使う helper と検証用 DLL）
  - `target/i686-pc-windows-msvc/debug/` に `shiori-host32-helper.exe` と `shiori_loadu.dll` の 2 つがあり、どちらも最新のソースから建っている（2026-09-24 のタスク生成時点で建て済み。target を掃除したら建て直す）
  - _Requirements: 4.1, 4.4_

- [x] 5.2 i686 成果物を自分で揃え、①② に「SHIORI の失敗で終わっていない」を足す
  - `areka.exe` の隣に helper が無ければ環境変数 → workspace の i686 target（debug・release）の順で探して複製し、無ければ建て方（helper と検証用 DLL の 2 つの `cargo build --target i686-pc-windows-msvc`）を案内して失敗する。複製はプロセス内で 1 度に絞り、一時名へ書いてから改名する（並走するテストが書きかけを起動しない）
  - ①② の判定に「告知の題名を含む行が 0 件」を足す（終了コード 0 の判定は既存）。③ は無改変
  - i686 の helper がある環境で ①②③ が緑
  - _Requirements: 4.1, 4.2_

- [x] 5.3 ④ 失敗方向を足す
  - 同梱の emo2 の複製の descript を「SHIORI は検証用 DLL」の最小内容に差し替え、検証用 DLL を複製の中へ置き、`loadu` を偽にする環境変数・告知の抑止・自動終了 20 秒・一時のプロファイルで起動する
  - 判定: モニタ 0 台なら既存の受理で返る。そうでなければ見張り 60 秒の内側で終わり、終了コードが 0 以外、告知の題名を含む行がちょうど 1 件、窓の目印あり
  - 環境変数が helper へ届かず終了コード 0 で赤になったら、本番コードに口を足さず検証用 DLL（test crate）の側を環境変数なしで `loadu` が偽を返す形に直す
  - i686 の 2 成果物がある環境で ①〜④ が緑、本番コードが読む環境変数は増えていない
  - _Requirements: 3.5, 4.3, 4.4, 4.5, 4.6, 6.6_

- [ ] 6. 記録と実機確認
- [x] 6.1 (P) 互換方針の文書とロードマップの台帳を更新する
  - `doc/COMPAT_ARCHITECTURE.md` §8 の表に、Fault で終わるときの終了コード 1（完了 spec 要件 3.8 の Fault に限った上書き・プロセスが終わるときだけの限定・定義点・根拠）と、エラー応答を致命にしない裁量（正典の沈黙・定義点・根拠）の 2 行が既存の 4 列で入っている
  - `.kiro/steering/roadmap.md` の台帳 #57 の行が「#55 で修正」に更新されている
  - _Requirements: 3.6, 8.1, 8.3, 8.6_
  - _Boundary: doc, roadmap_

- [ ] 6.2 実機で 3 走行を見て記録する
  - ① 失敗する検体（抑止なし）で告知の題名・ゴースト名・「SHIORI に接続できなかった」・理由が出て、OK で終了コード 1・プロセスが残らない。② emo2 に短い応答期限で「応答が期限内に返らなかった」の告知が出て同様に終わる（使った値を記す）。③ ① に抑止と自動終了 20 秒で窓が出ずに 1 で終わる
  - `RUST_LOG` は判定の分岐（`shiori_down`／`shiori_failed`／`ghost_quit`／`app_exit`／`alert`）の水準まで開ける
  - spec フォルダの `signoff.md` に 3 走行のコマンド・終了コード・目印の件数が残っている
  - _Depends: 5.3, 7.1, 7.2_
  - _Requirements: 7.7, 7.8_
  - 1 回目（2026-09-25）で出た 2 件の欠陥は 7.1・7.2 で直してから取り直す（`signoff.md` の 1 回目の記録は残す）

- [ ] 7. 実機で見つかった欠陥を根本で直す（2026-09-25 開発者裁定＝推奨どおり境界を広げて直す）
- [x] 7.1 (P) i686 helper 経路の期限切れを「期限内に返らなかった」に写す
  - `crates/shiori-host32-ipc/src/lib.rs` の `send_copydata_with` で、`SendMessageTimeoutW` の直前に `SetLastError(ERROR_SUCCESS)` を呼び、戻り 0 のうち `GetLastError() == ERROR_TIMEOUT` だけを `IpcError::Timeout` にする（存在しない窓・応答なしの打ち切りなど他は従来どおり `SendFailed`）。同じファイルの説明を直す
  - 同じ crate に決定論的なテストを足す: 別スレッドの message-only 窓が `WM_COPYDATA` の手続きで眠る → 短い期限で送ると `Timeout`、存在しない窓へ送ると `SendFailed`。写しを戻すと前者が赤
  - i686 の helper を建て直し、host32 の既存テストと kanade のテストが緑
  - design の Out of Boundary・Non-Goals に「期限切れの分類 1 か所だけ本 spec で直す」例外を記す
  - _Requirements: 1.4, 2.2, 7.7_
  - _Boundary: shiori-host32-ipc, design_

- [ ] 7.2 (P) wintf の `run()` から戻った後にフレームを回さない
  - `crates/wintf/src/runtime/tick_bridge.rs` の tick タスクに「ループはまだ回っているか」の旗を渡し、起きたらまず旗を見て下りていればフレームを回さずに終える。World の強参照を待ちの間握らない（`upgrade` を待ちの後へ）。`crates/wintf/src/runtime/mod.rs` の `WinApp::run` で旗を作り、`block_on` から戻った直後・登録表を取り除く前に下ろす（同じ形の VSync 中継タスクにも渡す）
  - `tick_bridge.rs` のテストに、旗を下ろした後の通知ではフレーム数が進まないことを固定する 1 本を足す（修正前は赤）
  - design の Out of Boundary の「wintf（無改変）」と関連する表・リスク欄を「`run()` 復帰後にフレームを回さない守り 1 か所のみ改変」に改める
  - _Requirements: 1.11, 8.4_
  - _Boundary: wintf, design_

## Implementation Notes

- 1 ファイル 1,000 行の見張り（`log-capture-kit` の `file_length_guard_test`）がある。テストを足すたびに `cargo test -p log-capture-kit -j 4` も回し、超えるなら兄弟ファイルへ分ける（`OVER_LIMIT_ALLOWED` には足さない）。
- 別スレッド（kanade のアクター等）で出る記録を統合テストで数えるには全スレッドの捕捉が要り、`log-capture-kit/tests/with_default_guard_test.rs` の例外表への登録が要る。単体で数えられるならスレッド局所の `capture` を使う。
- 2.1 のデバッグ: 接続に失敗した SHIORI アクターが受信端を捨てると、kanade へ直送の Boot が死活報告より先に着き「通信が切れた」になっていた（順序の保証なし）。接続失敗後もアクターが受信を続け、要求に `Handshake(理由)`・Unload に `Unloaded` を返す形にした＝どちらが先でも種類は「接続できなかった」。理由の文言は着順で「shiori handshake failure: 〜」／素の理由に揺れる（どちらも対応する `error!` と同じ）ので、後続（3.3・5.3）は種類で判定し、理由は含有で見る。
- 3.2 の時点で `main` は停止通知の送出端を `wire_emo2_boot` へそのまま移している。4.2 で LogSink 側へも配るときは `tx.clone()` に変え、`zorder_wiring_tests.rs` の t_zwi08 が照合する字面も合わせて直す。
- （開発者依頼 2026-09-24）最終検証（`/kiro-validate-impl`）まで進んだら main を取り込む（スキルの調整が入っているため）。
- `/kiro-complete` で本 spec（#55）を完了するとき、roadmap 台帳 #57 の行も段列を `α 後` → `✅`・状態列を `✅ #55 で修正` にし、手書きの数 2 か所（「spec 台帳」の見出しと「brief の体制」の行）を ✅ +1・α 後 −1 に直す（6.1 で #55 完了前の ✅ を避けて保留）。
