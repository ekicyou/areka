# 実装計画

- [ ] 1. 通信層に「任意の文字コード」を表す型と交渉状態を用意する
- [x] 1.1 文字コードを表す値と、ラベル解決・符号化・復号を用意する
  - 通信層のクレートに文字コード基盤（`encoding_rs`・workspace 既存）を依存として足す
  - 文字コードを列挙せずに表す値を新設し、構築経路をラベル解決と 2 つの定数（UTF-8・既定の Shift_JIS）に限る。ラベルの一覧を通信層に持たない
  - ラベル解決は前後の空白・大文字小文字・別名を寛容に扱い、符号化の出力が要求と一致しないラベル（UTF-16 系・replacement 系）は「通信では使えない既知の限界」として解決不能と区別できる理由付きで退ける
  - 符号化は表せない文字を 10 進の数値文字参照へ置換し、置換した文字数を返す（置換があったときだけ文字ごとに数え、通常経路に追加コストを掛けない）
  - 復号は宣言された文字コードだけで行い（先頭の BOM で文字コードを切り替えない）、不正な並びは代替文字へ吸収して置換の有無を返す
  - 正規名は文字コードヘッダに書く綴りとして取り出せ、実際に符号化した文字コードと常に一致する
  - どのような文字列・バイト列でも panic せず、失敗は戻り値で表す
  - クレート根に新モジュールの宣言と型の公開を足す（旧い列挙型の再公開の差し替えは 2.1 で行う）
  - 新設する本番ファイルに兄弟テストファイルの接続宣言 1 行を置き、テスト本体はテストファイル側に書く
  - 完了状態: 別名 5 綴りが同じ値へ解決して正規名が一致すること・UTF-16 系と replacement 系が理由付きで退けられること・未知ラベルが別の理由で退けられること・UTF-8 の符号化が入力と同一バイトであること・絵文字を Shift_JIS へ符号化すると数値文字参照と置換数 1 になることを、兄弟テストファイルの決定論テストで固定して緑
  - _Requirements: 1.1, 1.2, 1.4, 1.5, 3.2, 3.4, 7.3, 10.1, 10.2, 10.3_
  - _Boundary: Charset_

- [x] 1.2 セッションの交渉状態と採用規則、ログと重複抑止を用意する
  - 現在の文字コード・強制の有無・警告済みの鍵を持つ交渉状態を新設し、窓も入出力も持たない純粋な値とする（規則の置き場はここ 1 か所）
  - 次の要求に使う文字コードと、応答の復号方針（強制／交渉の 2 値）を取り出せるようにする
  - 応答の文字コードヘッダの扱いを規則表どおりに実装する: 省略は継承、同じ値は無変化かつ無記録、異なる解決可能値は採用＋切替の詳細ログ 1 行、解決不能は採用せず現在の文字コードを継続、強制中はヘッダを復号にも採用にも使わず初回のみ詳細ログ
  - 要求の置換・応答の不正な並びも記録の対象とし、記録なしに後退する経路を持たない
  - 同じ内容の後退は種別＋鍵ごとに警告 1 回・以後は詳細ログへ落とす。ラベルの鍵は前後空白を落とし ASCII 小文字化してから持ち、綴り違いで警告が重複せず集合が際限なく育たない形にする
  - ログは正規名と後退の根拠を構造化フィールドで持ち、既存の公開ログ行の語彙を変えず追加のみとする
  - 交渉状態は後の工程で起動時のクロージャへ渡されるため、スレッド間移送可能な構成に保つ（移送できない値を内部に持たない）
  - 完了状態: 規則表の 7 行が各 1 本の決定論テストで固定され、同じ解決不能ラベルを 3 回受けて警告 1・詳細 2、大文字小文字違いも同じ 1 回と数えることをログ観測で固定して緑（テスト用のログ捕捉は開発用依存にのみ足す）
  - _Requirements: 2.2, 2.3, 4.2, 4.4, 4.5, 4.6, 4.7, 5.1, 7.1, 7.2, 7.4, 9.3, 10.3, 12.5_
  - _Boundary: CharsetNegotiator_

- [ ] 2. 要求の符号化と応答の復号を、選ばれた文字コードで行う
- [x] 2.1 要求を現在の文字コードで符号化して送れるようにする
  - 要求の組立順・既存の正典 URL コメント 9 行・固定ヘッダをそのまま保ち、組み上げた全文を現在の文字コードで符号化して返す形へ変える。戻り値は符号化済みバイト列と置換した文字数の 2 つの事実
  - 文字コードヘッダの値は正規名で書き、要求行の直後の最初のヘッダ行に置く（既存の順序で既に満たしていることを確認する）
  - 文字コードヘッダの書き出し行の直上に正典の URL コメントを 1 行置く
  - 旧い文字コード列挙型を撤去し、クレート根の再公開を新しい型へ差し替える（クレート外の名前解決が壊れないようにする）
  - 型と戻り値の変更に伴う呼出点（通信層の 1 往復結線・SHIORI/4 の直接呼出）を機械的に追随させ、この時点では従来どおり UTF-8 固定で動かす（挙動の変更 0）
  - 完了状態: 通信層とゴースト結線層の両クレートがコンパイルでき、UTF-8 の要求バイト列が本仕様適用前と同一のまま既存テストが期待値を変えずに緑
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 8.4, 8.5, 10.1_
  - _Boundary: shiori3 codec_

- [x] 2.2 応答の文字コードヘッダを読み、方針に従って復号する
  - 復号より先に、行を区切ってヘッダ名の ASCII 一致で文字コードヘッダの生の値を取り出す走査を足す（位置は問わない・値は前後空白を落とす）。走査の一致腕の直上に正典の URL コメントを 1 行置く
  - 復号に使う文字コードを方針から決める（交渉ならヘッダが解決できればそれ・できなければ方針の中の値／強制なら常に方針の中の値）。全体を復号してから既存の行解析へ渡し、ステータス行・ヘッダ名・ステータスコードの解析規則は変えない
  - 解析結果に「応答の文字コードヘッダの生の値」と「復号で置換があったか」の 2 つの事実を足す（採用の判断は交渉状態が行う）
  - 不正なバイト並びでは失敗せず、解析失敗はステータス行の欠落・数値コードなしだけに保つ
  - 呼出点（1 往復結線・SHIORI/4 の直接呼出は強制方針で UTF-8 固定・解析結果の構造体リテラル 9 か所）を機械的に追随させる
  - 既存テストのうち「不正な UTF-8 は解析エラー」の 1 本のみ、入力をステータス行つき（値に不正バイト）へ差し替えて成功＋代替文字＋置換ありを期待する形に更新し、ステータス行を持たない旧入力は期待値を変えないまま別テストとして残す
  - 完了状態: 通信層の既存テストが上記 1 本の更新以外は期待値を変えずに緑で、SHIORI/4 経路の挙動が適用前と同一
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.7, 4.8, 5.4, 8.4, 8.5, 10.2_
  - _Boundary: shiori3 codec_

- [x] 2.3 3 系統の定数バイト列で符号化・復号・採用の連鎖を固定する
  - 同じ 1 文字を含む要求を UTF-8・Shift_JIS・EUC-JP で組み、文字コードヘッダの綴りと参照値のバイト列を符号化器から導かない定数と比較する（ISO-2022-JP を 4 系統目に足してよい）
  - 同じ 3 系統の応答を復号して値が正しく読めること、ヘッダ省略時の継承、解決不能ラベルでの方針側の文字コードによる復号、強制方針でのヘッダ無視を固定する
  - 表せない文字の数値文字参照化と置換数、不正な並びの代替文字吸収と置換ありを固定する
  - 適用前の要求バイト列を逐語の定数として持ち、UTF-8 で組んだ出力と等値であることを固定する
  - 採用が次の要求に現れる連鎖を、交渉状態と符号化の連続呼出で固定する（初期 Shift_JIS → 応答ヘッダ EUC-JP を受ける → 次の要求の文字コードヘッダと参照値のバイト列が EUC-JP になる）
  - 32bit 成果物・実 SHIORI・実機を要さず、純粋な関数の入出力とログ観測だけで成立させる
  - codec の本番ファイルに、この兄弟テストファイルの接続宣言 1 行を足す
  - 完了状態: 符号化を「Shift_JIS ならそれ・他は UTF-8」に退化させた変異と、ラベル解決を 2 値表に退化させた変異の双方で赤になることを実際に確かめてから元に戻し、その較正結果をタスクの記録に残す
  - _Requirements: 9.1, 9.2, 9.3, 9.6, 9.7, 9.8, 12.1_
  - _Boundary: shiori3 codec テスト_

- [ ] 3. descript の宣言から初期値を決め、通信経路へ通す
- [x] 3.1 (P) descript の文字コード 2 キーを転記する
  - ゴーストのマウント情報に「SHIORI の文字コード」「強制する文字コード」の 2 つの生ラベルを足し、解決も整形もせずそのまま持つ（前後空白を落とさない）
  - 解決処理の既存キー取得の直下に 2 行の転記を足し、各行の直上に正典の URL コメントを置く
  - そのファイル自身の文字コードを表すキーを SHIORI 通信の初期値として使わない（使う箇所 0）
  - 触るファイルを 1,000 行未満に保ち、行数番人の例外表を変更しない
  - 完了状態: 2 キーの有無と生の値（末尾空白を含む形）の転記が決定論テストで固定されて緑・既存の構築点の追随でパーサ層のテストが緑
  - _Requirements: 2.5, 8.5, 12.4_
  - _Boundary: ShioriMount / resolve_

- [x] 3.2 初期の文字コードを優先順で決め、起動ログに残す
  - ファイル層と同じ固定写像で既定の文字コードを決める 2 腕の写像を結線層に置く（OS のロケール設定を読む箇所 0）
  - 強制 ＞ 宣言 ＞ 既定の優先順で初期値を決め、交渉状態を組み立てて返す。解決できない宣言は警告ログ 1 行（キー名・ラベル・理由・採用した後退先）を出して次の優先順へ後退し、強制が解決できないときは強制の効力も失う
  - 決定した文字コードの正規名と決定根拠を情報ログ 1 行で記録する
  - 決定は接続クロージャを返す前に同期に行い、接続の成否に依らず記録が残る形にする
  - 結線層の本番ファイルに、この兄弟テストファイルの接続宣言 1 行を足す
  - 完了状態: 優先順・後退・既定の各場合と、強制が通信で使えないラベルのときの警告 1 行＋非強制での後退が、起動ログの観測つき決定論テストで固定されて緑（本番の起動経路から呼ぶのは 3.4 なので、この時点では未使用の警告が出るだけでテストは緑）
  - _Requirements: 1.3, 2.1, 2.4, 2.6, 7.1, 9.4, 10.3_
  - _Boundary: shiori_wiring_
  - _Depends: 1.2, 3.1_

- [x] 3.3 交渉状態を 1 往復の結線と接続の持ち物へ通す（統合）
  - 1 往復の結線に交渉状態の可変借用を持たせ、現在の文字コードで符号化 → 置換の記録 → バイト列をそのまま送出 → 方針で解析 → 応答ヘッダと置換有無の記録 → 既存の結果写しの順に繋ぐ
  - SHIORI 接続の値に交渉状態のフィールドを足し、1 往復ごとに作り捨てられる結線へフィールド別借用で渡す（規則は実行層に書かない）。応答待ちのイベントと片道のイベントの双方が同じ交渉状態を使い、採用結果がイベントをまたいで保たれる
  - 符号化したバイト列は 32bit helper と IPC へそのまま渡し、helper・IPC 側のコードは変更しない
  - 片道イベントは符号化と置換の記録までを行い、応答は従来どおり解析も採用もせずに破棄する
  - 結線の構築点すべてを同時に追随させる（実行層の 2 か所＝応答待ち・片道の各メソッド／通信層の E2E テスト 5 か所）。接続の値の構築点 2 か所も同時に追随させる
  - 境界をまたぐ統合タスクである理由: 結線の引数形の変更と実行層の呼出点は同時に直さないとコンパイルが通らない（型の変更は全呼出点を同時に要求する）
  - 完了状態: 通信層・実行層の両クレートがコンパイルでき（実行に 32bit 成果物を要するテストもコンパイル確認が通る）、既存テストが緑で、接続 1 つにつき交渉状態が 1 つだけ存在し、片道イベントの応答から採用が起きないことがテストで固定されている
  - _Requirements: 3.6, 5.1, 5.3_
  - _Boundary: Shiori3Client, ShioriConnection_
  - _Depends: 1.2, 2.1, 2.2_

- [x] 3.4 起動経路から初期値を接続へ渡す（統合）
  - 実接続の入口に既定の文字コードの引数を足し、接続時に初期値から作った交渉状態を接続の値へ載せる。ゴーストを起動し直すたびに初期値へ戻り、前回セッションの採用結果を引き継がない
  - 実接続の呼び出し点 4 か所すべてを追随させる（本番の起動分岐・結線層内のテスト 2 か所・採取フィクスチャの統合テスト 1 か所。採取側は本番と同じ既定を渡す）
  - SHIORI/4 の直接呼出経路は交渉状態を持たず UTF-8 固定のまま据え置く
  - 宣言を持たない UTF-8 のゴーストでは最初の要求だけ既定の文字コードで送られ、最初の応答で UTF-8 を採用して以後は不変になることを、この結線で成り立つ形にする
  - 完了状態: 実行に 32bit 成果物を要する統合テストを含めてコンパイル確認が通り（結線層と通信層の 2 クレート）、ワークスペースの既存テストが緑
  - _Requirements: 2.7, 5.2, 5.4_
  - _Boundary: shiori_wiring, runtime_
  - _Depends: 3.2, 3.3_

- [ ] 4. surfaces.txt をファイルごとの宣言どおりに読む
- [x] 4.1 (P) 本番の surfaces.txt 読取 2 経路をファイル層の復号に載せる
  - 起動時の読取と配置採寸時の読取の 2 経路を、バイト列で読んでからファイル層と同じ復号（冒頭宣言を優先・未宣言は既定・解決できないラベルは既定へ後退・不正な並びは代替文字と詳細ログ）に載せる
  - 文字コードの宣言はファイルごとに解釈し、他のファイルの宣言を持ち込まない。共通ヘルパは作らない
  - ファイルの不在・読取不能は従来どおり明示エラーとして扱い、エラー型と既存のエラーログを変えない
  - 宣言を読む唯一の定義箇所に、シェル定義ファイル向けの正典 URL コメントを 1 行足す
  - テスト・サンプル専用の読取箇所は変更しない（変更 0）
  - 完了状態: 既存の UTF-8 固定物での起動・配置採寸の経路が適用前と同一の解析結果を産み、当該クレート群の既存テストが緑
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 8.5_
  - _Boundary: surfaces 読取 2 経路, charset/prescan_

- [x] 4.2 (P) surfaces.txt の固定物 4 種が同一の解析結果を産むことを固定する
  - 同内容（要素名に日本語を含む）の固定バイト列を 4 種用意する: UTF-8 宣言つき・Shift_JIS 宣言つき・Shift_JIS 宣言なし・EUC-JP 宣言つき
  - 4 種すべてが復号を通した後に同一の解析結果を産むことを決定論テストで固定する
  - シェル復号モジュールに兄弟テストファイルの接続宣言 1 行を足す
  - 完了状態: 4 種の等値が緑で、既定の文字コードだけを特別扱いする実装では EUC-JP の系統が赤になることを確認済み
  - _Requirements: 9.5_
  - _Boundary: shell/decode テスト_

- [ ] 5. 裁定と着地を正典文書・台帳へ反映する
- [x] 5.1 (P) 正典文書の未決項目を消し込み、裁定を登記する
  - 正典文書の未決項目から「Charset 交渉の具体」を外し、裁定の節へ導く
  - 裁定の節に出典仕様つきの行を足す: 未宣言時の既定の固定写像（OS ロケール不読）・表せない文字の数値文字参照・不正な並びの代替文字吸収（UTF-8 にも適用）・通信で使えないラベルの扱い・片道イベント応答から採用しない・宣言を持たない UTF-8 ゴーストの最初の要求のヘッダ値・非 ASCII ヘッダの後に置かれた文字コードヘッダを拾えない限界・BOM 付き UTF-8 のシェル定義ファイルの既知の差
  - 本仕様の着地で事実と食い違う 32bit helper の説明コメント 1 行の文言だけを追随させる（コードの変更 0）
  - 完了仕様の文書は改訂しない
  - 完了状態: 未決項目の消し込みと 8 行の登記が正典文書に入り、helper 側の差分が説明コメント 1 行だけであることを差分で確認できる
  - _Requirements: 2.7, 8.1, 8.2, 12.2, 12.3_
  - _Boundary: 正典文書, helper 説明コメント_

- [x] 5.2 網羅台帳 5 行を実装済みへ更新し、常設検査を緑にする
  - 要求側・応答側の文字コードヘッダ、descript の 2 キー、シェル定義ファイルの宣言の計 5 行を実装済みへ更新し、引受先と備考を現状（根拠の置き場所とログの event 名）へ書き直す
  - 台帳から作り直す報告 3 種を生成し直す
  - 完了状態: 台帳の常設検査が緑（報告の陳腐化・根拠なき実装済みの両方の検査を含む）で、根拠となる正典 URL コメントが各定義箇所に存在する
  - _Requirements: 8.3_
  - _Boundary: 網羅台帳_
  - _Depends: 2.1, 2.2, 3.1, 4.1_

- [ ] 6. 境界の不変と実機での挙動を確認する
- [x] 6.1 変えない約束を機械で確認する
  - 実行に 32bit 成果物を要するテストのコンパイル確認を 2 クレートで行い、型と引数の変更で壊れていないことを見る
  - ワークスペースのテストを通し、UTF-8 ゴーストの決定論テスト群・固定物・片道イベントの破棄・ステータス扱いが適用前と同一であることを確認する
  - 32bit helper と IPC のコード差分 0、テスト専用読取の差分 0、完了仕様の文書の差分 0、行数番人の例外表の差分 0、触ったファイルがすべて 1,000 行未満であることを差分と行数で確認する（実在するパスで確認してから「差分なし」と記録する）
  - 既存の公開ログ行の語彙が変わらず、本仕様のログが追加のみであることを確認する
  - 完了状態: 上記の各確認について、実行したコマンドと結果（緑・件数・行数）が記録として残っている
  - _Requirements: 9.8, 12.1, 12.2, 12.3, 12.4, 12.5_
  - _Depends: 2.3, 3.4, 4.1, 4.2, 5.2_

- [ ] 6.2 実機で 2 体のゴーストを確認し、記録を残す
  - 開発者が指定する絶対パスの検体（Shift_JIS・32bit の SHIORI を持つ標準テンプレート）を実バイナリで起動し、有界の自動終了とログ検索で確認する。ログ水準の指定は event の target 名で行う
  - 挨拶が文字化けせずバルーンに出ること（目視）、初期の文字コードの情報ログ 1 行が出ること、切替と解決不能の各行が期待どおりの件数であることを確認する
  - UTF-8 のゴーストでも同じ手順を通し、初期が既定・切替がちょうど 1 回・以後 0 回であること、挨拶の表示が適用前と同一であることを確認する
  - 完了状態: 環境・コマンド逐語・検索結果・目視所見・「0 件」が沈黙でないことの裏づけ（同じ target の別の行が記録に出ていること）を欄として持つ記録ファイルが作られ、2 体分の結果が埋まっている
  - _Requirements: 11.1, 11.2, 11.3_
  - _Depends: 6.1_

## Implementation Notes

- 1.1: `Charset` の派生 `Debug` は `Charset(Encoding { UTF-8 })` と印字する。ログは必ず `name()` を使う（`?charset` では正規名にならない）。
- 1.1: `Charset::decode` は BOM を吸収しない（design の明示裁定）。UTF-8 BOM は U+FEFF の 1 文字として残るので、2.2 のヘッダ走査・ステータス行解析はこの前提で書く。
- 1.1: クレート根の再公開は `LabelError` のみ。新型は `crate::charset::Charset` で到達する（根の `pub use shiori3::Charset` の差し替えは 2.1）。
- 1.2: ログの target／event 名／フィールド名は design §Monitoring の表と逐語一致させること（6.2 の実機 grep と後続 spec が同じ綴りを引く）。`warn` と `debug` の出し分けは module-private の `warn_then_debug!` 1 本で行う（`tracing` のマクロはレベルが静的）。
- 1.2: `charset_invalid_bytes_replaced` の `charset` は「採用後の current」＝実際に復号に使った文字コード名。2.2／3.3 は「採用 → 採用後の文字コードで復号 → `note_response`」の順序を保つこと。
- 1.2: `charset_unmappable_replaced` は `id`／`replaced` のみで正規名を持たない（design §Monitoring の表どおり）。要件 7.2 の「正規名を構造化フィールドで」は直前の `charset_initial`／`charset_switched` を辿る形で満たす。契約の改変は 6.2 の grep を巻き込むので統合担当の論点として残す。
- 1.2: `log_capture_kit::capture` は捕捉窓の内側で番兵イベントを発火し、捕捉できなければ panic する。ゆえに「イベント 0 件」の主張は恒真にならない（母数 0 の緑を自分で潰している）。
- 2.1: 完了仕様 `areka-P0-host32-request` の裸の課題番号が `shiori3.rs` に残る（`:138` の区画見出し「タスク 2.2」・`:473` のテスト doc「要件 1.6」）。本 spec のタスク番号と衝突して読み違いを生むので、2.2 がその区画を触るときに spec 名で修飾するか番号を落とすこと。
- 2.1: doc コメントの主張は必ず file:line で裏取りできる依拠先を書く（codec の契約に帰属させた UTF-8 保証が本タスクで偽になり 1 度差し戻した。正しい依拠先は「この呼出点が `Charset::UTF_8` を渡すから」）。
- 2.1: `parse_response` の第 2 引数は `let _ = request_charset;` で消費している（署名は不変）。2.2 が `CharsetPolicy` へ差し替える。
- 2.1: `EncodedRequest.replaced` の消費点は 0（`note_request` の結線は 3.3）。名前付き構造体を値で返すので 3.3 は `.replaced` を束縛するだけで拾える。
- 2.2: **⚠ 統合担当への申し送り** SHIORI/4 の直接呼出経路は交渉状態を持たない（design 追跡表 5.4 行の確定）。ゆえに `decode_had_errors` の消費点が無く、不正なバイト並びの代替文字置換がこの経路だけ無記録で通る。従来は解析エラーだった経路が「無言で化けた文字を返す」に変わる。要件 4.7「記録なしに握り潰さない」・7.1「ログなしの後退 0」に対する設計由来の穴。最終検証（6.1／`kiro-validate-impl`）で扱うこと。
- 2.2: 要件 4.8／10.2 の「警告ログ 1 行」は codec 側には無く `note_response` の所有。4.8 の完全充足は 3.3 の結線後に確認する。
- 2.2: `client.rs` の解析は当面 `CharsetPolicy::Force(Charset::UTF_8)`（挙動 0 のため）。3.3 が `negotiator.policy()` へ差し替える。継ぎ目コメントが 3.3 を名指ししている。
- 2.2: `scan_charset_header` の大小文字無視とヘッダ名側の空白許容にテストが無い。2.3 の兄弟テストで 1 本拾うこと。
- 2.2: 2.2 で足した新規テスト 4 本（解決可／継承／解決不能／強制無視）は 2.3 の予定項目 ⑵⑶⑷⑸ と重なる。2.3 着手時に重複を整理してよい。
- 2.3: **変異較正の記録（実装者とレビュアーの双方が実走・`charset.rs` は sha256 一致で復元済み）**
  - 変異⒜ `Charset::encode` を「Shift_JIS ならそれ・他は UTF-8」へ退化 → 兄弟テスト 3 赤（`request_bytes_and_charset_header_match_the_constants_in_four_families`／`unmappable_char_becomes_a_numeric_character_reference_in_the_request`／`adopted_charset_appears_in_the_next_request`）＋`charset_tests.rs` 1 赤。
  - 変異⒝ `Charset::for_label` を UTF-8／Shift_JIS の 2 値表へ退化 → 兄弟テスト 9 赤（上記 2 本以外すべて）＋`charset_tests.rs` 3 赤。緑のまま残る `utf8_request_equals_the_pre_spec_byte_stream`（`for_label` を通らない）と `invalid_byte_sequence_is_absorbed_and_reported`（主張がラベル解決に非依存）は正当。
  - 変異⒞ レビュアーが独自に追加。`build_request` の `SecurityLevel: local` を `Local` へ 1 文字退化 → `utf8_request_equals_the_pre_spec_byte_stream` が赤。適用前定数が「今日の出力の写し」でないことの証明。
- 2.3: 適用前バイト列の定数は `4db516fe` の `build_request` の組立順から手で書き下したもの（今日の符号化器を 1 度も通していない）。この由来を doc コメントに残すこと——回帰定数の監査証跡そのものなので、たどれない参照先を書かない。
- 2.3: 2.2 が in-file `mod tests` に置いた 4 本（解決可／継承／解決不能／強制無視）は兄弟テストの強い版へ差し替えて削除した。跡地に差し替え先を指すコメント 4 行を残してある。design が「`shiori3.rs` に残す」と名指しした 3 本（`parse_without_status_line_is_parse_error`／`parse_invalid_bytes_are_replaced_not_parse_error`／`parse_charset_header_found_at_non_first_position`）は無改変で現存。
- 3.1: **⚠ 統合担当への申し送り（文書の訂正）** design §Testing Strategy の `resolve_tests.rs` 行が名指しした固定物「`shiori.forceencoding, Shift_JIS ` の空白を含む生の値」と、tasks.md 3.1 の「前後空白を落とさない／末尾空白を含む形」は**実現不能**。`crates/areka-parsers/src/kv/parse.rs` の値挿入行が無条件に前後空白を落とすため（kv 層 R4.4）、行末の空白は転記層に到達し得ない。生値の取り出し口も無い。両文書を「`parse_kv` が返した値と 1 バイト違わないこと」へ直すこと。要件 2.5／8.5 の充足には影響しない。
- 3.1: 代替の固定物は `shiori.encoding,  Shift_JIS ,x  ` → フィールドは `"Shift_JIS ,x"`（値の中の空白とカンマが残る）。転記側に `.trim().to_ascii_lowercase()` を足す変異で 2 本が赤になることをレビュアーが独立に確認済み＝「転記層は整形しない」が検査で守られている。
- 3.1: 要件 2.5 の零（descript の `charset` キーを SHIORI 通信の初期値に使う箇所 0）の根拠。本番の読み手は 2 つだけ——`charset/prescan.rs` の `charset` キー一致腕（消費点は `charset/decode.rs` の 1 つのみ）と、`shiori3.rs` の応答ヘッダ走査（SHIORI の wire であって descript のキーではない）。`placement/config.rs` のヒットは `#[cfg(test)]` の内側。
- 3.1: 挙動でも零を固定した（`descript_charset_key_does_not_feed_shiori_encoding`）。`charset,Shift_JIS` を Shift_JIS のバイト列で書いた descript を読み、`name` が正しく読めること（＝復号が実際に効いている陽性対照）を見たうえで 2 フィールドが `None` であることを主張する。`decode` を「宣言を無視して既定を使う」へ退化させると赤になることを確認済み。
- 3.2: **⚠ 統合担当への申し送り（文書の訂正）** design §shiori_wiring の `default_charset` を「`Ansi → SHIFT_JIS／Utf8 → UTF_8` の 2 腕 `match`」とする記述は**文字どおりには実現不能**。`areka_parsers::charset::DefaultEncoding` は `#[non_exhaustive]` なので、クレート外からの網羅 match は E0004 になる。実装は 2 つの変種を名指ししたうえで型が要求するワイルドカード腕を足した形（覆うのは将来の新しい変種だけ）。design の「失敗経路が無い」という裁定の趣旨は保たれている。
- 3.2: 要件 1.3 の零（OS のロケール設定を読む箇所 0）の根拠は結線層だけでは完結しない。`default_charset` の唯一の入力 `DefaultEncoding` の生成点は本番では `areka` のブート設定と main のリテラル `Ansi` で、零が担保されるのはそこ。ワークスペース全体のロケール API 走査でも生成に関わるヒットは 0（ヒットは DirectWrite のロケール文字列と 32bit helper の `load` 引数の符号化で、いずれも別経路）。6.1 はこの形で記録すること。
- 3.2: この時点では `LOG_TARGET`／`default_charset`／`initial_charset` に未使用の警告が 3 件出る（tasks.md 3.2 が予告した状態）。`#[allow(dead_code)]` で塞がず、3.4 が本番の起動経路から呼んで消すこと。
- 3.2: ログの `fallback` は「後退先を先に決めてから記録する」。退けた宣言を溜めて決定を確定させてから警告を出す構造にしてあり、決定前の推測へ退化させると赤になることを確認済み。
- 3.3: **順序が契約なら検査で押さえること。** 「`note_response` は `map_get_result` より前＝204／400／500 の応答でも文字コードヘッダを採用する」は本タスクの中核契約だが、初稿では**どのテストも見ていなかった**——後半を `status == 200` で門番する退化を入れても通信層 124 本・実行層 286 本がすべて緑のまま通り抜けた。固定物が 200 応答しかなかったのが原因。`note_response` が status を引数に取らないのは構造上の備えであって検査ではない。
- 3.3: 呼出点の実数は E2E **5 か所**（3 ファイル）。tasks.md の「5 か所」が正しく、grep で 4 と数えたのは取りこぼし。
- 3.3: `parse_and_note` の第 2 引数（復号で置換があったか）の結線も無検査だったので、代替文字への置換が記録まで届くことを 1 本足した（`false` に固定する退化で赤になることを確認済み）。
- 3.3: **⚠ 統合担当への申し送り** `parse_response` が解析エラーを返すと `note_response` へ到達しないため、ステータス行も壊れている不正バイト応答は記録が出ない（記録のない後退が 1 経路残る）。2.2 の申し送りと同じ族。最終検証で要件 4.7／7.1 に対して扱うこと。
- 3.3: 片道イベント本体の検査は禁止語（採用経路の関数名）の不在を見る形なので、将来別名の採用入口が増えると素通りする。今日は採用状態を変える手続きが `note_response` の 1 つだけなので零は成立。
- 3.3: E2E の相手方は `crates/shiori-host32-testdll`（200 は `Charset: UTF-8` を名乗り、204／400 はヘッダを持たず継承する）。pilot の同名ファイルは別実行体の自前実装で E2E の相手ではない。
- 3.4: **⚠ 統合担当への申し送り（要件本文の数え違い）** 要件 2.7 の「最初の応答が届くまでは既定の Shift_JIS で要求を送り…2 要求目以降を UTF-8 で送る」と要件 12.1 の除外「最初の要求の `Charset` ヘッダ値」（単数）は、要件 5.3（片道イベントの応答は採用の根拠に用いない）と突き合わせると数が合わない。宣言を持たない UTF-8 のゴーストが既定で送る要求は **2 本**（`OnInitialize` の片道イベント → username 照会の応答待ちイベント）で、採用は 2 本目の応答で起き、UTF-8 になるのは 3 本目から。挙動は正しく要件 5.3 を守っている。要件本文の訂正は実装者の裁量外なので 6.1／6.2 で裁定すること。**6.2 の実機手順の期待値もこれに合わせる**（採用 1 回の前に Shift_JIS の要求が 2 本・emo2 の比較の「最初の要求だけが違う」は「最初の 2 本」）。
- 3.4: **構造検査の針に改行を混ぜない。** 作業ツリーの改行が CRLF になるため、`"…,\n"` を針にすると欠陥が無いのに普通のチェックアウトで赤になる（レビュアーが CRLF 変換の実走で証明）。既存の構造検査も改行を針に含めない形が先例。
- 3.4: 「接続の値がフィールド短縮記法で受け取る」という 2 つ目の主張は外した。意味の変わらない書き換え（`negotiator: negotiator,`）で赤になる一方、決定をクロージャ内側へ移す退化は出現数の主張と同期性のテストが既に捕まえる。欠陥を捕まえず書き方だけを縛る検査は置かない。
- 3.4: 較正の記録（すべて実走）——⑴ 接続リテラルへ暫定値を戻す変異で出現数の主張が赤（`left: 2 right: 1`）、⑵ 決定をクロージャ内側へ移す変異で同期性のテスト 2 本が赤、⑶ `shiori_wiring.rs` を CRLF に変換した状態で緑。
- 3.4: 構造検査の限界。綴り違いの組立（`<CharsetNegotiator>::new`・別名 import 経由・`Default`／`from` 系）は素通りし、将来この本番ファイルに正当な 2 つ目の組立点が増えると誤検知で赤になる。3.3 の禁止語検査と同族。
- 3.4: 採取フィクスチャは既定を **3 つ**持つ（ファイル層 UTF-8／`real_connect` へ渡す Shift_JIS＝本番一致／起動オプションの UTF-8）。3 つ目が無害なのはこのフィクスチャが結線を差し替えているからで、本番の結線へ替えると SHIORI の既定が黙って UTF-8 になる。6.1 の境界不変の記録に残すこと。
- 3.4: **ワークスペースの既存の赤 1 件は本仕様と無関係。** `areka` の起動スモークが落ちるが、本仕様の着手前のコミット（tasks.md 生成時点）に一時ワークツリーを立てて同じテストを走らせても同じく落ちた（1 passed / 1 failed・exit 101）。6.1 はこれを本仕様に帰属させないこと。
- 4.1: **パーサ層の性質テストは本番の配線を守らない。** 4.2 が固定するのは復号＋解析の等値であって、起動経路と配置採寸経路が実際に復号を通ることではない。初稿では 2 経路とも無検査で、`read_to_string` に戻しても全緑のまま通り抜けた。読取点ごとに兄弟テストで 1 本ずつ押さえてある（どちらも本番関数を駆動し、退化させると `stream did not contain valid UTF-8` で赤になることを実走で確認）。
- 4.1: **「安価な檻が作れない」と申し立てる前に、合成検体で足りるかを調べること。** 実 fixture の大きさ（65 ファイル 2.7 MB）を測っただけで先送りにしかけたが、`placement_shared_test_support.rs` の `synth_declared_dpi_ghost` が同じ引数の対を PNG 3 枚で組んでおり、同型で安価に書けた。レビュアーが実際に檻を書いて反証した。
- 4.1: 両方の檻は「本番の読取点が復号を経由すること」までしか主張せず、復号後の文字列そのものは見ない。Shift_JIS を別の 8bit 文字コードとして復号する退化は素通りする（文字コードごとの解析結果の等値は 4.2 のパーサ層の担当）。
- 4.1: **⚠ 統合担当への申し送り（文書の訂正）** design のテスト専用読取の名指しは 2 件だが、実数は **15 件**（本番 2・テスト/サンプル 15 の計 17 か所）。実装は正しく変更 0。要件 6.6 の充足には影響しないが、6.1 の零の記録は実数で書くこと。
- 4.1: **行数の余裕が少ない兄弟テスト 2 本**——`measure_tests.rs` 983 行（残り 17）・`assets_tests.rs` 941 行（残り 59）。次に何かを足す人は上限に当たる。同クレートに分割の先例（`placement_prepare_tests.rs`／`placement_shared_test_support.rs`）があるので、引受先を先に決めておくこと。6.1 の行数記録に残す。
- 4.1: emo2 の surfaces.txt は BOM 無しで `charset,UTF-8` 始まり（実測）。design が登記した「BOM 付き UTF-8 では従来 U+FEFF が 1 行目に残った」差はこの固定物では発火せず、要件 6.3 の同一性は保たれる。
- 4.2: **較正の記録（レビュアーが独自に 5 本実走）**——⑴ 宣言を無視して常に既定で読む退化 → 3 本赤で UTF-8 と EUC-JP の系統を名指し（完了状態の要求どおり）、⑵ 未宣言時を UTF-8 にする退化 → 未宣言 Shift_JIS の系統のみ赤、⑶ ラベル解決を 2 値表に退化 → EUC-JP の系統のみ赤、⑷ 宣言 Shift_JIS を EUC-JP へ誤解決 → 宣言 Shift_JIS の系統のみ赤、⑸ 復号が常に空文字列を返す退化 → 主張 3 本が赤。4 系統それぞれが個別に効いていることが実証された。
- 4.2: 固定物のバイト列は独立 2 経路（別の符号化ライブラリと JIS X 0208 第 4 区からの手計算）で 9 系列すべてを再導出して確認済み。**逐語主義は「符号化器を呼ばない」だけでは足りない——値そのものが正しいことを別経路で確かめること**（誤っていても自己整合していれば全緑になる）。
- 4.2: 相互等値だけでは空同士の等値で緑になり得る。日本語が要素名・領域名・alias キーの 3 か所へ実際に届いていることを 4 系統すべてで別に主張して塞いである。
- 4.2: **⚠ 統合担当への申し送り（文書の食い違い）** design の新規ファイル欄は「固定物 3 種」だが、同じ design の Testing Strategy と tasks.md 4.2 は「4 種」。実装は 4 種（正しい側）に従っている。design の変更行数見込み「+2」に対し実測 +5。いずれも実害なし。
- 4.2: 固定物の改行は LF（クレート内の既存シェルテストの流儀）。実物の CRLF を足しても文字コード次元の検出力は増えない（宣言値の前後空白落としとラベル解決の ASCII 空白剥がしが二重に CR を吸収するため）。行末処理は既存テストが被覆済み。
- 5.1: **⚠ 統合担当への申し送り（要件・設計・実機手順の同時訂正）** 正典文書の登記には**事実（既定の文字コードで出る要求は 2 本）**を書いた。要件 2.7・12.1 と design の該当行が「最初の要求だけ」（単数）と書いているのは誤りで、登記簿は後続 spec が典拠として引くので要件を鏡写しにすると偽を正典化することになる。レビュアーが boot 系列と起動スモークの期待値で独立に数え直して 2 本を確認済み。6.1／6.2 で要件本文・design 本文・実機手順の期待値を同時に訂正すること。
- 5.1: 32bit helper の説明コメントから「下流・本仕様非呼出」の括弧書きも落とした。この括弧書きは本仕様以前から偽（完了仕様の着地でこの関数は実際に呼ばれるようになっていた）。要件 12.2 の「古くなった説明を残さない」に沿う判断。コードの変更は 0 のまま。
- 5.1: 不正な並びの吸収の登記には、**記録の出ない 2 経路を明示して**書いた（ステータス行ごと壊れた応答／SHIORI/4 の直接呼出経路）。普遍的な記録を主張する行にはしない。6.1 でこの 2 経路を扱うこと。
- 5.1: BOM の扱いはファイル層と通信層で逆（ファイル層は吸収・通信層は吸収しない）。別の関数なので矛盾ではなく意図した使い分けで、登記でも書き分けてある。同梱のシェル定義ファイルは BOM 無しなので現状は発火しない。
- 5.2: **担当 5 行の外にも、本仕様の着地が偽にした記述が同じ台帳に 55 行あった。** 内訳——⑴ 6 行が「シェル定義ファイルは宣言を見ないまま UTF-8 決め打ちで読んで読み取りごと失敗しうる」と書き、状態を「三手に割れる／読む経路のある 3 枚」と数えていた（正しくは二手・4 枚）。⑵ 1 行が「SHIORI との受け渡しは UTF-8 固定なので表せない文字が出る場面そのものが起きない」と書いていた。⑶ 48 行が共有する定型文のキー列挙に、本仕様が新たに読むようにした 2 キーが入っていなかった（「だけである」で閉じる文）。いずれも是正済み。
- 5.2: **先送りの根拠は README を実際に読んでから言うこと。** 「台帳の中の記述は統合担当の持ち分」という分担は README に存在しなかった（統合担当に渡っているのは全体報告・ドメインを跨ぐ結び付き・段階の最終決定の 3 つだけ）。引受先の実在検証を怠ると、偽が正典化する。
- 5.2: **常設検査はこの種の陳腐化を構造上見張らない。** 備考本文とステータスの整合を見る検査は無く、55 行が偽のままでも全緑だった（レビュアーが 3 度実走して確認）。台帳は後続 spec が典拠として引くので、備考本文の陳腐化を機械で判定する仕組みは統合担当（`ukadoc-coverage-roadmap`）への申し送り候補。
- 5.2: 優先度の数値は据え置いた（シェル定義ファイルの行は壊れ方の段が変わったが、段の付け直しは README が統合担当の持ち分と定めている）。据え置きの理由は当該行の備考に明記してある。
- 5.2: **⚠ 6.1 への申し送り** 要求側の文字コードヘッダの備考は交渉経路だけを述べ、SHIORI/4 の直接呼出経路がこのヘッダを UTF-8 固定で書くことに触れていない。応答側の行ともう 1 行がその経路を明示したので実害は小さいが、記録に残す。
- 6.1: **⚠⚠ 検証手順そのものの欠陥（引受先が要る）** 既存の赤 1 件があるため `cargo test --workspace` は**打ち切り**になり、結果行が 3 本しか出ない（全 103 ターゲット中）。そのため 4.1 が入れた一時パスの迂回（共通窓口を通さず自前で組んだ）を見張る番人が別クレートに居ながら、**8 本のタスクレビューすべてを素通りした**。6.1 の全数走査で初めて赤 3 件として現れた。早く見つけるには ⑴ `--no-fail-fast` を必ず付ける、⑵ 番人の住むクレートは名指しの 1 テストでなく**全ターゲット**を走らせる。「ワークスペースが緑だった」と言ってよいのは、ターゲット数・失敗件数・その失敗が既存であることの 3 つを示せるときだけ。
- 6.1: 是正は**共通窓口へ寄せた**（例外表へ足す道は採らなかった——番人自身の失敗メッセージが窓口を薦めており、例外にすると迂回が前例になる）。是正後、`assets.rs` の読取を退化させると当該テストが赤になることをレビュアーが実走で再確認済み。
- 6.1: **完了ゲートは終了コードで判定しないこと。** 既存の赤があるので `cargo test --workspace` の終了コードは 101 のまま。判定は「103 ターゲット・失敗 1 件・その 1 件が着手前から赤」で行う。着手前コミットでの再現は確認済み。
- 6.1: 要件 12.1 が名指す固定物のパスは**存在しない**（実在は `crates/pilot/examples/shiori-host-32/fixtures/emo2/`）。実在しないパスへの `git diff` は空で成功するので、「差分なし」はパスの実在を証明してから記録すること。
- 6.1: 検証の記録は `verification/boundary-record.md`。各確認の逐語コマンド・終了コード・件数・行数と、申し送り 10 項目の一覧を持つ。
