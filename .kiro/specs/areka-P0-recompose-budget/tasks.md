# Implementation Plan

> 段階ループ（design.md Flow 1）と 1:1 対応する。第 0 段（観測）と第 1 段（計測資産）は表示コードの挙動を変えない。是正（第 2 段）の着手順は第 1 段の実測が決める（3.2 のゲート）。

- [x] 1. 第0段: 観測基盤の導入（計時と確保計数・表示結果は不変）
- [x] 1.1 段階別計時の記録器とサマリ行の出力を新設
  - 1 回の表示適用について、キャッシュ照会・合成・リサンプル・当たり判定マスク生成・供給面転写・合計の所要を記録する器を表示層に置く
  - 行には段階別の所要に加え、適用対象の識別子・引き当ての可否・run 内で安定な合成キーのハッシュを持たせる（命中率とキャッシュ容量の裁定材料を後段が機械抽出できるようにする）
  - 計測は無条件に実行し、出力の有無のみログ水準のフィルタに委ねる（表示経路がログ設定で分岐しない）
  - 固定文言・全段フィールド常時出現（実行されなかった段は 0）・1 適用 1 行のスキーマで出力する
  - 観測可能な完了状態: 単体テストで、記録器が固定文言と全段フィールドを備えた行を 1 本生成し、実行されなかった段が 0 で出ることを確認できる
  - _Requirements: 1.1, 1.2, 1.5, 7.2_
  - _Boundary: FrameTiming_

- [x] 1.2 バッファ確保の計数シームを新設（席の再利用は第2段）
  - 毎フレーム経路の確保発生点（合成先・リサンプル先・リサンプル作業領域・当たり判定マスク）ごとのカウンタを持つ器を置く
  - 現時点では各発生点が確保のたびに計数する（是正後は同じシームが再利用の可否を計数する）
  - 適用単位の増分と累積の両方を露出し、増分は計時行のフィールドへ同居させる
  - 観測可能な完了状態: 単体テストで、確保が起きたときだけカウンタが増え、増分の取り出しでリセットされることを確認できる
  - _Requirements: 1.3_
  - _Boundary: FrameBudget_

- [x] 1.3 表示適用の毎フレーム経路へ計時点と計数を配線
  - 表示対象の状態に計数器の席を 1 つ持たせ、適用をまたいで存続させる（累積カウンタの所有者を確定する）
  - 各段の境界で所要を記録し、既存の表示成立点ログの直後に計時行を出力する
  - 早期復帰の経路では出力しない（成立点の対のみ）
  - 既存の表示成立点ログ（文言・水準・フィールド）と、面の入れ替えから供給面転写までの帯の分岐・呼出順序・エラー経路は変更しない
  - 観測可能な完了状態: 既存テストが全て通り、実走ログで表示成立行と計時行が同一適用の対として隣接して出る
  - _Requirements: 1.1, 1.3_
  - _Depends: 1.1, 1.2_

- [x] 1.4 計時ログの較正テストを追加
  - 計時行の固定文言・ログ水準・全段フィールドの出現を検証する
  - 既存の表示成立点ログの文言・水準・全フィールドが不変であることを同時に固定する
  - 「出ないこと」の主張は同一の捕捉スコープ内で陽性と陰性を対で観測する
  - 実時間を合否に使わず、純 x64 の常時テストに含める
  - 観測可能な完了状態: 計時行の文言やフィールドを 1 つ壊すと赤になり、既存の表示成立点ログを変えても赤になる
  - _Requirements: 1.4, 1.5, 6.2, 6.3_
  - _Depends: 1.3_

- [x] 2. 第1段: 計測資産の新設（リポジトリ共通の恒久置き場・製品コード非接触）
- [x] 2.1 (P) 有界実走と CPU 時系列採取のランナーを新設
  - 短時間（7 分程度）・長時間（20 分超）の 2 水準と、開発ビルド・リリースビルドの指定で実走を起動する
  - 静寂状態の明示確認がない場合は起動を拒否する（測定条件の保証を機械化する）
  - 実行ログ・CPU 時系列・実行条件（ビルド種別・水準・開始終了時刻・マシン識別）を 1 つの出力先へ収める
  - 起動失敗・カウンタ取得失敗は即エラー終了し、部分成果を残さない
  - 観測可能な完了状態: 1 コマンドで実走が有界終了し、出力先に実行ログと CPU 時系列と実行条件が揃う
  - _Requirements: 2.1, 2.3, 2.6, 2.7_
  - _Boundary: invoke-perf-run_

- [x] 2.2 (P) 集計モードを新設
  - 計時行から段階別 p50/p95・キャッシュ命中率・コマ適用間隔・確保計数を集計し、CPU 時系列から統計を出す
  - 進行境界スキップの件数は既存ログの実文言で数える
  - 必要なログ種の欠落・段フィールドの欠落は部分集計を出さずエラーとして報告する
  - 較正値（間隔の期待値・許容率・測定窓の定義・アイドル CPU 目標・文言一覧）をスクリプト冒頭に明記し、出力先頭にマシン条件を併記する
  - キャッシュ容量の裁定材料として合成キーの異なり数を集計する
  - 観測可能な完了状態: 実走ログと CPU 時系列を渡すと集計レポートが出て、段フィールドを 1 つ削ったログでは部分集計を出さずエラー終了する
  - _Requirements: 2.2, 2.5, 2.6, 4.5, 7.2_
  - _Boundary: judge-perf 集計_
  - _Depends: 1.1, 1.2_

- [x] 2.3 判定モードを追加
  - 合格判定式のうち間隔・進行境界スキップ・確保計数を両ビルドへ、アイドル CPU をリリースビルドのみへ適用する
  - CPU 時系列が頭打ちへ収束するか単調上昇を続けるかを機械判定し、収束判定の方法と閾値を較正値として明記する
  - 観測可能な完了状態: 合格・不合格・判定不能・引数不正が終了コードで区別され、判定根拠が較正値つきで出力される
  - _Requirements: 4.2, 4.3, 5.1, 5.3_
  - _Depends: 2.2_

- [x] 2.4 判定スクリプトの自己較正セットと手順書を用意
  - 合格・不合格・観測ゼロの 3 態を再現する既知ログ断片を用意し、期待どおりの終了コードを逐語再現する自己検査を持たせる
  - 手順書に 2 水準の使い分け（長時間はベースラインと最終判定のみ・中間の是正ループは短時間）・静寂確認の運用・同一手順再測の条件・較正値の所在・未知の機序を記録して独立課題として起票する手順を書く
  - 観測可能な完了状態: 自己検査が 3 態とも期待終了コードを逐語再現し、赤も必ず出る
  - _Requirements: 2.4, 2.6, 5.2_
  - _Depends: 2.1, 2.3_

- [x] 3. 第1段実行: ベースライン採取と支配項の確定
- [x] 3.1 ベースラインを 2 水準で採取
  - 開発者へセッションを渡して静寂状態の確認を得てから実走する
  - 短時間水準を開発ビルドとリリースビルドの両方で、長時間水準をリリースビルドで採取する
  - 観測可能な完了状態: 3 通りの実走の出力（ログ・CPU 時系列・実行条件）が揃い、集計モードが欠落なしで通る
  - _Requirements: 2.1, 2.7_
  - _Depends: 1.3, 2.1, 2.2, 2.4_

- [x] 3.2 是正前の内訳を確定して着手順を裁定
  - 段階別所要・確保計数・命中率・間隔・CPU 時系列から支配項を特定する
  - 設計の仮説（確保と解放の繰り返しが支配的）と実測が食い違う場合は、支配項の是正設計を設計文書へ追補してから着手する
  - 観測可能な完了状態: 支配項の内訳表と着手順の根拠が spec 配下の成果物として残り、仮説と食い違った場合は該当段の是正設計が設計文書へ追補されている
  - _Requirements: 3.4_
  - _Depends: 3.1_

- [x] 4. 第2段: 上流クレートへの追加 API（既存の意味は変えない）
- [x] 4.1 (P) リサンプルの作業領域受け取り形を追加
  - 呼び手が作業領域を所有できる形を追加し、既存のリサンプルはその形へ委譲する（挙動不変）
  - 既存の項目・公開名は一切変更せず追加のみ行う（同一ファイルを並走 spec が編集するため・先着した側の実形へ後着側が追随する）
  - 作業領域の中身は外へ見せず、出力幅に到達した後は容量が成長しない
  - 既存形と追加形が同一入力でバイト等価であること、作業領域が再利用で成長しないことをテストで固定する
  - 観測可能な完了状態: 等価テストと容量非成長テストが緑で、既存のリサンプル利用箇所は無修正のまま通る
  - _Requirements: 3.1, 6.2, 6.3_
  - _Boundary: emo-compose 拡大縮小_
  - _Depends: 3.2_

- [x] 4.2 (P) 当たり判定マスクの再生成と共有供給を追加
  - 既存バッファを再利用して内容を作り直す形を追加する（寸法が縮む方向でも前の内容が残らない）
  - マスク資源の内部表現を共有参照へ変え、共有供給の口を追加する（既存の設定・取得の見た目と挙動は不変）
  - 追加形と既存形が同一入力で等価であることをテストで固定する
  - 観測可能な完了状態: 等価テストが緑で、当たり判定の既存テストが全て無修正で通る
  - _Requirements: 3.1, 6.2, 6.3_
  - _Boundary: wintf 当たり判定マスク_
  - _Depends: 3.2_

- [ ] 5. 第2段: 表示側の再配線（定常アロケーション完全ゼロ）と支配項の是正
- [x] 5.1 合成キャッシュに容量回収を追加し、呼出側を最小追随させる
  - 追い出すエントリを取り出して容量を回収する口を設ける（容量 1・キー完全一致・表示とマスクの原子対という承認済みの意味論は不変）
  - マスクは生成済みのものを引数で受け取る形にし、エントリは共有参照で持つ
  - 呼出側（表示適用経路と既存のキャッシュテスト）を同一タスク内で最小限追随させる（クレートが常に緑のまま進む整合作業）
  - 観測可能な完了状態: 容量回収後の挿入でも承認済みの意味論が保たれ、回収したバッファの容量が引き継がれることがテストで固定され、クレート全体が緑で通る
  - _Requirements: 3.1_
  - _Depends: 4.2_

- [ ] 5.2 既存の席へ再利用バッファを足し、計数を再利用の可否へ切り替え
  - 合成先の常設席・リサンプル作業領域の席・マスクの輪番（2 スロット・空きスロットへ作り直し）を既存の計数器の席へ足す
  - 表示バッファはキャッシュから回収した容量で回す
  - 席が期待どおり再利用できない境界では黙って確保せず、必ず計数に現す
  - 観測可能な完了状態: 初回確保後の再取得で計数増分が 0 になり、寸法変化時は 1 回だけ増えてまた 0 に戻ることが単体テストで固定される
  - _Requirements: 1.3, 3.1, 3.2_
  - _Depends: 4.1, 4.2, 5.1_

- [ ] 5.3 表示適用の毎フレーム経路を再利用形へ再配線
  - 合成をバッファ受け取り形へ切り替え、拡大率が等倍のときは合成先と表示バッファを入れ替えで交代させる（複写しない）
  - 容量回収は合成成功後に限る（失敗時に表示とキャッシュが適用前のまま保たれる）
  - マスクの下流供給を共有参照の受け渡しへ置き換える（面の入れ替え帯の中は 1 文の置換にとどめる）
  - 面の入れ替えから供給面転写までの帯の構造・転写失敗の分岐・既存の表示成立点ログは不変に保つ
  - 観測可能な完了状態: 既存テストが全て通り、短時間実走のログで確保計数が定常状態で 0 になる
  - _Requirements: 3.1, 3.2_
  - _Depends: 5.2_

- [ ] 5.4 リサンプル出力の冗長なゼロ埋めを除去（支配項の是正①・約6%）
  - 出力先を作り直すたびに全画素を 0 で埋めているが、リサンプルは直後に全画素を書き潰すため、この初期化は無駄である
  - 席の使い回しが成立した後にのみ外せる（安全な Rust は新規確保メモリを未初期化のまま使えないため・5.3 が前提）
  - 出力寸法が縮む方向でも前の内容が残らないこと、寸法が伸びた領域に未初期化が漏れないことを固定する
  - 表示バイトと当たり判定マスクを 1 バイトも変えない（6.2 のバイト等価テストが安全網）
  - 観測可能な完了状態: 既存 golden が全て無修正で通り、除去を戻すと所要が増えることが計測で確認できる
  - _Requirements: 3.1, 3.3, 6.4_
  - _Boundary: emo-compose ComposedSurface・design.md 追補 §A2_
  - _Depends: 5.3, 6.2_

- [ ] 5.5 リサンプル計算そのものを是正（支配項の是正②・1コマの約50%）
  - 出力 1 画素あたり 16 回の範囲検査つきバイト読み出しと 24 回の乗算を行っている形を見直す（`scale.rs:455-463`）
  - 先に `[profile.release]` の `opt-level='z'`（サイズ最優先）が計算速度にどれだけ効いているかを確かめる——設定側で説明がつくなら実装を触る前にそちらを裁定材料として出す
  - 公開名の削除・意味変更は行わない（並走 spec との調停・先着後 rebase の登記は不変）
  - 表示バイトと当たり判定マスクを 1 バイトも変えない（6.2 のバイト等価テストが安全網）
  - 観測可能な完了状態: 既存 golden とバイト等価テストが全て緑のまま、リサンプル段の所要が計測で有意に下がる
  - _Requirements: 3.1, 3.3, 6.4_
  - _Boundary: emo-compose scale.rs・design.md 追補 §A3_
  - _Depends: 5.4_

- [ ] 6. 第2段の固定: 決定論テスト
- [ ] 6.1 定常アロケーション 0 のテストを追加
  - 2 パターン交互適用（毎回引き当て外れ）で暖機した後、確保計数の増分 0・合成先と表示バッファの先頭位置不変・マスク輪番の 2 本集合不変を反復で検証する
  - 拡大率が等倍の経路（入れ替えで交代する 100% 表示の一般条件）でも同じ検証を行う（2 本集合の交互を許容する形）
  - 中身が空の実装で偽の合格が出ないよう、不透明画素が存在することを併せて確認する
  - 実時間を合否に使わず、純 x64 の常時テストに含める
  - 観測可能な完了状態: 再利用を 1 箇所でも壊すとテストが赤になる
  - _Requirements: 3.1, 3.2, 6.1, 6.2, 6.3_
  - _Depends: 5.3_

- [ ] 6.2 新旧経路のバイト等価テストを追加
  - 是正前の経路をテスト向けに残し、是正後の経路と同一入力で表示バイトと当たり判定マスクが一致することを固定する
  - 拡大率が等倍・非等倍の両方で行う
  - 既存の表示面往復テストが是正後もそのまま成立することを確認する
  - 実時間を合否に使わず、純 x64 の常時テストに含める
  - 6.1 と同じテスト接続宣言のファイルを編集するため並列実行しない（6.1 の後に行う）
  - 観測可能な完了状態: 是正が絵と当たり判定を 1 バイトも変えないことがテストで固定される
  - _Requirements: 3.3, 6.2, 6.3, 6.4_
  - _Depends: 6.1_

- [ ] 7. 第3-4段: 再測・機械判定・裁定ゲート
- [ ] 7.1 是正後の再測と合否判定
  - 静寂状態の確認を得たうえで、ベースラインと同一手順・同一 fixture で短時間水準の再測を両ビルドで行う
  - 間隔・進行境界スキップ・確保計数の判定を両ビルドへ適用する
  - 不合格なら残る最大内訳を特定して是正へ戻る。ただし残る最大内訳がキャッシュ不命中による再合成コストの場合は裁定ゲートへ回す
  - 観測可能な完了状態: 判定が終了コードで機械的に出て、合否と残る最大内訳が根拠つきで示される
  - _Requirements: 2.7, 4.1, 4.2, 4.3, 4.6_
  - _Depends: 6.1, 6.2_

- [ ] 7.2 長時間水準の最終判定と CPU 時間上昇の切り分け
  - 長時間水準で最終合格判定を行い、リリースビルドのアイドル CPU が目標を下回ることを確認する
  - CPU 時系列が頭打ちへ収束するか単調上昇を続けるかを機械判定し、判定根拠を較正値として残す
  - 収束しない場合は未知の機序として実測データを添えて記録し、本 spec の範囲で是正可能かを判定して範囲外なら独立した課題として起票する
  - 観測可能な完了状態: 長時間走行の判定が合格で、CPU の時間上昇について収束か未知機序かの結論が実測つきで残る
  - _Requirements: 2.7, 4.4, 5.1, 5.2, 5.3_
  - _Depends: 7.1_

- [ ] 7.3 キャッシュ容量の裁定ゲート運用
  - 再測の残余最大項が再合成コストの場合のみ、必要スロット数の実測（合成キーの異なり数）・置換方式・上流の承認済み要件の改訂手続きを添えて開発者へ提示する
  - 裁定が下りない限り容量を変更しない（本実装は容量 1 を前提に構築されており容量変更の経路を持たない）
  - 承認された場合のみ本 spec 内で変更を実施し、上流の要件文書と関連する宣言を追随改訂する
  - 観測可能な完了状態: 容量に触れずに合格した場合はゲート不発として記録が残り、提示に至った場合は実測つきの裁定材料が揃う
  - _Requirements: 7.1, 7.2, 7.3_
  - _Depends: 7.1_

## Implementation Notes

- **1.1 → 1.3 への申し送り（3 件）**:
  1. `timing.rs` の `compose_key_hash` は `PatternFrame.method` を `std::mem::discriminant` で混ぜているため、`Blend(Multiply)` と `Blend(Screen)` が同一ハッシュになる（系統的衝突・要件 7.2 の異なりキー数を過少に見積もる向き）。`method` が合成を駆動しない現状（`areka-emo-compose/src/pattern.rs:38`）ゆえ emo2 ベースラインでは到達しないが、1 行で修復可能（`if let ComposeMethod::Blend(mode) = &frame.method { discriminant(mode).hash(&mut hasher); }`）。1.3 の配線時に併せて入れ、等価テストへ `assert_ne!` 節を追加すること。
  2. ~~`timing.rs` の `AllocCounts` を 1.2 の `BudgetDelta` と統合すること~~ → **1.2 で解消済**（`AllocCounts` を削除し `budget.rs` の `BudgetDelta` に一本化。perf 行スキーマは `efc960b` とバイト同一であることをレビューで検証済み）。
  3. `timing.rs` 冒頭の `#![allow(dead_code)]` は未配線状態専用。1.3 の配線で必ず外すこと（外すと 14 件の dead_code 警告が出る状態＝全項目がテストで踏まれていることは検証済み）。
- **1.2 → 1.3 への申し送り（2 件）**:
  1. `budget.rs` 冒頭にも `#![allow(dead_code)]` がある（未配線専用・非 test ビルドで 7 件の警告、`cargo test --lib --no-run` では 0 件＝全項目がテストで踏まれていることを検証済み）。1.3 の配線コミットで `timing.rs` のものと併せて外すこと。
  2. 計数の唯一のシームは `FrameBudget::note_alloc(AllocSite)`。1.3 は現行の 4 つの確保発生点へこれを置く。5.2 で席メソッドの内部呼出へ移す際、`note_alloc` は `pub(super)` から私有へ落とすこと（`show.rs` から直接叩けると「シーム 1 箇所」が形骸化する）。
- **design.md:278 の誤記**: FrameTiming 節の「module-path target（`areka_emo_present::presenter::show` 配下）」は実現不能（`debug!` は `timing.rs` で展開されるため target は `…::presenter::timing`）。判定スクリプトの契約は固定文言とフィールド名であり target ではないので要件違反はない。1.3 の配線コミットで design.md の当該括弧書きを `…::presenter::timing` へ訂正する。
- **`CaptureSubscriber` がクレート内で 3 重複**（`balloon_test_support.rs:90`／`presenter_refresh_and_log_tests.rs:45`／`presenter/timing_tests.rs`）。各々が別モジュールに私有で到達不能なため境界内では統合不可。別課題（steering `structure.md:176` の共有ヘルパ集約）として扱う。
- **1.3 → 1.4 への申し送り（最重要）**: **現時点で show.rs の配線を固定しているテストは 1 本も無い**。レビューの変異検査で `timing.mark(Stage::Upload)` を削除しても全 160 件が緑のままだった（`PERF_LINE_MESSAGE` を参照するのは `timing_tests.rs` の注入時刻テストのみで、`apply_show` を駆動して行を検査するテストが存在しないため）。したがって 1.4 の檻は「固定文言・水準・全段出現」だけに縮めてはならず、**各段 mark の設置位置・emit が 1 適用 1 回であること・成立点で出て早期復帰（8 経路）では出ないこと**まで固定すること。
- **1.3 → 3.2 への申し送り（段の帯が名前より広い）**: 境界の制約上、3 つの段が名前より広い区間を含む。`t_cache_us` はターゲット照会と k 導出を含む。**`t_mask_us` は `cache.insert` 全体＝マスク生成＋スロット置換＋旧エントリ解放（約 4MB の free）を含む**。`t_upload_us` は初回適用の chain/mount 遅延生成を含み、キャッシュ命中経路では照会以降の全部を含む。3.2 で支配項を選ぶ際、`t_mask_us` が支配的でも「マスク生成だけが重い」と読んではならない（`AlphaMask::from_pbgra32` は `cache.rs:135-140` の内部にあり、1.3 の境界では呼出点でしか計れない）。いずれも 5.3 の再配線で同時に解消される。
- **未解消の残件（`compose_key_hash`）**: `timing.rs:283-292` の `ComposeMethod::Unknown(Box<str>)` は判別子のみで混ぜているため、異なる未知メソッド名同士が同一ハッシュになる（Blend で直したのと同じ系統的衝突・要件 7.2 の異なりキー数を過少に見積もる向き）。`method` が合成を駆動しない現状（`areka-emo-compose/src/pattern.rs:38`）ゆえ材料性は低い。修正は 3 行。`timing.rs:263-265` の doc コメントも Blend 修正後は陳腐化している。次に `compose_key_hash` へ触るタスクで併せて処理すること。
- **design.md の記載漏れ**: `## File Structure Plan` → `### Modified Files` は `budget: FrameBudget` の席の追加先として `presenter/target.rs` しか挙げていないが、`PresentTarget` の構造体リテラル構築点は `presenter/hub.rs:66-68` にあり機械的追随が必須。次に当該節へ触るタスクで補うこと。
- **1.4 の成果と前提（以後の全タスクが依存）**: `presenter_perf_log_tests.rs` が実際の `apply_show` を駆動して固定する内容＝perf 行の固定文言・debug 水準・15 名（スキーマ 14＋message）、既存 info! の文言・info 水準・13 名（12 フィールド＋message）の**完全一致**、5 段の mark 設置位置、1 適用 1 行の emit、成立点で info!/perf が隣接する対で出ること、早期復帰 5 経路（到達可能な全て）で**どちらも出ない**こと、`alloc_*` 4 種が実 `FrameBudget` から埋まること。**フィールドの追加でも赤になる**（design.md の Revalidation Triggers と「info! は不変」契約に一致）。5.x／6.x で両行に触れるとここが最初に鳴る。
- **`t_cache_us` 非 0 主張は `BindSet` の線形等価比較にぶら下がっている**: `Stage::CacheLookup` の mark を固定しているのはこの 1 本だけで、余裕は実測でヒット経路 56〜83µs 対 比較無しの 2〜22µs（レビュー実測・檻の doc は 30〜45 対 1〜15 と控えめに記載＝安全側）。`BindSet` の等価比較が短絡形になると崩れる。**間欠化しても主張を削除してはならない**（削除すると `mark(Stage::CacheLookup)` の削除が緑に戻り 1.4 の目的が消える）——構造的な固定点へ置き換えること。design.md の `### Revalidation Triggers` に 6 件目として登記済み。
- **既知の先行課題（本 spec 由来ではない）**: `presenter_refresh_and_log_tests.rs` の 9 件中 6 件は、否定的主張が同一ファイル内の陽性観測と対になっていない（捕捉ハーネスが嘘をついても緑のまま）。私有複製時代からの性質で 1.4 の統合が持ち込んだものではない。`test-cage-determinism`（W6.9）の観測対象。
- **テスト捕捉ハーネスの所在**: `presenter` stem ファミリでは `presenter_test_support.rs:432/455/466`（`CapturedEvent`／`FieldGrab`／`CaptureSubscriber`）が唯一の定義。別 stem ファミリ（`balloon_test_support.rs`・`presenter/timing_tests.rs`・`scale_tests.rs` ほか）の同型ハーネスは structure.md:176 の適用範囲外として存置。
- **2.1 → 2.2 への申し送り（3 件）**:
  1. CPU 時系列の**刻みを均等と仮定しない**こと。ランナーは絶対時刻へ積み上げるためドリフトしないが、マシンが詰まって刻みを飛ばした場合は印なしで欠測する。行ごとの `timestamp` の差から間隔を出すこと。
  2. レポート先頭のマシン条件に **DPI 構成**を併記すること。`run-meta.txt` に DPI は無いが、`run.log` に `primary_dpi` / `k` / `window_dpi` / `native_w` / `scaled_w` が出る（合成コストは DPI に比例し、ベースラインは 2 モニタ混在 DPI で採取される）。
  3. 出力先には `run.stderr.log` も在る（標準出力と標準エラーは同一ファイルへ流せないため）。必要ログ種の検査でこれを欠落扱いにしないこと。
- **2.1 → 3.1 への申し送り（未検証の 2 経路・最初に踏むのが 3.1）**:
  1. **release 実行体はまだ一度も起動していない**。release のみ GUI サブシステム（`main.rs:1`）になるため、標準出力のリダイレクトが tracing 出力を拾えるかが未確認（debug では 24KB 拾えることを検証済み）。**`cargo build -p areka --release` を先に済ませ、最初の 1 本で run.log が空でないことを必ず確認**すること。拾えない場合もランナーは exit 1 で声を上げる（黙って空の結果を出さない）。
  2. 7 分／25 分の全長経路（watchdog・約 100 点の刻み積み上げ・早期終了判定）は未踏。12 秒の有界起動で向きが正しいことまでは確認済み。
  3. 開発者へ提案できる配管確認（本 spec 側では未実施・デスクトップにマスコットが出るため同意なしに走らせていない）: `AREKA_APP_SMOKE_EXIT_MS` を 45 秒程度に落とした 1 回きりの走行で、run.log に段階別計時ログと smoke ゲートの証跡が出ること・cpu.csv が 3〜4 行になること・exit 0 で終わることを確かめる。
- **2.1 → 2.4（README）への申し送り**: `cpu.csv` の 1 行は約 1 秒幅の瞬時値で 15 秒平均ではない／`shiori_helper_present=False` の走行は発話のない別条件である／`areka_profile_dir` に前回の窓位置が残るため同一手順の再測では同じプロファイル位置を使う／性能カウンタ名が翻訳されている環境ではバナーの較正値を書き換える／**要件 4.4 の較正の出所である SSP 実測 2.2〜2.8% の採取方法が requirements.md にも design.md にも記録されていない**ため、再測時の取り違え防止として採取方法を README へ登記すること。
- **2.2 で確定した実ログの形（後続タスクの前提）**: perf 行は `<ISO8601>Z DEBUG <スパン>: areka_emo_present::presenter::timing: perf(apply_show): 段階別計時 target_id=TargetId(0) surface_id=1000 cache_hit=false t_cache_us=… key_hash=…` の形（実物を採取して確定）。**実 run.log には ANSI 色コードが入る**——`tracing-subscriber` 0.3.23 は出力先が端末かどうかを見ず `NO_COLOR` 未設定なら色を付ける（`fmt_layer.rs:739-755`）ため。judge-perf.py は除去してから解析する（色あり／色なしで集計が逐語一致することを検証済み）。
- **catch-up 文言の二重計上の罠**: `"loop ticker catch-up: skipped multiple boundaries, firing once"` は `"ticker catch-up: skipped multiple boundaries, firing once"` を部分文字列として含む（`areka-ghost/src/ticker.rs:205/225` が短い方・`:307` が長い方）。素朴に「含む行を数える」と loop_ticker が両方に計上される。長い方から先に判定すること。design.md の記載どおりで brief の `(loop)` 形が誤り、という点も逐語確認済み。
- **2.2 → 2.3 への申し送り（最重要）**: **定常状態が空の走行を合格にしてはならない**。`WARMUP_EXCLUDE_SEC` が走行長を上回る等で定常区間が空になると、判定式⑵（catch-up 0 件）と⑶（確保 0 件）が恒真で緑になる。集計モードは全区間の集計＋警告を出して exit 0 で正しいが、**判定モードは定常区間が空なら判定不能（exit 2）にすること**。
- **2.2 → 2.4 への申し送り（4 件）**:
  1. `--selftest` の fixture には **run-meta.txt を必ず同梱**すること（集計は必要ログ種として要求し、無ければ exit 2）。**色コード入りの正常系を必ず 1 本含める**（本物の run.log がその形のため）。
  2. `judge-perf.py` は必要ログ種に `run-meta.txt` を加えており design.md の 3 種記載と食い違う（要件 4.5 のマシン条件を書けない集計を通さないための意図的な強化）。README にこの差を登記すること。
  3. 印字される較正値バナーに**終了コード表と解析用の正規表現が含まれていない**（モジュール docstring 側にある）。2.3/2.4 と README は「バナーが唯一の所在」と読む前提なので、README は「ファイル冒頭（docstring＋バナー）」を指すか、バナーへ終了コード行を足すこと。
  4. `invoke-perf-run.ps1` が `NO_COLOR=1` を置けば raw run.log から色コードが消えて人が grep しやすくなる（judge-perf.py 側の除去は旧ログのため残す）。2.4 の境界を tools/perf/ 全体へ広げてこの 1 行を入れてよい。
- **2.3 は設計欠陥を 1 件掘り当てて是正した（レビュー 3 回差し戻し＋根本原因調査）**: 判定式⑴ が「除外は系列ごと・判定は target ごとにプール」という不整合を持ち、速い系列 3000 本の 5% の裾に 300ms の系列 30 本が丸ごと吸収されて **合格・exit 0** になっていた（3 通りの経路で同じ偽合格）。出所は **design.md の較正値台帳 `FRAME_INTERVAL_WINDOW` 行が design-validation.md の Issue 2 を誤って転記していたこと**——原文の「対象 target 限定」は*判定対象の絞り込み*（＝`FRAME_INTERVAL_JUDGED_SERIES`）の意なのに「対象 target 別 p95」＝*グループ化*として書かれ、同じ文で分離した系列を再併合していた。**判定を系列ごとへ戻し（まとめた p95 は参考表示）、design.md の台帳行・合格判定式⑴行・再検証トリガの 3 箇所を裁定どおりに改訂**。
- **3 回のレビューが見落とした理由（2.4 が必ず引き継ぐこと）**: 既存 77 fixture のどれ 1 つとして「同一 target 配下に判定対象の系列が 2 本ある」形を持っていなかった＝プール経路の**陽性対照が corpus にゼロ**だった。`--selftest` の fixture には赤ケース（`H1b` プール希釈・`H13` 境界超過・`H14` 不合格が判定不能に優先）と**同格で `H11`（健全な複数系列・合格が出ること）を必ず入れる**。素材は scratchpad の `r4/cases/`（生成器 `gen4.py`・非 import の独立パーサ `inspect4.py`・77 fixture ドライバ `matrix4b.py`）に content 検証済みで揃っている。同キー重畳の `P12`/`P13` も入れるとよい。
- **2.3 → 3.1 への申し送り（較正で必ず決めること・5 件）**:
  1. `FRAME_INTERVAL_JUDGED_SERIES` を実測から確定する。**指定は all-or-nothing**——漏れた系列は黙って除外され合格を止めない（`H1b` で速い方だけ指定すると exit 0 に戻る）。判定対象の系列集合を完全に列挙すること。
  2. 明示指定した系列が perf 行を 1 本も出さない場合、指定は黙って未達になる。「指定系列が `steady_perf` に実在すること」の関門を足すこと。
  3. `show` info! だけ出して perf 行を出さない対象は⑴から不可視（`RUST_LOG` の絞り込みミスで片方のゴーストの debug が落ちた走行が合格に見える）。perf 行の異なり `target_id` 数と `run-meta.txt` のゴースト数の突合など、新しい検査が要る。
  4. **系列が短いほど判定が厳しくなる**: n=30 では最近傍順位の p95 が「2 番目に悪い値」になる（28×172ms＋2×210ms で不合格）。`FRAME_INTERVAL_MIN_SAMPLES=30` が偽不合格の下限を決めている。
  5. **偽不合格の帯**: 平均が上限 197.8ms と自動除外の上限 1720ms の間にある散発系列は、emo2 のまばたき定数で裁かれて不合格になる（35 本×1000ms で exit 1）。判定対象の明示指定で外すのが本筋。いずれも合格側へ倒れる誤りではない。
  6. `FRAME_INTERVAL_IDLE_EXCLUDE_MARKERS` は今も空で、⑴ は毎回「較正が未確定」の警告を出す。talk の目印を確定して埋めること。同一 `(target_id, surface_id)` に 2 本のアニメが重なると互いを隠す（窓 B の絞り込みが黙って区間を落とす）ことも、窓の境界確定と併せて処理すること。
- **2.3 → 7.1 への申し送り**: ⑴ は **3.1 が判定対象系列を確定するまで、現実的な複数系列の走行では合格を返せない**（自動選別では char＋balloon の 2 系列走行が必ず判定不能になる。3 経路のうち 2 経路は 2.3 以前から）。7.1 は 3.1 の確定後に走らせること。⑴ の exit 2 は「未計測」であって甘い合格ではない。7.1 自身の走行に現れた系列集合に対して指定が妥当かを毎回再確認すること（ベースラインに無かった面は指定漏れ＝無言になる）。
- **2.4 完了＝第 1 段の資産が揃った**。`tools/perf/` に採取ランナー・判定スクリプト・自己較正 fixture 13 件・手順書。`python tools/perf/judge-perf.py --selftest` が 4 態（合格 5／不合格 3／判定不能 3／引数不正 2）を逐語再現し、**赤が 1 件も走らなければ自己検査が自分自身を落とす**（`SELFTEST_REQUIRED_RED_CODES`・不合格と判定不能の両方を独立に守る）。レビューが 4 通りの変異でいずれも赤を実演。fixture は 52 ファイル・生 1,354,402 バイトだが圧縮後 43,833 バイト（行がほぼ同一のため）。`fixtures/generate.py` でバイト等価に再生成でき、`fixtures/.gitattributes` の `* -text` が `core.autocrlf=true` による書き換えを止めている（有効性を `git check-attr` で確認済み）。
- **ランナーが `NO_COLOR=1` を置くようになった**（raw run.log が人にも grep しやすくなる）。`judge-perf.py` の色除去は**残す**——旧ログと手動起動の走行には色が入るため、色付き fixture がその経路を守っている。
- **3.1 実施（2026-08-14・開発者の静寂確認を得て実走）**: 3 通りとも正常終了・集計に欠落なし。生データはリポジトリ外の `%LOCALAPPDATA%reka-diagudget-base-{short-dev,short-release,long-release}`。実機 emo2＋実 pasta.dll（`shiori_helper_present=True`＝会話込みの本条件）・画面 192 DPI・k=2.0（382x547 → 764x1094）。**3.1 の着手前に i686 helper を各プロファイル隣へ配置し直す必要があった**（`target/debug|release/shiori-host32-helper.exe` は workspace ビルドで x64 に上書きされる既知の罠・PE を確認して i686=0x014C であることを毎回確かめること）。
- **3.2 の結論＝設計の仮説は実測で否定された**。詳細は `baseline-2026-08-14.md`、是正設計の追補は design.md の「実測に基づく是正設計の追補（task 3.2）」節（純加算 128 行・既存の D1〜D10／Flow 2／要件 3.1 は取り消していない）。
  - **支配項＝リサンプルの計算**（1 コマ適用の約 50%）。**確保と解放の churn は約 4%**。決定実験（クレート内の使い捨て計測・実測後削除）で確定し、レビューが独立に再現（計算の取り分 77.8〜85.3%）。複製ループを使わない経路でも同じ結論が出ることを確認済み。
  - **`FRAME_INTERVAL_EXPECTED_MS = 172` は誤り**。emo2 の `animation1400` は待ち 0→150→22 で**172 はサイクル全長**であり、コマ間隔は 150ms と 22ms の 2 種。単一の「指定間隔」は存在しない。判定式⑴の窓は「`seriko: loop 抽選発火` を起点に `a − f ≤ 172ms` かつ (a,b] に次の発火を含まない間隔のみ判定・上限＝最大コマ待ち 150ms×(1+許容率)」で確定（レビューが p95 を独立再導出：release long 164.0ms 合格／release short 178.6ms 不合格／dev short 822.0ms 不合格）。停止ログは終端に使わない（最後のコマの適用より先に出る）。
  - **CPU は単調上昇していない**（末尾窓−中間窓が 3 走行とも負：−4.21／−7.46／−30.79）。brief の 13.4%→21.6% は起動時の立ち上がり。**是正すべきは水準**（release 定常 24.9% 対 目標 3.0%）。
  - **判定式⑵（catch-up 0 件）は 3 走行とも不合格**（dev 78／release short 41／release long 82）。
  - **要件 7.2 の材料**: 定常の異なりキー 227（`TargetId(0)` だけで 216）・キャラ面は定常 1,049 適用で命中 0。LRU 再現の命中率は容量 1/2/3/4/8 で **4.4% / 10.5% / 58.7% / 60.9% / 65.3%**（レビューが独立再現）。
- **着手順（3.2 裁定）**: ⓪要件 7 の容量裁定ゲート（桁を変えうる唯一の手）→①定常アロケーション 0（約 4%・要件 3.1 は効果の大小を条件にしていない・かつ②の前提）→②リサンプル出力の冗長ゼロ埋め除去（約 6%・**①が前提**＝安全な Rust では新規確保メモリを未初期化で使えない）→③リサンプル計算（約 50%・**裁定待ち**）。
- **⚠開発者裁定が要る 2 件（自律ループはここで止まる）**:
  1. **要件 7.1 のキャッシュ容量**（承認済み要件で容量 1・「自律ループの唯一の例外」）。
  2. **支配項の是正が現在の境界の外**——`Out of Boundary` が「`scale.rs` の既存項目の変更——追加は関数＋opaque 型のみ」と定めており、リサンプル計算の是正も冗長ゼロ埋めの除去も既存項目に触れる。design.md 追補 §A4 に 3 択（本 spec へ取り込む／別 spec 起票／据え置き）を対等に提示済み。**裁定が下りるまで `resample`／`ComposedSurface` の既存項目は変更しない**。
  - 参考: `[profile.release]` は `opt-level = 'z'`（サイズ最優先）。計算の絶対値がビルド設定にどれだけ依存するかは未検証で、③へ手を入れる前に確かめる余地がある。
- **開発者裁定（2026-08-14・3.2 の実測を受けて）**:
  1. **支配項の是正を本 spec へ取り込む**——`Out of Boundary` の「`scale.rs` は追加のみ」を改訂し、既存項目への変更を許可した（公開名の削除・意味変更は引き続き禁止）。新規タスク **5.4**（冗長ゼロ埋め除去・約6%）と **5.5**（リサンプル計算・約50%）を追加。`Non-Goals` の「合成アルゴリズム本体（`build_plan`／`blit::execute`）の最適化」は維持——`resample` はそこに含まれていない。
  2. **キャッシュ容量は据え置き（まだ裁定しない）**——要件 4.6 の規定どおり、先に定常アロケーション 0 を入れて再測し、残る最大内訳が再合成コストだと確定してから 7.3 で改めて提示する。実測材料（容量 1/2/3 で命中率 4.4%/10.5%/58.7%）は `baseline-2026-08-14.md` §4 に保存済み。
  - **順序の要点**: 5.4／5.5 は表示バイトを変えてはならないので、**バイト等価テスト（6.2）を先に用意してから着手する**（5.4 の `_Depends:_` に 6.2 を入れた）。したがって実行順は 4.1／4.2 → 5.1 → 5.2 → 5.3 → 6.1 → 6.2 → 5.4 → 5.5 → 7.1 …となる。
- **4.1 の要点（後続が壊しやすい所）**:
  - **容量非成長の檻を守っているのは「より小さい出力幅の呼び出しで容量が縮まないこと」の 1 節だけ**（`scale_resample_tests.rs:629`）。「同一幅の反復で容量が増えない」だけでは、毎回 `Vec::with_capacity(out_w)` する実装を捕まえられない（毎回ちょうど同じ容量になり緑のまま）。レビューが当該節を外すと変異が素通りすることを実測確認済み。**この節を弱めると檻が死ぬ**。
  - 等価檻に**外形ゼロのケースを意図的に入れていない**。既存の `resample_zero_extent_is_empty_and_warns` が同一の `warn!` 呼出位置の捕捉に依存しており、subscriber 無しで先に踏むと捕捉の判定が固定されて既存檻が偽陰性になるため。レビューが当該檻の生存を変異で確認済み。
  - `lib.rs` は無修正。新規 2 名は `areka_emo_compose::scale::{resample_with, ResampleScratch}` のモジュールパスでのみ到達可能（`lib.rs:51` の平坦な再輸出は `ScaleRatio, resample` のまま）。**5.2/5.3 の配線はモジュールパスで参照すること**。
  - **本ブランチは exact（`areka-P0-scale-exact-rational`・PR#110 `9969fee`）を未取り込み**。`origin/main` との `scale.rs` 差分は module 冒頭と `ScaleRatio::as_f32` の doc 計 11 行のみで新規追加箇所と行域が重ならないが、design の再検証トリガに従い **rebase 時に `resample_with` 等価檻を再走させること**。
- **4.2 の要点**:
  - **再利用の檻を守っているのも「より小さい寸法での再生成で容量が変わらないこと」の節だけ**（`alpha_mask_regenerate_tests.rs:222`）。毎回 `vec![0u8; len]` する実装に変異させると**等価檻 7 本は全て緑のまま**で、再利用檻 3 本だけが赤になることをレビューが実測。4.1 と同じ構図。
  - `regenerate_from_pbgra32` の `clear()` は必須。`resize` だけだと縮小時に前の内容が残る（変異で 5 本赤・実測確認）。
  - `from_pbgra32` の本体を私有ヘルパ `pack_pbgra32_alpha` へ集約した（公開面は追加のみ）。**除去した元ループと新ヘルパ本体がインデント正規化後にハッシュ一致**することをレビューが確認＝挙動保存が証明済み。二重実装の乖離を構造で防ぐ意図。
  - `AlphaMaskResource::clone()` は Arc の浅いクローンになったが、`&mut AlphaMask` へのアクセサが存在せず `set`／`set_shared` は `Option` ごと差し替えるため、公開 API からは観測できない。
  - **`Changed<AlphaMaskResource>`／`Added` 依存はリポジトリ全体で 0 件を再確認**（205 箇所の変更検知のうち該当ゼロ・読み手は `hit_test/mod.rs:225` の `world.get` のみ・書き手は `show.rs:274` のみ）。
  - `set_shared` はまだどこからも呼ばれていない（5.3 が `show.rs:275` の 1 文を置換する）。
  - **既存フレークを 1 件観測（本 spec 由来ではない）**: `ecs::window::zorder_pair_maintain::always_on_top_tests::pair_fix_commands_keep_a_pair_inside_the_band_it_already_shares` が 8 回中 1 回失敗。実 HWND を作って `WS_EX_TOPMOST` を測る檻でデスクトップ環境依存。完了済み `areka-P0-ghost-window-zorder` の領域。
- **5.1 → 5.2/5.3 への申し送り（4 件）**:
  1. **`cargo fmt -p areka-emo-present` を素で掛けないこと**。クレート全体の `#[cfg(test)] #[path]` 宣言を並べ替えて無関係な 18 ファイルを汚し、本当の差分が埋もれる（本 spec 由来ではないリポジトリ全体の整形ずれ）。5.1 では全て復元して差分を 5 ファイルに保った。
  2. **5.3 は「合成失敗の後でもキャッシュがヒットし続けること」の明示的な主張を足すこと**。既存の失敗経路の檻（`presenter_display_tests.rs:174`）は表示バイトが変わらないことしか見ておらず、`take_recycled` を失敗経路に置いてスロットを空にしてしまう誤りを**検出できない**（どちらにせよ再転写が起きないため）。Flow 2 の「容量回収は合成成功後に限る」を守る唯一の檻になる。
  3. マスクの生成が `cache.insert` の内側から `show.rs` の呼出点へ出たため、**表示とマスクの原子対の担保が構造から流れの形へ移った**。ただしレビューが「マスクをゼロ埋めバッファから作る」変異を当てて `presenter::fractional_scale_tests::alpha_mask_bits_come_from_k_scaled_display_bytes` が赤になることを確認済み＝presenter 層で振る舞いとして固定されている。5.2 が輪番シームへ移す際も同じ檻が効く。
  4. `cache_tests.rs` が上流の `resample` を使うようになった（縮小方向の書き戻しを本物にするため）。**5.4 で冗長ゼロ埋めを外すときは `take_recycled_carries_over_buffer_capacity` の再検証が要る**。
  - なお `take_recycled` はまだ本番経路から呼ばれておらず、Flow 2 の規律を固定する檻も無い（呼び手が生まれる 5.3 の領分）。
