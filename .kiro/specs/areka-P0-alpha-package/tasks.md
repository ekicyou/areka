# Implementation Plan

> 設計 `design.md`（設計ディスカッション反映後・HEAD `b84128f0`）から生成。`crates/` の変更は 0、ワークスペースの常時のテストに足すテストは 0。判定の部品の較正（既知の入力で合と否の両方を出す）は使い捨てで、結果を各タスクの完了報告に書き、リポジトリにテストとして残さない。

- [ ] 1. `Cargo.lock` の追跡を始める（スクリプトが `--locked` で動くための前提）
- [x] 1.1 `Cargo.lock` を追跡へ入れ、謝辞を同じ変更で作り直す
  - 追跡外の手元の `Cargo.lock` を消し、着手時の main のソースから解決し直す
  - リポジトリの謝辞を完了の手順と同じ生成の形（機種の指定なし）で作り直す。手で直す箇所は 0
  - `.gitignore` の `Cargo.lock` の 1 行だけを外す（他の行は不変）
  - 3 つ（`.gitignore`・`Cargo.lock`・謝辞）を 1 つのコミットに入れる
  - 完了の形: `git ls-files Cargo.lock` が 1 行を返し、`cargo metadata --locked` と `cargo build --locked -p areka` が非 0 で止まらず、その後の `git status --porcelain` が空。謝辞をもう 1 回生成し直しても差分 0。`cargo build --locked` が「lock が古い」で止まったときは原因を確かめ、`.gitattributes` の是正を設計の外の事として報告する
  - _Requirements: 7.3, 1.6_

- [x] 1.2 (P) 完了の手順と開発の手順に `Cargo.lock` の扱いを書く
  - 完了の手順（kiro-complete）の謝辞の再生成の段の「追跡していないので版だけの上下は環境差として戻す」を、追跡後の扱い（謝辞の差分は `Cargo.lock` の差分と対応する・戻さずに原因を確かめる）へ改める。2026-09-26 の実例は履歴として残す
  - 完了の手順の PR を作る前の段に「main を取り込んだあと `cargo metadata --locked` を 1 回通す・非 0 なら `cargo update -w` で揃え謝辞を作り直して最終コミットに含める」を 1 行足す
  - 開発の手順（steering の workflow）の「ブランチ＆マージ戦略」の直後に「`Cargo.lock` の扱い」の節を 1 つ足す: 依存を変えた枝は lock の差分を含める／PR を出す前の確認（出る側）／着地を取り込むときは手元の追跡外の lock を先に消す（入る側）／衝突は main の lock を採って `cargo update -w`・手で行を直さない／配布スクリプトは lock が食い違う枝ではビルドの段で止まる
  - 完了の形: 完了の手順の変更が 2 か所だけ、開発の手順の変更が 1 節だけ（`git diff --stat` で他のファイルが動いていない）。両方の文書に「入る側」「出る側」の手順が読み取れる
  - _Requirements: 7.4, 7.5_
  - _Boundary: 完了と開発の手順の文書_

- [ ] 2. 文書（第三者向け README・根の README）
- [x] 2.1 (P) 第三者向け README の骨子と同梱物の条件を書く
  - 書く前に、設計の「事実の出どころ」の各行（起動の解決の順・メニューの項目の既定名・終了の経路・記憶の置き場・根の形）を着手時の main のソースに対して引き直し、消えた事実は書かない
  - 新しい最上位フォルダ `dist/` に `README.txt` を置く（UTF-8 BOM 付き・日本語・改行は作業コピーのまま・`.gitattributes` は作らない）
  - 見出しは「起動」「終了」「右クリックメニュー」「記憶の置き場」「既知の制限」「`.nar` の入れ方」「同梱物とライセンス」の 7 つをこの順に置き、書けない中身と「`.nar` の入れ方」には目印 `（未記入: alpha-release-signoff が仕上げます）` だけを置く
  - 「既知の制限」に署名なし・Windows 10/11 の 64 ビット専用・深いフォルダに展開しない・α の時点でできないこと（着手時の main で未着地のもの）の 4 点を書く
  - 「同梱物とライセンス」に設計の表の 7 行（areka 本体の MIT は本体だけ・`emo2` の辞書は条件の主張なし・`pasta.dll` は MIT・シェル \0 は作者の条件で抜き出し不可・シェル \1 は `CityPop.txt` の条件と作者のサイト（2026-09-26 実装時に「未確認」から改訂）・`emo2-kakukaku` の素材の出どころと抜き出し不可・`StayseeBalloon` は CC0）を、出どころの文書名か配布元とともに書く。推測で条件を足さない。内輪の言い回しを使わない
  - steering の structure の「その他の最上位」に `dist/`（配布物へそのまま入れる文書）を 1 行足す
  - `alpha-release-signoff` の brief に「City-Pop'n（\1 のシェル）の条件は `CityPop.txt` を出どころとして書いた・この書き方でよいか／作者のサイトでの確認を要するか／出すかの最終判断」を申し送りとして 1 行足す
  - 完了の形: `dist/README.txt` が先頭 3 バイト `EF BB BF` で始まり、7 つの見出しが上の順に在り、「`.nar` の入れ方」の本文が目印だけ。areka 自身のライセンスを「MIT」とだけ述べ「Apache」を含まない。完了報告に 7 欄と同梱物の表の各行の出どころ（定義名または URL）を添える
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.8, 6.4_
  - _Boundary: dist/README.txt, structure.md, alpha-release-signoff brief_

- [x] 2.2 (P) 根の README のライセンス表記と到達点の数を実物に揃える
  - バッジを「MIT」にしてリンク先を実在する `LICENSE-MIT` に、「ライセンス」の節を「MIT（`LICENSE-MIT` 参照）」にする
  - 「現在の到達点」の数を、着手時に `.kiro/specs/completed/` の直下の**フォルダ**だけを数え直した数と数えた日付に直し、「約70%」を消す
  - 「プロジェクト概要」の末尾に `dist/README.txt` への案内を 1 行足す
  - それ以外の古い記述（クレート構成・版の数など）は触らず、気付いたものは完了報告に書く
  - 完了の形: `git diff README.md` の変更がバッジ・ライセンスの節・到達点の数・案内の 1 行の 4 か所だけ。areka 自身のライセンスを述べる箇所（根の README の 2 か所・根の `Cargo.toml`・謝辞の雛形）に「Apache」を含むものが 0（`dist/README.txt` の側は 2.1 が確かめる）
  - _Requirements: 6.1, 6.2, 6.4, 6.5, 6.6, 6.7_
  - _Boundary: README.md_

- [ ] 3. 配布スクリプトで zip を組み、中身を判定する
- [x] 3.1 スクリプトの骨組み（引数・前提の確認・段の直列・後始末・較正値の表・使い方の説明）を作る
  - `tools/package-alpha.ps1` を新規に作る。PowerShell 7 を要求し、カレントがどこでもリポジトリの根へ移る
  - 引数 `-Check`・`-CheckDir`・`-SmokeExitMs` を受け、`-CheckDir` が絶対パスでない・展開先の長さが上限を超える・`-SmokeExitMs` が正の整数でないときは終了コード 3
  - 段「前提の確認」: `git` のコミット（7 桁固定）と未コミットの件数（全文も保持）・`cargo about`／`cargo deny` の有無・`Cargo.lock` の実在。無ければ何を入れるかを 1 行で印字して 3
  - 段を名前つきで直列に回し、非 0 ならその段の名前と外部コマンドの出力の末尾を印字して後始末（`.zip.tmp` の削除・環境変数の復元）をして終了コード 1。外部コマンドの標準エラーで例外を起こさせない。Git Bash から呼ばれたときの `link.exe` の除外を前例から写す
  - 較正値（自動終了のミリ秒・番犬の猶予・展開先の長さの上限・依存 DLL の拒否表・記録の目印・許可する実行ファイルの表・`BUILD-INFO.txt` に書くスクリプトの版）を冒頭の 1 か所にまとめる
  - 使い方の説明に「同じ検体で実機を回している最中に実行すると、その走行の展開した木を消す」を書く
  - 最後の段「git status 不変の確認」: 始めの `git status --porcelain` と違えば、何が変わったかを印字して 1
  - 完了の形: 不正な引数（相対の `-CheckDir`・`-SmokeExitMs 0`）で終了コード 3 が返り、`Get-Help tools/package-alpha.ps1` に「展開した木を消す」の文が出る。わざと失敗させた段で、その段の名前が印字され終了コード 1 になる
  - _Requirements: 1.4, 1.5, 1.6, 1.7_

- [x] 3.2 release ビルド・ライセンス検査・謝辞の生成の段を足す
  - i686 のターゲットを導入してから、x64 の本体と i686 の helper を release・`--locked`・専用の出力先（`target/alpha`）で組む
  - この 2 段の間だけ `RUSTFLAGS` を静的リンクの指定に**置き換え**（継ぎ足さない）、`CARGO_ENCODED_RUSTFLAGS`・`CARGO_BUILD_RUSTFLAGS` を外し、終わったら 3 つとも元に戻す。使った値を後の `BUILD-INFO.txt` のために保持する
  - PE の読み手（機種と取り込み表・PE32 と PE32+ の両方）を関数に切る（3.4 の判定が使い回す）
  - ライセンス検査（`licenses` だけ・`--locked`）と、機種 2 つに絞った謝辞の生成（`--locked`・出力は追跡外）を段にする。出力ファイルが無ければ失敗
  - 完了の形: 走行後に `target/alpha/` の下に x64 の `areka.exe`・i686 の helper・謝辞の生成物が在り、2 本の exe の取り込み表に拒否表の名前が 0（静的リンクが効いたことをこの時点で確かめる。効かなければ先へ進まず報告する）、スクリプトの前後で開発者のシェルの 3 つの環境変数の値が変わらず、`Cargo.lock` とリポジトリの謝辞に差分が 0
  - _Requirements: 1.1, 1.2, 1.6, 2.7, 7.1, 7.2_

- [x] 3.3 検体の展開・組み立て・圧縮の段を足す
  - 既存の展開の窓口（`nar-sample-path`）を `--locked` で `emo2` と `StayseeBalloon` について呼び、出力の `folder=`・`balloon.emo2-kakukaku=` を読む。鍵が無い・パスが実在しなければ失敗
  - 組み立て場所を毎回消して作り、設計の「zip の中身」の表の 9 行（本体・helper・`ghost/emo2/`・`balloon/emo2-kakukaku/`・`balloon/StayseeBalloon/`・`README.txt`・`LICENSE-MIT`・この走行の謝辞・`BUILD-INFO.txt`）だけを写す。helper は i686 の出力先から写す
  - `BUILD-INFO.txt` に `commit=`・`dirty=`・`built=`・`script=`（3.1 の較正値の版）・`rustflags=`（3.2 で保持した値）の 5 行を書く
  - 組み立て場所を最上位を含めない形で `.zip.tmp` に固める。前回の `.zip.tmp` が残っていれば先に消す
  - 完了の形: `.zip.tmp` を開くと最上位に 8 項目（`areka.exe`・helper・`ghost/`・`balloon/`・`README.txt`・`LICENSE-MIT`・`THIRD-PARTY-NOTICES.md`・`BUILD-INFO.txt`）が並び（`stage/` の階層が無い）、`ghost/` の下は `emo2` だけ、`balloon/` の下は `emo2-kakukaku` と `StayseeBalloon` だけ
  - _Requirements: 1.3, 1.4, 2.1, 2.5, 4.6, 5.7_

- [x] 3.4 zip の中身の判定と完成の段を足し、判定の部品を較正する
  - `.zip.tmp` を読み戻し、項目名の区切りを揃えてから設計の判定 1〜8（必須の項目・直下の許可表・`profile/` が 0・実行ファイルと DLL は 3 本だけ・PE の機種・依存 DLL の拒否表・説明書 2 本の `emo2.nar` とのバイト比較・README／ライセンス／謝辞のバイト比較と `BUILD-INFO.txt` の値）を全部行い、否の項目を全部並べてから 1 回否とする
  - 全部合なら最終名へ改名し、絶対パス・コミット・未コミットの件数を印字する。1 つでも否なら `.zip.tmp` を消して終了コード 1
  - 較正の入力の出どころ: 開発機の debug の `areka.exe` と x64 の helper は `cargo build -p areka -p shiori-host32-helper`（普段の `target/debug/`）で用意する
  - 較正（使い捨て・リポジトリに残さない）: 3.2 の PE の読み手が `emo2.nar` の `pasta.dll` で機種 `0x014c`・拒否表に当たらない（合）、開発機の debug の `areka.exe` で `0x8664`・`VCRUNTIME140.dll` に当たる（否）。中身の判定が正しい zip で合、`profile/` を 1 つ足した zip・helper を x64 に差し替えた zip・`readme.txt` を 1 バイト変えた zip でそれぞれ否
  - 完了の形: 較正の 6 通りが期待どおりの合否を出し、正しい走行では `target/alpha/areka-alpha-x64-<日付>-<コミット>[-dirty].zip` が残って `.zip.tmp` が残らない。結果を完了報告に書く
  - _Requirements: 1.4, 1.5, 2.2, 2.3, 2.4, 2.6, 2.7, 4.6, 5.7_

- [ ] 4. 展開した zip の起動確認（`-Check`）
- [x] 4.1 短いパスへの展開・環境変数の組み直し・起動・番犬を足す
  - 展開先は 3.1 の前提の確認で求めて長さを確かめた場所を使い（長さを改めて検査しない）、既定では一時フォルダの下に新しく作る（既に在れば失敗）。記録の置き場は展開先の隣
  - 子へ渡す環境変数: `AREKA_` と `WINTF_` で始まるものを全部外し、有界の自動終了・告知の抑止・`RUST_LOG=info`・`NO_COLOR=1` の 4 つだけ入れ、終わったら元に戻す
  - 展開した `areka.exe` を引数なし・作業フォルダ＝展開先で、標準出力と標準エラーを記録へ流して起こす
  - 番犬: 自動終了＋猶予まで待ち、超えたら自分が起こしたその子だけを止めて否とする。名前で探して止めるコードは書かない
  - 較正（使い捨て・リポジトリに残さない）: 番犬の猶予を一時的に 0 秒にし大きな `-SmokeExitMs` で回して、止めたのが自分が起こした子のプロセス番号だけであること・終了コード 2・番犬で止めた旨の行が印字されること（4.2 でその行を合否の一覧に畳み込む）を確かめ、猶予を元に戻す
  - 完了の形: `-Check` の走行で展開先と `run.log`／`run.stderr.log` が出来て絶対パスが印字され、走行の前後で開発者のシェルの `AREKA_*` の値が変わらない。展開先は消さずに残る。番犬の較正の結果を完了報告に書く
  - _Requirements: 3.1, 3.2, 3.4, 3.6, 3.7_

- [x] 4.2 記録の判定（6 条件）を足し、判定を較正する
  - 2 つの記録を連結し、設計の表の 6 条件（有界で走った・終了コード 0・窓が立った・接続の失敗が無い・会話が始まった・初回のバルーンは同梱）を全部見てから 1 回主張する。会話の目印は文言で見て、204 側の文言は数えない。バルーンは `route=Companion` かつ `dir=` が `\balloon\emo2-kakukaku` で終わること
  - 4.1 の番犬で止めた旨も合否の一覧に畳み込む。合否の一覧と、記録と展開先の絶対パスを合でも否でも印字する。否なら終了コード 2（zip は消さない）
  - 較正（使い捨て）: 正の目印 4 種が在り失敗の目印が無い記録で合、会話の行を 204 側の文言に替えた記録で否、`route=Only` の記録で否
  - 完了の形: 較正の 3 通りが期待どおりの合否を出し、否のときの一覧に否の条件の名前が全部並ぶ。結果を完了報告に書く
  - _Requirements: 3.3, 3.5, 3.6_

- [ ] 5. 実機での通しの確認（release ビルド・開発者の開発機）
- [ ] 5.1 組む・確かめるの通しの走行と較正値の確定
  - `tools/package-alpha.ps1` を素で回し、全段が緑・zip の絶対パスとコミットと件数が出る・`git status` が前後で同じことを確かめる
  - 完成した zip の 2 本の exe の取り込み表に `VCRUNTIME`・`api-ms-win-crt-*` が 0 件であることを実測する（静的リンクの効果の確定）
  - `-Check` を付けて回し、6 条件が合・終了コード 0。`run.log` に正の目印 4 種が在り失敗の目印 3 種が 0 件であること、会話の目印が自動終了より前に出ることを確かめ、短くできるなら自動終了の較正値を下げる
  - 完了の形: 2 回の走行の終了コードが 0 と 0、合否の一覧と zip の絶対パスを完了報告に貼る
  - _Requirements: 1.1, 1.4, 1.6, 2.6, 2.7, 3.1, 3.3, 3.5, 3.7, 7.1_

- [ ] 5.2 否の方向の確認
  - `-SmokeExitMs 300` で回す
  - 完了の形: 終了コード 2 で終わり、**かつ**合否の一覧に「会話が始まった＝否」の行が出る（終了コードだけでは判定しない）。一覧を完了報告に貼る
  - _Requirements: 3.3, 3.6_

## Implementation Notes
- PowerShell から `[Environment]::SetEnvironmentVariable(name, $null)` を呼ぶと `''` が入り変数は消えない。空の `CARGO_ENCODED_RUSTFLAGS` が残ると cargo は `RUSTFLAGS` を黙って無視する（3.2 で静的リンクの確認が捕まえた）。消すときは `Remove-Item env:NAME`（`Set-EnvValue`）。
- kanade は有界の自動終了の後にも「起動グリーティングを再生起動」を記録へ出す。会話の判定は件数だけでは空振りを通すので、`run.log` の最初の「smoke 自動 close: ゴースト窓を despawn しました」より前の行だけを数える（4.2 の差し戻しで判明・設計の表を追記）。
