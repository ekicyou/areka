# Implementation Plan

- [x] 1. Foundation: budouy依存関係の導入とライフサイクル方針の確定
  - budouy 0.2.2（vendored-models feature）を areka-emo-text へ依存追加し、最小コード片で分かち書き結果を一度取得する
  - モデルロード関数の戻り値形（Result の有無）と Parser 型の Sync 可否を実機ビルドで確認する
  - 確認結果に基づき、Parser キャッシュ機構（OnceLock か thread_local のいずれか）を確定する
  - ビルドが通り分かち書き結果が決定論的に得られることを確認できた状態を完了とする
  - _Requirements: 1.2, 8.1_

- [x] 2. Parsers: budoux_newline 拡張キーの転記
  - balloon モデルへ budoux_newline の生文字列を保持するフィールドとアクセサを、writing_mode と同じ型で追加する
  - descript 解析処理へ budoux_newline の転記を1箇所追加し、値の検証や解釈は一切行わない
  - 基層と画像別上書き層の後勝ちマージ経路で budoux_newline が正しく転記されることを確認するテストを追加する
  - 未知キーとして自然に無視される既存ゴーストの挙動が変わらないことを確認するテストを追加する
  - 転記フィールド追加に伴うモデル生成箇所（テストヘルパ含む）の機械的な引数更新を行う
  - 基層/画像別層いずれの budoux_newline も転記後の値がそのまま読み出せることを確認できた状態を完了とする
  - _Requirements: 1.1, 1.5_

- [ ] 3. Core: 折返しモード解決と分かち書き境界計算
- [x] 3.1 (P) 折返しモードの語彙解決
  - budoux_newline の転記値から折返しモードを解決する機能を、writing_mode の語彙解決と同じ姿勢（未知値は警告付きで既定へ縮退）で実装する
  - 1/true を ON、欠落・0・false を OFF（ログなし）として解決する
  - 受理語彙以外の値を警告ログ付きで OFF へフォールバックすることを確認するテストを追加する
  - 全ての入力パターン（ON・OFF・欠落・未知値）で決定論的に同じ結果が得られることを確認できた状態を完了とする
  - _Requirements: 1.2, 1.3, 1.4_
  - _Boundary: WrapMode（wrap.rs）_
  - _Depends: 2_

- [ ] 3.2 (P) 分かち書き境界の計算
  - 明示改行で区切られた各グリフ列ごとに、分かち書きライブラリを用いて塊の境界を計算する機能を実装する
  - 塊境界をグリフ通し番号上の範囲として表現し、全グリフが過不足なく塊に含まれることを保証する
  - 明示改行をまたいで塊が結合されないこと、全文（可視化前の全量）から境界が計算されることを確認するテストを追加する
  - 同一の入力に対して常に同一の境界が得られる（決定論・オフライン）ことを確認できた状態を完了とする
  - _Requirements: 2.1, 2.5, 7.1, 8.1, 8.2_
  - _Boundary: SegmentPlan（segment.rs）_
  - _Depends: 1_

- [ ] 4. Core: レイアウトエンジンのワードラップ拡張
- [ ] 4.1 塊先決による折返し判定の拡張
  - 折返し判定に分かち書き境界を使うモードを追加し、分割点を塊の境界にのみ置く（塊の合計幅が残り行幅に収まるかを塊の先頭で判定する）
  - 収まる場合は塊を分割せずに配置し、収まらない場合は塊の前で行送りしてから塊を配置する
  - 先決済みの塊は残グリフ数カウンタで追跡して塊内では追加判定なしで配置し、plan に含まれないグリフ（不整合時）は既存の文字単位規則で配置されることをテストで固定する
  - 塊先頭配置の判定が可視文字数に依存せず全文の境界計画のみから決まることを確認するテストを追加する
  - 縦書き（vertical_rl/vertical_lr）でも既存の軸読み替えのみで同一の判定式が成立することを確認するテストを追加する
  - 残り行幅にちょうど収まる境界値の入力で行送りの有無が判定式どおりになることを確認できた状態を完了とする
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 6.1, 6.2, 7.1, 8.2, 8.3_
  - _Boundary: layout ワードラップ拡張（layout.rs）_
  - _Depends: 3.2_

- [ ] 4.2 長大塊の文字単位縮退
  - 塊が行頭から始めても1行に収まらない場合に限り、その塊だけ既存の文字単位折返しへ切り替える処理を実装する
  - 縮退中も行頭の1グリフは閾値超過でも配置し、はみ出しや無限ループが起きないことを確認する
  - 縮退した塊の直後に続く塊では通常の塊単位判定が再開することを確認するテストを追加する
  - 極端に長い塊を含む入力でも全グリフが配置され表示が破綻しないことを確認できた状態を完了とする
  - _Requirements: 3.1, 3.2, 3.3, 8.3_
  - _Boundary: layout ワードラップ拡張（layout.rs）_
  - _Depends: 4.1_

- [ ] 4.3 OFF経路の不変性確保
  - 折返しモードが文字単位（既定）のときに新しい引数を追加してもレイアウト結果が変わらないよう、既存コード経路をそのまま温存する形で分岐を追加する
  - 折返しモードを扱う全ての呼出箇所（本番・テスト双方、約53箇所）へ新しい引数を機械的に追加する
  - 既存の折返しテスト一式が引数追加のみで変更なく成立し続けることを確認する
  - 同一の入力に対し文字単位モードの出力が変更前と完全に一致する（非回帰）ことを確認できた状態を完了とする
  - _Requirements: 4.1, 4.2, 4.3, 8.3_
  - _Boundary: layout ワードラップ拡張（layout.rs）_
  - _Depends: 4.2_

- [ ] 4.4 保留改行との整合とリフロー跳び不発生の保証
  - 保留中の改行が実体化した直後の行頭で塊の先決判定が働くよう、既存の改行処理の順序を保ったまま組み込む
  - 可視文字数を段階的に増やしながら描画した結果が、全文を一度に描画した結果の先頭部分と常に一致する（配置済みグリフの行が後から動かない）ことを確認するテストを追加する
  - 明示改行・保留改行の意味論そのものが変化しないことを確認するテストを追加する
  - 逐次表示の全段階で配置済みグリフの行位置が最終レイアウトと一致することを確認できた状態を完了とする
  - _Requirements: 5.1, 5.2, 5.3, 7.2, 7.3_
  - _Boundary: layout ワードラップ拡張（layout.rs）_
  - _Depends: 4.3_

- [ ] 5. Integration: バルーン解決結果への折返しモード配線
  - バルーン解決結果へ折返しモードを追加し、他の解決値（書字方向・領域・フォント）と同じ経路で解決する
  - 折返しモードが ON のときだけ分かち書き境界を計算してレイアウトへ供給し、OFF のときは境界計算そのものを行わない
  - 折返しモードを含む診断ログへ解決結果を追記し、実機での確認に使えるようにする
  - バルーンモデルから折返しモードが正しく解決されレイアウトへ渡ることを確認する結合テストを追加する
  - ON 設定のバルーンでのみ分かち書き境界計算が走り、OFF 設定では一切計算されないことを確認できた状態を完了とする
  - _Requirements: 4.2, 9.1_
  - _Boundary: ResolvedBalloonText 配線（actor.rs）_
  - _Depends: 4.4_

- [ ] 6. Validation: 実機での分かち書きワードラップ確認
- [ ] 6.1 fixture有効化と有界自動終了での動作確認
  - 実機確認用フィクスチャの descript へ折返しモードを有効化する1行を追記する
  - 有界な自動終了付きで実機を起動し、折返しモードが ON として解決されたことをログから確認する
  - 起動から自動終了までが安定して完走し終了コードが正常であることを確認できた状態を完了とする
  - _Requirements: 9.1, 9.3_
  - _Boundary: 実機確認セット_
  - _Depends: 5_

- [ ] 6.2 出力画像の目視確認
  - 分かち書きワードラップが有効なケースと長大塊縮退が発生するケースの出力画像を診断用に書き出す
  - 書き出した画像を目視し、塊が行末で途中分割されていないこと、長大な塊がバルーンからはみ出していないことを確認する
  - 通常ケースと縮退ケースの両方で可読性が向上していることを目視確認できた状態を完了とする
  - _Requirements: 9.2, 9.3_
  - _Boundary: 実機確認セット_
  - _Depends: 6.1_

## Implementation Notes

- **Task 1 spike（budouy 実 API 確定・tasks 3.2/5 へ供給）**:
  - 依存行は `budouy = { version = "0.2.2", default-features = false, features = ["std", "vendored-models"] }`（design.md の素の `features=["vendored-models"]` は `default`＝cli→seahorse を引くため精緻化した。`cargo tree` で seahorse 不在を実証）。
  - ローダは `budouy::model::load_default_japanese_parser() -> budouy::Parser`（**infallible**・Result ではない）。→ design Error Handling の「ロード API が Result を返す場合」分岐は不要。segment.rs はロード失敗を扱わない。
  - `Parser::parse(&self, &str) -> Vec<String>`（**所有 String**・design.md の `Vec<&str>` は誤り）。task 3.2 の glyph-index 写像は owned String チャンクを `chars().count()` で数える形にする。
  - `budouy::Parser: Sync + Send`（コンパイル時 bound で証明済み）→ キャッシュは **`static PARSER: OnceLock<budouy::Parser>`** で確定（design 主案どおり・thread_local フォールバック不要）。
  - spike テストは `crates/areka-emo-text/src/lib.rs` の `#[cfg(test)] mod budouy_spike` に恒久保持（決定論・無損失・>=2 塊分割を "今日はいい天気ですね" でピン）。
