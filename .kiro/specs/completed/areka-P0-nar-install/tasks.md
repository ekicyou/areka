# Implementation Plan

> 段 ①（収束）→ 段 ②（エンジン）→ 段 ③（切替）の 3 段で進む。段の合否は design.md「Migration Strategy」のとおり。

## 段 ①: 窓口へ寄せる（挙動不変）

- [x] 1. 検体の窓口を建て、検体パスを綴る 38＋1 ファイルを窓口経由へ寄せる
- [x] 1.1 テスト専用クレート `sample-ghost-kit` を新設し、登記表と窓口の中間形を持たせる
  - ワークスペースに新しいメンバを加え、`[dependencies]` は `thiserror` だけ、`publish = false` とする
  - 検体 4 つ（`emo2`・`R_POST_and_KOMAINU`・`emo2-kakukaku-offsetdpi`・`emo2-kakukaku-wplimit`）を種別と同時インストールのバルーン名つきで登記する定数表を置き、検体を足す作業が「`.nar` を 1 つ置く」と「1 行足す」の 2 手に収まる形にする
  - 検体名からフォルダを返す入口・同梱バルーン名からフォルダを返す入口・未登録の名前に対して既知の名前の一覧を含む失敗を返す入口を持つ（段 ① では追跡済みの展開形を指す。根を返す入口は段 ③ から）
  - 3 つの読み口が借用を返し、取得した値を保持せずにパスだけ取り出す書き方がコンパイルできないことを兄弟テストで確かめる
  - 完了状態: `cargo test -p sample-ghost-kit` が緑で、未登録名を渡したテストが既知の名前 4 つを含む失敗を受け取る
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
  - _Boundary: sample-ghost-kit 窓口_

- [x] 1.2 常設検査の走査部品にマニフェスト読みを移し、クレート名を引数化する
  - 既存の見張りが私有で持っているマニフェストの行分解と本番依存表の抽出を、共有の走査部品へ移してクレート名を引数に取る形にする
  - 移し元の見張りは呼び出しだけに置き換え、判定と例外表は 1 文字も変えない
  - 完了状態: 移し元の見張りのテスト名と合否が移動前と同一
  - _Requirements: 10.3_
  - _Boundary: log-capture-kit 走査部品_

- [x] 1.3 (P) `areka` の 14 ファイルを窓口経由へ書き換える
  - 単体テスト・`tests/`・`examples/` の私家版の検体パス関数と直書きを窓口の呼び出しに置き換え、同梱バルーンのパスを自分で組んでいる 1 か所も窓口の読み口へ寄せる
  - 起動記録を消す初期化は段 ① では残す（段 ③ で削除する）
  - 行数に余裕の無いファイルは私家版の本体を窓口呼び出しで置き換え、正味の行数を増やさない
  - `[dev-dependencies]` に窓口クレートを 1 行加える
  - 完了状態: `cargo test -p areka` と `cargo test -p areka --bin areka` のテスト名の集合と合否が書き換え前と同一で、差分がパスを得る行に限られる
  - _Requirements: 1.6, 1.7_
  - _Boundary: areka_
  - _Depends: 1.1_

- [x] 1.4 (P) emo 系 4 クレートの 14 ファイルを窓口経由へ書き換える
  - `areka-emo-text`・`areka-emo-atlas`・`areka-emo-compose`・`areka-emo-present` の単体テスト・`tests/`・`examples/` を窓口経由にする
  - 同梱バルーン名でパスを組んでいる箇所は窓口の読み口へ寄せ、名前を継ぎ足す形を残さない
  - 各クレートの `[dev-dependencies]` に窓口クレートを 1 行加える
  - 完了状態: 4 クレートのテスト名の集合と合否が書き換え前と同一
  - _Requirements: 1.6, 1.7_
  - _Boundary: areka-emo-text, areka-emo-atlas, areka-emo-compose, areka-emo-present_
  - _Depends: 1.1_

- [x] 1.5 (P) `areka-parsers`・`areka-seriko`・`areka-ghost`・`pilot` の 11 ファイルを窓口経由へ書き換える
  - 定数で持っている検体フォルダは関数呼び出しへ改める
  - i686 の helper が引数も環境変数も無いときに自分で検体へ落ちる退避経路では、窓口から得た値をプロセスの寿命だけ保持する（関数内の一時値にしない）
  - 各クレートの `[dev-dependencies]` に窓口クレートを 1 行加える
  - 完了状態: 4 クレートのテスト名の集合と合否が書き換え前と同一で、i686 helper が引数なしで起動しても検体を見つけられる
  - _Requirements: 1.6, 1.7_
  - _Boundary: areka-parsers, areka-seriko, areka-ghost, pilot_
  - _Depends: 1.1_

- [x] 1.6 検体パスの綴りを見張る常設検査を置く
  - 旧置き場・展開形の検体フォルダ・展開先の名前空間・同梱バルーン名でパスを組む 4 形の綴りが、窓口クレートの外の実行行に現れないことを判定する（コメント行は除く）
  - 窓口の読み口に名前を引数として渡す書き方と、説明文の中の名前には当たらないことを較正で確かめる
  - 窓口クレートが本番の依存に現れないことを、走査部品へ移したマニフェスト読み（1.2）で判定し、合成マニフェストで赤を作れることを較正する
  - 完了状態: 検査が緑で、既知の綴りを 1 件足すと赤になることと、除外領域に 4 形の実体が在ることが較正テストで示される
  - _Requirements: 1.7, 1.8, 10.3_
  - _Boundary: log-capture-kit 見張り_
  - _Depends: 1.2, 1.3, 1.4, 1.5_

## 段 ②: 展開エンジン

- [x] 2. `areka-nar` を建て、依存の辺と決定論テストの固定入力をそろえる
- [x] 2.1 本番クレート `areka-nar` を新設し、伸長器の依存を登記してライセンス検査を通す
  - ワークスペースの本番メンバとして加え、`publish = false` とする
  - 伸長器を既定機能を切って伸長に要る機能だけ明示する形でクレート側の設定に書き、既存の文字コード層と `key,value` 層を本番の依存に引く
  - 窓口クレートの側にも段 ② で要る辺を足す（`areka-nar` と、書き手が使う伸長器の圧縮側）。窓口クレートは開発専用なので本番の依存グラフには入らない
  - `areka-nar` の `[dev-dependencies]` に窓口クレートを足す（テストが「空の根」と固定入力を借りるため。cargo が許す開発用の循環）
  - ライセンス検査を走らせて緑にする
  - 完了状態: 空のクレートが `cargo build -p areka-nar` で通り、`cargo deny check` が緑で、本番の依存グラフに新しく入る外部クレートが 2 つだけであることを数えた結果が残る
  - _Requirements: 10.1, 10.3_
  - _Boundary: areka-nar Cargo.toml, sample-ghost-kit Cargo.toml_
  - _Depends: 1.1_

- [x] 2.2 閉じた拒否語彙と失敗の形を定める
  - 拒否の理由を 13 の変種で閉じ、製品側がそのまま失敗の理由に写せる短い語と、その全数の一覧を公開する
  - 入出力の失敗は「どの段階で・どこまで確定したか・巻き戻せたか」を持つ形にし、マニフェストの警告 5 種も同じ場所に置く
  - 完了状態: 全数の一覧の長さが 13 で、各変種の短い語が重複しないことをテストが判定する
  - _Requirements: 9.2_
  - _Boundary: areka-nar::error_
  - _Depends: 2.1_

- [x] 2.3 CRC-32 を自前で持ち、較正値で確かめる
  - 表引きの実装を置き、アーカイブの整合性検査と検体の刻印の両方から使える形で公開する
  - 完了状態: 既知の較正値を突き合わせるテストが緑
  - _Requirements: 2.6_
  - _Boundary: areka-nar::crc32_
  - _Depends: 2.1_

- [x] 2.4 決定論テスト用の最小 `.nar` 書き手を窓口クレートに置く
  - 無圧縮と deflate・名前の生バイト・「名前は UTF-8」の印の有無・外部属性・暗号化の印・対応外の圧縮方式を指定して組める形にする
  - CRC・宣言サイズ・データを意図的に壊す口を持ち、受理される無傷の入力と壊した入力の対を作れるようにする
  - フォルダの木を走査して 1 本のアーカイブに畳む入口も持たせる（区切りは `/` 固定・名前は印つきの UTF-8・内容は無変換）。段 ③ の検体の畳み込み（5.1）はこの入口を使う
  - 完了状態: 書き手が組んだ無圧縮と deflate の入力が、後続の読取層で同じバイト列に戻ることを確かめるテストが緑
  - _Requirements: 9.3, 9.4_
  - _Boundary: sample-ghost-kit::nar_writer_
  - _Depends: 2.1, 2.3_

- [x] 2.5 テストが借りる「空の根」を配る作業フォルダの型を窓口クレートに置く
  - ビルド成果物の置き場を環境変数か実行ファイルの祖先から見つけ、見つからなければ理由付きの失敗を返す
  - 本仕様専用の名前空間の下に、生存の札を先に作ってからフォルダを作る型を配り、破棄で木と札の両方を片付ける
  - OS の一時フォルダは使わない
  - 完了状態: 作業フォルダを取って破棄するテストの後に、名前空間の下に木も札も残っていない
  - _Requirements: 7.1, 7.10_
  - _Boundary: sample-ghost-kit::devroot_
  - _Depends: 2.1_

- [x] 3. アーカイブを読み、名前を復号し、`install.txt` を解釈する
- [x] 3.1 zip コンテナの読みと伸長・整合性検査を実装する
  - 末尾から終端記録を探して中央ディレクトリを読み、各エントリの方式・印・CRC・宣言サイズ・外部属性・名前の生バイトを取り出す
  - 無圧縮と deflate を受理し、伸長後の長さと CRC が宣言と食い違えば拒否する
  - 暗号化・対応外の方式・zip64・複数ディスク・範囲外のオフセット・宣言サイズの総和の上限超過を、それぞれ理由付きで拒否する
  - 拡張子が `.nar` でも `.zip` でも同じ手順で読む
  - 完了状態: 無傷の入力が受理され、壊した CRC・壊した長さ・終端記録の欠落・暗号化の印・対応外の方式の各入力がそれぞれ対応する理由で拒否されるテストが緑で、書き込みは 1 バイトも起きない
  - _Requirements: 2.1, 2.5, 2.6, 5.9_
  - _Boundary: areka-nar::container_
  - _Depends: 2.2, 2.3, 2.4_

- [x] 3.2 エントリ名の復号と安全性の判定を実装する
  - 「名前は UTF-8」の印が立っていれば UTF-8、無ければ Shift_JIS として復号し、損失があればエントリ番号と生バイトの 16 進を含む理由で拒否する
  - 絶対パス・ドライブレター・UNC・`..`・NUL・`\` 区切り・Windows で作れない名前（禁止文字・制御文字・末尾のドットや空白・予約名）・大小違いの衝突・シンボリックリンクのエントリを、書き込みの前に全て拒否する
  - 完了状態: 日本語の名前を印あり・印なしの両方で正しく復号し、上記の危険な名前 10 種以上がそれぞれ対応する理由で拒否されるテストが緑
  - _Requirements: 2.2, 2.3, 2.4, 2.7, 4.1, 4.2, 4.3, 4.4, 4.5, 4.6, 4.7_
  - _Boundary: areka-nar::names_
  - _Depends: 3.1_

- [x] 3.3 `install.txt` の解釈を実装する
  - アーカイブ最上位（検証済みの名前の列で階層が 1 つ）の `install.txt` を大小無視で見つけ、無ければ最上位の名前の一覧を含む理由で拒否する（包みフォルダを黙って剥がさない）
  - 文字コードは `charset` 行から決め、無ければ既存層の ANSI 既定で読む。行の分割は既存の `key,value` 層をそのまま使い、その上で全てのキーを ASCII 大小無視で引く
  - 受理する 4 種の種別と、拒否する種別・種別なしを分ける。`name`・`directory` の欠落と空、フォルダ名に区切り・`..`・絶対の形・使えない文字が入る場合を拒否する
  - `accept` は空値を「指定なし」として返し、照合はしない。同時インストールのバルーンは `*.directory` と `*.source.directory`（無ければ同値）から読む
  - バルーン以外の同時インストール指定・バルーンやサプリメントに書かれた同時インストール指定・その他の未知のキーは警告として記録して読み飛ばす
  - `refresh` と除外マスクを読み、サプリメントでは `refresh` を読み飛ばして警告に残す
  - 完了状態: 検体 2 つの実物と同じ綴り（大文字キー・値の前の空白）を含む 20 通り以上の固定入力に対し、受理された値と拒否の理由と警告の列がテストで判定される
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10, 3.11, 3.12, 3.13, 3.14, 3.15, 3.16_
  - _Boundary: areka-nar::manifest_
  - _Depends: 3.2_

- [x] 4. 配置を決め、原子的に展開して結果を返す
- [x] 4.1 配置計画を組む
  - 種別ごとの宛先を決める（ゴーストは根のゴースト格納先、バルーンは根のバルーン格納先、シェルは宛先ゴーストの下、サプリメントは宛先ゴーストへ重ね置き）
  - 同時インストールのバルーンは取り出し元の接頭辞を剥がしてバルーン格納先へ置き、ゴースト側には複製を残さない。取り出し元が空なら理由付きで拒否する
  - 宛先ゴーストが渡されない・実在しない場合を拒否し、サプリメントでは最上位の `install.txt` を重ねない
  - 宛先は検証済みの 1 階層名を継ぎ足す形でしか組まず、外へ出る余地が無いことをテストで数える
  - 完了状態: 4 種それぞれの配置と同梱バルーンつきの配置が期待どおりの相対パスの一覧になり、拒否 3 種が対応する理由で返るテストが緑
  - _Requirements: 4.8, 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7, 5.8, 6.3_
  - _Boundary: areka-nar::plan_
  - _Depends: 3.3_

- [x] 4.2 作業フォルダへの組み上げと既存の宛先の取り込みを実装する
  - 根の下の作業フォルダに配置ごとの木を組み上げ、フォルダのエントリを空フォルダとして作り、フォルダのエントリが無いアーカイブでも親フォルダを作る
  - 既存の宛先は、`refresh` が 1 でなければ丸ごと取り込んでから同名を上書き、1 なら除外マスクに挙がったファイル名（全階層・大小無視）だけを取り込んで組み直す。サプリメントは `refresh` の値によらず常に取り込む
  - 書き出す内容は読取層が持っているバイト列をそのまま置き、改行も文字コードも変換しない
  - 完了状態: 既存なし・上書き・全消去＋マスク残しの 3 通りで、作業フォルダの中身が期待どおりのファイル集合とバイト列になることをテストが判定する（宛先はまだ触らない）
  - _Requirements: 4.9, 5.9, 6.1, 6.2_
  - _Boundary: areka-nar::install（組み上げ）_
  - _Depends: 2.5, 4.1_

- [x] 4.3 入れ替えによる確定・巻き戻し・結果の列を実装する
  - 確定は配置ごとに「宛先を退避 → 作業フォルダを宛先へ → 退避を消す」の順で行い、2 つ目で失敗したら退避を戻す。消せなかった退避は残り物として返す
  - 複数配置のうち後の配置が失敗したら、確定済みを逆順に元へ戻し、戻せなかったものを列挙する
  - 宛先の中のファイルが他のプロセスに開かれていて確定できない場合は、宛先を呼ぶ前のまま保ち、対象パスつきの失敗を返す（解放は試みない）
  - 成功したら、インストールしたフォルダ 1 つにつき 1 要素（種別・名前・絶対パス・宛先ゴースト・新規／上書き／全消去のどれか）の列と、`accept` と読み飛ばしたキーの列を返す
  - 全配置の確定の後に作業フォルダを片付ける
  - 完了状態: 途中失敗で宛先が無傷・宛先が使用中で無傷のまま失敗・2 配置目の失敗で 1 配置目が戻る、の各場合をテストが判定し、成功時は確定後の全ファイルの実パスが宛先の配下にあって元のバイト列と一致する
  - _Requirements: 5.10, 5.11, 6.4, 6.6_
  - _Boundary: areka-nar::install（確定）_
  - _Depends: 4.2_

- [x] 4.4 公開面を結線し、失敗の記録と語彙の全数対応を判定する
  - 読み取りの入口が 1 バイトも書かずに全ての検証を終える形にし、展開の入口と合わせて 2 つの公開関数だけが失敗時に記録を 1 回出す（記録の無い失敗経路を持たない）
  - 13 の拒否変種それぞれに対応する固定入力を 1 本のテストで組み、得られた短い語の集合が全数の一覧と完全に一致することを判定する
  - 本番クレートのソースが伸長器の書き込み側の名前を綴らないことを字面で見張る
  - 新設ファイルが全て 1,000 行未満であることを確かめ、番人の例外表には触れない
  - 完了状態: `cargo test -p areka-nar` が実機・GPU・32bit の補助を要さずに緑で、語彙の突合テストが 13 件そろって一致する
  - _Requirements: 9.1, 9.2, 9.3, 9.6, 10.2, 10.8_
  - _Boundary: areka-nar::lib_
  - _Depends: 4.3_

## 段 ③: 保管形の切替

- [x] 5. 検体を `.nar` に畳み、開発用の根を使い捨てにする
- [x] 5.1 検体 4（5）本を `.nar` に畳み、展開結果が元のツリーと一致することを確かめる
  - 畳み込みは 2.4 の書き手の木を畳む入口を呼ぶ使い捨ての手順（開発用の実行体またはテスト 1 本）で行い、外部の書庫道具は使わない
  - 追跡ファイルだけを配布形の構造で含め、追跡外の永続化フォルダは含めず、改行も文字コードも変換しない
  - 同梱バルーンを持つ検体は、ゴーストのフォルダと同時インストールしたバルーンのフォルダの和が元のツリーと一致することを、畳む前後の写像のもとで突き合わせる
  - `.nar` をバイト保存で追跡する属性を確かめ、中身の全ファイル名に対する無視規則の判定が 0 件であることを、大小違いの名前を較正の 1 件として確かめる
  - 展開形の `StayseeBalloon` がこの時点で在れば同じ手順で畳んで登記し、無ければ登記せず、畳むのは後続の仕様が行う旨を検証報告と当該仕様の brief に書く
  - 完了状態: 検体ごとのファイル数とハッシュが検証報告に残り、4（5）本すべてで一致が確かめられる
  - _Requirements: 8.2, 8.3, 8.4, 8.5, 8.6, 8.7_
  - _Boundary: vendors/sample_ghost_
  - _Depends: 2.4, 4.4_

- [x] 5.2 原本の刻印と初回展開の競合を実装する
  - `.nar` の長さと CRC を刻印にし、刻印を名前に含む読み専用の原本を、作業フォルダで組んでから所定の名前へ移す形で作る（名前が合う原本は完全、が不変条件）
  - 原本が無い・刻印が変わったときだけ展開し直し、刻印の違う古い原本は作業フォルダへ退避してから消す。回収の失敗は記録に出して取得は続行する
  - 多重プロセスの初回展開は、原本の置き場への移動の勝敗で決め、負けた側は自分の作業を捨てて勝者を使う
  - 名前空間を丸ごと消しても、途中で落ちた残骸が在っても、次の取得で作り直す
  - 完了状態: 原本が無い状態からの取得と、刻印を変えた `.nar` での取得が、どちらも完全な原本 1 つに落ち着くことをテストが判定する
  - _Requirements: 7.2, 7.3, 7.6, 7.7, 7.8_
  - _Boundary: sample-ghost-kit::devroot（原本）_
  - _Depends: 2.5, 4.4_

- [x] 5.3 複製の配布と札による保護・掃除を実装する
  - 取得のたびに札を先に開いてから原本の木を複製し、複製は札を開いている間だけ生きて、破棄で木と札の両方が消える
  - 取得のたびに作業フォルダを走査し、札を消せた残骸と札の無い残骸を、退避してから消す。共有違反と不在はいずれも触らない
  - 開発用の根は再インストールの経路を通らず、常に空の根へ展開する
  - OS の一時フォルダは使わない
  - 完了状態: 取得と破棄を繰り返しても名前空間の使用量が増え続けず、生きている複製が他者の掃除で消えないことをテストが判定する
  - _Requirements: 6.5, 7.5, 7.7, 7.9, 7.10_
  - _Boundary: sample-ghost-kit::devroot（複製・掃除）_
  - _Depends: 5.2_

- [x] 5.4 窓口を段 ③ の形へ切り替える
  - 取得が複製を作り、根・検体フォルダ・同梱バルーンのフォルダを根の配下の位置で返すようにする（呼び手の 1 行は段 ① から変えない）
  - 展開結果の要素が登記（種別・フォルダ名・同梱バルーン）と食い違えば理由付きの失敗を返す
  - 完了状態: 段 ① で書き換えた 38＋1 ファイルを 1 行も触らずに、全クレートのテストが緑
  - _Requirements: 1.1, 1.2, 1.3, 7.4_
  - _Boundary: sample-ghost-kit 窓口_
  - _Depends: 5.3_

- [x] 5.5 絶対パスを印字するコマンドを用意する
  - 検体名を受け取り、手動用の根へ展開し直してから、根・検体フォルダ・同梱バルーンごとのフォルダを 1 行 1 組の形で標準出力に出す
  - 未登録の名前は標準エラーに理由を出して 0 以外で終わる
  - 手動用の根は呼ぶたびに作り直し、掃除の対象にしない
  - 完了状態: コマンドを 2 回続けて呼ぶと、2 回とも起動記録の無い根が印字される
  - _Requirements: 1.9_
  - _Boundary: sample-ghost-kit bin_
  - _Depends: 5.4_

- [x] 5.6 展開済みツリーと、その場しのぎの初期化と無視規則を削除する
  - 旧置き場の展開済みツリーと、展開形で追跡していた検体フォルダ（`vendors/sample_ghost/` の下の展開形を含む）を削除する
  - 起動記録を消す初期化を削除し、削除したツリーを指す無視規則のファイルも削除する
  - 旧置き場をコメント行だけで綴っていたファイルの文言を同時に直す
  - 完了状態: 追跡対象の検体が `.nar` だけになり、常設検査（1.6）が緑のまま
  - _Requirements: 8.1, 8.8, 8.9_
  - _Boundary: pilot examples, areka 起動経路, vendors/sample_ghost 展開形_
  - _Depends: 5.4_

- [x] 5.7 開発用の根の性質を決定論テストで判定する
  - 登記の往復（各 `.nar` を空の根へ展開すると要素が登記と一致する）を常設のテストにする
  - 取得した根に起動記録を書いて破棄し、もう一度取得すると起動記録が無いことを判定する
  - 同一プロセスの多スレッドで同じ検体を取得し、一方の書き込みが他方の根に現れないことと、利用中の木が差し替えられないことを判定する
  - 自分自身のテストバイナリを複数同時に起こして、全員が完全な木を得ることと、原本の置き場に半端な木が残らないことを判定する
  - 完了状態: これらが実機・GPU・32bit の補助なしに `cargo test` の一部として緑
  - _Requirements: 7.4, 7.5, 7.6, 7.7, 8.4, 9.5, 9.6_
  - _Boundary: sample-ghost-kit テスト_
  - _Depends: 5.4_

- [x] 6. 依存・文書・台帳を追随させ、実機で一周する
- [x] 6.1 (P) 検体パスを綴るスクリプトと文書を窓口経由の得方へ書き換える
  - 性能計測のスクリプト 3 本が既定の検体の根をコマンド（5.5）の出力行から得る形にする
  - 適合範囲の文書と example の README の記述を新しい得方へ改める
  - 完了状態: 旧置き場の綴りがスクリプト置き場と、網羅の文書置き場を除く文書置き場に 0 件であることを数えた結果が残る
  - _Requirements: 10.5_
  - _Boundary: tools/perf, doc/emo2-conformance-scope.md, pilot example README_
  - _Depends: 5.5, 5.6_

- [x] 6.2 (P) steering の 3 本と謝辞を追随させる
  - 技術の steering に追加した依存を「意図的依存追加＝日付・承認済」の書式で登記する
  - 構成の steering のクレート一覧に新設 2 クレートと窓口の置き場、見張りの節に新しい 1 本を加える
  - ロードマップの実機運転の定石に、検体の絶対パスの得方を 1 行足し、承認待ちの依存として旧候補を綴る 3 か所を承認済みの実体へ直す
  - 謝辞を `cargo update` を伴わずに再生成し、増える項目が追加した依存とその推移的依存だけであることを差分で確かめる
  - 完了状態: 謝辞の差分が 2 項目だけで、steering の 3 本に新しい記述が入っている
  - _Requirements: 10.1, 10.4, 10.9_
  - _Boundary: .kiro/steering, THIRD-PARTY-NOTICES_
  - _Depends: 2.1, 5.6_

- [x] 6.3 (P) 網羅台帳の担当欄を登記し、同じコミットで関連文書を追随させる
  - `install.txt` のキー 11 項目に本仕様を担当として登記し、対象外の 4 見出しは担当を空のまま備考に理由を書く
  - ロードマップ草案の表に本仕様の行（担当件数・束・ウェーブ）を足し、本仕様を「表に無い」「意図しない重なり」と記す散文と brief の件数も同じコミットで直す
  - 網羅のブリーフィング文書が綴る旧置き場の 3 か所も同じコミットで新しい得方へ直す（この文書の手書きの数は台帳と突き合わされるため、台帳と分けない）
  - ドメイン別報告と全体報告を作り直す
  - 完了状態: 整合検査（`cargo test -p ukadoc-survey`）が緑で、網羅の文書置き場に旧置き場の綴りが 0 件であることを数えた結果が残る
  - _Requirements: 10.5, 10.6, 10.7_
  - _Boundary: doc/ukadoc-coverage_
  - _Depends: 5.6_

- [x] 6.4 (P) 検体を畳む手順を次の人が見つかる場所に書く
  - 追跡ファイルだけを含める・永続化フォルダを含めない・改行も文字コードも変換しない・配布形の構造にする・畳んだ後に一致を確かめる、の 5 点と、5.1 で使った畳み込みの呼び方を保管場所の README に置く
  - 完了状態: 保管場所に README が在り、5.1 で実際に踏んだ手順と食い違わない
  - _Requirements: 10.10_
  - _Boundary: vendors/sample_ghost README_
  - _Depends: 5.1, 5.6_

- [x] 6.5 実機で一周し、作業木が汚れないことを確かめる
  - コマンド（5.5）で絶対パスを取り、有界の自動終了で 1 周し、ログで初回起動のイベントを確かめ、もう一度取得して同じことをもう 1 周行う
  - 一周の前に 32bit の補助実行ファイルを所定の場所へ置く（既存の定石）
  - ワークスペース全体のテストと実機一周の後に、追跡外のファイルが 1 つも増えていないことを確かめる
  - 完了状態: 2 周とも初回起動のイベントがログに出た記録と `git status` が清潔である記録が検証報告に残る
  - _Requirements: 7.11, 9.7_
  - _Boundary: 実機サインオフ_
  - _Depends: 5.7, 6.1, 6.2, 6.3, 6.4_

## Implementation Notes

- 1.1: `SampleError` は `#[non_exhaustive]`。1.3〜1.5 の呼び手が `match` するなら包括の腕が要る。
- 1.1: `Sample` に段 ①だけの欄 `checked_in_parent`（追跡済み展開形の親）を置いた。段 ③（5.4/5.6）で削除する。
- 1.1: 借用のみを返す証明は rustdoc の `compile_fail` doctest（`trybuild` は依存許可に無い）。`compile_fail` はどんなコンパイル失敗でも緑になるので、較正では「`compile_fail` を外すと E0716 が出る」ことまで確かめること。
- 1.2: `workspace_scan/mod.rs` の較正の定位置は `tests/workspace_scan_test.rs`（module doc が名指し）。移し元の見張りに較正を足すとテスト名が増えて「移動前と同一」を自分で破る。
- 1.3: 読み口は借用を返すので、呼び手は `SampleRoot` を**プロセス寿命の `static LazyLock<SampleRoot>`**（共有のテスト支援モジュール 1 か所）で保持し、私家版の助け手はそこから `to_path_buf()` する。一時値から複製すると段 ③ で木が消える。ファイルごとに静的を複製しない（段 ③ の複写回数がそのまま増える）。
- 1.3: 段 ③（5.6）への申し送り＝`spine.rs` は段 ③ で 1 つの根を 2 つの起動地点で共有するので、`remove_dir_all` を消すと 2 度目の起動が 1 度目の `ghost/master/profile/areka/` を見て `OnFirstBoot` の台本が崩れる。5.6 の境界（areka 起動経路）で解くこと。
- 1.3: 実着地は design の「`crates/areka`（13）＋setup.rs＝14」に対し変更 15＋新設 1（`emo2_boot/sample_test_support.rs`）。5.4 の「38＋1 ファイル」の数え直しが要る。
- 1.4: 1.6 の走査語 ⑷（同梱バルーン名でパスを組む形）は、窓口の読み口の**引数全般**（`balloon(...)` だけでなく `acquire(...)` も）を除外すること。`shipped_fixture_region_test.rs` に `acquire("emo2-kakukaku-wplimit")` が在る。
- 1.4: `src/` の本番マウント配下（`wic_arm.rs`・`world.rs`・`balloon_test_support.rs`）へ窓口を引くときは、参照が `#[cfg(test)]` の内側に在ることを**非テストビルド**（`cargo build -p ...`）で確かめる。`cargo test` だけでは通ってしまう。
- 1.4: `areka-emo-text` の `lib.rs` には `PURE_SOURCES` / `SOURCES_OUTSIDE_THE_PURE_SCAN` の全数一致の見張りが在る。`src/*.rs` を新設すると一覧の更新が要る（既存の支援ファイルを使えば不要）。
- 1.5: `pilot/examples/shiori-host-32/shiori3.rs:77` の逆スラッシュ綴り（`crates\pilot\...\fixtures\emo2\ghost\master`）は据え置いた。`build_onboot` が引数を捨てる合成のリテラルで、1.6 の走査語（スラッシュ形）にも掛からない。**5.6 で明らかに合成と分かる値へ置き換えること**（段 ③ 後は実在しない木を名指す行として残る）。
- 1.5: 窓口の `workspace_root()` は正規化していない（`…/crates/sample-ghost-kit/../..`）。実走の出力で区切り記号が混ざる。今日は無害だが正規化パス比較で躓く。担当は 1.1 のファイル。
- 1.6: `workspace_scan` の `scan_tokens` は**すべての**走査語を左端に錨付けしていたため、区切り記号で始まる語（設計の ⑷ の `/<バルーン名>"`）は決して当たらない死語だった。錨は識別子文字で始まる語だけに効かせるよう是正。姉妹の見張りの走査語 7 本はすべて識別子文字始まりなので影響なし（較正は `workspace_scan_test.rs`）。
- 1.6: 除外領域（`crates/sample-ghost-kit/src/`）に在るのは段 ① では ⑴ だけ。`EXCLUSION_FORMS` が 4 形それぞれに状態（`Present` / `ArrivesIn(タスク)` / `ComposedAtRuntime(理由)`）を宣言し、**両方向**で赤になる（消えたら赤・現れたら宣言の書き換えを要求して赤）。段 ③ で `checked_in_parent` を消すと ⑴ の `Present` が赤になるので、5.4/5.6 は宣言も直すこと。
- 1.6: `workspace_manifests(skip_crate)` の `skip_crate` 引数には直接の較正が無い（HEAD の固定値のときも無かった）。飛ばす対象は自分自身のマニフェストだけで違反を生み得ないため実害は無いが、次に触る人は較正を 1 本足すこと。
- 2.1: **本番の依存グラフの増分＝外部クレート ちょうど 2 件（`miniz_oxide 0.9.1`・`adler2 2.0.1`）**。areka が組む 3 つの的（`x86_64`／`i686`／`aarch64-pc-windows-msvc`）のいずれでも同じ。設計「### Technology Stack」と research 9.4 の「2」は正しい。→ **6.2 は謝辞に増える項目をちょうど 2 件と期待し、tech.md の登記も 2 と書く**。
- 2.1: 依存グラフを数えるときに **`--target all` を使わない**。areka が組まない Linux／macOS が混ざり、`backtrace 0.3.76` が `miniz_oxide` を `cfg(not(all(windows, target_env = "msvc", ...)))` で引くせいで「既に本番に居る」ように見えて結論が反転する。数は必ず的の名前と一緒に記録する。
- 2.1: 数えの正典形＝`cargo tree -e normal --target <的> --workspace --exclude sample-ghost-kit --exclude log-capture-kit --exclude temp-path-kit [--exclude areka-nar] --prefix none --format "{p}" | sed 's/ (\*)$//' | grep . | sort -u`。`grep .` が要る（`cargo tree` は根ごとに空行を挟み、`sort -u | wc -l` がそれを 1 件と数える）。記録するのは**増分と差分集合**であって総数ではない（総数は他所の依存が増えた瞬間に古びる）。
- 2.1: `cargo deny` の重複警告は 8 件（既存 7＋`miniz_oxide`）。`deny.toml` に `[graph] targets` が無く的を跨いだ lockfile 全体を見るため。出荷バイナリに `miniz_oxide` が 2 版同居することは無い。
- 2.1: `Cargo.toml` の編集を許す `_Boundary:_` は 2.1 だけ（30 行中 1 件）。よって設計の許可依存 5 本をここで全部登記した。**5.5 は `[[bin]]` 節を書けないが、cargo が `src/bin/*.rs` を自動で見つけるので止まらない。**
- 2.2: **4.1 / 4.3 への指示**＝`ElementKind`・`ExistingState`・`InstalledElement` を `plan.rs`／`install.rs` で再宣言しないこと。`use crate::error::{...}` で既存の定義を使う（同名の別型を作ると `NarError::Io.committed` に入らない）。設計は `#### plan`／`#### install` に置いているが、設計自身の `Io.committed: Vec<InstalledElement>` が `error` をそれらへ依存させるので非循環の置き場が無く、`error.rs` に置くのが現時点で唯一の形。`install.rs` へ移すのは任意（移すなら `lib.rs` の `pub use` も同じコミットで）。
- 2.2: **4.4 への指示**＝`NarError::Io` の Display は設計の逐語で `committed`／`rolled_back` を落とすので、唯一の `error!` 呼び出しに `committed = committed.len()` と `rolled_back` を構造化フィールドとして足すこと（要件 6.4 が見えるよう求めている値）。また `NarError` は意図的に `PartialEq` でないので、13 変種のテストは `NarError::Refused { reason, .. }` を分解して `reason.kind()` を集めること。
- 2.2: `thiserror` は `Path`／`PathBuf` の欄を自動で `.display()` 経由にする（`PathBuf: !Display` は真だが結論に届かない）。`Vec<String>` には及ばないので `{:?}` が要る。
- 2.2: メッセージの照合に `contains` を使うと Debug 引用符（`{x:?}` への後退）を**素通しする**。実測で、両方を `{:?}` に戻しても旧 `contains` 形は 19/19 緑のままだった。字面を固定するなら `assert_eq!` にすること。
- 2.3: 較正の主役は**実在アーカイブ（`crates/areka/shell/org/2.zip`・第三者のエンコーダが 2012 年に刻んだ CRC）との突合**。表の 1 語を壊すと**このテストだけ**が赤になる（目録の値と例文はいずれも緑のまま通る）。母数 0 の緑を避けるため「中身のあるエントリを 2 つ以上検査した」ことを先に判定している。
- 2.3: ⚠ `crates/areka/shell/org/` は**絵の作業フォルダ**（`.pdn`・`.ai`・日付つきの作業ディレクトリ）で、宣言された fixture 置き場ではない。この新しいテストが唯一の参照者。**3.1 が `nar_writer` の固定入力の置き場を作るとき、この突合の相手を宣言された置き場へ移すこと**（バイト列のリテラルに焼くと「第三者のエンコーダ」という最も価値ある性質が消えるので、写すなら zip のまま）。
- 2.4: **3.1 への必須の申し送り**＝2.4 の完了状態「書き手が組んだ入力が**後続の読取層で**同じバイト列に戻る」は読取層（3.1）が無いので 2.4 では構造的に満たせない。そして **3.1 自身の完了状態は受理／拒否しか求めていない**ので、このままだと落ちる。**3.1 は無圧縮と deflate の両方について `NarArchive::open` の返すバイト列が投入と一致することを明示的に判定するテストを足すこと。**
- 2.4: 外部の錨は開発時の Python `zipfile`（`testzip()` 緑・壊した 2 種を独立に拒否）。設計が「外部道具に依存しない」と決めているので常設テストには入れない。**3.1 が着地したら `areka-nar` が毎回書き手を突き合わせるので python の錨は恒久的に不要**になる。そのとき `nar_writer_tests.rs` 冒頭の doc の第 3 項を新しい往復テストを指す文言へ書き換えること（腐る主張を残さない）。
- 2.4: 設計の API 一覧に無い 2 つを足した。`EntryBuilder::declared_size(u64)`（総和 1 GiB 超過の固定入力を 2 バイトで作るため）と `NarBuilder::damage(Damage)`（zip64 の印・複数ディスク・中央ディレクトリのオフセット範囲外・EOCD 不在＝エントリ単位では表せない書庫まるごとの壊し）。`CentralOffsetOutOfRange` は `0xFFFF_FFF0`（`0xFFFF_FFFF` は zip64 の印と読まれる）。
- 2.4: `every_fixture_the_testing_strategy_lists_can_be_built` は**コンパイルと形の検査**であって意味の判定ではない（`method()` を no-op にしても緑のまま）。各ビルダの意味は兄弟テストが個別に判定している。未列挙は「包みフォルダ 1 段」と `refresh` 再インストール 2 形（いずれも既存 API で組める）。
- 2.4: ⚠ `Corrupt::Data` を deflate のエントリに立てると**圧縮後**のバイトを壊すので、3.1 では `Integrity::Inflate` として現れる（`Crc` ではない）。
- 2.5: 1.6 の見張りの `EXCLUSION_FORMS` の形 ⑶（`nar-samples`）を `ArrivesIn` → `Present` へ反転した（本タスクが名前空間を導入するため。見張りはこの書き換えを要求して赤になる設計）。反転が新しい向きで効くことは摂動で確認（逐語の綴りを `concat!` で割ると赤）。`ArrivesIn` は使い手が消えるので `#[expect(dead_code)]` を付けたが、**これは自分で消える仕掛け**＝次の形が加わって変種が再び使われると `unfulfilled lint expectation` が立つ。
- 2.5: **5.3 への申し送り**＝`WorkDir::new` は `create_new` を試す**前**に連番を進めるので、pid 再利用で残骸の札とぶつかるのは**そのプロセスの最初の 1 回だけ**。症状は「1 本だけ落ちて兄弟は通る」非対称な失敗（黙って縮退はしない）。掃除を入れるとき失敗の文言に「名前空間を消せば直る」を足すとよい。
- 2.5: 要件 7.10 の綴り（OS の一時フォルダ）は `devroot` に持てない。持つと `temp_path_guard_test` の例外表へ「窓口の迂回」として登録させることになり迂回の数が嘘になる（当初この形で 3 本赤になった実測あり）。
- 2.5: `Drop` の後始末の失敗は標準エラー 1 行（kit に `tracing` は無い）。家の前例 `temp-path-kit` は `let _ =` で黙って捨てているので、これより静かではない。
- 3.1: **4.4 への指示（1）**＝13 変種の到達性テストで `UnsupportedEntry` を証言させる入力には**必ずエントリ由来のもの**（暗号化ビット、または方式 12）を使うこと。zip64／複数ディスクは書庫全体の性質なのに `UnsupportedEntry { index: 0, name: "書庫全体" }` という番人で運んでいるので、それを唯一の証人にしない。この `index: 0` はエントリ番号ではない。`RefuseReason` を 13 変種から広げないこと。
- 3.1: **4.4 への指示（2）**＝`container` の契約は `open` に全エントリの伸長を強制できない（`RawEntry` の `data: Range<usize>` を伸長せずに `install` へ渡せてしまう）。4.4 は `open` の中で全エントリを伸長して突合し、**伸長済みのバイト列を保持**した上で、`install` が二度目の伸長を行わないことを示すテストを持つこと。要件 2.6 と 4.1 が同じ場所で成り立つ設計上の要。
- 3.1: 語彙の所見の訂正＝`Unsupported::Zip64`／`MultiDisk` は `UnsupportedEntry` の**積荷側の下位変種**で、`kind()` が数える外側 13 変種には含まれない。`CorruptArchive` へ寄せても 13 変種のテストも要件 2.5 も壊れなかった（代償は 2.2 が用意した表示文字列 2 本が宛先を失うことだけ）。番人はその軽い理由で維持している。
- 3.1: ⚠ **字面の走査語を「書き込みの呼び出しの数え上げ」で作らない**。10 種を数え上げた最初の版は 11 種中 6 種に回り込まれ（`use std::fs::*` 後の素の `write`・`Command`・`File::options()`・`hard_link`・`soft_link`・`symlink_file`）、**さらに走査語を空にすると走査も較正も両方緑になった**（`for` が 0 周する）。上位の性質（`fs::`・`Path`・`process::` を一切持たない）を 3 語で見張る形が短くて強い。較正の先頭に語数の固定を置くこと。
- 3.2: 予約名の判定は「**最初のドットまでの語**」（Win32 の実際の規則と一致）。`CON.tar.gz` は拒否・`.CON` は受理（語が空）・`CONSOLE`／`COM0`／`COM10`／`PRNS` は受理。`CON.` は末尾ドットの規則で拒否。
- 3.2: `a/` と `a` は衝突として拒否する（末尾 `/` を落とすと宛先が同じ）。5.1 が畳む実ツリー 2 本（計 239 エントリ）を実地に歩いて、予約名 0・末尾ドット/空白 0・禁止文字 0・フォルダとファイルの同名対 0 を確認済みなので、この判断で 5.1 は落ちない。そもそも実ファイルシステムを走査する `fold_into` では同名対は作れない。
- 3.2: ⚠ **設計の穴（実装の欠陥ではない）**＝パス長・要素長の上限規則が無い。30 万文字の要素が受理される（要件 4.6 にも設計の列挙にも長さの項が無い）。展開は作業フォルダへ組んでから入れ替えるので木が半端に残ることは無いが、敵対的な `.nar` を入口で撥ねたいなら仕様側の追記が要る。
- 3.2: 字面の見張りの走査語は `container` と 1 語違い＝`Path` ではなく `path::`（本モジュールは拒否語彙 `RefuseReason::UnsafePath` を綴るため）。`use std::path as p;` の別名 import は素通りするが、`container` 側の `fs::` も `use std::fs as f;` で同じく素通りするので強さは同等（`Path` だけでは何も書けない）。
- 3.3: **5.1 への依頼**＝`manifest_tests.rs` の検体 4 定数（`EMO2_GHOST`／`R_POST`／`EMO2_BALLOON`／`HELLO_PASTA`）は実ファイルを `include_bytes!` せず**書き写して**ある（5.6 がその置き場を消すため）。5.1 は同じツリーをまだ在るうちに畳むので、**そのとき 4 定数と実ファイルのバイト比較を 1 度行い、結果を検証報告に残すこと**。原本がディスクに在る最後の機会。レビュアーが本日手で照合して 4 本とも一致を確認済み。
- 3.3: **4.4／`ghost-install` への申し送り**＝`charset_declared` は**実際に使われた文字コードではない**。`areka-parsers` の先読みは最初の非 ASCII バイトで打ち切るので、非 ASCII 行より後ろの `charset` 行は記録だけされて復号には使われない（`charset` 層の改変は Out of Boundary）。この欄で再復号してはならない。
- 3.3: 設計に無い判断 4 件（いずれもレビュアーが妥当と判定・`ghost-install` が継ぐ）: ⑴ 非ゴーストの同時インストールは扱わない種別でも `CompanionOnNonGhost`（種別の判定が先）⑵ supplement の `RefreshIgnoredForSupplement` はマスクだけでも 1 度出す ⑶ `refresh` が `1` でなければマスクを読まない ⑷ `balloon.directory` の無い `balloon.source.directory` は `IgnoredKey`。⚠ ⑷ の文言「知らないキー」は事実と違う（キーは既知・宛先が無い）・⑵ の文言も `refresh` を書いていない検体に対して「無視した」と読める。⚠ ⑶ の帰結として、ゴーストに `refreshundeletemask` だけを書くと**警告が 1 件も出ない**（`refresh,l` の打ち間違いが沈黙する）。
- 3.3: `locate_install_txt` から `components.len() == 1` を**削った**。`EntryName::path` は `components.join("/")` で空要素は `names` が先に撥ねるので、`path` が `install.txt` に等しいことが階層 1 を含意する（潰しても赤が出ない恒真の枝になる）。「フォルダでなくファイル」の側は `!entry.is_dir` が受け持ち、こちらは赤にできる。
- 4.1: **4.2／4.3 への申し送り**: ⑴ 固定入力に `emo2`／`emo2-kakukaku` の実名を使うと 1.6 の見張りの走査語 ⑷ に当たる。`test-ghost`／`test-balloon` など作り名を使うこと。⑵ `plan_tests.rs` の `snapshot` は（相対パス, バイト長）しか記録しないので**同じ長さの書き換えを見逃す**。5.9 のバイト一致を主張する `install_tests` はこの形を流用せず内容のハッシュを取ること。⑶ `InstallRequest` は `plan.rs` に置いた（`install.rs` が未存在のため）。4.3 が `install.rs` へ移すのは任意で、`mod plan` が私有ゆえ公開面の変化 0 で移せる。
- 4.1: 宛先ゴーストの名前が 1 階層名かを**実在検査より先に**調べる（第 3 の `TargetGhostMissing`）。`root/ghost/..` は**実在する**ので、これが無いと `target_ghost = Some("..")` で宛先が根の外へ出る（4.8 の破れ）。呼び手は `accept` 由来＝アーカイブの内容なので到達可能。語彙は 13 のまま。
- 4.1: 同時インストールの除外は `Ghost` だけでなく `Shell` の本体にも効かせる。要件 5.3 の条件節が「`type` が **ghost または shell**」で、設計の `#### plan` の記述のほうが要件より狭い（設計文書の追随は任意）。
- 4.1: 本体側の除外と同梱側の取り込みは**同一の述語関数**を使う。片側だけずれることが構造的に起きないので「複製を残さない」と「取りこぼさない」が同時に保たれる（変異させると両側のテストが同時に赤くなる）。
- 4.2: **4.3 への申し送り（1）残骸掃除を緩めること**＝現状は `<根>/.nar-work` を消せなければ一律 `StageError` で止まる。この run の木は `<pid>-<連番>/` に閉じているので他プロセスの残骸が混ざる実害は無く、真の危険は Windows の PID 再利用で**自分と同じ** `P-0/` が残っている場合だけ。推奨形＝残骸は best-effort で掃き、消せない要素ごとに記録を 1 行出し、自分の `<pid>-<連番>/` が既に在って消せないときだけ硬く失敗する（隣の `devroot::report_cleanup` と同じ形）。今のままだと常駐ソフトが掴んだ 1 ファイルで根が恒久的に使えなくなる。
- 4.2: **4.4 への申し送り（2）読み取り専用ファイルの診断が指すパスが違う**＝既存の宛先に読み取り専用のファイルが在り書庫にも同名が在ると、`copy_existing` の `fs::copy` が属性ごと作業フォルダへ写し、続く `fs::write` が os error 5 で落ちる。報告されるパスは次の走行で消える**作業フォルダ側**なので利用者が探せない。宛先側のパスを併記するか、`fs::copy` の直後に作業フォルダ側の読み取り専用属性を落とす（1 行）。
- 4.2: **4.4 への申し送り（3）**＝`install.rs` の `contents[*index]` は範囲外で panic する（今日は到達不能）。`open` と `install` を繋ぐ 4.4 が `contents` と `names` の 1:1 を保つ責任を負う。`debug_assert!` で無料で檻に入る。
- 4.2: ⚠ 観察のみ＝`refreshundeletemask` に**フォルダ名**を書いても何も残らない（`copy_existing` はフォルダへ再帰する際に `kept()` を見ない）。要件 6.2・設計・ukadoc はいずれも「ファイル名」と言っており忠実だが、この形だけテストが無い。
- 4.2: ⚠ 道具の罠＝摂動ハーネスが `FAILED` の有無だけを見ると、**コンパイルエラーになった摂動（`test result:` 行が出ない）を緑と誤判定する**。「結果行が無い」と「passed」を区別すること。
- 4.3: **設計の自己矛盾を 1 件是正した**（design.md を別コミットで追随済み）＝「確定ごとに `remove_dir_all(old)`」と「後の配置が失敗したら `old` を戻す」は両立しない。配置 0 が退避を消した後に配置 1 が失敗すると戻す元が無く、要件 5.11 を満たせない。実測: 設計の逐語どおりに戻すと `a_failure_on_the_second_placement_rolls_the_first_one_back` が赤。**退避の削除は全配置の確定後の後片付け 1 回へ寄せた**（`old-<k>` は作業フォルダの配下なので `remove_dir_all` 1 回で済む）。
- 4.3: ⚠ **設計の Testing Strategy の `share_mode(0)` は使えない**（design.md 追随済み）。共有ゼロだと**組み上げ**の複写が os error 32 で先に失敗し、確定の段に届かないので要件 6.6 が無検査になる。`FILE_SHARE_READ`（=1）なら複写は通り親フォルダの `rename` だけが os error 5 で拒まれる＝読み込み中の DLL の正しい再現。
- 4.3: **4.4 への申し送り**＝失敗の経路で**作業フォルダのパスが呼び手に届かない**。`CommitError` は `phase`／`path`／`committed`／`rolled_back` しか持たないので、`rolled_back: false` のとき利用者は「どの宛先が止まっているか」は分かるが「元の木がどこに生き残っているか」（`<根>/.nar-work/<pid>-<連番>/old-<k>/`）が分からない。`NarError::Io` を結線する 4.4 が載せるかを決める。
- 4.3: ⚠ **変異検査に「文の入れ替え」型を入れること**。置換だけの変異表 28 本では、`undo.push(Undo::Remove(dest))` を `rename` の**前**へ動かす変異が漏れ、174 本すべて緑のままだった（自分が作っていない木を `remove_dir_all` する経路）。姉妹の `Undo::Restore` 側は 3 本のテストが既に捕まえるが、実装者が挙げた「`NotFound` 許容だから無害」という理由は**誤り**——あちらの `remove_tree(dest)` は利用者の原本を狙う。
- 4.4: ⚠ **`Cargo.toml` を 2.1 以外で 1 度編集した**（`areka-nar` の `[dev-dependencies]` に `log-capture-kit` 1 行）。理由＝自前の `Subscriber` を書くと `log-capture-kit` の常設検査が赤になり、例外表へ足すのは最後の手段だから共有窓口へ差し替えた（同検査は `a_dev_dependency_on_the_shared_crate_is_not_a_violation` という較正を自ら持つ＝この形が想定されている）。**本番グラフは動いていない**（3 つの的で 171/166/171 が前後同一）ので 2.1 の「外部 +2」も 6.2 の謝辞の期待も無傷。
- 4.4: **後続への申し送り**＝失敗の記録の `work` 欄（利用者の原本が残る場所）は**値が固定されていない**。13 検体はすべて `WorkArea::create` より前に拒否されるので「空であること」しか主張していない。`install_commit_tests` も `NarArchive::install` を通らないので記録自体が出ない。commit の失敗を `NarArchive::install` 経由で踏むテストを作るとき、記録された `work` が `root/.nar-work/` 配下の実在フォルダで `old-0/` を含むことを判定すること。
- 4.4: `Placement::skip_top_level_install_txt` は本番から読まれない（欄単位の `#[allow(dead_code)]` 付き）。実際に最上位 `install.txt` を除いているのは `build_plan` が計画を組む時点の絞り込みで、**設計の記述どおり**（設計はこの欄の読み手を定めていない）。代わりにモジュール宣言に付いていた広い `allow(dead_code)` 5 つは全部撤去した。
- 4.4: 振る舞いの集合一致だけでは**並べ替えの変異を捕まえられない**（マニフェストの解釈を全エントリ検証より前へ動かす変異は、検体が 1 つずつしか欠陥を持たないので集合が変わらない）。字面で `inflate_entry(` の出現数を固定する見張りだけが赤にした。
- 5.1: **6.4 が README に書くべき呼び方は `cargo run -p sample-ghost-kit --example fold-samples -- --from <フォルダ>`**（引数無しの形は `SAMPLES` と `checked_in_parent` を辿るので 5.6 の後は最初の検体で落ちる）。`--from` は登記表にも `checked_in_parent` にも触れず、`.nar` 名はフォルダ名から・宛先の写像は `install.txt` の解釈から取る。レビュアーが `GIT_INDEX_FILE` で 5.6 後の索引を模擬し、引数の形だけが通ることを実証済み。
- 5.1: **5.6 への申し送り（2 件）**＝⑴ `vendors/sample_ghost/.gitattributes` の `* -text` は理由書きが「消える展開形の CRLF」だけを根拠にしており、読んだ人が狭めるのが自然。`*.nar binary` を足すか理由書きを `.nar` の話へ書き改めること。⑵ `.gitignore` の否定 2 行も当たる相手が居なくなる。
- 5.1: `.nar` は**無圧縮**（方式 0）のまま。deflate しても git オブジェクトで 5,442,668 → 5,273,192 バイト＝**3.2% しか減らない**（git 自身の zlib が既に効いており、小さいバルーン 2 本は無圧縮のほうが小さい）。「展開形が消えるから釣り合う」は誤りで、履歴は永久に残る。⚠ 帰結として**要件 9.7 の実機一周は方式 0 しか踏まない**（方式 8 は決定論テスト 8 本だけ）。
- 5.1: `git check-ignore` は索引に在るパスへ「無視されない」と即答するので**追跡済みゆえの恒真 0** になる。`--no-index` で規則そのものに問うこと。⚠ `dic09_Test.txt` はリポジトリ直下の `*_test.txt` に当たっており（`core.ignorecase=true`）、`vendors/sample_ghost/.gitignore` の否定だけが里々の辞書を救っている。
- 5.2: **5.3 が較正すべき順序が 3 つ**（`devroot.rs` のモジュール doc に「掃除（後続タスク）が較正する 3 つの順序」として記載）＝⑴ 札を作るのがフォルダを作るより先 ⑵ 組み上げ中も札が開いたまま ⑶ `FILE_SHARE_READ` だけで開く（⑶ だけは既に較正済み）。⑴⑵ はいずれも入れ替えても全 35 本が緑＝掃除が無い今は観測できない。
- 5.2: **5.4 は `cached_root` を `pub` → `pub(crate)` へ絞ること**（doc に明記済み）。今は窓口が 2 つ在って要件 1.1 の「唯一の入口」が偽になっている。
- 5.2: **5.7 は多重プロセスのテストを新設せず 5.2 のものを引き継ぐこと**（設計の Integration Tests が同じ形を指定しているため）。子は意図的に 1 ファイルしか見ない（競合が生むのは半端な木であって byte 違いではなく、byte 一致は 5.7 の「登記の往復」が持つ）。時間切れは**足さない**（壁時計の期限は他の cargo と競合すると飢餓して赤くなる・ぶら下がりのほうが診断可能）。
- 5.2: ⚠ **変異は「忠実な編集か」を先に確かめてから色を信じる**。実装者の当初の M1 は `create_dir` を**足した**ので本物の `create_dir` が失敗して赤くなり、順序とは無関係の理由で「赤」を記録していた。何も証明しない赤は、恒真の緑と同じ穴の裏返し。
- 5.2: ⚠ 設計は回収の失敗を `warn!` と書いているが、kit に `tracing` が無いので stderr（`report_cleanup`）へ出している。**design.md の当該文の追随が未了**。
- 5.2: 実測＝Windows は**開いた子孫ハンドルがあるフォルダの `rename` を共有モード 7 でも拒む**（os error 5）。よって掴まれた古い原本は退避（移動）の段で失敗し、削除には至らない。取得は続行して次回に再試行する。
- 5.3: 5.2 が残した 3 つの順序をすべて檻に入れ、モジュール doc の該当節を削除した。⑴⑵ は並走の掃除のテストが捕捉（レビュアーが忠実な変異で 3/3 とも赤を再現）、⑶ は既存の札のテストが持つ。
- 5.3: ⚠ **「使用量が増え続けない」のテストは掃除ではなく `Drop` を測っている**（掃除を丸ごと外しても緑・`Drop` の木の削除を壊すと赤）。要件 7.9 の 2 つの半分はそれぞれ別のテストが判定する（前回走行の残骸の回収は `an_acquire_sweeps_away_a_dead_pair_and_a_lockless_leftover`）。後から読む人が増加のテストを掃除の檻だと誤読しないよう doc に 1 行足す余地がある。
- 5.3: 掃除の `NotFound` の腕は時刻に頼らず作れないので、判断を純関数 `owner_is_gone` に出して 3 入力で判定している。⚠ `sweep` の側に `NotFound` の特例を**足す**と 43 本すべて緑のまま（既存コードの変異は全部捕捉されるが、追加は捕捉できない）。`sweep` を次に触る人への注意。
- 5.3: 札は**同じプロセスからでも消せない**（共有モードはハンドル単位）。よって別スレッドの掃除は別プロセスと同じ経路を通る。
- 5.4: 1.6 の見張りに **4 つ目の状態 `Presence::Retired`** を足した（`checked_in_parent` が消えて形 ⑴ の実体が除外領域から無くなったため）。`Present` は偽・`ArrivesIn` は向きが逆・`ComposedAtRuntime` は嘘（窓口は旧置き場をもう知らない）。項目ごと消す手は取れない——表は `declared == FORMS` を主張するので、消すと走査語 ⑴ ごと失われ「旧置き場が**外側**に再出現しないか」の見張りが死ぬ。⚠ `Retired`／`ArrivesIn`／`ComposedAtRuntime` は主張本体が同一で散文だけが違う。**5 つ目を足すのは止めどき**。
- 5.4: ⚠ **段 ① の呼び手が `.to_path_buf()` でパスを写しても安全なのは、保持している `SampleRoot` が `static LazyLock` で決して破棄されないからだけ**。`static` でない呼び手が同じことをすると、消えた木を指すパスを握ることになり、これを捕まえる仕組みは無い。窓口の doc に 1 文足す価値がある。
- 5.4: ⚠ **検証報告 § 5.1 の :18（引数無し）と :20（`--check` だけ）の呼び方は、5.4 以降は両方ともエラーになる**（`--from` が必須になったため）。報告は追記専用なので、5.6 か 6.4 の節で「その 2 つの形は無くなった」と 1 行書くこと。
- 5.4: ⚠ **設計の見張りの節がまだ「除外領域に ⑴〜⑷ の実体があること」と書いている**。実態は 4 形中 1 形（1.6 の時点で既に 2 形・5.4 で 1 形へ）。完了前に design.md の追随が要る。
- 5.4: 実行時間は悪化しない（暖機で HEAD 176 秒 → 162 秒）。複製は**テストごとではなくプロセスごと** 1 回だから（呼び手が `static` で保持している）。
- 5.5: **6.1 の台本が躓く点 3 つ**＝⑴ `R_POST_and_KOMAINU` は `balloon.` 行を**丸ごと出さない**（空値の行ではなく行が不在）ので素朴な抽出は黙って null を渡す ⑵ バルーン検体の `folder=` は `<根>\balloon\<名>` を指す（「`folder` は必ず ghost の下」と決めつける台本は壊れる）⑶ 分割は**最初の `=` 1 つ**で行うこと（`cut -d= -f2-` は可・`-f2` は不可）。
- 5.5: ⚠ **設計の空白＝`manual/` は札を持たないので同時起動を規定していない**。2 つのプロセスが同じ検体を同時に呼ぶと片方の `remove_dir_all` がもう片方の `copy_tree` の最中に走り、**欠けた木の絶対パスを印字しうる**（`check_registry` は最上位 2 要素しか見ない）。開発者 2 人が同じチェックアウトで走らせる／`cargo test` と `cargo run --bin` を並走させるは現実に起こる。札を付けるか「同時に 1 つ」と明記するかの裁定が要る。
- 5.5: ⚠ `manual_paths` と `SampleRoot::from_copy` が**位置の組み立てを 2 か所に持っている**（`root.join(store).join(name)` と `root.join("balloon").join(d)`）。片方だけ変えるドリフトを捕まえるテストは無い。次に `lib.rs` を触るとき `compose` を 1 本抜いて両者から呼ぶのが安い（実質 4 行）。
- 5.5: 設計 File Structure Plan は `Cargo.toml` に `[[bin]]` と綴っているが、cargo の自動発見に任せて `Cargo.toml` は触っていない（2.1 が唯一マニフェストを触れるタスクのため）。得られる bin ターゲットは同一。
- 5.6: 1.3 の申し送り（段 ③ で起動記録消しを外すと 2 度目の起動が 1 度目の永続化を見る）は、**初期化を消すのではなく取得の粒度を起動に合わせて**解いた。`boot_with` が起動ごとに `acquire_emo2()` を呼び、`SpineHarness` が `SampleRoot` を保持する。読むだけの 4 か所は従来どおり静的を共有。実測: 初期化だけ消すと `OnFirstBoot` が欠ける（呼出列に現れない）。
- 5.6: ⚠ spine スイートは **2.44 秒 → 4.95 秒（2 倍）**。起動 20 回＋静的 1 回＝テストバイナリあたり木を 21 回複写する（1 プロセス 1 回ではなくなった）。ワークスペース全体 12 分に対し +2.5 秒なので受容。
- 5.6: ⚠ `SpineHarness` の欄の順（`sample` を最後に置いて最後に捨てる）は**用心であって裏付けるテストは無い**。レビュアーが両方向で実証——先頭へ動かしても、`drop(sample)` を終了処理の前に出しても 33 本すべて緑（終了処理の永続化は失敗しても誰も読まない）。コメントはその事実どおりに書き直してある。
- 5.6: **6.1 へ**＝`tools/perf/` 3 本・`doc/emo2-conformance-scope.md:25`・`crates/pilot/examples/shiori-host-32/README.md:26` が消えた置き場を綴ったまま（6.1 の境界なので 5.6 は触っていない）。**6.3 へ**＝`doc/ukadoc-coverage/briefing-assets.md:362,377,439`。**6.4 へ**＝`vendors/sample_ghost/.gitignore` の理由書きが `vendors/sample_ghost/README.md` を参照しているが、その README は 6.4 が作る。完了前に必ず着地させること。
- 5.6: ⚠ **既存の（本タスク由来でない）不通**＝`cargo clippy -p areka-emo-text --all-targets` が exit 101（`deny` の `absurd_extreme_comparisons` 3 件）・`cargo test -p areka --bin areka spine -- --test-threads=1` が `STATUS_ACCESS_VIOLATION`。いずれも HEAD の版でも再現する。
- 5.6: ⚠ 道具の罠＝clippy の出力を grep の後段で `head -5` に通すとエラーが折り返しの下に切り落とされ「緑」と誤報した。`Select-Object -First N` と同型。
- 5.7: **5.2 の申し送り（子は 1 ファイルだけ見ればよい）を覆した**。「競合が生むのは半端な木であってバイト違いではない」は誤り——`descript.txt` **以外**の 1 ファイルが欠けた半端な木は 1 ファイル照合を素通りする。レビュアーが同一の変異で実証（拡張ありで赤・5.2 の版に戻すと 48/48 緑）。子は自分の空の根へ展開し直して丸ごと突き合わせる形にした。
- 5.7: 登記の往復のテストは**展開器が報告した `InstalledElement.kind` と相対パス**を見る（ディレクトリ走査ではない）。レビュアーの変異で、種別の誤報を捕まえるテストはクレート中これ 1 本だけと判明。⚠ **要件 8.4 のバイト一致の半分を判定する常設テストは存在しない**（比較対象の元ツリーを 5.6 が消したため）。設計どおり、その半分は検証報告 §8.4 の 1 回きりの記録に依る。
- 5.7: ⚠ 8 スレッドの並走テストは、関門 1 の前で `Err` が返る分には全員が両方の関門に着くが、**panic すると `std::sync::Barrier` に毒が無いので残り 7 本が永久に止まる**（レビュアーが実測・120 秒で打ち切り）。壁時計の期限は入れない方針なので欠陥ではないが、直すなら関門 1 の中身を `catch_unwind(AssertUnwindSafe(..))` で包んで `Result` に落とすのが 1 行。
- 5.7: ⚠ `lib_tests.rs` の恒真よけが `SAMPLES.len() == 4` を固定している。要件 8.5 で `StayseeBalloon` を登記すると赤くなる（見直しを強制する意図とも読めるが、`default-balloon-bundle` の担当者は承知しておくこと）。
- 5.7: 要件 9.6（実機・GPU・32bit の補助なし）は**判定するテストが無く構造論に依る**（クレートの走査 0 件・依存閉包が純 Rust・子は `current_exe()`・単体で 8 回緑）。「全項目に○○」型なので per-test では証明できない類型。
- 6.1: ⚠ **本タスクで偽ゼロが 4 度出た**——⑴ ハーネスのシェルが検索語のバックスラッシュを落とした ⑵ `git grep` がリポジトリ全域で 0 を返した ⑶ ワイルドカードの幅不足（`<GhostRoot>\emo2-kakukaku` の区切りは `>` と `\` の**2 文字**なのに `.` 1 つで書いた）⑷ 対照行を `printf` で合成したら `\e` が ESC と解釈されて対照そのものが壊れた。**教訓＝検出台本の対照は合成せず `git HEAD` の実物を使う**（commit 済みのバイト列はシェルにも `printf` にも正規表現にも化けさせる余地が無い）。区切りは**文字クラス**で書くこと。
- 6.1: `tools/perf/invoke-perf-run.ps1` の `-BalloonRoot` 既定（`<GhostRoot>\emo2-kakukaku` の継ぎ足し）は**直さずに削除**して必須引数にした。新配置ではバルーンがゴーストの下に無いので常に存在しないパスになり、6 か所＋README が「使える」と案内していた。`perf-loop.measure.ps1` は必ず両方を明示して渡すので既定に依存する呼び手は 0 だった。
- 6.1: ⚠ **1,000 行の番人は `.ps1` を見ていない**（`workspace_scan/mod.rs` の `collect_rs_files` が `.rs` で絞る）。`perf-loop.measure.ps1` は 993 行で、超えても赤にならない。共有部品は `perf-loop.common.ps1` へ置いてある。
- 6.1: **6.5 が引き継ぐ未検証 4 件**＝⒜ 実測 3 種での `-GhostRoot`/`-BalloonRoot` の受け渡し ⒝ `invoke-followup-checks.ps1` の**単独起動**時の窓口経路（perf-loop 経由では measure が必ず両方渡すので通らない）⒞ **プロセス跨ぎの競合——窓口は呼ぶたび `manual/<検体>` を消して作り直すので、計測中に別端末で `nar-sample-path` を打つと走行中の木が消える。6.5 の 2 周は逐次で行うこと** ⒟ `check-quiet` 経路（32bit ヘルパ・管理者権限・静寂な機械）。
- 6.1: ⚠ 既存の欠陥（本タスク由来でない）＝`tools/perf/check-quiet.ps1` の自己較正が 1 件赤く、`perf-loop.ps1 selftest` が code=4 で止まる。HEAD を取り出しても同じ。計測を回す前に誰かが直す必要がある。
- 6.2: ⚠ **完了状態「謝辞の差分が 2 項目だけ」は誤り**。実測の差分は 5 行＝MIT 件数 209→213 と、`miniz_oxide 0.9.1` **2 行**（MIT の本文が 2 種あり節が 2 つに分かれる。`0.8.9` も同じ 2 か所に居る）・`areka-nar 0.0.1`・`sample-ghost-kit 0.0.1`。**第三者の増分は `miniz_oxide 0.9.1` の 1 件だけ**で、`adler2 2.0.1` は再生成の前から載っていた（`human-panic → backtrace → miniz_oxide 0.8.9 → adler2`＝Windows では通らない枝を、`about.toml` が targets 未指定なので cargo-about が歩く）。残る 2 行は自ワークスペースのクレートで、謝辞はもともと全ワークスペースクレートを載せている（`areka`・`dola`・`wintf` 等が既載）。**要件 10.1 の実際の義務（増分が追加依存とその推移的依存に限られる）は満たしている。**
- 6.2: `about.toml` の targets 未指定は**恒久的に受容**（先送りではない）。依存グラフの数えでは過剰計上が事実誤認だが、**謝辞では過剰計上は無害で、脱落こそが法務上の risk**。`about.toml` 自身のヘッダと `deny.toml` の全ターゲット横断が同じ理由で書かれている。
- 6.2: ⚠ **5 つ目の偽ゼロ**が、`roadmap.md:148`（A0-③）の「保管は**展開フォルダ** `vendors/sample_ghost/StayseeBalloon/`＝`R_POST_and_KOMAINU` と同じ現行慣行」という**生きた指示**を隠していた。5.6 がその慣行を廃した後なので、次の spec に廃止済みの形を指示する状態だった。配布形 `.nar` へ書き改め済み（畳む手順の README は 6.4 が作る＝同一 PR で同時に着地）。
- 6.2: `roadmap.md` の `zip` は 7 行残る（20・22・103・153＝配布物／132・148＝「採らなかった」の記述／195＝日付つき記録）。**承認待ちの依存として綴る箇所は 0**。⚠ 行単位で `zip` ∧ `承認待ち` を数えると L132 に当たるが、その承認待ちは `md-5` に掛かっている。
- 6.3: 台帳の `owner` を埋めると `roadmap-draft.md` の `owner_count` が突合対象になる（PR#147 の罠）。実演済み——`owner` だけ書いて表を直さないと `spec_checks` の 2 本が「宛先が `[[spec]]` にも `[owner_completed]` にも無い」で赤。要件 10.7 の「分けない」はこの赤そのもの。
- 6.3: ⚠ **`status` は `absent` のまま**（要件 10.6 の後半が未達・**引受先の spec が実在しない先送り**）。理由は 2 つで、⑴ `implemented` を名乗るには定義箇所に `// ukadoc: <URL>` が要る（`crates/areka-nar/src/` は 0 行）、⑵ **`areka-nar` は本体 `areka` の依存グラフに入っていない**（唯一の依存元は試験専用の `sample-ghost-kit`）。台帳の読み方が定める実装済みは「areka が正典どおりに動く」＋証跡なので、`absent` が正しい実測。11 件の `note` に両方書いた。**本体へ繋ぐ配線は別 spec 起票が要る。**
- 6.3: ⚠ **6 つ目の偽ゼロを自分で作りかけた（2 度）**。⑴ 実装側が断り書きに旧置き場を逐語引用して 1 件残した（「数える文が数えられる側に回る」型）。⑵ 検算で `grep -rhc … | paste -sd+ | bc` と書いたが **`bc` が無く** `${n:-0}` の既定値が 0 を印字した。**空の変数を 0 に既定するのは偽ゼロの製造機**——数えは `grep -rE … | wc -l` の単段で書き、フォールバックを挟まないこと。
- 6.3: ⚠ **対照列が 1 本消えた**。6.1・6.2 は `doc/ukadoc-coverage/` で 2 件・3 件当たることを検索語の生存証明に使っていたが、本タスクがその 3 行を直した。以後この 2 語の 0 を主張する者は `git show HEAD~<n>:<path>` から当たりを取ること（本タスク時点の代替対照＝本 spec の `validation-report.md` 側に 33 件点灯）。
- 6.3: 報告 5 本は `cargo run -p ukadoc-survey -- report` / `-- report-summary` で作り直したが**中身は 1 バイトも変わらない**（報告は `owner` 欄を載せない）。作業ツリーに CRLF→LF の揺れだけが残るので戻すこと。
- 6.4: ⚠ **`vendors/sample_ghost/` の `profile/` に当たる無視規則は 1 つも無い**。初稿は「規則で落ちていた」と書いたが誤りで、47 件は**誰も `git add` しなかった**だけ。守っていたのは `fold-samples` の出どころが追跡ファイルだけという側。実測＝`git check-ignore -v --no-index` が `profile/` のパスに対して全ての出どころで終了コード 1（較正の対照＝`crates/areka/dic09_Test.txt` は終了コード 0 で `.gitignore:6:*_test.txt` を印字）。
- 6.4: ⚠ **その誤りの出どころは 5.1 の箇条書き 3 点目**——「47 件が規則で落ちている」と書いており、引いている規則は 5.6 で消えた別の木（`crates/pilot/examples/shiori-host-32/.gitignore`）のもの。**追記のみの約束があるので 5.1 は直せない**ので、6.4 側に「あの 1 行は検体の木の話ではない」と注記した。**消えたファイルを引く根拠は、消した後も報告の中で生き続ける。**
- 6.4: 既に起動した木や SSP のインストール先から複製した木を `git add` すると `profile/` は**本当に追跡されて書庫に入る**。README の手順 2 に `git ls-files … | grep -E '/profile/' | wc -l` が 0 であることを確かめる守りを足し、**危険を実際に作って赤（2 件）と緑（0 件）の両方を採って較正した**。
- 6.4: 文書の守りは「書いて終わり」にしない——判定の付かない表（README 先頭の置き場の表）は、更新指示を**その表を開く人が読む場所**（README の手順 5）に置く。検証報告の懸念欄に書いても次の担当は読まない。
- 6.5: 実機 2 周とも `OnFirstBoot`（`lap1.log` 23:35:16 / `lap2.log` 23:36:47）。⚠ **周 2 の根のパスは周 1 と 1 バイトも違わない**（`manual/<検体>/` は同じ場所を消して作り直す）ので、**新品であることをパスでは示せない**。中身で示した——周 1 が `profile/` に 47 ファイル（`sylphya.toml` = `[boot] count = "1"`）を残し、周 2 の窓口呼び出し後に全部消えていた。同種の「作り直す」設計はどれもこの立証が要る。
- 6.5: ⚠ **7 つ目の偽ゼロ**——`vendors/pasta/crates/pasta_lua/profile` を「ファイル 0 個」と書いたが `pasta/save/save.json` が 1 件あった。無関係の根拠は「空だから」ではなく**日時（走行の 2 日前）と追跡状態（サブモジュールが清潔）**。**「空だから無関係」は、空でなかったときに結論ごと崩れる論法。**
- 6.5: ⚠ **記録済みの但し書きを「実測で覆した」と書きかけたが、自分のログが反証していた**——`AREKA_APP_SMOKE_EXIT_MS` は記録どおり ForceQuit 経路（`lap1.log:207` に `event="force_quit"`「終了系列（Forced）へ直行」）。実測が足したのは**経路の訂正ではなく**「ForceQuit 経路**でも** `OnClose` NOTIFY → `unload_clean` → `persist flush confirmed` まで到達する」という別の事実。**過去の記録を覆すと言う前に、自分の証跡が本当にそれを言っているか読み直すこと。**
- 6.5: ⚠ **引受先不在の先送り 3 件**（6.1 → 6.5 の申し送り ⒜⒝⒟）。6.5 は本 spec 最後のタスクで後続が無いため、最終検証と開発者へ上げる。⒜ `tools/perf/invoke-perf-run.ps1` の `-GhostRoot`／`-BalloonRoot` を実測 3 種で渡す経路・⒝ `invoke-followup-checks.ps1` の単独起動・⒟ `check-quiet.ps1` の経路（**`perf-loop.ps1 selftest` が終了コード 4 で赤なのを先に解く必要がある**）。⒞（多重プロセスの競合）は 2 周を逐次にしたことで消化済み。
- 6.5: 実機一周が踏んだ圧縮方式は **0 だけ**（検体 4 本とも無圧縮）。方式 8（deflate）は決定論テストでしか通っていない。
