# ギャップ分析: areka-P0-cursor-tag-canon

> 実施日: 2026-09-02 ／ 対象: 確定済み `requirements.md`（Requirement 1〜10）と現行コードベースの差
> 実測基準: ブランチ `claude/areka-p0-cursor-tag-canon-c24d2c`（`main` 相当・PR#130 マージ後）
> 本書は**判断材料であって決定ではない**。設計判断は末尾 §8 に番号付きで並べ、要件ディスカッションへ渡す。

---

## 1. 分析サマリ

- **受口はすでに全語彙を素通しできている**。さくらスクリプトの字句・意味写像（`areka-parsers`）は `\_l[x,y]` の引数を**文字列のまま**運ぶ（`crates/areka-parsers/src/sakura/decode.rs:212`・`:223-229`）。`centerx` も `@-1650%` も既に無傷で emo テキスト層まで届く。**パーサ側の改修は不要**——編集は `areka-emo-text` に閉じる。
- **足りないのは 2 段だけ**——⑴ 語彙表（`crates/areka-emo-text/src/state.rs:108-133`・`:148-183`）に `centerx`／`centery` の受け皿が無い、⑵ 換算（`crates/areka-emo-text/src/layout.rs:650-676`）が非負の `Px`／`Em`／`Lh` しか値を返さない。`%`・`@`・負値は仕組みとしては語彙保持済みで、換算だけが `None` を返している。
- **縦書きの不整合は 1 行で起きている**。カーソルの原点は `layout.rs:453-454` が `region.left()`／`region.top()`（文字描画範囲の左辺・上辺）から取るのに対し、列送りは `layout.rs:307-311` の `vertical_rl` 行が `-1.0`（減少方向）、列矩形は `layout.rs:618-623` が `left: block_pos - font_height`（`block_pos` が列の**右端**）である。`\_l[0,0]` の着地先が 1 列目にならないのはこの食い違いの帰結で、`region.start()`（`region.rs:283`・`vertical_rl` の書字開始角は `region.rs:225` の `(right, top)`）を原点に採れば 3 方向が 1 本の式で揃う。
- **正典文そのものが 2.8.80 → 2.8.83 で書き換わっている**（本書 §3.1 で逐語対照）。数値座標の原点の定義が「文字描画範囲左上」から「**文字描画開始点（`origin` の位置**。横書きでは文字描画範囲の左上）」へ変わった。現行実装は**旧文のとおり**に validrect の辺を使っている。要件 2.1 は新文に従うので、`origin` を明示宣言したバルーンでは**横書きの表示結果も変わりうる**——Requirement 2.7（横書きの既存表示を変えない）との両立可否が本仕様最大の設計判断である（§8-1）。emo2 のバルーンは `origin` を宣言していない（実測）ため emo2 適合には影響しない。
- **規模は M（3〜7 日）・リスクは中**。編集集合は狭い（`state.rs`／`layout.rs`／`region.rs`＋兄弟テスト＋文書）が、①ファイル行数の上限（`draw.rs` は 980 行で残り 20 行・`layout.rs` は 764 行）、②完了仕様の正典改訂（`areka-P0-choice-render`）、③縦書きテストが現在 0 本という 3 点が工数の実体である。

---

## 2. 現状調査

### 2.1 `\_l` が通る経路（実測）

| 段 | ファイル:行 | 現状 |
|---|---|---|
| 字句・意味写像 | `crates/areka-parsers/src/sakura/decode.rs:212` → `:223-229` | `"_l"` を `Instruction::Cursor { x: String, y: String }` へ。引数は**加工しない**。3 個目以降の引数は捨てる |
| 語彙モデル | `crates/areka-parsers/src/sakura/model.rs:45-46` | `Cursor { x: String, y: String }` |
| cue 化 | `crates/areka-sakura/src/compile.rs:137-145` | `CueCommand::Cursor` へ不透明のまま写す。`\_l[,]`（両軸空）でも cue を発行する |
| 状態適用 | `crates/areka-emo-text/src/state.rs:409-416` | `parse_cursor_coord` で各軸を語彙化し `TextItem::CursorMove { x, y }` を追記 |
| 語彙化 | `crates/areka-emo-text/src/state.rs:148-183` | 空＝`Omitted`／`@` 接頭＝`Relative`／末尾 `%`・`em`・`lh` で単位／本体を `f32`（非有限は `Invalid`）／その他＝`Invalid` |
| 換算 | `crates/areka-emo-text/src/layout.rs:650-676` | **非負の `Px`／`Em`／`Lh` のみ** `Some(origin + value × 係数)`。それ以外は `None` |
| 原点の供給 | `crates/areka-emo-text/src/layout.rs:453-454` | X＝`region.left()`・Y＝`region.top()`（＝validrect の辺・**書字開始点ではない**） |
| 軸写像 | `crates/areka-emo-text/src/layout.rs:462-466` | 横書き＝`(x, y)`／縦書き 2 方向＝`(y, x)` を行内/行送りへ |
| 実体化 | `crates/areka-emo-text/src/layout.rs:325`・`:349-371`・`:467-471` | 到着時に換算だけ済ませて保留し、**次の可視グリフの直前**で行を閉じてから位置を上書きする |
| 縮退の記録 | `crates/areka-emo-text/src/layout.rs:678-761` | 4 分岐（`NegativeAbsolute`／`Percent`／`Relative`／`Invalid`）をキャラクターごと初回のみ `warn!` |

### 2.2 語彙ごとの実装状況（2026-09-02 再実測）

| ukadoc 語彙 | 現状 | 根拠 |
|---|---|---|
| 数値（非負） | 実導出 | `layout.rs:658`（`value >= 0.0` の門） |
| 数値（負値） | 当該軸不動＋警告 | `layout.rs:675`（`_ => None`）・`layout.rs:729` で `NegativeAbsolute` に分類 |
| （省略） | 実装済み（正常形・無音） | `state.rs:150-152`・`layout.rs:727` |
| `centerx`／`centery` | **語彙が無い**＝`Invalid` へ落ちる | `state.rs:161-182`（`%`／`em`／`lh` 以外は数値パースへ行き失敗） |
| `XXem` | 実導出（非負のみ） | `layout.rs:661` |
| `XXlh` | 実導出（非負のみ） | `layout.rs:662` |
| `XX%` | 語彙保持のみ・不動＋警告 | `layout.rs:664`（`Percent => return None`） |
| `@XX`（単位共存を含む） | 語彙保持のみ・不動＋警告 | `state.rs:156-159` で `Relative` になるが `layout.rs:675` で `None` |
| `\_l` 直後の左寄せリセット | 実装なし（`\f[align]` 系が未実装） | `decode.rs` に `"f"` 腕が無い |
| `\c[line]` の行分割単位 | `\c[line]` 自体が未実装 | `decode.rs:191-220` の `decode_tag` に `"c"` 腕が無く `Raw` へ素通し（bare 形 `\c` のみ `decode.rs:177` で `Clear`） |

### 2.3 テスト資産

- `crates/areka-emo-text/src/layout_cursor_tests.rs`＝**670 行・13 本**。`WritingMode::` の出現 22 箇所は**すべて `HorizontalTb`**（`grep -o` で計数）＝**縦書き `\_l` の実行テストは 0 本**。
- `crates/areka-emo-text/src/state_cursor_coord_parse_tests.rs`＝202 行。語彙化（文字列→`CursorCoord`）側の検査。
- `crates/areka-emo-text/src/layout_test_support.rs:1-110` に `model()`／`model_rect()`／`glyphs()`／`window_for()` があり、`WritingMode` を引数で受ける形になっている——**縦書きの検査は新しい足場を作らずに書ける**。
- `crates/areka-emo-text/src/region_vertical_canon_tests.rs`（677 行）が `region.start()` の縦書き値を固定している（例: `region.rs:599` で `vertical_rl` の `start` が `(356.0, 46.0)`）。本仕様が原点を `start()` に切り替えるなら、この既存の検査がそのまま前提の裏付けになる。
- `crates/areka-emo-text/src/layout_visible_window_tests.rs`（262 行）があふれ判定を押さえている。

### 2.4 ファイル行数の制約（実測）

`crates/log-capture-kit/tests/file_length_guard_test.rs` が `crates/**/*.rs` を 1,000 行で見張り、例外表に `areka-emo-text` の項目は**1 件も無い**。

| ファイル | 行数 | 残り |
|---|---|---|
| `crates/areka-emo-text/src/draw.rs` | 980 | 20 |
| `crates/areka-emo-text/src/layout.rs` | 764 | 236 |
| `crates/areka-emo-text/src/region.rs` | 756 | 244 |
| `crates/areka-emo-text/src/state.rs` | 471 | 529 |
| `crates/areka-emo-text/src/layout_cursor_tests.rs` | 670 | 330 |

`draw.rs` に 21 行足せば赤になる。要件が求める「3 書字方向 × 全語彙 × 境界値」の検査は 330 行では収まらない見込みで、**テストファイルの分割が事実上の前提**である（§8-8）。

---

## 3. 要件の実現可能性

### 3.1 正典文の版差（本仕様の前提を左右する実測）

ライブ ukadoc（2.8.83・本日取得）と MCP スナップショット（2.8.80）で、**数値座標の定義文が違う**。

| 版 | 数値座標の定義 | `centerx`／`centery` | 縦書き段落 |
|---|---|---|---|
| 2.8.80（スナップショット） | 「バルーンの**文字描画範囲左上**からのピクセル単位座標」 | 記載なし | 無し |
| 2.8.83（ライブ） | 「バルーンの**文字描画開始点**（`origin` の位置。横書きでは文字描画範囲の左上）からのピクセル単位座標」 | あり（「これだけは文字描画開始点ではなくバルーン画像そのものが基準」） | あり |

現行実装（`layout.rs:453-454` の `region.left()`／`region.top()`）は**旧文のとおり**である。つまり「縦書きだけの不整合」ではなく、**原点の定義そのものが版で動いた**。横書きでも `origin.x`／`origin.y` を明示宣言したバルーンでは新旧で着地点が変わる。

- 宣言された `origin` は上流仕様の裁定により**字義どおり**解決される（`region.rs:311-347` の `resolve_origin_component`。validrect の外でも位置を動かさず記録だけする）。したがって新文へ移ると `\_l[0,0]` は「宣言された `origin`」を指す。
- emo2 のバルーン（`crates/pilot/examples/shiori-host-32/fixtures/emo2/emo2-kakukaku/descript.txt`）は `origin.x`／`origin.y` を**宣言していない**（未宣言成分は書字開始角へ縮退＝横書きでは validrect 左上）。よって emo2 の表示は新旧で同一である。
- 一方 `crates/pilot/examples/shiori-host-32/fixtures/emo2-kakukaku-offsetdpi/descript.txt:13-14` は `origin.x,0`／`origin.y,0` を宣言している。こちらは `\_l` を使わないが、「宣言 origin を持つバルーンは現に存在する」ことの実例である。

### 3.2 要件 → 資産マップ

| 要件 | 必要な技術要素 | 既存資産 | 種別 |
|---|---|---|---|
| 1.1 全書式の受理 | `centerx`／`centery` の語彙追加 | `state.rs:108-133` の enum ／ `:148-183` のパース | **不足**（enum に 1 バリアント・パースに 1 分岐） |
| 1.2 両軸独立 | 既に軸ごとに独立呼出 | `layout.rs:453-454` | 充足 |
| 1.3 単位の意味 | `%` の係数（`font_height / 100`） | `layout.rs:659-665` の係数表 | **不足**（1 行） |
| 1.4 単位は軸非依存のスカラー | 現行も軸で係数を変えていない | `layout.rs:659-665` | 充足 |
| 1.5 解釈不能は当該軸不動 | `Invalid` 経路が既にある | `layout.rs:675`・`:738` | 充足（`centerx` の軸取り違えを同経路へ落とす判断は §8-5） |
| 1.6 両軸省略で無効果 | 既に完全 no-op | `layout.rs:472-477` | 充足 |
| 2.1 原点＝解決後の `origin` | `region.start()` を渡す | `region.rs:283` に取得口あり | **不足**（`layout.rs:453-454` の 2 行・ただし §8-1 の裁定待ち） |
| 2.2/2.3/2.4 3 方向の原点 | `region.rs:223-226` の書字開始角がそのまま使える | `region.rs:225`＝`vertical_rl` は `(right, top)` | 充足（2.1 を採れば自動的に成立） |
| 2.5 Y は字送り方向 | 縦書きの行内軸＝y・正方向 | `layout.rs:309-310`・`:463-465` | 充足 |
| 2.6 範囲外は字義どおり＋DEBUG | 位置は字義どおりになるが**記録が無い** | — | **不足**（記録 1 分岐） |
| 2.7 横書き・`vertical_lr` の非回帰 | 既存 13 本の検査 | `layout_cursor_tests.rs` | **制約**（2.1 と衝突しうる＝§8-1） |
| 2.8 あふれ判定の一貫性 | `visible_window` は行が行送り方向に**単調**である前提で最初に収まる行を探す | `layout.rs:513-559`（`position()` 探索） | **要調査**（§7-3） |
| 3.1〜3.6 `@` 相対 | 実行時点のカーソル位置を基点にする | `layout.rs` の `inline_pos`／`block_pos`（走査ローカル） | **不足**（換算関数の引数追加＋軸の逆写像＝§8-3/8-4） |
| 4.1〜4.5 `centerx`／`centery` | **バルーン画像の原寸**が要る | `TextRegion` は validrect と `start` と折返し閾値しか持たない（`region.rs:178-191`）。ただし `TextRegion::resolve` は `image_size` を**受け取っている**（`region.rs:203`） | **不足**（`TextRegion` に原寸を保持＝§8-2） |
| 5.1〜5.5 縮退と観測 | 4 分岐が 1 分岐（解釈不能のみ）へ縮む | `layout.rs:678-761` の `CursorDegrade`／`CursorWarnGuard` | **不足**（分岐表の作り替え＝§8-7） |
| 6.1〜6.4 行構造 | `\_l` は既に行の分割点として振る舞う | `layout.rs:349-359`（保留フラッシュで行を閉じる） | 充足。ただし「`\_l` 由来か `\n` 由来か」「内容が空か」の**印は無い**（§8-9） |
| 7.1〜7.5 語彙登記と申し送り | 文書のみ | `.kiro/specs/areka-P0-text-decoration-canon/brief.md:34`・`:47` が本仕様を相互指名済み（実測） | 充足（相手側は既に指名済み・本仕様側の登記が要る） |
| 8.1〜8.6 文書追随 | §5 の対象一覧 | — | **不足** |
| 9.1〜9.6 テスト網 | 縦書きの `\_l` 検査 0 本から新設 | `layout_test_support.rs` の足場は流用できる | **不足**（本仕様の作業量の主）|
| 10.1〜10.3 一括着地 | 作業分割はしてよいが統合は 1 本 | — | 運用 |

---

## 4. 実装方式の選択肢

### 案 A: 既存ファイルを広げる（`state.rs` と `layout.rs` に足す）

- `state.rs` の `CursorCoord` に `Center`（または `CenterAxis`）を足し、`parse_cursor_coord` に 2 分岐を足す。
- `layout.rs` の `cursor_to_image_px` の引数を増やし（原点・文字高・行送り に加えて **現在位置**と**バルーン原寸**）、非負の門を外し、`Percent` の係数を入れ、`Relative` を実導出へ移す。
- 縮退分岐表を作り替え、`\_l` の原点を `region.start()` から取るよう `layout.rs:453-454` を差し替える。

**評価**: 既存の並び（純粋関数＋呼び手が記録する形）にそのまま乗る。ただし `layout.rs` は 764 行で、換算・縮退・警告の 3 かたまり（`:638-761`＝約 124 行）が育つ。カーソルの検査ファイルは 670 行から 3 方向分に膨らむため、**テスト側は案 A のままでは上限に触れる**。

- ✅ 変更が読み手にとって 1 箇所に見える／既存の呼び手を触らない
- ✅ 挙動の非回帰が既存 13 本でそのまま測れる
- ❌ `layout.rs` が「行の組み立て」と「カーソル語彙の意味論」の 2 役を持ち続ける
- ❌ テストファイルの分割は結局必要

### 案 B: カーソル意味論を兄弟ファイルへ独立させる

- `crates/areka-emo-text/src/layout_cursor.rs`（新設）に、換算・縮退分類・警告の管理・書字方向別の原点解決をまとめる。`layout.rs` からは `#[cfg(test)]` ではない通常の `mod` 宣言で繋ぐ（`layout.rs:750-764` の兄弟モジュール宣言と同じ並べ方）。
- 検査も `layout_cursor_tests.rs`（横書きの既存 13 本＝非回帰）と、方向別・語彙別の新ファイル（例 `layout_cursor_vertical_tests.rs`／`layout_cursor_units_tests.rs`）へ分ける。ファイル名は既存の導出規則（`<stem>_<テーマ>.rs`）に従う。
- `layout.rs` 側に残るのは「保留 → 実体化」の配線と軸写像だけになる。

**評価**: 本仕様が触る意味論がひとかたまりになり、完了仕様から本仕様へ**所有が移った証跡**がファイル境界として残る（Requirement 8.1 の改訂を読み手が追える）。行数の上限にも余裕が出る。

- ✅ 責務が分かれる／上限に触れない／改訂の証跡がファイルとして残る
- ✅ 純粋関数の集合になるので検査が書きやすい
- ❌ ファイルが増える（新設 1 ＋テスト 2〜3）
- ❌ `layout.rs` と新ファイルの間で「どちらが原点を決めるか」の境界を決める必要がある

### 案 C: 併用（語彙は `state.rs`・意味論は新ファイル・配線は `layout.rs`）

- 語彙（文字列 → `CursorCoord`）は既存どおり `state.rs` に置き、`centerx`／`centery` の 1 バリアントだけ足す（受口の規約＝「面引数は不透明転写・解決は下流」に沿う）。
- 換算・原点・縮退は新設 `layout_cursor.rs` へ。
- `layout.rs` は「保留に積む」「フラッシュで上書きする」の配線のみ。
- 作業は 5 つに割れる（語彙／換算／原点と書字方向／記録と縮退／文書）が、**統合は 1 本**（Requirement 10.3）。

**評価**: 現行の層の切り方（転写層 → 意味論 → 配線）をそのまま延長した形。Requirement 10 の「分割してよいのは作業、着地は 1 本」と噛み合う。

- ✅ 既存の層の規律を崩さない／作業の並び順が要件の並びと一致する
- ✅ 完了仕様の縮退表の改訂が、コード上の 1 ファイルの差分として説明できる
- ❌ 3 ファイルにまたがるので、レビューは差分を横断して読む必要がある

---

## 5. 文書追随の対象（実測で所在を確認済み）

| 対象 | 実所在 | 要件 | 内容 |
|---|---|---|---|
| 縮退表の正典 | `.kiro/specs/completed/areka-P0-choice-render/design.md:607-625`（表本体は `:609-624`。`\_l` 行は「軸省略」「両軸省略/全縮退」「負値絶対」「`%`／`@` 相対」「パース不能」の 5 行） | 8.1 | `\_l` の 5 行を本仕様の分岐表へ移す |
| 同・要件側 | `.kiro/specs/completed/areka-P0-choice-render/requirements.md:47-56`（Requirement 2＝`\_l` カーソル消費・R2.4 が縮退）・`:95-105`（Requirement 6・R6.5 が縮退境界） | 8.1 | 所有の移管を明記 |
| 同・単位表 | `.kiro/specs/completed/areka-P0-choice-render/design.md:123`＝「裸数値＝image px（**文字描画範囲＝validrect 左上原点**）」 | 8.1 | **原点の定義がここにも書かれている**（§8-1 の裁定が決まり次第、正典 2.8.83 の「文字描画開始点」へ改める） |
| 同・縮退の要約 | `.kiro/specs/completed/areka-P0-choice-render/design.md:124`＝「`%`・`@`（相対）・負値絶対＝語彙保持＋warn-once＋状態不変スキップ」 | 8.1 | 実導出へ移った旨に改める |
| 同・境界節 | `.kiro/specs/completed/areka-P0-choice-render/design.md:31`（`\_l` の語彙パース・換算・カーソル移動を自仕様の担当と宣言している行） | 8.1 | 所有の移管を明記 |
| 同・検査計画 | `.kiro/specs/completed/areka-P0-choice-render/design.md:632`（`parse_cursor_coord`＋`cursor_to_image_px` の全網羅） | 8.1 | 追随 |
| **誤登記①** | `.kiro/specs/areka-P0-cursor-tag-canon/brief.md`（「縮退表の正典は完了 spec `areka-P0-emo-text-layer`」の行） | 8.2 | 是正 |
| **誤登記②** | `.kiro/steering/roadmap.md:89`（編集集合に「完了 spec `emo-text-layer` 縮退表」） | 8.2/8.5 | 是正＋編集集合の更新（後述） |
| **誤登記③** | `.kiro/specs/completed/areka-P0-balloon-vertical-canon/` の該当登記 | 8.2 | 是正（訂正である旨と根拠を明記＝8.3） |
| 正典文書 | `doc/COMPAT_ARCHITECTURE.md:183`（「`\_l` の縦書き座標系の正典写像と、areka の既知非互換」の行） | 8.4 | 「未実装（語彙記録＋既知非互換の登記）」→ 実装済みへ・既知非互換の取り下げ・逐語引用は維持 |
| ロードマップ | `.kiro/steering/roadmap.md:89`・`:133`・`:139`・`:143`（2026-09-02 の origin/main rebase 後に `:138`→`:139`・`:142`→`:143` へずれた。`:89`・`:133` は不変） | 8.5 | 着地後の実態へ |
| ソース内の説明 | `layout.rs:449-452`（「origin＝validrect 左辺／上辺」）・`:455-457`（「縮退 4 分岐」）・`:638-649`（換算の説明）・`:678-690`（分岐種別）・`:692-700`（警告の管理）・`state.rs:102-107`（M1 の縮退）・`:135-147`（語彙の文法） | 8.6 | 改訂後の正典と一致させる |

**誤登記の根拠（自ら再実測）**: `.kiro/specs/completed/areka-P0-emo-text-layer/requirements.md` に `\_l` の記述は無く（`_l` の出現 2 件はいずれも `writing_mode` の説明で `\_l` ではない）、Requirement 6（縦書き/横書きの軸解釈）は**受入基準 4 項目**で R6.5 は存在しない。一方 `areka-P0-choice-render` は R2.4／R6.5 と design の縮退表を持つ。したがって Requirement 8.2 の主張は成立する。

**roadmap.md:89 の編集集合は狭すぎる**（実測）。現在は `areka-emo-text/src/{layout,state}.rs`＋兄弟テスト＋`emo-text-layer` 縮退表＋COMPAT §8 と書かれているが、`centerx`／`centery` にはバルーン画像原寸が要り、原点を書字開始点へ移すなら `region.rs` を読む（案によっては触る）。少なくとも `region.rs` が編集集合に加わる。

---

## 6. 規模とリスク

| 区分 | 判定 | 理由 |
|---|---|---|
| 規模 | **M（3〜7 日）** | コードの変更点は狭く既存の並びに乗る。作業量の主は「3 書字方向 × 全語彙 × 境界値」の検査新設（縦書きは 0 本から）と、完了仕様 1 本＋正典文書＋ロードマップの追随 |
| リスク | **中** | 未知の技術は無く、正典は逐語で採取済み。中どまりの理由は ⑴ 原点定義の版差が横書きの既存表示に触れうる（§8-1）、⑵ あふれ判定が行の単調性を前提にしている（§7-3）、⑶ 完了仕様の改訂という手続き上の重さ |

**リスクを下げる順序の候補**: ①先に縦書きの検査を「現状の値のまま」置いて既存挙動を書き留める → ②原点を切り替えて、変わった値が正典どおりであることを 1 件ずつ示す → ③語彙（`centerx`・`%`・`@`・負値）を足す → ④文書。Requirement 9.6（期待値の変更は退行でなく正典追随であることを示す）は、この順序なら差分そのものが根拠になる。

---

## 7. 要調査（設計フェーズへ持ち越す）

1. **`@` と単位の合成の意味**。正典の例 `\_l[@-1650%,100]` は「最後の文字から文字高さ 1650% 分左」＝ `-16.5 × 文字高さ` の相対移動と読める。`@` は基点の切り替えで、単位は量の係数——この読みでよいかを実装前に一度固定する（現行の語彙化は既に `Relative { value, unit }` の形で両方を保持しているので、実装は係数の掛け方だけ）。
2. **`centerx`／`centery` の座標空間**。「バルーン画像の中央（幅／高さの半分）」はバルーン画像の原寸（image px）に対する半分である。一方カーソルの他の書式は「文字描画開始点からの相対」で、内容キャンバスは validrect を原点とする局所座標（`canvas.rs:307-311` が `rect.left - region.left()` で局所化）。この 2 つの空間の間の変換を 1 箇所に閉じられるか確認が要る。
3. **あふれ判定の前提**。`layout.rs:513-559` の可視窓決定は、行を行送り方向へ正規化したうえで「最新行が収まる最小の先頭可視行」を `position()` で前から探す。これは**行が行送り方向に単調に並ぶ**ことを暗黙に前提にしている。負値絶対・`centery`・`@` の負値が入ると行は後戻りしうる。Requirement 2.8（`\_l` を使わない場合と同一の規則）が何を指すのか——⑴ 式を変えない、⑵ 単調でない並びでも意味のある窓を返す、のどちらかを決める必要がある。なお現行でも非負の絶対座標で後戻りは作れるため、これは**本仕様が新しく作る問題ではなく、可視化される問題**である。
4. **範囲外へ出た文字の見え方**。ダーティ矩形は `viewbox.rs:722-746`（`expand_guard_clamp`）で 0〜描画面寸へ丸められ、退化すれば `None` になる。つまり validrect の外へ出した文字は「落ちない代わりに見えない」。Requirement 2.6 は「字義どおり用いて DEBUG を記録する」なので矛盾はしないが、利用者から見える結果として仕様に書くかどうかを確認する。
5. **`\_l` の 3 個目以降の引数**。`decode.rs:224-229` は 3 個目以降を捨てる。正典は 2 引数しか定めていないので現状で整合するが、記録が無い。本仕様の射程に含めるかは判断（§8-10）。

---

## 8. 設計判断イシュー（要件ディスカッションへ）

> **2026-09-02 要件ディスカッションでの仕分け**——下の 12 項目のうち、
> 本質は「受理は正規表現 1 本・解決は式 1 本（位置＝基点＋値×係数）・ぼやけているのは原点だけ」。
> **開発者裁定で決まったのは 1（原点の定義）のみ＝正典 2.8.83 に従う（Requirement 2.1／2.7／2.9）**。
> 3（`@` の基点＝次の文字が置かれる位置・保留改行の扱いは設計で固定）・§7-3（あふれ判定＝式を変えない・Requirement 2.8）・
> 10（3 個目以降の引数＝登記のみ・Requirement 7.6）・11（完了仕様の扱い＝家法どおりアーカイブ非改変＋COMPAT §8 上書き記録・Requirement 8.1〜8.3）は
> 要件側で決めて登記した。**残る 2・4・5・6・7・8・9・12 は設計フェーズ（`/kiro-spec-design`）で解決する**——いずれも
> 実装の組み方（値の配り方・処理順・分類の作り方・ファイルの置き場所）に閉じており、要件の文面を変えずに決められる。

1. **絶対座標の原点を「validrect の辺」から「解決後の `origin`」へ移すか**。正典 2.8.83 は後者（Requirement 2.1）。現行は前者（`layout.rs:453-454`）で、これは正典 2.8.80 の文言どおりである。移すと、`origin` を明示宣言し validrect の辺と異なるバルーンでは**横書きの表示も変わる**——Requirement 2.7（横書きの既存表示を変えない）と正面から当たる。選択肢は ⑴ 正典を優先し 2.7 を「非宣言バルーンについて」と読み替える（emo2 は非宣言なので適合 e2e は無傷・実測）、⑵ 横書きだけ validrect の辺のままにして縦書きだけ書字開始点にする（方向で式が割れる・正典に反する）、⑶ 2.7 を改訂する。**⑴ が正典に忠実だが、要件の文面をどう扱うかの裁定が要る。**
2. **バルーン画像の原寸をどう配る**。`centerx`／`centery` に必須。`TextRegion::resolve` は既に `image_size` を受け取っている（`region.rs:203`）ので、`TextRegion` に保持させるのが最小（呼び手は全員すでに `region` を渡している）。対案は `layout` の引数を 1 つ増やす（呼出箇所が 10 箇所以上ある）。
3. **`@` の基点の定義**。「現在の文字描画位置」を ⑴ 直前に置かれたグリフの次の位置（保留中の改行を数えない）とするか、⑵ 保留中の改行を適用した後の位置とするか。正典の記述例は「最後の文字から」なので ⑴ が素直だが、`\n\_l[,@1em]` のような並びで結果が変わる。あわせて、**グリフを挟まずに `\_l[@..]` が連続したとき**に基点が累算されるか（現行の保留は後の `\_l` が前の `\_l` を上書きする＝`layout.rs:470`）を決める。
4. **軸写像と換算の順序**。現行は「image 軸（x, y）で換算 → 行内/行送りへ写す」（`layout.rs:453-466`）。`@` の基点は行内/行送り側の変数（`inline_pos`／`block_pos`）にあるため、⑴ 基点を image 軸へ逆写像してから今の順序を保つか、⑵ 先に軸を写してから換算するか、のどちらかに揃える必要がある。
5. **`centerx` を Y 軸に、`centery` を X 軸に書いた場合**。正典は沈黙している。Requirement 1.5 は「解釈不能」として当該軸不動に落とす形。記録の分類を「解釈不能」と同じにするか、専用の分類にして書き手に伝わりやすくするかを決める。
6. **`%` の係数**。「100%＝タグを書いた時点での文字高さ」なので `value × font_height / 100`。`em` と同じ量を 100 分の 1 の刻みで書く別記法という理解でよいか（`layout.rs:659-665` の係数表に 1 行足すだけ）。
7. **縮退の分類をどう作り替えるか**。現行 4 分岐（負値絶対／`%`／`@`／解釈不能）のうち 3 つが実導出へ移り、残るのは「解釈不能」だけになる（Requirement 5.2）。⑴ 分類を 1 つに畳んで列挙型を廃す、⑵ 列挙型は残して 1 バリアントにする、⑶ Requirement 2.6 の「範囲外」を DEBUG の別分類として足す（警告ではないので初回一回化の対象外）。読み手が「なぜここに列挙型があるのか」を後から復元できる形を選びたい。
8. **ファイルの置き場所**。`layout.rs` は 764 行、カーソル検査は 670 行、`draw.rs` は残り 20 行。案 B／C のようにカーソル意味論と検査を兄弟ファイルへ出すか、案 A のまま `layout.rs` に足して検査だけ分けるか。`draw.rs` には**足さない**ことは要件の前提として確定している。
9. **行構造の観測をどこまでやるか**（Requirement 6.3）。`\c[line]` の「行」判定には「`\_l` 由来の分割か否か」「内容が空か」の区別が要るが、現在 `PositionedLine`（`layout.rs:156-170`）は行矩形とグリフ列しか持たない。⑴ 行に由来の印を付けて今から観測可能にする、⑵ 「印を付けられる形になっている」ことの確認と登記にとどめる（Requirement 6.4 は `\c[line]` そのものを実装しないと定めている）。過剰実装と申し送り不足の間の線引き。
10. **`\_l` の 3 個目以降の引数**（`decode.rs:224-229` が黙って捨てている）を本仕様の射程に含めるか。正典は 2 引数しか定めていないため実害は見えないが、「黙って捨てる」ことの記録が無い。
11. **完了仕様の訂正の書き方**（Requirement 8.3）。`areka-P0-choice-render` の R2.4／R6.5 と design の縮退表を、⑴ 追記型（元の記述を残し「本行の所有は `areka-P0-cursor-tag-canon` へ移った」を添える）にするか、⑵ 書き換え型（訂正の但し書き付き）にするか。誤登記 3 箇所（brief・`roadmap.md:89`・完了仕様 bvc）の是正も同じ書き方で揃えたい。
12. **ロードマップの編集集合の更新**（Requirement 8.5）。`roadmap.md:89` は編集集合を `{layout,state}.rs` としているが、実測では少なくとも `region.rs` が加わる（バルーン原寸・書字開始点）。着手前に直すか着地後に直すか。

---

## 9. 設計フェーズへの申し送り

- **推奨の出発点は案 C**（語彙は `state.rs`・意味論は新しい兄弟ファイル・配線は `layout.rs`）。現行の層の切り方をそのまま延ばした形で、ファイル行数の制約にも当たらず、完了仕様からの所有の移管がファイル境界として残る。ただし §8-1 の裁定が先で、そこが決まらないと原点の解決をどこに置くかが決まらない。
- **最初に置くべき検査**は「現状の縦書き `\_l` の着地点」。0 本の状態から始めるので、直す前の値を書き留めておかないと Requirement 9.6（期待値の変更が正典追随であることを示す）の根拠が作れない。
- **相互登記の相手はすでに準備できている**——`.kiro/specs/areka-P0-text-decoration-canon/brief.md:34`・`:47` が本仕様を「`\_l` 側の実装所有者」として名指ししている。本仕様側から指し返すだけでよい。
- **`\c[line]` に所有者が居ないことは実測で確認した**（`.kiro/specs/` 直下の 21 仕様のうち `\c[line]` に言及するのは本仕様の brief と requirements のみで、他は完了仕様の調査記録と roadmap の履歴）。Requirement 7.3 の登記先は `areka-P0-ukadoc-survey-sakura-script`（実在確認済み）。
- **並走仕様との衝突は見当たらない**。`areka-P0-sakura-bare-tag-lexer` は `areka-parsers` の `lexer.rs`／`decode.rs` を触るが、本仕様は `areka-emo-text` に閉じる（`decode.rs:212` の `"_l"` 腕は角括弧形なので bare 形の修正とは別の分岐）。

---

## 10. 設計フェーズの記録（2026-09-02・`/kiro-spec-design`）

### 10.1 サマリ

- **Discovery Scope**: Extension（既存システムの拡張・light discovery）。外部依存の追加なし・Web 調査なし。コードベースの実測（§2〜§5 の再確認）と正典（`requirements.md` 付録 A）だけで設計が閉じた。
- **Key Findings**:
  - `TextRegion::resolve` は既に `image_size` を受け取っている（`region.rs:203`）ので、`centerx`／`centery` の基準は `TextRegion` に原寸を保持するだけで配れる（呼び手 134 箇所は無改変）。
  - `vertical_rl` の `block_pos` は列の**右端**（`layout.rs:619-621`）。原点を `region.start()`（`(right, top)`）に切り替えると `\_l[0,0]` が自動配置の 1 列目と厳密一致し、`\_l[@-1lh,0]` は自動列送り `block_pos += −pitch` と同じ値を与える。式に分岐は要らない。
  - `structure.md` のテスト分離規約「最長 stem 優先」により、新設ファイルを `layout_cursor.rs` と名付けると既存 `layout_cursor_tests.rs` の所属が `layout` から `layout_cursor` へ変わって接続宣言が規約違反になる。新設ファイルは **`cursor_tag.rs`** とした（§10.3 DD-8）。
  - 里々 Wiki の実例（ukadoc MCP 検索 `\_l`）が `@` 相対の実用形を示す——`\_l[@0,@3]…\_l[@0,@-3]`（記号だけを 3px 下げて戻す）と 2 段組メニューの `\_l[,@-70]`＋`\_l[160,]`。後者は連続する `\_l` が軸ごとに合成されることを前提にしており、現行の「後の `\_l` が保留を丸ごと上書き」（`layout.rs:470`）では Y が失われる。

### 10.2 調査ログ

#### 正典の版差（MCP スナップショット 2.8.80 と付録 A 2.8.83）
- **Context**: 設計が依拠する正典文の確認。
- **Sources**: ukadoc MCP `search_docs("\_l")`（`ukadoc:list_sakura_script:_5c_l_5bx_2cy_5d:1`）・`requirements.md` 付録 A。
- **Findings**: スナップショットは「文字描画範囲左上」・`centerx`／`centery` なし・縦書き段落なし。付録 A（ライブ）は「文字描画開始点（`origin` の位置）」・`centerx`／`centery` あり・縦書き段落あり。`\c[line]`・`\f[align]` の文はスナップショットと付録 A で同文。
- **Implications**: 設計は付録 A を正典とする。`doc/COMPAT_ARCHITECTURE.md` §8 の `\_l` 行にも版の注記を残す（既存行が既に 2.8.83 逐語を引いている）。

#### `@` の基点と保留改行
- **Context**: §8-3（要件側で「基点＝次の文字が置かれる位置」と決まり、保留改行の扱いは設計へ）。
- **Findings**: 保留改行を無視して走査ローカルの位置を基点にすると `\n\_l[@0,@0]` が改行を取り消す。フラッシュと同じ順（保留改行 → 保留カーソル）で仮適用した「実効位置」を基点にすれば `\_l[@0,@0]` はどこでも無効果になり、連続する `@` は累算される。
- **Implications**: design DD-3。走査ローカル状態は書き換えない（遅延モデルと末尾蒸発は不変）。

#### ファイル名とテスト分離規約
- **Context**: §8-8。
- **Findings**: `structure.md` Unit Tests「最長 stem 優先」「前向きの衝突禁止」。`layout_cursor.rs` を作ると `layout_cursor_tests.rs`・`layout_cursor_*_tests.rs` はすべて `layout_cursor` の所属になり、配線のテスト（`LayoutEngine::layout` を通す）が意味論ファイルの配下へ移って `layout::test_support`（`pub(super)`）へ届かなくなる。
- **Implications**: 意味論は `cursor_tag.rs`＋`cursor_tag_tests.rs`、配線のテストは `layout.rs` 配下（既存 `layout_cursor_tests.rs`＋新設 `layout_cursor_vertical_tests.rs`）。可視性の変更も複製も要らない。

### 10.3 設計判断（§8 の残項目 2・4・5・6・7・8・9・12 への回答。正本は design.md「設計判断」表）

| § | 決定 | 選ばなかった案と理由 |
|---|---|---|
| 8-2 | `TextRegion` に `image_size` を保持（DD-2） | `layout` の引数を増やす案＝呼出 10 箇所以上を触るうえ、原寸は領域の属性であって配置の入力ではない |
| 8-3 | 基点＝実効位置・保留は軸ごとに合成（DD-3） | 保留改行を無視する案＝`\n\_l[@0,@0]` が改行を取り消す。上書き案＝里々 Wiki の 2 段組メニューが壊れる |
| 8-4 | image 軸で解決してから写す（DD-4） | 先に軸を写す案＝正典が「座標軸はバルーン画像そのまま」と定めるため、意味論を image 軸で閉じる方が読みやすく、逆写像は 1 行 |
| 8-5 | 専用分類 `CenterAxisMismatch`（DD-5） | `Unparsable` に畳む案＝書き手に「軸を取り違えた」と伝わらない。分類が 1 つ増えるだけで式は不変 |
| 8-6 | `値 × 文字高さ / 100`（DD-6） | — |
| 8-7 | 列挙型は残し 2 バリアント・範囲外と無効果は DEBUG（DD-7） | 列挙型を廃す案＝要件 5.3「分岐ごと」の鍵が無くなる。範囲外を warn にする案＝正典どおりの正常形を警告してしまう |
| 8-8 | `cursor_tag.rs`＋`cursor_tag_tests.rs`・配線テストは `layout` 配下（DD-8） | `layout_cursor.rs`＝§10.2 の規約衝突。案 A（`layout.rs` に足す）＝2 役の同居が続く |
| 8-9 | 拡張の口の確認と登記のみ（DD-9） | 由来の印を今付ける案＝`\c[line]` を実装しない（6.4）以上、消費者の無い field を足すことになる |
| 8-12 | 着地時に更新（DD-10） | 着手前に直す案＝実装の差分と食い違う |

### 10.4 統合（design-synthesis）の結果

- **一般化**: 7 つの座標書式は「基点＋値×係数」の 1 式の特殊化。基点 3 種（原点／現在位置／画像中央）と係数 4 種（1／文字高さ／行送り／文字高さ÷100）の直積で全語彙が尽きる。書字方向は式に入らず、原点の位置（`TextRegion::start()`）と軸写像（既存の正準表）だけが回る。
- **採用 vs 自作**: 既存資産で足りる——`parse_cursor_coord`（語彙）・`TextRegion::start()`（原点）・保留フラッシュ（実体化）・`CursorWarnGuard`（一回化）・`FixedMetrics`／`layout_test_support`（テスト足場）。新しい抽象は `CursorAxis`・`CursorBasis`・`resolve_cursor_axis` の 3 つだけ。
- **簡素化**: 旧 `cursor_to_image_px` の非負ゲートと `Percent => None` を撤去し、4 分岐の縮退を 2 分岐へ。`draw.rs`・`visible_window`・パーサは非接触。

### 10.5 リスクと緩和

- **宣言 `origin` バルーンの横書きが動く**（2.9）——正典追随として要件で裁定済み。emo2 は未宣言（`emo2-kakukaku/descript.txt`）で不変。テスト V7 が 3 方向の宣言値着地を固定する。
- **後戻り行とあふれ判定**（2.8）——式は変えない。テスト V8 が既存の式の返す値を固定し、将来の「意味のある窓」の議論は別仕様へ。
- **連続 `\_l` の軸ごと合成が既存形の連続の結果を変える**——2.8.80 でも「省略＝移動しない」なので正典追随（テスト H2・9.6）。
- **`centerx`／`centery` の大小文字**——正典沈黙。小文字完全一致のみ（COMPAT §8 の先例に揃える）。緩める必要が出たら語彙層の 1 分岐で済む。

### 10.6 レビューゲート

- 機械検査: 要件 ID 56 件すべてが design.md の Traceability に出現（初回は `10.1〜10.3` の範囲表記で 10.2 が検出されず → 3 行に分解して再検査で 0 件欠落）。境界 4 節・File Structure Plan は具体・孤児コンポーネントなし。
- 判断検査: 要件の矛盾・曖昧は見つからず。修正 2 件（`warn_cursor_degrade` の誤記・H5 の warn 件数を鍵 `(actor, degrade)` と整合）。`cursor_tag.rs` の依存から `writing.rs` を外した（書字方向を知らない純関数）。
- 結果: **通過**（修復 1 回）。

### 10.7 参照

- `requirements.md` 付録 A（SSP 2.8.83 逐語）・付録 B（疑義表）。
- ukadoc MCP: `ukadoc:list_sakura_script:_5c_l_5bx_2cy_5d:1`（2.8.80 スナップショット）・`satori:選択肢#dc10653c`（2 段組メニュー）・`satori:絵文字（Unicode文字）を表示する#p15a4438`（`@` 相対の微調整）。
- `.kiro/steering/structure.md` Unit Tests（テスト分離規約）・`doc/COMPAT_ARCHITECTURE.md` §8 `:147`／`:153`（上書き行の雛形）・`.kiro/specs/completed/areka-P0-balloon-vertical-canon/design.md` DD4。
