# 確定台帳（task 4.3・2026-08-20）

## 0. 本書の位置づけ

本書は **task 4.3「確定台帳を起こし実機専用の上限を確定する」の成果物**である。設計 C9 が定める様式で、この症状を作っている機序を 1 件ずつ登記する。

- **本書は合否を主張する文書ではない。** 合否は手順書（`signoff-procedure.md`）の様式で task 7.3 が出す。本書が言うのは「何が・どの証跡で・確定しているか／未特定か」だけである。
- **数字の出所は 2 つだけ**である。要件 1.1〜1.7 の採取条件・帰着・未特定は第 1 段再観測（`reobservation-2026-08-15.md`）から転記し、フレーム番号つきの量は是正前の基準値（`baseline-2026-08-20.md`）と、その生ログ（`C:\Users\maz-o\AppData\Local\areka-diag\atom-20260820-193457\atom-signoff.log`）から採った。本書で新たに実機を回してはいない。
- **file:line は本書を書く時点で 1 件ずつ読み直した。** 設計 C9 の初期登記が持っていた行番号のうち 4 件は、群 1〜3 の観測点設置で行がずれて古びていた（§6 の注記）。以後は**本書が file:line の正本**であり、設計 C9 は初期登記の記録として読む。

---

## 1. 証跡クラスの定義（要件 3.3）

| クラス | 何を根拠にするか | 何を主張してよいか |
| --- | --- | --- |
| **実機** | 実機採取のログ行（引用と件数）。出所は上の 2 文書に限る | 「実際にそう振る舞った」。件数は採取した回数までの話であり、それ以上へ広げない |
| **静的構造** | `file:line` で読み直せるコードそのもの | 「コードがそう書いてある以上そうなる」。実機の再現を待たずに確定させてよい（記憶〈確定の証跡は実機ログと静的構造証跡の二本立て〉） |

2 クラスを分けるのは、**片方だけで確定できる項目がある**からである。作業領域源が起動時固定であること（L3）はコードを読めば確定するので実機は要らない。逆に `SetWindowPos` 1 回に何 µs かかるか（L7）はコードからは出ない。

**消灯している観測点の 0 件を根拠に使わない**（要件 8.5）。本書では L2 と L4 がこの区別に触れるので、それぞれの行で明示する。

**証跡は是正前の状態の記録である**（task 5.3 のレビューで一般化）。本書の各項目は「そこに欠陥があった」ことを登記するものなので、群 5 の是正が着地すると根拠に引いた `file:line` と doc は現在のコードと合わなくなる。これは構造的に起きることで、5.3 が L5 を、5.4 が L6 を、5.6 が L4 を同じように陳腐化させる。**そのときは証跡を消さず、書き換えもせず、当該節へ「是正前の記録である」旨を注記する**——消せば何を確定したかの記録が失われ、是正後のコードを指すよう書き換えれば「欠陥を登記した行」が自己矛盾する。§2 の表の `是正の有無` 列が引受先のタスクを既に名指ししているので、読み手はそこから現在の姿へ辿れる。

---

## 2. 台帳

様式は設計 C9 のとおり `ID | 項目 | 証跡クラス | 根拠 | 状態 | 是正の有無`。根拠の全文と数え方は §3 に 1 件ずつ置く。

| ID | 項目 | 証跡クラス | 根拠（要約・詳細は §3） | 状態 | 是正 |
| --- | --- | --- | --- | --- | --- |
| L1 | 一括書込が窓ごとに 1 枚ずつ進む（逐次 flush） | 実機 | 12 遷移すべてで書込 6 本（#11 のみ 5 本）が順に並び、連続する書込の間隔は n=59・最小 1,683・中央値 16,562・最大 82,074µs | **確定**（ただし第 1 段からフレーム軸の読みが変わった・§3.1） | あり（C8 の **B-2b** を task 7.1 で採用・**task 7.2 で実装着地**・§10.5／§10.6.1） |
| L2 | 経路 A（OS 提案位置の同期書込）が 0 回 | 実機 | `origin=dpi-suggested` 0 件・`stage=sync` 0 件。**同一採取で `kind=write` 77 件・`msg=WM_DPICHANGED` 48 件（`in_swp=true` 48/48）**が出ており観測点は点灯している | **確定（真の 0）** | 不要（`dpi-window-vanish` の是正が効いている・維持すべき状態） |
| L3 | 作業領域源が起動時固定で追随しない | 静的構造＋実機 | `crates/areka/src/main.rs:574`（構築は起動経路 1 箇所・:569 の注記が「セッション内固定＝M1 受容」と明言）／読み口 `crates/areka/src/placement/follow/window_move.rs:288`。実機は接地点差 −48px が 192→96 の 6/6 | **確定** | あり（task 5.1・5.2） |
| L4 | 連鎖を一度確定したら解き直さない | 静的構造＋実機 | `crates/areka/src/emo2_boot/frame/drain_resnap.rs:299-301`（`ChainFinalized` があれば即 return）／実機は第 1 段の 100% で二体の隙間 359px | **確定** | あり（task 5.6） |
| L5 | 同一 hwnd への 2 指令が合流しない | 静的構造＋実機 | `crates/wintf/src/ecs/window/command.rs:240-242`（doc が「先着の枠へ畳まない」「`merged_into_seq` は常に番兵」と明言）／実機は `merged_into_seq` が 77/77 で番兵 | **確定** | あり（task 5.3） |
| L6 | Z 書込が先行したときの 2 段書込経路 | 静的構造 | `zorder_pair_maintain.rs:483` → `window_pos.rs:325`（受理・記録 :335-343）→ `:387-389`（DPI 成分を直接更新＝`Changed<DPI>` 発火）→ `emo2_boot/frame/dpi.rs:232` → `window_move.rs:288`（起動時 snapshot を読む） | **確定（経路の存在）**。本採取では**発火していない**（経路 A 0・§3.6） | あり（task 5.4 の整合待ちが塞ぐ） |
| L7 | 窓書込 1 回の内訳 | 実機 | 一括書込の総所要は `Σcall_us` とほぼ一致（99.8〜99.9%）＝**遅いのは `SetWindowPos` 呼出そのもの**。77 本は入れ子メッセージで 2 群に割れ、**当該窓の `WM_DPICHANGED` を内側に持つ 48 本が総呼出時間の 94.7%** を占める | **部分的に確定**（呼出の内側の OS／DWM の内訳は**未特定**・§3.7） | あり（C8 の **B-2b** を task 7.1 で採用・**task 7.2 で実装着地**・§10.5／§10.6.1） |
| L8 | 積み上げから一括書込までの間 | 実機 | 最後の `kind=enqueue` から `kind=flush stage=begin` まで **24,896〜116,201µs（中央値 67,580）**。前半には文字層の再構築とテキスト測定のログが並び、**後半（10,288〜57,242µs・中央値 30,956）は観測行が 1 行も無い** | **部分的に確定**（後半は**未特定**・§3.8） | 前半は**申し送り**（§5）／後半は追加の観測点が要る |
| L9 | 実機専用の上限の確定値 | **裁定**（L1〜L8 の機序とは種類が違う——根拠は要件 4.2／4.5 からの導出であって、コードを読み直して確かめられる事実ではない。`file:line` は確定値の置き場所を指すだけである。導出は §4 を見ること） | 上限＝**16,667µs**（両量とも）。根拠は「提示される 1 フレームはリフレッシュレートが 60Hz を下回らない限り高々 1/60 秒」——要件 4.2 を µs の側から**一方向に**押さえた形（超えていれば必ず提示フレームをまたぐ。超えていないことは 4.2 の成立を意味しない）。定数は `crates/areka/src/placement/transition_judge_verdict.rs:90`／`:99` | **確定** | 判定器へ反映済み（§4） |

---

## 3. 根拠の詳細

### 3.1 L1 一括書込が窓ごとに 1 枚ずつ進む

**確定。証跡クラス＝実機。**（本節は**是正前**の記録である。是正後の同じ量は §10.4 を見ること——L1 は残っている。）

12 遷移すべてで、一括書込の内側の書込が順に並ぶ（基準値 §3.4）。

- 連続する書込の間隔: n=59・最小 1,683µs・中央値 16,562µs・最大 82,074µs。**12 遷移すべてに 40,000µs 超の間隔が 1 本以上**ある。
- 書込の順序は 12 遷移で 2 通りしか出ない（基準値 §3.4 の注記）。ばらつきではなく決まった並びである。

**第 1 段から状態が変わっている。ここは書き分ける。**

| | 第 1 段（`reobservation-2026-08-15.md`・時刻近似） | 本採取（フレーム番号つき） |
| --- | --- | --- |
| フレーム軸 | 「255〜309ms ≒ 60Hz で 15〜19 フレーム・120Hz で 31〜37 フレーム」と**換算**して有界フレーム内でないと判定 | 全書込が起点と**同一フレームの内側**（`frames_to_last_write` が 12/12 で 0） |
| 時間軸 | 61〜94ms 間隔で 1 枚ずつ | 1.7〜82.1ms 間隔で 1 枚ずつ（**残存**） |

第 1 段の換算が誤りだったのは、**遷移を含むフレームそのものが 285.4〜415.4ms 続く**（同じログの定常区間は 8.3729〜8.8151ms）からである。第 1 段は「フレーム周期は一定」を前提に時間をフレーム数へ割ったが、tick は vblank 駆動なので遷移中の長い処理はそのまま 1 つの tick を引き伸ばす。

**したがって「逐次適用」の主張はフレーム軸から時間軸へ移った。** 是正の効き目を測るのはフレーム差ではない——それは是正前から 0 である（基準値 §6 ⑵）。measure すべきは L7・L8 の µs である。

### 3.2 L2 経路 A が 0 回

**確定。真の 0 である（消灯による 0 ではない）。証跡クラス＝実機。**

- 判定量: 12 遷移すべてで `path_a_writes=0`・`sync_stage_writes=0`（札と段の両方）。
- **観測点が点灯していることの裏取り**: 同一採取・同一 target・同一水準で `kind=write` **77 件**、`kind=msg` **237 件**（うち `msg=WM_DPICHANGED` **48 件**・`in_swp=true` が **48/48**）が出ている。
- 発行点は 1 箇所である。`crates/wintf/src/ecs/window_proc/window_pos.rs:325` の `WM_DPICHANGED` ハンドラの内側に、受理の記録（:335-343）と採用時の同期書込の記録（:466-490・ガードの読みは :460）が**同じ前置ガード `transition_diag::is_enabled()` の下**に並ぶ。後者だけがさらに採否の分岐（:442 `if let Some((x, y)) = decision`）の内側にある。
- ゆえに **48 回このハンドラを通っており前置ガードは真**であった。`stage=sync` の 0 は観測点が消えていたからではなく、**OS 提案位置を採用する分岐へ入らなかったから**である。

**言ってよいことの境界**: 採否そのものを刷る `[WM_DPICHANGED] suggested position write decision`（:427-438）は既定 target の `debug!` なので、この `RUST_LOG` では消灯している（実測 0 行）。**採否の判断を「この行が無いこと」で語ってはならない。** 語れるのは「書込が 0 件だった」までである。

**是正は不要。維持すべき状態である**（`dpi-window-vanish` の不採用の政策）。0 でなくなったら回帰であり、task 7.3 はそれを回帰として扱う。

### 3.3 L3 作業領域源が起動時固定

**確定。証跡クラス＝静的構造＋実機。**

- 静的構造: `crates/areka/src/main.rs:574` が `boot_monitor_snapshot(&enumerate_monitors())`（関数は同 :530）を呼ぶ**唯一の構築点**であり、起動経路（`open_startup_window`）の内側にある。資源への挿入も同関数内の :611 だけである（本番コードで `boot_monitor_snapshot` を呼ぶ箇所は他に無い——残りはテスト）。同所の注記 `:569` が「セッション内固定＝M1 受容」「WM_DISPLAYCHANGE 追随は後続」と明言している。読み口は `crates/areka/src/placement/follow/window_move.rs:288`（`world.get_resource::<MonitorSnapshot>()` を射影 T へ渡す）。**実行時に作り直す経路はコード上に存在しない。**
- 実機: `kind=ground` 24 件すべてで `ground_y=1704`（起動時 200% の作業領域下端）が一定。`wa_bottom` だけが 1704 ⇄ 1752 と往復し、192→96 の 6 遷移すべてで差 **−48px**、96→192 の 6 遷移すべてで 0。

96→192 で差 0 になるのは是正されているからではなく、**起動時の拡大率（200%）と一致する側だけ偶然合う**からである。

是正は task 5.1（源の実行時同期）と task 5.2（作業領域変化を契機とする再スナップ）が持つ。task 5.1 は `:569` の注記の撤回も伴う（設計 File Structure Plan に登記済み）。

### 3.4 L4 連鎖を解き直さない

**確定。証跡クラス＝静的構造＋実機。**

- 静的構造: `crates/areka/src/emo2_boot/frame/drain_resnap.rs:299-301`。`finalize_chain_once_with` の冒頭で `if world.get_resource::<ChainFinalized>().is_some() { return; }` ——一度確定したら以後どのフレームでも即 return する。
- 実機: 第 1 段再観測 §7。100% のとき二体の隙間が **359px**（各窓が中央保存で縮んだ幅の半分の和 = (764−382)/2 + (672−336)/2 = 191+168）。200% へ戻すと 0 に戻る。

**本採取の `chain_realigned=0` は根拠に使わない。** `kind=chain stage=realigned` の発行点は task 5.6 が入れるもので**まだ着地していない**（基準値 §4.5 の点灯確認で `chain` 0 件）。要件 8.5 が禁じる「消灯による 0」に当たる。L4 を支えるのは上の 2 つであって、この 0 ではない。

是正は task 5.6。

### 3.5 L5 同一 hwnd の 2 指令が合流しない

**確定。証跡クラス＝静的構造＋実機。**

- 静的構造: `crates/wintf/src/ecs/window/command.rs:242` の `pub fn enqueue(cmd: SetWindowPosCommand)`。直上の doc（:240-241）が「積み上げは指令を**そのまま**押し込む（先着の枠へ畳まない）。よって観測レコードの `merged_into_seq` は常に番兵＝『合流しなかった』になる」と明言する。
- 実機: `kind=enqueue` 77 件すべてで `merged_into_seq=-`。

窓ごとの書込回数の内訳（基準値 §3.2 ⑵）はこの機序の直接の帰結である。バルーンが 2 回書かれるのは、寸を変える書込（`origin=KeepPositionResize`・`flags=0x14`）と位置だけを動かす追従の書込（`origin=BalloonFollow`・`flags=0x15`）が畳まれずに別々に出るためであり、決定論の上限 1 に対して **24 件の違反**（12 遷移 × 2 スコープ）になる。

是正は task 5.3（合流・C8 の B-2a＝確定済みの採用）。

> **是正後の注記（task 5.3 着地）**: 上の `command.rs:242` とその直上の doc は**是正前の版**を指す。合流の着地で当該 doc は書き換わり、行番号も動いた（現在の合流の実体は同ファイルの `coalesce_geometry`／`is_coalescible`／`find_merge_target`／`merge_into`）。**本節の根拠は是正前の状態の記録であって、現在のコードに当てて読み直すものではない。** 是正前の赤と是正後の緑の対は task 6.3 が集約する。

### 3.6 L6 Z 書込が先行したときの 2 段書込経路

**経路の存在は確定。証跡クラス＝静的構造。ただし本採取では 1 度も発火していない。**

経路は次の 5 点で繋がる（すべて本書作成時点で読み直した）。

1. `crates/wintf/src/ecs/window/zorder_pair_maintain.rs:483` — `SetWindowPosCommand::enqueue(pair_fix_command(...))`（Z 専用の書込を積む）。
2. `crates/wintf/src/ecs/window_proc/window_pos.rs:325` — その `SetWindowPos` の内側で当該窓の `WM_DPICHANGED` が同期受理される（記録は :335-343・実機 `in_swp=true` 48/48）。
3. 同 `:387-389` — ハンドラが DPI 成分を**直接更新**し `Changed<DPI>` を発火させる。
4. `crates/areka/src/emo2_boot/frame/dpi.rs:232`（`dpi_phase_with`）— その `Changed<DPI>` を拾って窓を再射影する。
5. `crates/areka/src/placement/follow/window_move.rs:288` — 再射影が読む作業領域源は**起動時の snapshot**（L3）なので、旧下端で書かれる。

**本採取ではこの経路を通った書込は 0 件である。** モニタ表更新が `WM_DPICHANGED` より先に届いた（経路 (b)）ため、DPI 成分は既に新値で `Changed<DPI>` が二重に立たなかった。Windows は 2 つの順序を保証しないので、経路が存在する限り塞ぐ必要がある——これが設計 D15 が「dpi 相だけの防御では足りない」とした理由である。

是正は task 5.4（拡大率と表の整合待ち）。

### 3.7 L7 窓書込 1 回の内訳（**未特定だった 2 項目のうち 1 つ**）

**名指しできるところまで名指しした。証跡クラス＝実機。**（本節は**是正前**の記録である。是正後の同じ量は §10.3 ⑵ ⑶ と §10.4 を見ること——⑴ ⑵ は同じ形のまま強まり、⑷ は未特定のままである。）

#### ⑴ 一括書込の総所要は `SetWindowPos` 呼出そのものである（確定）

遷移ごとに `Σcall_us` と `flush stage=end` の `total_us` を並べると、差は 217〜358µs（総所要の 0.1〜0.2%）しかない。

| # | 起点フレーム | Σ`call_us` | `total_us` | 差 |
| --- | --- | --- | --- | --- |
| 1 | 1423 | 140,921 | 141,179 | 258 |
| 2 | 2057 | 210,049 | 210,307 | 258 |
| 3 | 2611 | 200,753 | 200,970 | 217 |
| 4 | 3223 | 166,553 | 166,843 | 290 |
| 5 | 3903 | 214,378 | 214,652 | 274 |
| 6 | 4592 | 182,227 | 182,540 | 313 |
| 7 | 38903 | 223,335 | 223,570 | 235 |
| 8 | 39461 | 179,922 | 180,228 | 306 |
| 9 | 40048 | 231,513 | 231,783 | 270 |
| 10 | 40594 | 198,480 | 198,749 | 269 |
| 11 | 41058 | 234,434 | 234,670 | 236 |
| 12 | 41813 | 263,701 | 264,059 | 358 |

**ゆえに一括書込の遅さはキューにも積み上げにも取り出しにも無い。呼出そのものである。**

#### ⑵ 呼出は 2 群に割れ、遅い群は当該窓の `WM_DPICHANGED` を内側に持つ（確定）

各 `kind=write` の呼出区間 `[t_us − call_us, t_us]` に入る `kind=msg` を数えると、77 本はきれいに 2 群へ割れる（中間の形は 1 本も無い）。

| 呼出の内側にあるメッセージ | 本数 | 最小 | 中央値 | 最大 | 合計 | 総呼出時間に占める割合 |
| --- | --- | --- | --- | --- | --- | --- |
| `WM_WINDOWPOSCHANGED` → **`WM_DPICHANGED`** → `WM_WINDOWPOSCHANGED` | **48** | 9,198 | **39,956** | 127,542 | 2,321,585µs | **94.7%** |
| `WM_WINDOWPOSCHANGED` のみ | 29 | 657 | **2,586** | 15,801 | 130,845µs | 5.3% |

中央値で **15.5 倍**である。48 = 12 遷移 × 4 窓——**各ゴースト窓は遷移ごとにちょうど 1 度、自分の `WM_DPICHANGED` を自分の最初の `SetWindowPos` の内側で受け取り、その回だけが遅い。**

#### ⑶ 遅い 48 本の内訳（部分的に確定）

その 48 本を、入れ子のメッセージ記録の時刻で 3 区間に割る。

| 区間 | n | 最小 | 中央値 | 最大 | 合計 | 48 本の呼出時間に占める割合 |
| --- | --- | --- | --- | --- | --- | --- |
| 呼出開始 → 1 つ目の入れ子メッセージ | 48 | 2,429 | **19,305** | 101,576 | 1,263,410µs | **54.4%** |
| 1 つ目 → 3 つ目の入れ子メッセージ（`WM_DPICHANGED` を挟む） | 48 | 4,000 | 15,146 | 77,155 | 948,277µs | 40.8% |
| 3 つ目の入れ子メッセージ → 呼出終了 | 48 | 502 | 1,208 | 13,651 | 109,898µs | 4.7% |

**過半（中央値 19.3ms）は 1 つ目の入れ子メッセージが届く前**にある。そこは我々のウィンドウプロシージャがまだ 1 度も走っていない区間であり、`SetWindowPos` の内側の OS 側にある。

#### ⑷ ここから先は未特定

**この 19.3ms（および 2 区間目の 15.1ms のうち我々のハンドラ以外の部分）が OS のどの処理か——DWM の再合成待ちか、レイヤ化ウィンドウの再構成か、DPI 変更に伴うウィンドウマネージャ側の同期か——は、本採取のログでは分解できない。** ハンドラの入口に記録はあるが出口に無いので、我々のハンドラの所要そのものも測れていない。**憶測で埋めない。**

分解には別の道具が要る（ハンドラ出口の記録／OS 側のトレース）。**この分解は L7 の是正候補を選ぶ前に必要とは限らない**——C8 の候補 B-2b（`DeferWindowPos` による一括バッチ）は「呼出の回数を減らす」候補であり、⑵ の 2 群構造はその採用条件（`msg` が各 `write` の内側にあり `Σcall_us` が所要の大半）を**満たしている**。採否そのものは task 7.1 の裁定であり、本書は候補表の採用条件が満たされたことだけを登記する。

### 3.8 L8 積み上げから一括書込までの間（**未特定だった 2 項目のうち 1 つ**）

**名指しできるところまで名指しした。証跡クラス＝実機。**

第 1 段が「20〜80ms・tick のどこかは未特定」とした区間である。フレーム番号が付いたので、**この区間が起点と同じフレームの内側にある**ことが確定した（tick 境界ではない）。

区間は「その遷移フレームの最後の `kind=enqueue`」から「同フレームの `kind=flush stage=begin`」まで。

| # | 起点フレーム | 最後の積み上げ `t_us` | 一括書込開始 `t_us` | 間隔 µs | うち文字層のログが並ぶ前半 | 観測行の無い後半 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 1423 | 79,677 | 157,306 | 77,629 | 47,214 | 30,423 |
| 2 | 2057 | 51,999 | 90,618 | 38,619 | 25,376 | 13,249 |
| 3 | 2611 | 46,746 | 90,247 | 43,501 | 25,850 | 17,656 |
| 4 | 3223 | 45,923 | 140,684 | 94,761 | 62,205 | 32,565 |
| 5 | 3903 | 51,994 | 116,972 | 64,978 | 26,385 | 38,598 |
| 6 | 4592 | 65,244 | 135,425 | 70,181 | 25,240 | 44,948 |
| 7 | 38903 | 47,251 | 85,124 | 37,873 | 25,948 | 11,930 |
| 8 | 39461 | 51,564 | 167,765 | 116,201 | 82,416 | 33,793 |
| 9 | 40048 | 48,385 | 79,644 | 31,259 | 20,971 | 10,288 |
| 10 | 40594 | 51,907 | 130,035 | 78,128 | 20,894 | 57,242 |
| 11 | 41058 | 47,092 | 71,988 | 24,896 | 8,518 | 16,387 |
| 12 | 41813 | 71,149 | 152,170 | 81,021 | 49,539 | 31,490 |

- 全体: **24,896〜116,201µs**（中央値 67,580）。第 1 段の「20〜80ms」と同じ帯である。
- 前半（8,518〜82,416µs・中央値 25,899）: この区間には `areka_emo_text::actor` の行が 12 遷移すべてで並ぶ（12 遷移合計で「文字層の再追従」23 行・「テキスト供給面を予約スロットへ装着した」23 行。遷移 #11 だけは片方のスコープが見送られて 1 組）。**文字層の再構築とテキスト測定である。**
- 後半（10,288〜57,242µs・中央値 30,956）: **観測行が 1 行も無い。**

**後半は未特定である。** 位置としては「文字層の処理が終わってから一括書込が始まるまで」＝ tick の残りのスケジュールと `Draw` 相と World 借用の解放までだが、そこに観測点が 1 つも無いので**どのスケジュールが何 µs かは名指しできない**。憶測で埋めない。

**前半の帰着は合成コストである。** 要件 3.5 に従い申し送る（§5）。

### 3.9 目視所見と時刻の対応——**未特定**（要件 1.7 の流儀）

開発者の目視申告 ⑴⑵（「100% へ切り替えたとき、最初のチックでは 200% のときのサイズのまま位置だけが補正され、次のチックで画像サイズが 100% へ補正される」・基準値 §6 ⑶）が、§3.8 の表のどの時刻対に当たるかは**未特定**である。

- ログの時刻順は申告と**逆**である。同じフレームの `t_us` で並べると、可視化（描画内容が新寸になる点）は **22,875〜79,621**、窓矩形の書込は **176,697 以降**（基準値 §3.4）であり、**寸が先・位置が後**である。申告は「位置が先・寸が後」と言っている。
- 確定しているのはここまで: 全書込が起点フレームの内側にあり、次フレームは 4 面とも `stage=skipped reason=k-unchanged` なので**申告の「チック」は本当の tick 境界ではない**。
- 名指しできない理由: 可視化の記録時刻と**実際に画面へ提示された時刻**は一致しない（DWM の合成と提示が挟まる）。ログはアプリ側の時刻しか持たない。

**この不一致は L1〜L8 のどの行の根拠にも使っていない。** 目視と機械判定の突合（手順書 §6.5）では「AGREEMENT: 一致」——症状の**有無**については両者が一致している（浮きと途中状態の可視）。食い違っているのは**見え方の順序**だけであり、それは判定量の側に対応する量が無い。task 7.3 で同じ申告が出たら、この行を見て「順序は未特定のまま」と扱うこと。

---

## 4. 実機専用の上限の確定（L9）

### 4.1 何が問題だったか

暫定値は両量とも **16,700µs**、根拠は「画面更新周期 60Hz 1 回分」だった（手順書 §6.2・判定器の旧定数）。これは**この実機と一致しない**。tick ループは vblank 通知で駆動される（`crates/wintf/src/runtime/tick_bridge.rs:212-230`——`:219` の `listener.await` が次 vblank notify を待つ）ので実効のフレーム周期は画面の更新周期であり、本採取の実機は **8.3729〜8.8151ms（113〜119 フレーム/秒）**である（基準値 §3.5）。

### 4.2 裁定——**採取機に依らない根拠へ替える**

選択肢は 2 つあった。

| 案 | 内容 | 採否 |
| --- | --- | --- |
| A | 採取機の実測周期（8.37〜8.82ms）を定数にする | **不採用**。リフレッシュレートの違う機械で判定が変わる。非決定量ゆえ決定論テストでは固定しない量とはいえ、上限の**根拠**は誰でも再現できる形であるべきである |
| B | 表示のリフレッシュレートの下限から置く | **採用** |

**確定値＝両量とも 16,667µs（= 1/60 秒）。**

根拠は次のとおりで、採取機に 1 つも依らない。

> 画面に提示される 1 フレームは、リフレッシュレートが 60Hz を下回らない限り**高々 1/60 秒**である。ゆえに `visualize_to_write_us` がこれを超えたなら、描画内容と窓矩形が食い違ったまま**必ず 1 枚以上の提示フレームをまたいだ**ことになる——どの機械でもそうである。これは要件 4.2「食い違いの可視フレームを提示しない」を µs の側から**一方向に**押さえた形である——**同値ではない**。超えていれば必ずまたぐ、は言えるが、超えていないことは 4.2 の成立を意味しない（上限以下の隔たりも 1 フレームに収まり**得る**だけであり、この実機では本上限は提示フレーム約 2 枚ぶんに当たる）。ゆえに上限以下は違反として立てない（偽陽性を作らない側へ倒す）。
>
> `flush_total_us` も同じである。一括書込が 1 フレームを超えると、区間の前半で書いた窓と後半で書いた窓が**別の提示フレームに現れる**——要件 4.5 が「1 回の書込」で消そうとしている逐次適用が、書込の回数を下限化しても時間の側に残る形である。ゆえに上限を**窓の枚数に比例させない**（枚数ぶん緩めると、枚数を増やすだけで合格する上限になる）。

**この上限は実測より 10〜23 倍厳しい。それでよい。** 上限は「あるべき所要」から置くものであって、実測に合わせて緩めるものではない（手順書 §6.5 の「目視に合わせて上限を緩めてはならない」と同じ規律）。実測（可視化から書込まで 174,853〜383,853µs・一括書込の総所要 141,179〜264,059µs）の上に上限を置けば、判定器は症状を 1 件も捕まえなくなる。

**採取機ではこの上限は 2 フレームぶんに当たる**（8.3729ms × 2 = 16.7ms）。つまり本採取に対しては**緩い側**の値であり、それでもなお全件が超過している。

### 4.3 前提が崩れる条件（先に書いておく）

**リフレッシュレートが 60Hz を下回る表示では 1 フレームが 16,667µs より長くなるので、この上限は厳しすぎる側へ倒れる。** そのときは実測周期を添えて本書で再裁定する。**目視に合わせて緩めない**——手順書 §6.5 の「判定 FAIL ／ 目視で跳ねなし」の行が定める手順（上限の根拠を台帳で見直す）に従うこと。

### 4.4 反映

| 反映先（**行番号は task 4.3 当時のもの**。現在の所在は L9 の行を見ること） | 変更 |
| --- | --- |
| `crates/areka/src/placement/transition_judge_verdict.rs:87` | `PROVISIONAL_VISUALIZE_TO_WRITE_US_MAX: u64 = 16_700` → `VISUALIZE_TO_WRITE_US_MAX: u64 = 16_667`（doc に §4.2 の根拠を書いた） |
| 同 `:96` | `PROVISIONAL_FLUSH_TOTAL_US_MAX` → `FLUSH_TOTAL_US_MAX`（同上） |
| 同 `:162-170` | `Bounds::signoff` の doc を「暫定値」から「確定値」へ |
| `signoff-procedure.md` §6.2・§6.6・§8 | 「暫定値 16700µs＝60Hz 1 回分」を確定値と根拠へ書き換え |

**名前から `PROVISIONAL_` を落としたのは、確定した後も「暫定」と書いてあると読み手を誤らせるからである。** テストは結線（`Bounds::signoff` がこの定数から引いていること）と「0 でないこと」だけを固定しており、**値を 1 度も書いていない**ので、名前の追随だけで緑のまま通った（C7 の「非決定量は回帰テストで値を固定しない」がそのまま効いた）。

---

## 5. 申し送り（要件 3.5）

**L8 の前半（文字層の再構築とテキスト測定・8,518〜82,416µs・中央値 25,899）は合成コストへの帰着である。本仕様では最適化しない。**

- 引受先: **`areka-P0-draw-load-parity`**。`.kiro/specs/areka-P0-draw-load-parity/brief.md` の実在を本書作成時点で確認した（W8・優先度低・開発者指示）。同 brief は既に「真の最大項は `try_tick_world` が 13 スケジュールを 120回/秒 全部回すこと」を持っており、遷移フレームで文字層の再構築が数十 ms を占める本件はその延長にある。
- 渡す数字: 本書 §3.8 の表（12 遷移ぶんの前半・後半の分割）。
- **本仕様が最適化しない理由**: 遷移 1 回の総所要（起点→最終書込 285.4〜415.4ms）に対して L8 全体は 24.9〜116.2ms であり、支配項は L7（`Σcall_us` 141,179〜264,059µs）である。文字層を速くしても L7 は 1µs も減らない。第 1 段の帰着（**遷移経路**・`reobservation-2026-08-15.md` §5）はそのまま維持される。

**L8 の後半（観測行が 1 行も無い 10,288〜57,242µs）は申し送らない。** 帰着が判っていないものを引受先へ渡すと、受け手は測る対象を持たない。追加の観測点が要る旨を本書 §3.8 に残す。

---

## 6. 未確定・宣言待ちのもの

- **要件 5.5 の「解決できない」の適用範囲は裁定済み**（2026-08-20・開発者。全文は requirements.md 要件 5 項目 5 直下の注記）。ここには実装の経緯だけを残す。
  - 警告は**2 つの矩形**に対して別々に出す。射影が**決めた位置**＝`[visibility-guard] NearestFallback`（先行仕様 `dpi-window-vanish` が配線済み）と、射影の**入力に使った矩形**＝`[visibility-guard] OffscreenPull`（本仕様 task 5.1 が配線・`crates/areka/src/placement/follow/visibility.rs:340`・判定語は同 :173）。
  - 後者を足した理由: 画面外に居た窓でも**決めた位置がモニタ内へ収まれば前者の腕に入らない**ため、画面外から最近傍モニタへ引き寄せられた事実が無観測で消えていた（是正前の実測で観測行 0・同じ捕捉に `SetWindowPosCommand::enqueue x: 600, y: 640` だけが並ぶ）。
  - 発火条件は「帰属しない**かつ**どの work area とも面積を持って交差しない」＝真に画面外に居た窓に限る。**帰属だけを条件にすると偽陽性になる**——下端吸着の正常な resize で入力矩形の中心が work area 下端にちょうど載る形（`cy=1444` が `wa.bottom=1444`）を叩く。これは実装中に既存の決定論テストが捕まえたもので、`apply_visibility_guard` の doc が予告していた事象そのものである。境界の振る舞いはレビューの変異検査で確認済み——面積 0 の辺接触は警告する／1px でも重なれば警告しない。
  - 判定語は既存 2 語（`NearestFallback`／`WorkAreaUnresolved`）と**互いに部分文字列にならない**語を選んである。既存のテストが件数で判定しているため、部分一致すると静かに壊れる。


- **手順書 §4.4 の無効化チェック（採取中にゴースト窓へ触れていない旨の宣言）は未申告**である（基準値 §1.5）。本書は合否を主張しないので支障はないが、**task 7.3 の合否判定に進む前に採取者の宣言を得ること。** 機械側の `ATOM-NO-DRAG` は消灯している観測点への grep なので根拠に使えない（手順書 §0.3・要件 8.5）。
- **L8 の後半（最後の観測行から一括書込の開始まで）は未特定のまま残っている**（§3.8）。12 遷移で 10,288〜57,242µs（中央値 30,956）を占めるが、この区間には観測行が 1 行も無く、追加の観測点なしには分解できない。**帰着が分からないので申し送っていない**——要件 3.5 の申し送りは帰着が合成コストだった場合の規定であり、帰着不明のものを引き受け先へ投げると、受け取った側は所在を特定できない区間を渡されることになる。要件 3.1・1.7 は「未特定」を終端の状態として認めている。**分解したくなったら、まず観測点を足すタスクを起こすこと**（本仕様の群 2 と同じ形）。
- **要件 5.5 の「窓がどのモニタにも属さない」は 2026-08-20 に開発者が裁定済み**であり、**未確定ではない**（本節へは所在を示すためだけに 1 行置く）。裁定＝最近傍フォールバックは解決の失敗ではなく解決であり現行挙動が正・5.5 の現状維持が効くのはモニタ表が空のときに限る・最近傍へ落ちた事実は警告として記録する。判断の軸はゴーストが触れなくなる事態を避けること。全文は `requirements.md` の要件 5 項目 5 直下の裁定の注記（同趣旨は design.md の C6 と Traceability 5.5 の行）。

- **設計 C9 の初期登記が持っていた file:line のうち 4 件が古びていた**（群 1〜3 の観測点設置で行がずれた）。本書はすべて読み直した値で登記している。

読み直した 6 件のうち、**古びていたのは下表の太字の 4 件**である（残り 2 件は設計の記載のままで正しかった——「全部ずれている」でも「全部合っている」でもないので 1 件ずつ当たるほかない）。

| 項目 | 設計 C9 の記載 | 本書で読み直した値 |
| --- | --- | --- |
| L3 | `main.rs:573-574` | 構築は :574 で**設計の記載のまま正しい**（注記は :569・挿入は :611） |
| L4 | `drain_resnap.rs:299-301` | **設計の記載のまま正しい**（ファイルは `crates/areka/src/emo2_boot/frame/drain_resnap.rs`） |
| L5 | `command.rs:164-166` | **:240-242** |
| L6 | `zorder_pair_maintain.rs:475` | **:483**（クレート名も落ちていた＝`crates/wintf/src/ecs/window/`） |
| L6 | `window_pos.rs:352-363` | **:387-389** |
| L6 | `dpi.rs:242-252` | **`crates/areka/src/emo2_boot/frame/dpi.rs:232`**（`dpi_phase_with` の入口） |

---

## 7. 判定器を触った件（task 4.3 の裁定）

**判定量を合否によらず刷らせる形を採った。**

- 何が問題だったか: `Report` の `Display` は `PASS` になった系統の量を 1 つも刷らず、違反があった量だけを実測値つきで並べていた。是正の前後で並べるのは「上限の合否」ではなく**量そのもの**なので（基準値 §7）、task 4.2 は 9 量 × 12 遷移を**生ログから手で起こす**羽目になった。
- 採った形: 遷移ごとに `量:`（遷移全体の量）・`量(参考):`（実時間の参考値）・`量(窓):`（窓ごとの 3 量を鍵つきで 1 行）・`量(見送り窓):` を**無条件に**刷る。欠けている量は番兵 `-` で刷る（空欄や 0 にすると「測っていない」と「測って 0 だった」が混ざる）。
- 対テスト: `transition_judge_verdict_tests.rs` に 2 本を足した。⑴ **2 系統とも合格する**対照の遷移で 9 量が字面で読めること（違反が 1 行も出ない入力なので、刷られているのが違反ではなく量であることが確かめられる）。⑵ 量が欠けている遷移で番兵が出て 0 に化けないこと。
- 本番の挙動は変えていない。`transition_judge` は `#[cfg(test)]` のモジュール（`crates/areka/src/placement/mod.rs:40-41`）であり、本番ビルドに 1 行も入らない（要件 3.4）。

**効果の裏取り**: 確定値を入れた判定器で本採取のログを刷り直すと、基準値 §3.2・§3.3 が手で起こした 12 遷移ぶんの表が**そのまま出る**（例: 遷移 #1 の `flush_total_us_max=141179`・`ground_diff_max=-48`・窓ごとの `visualize_to_write_us` 208,435／220,253／215,786／267,633）。task 7.3 は同じ表を機械で得られる。

---

## 8. 再判定——**結論は変わらない**

確定値（16,667µs）を入れた判定器で、task 4.2 と**同じログ**を判定し直した。

```
AREKA_TRANSITION_LOG=C:\Users\maz-o\AppData\Local\areka-diag\atom-20260820-193457\atom-signoff.log
cargo test -p areka transition_signoff -- --ignored --nocapture
```

| | task 4.2（暫定 16,700µs・判定器 `c4fadc23`） | 本タスク（確定 16,667µs） |
| --- | --- | --- |
| `[judge]` 行 | `records=617 transitions=12 unassigned=50 (malformed 0)` | 同一 |
| 決定論の違反 | **30 件**（窓ごとの書込回数 24・接地点差 6） | **30 件**（同一内訳） |
| 実機専用の違反 | **59 件**（可視化から書込まで 47・一括書込の総所要 12） | **59 件**（同一内訳） |
| `Unmeasured` | 0 件 | 0 件 |
| 合否 | FAIL（落ちるのが正しい・是正前だから） | FAIL |

**遷移 12 本すべてについて、違反行を 1 行ずつ突き合わせて完全一致することを確かめた**（上限の数値 `16700`→`16667` を伏せた比較で差分 0）。

- **決定論側は 1 件も動いていない。** 確定値は実機専用の 2 項目にしか触れておらず、`Bounds::deterministic` はその 2 項目を `None` のままにしているからである（2 系統の分離＝C7）。
- **実機専用側も件数が変わらなかった。** 実測が上限を 10〜23 倍超えているので、16,700 と 16,667 のあいだに落ちる量が 1 つも無い。**上限を確定させたことの意味は件数ではなく根拠にある**——旧根拠「60Hz 1 コマ」はこの実機の実効周期（8.37〜8.82ms）と一致しておらず、値が偶然近かっただけである。

> 参考: task 4.3 へ渡された引き継ぎは決定論側を「29 件」としていたが、同じ引き継ぎが挙げる内訳（24 + 6）は 30 である。task 4.2 が保存した `report.txt` を数え直したところ **30 件**であり、本タスクの再判定も 30 件だった。**基準値は 30 件である**（引き継ぎの 29 は数え誤り）。

---

## 9. task 7.3 への受け渡し

- 台帳の各行の「是正」の列が、そのまま是正タスクの割当である（L3→5.1／5.2、L4→5.6、L5→5.3、L6→5.4、L1・L7→C8 の候補・採用は 7.1）。
- **L2 は「0 のままであるべき」行である。** 是正後に 0 でなくなったら回帰として扱う。
- **L9 の上限は task 7.3 でも同じ値を使う。** 実機が変わって 60Hz を下回る表示になったときだけ §4.3 に従って再裁定する。
- **段階裁定は §10 で終わっている。** task 7.1 は C8 の候補表から **B-2b（`DeferWindowPos` 一括）** を採用し、B-4・B-3 は梯子の前段が未消化ゆえ本段では採らず、自前 handler 是正は条件を満たさないとして採らなかった（根拠と数量は §10.5）。L1・L7 の「7.3 の対テスト」は **task 7.2** が作る（§10.6）——**task 7.2 で着地した**（決定論で固定できたもの・実機でしか測れないもの・観測の読み方の変化は §10.6.1）。
- §3.9（見え方の順序）は未特定のまま持ち越す。是正後に開発者の申告 ⑴⑵ が消えることが、要件 9.1 の ⑶ が成立したことの根拠になる（基準値 §6 ⑶ の導出を辿ること）。

---

## 10. 是正後の再採取と段階裁定（task 7.1・2026-08-21）

**本節は task 7.1 の成果物である。**§2〜§9 は**是正前**の登記であり、§1 の流儀に従って消しも書き換えもしない。本節は「同じ量を是正後にもう一度採ると何になるか」と「C8 の候補表のどれを採るか」だけを書く。

### 10.1 採取条件と保存先

| 項目 | 値 |
| --- | --- |
| 採取日 | 2026-08-21（19:14:50 開始・自動終了） |
| コミット | `4fd2fb3c`（`release` ビルド） |
| 遷移回数 | **7**（低→高 3・高→低 4・200%↔100% の往復） |
| 実機構成 | 主モニタ 1440×900（作業領域下端 852）＋副モニタ 1 台 |
| ドラッグ | 0（`ATOM-NO-DRAG` PASS。ただし消灯観測点への grep なので単独の根拠にはしない＝§6） |
| 観測行 | `[transition]` **438 行** |
| 保存先 | `C:\Users\maz-o\AppData\Local\areka-diag\atom-71-recapture-1\`（`atom-signoff.log`／`verdict.txt`／`meta.txt`／`profile\`） |

- **保存先はリポジトリの外である**（手順書 §2.4 の受け渡し契約）。本書へは**引用と数字だけ**を転記し、生ログは複製しない。
- 採取は task 6.6（判定器の是正）の**前**のビルドで行った。6.6 は `#[cfg(test)]` の判定器だけを直しており**本番挙動を 1 行も変えていない**（§7 と同じ形）ので、このログは現行コードの挙動として有効である。保存されている `verdict.txt` は 6.6 **前**の出力なので、本節の判定量は**判定器を回し直して**採った。

```
AREKA_TRANSITION_LOG=C:\Users\maz-o\AppData\Local\areka-diag\atom-71-recapture-1\atom-signoff.log
cargo test -p areka transition_signoff -- --ignored --nocapture
  -> [judge] records=438 transitions=7 unassigned=54 (malformed 0)   FAILED
```

**FAILED が正しい。** µs の 2 系統は上限を超えたままであり、それが本節の裁定する当の残件である。

### 10.2 全 7 遷移で共通の量（是正の成立）

```
frames_to_last_write=0   writes=4   path_a_writes=0   sync_stage_writes=0
balloon_same_frame=true(pairs=2)   holds=0   chain_realigned=1
ground_diff_max=0   malformed=0   frames_indeterminate=false
```

決定論側の違反は **7 遷移で 1 件**（遷移 #5 の `mismatch_frames` が未測定・理由は §10.7 ⑶）。**L2 の「0 のままであるべき」は保たれている**（`path_a_writes=0`・`sync_stage_writes=0`）。

開発者の目視（記録票より・3 往復 6 遷移ぶん）: 接地点の浮きは **6/6 で消失**（是正前は 192→96 の 3/3 で −48px）／二体の隙間は安定（是正前は 100% で 359px）／残る違和感は「遷移中にバルーン位置が一瞬不安定」と「100% 側で定常的にバルーンがずれる」。後者は追従オフセットが k 倍されない帰結であり `areka-P0-balloon-offset-dpi` の所有（設計 D10・同 brief 追記(70)）。

### 10.3 名指しされた 3 つの量

#### ⑴ 可視化から窓書込まで（µs・窓ごとに遷移区間内の最大）

基準値 §3.3 と同じ形で並べる。

| # | 拡大率 | scope=0 char | scope=0 balloon | scope=1 char | scope=1 balloon |
| --- | --- | --- | --- | --- | --- |
| 1 | 192→96 | 232,212 | 219,701 | 241,752 | 170,375 |
| 2 | 96→192 | 228,700 | 212,514 | 234,760 | 157,183 |
| 3 | 192→96 | 218,907 | 192,072 | 242,039 | **128,888** |
| 4 | 96→192 | 141,337 | 見送り（除外・`reason=invisible`） | 208,639 | 132,522 |
| 5 | 192→96 | 222,215 | **未測定**（§10.7 ⑶） | **291,980** | 154,361 |
| 6 | 96→192 | 232,044 | 217,779 | 253,582 | 147,812 |
| 7 | 192→96 | 208,424 | 192,994 | 215,494 | 140,133 |

- 実値は **128,888〜291,980µs**。**26 件**が確定上限 16,667µs（L9）を超える。**超えなかった窓は 1 つも無い。**
- 是正前は 174,853〜383,853µs で 47 件超過（12 遷移 × 4 窓のうち 1 件が見送り除外）。**窓あたりの値は下がったが、上限に対しては 7.7〜17.5 倍のままである。**

#### ⑵ 一括書込の総所要（`flush stage=end` の `total_us`）と `Σcall_us`

| # | 起点フレーム | 拡大率 | Σ`call_us` | `total_us` | 差 | Σ`call_us`／`total_us` |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 1234 | 192→96 | 146,849 | 147,107 | 258 | 99.82% |
| 2 | 8362 | 96→192 | 186,437 | 186,649 | 212 | 99.89% |
| 3 | 14884 | 192→96 | 196,985 | 197,140 | 155 | 99.92% |
| 4 | 31750 | 96→192 | 193,165 | 193,329 | 164 | 99.92% |
| 5 | 37375 | 192→96 | 268,464 | 268,772 | 308 | 99.89% |
| 6 | 41809 | 96→192 | 208,445 | 208,661 | 216 | 99.90% |
| 7 | 57239 | 192→96 | 143,926 | 144,071 | 145 | 99.90% |

- **7 遷移すべてが上限 16,667µs を超える**（144,071〜268,772µs＝8.6〜16.1 倍）。是正前は 141,179〜264,059µs で 12/12 が超過。
- **差はキュー・積み上げ・取り出しの全部で 145〜308µs（0.08〜0.18%）しかない。** §3.7 ⑴ の結論は再採取でも同じである——**遅さは `SetWindowPos` 呼出そのものにある。**
- 1 本ごとの `call_us` は 28 本で最小 13,892・中央値 **51,666**・最大 110,965µs。**上限 16,667µs 以下で終わった呼出は 28 本中 1 本だけ**である。

#### ⑶ メッセージ受理の位置（`kind=msg` の `in_swp`／`since_flush_us`）

| 観点 | 是正前（12 遷移・遷移区間内 72 本の書込） | 是正後（7 遷移・遷移区間内 28 本の書込） |
| --- | --- | --- |
| 遷移区間内の `msg` 行 | 216（`WINDOWPOSCHANGED` 124／`DPICHANGED` 48／`DISPLAYCHANGE` 44） | 108（`WINDOWPOSCHANGED` 56／`DPICHANGED` 28／`DISPLAYCHANGE` 24） |
| うち `in_swp=true` | 168（`DISPLAYCHANGE` 以外の全部） | **84**（同左＝56+28） |
| 生ログ全体の `WM_DPICHANGED` | 48 行・**48/48 が `in_swp=true`** | 28 行・**28/28 が `in_swp=true`** |
| 呼出の内側の形 | **2 群**（`(WPC, DPICHANGED, WPC)` ×48 ／ `(WPC,)` ×24・中間形 0 本） | **1 群**（28/28 が `(WPC, DPICHANGED, WPC)`。軽い群は**1 本も残っていない**） |
| 遅い群が総呼出時間に占める割合 | 94.8%（2,321,585／2,447,953µs） | **100.0%**（1,344,271／1,344,271µs） |

`since_flush_us` は 7 遷移とも 12 行ぶん並び、一括書込区間の**ほぼ全域**を覆う。

| # | 起点フレーム | `total_us` | `in_swp` の `msg` | `since_flush_us` 最小 | 最大 | 最小／`total` | 最大／`total` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 1234 | 147,107 | 12 | 14,795 | 146,319 | 10.1% | 99.5% |
| 2 | 8362 | 186,649 | 12 | 56,713 | 185,557 | 30.4% | 99.4% |
| 3 | 14884 | 197,140 | 12 | 45,815 | 196,641 | 23.2% | 99.7% |
| 4 | 31750 | 193,329 | 12 | 51,560 | 190,223 | 26.7% | 98.4% |
| 5 | 37375 | 268,772 | 12 | 47,151 | 266,548 | 17.5% | 99.2% |
| 6 | 41809 | 208,661 | 12 | 47,361 | 208,204 | 22.7% | 99.8% |
| 7 | 57239 | 144,071 | 12 | 32,777 | 143,595 | 22.8% | 99.7% |

**一括書込区間は、開いてから閉じるまでのほぼ全域で OS 同期処理を内側に持っている。** 最初のメッセージが届くまでですら 14,795〜56,713µs（上限 16,667µs の 0.89〜3.40 倍）かかっている。

呼出 1 本を入れ子メッセージの時刻で 3 区間に割ると（§3.7 ⑶ と同じ割り方・是正後は 28 本すべてが遅い群）:

> **区間の定義**: 窓書込 1 本の呼出区間を `[t_us − call_us, t_us]` とし、**同一 `frame` の** `kind=msg` の `t_us` で 3 分割する（区間1＝呼出開始→内側の最初の `msg`／区間2＝最初の `msg`→最後の `msg`／区間3＝最後の `msg`→呼出終了）。**`frame` の一致を条件に入れること**——`t_us` はフレームごとに 0 から数え直す量なので、`t_us` だけで絞ると**後続フレームの `msg` が数値的に紛れ込む**。本採取では `WM_DISPLAYCHANGE` が各遷移の**起点の 1 つ前のフレーム**に 4 行ずつ出ており（frame 1233／8361／14883／31749／37374／41808／57238・計 28 行・`in_swp=false` 28/28）、遷移 #4 の `frame=31750 seq=0`（`hwnd=0x7609AA`・`call_us=91,641`）が frame 37374 の行を拾って区間1／区間2 の境が 38,882µs ずれた（下表は是正済み・正しくは区間1＝51,512／区間2＝38,836／区間3＝1,293）。
>
> **道具の較正**: 同じ実装で**是正前ログ**を解析し、§3.7 ⑵（遅い 48 本 9,198／39,956／127,542／2,321,585µs／94.7%・軽い 29 本 657／2,586／15,801／130,845µs／5.3%）と §3.7 ⑶ の 3 区間 12 個の統計量（2,429／19,305／101,576／1,263,410／54.4%・4,000／15,146／77,155／948,277／40.8%・502／1,208.5／13,651／109,898／4.7%）を**逐語再現**できることを先に確かめてある。3 区間の和は遅い群の Σ`call_us` と厳密に一致する（是正前 2,321,585／是正後 1,344,271）。

| 区間 | n | 最小 | 中央値 | 最大 | 合計 | 呼出時間に占める割合（括弧内は是正前の遅い 48 本） |
| --- | --- | --- | --- | --- | --- | --- |
| 呼出開始 → 1 つ目の入れ子メッセージ | 28 | 3,167 | **18,059** | 56,673 | 639,106µs | **47.5%**（54.4%） |
| 1 つ目 → 最後の入れ子メッセージ | 28 | 4,832 | 14,112.5 | 61,375 | 629,048µs | 46.8%（40.8%） |
| 最後の入れ子メッセージ → 呼出終了 | 28 | 433 | 1,788.5 | 15,375 | 76,117µs | 5.7%（4.7%） |

### 10.4 L1・L7 の現況（§3.1・§3.7 は是正前の記録として残す）

- **L1（一括書込が窓ごとに 1 枚ずつ進む）は残っている。** 連続する書込の間隔は、是正前 59 本で最小 1,683・中央値 16,562・最大 82,074µs（29/59 が上限超）だったのに対し、是正後は **21 本で最小 13,919・中央値 31,907・最大 77,389µs（20/21 が上限超）**である。合流（B-2a）が消したのは**短い間隔の側**であり、「1 枚ずつ進む」構造そのものは変わっていない。
- **L7 の ⑴ ⑵ は再採取で同じ形のまま強まった。** ⑴ Σ`call_us`／`total_us` は 99.82〜99.92%（是正前 99.82〜99.89%）。⑵ 2 群構造は**1 群へ収束**した（遅い群 28/28＝100%・是正前は 48/72＝94.8%）——合流が畳んだのはちょうど軽い群だったからである。⑶ の 3 区間の内訳は 10.3 ⑶ の表のとおり。**⑷（OS 側の内訳）は未特定のまま**であり、本採取でも分解できない（ハンドラ出口の記録が無い）。
- **合流の時間効果は、実装ノートの予告どおり小さい。** 窓ごとの書込回数は 6→4（`flush` の `count` も 6→4）、遷移区間の `enqueue` 54 本のうち **26 本が合流**して書込 28 本になった。それでも一括書込の総所要の中央値は 205,638.5→193,329µs（**−6.0%**）、遷移フレームそのものの長さは 285.4〜415.4ms→**254.3〜351.2ms** にとどまる（同じログの定常フレーム周期は **8.0929〜8.4997ms**——200 フレーム以上続く定常区間 101 本の全走査。**是正前を同じ走査規則で採り直すと 8.2133〜8.8310ms**〔同じく 101 本〕であり、`baseline-2026-08-20.md` §3.5 の 8.3729〜8.8151ms は走査規則が違う値なので直接は比べない。**遷移フレームはその約 32.3〜50.6 倍→約 29.9〜43.4 倍**である）。**回数の下限化は達成され、時間は達成されていない。**

### 10.5 候補 4 案の採否（C8 の候補表の内側でのみ決める）

設計 C8 の候補表は **B-2a 確定 → B-2b → B-4 → B-3** の**段階（梯子）**として書かれている——B-4 の採用条件は「**B-2 後も**上限超」、B-3 は「**B-4 でも**上限超が残る場合の**最後の手段**」であり、前段を消化しないと次段の条件が評価できない形になっている（設計 Traceability 4.2 の「C8 段階裁定（B-2a 確定・B-2b/B-4/B-3 段階）」も同じ順序を書く）。自前 handler 是正は梯子の外に置かれた別条件の候補である。以下、4 案それぞれについて条件と数量を突き合わせる。

| 候補 | 採用条件（C8 の表の字面） | 本採取の数量 | 判定 |
| --- | --- | --- | --- |
| **B-2a 合流** | （確定済・task 5.3 で着地） | 書込 6→4／`enqueue` 54 本中 26 本が合流／`flush count` 6→4 | **着地済み。維持**（回数の下限化と R6 の同一 tick 解決に要る） |
| **B-2b `DeferWindowPos` 一括** | `msg` レコードが各 `write` の内側に OS 同期処理（`WM_DPICHANGED` 等）を示し、`Σcall_us` が所要の大半 | ⑴ **28/28 の書込**が `(WPC, DPICHANGED, WPC)` を内側に持つ（`WM_DPICHANGED` は 28/28 が `in_swp=true`・中間形 0 本）。⑵ Σ`call_us`／`total_us` ＝ **99.82〜99.92%**（差 145〜308µs） | **条件を両方とも満たす → 採用** |
| **B-4 窓内下端中央補償** | B-2 後も `visualize_to_write_us` max が signoff 上限超 | **前段（B-2 系統）が未消化**。現況は max 291,980µs＝上限の 17.5 倍で残っている | **本段では採らない**（不要と判断したのではない。次段の候補として生かす） |
| **B-3 可視化 2 相化** | B-4 でも上限超が残る場合の**最後の手段** | B-4 が未評価なので順番が来ていない | **本段では採らない**（同上） |
| **自前 handler 是正** | `msg` と `call_us` の突合で**自前区間が主** | **主であることを支持する数量が 1 つも無い**——自前プロシージャが 1 行も走っていない区間だけで **47.5%**（639,106／1,344,271µs）（下記） | **採らない** |

**自前 handler 是正を採らない根拠（数量つき）**

1. **自前のウィンドウプロシージャが 1 行も走っていない区間だけで 47.5%** を占める（呼出開始 → 1 つ目の入れ子メッセージ＝639,106µs／1,344,271µs・中央値 18,059µs）。この区間は `SetWindowPos` の内側の OS 側であり、我々のコードは 1 度も呼ばれていない。
2. **自前ハンドラ 1 回ぶんの実測代理は中央値 2,749.5µs である。** 是正前の「軽い群」24 本（`WM_WINDOWPOSCHANGED` を 1 つだけ内側に持つ呼出）の全所要が最小 1,621・中央値 **2,749.5**・最大 15,801µs だった。これは OS 側の `SetWindowPos` 費用も込みなので**自前ハンドラの過大評価**である。是正後の 1 本は同ハンドラを 2 回通すので 2×2,749.5＝5,499µs、**中央値の呼出 51,666µs の 10.6%** にしかならない。
3. `WM_DPICHANGED` ハンドラの所要そのものは**測れていない**（ハンドラ出口の記録が無い・§3.7 ⑷）。ゆえに「自前区間は 0」とは**主張しない**（要件 8.5）。主張するのは「**主であることを支持する数量が無い**」だけである。分解したければ先にハンドラ出口の観測点を足す必要があり、それは候補表の外側の作業である。

**採否は一意である。** 4 案のうち、本採取の数量が採用条件を**肯定的に**満たすのは **B-2b の 1 案だけ**であり、B-4・B-3 は梯子の前段が未消化、自前 handler 是正は条件を満たさない。

**要件 9.3 の判定（tick 構造の大改造に当たるか）**: **当たらない。** B-2b の接触集合は `SetWindowPosCommand::flush`（裁定時点で `crates/wintf/src/ecs/window/command.rs:425`・関数は `:425-505` の 81 行。**task 7.2 の着地後は `:723-807`**）**1 関数とその補助**であり、スケジュールの構成・相の並び・tick の駆動には触れない。ゆえに 3 関心（(a) 観測基盤＋機序確定／(b) 原子性是正／(c) 作業領域追随）の分割を再裁定する必要は無い。段が B-3 まで進んだときは接触集合が `crates/areka-emo-present/src/presenter/show.rs` の可視化経路（`:375` の `mount.set_visible(world, true)`／`:381` の `mount.set_bounds(world, size)`）へ移り `recompose-budget`・`test-cage-determinism` の予算域に隣接するので、**そのときに 9.3 を再裁定する**。

### 10.6 採用の帰結（task 7.2 への受け渡し）

- **対テスト**（設計 C8 の「7.3 の対テスト」列を埋めた）: 決定論＝**一括書込が 1 バッチであること**（`flush` レコードの `count` と `write` の `in_batch`——**実際に出る語は `in_batch=true`／`in_batch=false`** である。本節の初出は `in_batch=1` と書いていたが、観測語彙の真偽は `ok=`／`in_swp=` と同じ綴りに揃えた〔§10.6.1〕。**`in_batch=1` で grep すると 0 件になる**）。**時間効果は実機のみ**（`flush_total_us`）。是正前赤／是正後緑の対は **task 7.2 が作る**（確定台帳 L1・L7 の対テストの引受先＝tasks.md の Implementation Notes の登記どおり）。
- **実装が先に読むべき静的事実 3 件**（本節を書く時点で `file:line` を 1 件ずつ読み直した）:
  1. `guarded_set_window_pos`（`crates/wintf/src/ecs/window/command.rs:129`）は `SetWindowPosGuard`（`:138`）で `SELF_INITIATED_DEPTH` を上げ、`is_self_initiated()`（`:86`）が echo 判定に使われる。**`EndDeferWindowPos` も同じガードの内側で呼ばないと**、同期送達される `WM_WINDOWPOSCHANGED` が外部権威の腕へ落ちる。
  2. `WM_WINDOWPOSCHANGED` ハンドラ（`crates/wintf/src/ecs/window_proc/window_pos.rs:41`）は手順③で `flush_window_pos_commands()` を**無条件に**呼ぶ（`:290`）。よって一括バッチの**内側で入れ子 flush が起き得る**（`flush()` の doc が `FlushEpoch` で時刻基準を復元する形を既に持つ）。本採取では入れ子 flush は 0 件（`flush stage=begin` 7／`end` 7・すべて `count=4`）。是正前は 13/13 で、うち 1 本が `count=1` の入れ子だった。
  3. Z 専用指令は合流の対象外である（`is_coalescible` の 3 連言・`command.rs:229`／`REQUIRED_FOR_COALESCE` `:223`／`COALESCIBLE_FLAGS` `:216`）。`DeferWindowPos` へ移すときも **Z 専用は per-window flags で同居させ、適用順と結果を変えない**（要件 10.3）。
- **上限に届く見込みについて先に書いておく**（7.2 が数字を持たずに走らないため）。1 本目の `SetWindowPos` の内側で最初のメッセージが届くまでですら **14,795〜56,713µs**（上限の 0.89〜3.40 倍）かかっており、呼出 1 本の中央値は 51,666µs（3.10 倍）である。**B-2b が 4 本を 1 バッチへ畳んでも、上限 16,667µs へ届く保証は本採取の数量からは出ない。** 届かなければ梯子は B-4 へ進む——それが C8 が段階裁定として書かれている理由である。**B-2b の採用は「これで 4.2 が成立する」という主張ではなく、「梯子の次の段の条件が満たされた」という主張である。**
- **申し送り**: `areka-P0-draw-load-parity`（`.kiro/specs/areka-P0-draw-load-parity/brief.md` の実在を本節作成時点で確認）へ **追記(74)** で登記した。C8 の申し送り列が並記する `ghost-window-zorder` は `.kiro/specs/completed/` にあり**申し送りを消化できない**ため、その受け先も同 brief が兼ねる（task 6.4 が追記(71) ⑷ で確定済み）。

### 10.6.1 task 7.2 の着地（B-2b の実装と、決定論で測れた／測れなかったもの）

**採用した 1 案だけを実装した**（B-4・B-3・自前 handler 是正には触れていない）。**挙動を変える接触集合**は §10.5 の宣言どおり `crates/wintf/src/ecs/window/command.rs` に留まる——`SetWindowPosCommand::flush`（現 `:723-807`）と、その補助として新設した `window_pos_args`（`:282`・引数を組む純関数）・`apply_as_batch`（`:386`）・`apply_sequentially`（`:453`）・`BatchDegrade`（`:299`）・`begin_batch`（`:349`）・`log_window_write`（`:188`）である。観測側は `write` レコードへ `in_batch` を 1 つ足した（`crates/wintf/src/ecs/window/transition_diag.rs`）。**触ったファイルは 11 本**（語彙 1 フィールドの追加に発行点・判定器の任意フィールド表・行を組む既存テスト 3 本が追随する）で、内訳は設計の Modified Files にある——「`command.rs` 1 ファイル」ではない。

- **決定論で固定できたもの**（`crates/wintf/src/ecs/window/command_batch_tests.rs` の 8 本）
  1. **1 区間 1 バッチ**——実窓 3 指令が `flush stage=begin count=3` ／`stage=end count=3` の 1 組だけを作り、3 本とも `in_batch=true`。**同じテスト本体の内側に縮退した側**（バッチ確保を倒した対）を置いてあるので、札が定数で真でも緑にならない。
  2. **引数と並びが変わらないこと**（要件 10.3）——`window_pos_args` が 7 項目を逐語で運ぶこと（Z 専用の `hwndInsertAfter`・表示フラグ・観測タグが引数へ漏れないこと）と、キューの並びが写像で保たれること（Z 専用指令の位置を含む）。既存の合流の檻 21 本は**1 行も変えずに緑**である。
  3. **Z の最終順序が投入形に依らないこと**（要件 10.3）——実窓 2 組へ同形の Z 指令列を流し、バッチ側と 1 本ずつ側の相対順序が一致することを実測で固定した。
  4. **自発書込の判定**（要件 10.2）——`EndDeferWindowPos` が同期送達する `WM_WINDOWPOSCHANGED` の内側で `is_self_initiated()` が真であることを、実窓の wndproc から直に読んだ（ガードの外では偽になる較正を同じ本体に置いた）。
  5. **縮退が無音でないこと**——偽ハンドルで `DeferWindowPos` が失敗すると 1 本ずつへ落ち、理由語つきの `warn!` と `in_batch=false` が両方残る。
- **是正前赤／是正後緑の対**（Feature Flag Protocol で実測）: バッチ投入を切った状態では `a_flush_of_real_windows_is_submitted_as_one_batch_and_says_so`（「1 バッチで投入した書込が in_batch=true を名乗っていない」——行は `… call_us=224 ok=true in_batch=false`）と `an_invalid_handle_discards_the_batch_and_degrades_to_one_write_per_command`（切り方によって 「`DeferWindowPos` の失敗が記録されていない」または「確保は成功しているのに確保失敗の理由が出ている」）と `the_write_log_line_is_emitted_once_per_command_on_both_paths`（「バッチ経路の行が呼んだ API を名乗っていない」——`left: 0 / right: 2`。**バッチ投入を忠実に切れば `via="DeferWindowPos"` の行は必ず 0 になるので、切り方に依らずこの 1 本は赤になる**）の **3 本が赤**、投入を入れると **8 本とも緑**。残る 5 本（引数の逐語運搬・並びの写像・理由語・自発ガード・Z 順序）は**是正が行き過ぎる向きを見張る側**なので是正前も緑である。
- **実機でしか測れないもの（task 7.3 の持ち分）**
  - **時間効果**（`flush stage=end` の `total_us`）。**速くなる保証は §10.6 のとおり数量から出ない**——本タスクは「1 バッチであること」までしか主張しない。
  - **遷移の実配線での適用順**（実 DPI 遷移で 4 窓・Z 指令混在の並びが実機で崩れないこと）。決定論側は 3 窓の Z 指令で相対順序の一致までを押さえている。
  - **バッチが実機で本当に成立していること**——`in_batch` が 1 件でも `false` なら縮退しており、`total_us` の是正前後比較は形の違う 2 つを比べていることになる。手順書 §6.3 に起こし方の行を足した。
- **観測の意味が 1 つ変わった（7.3 が読むときの注意）**: バッチでは `write` の `call_us` は**投入（`DeferWindowPos`）だけ**の所要であり、窓が実際に動く時間は 1 区間ぶんまとめて `flush stage=end` の `total_us` に入る。ゆえに **§3.7 ⑵⑶ が使った「各 `write` の呼出区間 `[t_us − call_us, t_us]` の内側に `msg` があるか」という読み方は是正後のログには当てはまらない**（`msg` は `End` の内側に固まり、書込レコードは適用の**後**にまとめて出る）。是正前後で直接比べられるのは `total_us` と `in_batch` である。
- **変異検査**（本番は sha256 照合でバイト単位に復元済み）: ⑴ バッチ全体のガードを外す → 自発判定の 1 本**だけ**が赤。⑵ **バッチへの積み上げを逆順にする** → Z 順序の 1 本だけが赤（実測 `[1,2,0]` 対 期待 `[2,0,1]`＝手計算と一致）。⑶ `hwndInsertAfter` を `None` へ落とす → Z 順序の 1 本だけが赤。⑷ 縮退しても `in_batch=true` を名乗らせる → バッチ／縮退の 2 本が赤。⑸ 実施ログを積み上げ時点へ戻す → O9 本数の 1 本だけが赤（`left: 3 / right: 2`）。
- **⚠ 檻の作り方で 1 度躓いた（申し送り）**: Z 順序の檻の**最初の形は並べ替えに鈍感**で、⑵ の逆順変異が**生き残った**。原因は指令列（「0 を 2 の後ろへ」「1 を 0 の後ろへ」）が初期順 `2,1,0` に対して順序を問わず同じ終状態へ収束する形だったこと。是正では ⒜ 助走で相対順を `0,1,2` へ**明示的に揃え**（生成順に依る初期 Z を当てにしない）、⒝ **逆順にすると終状態が変わる**指令列へ替え、⒞ 比較を A/B だけでなく**手計算した期待値**にも当てた（A/B だけだと両方が同じ向きに壊れたときに緑になる）。**書込レコードの `seq` は順序の証拠にならない**——レコードは適用の後にキューの順で出るので、投入順を入れ替えても `seq` は並んだままである。
- **`in_batch` を必須フィールドにしなかった理由**: 本フィールドは task 7.2 で増えたので、**是正前に採取した実機ログには 1 行も無い**。`WRITE_FIELDS` へ入れると判定器が §3・§10 の逐語ログを軒並み「壊れた行」として数え、確定済みの証跡を後から否定する。よって発行側に `WRITE_OPTIONAL_FIELDS` を新設し、判定器は `optional_fields` で受け入れる。**発行側が必ず載せること**は逐語テスト（`transition_diag_tests::write_line_is_verbatim`）が固定する。
- **窓書込の実施ログの字面は変えていない**: `[guarded_set_window_pos] Calling SetWindowPos` は `dpi-window-vanish` の診断手順書の観測点 **O9** であり、`ghost-window-zorder` の維持系の檻 **16 本**（`zorder_pair_*_tests.rs` の `SET_WINDOW_POS_TAG`）が本数を数えている固定トークンである。実装中にこの 16 本が**実際に赤へ倒れて**判明した（バッチ経路が別の字面を出していたため）。是正は字面を戻し、実際に呼んだ API を `via=`（`SetWindowPos`／`DeferWindowPos`）という新フィールドで名乗らせる形にした。
- **⚠ レビュー第 1 巡が実測で見つけた欠陥＝実施ログ（O9）の二重計上**（是正済み）。実施ログを**積み上げ（`DeferWindowPos`）の時点**で出していたため、`Defer` が失敗してバッチが破棄され 1 本ずつへ書き直されると、**同じ指令が 2 度「書いた」と名乗って**いた（レビュアー実測: 実窓 2 指令＝2 行に対し、偽ハンドル 2 指令＝**3 行**。`EndFailed` なら 2N 行）。この行の本数は診断手順 O9 と `zorder_pair_*_tests.rs` の 16 本が測っている量であり、既存 16 本は実窓なので**今日は赤にならない＝静かに壊れる形**である。**是正は案 ⒜（ログを消すのではなく、実施していない書込を記録しない）**——`apply_as_batch` は `EndDeferWindowPos` が成った後に指令の順でまとめて出す。これで「O9 の行数＝指令数」が Begin 失敗・Defer 失敗・End 失敗・成功のすべてで成り立つ（`End` 失敗は部分適用があり得るが、最終状態を決めるのは縮退の書き直しなのでそちらの N 行が正しい）。対テストは `command_batch_tests::the_write_log_line_is_emitted_once_per_command_on_both_paths`（3 経路の本数と `via=` の内訳を数え、`via=` の絞り込みが恒真でないことを同じ本体で較正する）。変異で load-bearing を確認済み——ログを積み上げ時点へ戻すと本檻**だけ**が赤（`left: 3 / right: 2`＝レビュアーの実測と逐語一致）。
- **`in_batch` の綴りは `true`／`false`**（初出は **§10.6:502**——§10.5 は `in_batch` の語を 1 度も含まない）。観測語彙の真偽は `ok=`／`in_swp=` が既に `true`／`false` であり、2 つ目の綴りを持ち込まない。追随させた先は **⑴ 設計 C8 の候補表 B-2b 行・⑵ 本節・⑶ §10.6:502 の受け渡し文**の 3 つである（⑶ は task 7.2 のレビュー第 1 巡の指摘で追加した——旧綴りのまま残すと、7.3 の採取者が `in_batch=1` で grep して0 件を得て「バッチが成立していない」と誤読する）。

### 10.7 訂正と申し送り 3 件（⑴ ⑵ は記録票の誤り・⑶ は裁定の前提が崩れた件）

1. **記録票 `meta.txt` の「違反の内訳（計 33 件）」の総数は誤りである。** 33 は µs 2 系統（26+7）の小計であり、列挙されている実数は **47 件**（26+7+12+1+1）である。task 6.6 の是正後に判定器を回し直すと `balloon_same_frame` の未測定 12 件が消えて **35 件**（26+7+1+1）になる。
2. **同じ表の「可視化から書込まで … 実値 140,133〜291,980µs」の下限は誤りである。** 26 件の最小は **128,888µs**（遷移 #3・`scope=1 balloon`）で、140,133 は遷移 #7 の同じ窓の値である。保存されている `verdict.txt`（6.6 前）にも 128,888 が刷られており、読み違いである。
3. **未測定 2 件が出た遷移 #5 の理由は `reason=k-unchanged` であり、`invisible` ではない。**（本項の出所は**記録票ではない**——`meta.txt` に `invisible` の語は 1 度も現れない〔`grep -c invisible meta.txt` ＝ 0〕。出所は生ログと `verdict.txt` である。`invisible` による見送りが起きたのは遷移 **#4** で、`verdict.txt:104` の「量(見送り窓): [scope=0 win_kind=balloon]」がそれに対応する。）生ログの `frame=37375` には `surface stage=skipped target_id=1 reason=k-unchanged` があり、`invisible` は 1 行も無い。機序としては、**#4 で不可視ゆえ見送られた窓が、次の遷移では「再導出した k が前回適用値と等しい」ため `k-unchanged` を出している。**

   > **裁定は既に在る。崩れたのはその前提である（引受先＝task 7.3）。** 要件 4.6 の裁定の注記は、この近似を**自分で予告している**——「実装は理由語による近似である: 判定器は『遷移時点かどうか』を直接判定せず、`reason=invisible` の行だけを除外する。遷移時点に出た `k-unchanged` … は上の時間的な条件では 4.6 の窓に当たるが、実装では除外されない」。ゆえに**未裁定ではない**（判定器は裁定どおりに動いている）。
   >
   > **崩れたのは、その予告が近似を許した根拠のほうである。** 同じ注記は「**書込の無い窓は判定対象へ入らないため今日の合否は変わらない**」と書いているが、遷移 #5 の `scope=0 balloon` は `writes=1` を持ちながら同フレームに `stage=visualize` が無く、**未測定 2 件として合否に立った**。「書込が無い」という前提が本採取で成り立たなかったのである（要件 4.6 直下の注記・第 3 ポツ）。
   >
   > **引受先は手順書側＝task 7.3 に一本化する。** 7.3 は `signoff-procedure.md`／`mechanism-ledger.md` を担当ファイル集合に持つ（tasks.md の Implementation Notes「【task 7.3 への申し送り】警告文を grep 語へ足す」と同じ形）。**task 7.2 へは渡さない**——7.2 は「7.1 で採用した 1 案のみ」を実装する枠であり、同 Notes が既にその旨を登記している。**判定器そのものを直すという裁定になったときは、task 6.6 と同じ形で新規タスクの起票が要る**（本タスクは `tasks.md` を編集しないので、その判断は親コントローラへ報告する）。
   >
   > **本件は §10.5 の採否を動かさない。** 当該窓を除外しても可視化から書込まで 26 件・一括書込の総所要 7 件は変わらず、未測定 2 件が消えて 35→33 件になるだけである。**本タスクは判定器も本番コードも 1 行も変えない**（7.1 は裁定と登記のタスクである）ので、ここでは登記に留める。

---

## 11. 判定に載らない症状（task 7.3・2026-08-22）

**本節は task 7.3 の実機サインオフの成果物であり、手順書 §6.5 の表が命じている登記である**（7.3 当時の 2 行の表では「判定 PASS ／ 目視で跳ねあり」の行。**3 行目が新設された今は同表の 3 行目**＝§11.5 の末尾）。 出所はすべて `atom-73-signoff-1`（`C:\Users\maz-o\AppData\Local\areka-diag\atom-73-signoff-1\`・生ログ 2,560 行・観測行 538・遷移 8・コミット `06ea39f0`・`release`）。**§10 に既にある数量は再掲しない**——本節は「§10 の数量に対して 7.3 の採取が何を足したか」だけを書く。

### 11.1 症状（開発者の実機目視・2026-08-22）

**拡大率を切り替えたとき、1 フレームだけ「絵は旧寸のまま、窓の位置だけ新しい」状態が画面に見える。**

開発者の原文（要旨）: 「描画で 1 フレーム分だけ元の画像サイズで位置だけ補正されてるように見える。多分位置補正が最初のフレームで走って、次のフレームで画像サイズ補正が走ってる」。所感は「まあこれはよい」。

これは要件 4.2（窓矩形と描画内容が食い違う中間フレームを可視化しない）が名指しで禁じている形そのものである。

### 11.2 判定器が `mismatch_frames=0` を出す理由——**測っている量が違う**

判定器は 8 遷移 32 窓すべてで `mismatch_frames=0` を出し、`deterministic` は 8 遷移とも PASS である（`verdict-clean.out`）。目視と機械のこの食い違いは**矛盾ではない**。

- 判定器の `mismatch_frames_per_window` は、**アプリ側の tick 内順序**を測る量である——同じ `frame=` を持つレコード列の内側に、当該窓の `kind=surface stage=visualize`（新しいサーフェス寸）と `kind=write`（新しい窓矩形）の両方が揃っているか。揃っていればフレーム差は 0 になる。
- 目視が見ているのは**合成器が画面へ出す順序**である。1 つの tick の内側で「絵を新寸にする」と「窓を動かす」の間に隙間があれば、その隙間のあいだ画面には**両者が食い違った状態**が出る（どちらが先に見えるかは §11.3 のとおり未特定である。ログはアプリ側の時刻しか持たず、DWM の合成と提示が挟まるため）。
- **同じ tick で両方が起きていること**（判定器が 0 と出す条件）と、**その 2 つが同じ提示フレームに載ること**（目視が要求する条件）は別の命題である。前者は後者を含意しない。

**ゆえに判定器に欠陥は無い。** `mismatch_frames` はフレームを単位とする決定論量として正しく動いており、上限も正しく当たっている。捉えていないのは、目視が見ている量が **フレーム単位ではなく µs 単位の隙間**だからである——そしてその µs の隙間は `signoff` 側の `visualize_to_write_us` が**現に測っており、現に上限を超えて FAIL を出している**（§11.3）。**2 つの系統は別々に評価して両方を並べる**という手順書 §6.2 の規律が、まさにこの形のために効いた。

### 11.3 数量の裏づけ（7.3 が足したもの）

生ログから判定器の実装に依らない別実装で起こし、判定器の出力と**逐語一致**することを確かめてある（記録票 `meta.txt` §6・32 窓と 8 遷移の総所要が 1 桁も違わない）。

| 量 | 遷移 #1（絵と窓の時刻） | 8 遷移全体 |
|---|---|---|
| 絵が新寸になる（`kind=surface stage=visualize` の `t_us`） | 22,297〜48,845 | 22,297〜73,104（32 件） |
| 窓が動く（`kind=write` の `t_us`） | 272,491〜272,551 | 253,890〜339,998（32 件） |
| **その隙間**（＝`visualize_to_write_us`） | **223,706〜250,194µs（約 224〜250 ミリ秒）** | **210,329〜306,301µs**（上限 16,667µs の **12.6〜18.4 倍**） |

- **上限以下で終わった窓は 32 窓中 1 つも無い。**
- 隙間は同一フレームの内側にある（だから `mismatch_frames` は 0 になる）。**0.21〜0.31 秒は 60Hz の提示フレーム 12.6〜18.4 枚ぶんである**——L9 の根拠（§4.2）をそのまま当てれば、描画内容と窓矩形が食い違ったまま**必ず 1 枚以上の提示フレームをまたいだ**ことになる。要件 4.2 が禁じる形である。
- **ただし「どちらが先に見えるか」は本採取でも未特定である**（§3.9 の行をそのまま引き継ぐ）。ログの時刻順は **寸が先・位置が後**（可視化 22,297〜73,104 に対し窓書込 253,890〜339,998）だが、開発者の申告は **「元の画像サイズのまま位置だけ補正されている」＝位置が先・寸が後**であり、**§3.9 が記録した不一致が 7.3 でも同じ向きで再現した**。同 §3.9 は「task 7.3 で同じ申告が出たら、この行を見て『順序は未特定のまま』と扱うこと」と定めているので、本節も**見え方の順序を断定しない**。名指しできない理由も同じ——可視化の記録時刻と実際に画面へ提示された時刻は一致しない（DWM の合成と提示が挟まる）。**ログはアプリ側の時刻しか持たない。**
  - **この不一致そのものが §11.2 の裏づけでもある**: 判定器が測る tick 内順序と、目視が見る提示順序は、実測で**向きすら一致しない**。両者が別の量であることを、これ以上ないほど直接に示している。
- **接地点差は 32 窓とも 0**（`kind=ground` 17 件すべて `diff=0`）なので、この隙間は要件 5 の側では見えない。要件 4.2 だけが名指しできる形である。
- **§3.9 との差分（7.3 が足したもの）**: §3.9 は「AGREEMENT: 一致——症状の**有無**については両者が一致している」と書いていた。**7.3 では AGREEMENT が食い違いへ転じた**——是正によって機械側の `mismatch_frames` が 0（かつ `deterministic` が PASS）になった一方、目視の症状は残ったからである。**是正が症状の一部を消したことで、残った症状を機械が名指しできなくなった**のが 7.3 の新事実である。名指ししているのは `signoff` 側の `visualize_to_write_us` だけであり、それは提示順序ではなく隙間の**長さ**を測る量である。

### 11.4 引受先

**`areka-P0-present-write-coherence`**（`.kiro/specs/areka-P0-present-write-coherence/brief.md`・2026-08-22 起票・W8・優先度低）。本節の作成時点で brief の実在を確認した。

- 出発点は設計 C8 の候補表の梯子の**次の段**（B-3 可視化 2 相化／B-4 窓内下端中央補償）であり、候補の中身・接触集合・対テストの形はすべて本仕様の設計に既に書かれている。
- 判定器・観測語彙・サインオフ手順書は本仕様が着地させたものをそのまま流用でき、新設は不要である。機械側の完了条件も既存の `visualize_to_write_us ≤ 16,667µs` がそのまま使える。
- **開発者裁定（2026-08-22）**: 「大改造が必要なら無理に治さなくて良い」。拡大率の切り替えは滅多に起こらない事象であり、M1 の完成を妨げない。

> **⚠ 引受先 brief の数量を訂正した（本節が正本・2026-08-22・task 7.3 で反映済み）。** brief の「Current State」の表は本採取を引いていたが、次の 5 点が実測と食い違っていた。**本タスクで brief 側を実測へ差し替え、あわせて数量の正本が本節であることを brief の同節へ明記した。** 以後この表を更新するときは本節 §11.3・§11.6 と突き合わせること。
>
> | brief の記載 | 実測（本節・`atom-73-signoff-1`） |
> |---|---|
> | 絵が新寸になる `22,297 … 48,845`（遷移 8 回の値として） | それは**遷移 #1 だけ**の域。8 遷移では **22,297〜73,104** |
> | 窓が動く `272,491 … 272,551`（同上） | 同じく遷移 #1 だけ。8 遷移では **253,890〜339,998** |
> | `visualize_to_write_us` `217,291 … 306,301µs`（上限の 13〜18 倍） | 下限は **210,329µs**（遷移 #7・`scope=1 char`）。倍率は **12.6〜18.4 倍** |
> | `flush_total` の平均 `192,247 → 190,720µs（−0.8%）` | 是正後の平均は **188,711µs**（**−1.8%**）。8 遷移の総和 1,509,687µs ÷ 8 |
> | 早い窓の `visualize_to_write_us` の平均 `201,478 → 262,920µs` | 是正後の平均は **255,345µs**（32 窓の総和 8,171,034µs ÷ 32）。是正前 201,478µs は §10.3 ⑴ の 26 件と一致するので**そちらは正しい** |

**引受は成立した（2026-08-22）。** task 7.3 の GO/NO-GO で開発者は **GO** を採り、`ATOM-SIGNOFF: FAIL` のまま 7.3 を完了とした。その根拠の 1 つが本節の引受先が実在することである（`deferral-requires-verified-owner` の規律＝ウェーブ名は担当者ではない）。**FAIL は書き換えていない**——以後この採取を引くときは「FAIL だが GO」と読む。裁定の全文は要件 8 の注記と記録票 §9。

### 11.5 手順書 §6.5 の表が持っていない穴——**本採取がその初例である**

手順書 §6.5 の食い違いの表は、task 7.3 の時点では 2 行しか持っていなかった（**この穴は task 7.4 が塞いだ＝本節末尾**）。

以下は **7.3 時点の 2 行の表の要旨**である（列見出しも文言も原文どおりではない。原文と 7.4 の是正後の形は手順書 §6.5 を見ること）。

| 食い違いの向き | 表が書く「意味」 | 表が命じる行動 |
|---|---|---|
| 判定 PASS ／ 目視で跳ねあり | **判定量が症状を捕まえていない** | 上限か判定量の側を疑い、確定台帳へ「判定に載らない症状」として登記する |
| 判定 FAIL ／ 目視で跳ねなし | 上限が厳しすぎるか、観測が壊れている | 上限の根拠を確定台帳で見直す（目視に合わせて緩めない） |

**上の行は「判定器の側に非がある」ことを前提に書かれている**——「意味」の列が `判定量が症状を捕まえていない` と断定し、行動の列が `上限か判定量の側を疑い` と命じている。**この表は、両者が別の量を測っているだけで判定器にも上限にも欠陥が無い場合の扱いを持たない。**

本採取がその形の**初例**である。実際に起きたことは次のとおりで、表のどの行にも正確には当てはまらない。

1. `deterministic` の `mismatch_frames` は **PASS**——フレームを単位とする決定論量としては正しく 0 である。
2. `signoff` の `visualize_to_write_us` は **FAIL**——目視の症状に対応する µs の量は、**捉えたうえで上限超を宣言している**（40 件の違反のうち 32 件がこれ）。
3. ゆえに「判定量が症状を捕まえていない」は**偽**である。捕まえているのは `signoff` 側であり、`deterministic` 側は最初から別の量を測っている。
4. それでも §6.5 の合否規約は正しく働いた——**AGREEMENT は食い違いであり、`ATOM-SIGNOFF` は FAIL である**（記録票 §8）。表の**行動**（台帳へ登記する）も本節として履行した。欠けているのは**「意味」の列の語彙**だけである。

**登記する事実**（7.3 当時の 2 行の表について言う。**この穴は 7.4 が塞いだ**＝本節末尾）: 手順書 §6.5 の表の上の行は、原因として「判定器の欠陥」しか語彙を持たない。**第 3 の向き＝「機械も目視も正しく、測っている量が違う」**が実在し、それが本採取である。この向きでは ⑴ 判定器を直してはならない（正しく動いている）⑵ 上限を緩めてもならない（§6.5 の禁止）⑶ 採るべき行動は**別の量を測る系統がどれかを名指しし、その系統の未達として引受先へ渡すこと**である。本採取ではそれが `signoff` 側の `visualize_to_write_us` であり、引受先が §11.4 である。

> **task 7.3 では §6.5 の表を変更しなかった**（手順書の他の節には §11.7 のとおり §3.5 を足しているので、「手順書に一切触れていない」ではない）。表へ 3 行目を足すのが正しい是正だが、それは**合否の判定規約そのものの改訂**であり、⑴ 新しい向きに何という語を当てるか ⑵ その向きで `AGREEMENT` を何と書くか ⑶ 判定器に手を入れずに済むか、の 3 点を決める裁定が要る。7.3 の持ち分（記録と未達の登記）を超えるので独立したタスクとして起票した——それが **task 7.4** である（task 6.5 が同じ形で新規起票されたのと同じ作法）。なお §11.7 で足した §3.5 は、`tasks.md:346` が 7.3 を名指しして命じた作業であり、判定規約の改訂ではなく grep 語の追加である（要件 8.1）——性質が違う。

**task 7.4 が閉じた（2026-08-22）。採った裁定は次のとおりで、手順書 §6.5 の表は 4 行になっている**（食い違いの向き 3 つ ＋ 食い違いではない組み合わせ 1 つ）。

| 決めるべき点 | 裁定 |
|---|---|
| ⑴ この向きに当てる語 | **「判定 FAIL（実機専用系統だけ）／ 決定論系統は PASS ／ 目視で跳ねあり」**。向きを決めるときは合否を 1 語へ潰さず、**系統ごとの PASS／FAIL** を見る（手順書 §6.5 の表の直前に明記した） |
| ⑵ その向きで `AGREEMENT` を何と書くか | **`食い違い` のまま。§6.6 の語（`一致\|食い違い`）を増やさない。** 機械が症状を名指しできたかどうかは目視と噛み合っておらず（決定論系統は 0 を出す）、しかも合否の結論は既に両者とも不合格の側で揃う。語を増やすと §6.6 の 7 行・記録票・要件 8 の注記を同時に直すことになり、**確定済みの 7.3 の採取結果を後から書き換えてしまう** |
| ⑶ 判定器に手を入れずに済むか | **済む。** 両系統とも正しく動いており、症状に対応する量は実機専用系統が現に FAIL として宣言している。3 行目が足すのは**説明と行動の語彙だけ**で、判定の挙動も上限 16,667µs（L9）も 1 つも変えない |
| ＋ レビューが掘り当てた 4 つ目の組み合わせ | **「決定論系統が FAIL ／ 目視で跳ねあり」がどの行にも入らなかった**（3 行目までは「決定論系統は PASS」を条件に持つため）ので、**4 行目として明記した**。これは食い違いの 4 つ目の向きではなく、**食い違いでないこと**（`AGREEMENT: 一致`）を明示する行である。実在の形である根拠は 2 つ——⑴ 手順書 §6.2 の出力例が決定論違反と実機専用違反を同時に並べている ⑵ **task 4.2 の基準採取が現にこの形で、記録した `AGREEMENT` は `一致` だった**（接地点差 −48px の決定論違反と目視の跳ね・浮きが同居。`ground_diff_abs_max` は `Bounds::deterministic` だけが当てる上限であることを `transition_judge_verdict.rs` で確認した）。**3 行目の条件を「症状に対応する量では違反していない」へ緩めると、その 4.2 の確定記録を後から食い違いへ書き換えてしまう**ので採らない |

あわせて **1 行目を排他にし、表全体を網羅にした**——分岐は手順書 §6.5 の 3 つの問いを上から順に当てて決まる（⑴ 目視で跳ねが無いか ⑵ 決定論系統が 1 件でも違反を立てているか ⑶ 症状に対応する量で違反を立てた系統があるか）。1 行目が「判定量が症状を捕まえていない」と断定できるのは、**決定論系統が PASS で、かつ症状に対応する量で違反を立てた系統が 1 つも無い**ときだけである。3 行目が命じる行動は**症状に対応する量を持つ系統とその量の名前を記録票で名指しし、その系統の未達として引受先へ渡すこと**であり、本採取ではそれが実機専用系統の `visualize_to_write_us` と §11.4 の引受先である。**task 7.3 の合否（`ATOM-SIGNOFF: FAIL`・`AGREEMENT: 食い違い`）は 7.4 でも変えていない。**

> **7.3 の記録票との読み合わせ（7.4 で確認した）。** 記録票 `meta.txt` §7 の追加所見 ⑵ は、本件を「手順書 §6.5 の表の**上の行**（判定 PASS ／ 目視で跳ねあり）に当たる」と書いている。これは**当時の 2 行の表に照らした分類**であり、3 行目が新設された今は**3 行目に当たる**。**どちらに当てても合否は動かない**——記録票 §8 の `ATOM-SIGNOFF: FAIL`・`AGREEMENT: 食い違い`・`SIGNOFF-BOUNDS: FAIL (違反 40 件)` はいずれも 3 行目の規約でも同じ値であり、命じられる行動（台帳への登記・未達を引受先へ渡す）も §11.4 で既に履行済みである。**記録票（リポジトリ外・7.3 の確定記録）は 7.4 では書き換えていない。**

### 11.6 B-2b（`DeferWindowPos` 一括）の効果の再確認——**回数と同時性は達成、時間は未達**

§10.4 は B-2a（合流）について「回数の下限化は達成され、時間は達成されていない」と結論した。**B-2b についても同じ形である。** 両採取の生ログを同じ規則で走査して比べる（7.1 ＝ `atom-71-recapture-1`・B-2b **前**・7 遷移／7.3 ＝ `atom-73-signoff-1`・B-2b **後**・8 遷移）。

| 量 | B-2b 前（7.1・7 遷移） | B-2b 後（7.3・8 遷移） | 効果 |
|---|---|---|---|
| **1 遷移内の書込の散らばり**（`last_write_t_us − first_write_t_us`） | **93,152〜157,684µs** | **40〜101µs** | **約 1,500〜2,000 分の 1**。4 窓が同時に動くようになった |
| 一括書込の総所要（`flush stage=end` の `total_us`） | 144,071〜268,772µs（平均 192,247・中央値 193,329） | 143,231〜231,910µs（平均 **188,711**・中央値 182,684） | **平均 −1.8%。実質変わらない** |
| バッチ投入（`in_batch=`） | 当該フィールドが存在しない（7.2 で新設） | `true` **37 件** ／ `false` **0 件**・縮退 WARN 0 件 | **実機でバッチが成立している**（縮退していないので、上の `total_us` は同じ投入形どうしの比較である） |
| 窓ごとの `visualize_to_write_us` | 128,888〜291,980µs（平均 201,478・26 件） | 210,329〜306,301µs（平均 **255,345**・32 件） | **平均で +27%。上限には近づいていない** |

読み方:

- **B-2b が消したのは「窓が 1 枚ずつ順に動く」ことである。** §10.4 が「合流（B-2a）が消したのは短い間隔の側であり『1 枚ずつ進む』構造そのものは変わっていない」と書いた L1 の残りを、B-2b が潰した。1 遷移内の散らばりは §10.4 が記録した**間隔 1 本の最小値 13,919µs をも 2 桁下回る** 40〜101µs である。要件 4.3（随伴の同一フレーム性）と 4.4（有界フレーム数）は 8 遷移とも成立している。
- **`flush_total` は縮まらなかった。** §10.3 ⑵・§10.5 が予告したとおりである——遅さは `SetWindowPos` 呼出の**内側の OS 側**にあり（Σ`call_us`／`total_us` ＝ 99.82〜99.92%＝**是正前**の §10.3 ⑵ の値）、投入をまとめても OS 側の総量は減らない。**この比を是正前後で比べてはならない**——B-2b 以後の `call_us` は `DeferWindowPos` への投入だけの所要へ意味が変わっており（§10.6.1）、是正後の同比 6.0〜18.1% は OS 側のコストが減ったことを意味しない。比べられるのは `total_us` と `in_batch` である。**B-2b の採用は「これで 4.2 が成立する」という主張ではない**（設計 C8 の段階裁定の注記）ことが、実機で確認された。
- **窓ごとの隙間はむしろ伸びた。** 全窓の書込がバッチ末尾へ揃った結果、早く可視化された窓の待ち時間が長くなる。これは欠陥ではなく**同時性と引き換えの帰結**だが、要件 4.2 の観点では隙間が**均一に長くなった**ことを意味する。
- **ゆえに C8 の梯子は次段（B-4）の条件を満たしたままである**——「B-2 後も `visualize_to_write_us` max が signoff 上限超」。現況 max は **306,301µs＝上限の 18.4 倍**。ただし本仕様ではそこまで進めない（開発者裁定・§11.4）ので、梯子は引受先 `areka-P0-present-write-coherence` が続ける。

### 11.7 tasks.md が 7.3 へ名指しした申し送りを閉じた（警告語を grep 語へ足す）

`tasks.md:346` の**【task 7.3 への申し送り】警告文を grep 語へ足す**を本タスクで閉じた。受け皿のファイル集合（`signoff-procedure.md`／`mechanism-ledger.md`）は申し送り自身が指定していたとおりである。

- **足した先**: 手順書に **§3.5「整合待ちの札の破れを見る警告語（task 6.5 の検出側・要件 8.1）」** を新設した（§3 の点灯表の末尾）。あわせて §5 の手順 8（回す検査の列挙）と §7 の受け渡し契約の「点灯確認」の行にも `ATOM-HOLD-BREACH` を通した。**手順書は決定論テスト `crates/areka/src/placement/transition_signoff_procedure_tests.rs` が逐語一致を検査している文書なので、加筆後に同テスト 8 本が緑であることを確かめてある。**
- **検査語**: `整合待ちの札がある窓へ窓書込が到達した`（発行点 `crates/areka/src/placement/follow/window_move.rs`。`warn!` は `:682` から始まり、検査語を含むメッセージ行は `:687` である＝grep の錨は `:687`。同 `:689` に `debug_assert!(false, ..)` が並ぶ。行番号は task 7.5 の是正で `:594`／`:599`／`:601` から移った＝§12）。task 6.5 が塞いだ穴——整合ゲートの見送りが 3 点では足りず `resnap_for_work_area_change` が 4 つ目の窓書込口だった件——の**検出側**である。
- **閉じ方＝この観測点は D-ATOM で点いている（消灯ではない）**。ここが本件の要点なので根拠を残す。
  - target は `warn!` に `target=` を与えていないので**モジュールパス既定** `areka::placement::follow::window_move`、水準は `warn`。D-ATOM の先頭セグメント `info` が既定水準であり（手順書 §2.1 の `info` の行＝「`warn!`／`error!` は常に通る」）、`warn` はそれを上回るので通る。`areka::placement::diag=debug` はこの target の接頭辞に**ならない**ので影響しない。
  - **較正（推測で書かない）**: 同じく `target=` を与えていない兄弟モジュール `areka::placement::measure` の `warn!` が、D-ATOM で採った実機ログ（`atom-73-signoff-1`）に**実際に出ている**（`WARN areka::placement::measure: measure: shell bake で脱落した element…`）。`areka::placement::` 配下の既定 target の `warn` が D-ATOM で通ることの実証である。
  - **ゆえに `[start_preparing]`（手順書 §4.4）とは扱いが違う。** あちらは消灯した観測点なので 0 件を単独の根拠にできず目視の宣言で代えるが、**本語の 0 件は発生 0 回の根拠として使ってよい**。
- **`release` では panic しない**——`[profile.release]`（リポジトリ直下 `Cargo.toml:95-105`。末尾は `strip = false`）は `debug-assertions` を指定しておらず既定は off なので、`debug_assert!` は無効化される。実機採取（`release`）では**この警告行だけが唯一の徴候**であり、`debug` ビルドの常時テストでは同じ形がその場で落ちる。**実機でだけ静かに通り抜ける形だからこの grep 語が要る**——これが申し送りの動機である。
- **本採取の実測**: `atom-73-signoff-1` で **0 件**（`ATOM-HOLD-BREACH: PASS`）。参考として task 7.1 の採取 `atom-71-recapture-1` でも **0 件**。記録票 `meta.txt` の点灯確認の節へ判定語と根拠を併記した。
- **§6.6 の内訳 6 行には足していない。** 本検査は合否の内訳ではなく採取の健全性の前提であり、内訳行を増やすと既に確定している合否の様式（7 行）と記録票・要件 8 の注記が食い違う。**1 件でも出たときは本番の欠陥として `ATOM-SIGNOFF` を FAIL にする**旨を §3.5 と §5 の手順 8 に明記してあるので、判定に穴は空かない。

### 11.8 本節が動かさないもの

- **上限 16,667µs（L9）は動かしていない。** §4.3 の再裁定条件（採取機のリフレッシュレートが 60Hz を下回る）に当たる事実は本採取に無い。手順書 §6.5 の「目視に合わせて上限を緩めてはならない」に従う。
- **判定器のコードにも本番コードにも触れていない。** 本節は登記である。
- **§10.5 の採否（B-2b の採用）を動かさない。** §11.6 の数量は B-2b が「1 バッチであること」を実機で満たしたことを示しており、採用条件そのものは §10.5 で既に確定している。時間効果が出なかったことは、C8 が梯子として書かれていた理由の裏づけである。

## 12. 整合待ちの札の監視が偽の警報を鳴らしていた（task 7.5・2026-08-22）

最終ゲート（`/kiro-validate-impl`）のタスク横断検査が確定させた欠陥である。task 6.5 と**同型**——「列挙で守る規約が静かに穴を持つ」形だが、穴が空いていたのは**見送り**ではなく**監視**の側であった。

### 12.1 機序

単一の窓書込口 `enqueue_window_set_pos`（`crates/areka/src/placement/follow/window_move.rs:634`）の入口には、待ち札の適用範囲の不変条件を見張る `warn!`＋`debug_assert!` がある（設計 C5・要件 5.8）。この監視の免除は **`route != Some(PlacementRoute::BalloonFollow)` の 1 語だけ**だった。

ところが本番には、整合ゲートの見送り 4 点（`frame/dpi.rs` の `apply_dpi_phase_gate`／`frame/scale_text.rs:170`／`frame/drain_resnap.rs:234`／`frame/work_area_sync.rs:238`）を**通らない**窓書込口が他にもある。札の付いた窓へそれが届くと、`warn!` の直後の `debug_assert!(false, ..)` で **debug ビルドが panic** した。

到達条件は task 6.5 と同一（窓中心が属するモニタと OS が拡大率を決めるモニタが食い違う配置で札が残り、有界の待ち＝30 フレームの内側に書込が届く）だが、**到達しやすさが違う**——⑴ 利用者のドラッグ（`follow/drag_follow.rs:89`・`:183`・route 無し）⑵ ドラッグ解放時のバルーン位置補正（同 `:902`・`BalloonLimitRelease`）⑶ `\![move]`（`emo2_boot/move_cue.rs:643` → `move_window_to`・`MoveCue`）は、**利用者・スクリプトが任意の時点で起こせる**。実際、決定論テストでは札のある窓を 1 回ドラッグしただけで落ちた（§12.4 の RED）。

### 12.2 裁定——直すのは監視であって見送りではない

**本番コード自身が既に裁定を書いていた。** `move_window_to` の doc（`window_move.rs:40-41`）は「**スクリプトの明示操作**ゆえ遷移ガードは適用しない（ドラッグ・`Restore` と同族・D13 帰結⑵）」と述べる。すなわち明示操作は**見送らないことが正しい**。止まらないことが正しい書込を「漏れ」として鳴らすのは欠陥の通報ではなく**偽の警報**である。

ゆえに是正は「監視が鳴る対象を、**見送りが覆うべき経路**へ限定する」——見送りの 4 点は 1 bit も変えていない。判定は新設の純関数 `deferral_covers_route`（`window_move.rs:508`・`Option<PlacementRoute>` の**網羅 match**）が 1 本で持ち、監視の条件は `:678` でそれを呼ぶ。`warn!`（`:682`・メッセージ行 `:687`）と `debug_assert!`（`:689`）は両方そのまま残す（log-first・silent failure を作らない）。

### 12.3 全 12 語＋route 無しの分類と根拠

**鳴る（見送りが覆う）7 語**

| 経路語 | 覆っている見送り | 根拠 |
|---|---|---|
| `DpiReproject` | 拡大率の相 `apply_dpi_phase_gate`（`frame/dpi.rs:302`） | 4 点の 1 |
| `ReportedSizeReconcile` | `frame/scale_text.rs:170` | 4 点の 2 |
| `Resnap` | `frame/drain_resnap.rs:234` | 4 点の 3 |
| `WorkAreaResnap` | `frame/work_area_sync.rs:238` | 4 点の 4（task 6.5） |
| `KeepPositionResize` | 上の 1・2 の**内側** | 唯一の発行元 `reconcile_window_size`（`frame/dpi.rs:159` のバルーン腕）は拡大率の相と報告寸の突合からしか呼ばれない |
| `ChainRealign` | **自前の見送り**（C4 の解決条件 2・`chain_realign.rs:144` の `held_ghost_window_scope`） | 4 点は通らないが、札を持つゴースト窓が 1 つでもあれば解き直しごと見送る。窓ごとではなく全窓一括で止める点だけが 4 点と違う |
| `AnchorChange` | 現状どの見送りも通らない | **本番では未結線**（`anchor_changed_system` を schedule へ登録している箇所は無く、呼ぶのは決定論テストだけ＝`window_move.rs` の `#[allow(dead_code)]` の理由）ゆえ現に到達しない。由来は基盤側の置き直しなので、結線した瞬間に「見送りを足すか」を必ず決めさせるため鳴る側に置く |

**鳴らない（見送らないことが正しい）5 語＋route 無し**

| 経路語 | 根拠 |
|---|---|
| `MoveCue` | `\![move]` のスクリプト明示移動（`move_window_to` の doc の裁定）。起動時の連鎖確定（`frame/drain_resnap.rs:335`）も同じ語で書く |
| `Restore` | 位置永続化の復元マージ。D13 が `MoveCue`・ドラッグと同族の明示操作と定める。現状どこからも構築されない（語彙のみ保持） |
| `BalloonLimitRelease` | バルーンドラッグ**解放時**の `windowposition.limit` 補正＝ドラッグの一部。見送ると画面外のバルーンが引き戻されないまま残る |
| `BalloonFollow` | task 5.4 で確定した例外。バルーンの位置はキャラ窓の従属量であり、そもそも 4 点を通らない |
| `SpawnInitial` | 位置**そのものを作る**書込。保つべき「遷移前の値」が無く、見送れば窓が置かれないまま残る。現状は単一ライターを通らない |
| route 無し（`None`） | **ドラッグ経路のキャラ窓書込のみ**（`on_char_drag`／`on_char_drag_end`）。利用者の明示操作 |

**`is_system_reanchor` を流用しない。** あちらが答えるのは「誰が動かしたか」（既定位置の追跡と可視性ガードが読む区分・D9／D16）、こちらは「見送りが覆うか」である。実際に `KeepPositionResize` の 1 語で割れる（あちらは `false`・こちらは `true`）。`diag.rs` の doc が求める「2 つの区分を意図的に割るなら理由を設計へ登記する」に従い、設計 C5 と本節へ登記した。

### 12.4 実行で示したこと（要件 5.8）

新設 `crates/areka/src/placement/follow_window_move_hold_watch_tests.rs`（9 本）。

- **RED（是正前）**: 札のある窓へ `\![move]`（`MoveCue`）と**本物のドラッグ経路**（`on_char_drag`・route 無し）で書く 2 本が `DpiSyncHold のある窓へ窓書込が到達した: ... route=None since_frame=3` などで panic して赤。同じ土台の鳴る側 2 本と分類 3 本は緑のまま。
- **GREEN（是正後）**: 免除側は `debug_assert!` が撃たれず `warn!` も 0 件・窓は実際に動く（零件が空虚にならない対）。鳴る側（`ChainRealign`・`KeepPositionResize`）は `#[should_panic(expected = "DpiSyncHold")]` で従来どおり落ち、札を外した対では通る。
- **分類の網羅**: `PlacementRoute::ALL` の 12 語を表と突き合わせ、語数一致・重複無し・route 無しを別々に主張する（テスト側にも独立に書き下す二重記帳）。
- **ミューテーション検証**: `debug_assert!` の条件を反転（`false`→`true`）すると**鳴る側 4 本だけ**が赤になり免除側は 1 本も動かない／分類の使い方を反転（`if !deferral_covers_route`）すると免除側・鳴る側あわせて 12 本が赤。本番はいずれも復元済みで、`cargo test -p areka` は 1,192 passed。

### 12.5 既存テスト 2 本の経路語を差し替えた

`placement/dpi_sync_tests.rs` と `emo2_boot/frame_work_area_resnap_hold_tests.rs` の「監視が生きていることを示す陽性の対」は、いずれも `move_window_to`（＝`MoveCue`）で鳴らしていた。是正後は鳴らないのが正しいので `move_window_with_route(.., ChainRealign)` へ差し替え、役目（監視が無条件に黙っていないことの証明）を保った。前者には「明示操作は同じ札のある窓へ届いても鳴らない」対を同一土台で 1 本足した。**退役ではなく更新**である——測っていた性質は今も生きている。

### 12.6 本節が動かさないもの

- **整合ゲートの見送り 4 点は 1 bit も変えていない。** 直したのは監視である。
- **判定器（`transition_judge*.rs`）と上限 16,667µs に触れていない。** task 7.3 の確定した合否（`ATOM-SIGNOFF: FAIL` だが開発者裁定 GO）も変えていない。
- **`ChainRealign` を見送りの 4 点へ足していない。** 自前の見送り（C4 の解決条件 2）が既にあるからであり、足すと同じ量を 2 箇所が止めることになる。

### 12.7 申し送り（本仕様では扱わない）

- **`MoveCue` は 1 語で 2 つの実在トリガを指している**——`\![move]`（`emo2_boot/move_cue.rs:643`）と**起動時の連鎖確定**（`emo2_boot/frame/drain_resnap.rs:335`）。D13 の「1 語＝1 実在トリガ」に照らすと語彙側の未完了だが、本タスクの分類では両者とも「見送らない」側で答えが一致するため判断は割れない。切り分けが要るのは、起動時確定にだけ別の扱いを与えたくなったときである。

## 13. 最終ゲート（`/kiro-validate-impl`）が残した申し送りの処理（2026-08-22）

群 7 の全タスク着地後の最終ゲートで、**タスク横断の整合検査が本番の欠陥 1 件（→ task 7.5 として起票・着地）と、引受先の無い申し送り 4 件**を掘り当てた。本節は後者 4 件の処理を登記する。**「先送りは担当 spec の実在検証＋即報告」の規律に従い、引受先はすべてファイル集合で実測して決めた。**

### 13.1 見送り窓の規則を `writes_per_window` へ広げない裁定

`tasks.md` の**【task 5.3 への申し送り】**は「`frames_to_last_write` に既に適用した『見送り窓への書込を数えない』規則を `writes_per_window` へも広げること」を求め、**「task 7.3（実機サインオフ）で見つけるのでは遅い」**と明記していた。5.3 の着地後、広げる作業も広げない裁定も行われないまま 7.3 が通過した。

**裁定＝広げない。** 根拠は 3 点である。

- **触発条件が実機で 1 度も成立していない。** 広げる必要が生じるのは「見送り窓（再表示が `invisible` で見送られた窓）が 1 遷移の中で**別々の tick に 2 本**書く」形である。実機採取 2 回（7.1＝`atom-71-recapture-1`・7 遷移／7.3＝`atom-73-signoff-1`・8 遷移＝**計 15 遷移**）で、`writes_per_window` の違反は **0 件**だった。偽の違反は一度も立っていない。
- **広げる方向は判定を緩める向きである。** 見送り窓の書込を数えないのだから、違反が消えることはあっても増えることはない。7.3 の `writes_per_window` は 8 遷移すべて PASS ゆえ、**今広げても 7.3 の合否は 1 文字も動かない**——つまり合否の正しさのために急ぐ理由が無い。
- **広げると確定済みの記録を書き換える。** 現行挙動を固定しているテストは 3 本ある（`transition_judge_tests.rs::summarize_excludes_windows_whose_redisplay_was_skipped`・`transition_judge_verdict_tests.rs::a_deferred_resize_after_a_punctual_follow_is_not_a_companion_defect`・`transition_judge_reobservation_tests.rs::every_baseline_transition_is_judged_exactly_as_the_report_describes_it`）。最後の 1 本は**基準値の採取を逐語で再現する檻**であり、規則を変えれば過去の採取に対する判定が変わる。task 7.4 のレビューで確認したのと同じ形——**判定規約を後から緩めると、過去の採取の確定記録を書き換えてしまう**——であり、実機で 1 度も触発していない改良のためにそれを踏むのは筋が悪い。

**引受先＝`areka-P0-emo2-conformance-e2e`（承知の登記のみ・コードの持ち分は渡さない）。** 一周走行が本仕様のランナーと判定器を流用するので、**限界を知らずに使うことがないよう** brief 追記(77) ⑹ へ「`writes_per_window` の違反を見たら、まず当該窓が `skipped_windows` に居ないかを確かめる」と書いた。**判定器そのものを直す持ち分は誰にも渡していない**——直すべき形（当該遷移のフレームに落ちる書込だけを数える）は上の申し送りが既に具体で書いており、触発する採取が現れた時点で本節を根拠に起票すれば足りる。

### 13.2 自発書込カウンタの型（`SELF_INITIATED_DEPTH`）——引受先が決まっていなかった

`tasks.md` の**【task 4.3 の台帳へ登記する項目】**は `crates/wintf/src/ecs/window/command.rs:49` の `SELF_INITIATED_DEPTH` について「プロセス共有の `AtomicI32` だが意味論はスレッド局所・是正は `Cell<i32>` 1 手・挙動変更ゆえ要件 3.4 により本仕様の対象外——**台帳へ登記して申し送る**」と述べていた。**台帳への登記が行われておらず、引受先も 0 だった**（最終ゲートの実測）。

- **本節が登記そのものである。** 内容は上の申し送りのとおりで、**本番の欠陥ではない**（wintf に `thread::spawn` は 1 つも無く、メッセージポンプは `com/wuc.rs:110` の現スレッド 1 本、`world/mod.rs:123-125` が WUC 接触を UI スレッドへ構造的に固定し、ランタイムは `!Send`／`spawn_local`）。効くのは**テストの並列実行**であり、カウンタを読む／上げるテストが互いを汚染する（実測 60 回中 11 失敗）。
- **引受先＝2 本へ登記した。** 症状の側（檻どうしの汚染・錠 `lock_self_initiated_for_test()` の退役・zorder 系 3 ファイル約 35 箇所の汚染源）は `areka-P0-test-cage-determinism` の brief 追記(76) ⑹。本番コード 1 行の側は `command.rs` を接触集合に持つ `areka-P0-draw-load-parity` の brief 追記(78) ⑹。**着手する側がもう一方へ知らせる**と両方に書いた。

### 13.3 1 ファイル 1,000 行の目安が漂流している

`tasks.md` の**【task 5.5 への申し送り】**は「`window_move.rs` は 974 行で 1,000 行の目安まで残り 26 行・**着手前にファイル分割が要る**」と述べていたが、分割も裁定も行われないまま群 5〜7 が進み、**最終ゲートまで誰も気づかなかった**。

- **規則の所在**: `.kiro/steering/structure.md:176`「1 ファイル 1,000 行以下の目安は本番ファイル・テストファイルの双方に適用する」。`file-slimming`（W5.95・PR#103）が 1,000 行超 54→0 本まで落として達成した。
- **実測（2026-08-22・`crates/**/*.rs` 全数）**: **9 本**が超えている。うち **5 本は本ブランチの分岐点（`git merge-base origin/main HEAD`）で既に超えていた**（`cache_tests.rs` 1,604／`actor_bind_loop_tests.rs` 1,330／`plan_ops_tests.rs` 1,076／`budget_tests.rs` 1,047／`bind.rs` 1,033）。本仕様が足したのは 4 本（`frame_transition_branch_tests.rs` 1,255／`window_move.rs` **965→1,223**／`transition_judge_tests.rs` 1,039／`transition_judge_verdict_tests.rs` 1,037）。**つまり「本仕様が生きた不変量を壊した」のではなく「既に破れていて本仕様が積み増した」。守っているテストも lint も 1 つも無い。**
- **本仕様の内側では割らない。** `window_move.rs` を割ると spec 文書 7 本に散る **41 箇所**の `window_move.rs:<行>` が全部動く。**file:line の陳腐化は本仕様が 4 度踏んだ失敗そのもの**であり、実機サインオフを閉じた直後にそれを自ら仕込む判断はしない。**現に task 7.5 は +85 行を挿しただけで design.md の 6 箇所を陳腐化させ、レビューで差し戻された**——分割の規模ならこの比ではない。
- **引受先は存在しない。** `file-slimming` は `.kiro/specs/completed/` にあり申し送りを消化できない。上の 9 本のいずれかを名指ししている**生存 spec は 1 本も無い**（`.kiro/specs/` の completed を除く全ディレクトリを 5 本のファイル名で走査して 0 件）。**ゆえにこれを「先送り」とは呼ばない**——`.kiro/steering/roadmap.md` の**追記(79)** へ全数量と 3 択（番人を置く／掃除 spec を起票する／規則を実態へ合わせる）を出して開発者の裁定を仰いだ。本節はその写しである。

### 13.4 要件 10.6 の「ドラッグ追従の比 1.000」は、どちらの系統でも測っていない

- **決定論では維持が示されている**——`crates/areka/src/placement/follow_drag_tests.rs`（とりわけ `on_char_drag_writes_only_policy_applied_positions`・`on_char_drag_balloon_follows_policy_applied_position`）が、生ドラッグ座標が射影 T を通って窓書込指令へ 1:1 で写ることを固定しており、全タスク着地後もワークスペース全体が緑である。
- **ただしそれは指令キューまでである。** これらの檻は `drain_window_pos_commands()` でキューを直接読むので、**task 7.2 が新設した一括バッチから先は構造的に通らない**（設計 D11）。
- **実機サインオフでも測っていない。** 手順書 §4.4 は採取中にドラッグしないことを条件とし、2026-08-22 の採取は `ATOM-NO-DRAG: PASS` である。
- **かつドラッグは隔離されていない。** アンカーが `Free` 以外のキャラ窓（本番のゴースト窓＝`Anchor::Bottom`）のドラッグ追従は `crates/areka/src/placement/follow/drag_follow.rs:89`・`:183` が `enqueue_window_set_pos` を呼ぶ＝**合流も一括バッチも通る**。構造的に隔離されているのは `Free`／`Anchored` 不在の側だけで、そちらは `crates/wintf/src/ecs/window_proc/mouse_move.rs:438-450` が `guarded_set_window_pos` を直接呼ぶ（本仕様による同関数の変更は `debug!` を `log_window_write(.., VIA_SET_WINDOW_POS)` へ置き換えた 1 点のみで、`SetWindowPos` の呼出自体は無改変）。
- **引受先＝`areka-P0-emo2-conformance-e2e`**（brief 追記(77) ⑸・要件 10.6 直下の注記と対）。**本仕様の内側で追加採取はしない**——サインオフの条件（ドラッグ禁止）を緩めると要件 8 の採取条件が変わり、既に確定した task 7.3 の合否の意味が動く。

### 13.6 task 4.1 のレビューが残した硬化提案 3 件の処理

`tasks.md` の**【task 4.2・4.3・7.3 への申し送り】**は硬化提案 3 件を挙げていたが、名指しされた 3 タスクが `[x]` になっても 3 件とも実施されず、実施しない裁定も無いまま最終ゲートまで残った。

**⑴＝閉じた。ただし申し送り自身の診断が誤っていたので、種別を書き分けて登記する。**

- 申し送りは「窓ごとの `Unmeasured` 2 腕（`MismatchFrames`／`VisualizeToWriteUs`）を**どのテストも起こせない**」＝未踏の分岐と述べていた。**これは実測では偽である**——両腕は `transition_judge_negative_tests.rs:117` と `:203`／`:209` が現に踏んでいる。
- **実際の欠陥はもっと狭く、種類が違った。** `signatures_producible_by`（`transition_signoff_procedure_tests.rs:401`）は「この上限系統が出し得る違反の**全体**」を名乗るが、渡す summary の変種が 2 つしか無く**窓ごとの量が欠ける形を作れない**ため、上の 2 署名が集合に入らない。そして `misattributed_violations`（同 `:429`）は「その集合に無い行」を誤帰属と断ずる。ゆえに**手順書が当該行を正しい系統の下に置いても偽と判定され得た**——未踏の腕ではなく、**網羅を名乗る集合の取りこぼしによる偽陽性**である。
- 是正: 第 3 の変種 `summary_with_written_window_missing_its_quantities()`（同 `:383`・`:406` で結線）を足した。書込は在り窓ごとの量が欠ける形で、書込回数は `WRITES_PER_WINDOW_MAX` に置いて足された署名が一意になるようにしてある。新設検査は `the_producible_signatures_cover_the_per_window_unmeasured_arms`（同 `:531`）。
- 実行での確認: RED 相で当該検査が赤（署名集合に `mismatch_frames_per_window[…]` も `visualize_to_write_us[…]` も 1 件も無いことを実出力で提示）。着地後、`transition_judge_verdict.rs` の 2 腕をそれぞれ削るミューテーションで**新設検査と既存の negative テストだけ**が赤になることを確かめ、**本番コードは md5 一致で復元済み**（`git status` に同ファイルは現れない）。

**⑶＝閉じた。「走査を広げると偽陽性になる」という懸念は、この 3 件については当たらなかった。**

- 実測: 両系統に出る違反は**ちょうど 3 件**（`語彙の規約を破った行`／`フレーム差が判定不能（一様に 0／読めない）`／`区間の先頭が起点レコードでない`）。決定論のみ 12 件・実機専用のみ 4 件。門の外にある判定器の入力健全性ブロックの 3 件がそれである。
- **この 3 件は両系統の署名集合に入るので、手順書がどちらのブロックの下に置いても現行の走査は素通しする。** ゆえに `misattributed_violations` を広げる必要は無く、**広げていない**。
- 欠けていたのは検査ではなく**文書**だった——§6.2 の「どちらのブロックの下にあったかで系統を判断する」規則には**言外の例外が 3 件ある**のに、手順書が名指ししていたのは `フレーム差が判定不能` の 1 件だけだった。3 件を字面で並べた箇条を `signoff-procedure.md:381` へ置き、§0.2 の検査一覧へ ⑸ を追加、§8 の参照を ⑴〜⑸ へ更新した（**task 7.3 の合否に触れる記述は無改変**）。新設検査 `the_two_families_share_exactly_the_input_sanity_violations`（`transition_signoff_procedure_tests.rs:575`）は**共通集合を判定器から起こして**手順書が 3 件を名指ししているかを見る。文書側・判定器側の両方向でミューテーション較正済み。

**⑵＝閉じない裁定。根拠は実測である。**

- §6.2 の 2 つの散文箇条（`signoff-procedure.md:379`／`:380`）に対し、判定器が刷る違反 11 語のうち**字面一致は 6 語**、**不一致は 5 語**である——`判定対象の量が欠けている`（記載なし）／`同期段の書込`・`経路 A の書込`（どちらも「経路 A の件数（札と段の両方）」と要約）／`連鎖再解決`（「連鎖の回数」）／`随伴バルーンがキャラ窓と別のフレームで書かれた`（「随伴バルーンが別フレームで書かれた」＝「キャラ窓と」が落ちている）。
- **逐語検査に掛けるにはこの 5 語を書き換えねばならない。** それは task 7.3 の合否根拠文書の散文を「**どの量に上限を当てるか**」の説明から「**出力文字列の一覧**」へ変質させる。採取者が §6.2 を読む目的は前者である。
- **しかも主張として効いている半分は既に実行で固定済みである**——`:380` の「フレームを単位に測る量はすべて決定論系統」は `the_two_bounds_families_produce_different_violations`（同 `:485`）が `Bounds` から起こして固定しており、武装された項目が動けば赤になる。
- 無検査で残るのは `:379` の列挙の**網羅性**（新しい上限を足したときに散文が黙る）だけで、それを塞ぐには「語句 ↔ `Bounds` フィールド」の**手書きの表**が要る。**同ファイル自身が「表を 2 箇所に持つと必ず片方が腐る」として退けている形**である。**新しい上限を足すときは `:379` の列挙も手で追随させること**——それが本裁定の代償であり、`signoff-procedure.md` §8 のメンテ規約が受け持つ。

### 13.7 「別窓は仕切りにしない」の未強制の前提を引き渡した

`tasks.md` の**【⚠ task 5.4・群 6 への申し送り】**は、要件 10.3（Z 指令の順序・結果不変）の成立が**コードの強制していない前提 1 つ**に依っていることを述べていた——各 `SetWindowPos` が同期送出する `WM_WINDOWPOSCHANGED` のハンドラが `flush_window_pos_commands()` へ**再入**し（`crates/wintf/src/ecs/window_proc/window_pos.rs:290`・手順 ③）、ジオメトリ書込の順を入れ替えると再入 flush が他窓の Z 指令と噛み合う時点が変わる。現状が無害なのは「ウィンドウプロシージャ側が Z 指令を積まない」という事実に依っているだけで、**コードはそれを強制していない**。

**部品は渡っていたが結論は宙に浮いていた。** 入れ子 flush と Z 非合流は `areka-P0-draw-load-parity` の brief 追記(74) ⑷②⑷③ が既に持っていたが、「**前提が未強制である**」という結論を持つ文書は spec にも隣接 brief にも 1 つも無かった（最終ゲートの実測）。**同 brief の追記(78) ⑺ へ結論として登記した**——flush の駆動・間引き・順序に手を入れるとき、あるいは wndproc 側から Z を積む経路を足すときにこの前提が壊れる、と明記してある。

### 13.5 本節が動かさないもの

- **判定器のコードにも上限 16,667µs にも触れていない。** task 7.3 の確定した合否（`ATOM-SIGNOFF: FAIL` だが開発者裁定 GO）も、task 7.4 が確定した手順書 §6.5 の 4 行の表も変えていない。
- **13.1〜13.7 はいずれも本番の挙動を 1 bit も変えない。** 変えたのは記録と引受先の割り当て、および**テスト側の網羅**（§13.6 ⑴⑶）だけである。判定器の本番コードは 1 行も触れていない。
