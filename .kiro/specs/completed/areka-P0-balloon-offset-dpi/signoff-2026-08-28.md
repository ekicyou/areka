# 実機サインオフの記録（task 10.1・2026-08-28）——**合格**

要件 8.3 が定めるとおり、**合否は記録の機械判定で決めた**。目視は §6.3「判定器が見ないもの」を
補うためだけに行い、判定器の合否を上書きしていない。

## 判定結果（機械判定）

```
== balloon offset signoff: signoff-2026-08-28.log ==
追随レコードの判定: 遷移 15 本・offset 行 14 件・違反 0 件
test ... judges_a_real_machine_offset_log ... ok
```

判定は 4 つとも成立した——⑴ 往復の前後で反映後の値が bit 同一に戻る ⑵ 遷移ごとの判定語が期待の腕
⑶ 低い拡大率側で追随の判定語が出ている ⑷ キーワード指定スコープの揃えの残差が許容量以内。

**この「違反 0 件」が空虚でないことは、原本を触らず複製 3 種で実証した**（レビュー時）——
scope 0 の `rescaled` 6 行を落とす→赤／`new_offset` を 1 行壊す→`残差 85.50px > 3px` で赤／
門の行を落とす→赤。**判定 ⑷ は本ログで実際に 6 行を測っている。**

## 環境（要件 8.1）

- DISPLAY1（主）: `0..2880`・**dpi=192**／DISPLAY2: `-2560..0`（左側）・**dpi=144**
- 実効 DPI は DPI 対応プロセス（areka 自身）のログで確かめた
- areka: 本ブランチの `target/debug/areka.exe`
- ゴースト: `crates/pilot/examples/shiori-host-32/fixtures/emo2`
- バルーン: `crates/pilot/examples/shiori-host-32/fixtures/emo2-kakukaku-offsetdpi`（混成検体）
- 観測: `RUST_LOG=wintf::transition=debug`

## 操作（要件 8.2）

**モニタ間の往復は開発者が人の手でドラッグして行った。** 合成マウス入力ではゴーストを掴めないため
（手順書 §7.2 の限界）、この手は人が実際に手を動かして実施した。外から窓を移動させた採取は
サインオフの証跡にしていない。

起点は `kind=windpi` が **28 行**、`kind=monitor` は **0 行**——モニタ表は変わらず**窓の**拡大率だけが
変わる操作であることが、記録の側からも裏づけられている。

## 目視（要件 8.4 の目視側）

開発者の確認: 「**DPI が異なるモニタ間の移動でも、バルーン座標は DPI に応じた位置にあるように見える**」
「見た目上の問題なし」。**先行仕様の残所見「低い拡大率の側で定常的にバルーンがずれる」は再現しなかった。**

## 証跡

- ログ: `signoff-2026-08-28.log`（1,506,663 バイト・8,087 行・md5 `d2c48abe0077df7be2dd1cd838117d8c`）
- 判定の再現: `$env:AREKA_TRANSITION_LOG` に上記ログの絶対パスを入れて
  `cargo test -p areka transition_judge_offset_signoff -- --ignored --nocapture`

## この採取で判った 3 件（手順書へ反映済み・task 8.7）

手順どおりに回そうとして 3 度止まった。いずれも**製品は正しいのに偽の赤が立つ**種類で、
**檻の中では出ない**ものである。詳細は手順書 §2.2／§2.3／§7。

1. **隣に置かれた 64bit の helper がゴーストの SHIORI を殺し、何も表示されなくなる**
   （`0x800700C1`）。ワークスペース全体のビルドが同名の実行体を `target/debug/` へ置く。
2. **前の採取が残した保存済みバルーンオフセットが、次の採取の門の行を消す**。
3. **必須手「素材未消費のまま遷移へ通す」は人の手では原理的に実行できない**——素材は起動から
   0.73〜5.0 秒で areka 自身の boot 系列が自動的に消費し、最初の起点はユーザーのドラッグ由来ゆえ
   必ずそれより後。**task 8.6 で判定 ⑷ の母数の作り方を改め、この手を撤去した。**

⚠ **窓が見えているかどうかを `IsWindowVisible` で判断してはならない**——上記 1 のとき窓は 4 枚とも
`IsWindowVisible` が真で画面内の座標にあったが、中身が 1 枚も合成されていないので目には何も映らなかった。
