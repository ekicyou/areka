# Implementation Plan

- [ ] 1. テスト先行：欠陥を再現する決定論テストを先に置き、直す前の赤を記録する
- [x] 1.1 起動挨拶を追跡したまま基盤バージョン通知の応答待ちまで起動を進める共有テスト補助を用意する
  - 起動指示から（任意で終了指示を挟んで保留させ）初期化・利用者名の照会・初回起動の挨拶までを駆動し、挨拶トーク（識別子 1）を追跡した応答待ちの状態を返す
  - 戻る前に相が「応答待ち・追跡あり（識別子 1）」であることと、保留された終了指示が引数と一致することを表明する（終了理由は等値比較を持たないので綴りで比べる）
  - 各段のコメントは実際に通る相の順で書き、既存テストのコメントや本体には触れない
  - 完了時: `cargo test -p areka-kanade --lib` が新たな警告以外のエラーなくビルドされ、既存テストが全緑のまま（補助の働きの確認は 1.2 のテストが担う）
  - _Requirements: 5.1, 5.3, 4.5, 5.6_

- [ ] 1.2 応答待ち中の完了通知を扱う決定論テスト 3 本と、無関係な入力の警告を固定する較正テスト 1 本を追加し、直す前の結果を記録する
  - 系列テスト（完了理由 Ended）: 完了通知で枠が空・指示 0 件・無視の警告 0 行・受理の info がちょうど 1 行（talk_id 付き）・error 0 行、続く通知完了で枠が空の定常運転へ、続く終了指示で `OnClose` の問い合わせが発行される
  - 保留経路テスト: 起動中の終了指示を保留させたまま完了通知を受理しても指示 0 件で保留は残り、通知完了後の次の Tick で `OnClose` の問い合わせが発行される
  - 中断理由テスト: 完了理由 Interrupted でも Ended と同じ結果になる（横断遷移が先に書く既存の info 行は受理の行数に数えない）
  - 較正テスト: 応答待ち・追跡ありの状態に Tick を入れると無視の警告が従来どおり出て、受理の info は出ない
  - 系列 3 本は起動系列テストファイルの末尾、較正 1 本はログ発火テストファイルの既存の無視警告テストの直後に置き、どちらのファイルも 1,000 行以下を保つ
  - 完了時: `cargo test -p areka-kanade --lib boot_version_talkdone` で系列 3 本が赤、`cargo test -p areka-kanade --lib warn_boot_input_ignored_still_fires` で較正 1 本が緑（どちらも実行件数が 0 でないことを確かめる）となり、系列 3 本それぞれで実際に最初に失敗した表明の文言（予想の文言ではなく実測）をコミットメッセージに残したテストのみのコミットが存在する
  - _Requirements: 1.1, 1.2, 1.3, 1.5, 1.6, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 5.1, 5.2, 5.3, 5.4, 5.6_

- [ ] 2. 起動系列の最終段で追跡中トークの完了通知を受理する
- [ ] 2.1 起動系列の相固有遷移に、応答待ち・追跡ありのときだけ完了通知を受け取る腕と受理処理を加える
  - 受理は追跡を外して枠を空にし、応答待ちの待ち点を維持し、副作用指示を 1 件も返さず、保留中の終了指示・選択肢の帳簿には触れない
  - 受理を info の `boot_talk_done`（target `kanade`・`talk_id` 付き）でちょうど 1 行記録する
  - 条件を外れる入力は既存の無視の腕へ落とし、`boot_input_ignored` の綴り・レベル・メッセージ・発行点 1 か所を保つ（その腕の注記だけ「Tick など」へ改める）
  - 横断遷移・定常運転・終了系列・アクターシェルは変更しない
  - 完了時: 1.2 と同じ 2 つの名前指定実行で 4 本（系列 3・較正 1）がすべて緑になる
  - _Requirements: 1.1, 1.2, 1.4, 1.5, 1.6, 2.3, 2.4, 3.1, 3.2, 3.3, 3.4, 4.1, 4.2, 4.3, 4.4_

- [ ] 2.2 (P) 直った欠陥を「残る危険」と書いている areka 側の一周テストの doc コメント 1 段落を、受理経路が入った事実へ追随させる
  - 改めるのはその段落のみで、テスト本体・表明・同 doc の他段落（ずれる行番号参照を含む）は触らない
  - 新たに足す参照は行番号でなく「何の定義か」で指す
  - 完了時: 当該段落が「本仕様で受理経路が入った」旨を述べ、`git diff` がコメント行だけを示す
  - _Requirements: 5.5_
  - _Boundary: areka emo2_boot 一周テストの doc コメント_

- [ ] 3. 全体検証
- [ ] 3.1 欠陥の検出器・非回帰の検出器・1,000 行の番人を終了コードで判定して全緑を確認する
  - `cargo test -p areka-kanade` を全走させ終了コード 0 を確認する（欠陥の検出器）
  - `cargo test -p areka --bin areka` を他の cargo と並走させず単独で全走させ終了コード 0 を確認する（非回帰の検出器・直す前も緑なので直しの証拠には使わない）
  - `cargo test -p log-capture-kit --test file_length_guard_test` を通す
  - 出力の一部だけを切り出して判定しない。ワークツリーで submodule 未取得なら先に取得する
  - 完了時: 3 コマンドすべての終了コード 0 が実装の記録に残る
  - _Depends: 1.2, 2.1, 2.2_
  - _Requirements: 4.5, 5.5, 5.6_
