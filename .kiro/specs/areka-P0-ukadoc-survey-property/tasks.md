# 実装計画

> **2026-09-05 更新**: 上流 `areka-P0-ukadoc-survey-toolkit` の実装が main に着地し（PR#136）、本ブランチへ merge 済み。初期台帳・カタログ・常設整合検査・`ukadoc-survey` の 8 副手続き（`catalog`／`ledger-init`／`report`／`report-summary`／`check`／`evidence`／`candidates`／`diff`）が揃った。これに伴いタスク 1.1・1.2・6.1・6.2 を書き換え、証拠のコメント 2 行を 1.3 へ前送りし（上流の `check` が証拠の無い `implemented` を赤にするため）、残った回帰確認 1 本を群 5 に畳んである。判断の中身（規則 0〜8）は 1 つも変わっていない。
> 着手前に **`git submodule update --init --recursive` を済ませておく**（未初期化だと `cargo` 自体が `pasta_core` を見つけられずに転ぶ）。

- [ ] 1. 骨組みと受け入れ確認の土台を用意する
- [x] 1.1 着地済みの骨組みを確かめ、版番号を埋める
  - 上流が着地させた `doc/ukadoc-coverage/ledger/property.toml` を読み、骨組みが期待どおりであることを確かめる（先頭に `[ledger]` 表・`domain = "property"`・`pages = ["list_propertysystem"]`・`[entry."..."]` が 188 件・必須キー 7 つが空値で置かれ・全件 `status = "unclassified"`・`introduced` は全件空）。**骨組みを自分で生成しない**（上流 `ledger-init` の持ち物）
  - `cargo run -p ukadoc-survey -- check` を回して起点が緑であることを確かめる。merge 直後の実測は「食い違い 0 件／証拠のある項目 0 件」
  - スナップショットが読めることを確かめる（既定は `%APPDATA%\npm\node_modules\ukagaka-doc-mcp\data\index.json`。環境変数 `AREKA_UKADOC_SNAPSHOT` があればそちらを優先）。読めなければここで止める
  - 各 id の本文から `x.y.z` 形の版番号を拾って `introduced` に入れる。集合が空なら空文字にする（最古と決めつけない）。`system.os.(キー)` は本文が例示する OS の版 `5.19.0` を採らず、残る 2 つの古い方 `2.6.26` を採る。id は上流が写したものをそのまま使い、符号化された部分（`_28_30ad_30fc_29` の形・実測 67 件）と `_3f_3f_3f_3f` に手を入れない
  - 版番号を拾う手順は使い捨てとし、`doc/ukadoc-coverage/` の外の一時領域に置いてコミットしない。正典の本文そのものは repo に写さない
  - 完了状態: 版番号を持つ項目が 99 件・番号の種類が 21 種になり（**2026-09-05 訂正**: 当初は 98 件と書いていたが、カタログを数え直すと `versions` が空でない項目は 99 件〔188 − 空の 89〕。98 は版番号を 1 つだけ持つ束の数で、3 つ持つ `system.os.(キー)` を数え落としていた。規則 2 は同項目にも `2.6.26` を与えるので非空は 99 件になる）、`check` が引き続き食い違い 0 件で通る。編集したファイルは `property.toml` 1 本だけで、カタログ・他ドメインの台帳・テーマ定義には触れていない
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.7, 1.8, 3.1, 3.2, 10.6, 10.7_

- [x] 1.2 道具が持たない 4 検査だけを使い捨てで用意する
  - 上流の `check` と常設テストが既に持つ検査（台帳として読める・件数 188・カタログとの逐語一致・文字順と重複・他ページ排除・必須キーと `alias_of` の置き場所・別名の連鎖・関連の相手の実在・版番号・テーマ名・実装済みの証拠・報告の陳腐化）を**作り直さない**。二重になるものは書かない
  - 道具が持たない 4 つだけを、1.1 と同じ一時領域の短い手順で用意する — 検査 6b（`unclassified` が 0 件）・検査 6c（別名と対象外を除く全項目で備考の**中身**が空でない。キーが在るだけでは緑にしない）・検査 7（担当欄が `.kiro/specs/`〔`completed/` を含む〕に実在する spec 名か空文字）・検査 8（優先度が空文字・`C10`・`C20`・`C30`・`C90` のいずれか）
  - 各検査は合否だけでなく「検査した件数」を出す。対象 0 件で緑になったのか実際に働いて緑になったのかを後から区別できるようにする
  - 常設の検査コードを repo に置かない。同じ検査は上流の常設テストが持つ側にあり、本 spec の側に恒久的な仕掛けを残さない
  - 完了状態: 1.1 を終えた台帳に対して手順が走り、検査 7・8 が緑（担当も優先度もまだ全件空文字）、検査 6b が `unclassified` 188 件で赤、検査 6c が備考の空 188 件で赤になる。**赤が出ることで検査が実際に働いていることが分かる**
  - _Requirements: 10.1_

- [x] 1.3 実装済み 2 件の定義行に正典 URL のコメントを足す
  - `crates/areka-ghost/src/sylphya_wiring.rs` の `baseware.name` と `baseware.version` の各要素の直前行に、id 付きの正典 URL のコメントを 1 行ずつ足す
  - 足すのは URL 1 本だけのコメント 2 行で、説明文を伴わない。`vec![...]` の要素は Rust の doc コメントを置ける位置ではないので `//` を使う。上流の証拠収集は `///`・`//!`・`//` の 3 つを長い印から順に剥がすので `//` でも拾われる（`crates/ukadoc-survey/src/evidence/extract.rs:37`）
  - **証拠をここで先に置くのは 2.1 のため**。上流の `check` は証拠の無い `implemented` を赤にする（`crates/ukadoc-survey/src/check/content.rs:94`・上流要件 6.6）ので、2.1 で状態を書いた瞬間に赤の窓が開かないよう順を前に出してある
  - 完了状態: 当該ファイルの差分が追加 2 行・削除 0 行になり、2 行がそれぞれ `#baseware.name:1`／`#baseware.version:1` で終わる。`cargo run -p ukadoc-survey -- evidence` が当該 2 id に証拠 1 件ずつを並べる
  - _Requirements: 2.10, 9.1, 9.2, 9.7_
  - _Boundary: areka-ghost の sylphya 配線_

- [ ] 2. 台帳の状態と備考を埋める
- [x] 2.1 規則 0・1・3 で全 188 件の状態と別名を決める
  - 規則 0 に従い、見出しが葉の名前だけの 35 件は族の頭が定める前置きを補った完全な名前で判定する。id そのものは逐語のまま変えない
  - 規則 1 の決定表を上から先勝ちで当てる。`doc/COMPAT_ARCHITECTURE.md:136` は書き込み側の一般規則なので縮退の引き金にせず、縮退は同 `:184`／`:185`／`:207` に個別に登記された 8 件だけとする
  - 規則 3 で同じ値へ到達する名前の群を探し、あれば `alias_of`（向きは本文の注記→版番号→人手の判断の順）と任意の `supersedes` を書く。`alias_of` の指す先が `alias` である連鎖を作らない
  - 状態は「areka が値を返すか否か」だけで決め、返す値の意味が正典どおりかの判定は所有 spec に委ねる
  - 完了状態: 内訳が実装済み 2・縮退 8・語彙のみ 178・別名 0・対象外 0・未対応 0 の計 188 件になり（**2026-09-05 訂正**: 当初は縮退 7・語彙のみ 179 と書いていたが、`doc/COMPAT_ARCHITECTURE.md` を全行読み直すと `:207` が `currentghost.seriko.zorder` 1 項目の読み取り応答を個別に登記していた。正典は SSP 2.8.78 でこのプロパティを定め設定も有効とするのに、areka は「本リリースでは提供しない」と決めて未提供のプロパティと同じ応答を返す。これは規則 1 の第 4 行の条件そのものなので縮退へ移し、語彙のみを 1 件減らした）、検査 6b が緑（`unclassified` 0 件）、`check` が食い違い 0 件・証拠のある項目 2 件で通る。検査 9（別名の連鎖）は対象件数と一緒に記録する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.9, 3.4, 3.5, 3.6_

- [x] 2.2 (P) 正典の SET 有効 26 件を確定し、sylphya の 21 名と突き合わせる
  - 正典で SET が有効かどうかを 3 形で読む（本文の `[SET有効]` の印・本文の記述・族の頭からの継承）。族の頭が「以降 ... 共通」と書いている箇所を全件走査して継承の範囲を確定する
  - `crates/areka-sylphya/src/vocab/dotted.rs` の SET 有効群 21 名を正典の完全な名前へ広げ、双方向で突き合わせる
  - 4 つの区分（21 名のうち id に当たらないもの／正典では有効なのに 21 名に無いもの／21 名にあるが正典が有効としないもの／1 名が 2 id に当たるもの）で結果をまとめる
  - この作業では台帳ファイルを編集しない。結果は 2.3・3.3・4.1 の入力として一時領域にまとめておく
  - 完了状態: 正典側の SET 有効が 26 件（印 14・本文 3・継承 9）に確定し、21 名が指す 25 id のうち 21 id が正典側でも有効、差の 5 件が `areka-P0-property-query-channels` brief の「21→26」と一致することを数で確かめられる
  - _Requirements: 6.4_
  - _Boundary: SET 突合表（台帳ファイルは触らない）_

- [x] 2.3 規則 8 の必須 3 行を全項目の備考に書く
  - 「壊れ方」「ログ」「書き込み」の 3 行を、状態が別名・対象外の項目を除く全件に書く。ログ行の要旨は実測した 3 か所（`crates/areka-sylphya/src/reader.rs:137-142`・`crates/areka-sylphya/src/actor.rs:302-307`・同 `:345-350`）から引き、読み取りの失敗は既定の実行ではログが出ないことを書く
  - 「書き込み」の行には正典で SET が有効か（3 形のどれで読んだか）と areka の応答、両者が一致するかを 1 件ずつ書く
  - 備考にはこの後 3.1（候補・語彙表だけを触る spec・裁定待ち）と 3.3（テーマを付けた理由）が行を足す。ここでは 3 行だけを書き、後続が足す行の場所を空けておく
  - 完了状態: 検査 6c が緑（別名・対象外を除く全項目の備考の中身が空でない）になり、その全件に 3 行がそろっている
  - _Requirements: 2.5a, 2.8_
  - _Depends: 2.1, 2.2_

- [x] 2.4 条件付きの備考の行を該当項目に足す
  - 縮退 8 件に転記元の行（`doc/COMPAT_ARCHITECTURE.md` の該当行）を足す。古いスナップショットに基づく 6 件は意味を判定せず登記を指すだけにする。内訳は `:184` が 1 件・`:185` が 6 件・`:207` が 1 件（`currentghost.seriko.zorder`）（**2026-09-05 訂正**: 当初は 7 件と書き `:207` を数え落としていた）
  - 関連に書けない値の源（正典に相手の id が無いもの）を「値の源:」の 1 行で書く
  - 葉だけの 35 件に「前置き」の行（どのルート枝の下で使われるかの一覧）を足す。汎用プロパティ名の葉 17 とサウンドの要素葉 18 は、複数のルート枝の下で使われてもルート枝ごとに項目を増やさない
  - `system.os.(キー)` に「版: `5.19.0` は本文が例示する OS の版のため採らない」の行を足す
  - 見出しが重複する `name`／`path` の 4 件に、どちらの群かと本文の根拠の行を足す
  - 完了状態: 縮退 8 件すべてに転記元の行があり、葉だけの 35 件すべてに前置きの行があり、重複する見出しの 4 件すべてに群の判別の行があり、`system.os.(キー)` に版の行がある
  - _Requirements: 3.3, 6.3, 6.6, 6.7, 9.4, 10.4_
  - _Depends: 2.3_

- [ ] 3. 担当・関連・優先度を埋める
- [x] 3.1 規則 4 で担当 spec を決める
  - 4 本の brief が枝と概数で書いている範囲を実測の id 群へ広げる（brief は id を 1 つも列挙していない）
  - 規則 4 の表を上から先勝ちで当てる。裁定待ちの 2 件（`currentghost.seriko.zorder`・`currentghost.seriko.sticky-window`）は 4.7 の行より先に当たるので担当欄が空文字になる
  - 裁定待ちの 2 件の備考に「裁定待ち: 争っている spec 名と争点」の行を足す
  - 未実装の項目に完了済み spec 名を書かない。起票前の引受先の名前は担当欄に書かず、備考に「候補: ...」の形で書く。語彙表の 1 行だけを触る側も備考に回す
  - 完了状態: 検査 7 が緑（担当欄がすべて `.kiro/specs/`〔`completed/` を含む〕に実在する spec 名か空文字）になり、実装済み 2 件だけが完了済み spec `areka-P0-sylphya` を指し、担当欄が空文字の 2 件に裁定待ちの行が付いている
  - _Requirements: 4.4, 4.5, 4.6, 4.7, 4.8, 4.9, 4.10, 5.4_
  - _Depends: 2.4_

- [x] 3.2 規則 7 で関連を書く
  - 種別は上流が凍結した 6 種に限る。本台帳は `queries` を書かず、照会経路を指すときは `same-feature` を使う（凍結された向きを裏返さない）
  - 全項目に一様に効く読み取り経路 2 本は台帳に書かず、ブリーフィングの前置きに 1 度だけ書く
  - 設計の規則 7 の表のとおりに関連を張る（SET 有効 26 件→`\![set,property,...]`／拡張プロパティ 2 件ずつ→SHIORI 側とプラグイン側の `property.get`・`property.set` に本文が名指しする方だけ／裁定待ち 2 件・サウンド系・面と動作の番号→本文が名指しするタグ／初期値が descript のキーである項目→`configures`）
  - 相手の id は書く直前にスナップショットで実在を確かめる。正典に id が無い値の源は関連に書かず、2.4 で備考に書いた「値の源:」の行で持つ
  - 完了状態: `check` の検査 10（関連の相手・`alias_of`・`supersedes` の id がカタログに実在）が緑になり、`queries` を使った関連が 0 件であることを数で確かめられる
  - _Requirements: 6.1, 6.2, 6.5_
  - _Depends: 2.2, 2.4_

- [x] 3.3 規則 5・6 で優先度とテーマを置く
  - 規則 6 でテーマを付けるのは着せ替えに直接関わる 3 件（`currentghost.seriko.surfacelist.all`・同 `.defined`・`currentghost.scope(ID).seriko.defaultsurface`）に限り、「装い」1 語だけを使う。付けた 3 件には「無いと利用者がゴーストの何を失うか」の行を備考に足す。それ以外は空にする
  - **テーマを付けた 3 件の優先度は揃わない**（**2026-09-05 訂正**: 当初はこの 3 件が一様に扱えるように読めた）——`currentghost.scope(ID).seriko.defaultsurface` は正典で SET が有効な 26 件にも入っているため、規則 5 の第 2 行（SET 有効かつ未実装 → `C10`）が第 3 行（テーマを付けた項目 → `C20`）より先に当たり、`C10` になる。テーマを理由に `C20` が付くのは `currentghost.seriko.surfacelist.all`・同 `.defined` の **2 件だけ**である。テーマを付けること自体は優先度と独立なので、`defaultsurface` にも `values` と備考の「テーマ:」行は要る（要件 7.2）
  - 規則 5 の表を上から先勝ちで当てて優先度を置く。既定は `C90` とし、90 より大きい数値を使わない
  - 縮退 8 件にはテーマを付けない。優先度は規則 5 を上から先勝ちで当てる——バルーンの縦書き族 7 件は第 4 行で `C30`、`currentghost.seriko.zorder` だけは正典本文に `[SET有効]` の印があり実導出も無いため**第 2 行が先に当たって `C10`** になる（第 4 行までは届かない）（**2026-09-05 訂正**: 当初は「縮退 7 件にはテーマを付けず、優先度 `C30` で前に出す」と書いていた。件数が 8 件に増えたのに合わせ、増えた 1 件が `C30` ではなく `C10` になることを明記した。この 1 件は縮退へ移す前も第 2 行で `C10` だったので、今回の移動で優先度は 1 つも動かない）
  - 完了状態: 検査 8 が緑（使われた値が空文字・`C10`・`C20`・`C30`・`C90` のいずれか）、検査 12 が緑（テーマが凍結 8 語のいずれか・実際に使ったのは「装い」3 件だけ）になる。優先度の内訳は空文字 2・`C10` 26・`C20` 2・`C30` 7・`C90` 151 の計 188 件になる（**2026-09-05 訂正**: `C20` はテーマ 3 件ぶんではなく 2 件。残る 1 件の `defaultsurface` は第 2 行が先に当たって `C10` に入る）
  - 着地した `doc/ukadoc-coverage/values.md` は「記憶」の代表項目として本台帳の 2 件（`rateofuselist(名前).boottime`・`history.ghost.count`）を名指ししているが、**本タスクは「記憶」を付けない**（要件 7.1 の原則・要件 7.3 の例示・本タスクの「3 件に限り」・要件 7.6 の段階決定の委任による）。決めずに記録して統合担当へ回す扱いは要件 5.6 の流儀と同じで、詳細と帰結は design.md の規則 6 の 2026-09-05 訂正に書いた
  - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5_
  - _Depends: 2.2, 3.1_

- [ ] 4. ブリーフィングを書く
- [x] 4.1 前置きと所有の突合表を書く
  - `doc/ukadoc-coverage/briefing-property.md` を新設し、前置きに ⓐ 全項目に共通の読み取り経路 2 本 ⓑ `????` が正典の表記そのもので写しの過程で失われた記号ではないこと ⓒ 縮退 8 件と語彙のみ 178 件の実質差は登記の有無だけであること、を書く
  - ⑴ 節に 4 本の brief の記述を 1 行ずつ並べ、対応する id 群を接頭辞と件数で示す。id を全列挙するのは食い違いのある行だけにする。id に対応が付かない記述は表記の揺れとして残し、近い id へ憶測で結び付けない
  - 続けて 2.2 の SET の突合の 4 区分を置く
  - 節を別ファイルに切り出さず、ブリーフィング本文の 1 節として持つ。プロジェクトの中でしか通じない言い回しを使わず平易な日本語で書く
  - 完了状態: 4 本の brief すべてが表に 1 行以上を持ち、各行の id 群の件数の合計が台帳の 188 件と突き合わせられる状態になる
  - _Requirements: 4.1, 4.2, 8.1, 8.6_
  - _Depends: 2.2, 3.1_

- [x] 4.2 別名の一覧と、所有者なし／裁定待ちの一覧を書く
  - ⑵ 節に同じ値へ到達する名前の群の一覧を書く。群が 0 件ならその旨を明記し、brief が例示した旧 `balloon.*` 系がカタログに存在しなかったことも記録する
  - ⑶ 節に担当 spec 欄が空文字の全件を優先度つきで並べる。起票前の引受先は「候補: ...」の形で書く。裁定待ちの 2 件も同じ一覧に出す
  - 完了状態: 担当欄が空文字の台帳項目の件数と ⑶ 節の行数が一致し、⑵ 節が「群 0 件」を明示している
  - _Requirements: 3.7, 3.8, 4.5, 4.6, 5.4, 8.1_
  - _Depends: 3.1, 3.3_

- [x] 4.3 二重所有の裁定案を書く
  - ⑷ 節の冒頭に「これは案であって決定ではない」と明記する。まず 3 本の spec が何を主張しているかを brief の該当箇所を示して並べる
  - 推す案（値の導出は `areka-P0-zorder-property` が単独で持ち、`areka-P0-currentghost-property-tree` は `seriko.*` から `zorder` を外し、語彙表の 1 行は `areka-P0-property-query-channels` が持つ）を 1 つに絞って提示し、選んだ理由に `.kiro/steering/roadmap.md:111` の推奨との一致を挙げる
  - 採ったとき／採らなかったときの影響を 3 本の spec ごとに併記する。退けた代案を 1 行ずつ理由付きで残す
  - `currentghost.seriko.sticky-window` も同じ案の対象に含め、争っているのが 2 本だけであることと、案を採ったときの帰結を書く
  - 完了状態: ⑷ 節が「主張の並置」「推す案 1 つ」「採る／採らないの影響」「退けた代案」の 4 つを持ち、裁定そのものは行っていない
  - _Requirements: 5.1, 5.2, 5.3, 5.5, 5.6, 8.1_
  - _Depends: 4.2_

- [x] 4.4 既存 brief への是正候補と件数の差を書く
  - ⑸ 節に「どの brief のどの記述を、どの id の一覧へ書き換えるか」の形で是正候補を並べる。既存 brief の本文そのものは書き換えない
  - ⑸ 節に上流の README への提案 1 件を書き添える — 優先度の末尾の数値形（`C90` を既定とし 10 刻みにする）。**証拠のコメントの形の提案は取り下げる**（着地した実装が `///`・`//!`・`//` の 3 つを拾うことを `crates/ukadoc-survey/src/evidence/extract.rs:37` で確認済み）
  - ⑹ 節にカタログとの件数の差を書く（`currentghost` 69 対約 65・`history` 12 対 8・`headlinelist` 3 対 2・`pluginlist` 5 対 4）。原因が特定できた差は是正候補と同じ形でも書く。見出しが重複する `name`／`path` の 4 行の表もここに置く
  - 完了状態: ⑹ 節の 4 つの差すべてに原因（`.count` の葉の数え落とし・どの束にも入っていない `currentghost.balloon.mousecursor` 系 4 件）が書かれ、カタログの側を正としていることが読める
  - _Requirements: 1.6, 4.3, 7.6, 8.1, 8.2, 9.4_
  - _Depends: 4.3_

- [ ] 5. ソースの差分と標準のテストで回帰がないことを確かめる
  - 先に `git submodule update --init --recursive` と host-32 の i686 成果物のビルドを済ませる。submodule 未初期化だと `cargo` が `pasta_core` を見つけられずに転び、i686 未ビルドだと helper 系のテストが黙って飛ばずに明示的に失敗する
  - `cargo test --workspace` を通す。語彙表の件数を固定するテスト（`crates/areka-sylphya/src/vocab/dotted.rs:191`・`crates/areka-sylphya/src/ledger_key_determinism_tests.rs:201-204`）が現状のまま緑であることを確かめる
  - `crates/areka-sylphya/src/vocab/` の 3 ファイルに差分が無いこと、`dotted.rs` の URL を伴わない「ukadoc」の語 3 か所（`:3`・`:15`・`:67`）がそのまま残っていることを確かめる。後から実装する spec が自分の証拠を自分で置く前提なので、ここで先回りして URL を足さない
  - 完了状態: 検査 15（ソースの差分がコメント 2 行の追加だけで他のソースに差分なし）と検査 16（`cargo test --workspace` が成功）が両方緑になる。上流 `crates/ukadoc-survey` の**本番コード**（`src/` 配下）に差分が無い。*(2026-09-05 訂正: 当初は「上流のコードに差分が無い」と書いていたが、上流の空振り検査の国勢調査 `crates/ukadoc-survey/tests/consistency/checks.rs` は、台帳に初めて中身が入った側が主張を `Subjects::Zero` から `NonEmpty` へ書き換える設計になっている〔テスト自身の doc コメントがそう指示する〕。1.1 と 1.3 でこの受け渡しが起き、同ファイルには差分が残る。差分が許されるのはこのテスト 1 本だけで、`crates/ukadoc-survey/src/` は 1 バイトも動かさない)*
  - _Requirements: 9.3, 9.3a, 9.5, 9.6, 9.8_
  - _Depends: 1.3_

- [ ] 6. 受け入れ確認を回して結果を残す
- [ ] 6.1 報告を作り直し、道具の検査と自前の 4 検査を回す
  - `cargo run -p ukadoc-survey -- report` で `doc/ukadoc-coverage/report/property.md` を作り直す。手で編集しない。作り直した報告は台帳と同じコミットに載せる（`report-summary` は統合担当の持ち物なので走らせない）
  - `cargo run -p ukadoc-survey -- check` を回して食い違い 0 件を確かめる。出力の 2 つの数（食い違いの件数・証拠のある項目の件数）を控える
  - 1.2 で用意した手順で自前の 4 検査（6b・6c・7・8）を回し、各検査の合否と検査した件数を取る
  - 赤が出たら台帳を直し、報告を作り直してから回し直す。検査 9 のように対象 0 件で緑になったものは、その旨が分かる形で結果を残す
  - 完了状態: `check` が食い違い 0 件・証拠のある項目 2 件で通り、自前 4 検査がすべて緑になり、各検査に「検査した件数」が付いた結果の一覧が手元にある。`report/property.md` の状態の分布が実装済み 2・語彙のみ 178・縮退 8・未対応 0 を映している（**2026-09-05 訂正**: 当初は語彙のみ 179・縮退 7 と書いていた。2.1 の訂正に合わせる）
  - _Requirements: 2.1, 3.1, 4.4, 6.5, 7.4, 8.3_
  - _Depends: 3.2, 3.3_

- [ ] 6.2 結果と末尾注記をブリーフィングに書き、境界を最終確認する
  - ブリーフィングの末尾に検証の結果を書く — 道具の `check` の 2 つの数、自前 4 検査の件数と合否、`cargo test --workspace` の結果、対象 0 件で緑になった検査の名指し
  - 同じ末尾に 1 行だけ経緯を残す — 設計時は「上流の整合検査 6.4・6.7 は他 3 台帳が揃うまで確かめられない」と注記する予定だったが、上流の実装が 4 ドメインの台帳ごと着地したため両方が成立し、注記は不要になった
  - 触っていないことを最終確認する — `values.md`・`README.md`・`catalog.toml`・`report/summary.md`・他 3 ドメインの台帳と報告・既存 4 brief の本文・`.kiro/steering/roadmap.md`・上流 `crates/ukadoc-survey/src/`（本番コード）に差分が無い
  - 完了状態: 本 spec がコミットするファイルが `doc/ukadoc-coverage/ledger/property.toml`・`doc/ukadoc-coverage/report/property.md`・`doc/ukadoc-coverage/briefing-property.md`・`crates/areka-ghost/src/sylphya_wiring.rs`・`crates/ukadoc-survey/tests/consistency/checks.rs` の 5 本だけであることを差分で確かめられ（*2026-09-05 訂正: 当初は 4 本と書いていた。5 本目は上流の国勢調査の受け渡しで、1.1 と 1.3 が主張を裏返した分。上流の本番コード `src/` には差分が無い*）、一時領域の使い捨て手順がコミットに含まれていない
  - _Requirements: 8.3, 8.4, 8.5, 10.2, 10.3, 10.5_
  - _Depends: 5, 6.1_

## Implementation Notes

- **上流の「空振り検査の国勢調査」は、データを埋めた側が主張を書き換える** — `crates/ukadoc-survey/tests/consistency/checks.rs` の `census()` は、実データが 0 件である要件の面を `Subjects::Zero` として固定している。台帳に初めて中身を入れるとこのテストが赤になり、失敗文と doc コメント（`:249-253`）が「その行を `Subjects::NonEmpty` へ移し、doc コメントの 2 つの列挙を追随させよ」と指示する。これは設計された受け渡しであって迂回ではない。**まだ `Zero` のまま残っている行と、それを裏返す担当タスク**: `6.5 ソースの正典 URL`・`6.11 証拠の付いた項目`＝1.3／`6.6 状態が implemented の行`＝2.1／`6.7 関連・別名・後継の相手`（および `6.7 状態が alias の行`・本ドメインは別名 0 件の見込みなので裏返らない可能性が高い）＝3.2／`6.8 台帳に書かれたテーマ名`＝3.3。データが着地していない行を先に裏返すと逆向きに赤になるので、必ず自分のデータを入れてから触ること。
- **台帳を書き換えたら報告を作り直してから commit する** — `report/property.md` の「SSP 世代別の対応表」などは台帳を鍵にしている（`crates/ukadoc-survey/src/report/domain.rs:81-83`）ので、台帳だけを commit すると上流の `check` が `DomainReportStale` で赤になる。design.md の File Structure Plan が「台帳と同じコミットに載せる」と定めているとおり、`cargo run -p ukadoc-survey -- report` を回してから同じコミットに載せる（親が commit 時に行う）。なお `report` は 4 本すべてを書き出すが、他 3 本は内容差分ゼロ（改行コードの揺れだけ）なので `git diff` には現れず、staging しなければ要件 10.2 に触れない。
- **設計・タスクの実測値は疑ってかかる** — 1.1 で「版番号を持つ項目 98 件」が実測 99 件と食い違った。原因は複数版を持つ 1 件（`system.os.(キー)`）の数え落とし。数を合わせに行かず、カタログ（`doc/ukadoc-coverage/catalog.toml` の `versions`）を正として実測を報告し、spec 文書の側を訂正すること。
- **自前 4 検査の手順の在処（タスク 6.1 が再実行する）** — `C:\Users\maz-o\AppData\Local\Temp\claude\C--home-maz-git-areka--claude-worktrees-areka-p0-ukadoc-survey-57d0e2\18f57362-d173-4c21-8eeb-3c62500dc579\scratchpad\selfcheck4.py`。引数なしで検査 6b・6c・7・8 を回し、各検査の合否と「検査した件数」を出す。`--calibrate` は 20 例の較正だけ、`--all` は較正を先に走らせてから本検査。`--repo` / `--ledger` / `--specs-root` でパスを差し替えられる。4 検査すべてが緑のときだけ exit 0。**検査 7・8 は「非空の件数」も出す** — 全件が空文字のうちは実在照合も値の照合も 1 回も走っていないので、緑に意味が出るのは非空が 0 でなくなってから。ブリーフィング末尾にはこの区別ごと書き写すこと。repo には 1 行も置いていない（要件 10.1）。
- **SET 突合の結果は一時領域の `set-matrix.json` が正本（2.3・3.3・4.1 が読む）** — 一時領域（`.../18f57362-d173-4c21-8eeb-3c62500dc579/scratchpad/`）の `set-matrix.json` と `set-matrix-summary.md`。全 188 件について「正典が SET 有効とするか・3 形のどれで読んだか・その根拠の本文」を持ち、4 区分は件数ではなく id の一覧で入っている。**`open_question` の欄に未決の判断点が 1 件ある** — `activeghostlist(…).ext.拡張プロパティ名` 系 4 件は本文が「取得・設定する」と書くのに `[SET有効]` の印が無い。設計どおり 26 件のまま（含めない）で進め、広い読み（30 件・区分 ⑵ が 9 件・当該 4 件の優先度が C90→C10・brief の「21→26」との一致が崩れる）は 4.1 がブリーフィングへ 2 つ目の裁定案として書き、開発者と統合担当へ回す（要件 5.6 と同じ流儀）。レビューは「正典の本文だけで見れば広い読みも同じくらい強い」と判定しているので footnote 扱いにしないこと。同欄には 2.3 が当該 4 件の「書き込み」行に使う文言も入っている（「SET の定めなし」と書いてはならない）。
- **備考に正典の本文を写さない（要件 10.6）** — 2.3 の 1 周目は「壊れ方」の一文を正典本文の第 1 文から起こしてしまい、本ページの本文の約 29%（4,240 字）が台帳に入った。引用長は説明文の平均 91.8%＝実質そのままの写しで、`備考` が許された欄であることは本文を入れてよい根拠にならない。**正しい形は規則 8 の 3 型の表が型ごとに与える定型文**で、型の中で同じ文言になるのは設計の意図。項目が何を指すかは id と `catalog.toml` の `title` が持っているので台帳で言い直さない。区別が要る群だけ台帳自身の言葉で 1 句を足す（`rateofuselist` の通算版／月報版／週報版）。
- **要件 10.4 の 6 件には意味を 1 語も書かない** — `currentghost.balloon.scope(ID)` の `validwidth`・`validheight`・`lines`（各 `.initial` 込み）はスナップショットが 2.8.80 のままで、2.8.83 で役割が入れ替わっている。「幅とは何か」「スクロール前か後か」を書くこと自体が禁じられた意味の判定に当たる。備考は縮退の定型文だけを持ち、転記元は 2.4 が足す。
- **4.1 への申し送り**: ブリーフィングで `rateofuselist` の備考の重複を説明するときは「呼び出し形の対」ではなく **「3 つの別の量（起動時間・分／起動回数／起動時間割合）× 2 つの呼び出し形（`.index(順位)` と `(名前)`）」** と書くこと。1 周目の報告は前者と書いていたが実測は後者。
- **3.2 への申し送り: 規則 7 の関連の表に載っていない「値の源」が 2 件ある（レビューが実測）** — ⑴ `currentghost.scope(ID).x`／`.y` は本文が `sakura(kero,char*).defaultx`／`.defaulty` を名指しし、相手はカタログに実在する（`descript_ghost` 側 6・`descript_shell` 側 6 の計 12 id）。⑵ `currentghost.status` は本文が SHIORI Status ヘッダを名指しし、相手 `ukadoc:spec_shiori3:Status_20_5bSSP_62e1_5f35_5d:1` が実在する。どちらも規則 7 の `configures` の行が `menu` 群と `scope(ID).name` しか挙げていないため、**3.2 が足さないと値の源がどちらの仕掛けでも登記されない**（2.4 は「相手にカタログ id がある」ことを理由に備考の「値の源」行を意図的に付けていない＝要件 6.5 と規則 7 の分担どおり）。⑶ 弱い候補として `currentghost.seriko.surfacelist.all`／`.defined` があるが、相手は `descript_shell_surfaces` ページ全体であって単一のキーではないので 3.2 の判断に委ねる。
- **備考の欄の分担**: 相手にカタログ id が**ある**なら 3.2 の `links`、**無い**なら 2.4 の「値の源:」行。二重登記しない。2.4 が書いた 32 件（`system.*` 25・`currentghost.scope(ID).currentmonitor.*` 5・`scaling` 2）はいずれも規則 7 の表に現れない。
- **報告は担当欄を消費しない** — `crates/ukadoc-survey/src/report/domain.rs` は `owner` を 1 度も読まない（節は状態分布・ページ別・世代別・別名・テーマ・関連の束の 6 つ）。担当欄を埋めても `report/property.md` は動かず、陳腐化検査も鳴らない。報告が動くのは `status`・`introduced`・`values`・`links` を触ったときだけ。`summary.rs` の `owner` は「どのドメインの台帳が id を持つか」で台帳の担当欄とは別物。
- **4.4 へ渡す件数の差は 6 つ（要件 4.3 が挙げているのは 4 つ）** — 実測でカタログが正: tree `currentghost.*` 約 65 対 **69**（差は `currentghost.balloon.mousecursor`／`.arrow`／`.text`／`.wait` の 4 件）／lists `history` 8 対 **12**・`headlinelist` 2 対 **3**・`pluginlist` 4 対 **5**（いずれも `.count` の葉の数え落とし）。要件 4.3 が挙げていない 5 つ目と 6 つ目 — ⑸ lists の「`ghostlist` 5・`activeghostlist` 5＋`.ext`」＝ 12 は **10**（`.ext` 2 件は `activeghostlist` の 5 件に既に含まれるので二重に数えている）／⑹ lists の「汎用プロパティ名 17」は ukadoc 上では **13**（17 は sylphya の `GENERIC_PROP_NAMES` の件数で、13＋`menu` 群 4）。
- **4.4 へ渡す判断 2 件（記録を残すこと）** — ⑴ `menu` 群 4 件を lists に付けたのは規則 4 第 3 行（唯一の主張者）による。lists の brief が「汎用 17 葉」を `GENERIC_PROP_NAMES` と名指しで同定しており、その列挙に 4 件が実在するため憶測ではない。ただし正典はこの 4 件を `currentghost` の下だけに置き（本文が「currentghost.shelllist のみ有効」「currentghost のみ有効」と書く）、lists が挙げる理由（各根の下で乗算される）は成り立たない＝件数の差 ⑹ と同じ 4.3 級の食い違いなので是正候補に並べる。⑵ `currentghost.sound.*` の頭 3 件には「語彙表だけを触る spec」の行を付けていない。channels の記述が「サウンド語彙 ≈18 葉」＝葉を指しており、頭まで広げるのは要件 4.2 が戒める推測に当たるため。担当欄はどちらの読みでも lists で変わらないので、判断が見えるよう ⑸ 節に 1 行残す。
- **4.1 が必ず書くこと（3.2 のレビューが 2 件を名指し）** — ⑴ **全項目に共通の読み取り経路 2 本**（`%property[プロパティ名]` と get,property のタグ）。要件 6.1 はこの 2 本を関連の相手として挙げているが、設計はあえて台帳から外してブリーフィングの前置きへ回した。4.1 が落とすと 6.1 が誰にも拾われずに未達になる。⑵ **報告の「関連の束」節が空である理由**。関連は 73 本あるのに `report/property.md` は「ドメイン内で閉じている束はありません。」と出る。上流の `closed_bundles`（`crates/ukadoc-survey/src/report/domain.rs:203-213`）が両端とも同じ台帳にある束だけを残す作りで、73 本すべてが本ドメインの外を指すため。書かないと読者は「関連が無い」と誤読する。
- **4.1 が書いてはいけないこと（撤回された理由付け）** — 3.2 は `descript_shell:seriko.zorder`／`sticky-window` へ関連を張らない判断をしたが、その 2 つ目の理由「設定キー→挙動を本台帳から書くのは `queries` と同じ裏返しになる」は**誤りなので撤回された**。規則 7 はその向きを明示的に認めており（「assets 台帳の持ち物。指すだけ」）、実際 24 本の `configures` を本台帳から書いている。**成り立つのは 1 つ目の理由だけ** — どちらのプロパティの本文も descript に触れず、タグしか名指ししない。裏返しの議論が有効なのは `queries` だけ（本台帳の項目は常にその矢の終点）。
- **台帳の備考に逆斜線を書かない** — TOML の `"""` 文字列の中で `\!` は不正なエスケープで、`check` が赤になる。タグ名を備考に書くときは `set,scaling` のように逆斜線を落とす。現在の台帳には逆斜線が 1 つも無い。
- **4.1 への注意（レビューの補足）** — バルーン側の `currentghost.balloon.scope(ID).scaling` が指す `descript_shell_surfaces:scaling` の本文はキャラクター窓とサーフェスの話で、バルーン専用の相手はカタログに無い。関連としては強制された唯一解だが、ブリーフィングでバルーンの被覆を過大に言わないこと。
- **4.1／6.1 が書くこと: テーマ「記憶」の非採用をブリーフィング本体にも載せる** — 着地した `doc/ukadoc-coverage/values.md` の「記憶」節は本台帳の id を 2 件（`rateofuselist(名前).boottime`・`history.ghost.count`）代表例に挙げているが、本 spec はテーマを付けなかった（要件 7.1 の「原則として空」・7.3 の例示・tasks 3.3 の「3 件に限り」・7.6 で段階の決定は統合担当、による）。**結果として `report/property.md` は「記憶 0 件」と出る一方 `values.md` は本ドメインの id を名指ししており、両方を読む人には食い違って見える。** design.md 規則 6 には記録済みだが、**統合担当が出会うのは出荷される文書の側**なので、ブリーフィングにも影響つきで書くこと（採るなら各 id が `C90`→`C20`・テーマ行が要る・「装い 1 語だけ」の記述を要改訂／2 件だけか `rateofuselist` 24＋`history` 12 の枝ごと最大 36 件かは未決）。ext の SET の件と同じく裁定案として並べる。
- **ブリーフィングの節の約束（4.1 が敷いた）** — 見出しは `##`（`## 前置き`・`## ⑴ 所有の突合表`）。4.2 は `## ⑵`・`## ⑶`、4.3 は `## ⑷`、4.4 は `## ⑸`・`## ⑹` を同じ深さで追記し、6.2 が末尾の検証記録を足す。節の中は `###`／`####`。冒頭に「この文書は次の順で書く」の一覧が既にあるので消さないこと。id の表は `| 見出し | id |`。本ページの id は `ukadoc:list_propertysystem:` の接頭辞を省く約束を前置きで宣言済み・他ページの id は全長で書く。
- **4.3 への指示: テーマ「記憶」の非採用を ⑷ 節へ移す** — 4.1 は前置きに書いたが、レビューの判定は「⑷ が正しい置き場」。理由＝ext の件は ⑴ 節が主張する「26」という数の出どころなので ⑴ に置くのが正しいが、記憶は前置きに掛かる錨が無く、影響（`C90`→`C20`・テーマ行が要る・「装い 1 語だけ」の記述の改訂・2 件か最大 36 件かが未決）を読むべき相手はまさに ⑷ を読む人。**移動であって複製ではない**。前置きには 1 行の案内だけ残す。
- **4.4 への指示 2 件（4.1 のレビューが名指し）** — ⑴ **サウンドの要素葉 18 件は id を全列挙する表が要る**。他の行と違って共通の接頭辞が無く（`duration`・`error`・`id`・`loop`・`meta.*` 8・`name:2`・`path:2`・`pause`・`playing`・`position`・`preload`）、「サウンドの要素葉 18」だけでは brief の担当者が id 一覧へ書き換えられない。⑹ 節に既に `name`／`path` の 4 行の表があるのでそこが置き場。⑵ **tree brief の 69 対約 65 の説明は「mousecursor 系 4 件」だけでは足りない**。brief 自身の内訳の合計は 62 で、62＋4＝66≠65。4.1 は 62 を正直に報告して差の説明ごと ⑹ へ送っているので、4.4 は 4 件より多くを説明することになる。
- **要件 3.8 の前提そのものが読み違いだった（4.2 のレビューが裁定）** — 3.8 は「brief が旧 `balloon.*` 系を新旧の例として挙げているが、カタログに存在しない」と書いているが、`areka-P0-currentghost-property-tree` brief:23 を文脈ごと読むと、その箇条書きは 17 の葉を接頭辞を落として並べており、`balloon.汎用`／`balloon.count` も同じ省略形（`currentghost.balloon.*` の直下という意味）にすぎない。**旧い名前を挙げているのではなく、短く書いただけ。** 算数も裏付ける（17＋1＋1＝19 が brief 自身の 19 と一致）。ブリーフィング ⑵ 節はこの読みで書かれており、brief の担当者には「壊れている」ではなく「全長で書いてほしい・理由は要件 3.8 自身が読み違えたほど紛らわしいから」と伝えている。要件の受入基準そのものは書き換えていない（満たし方は変わらないため）。
- **4.4 が直す軽微 2 件（4.2 のレビュー）** — ⑴ ブリーフィング ⑵ 節の「11 件はすべて動きの説明」は 1 件だけ言い過ぎ（`currentghost.scope(ID).name` は値の源の記述）。結論（「ある名前を別の名前へ読み替えよと述べたものは 0 件」）は動かない。⑵ 同節の「上流の空振り検査の国勢調査」は要件 8.6 が戒める内輪の言い回しに最も近い。平易な言い方へ直す。
- **設計が引いていた `roadmap.md:106` は誤りで、正しくは `:111`（4.3 が発見・実測で確認）** — `:106` は「e2e ⇄ toolkit」の行で推奨は載っていない。三重所有の推奨（「推奨＝切り出し: 値の導出は `zorder-property` 単独・tree は `seriko.*` から `zorder` を除外・台帳行 1 本は channels⑶ が持つ」）が載っているのは `:111`。引用の中身は設計が書いたとおりだったので案 甲の理由付けは成立する。design.md 2 か所・tasks.md 1 か所を訂正し、design-validation.md:26 の「逐語で確認・一致」という**検証の主張**にも訂正注記を入れた（古い計画の記述より、古い検証の記録の方が害が大きいため）。research.md の 2 か所は当時の計画の記録なのでそのまま。
- **4.4 が ⑸ 節へ必ず載せる是正候補 1 件（4.3 のレビューが名指し）** — ⑷ 節が「`areka-P0-zorder-property` brief:22 の『dotted.rs の 21 項に入れない・本 brief が語彙の正本』は ⑸ 節の是正候補に載せる」と前を指している。4.4 の指示にこの項目が名指しされていないので、明示的に引き継がないと参照が宙に浮く。
- **4.4 が直す軽微 3 件（4.3 のレビュー）** — ⑴ ⑷ 節の中で `1.`／`2.` で番号を振った一覧を後から ⑴／⑵ で参照している箇所がある。この文書では ⑴〜⑹ が節番号で、⑴ 節の中にはさらに区分 ⑴〜⑷ があるので紛らわしい。「1 つ目」「2 つ目」に直す。⑵ ⑷ 節の中で同じものを「書き込み有効一覧」と「語彙表」で呼び分けている。⑶ 4.2 のレビューが挙げた ⑵ 節の 2 件（「11 件はすべて動きの説明」の言い過ぎ・「上流の空振り検査の国勢調査」の内輪言葉）も同時に直す。
- **6.2 が仕上げる軽微 3 件（4.4 のレビューが名指し・いずれもブリーフィングの文言）** — ⑴ 4.4 の用語統一は ⑷ 節に限って行ったので、⑴ 節の「この文書のほかの場所では『areka 側の語彙表』と書いている」という但し書きと、⑴ 節 Z2・⑶ 節が同じ `SET_EFFECTIVE` の 1 行を「語彙表の 1 行」と呼んでいる箇所が残る。文書全体で 1 度そろえる。⑵ ⑴ 節 L6 の前方参照「語彙表の側を直すべきかどうかは ⑸ 節で扱う」は、⑸ の是正 6 が brief の書き方だけを裁いて「areka の 17 名は変えない」を暗黙にしている。1 行で明示する。⑶ ⑵ 節の見出しがまだ「古い `balloon.*`」の枠組みを引きずっており、同じ節の本文と ⑸ の是正 5 がそれを否定している。見出しを本文に合わせる。
- **文書内の並び順の数え方は 1 起点（4.4 のレビューが裁定）** — `name:1`=154・`path:1`=159・`char*.bind.menu`=170・`name:2`=171・`path:2`=172。2.4 の報告にあった 153／158／169／170 は 0 起点で数えた値で、事実の食い違いではない。**台帳の備考は位置を数値でなく「群の先頭」「char*.bind.menu の直後」と序数で書いているので直す必要は無い**。ブリーフィング ⑹ 節が 1 起点で書いている。
- **`currentghost` の「約 65」は brief のどの足し算とも合わない（4.4 が確定・レビューが追認）** — 実測の積み上げは 62（brief の枝別の合計）＋4（brief:23 が括弧で挙げてどの束にも足していない `currentghost.balloon.mousecursor` 系）＋3（`currentghost.sound.*`＝lists brief:22 が tree から切り出したと書いている分）＝69。65 に届く組み合わせは 62＋3 の 1 通りだけで「切り出し前の値が残ったもの」と読めるが、裏づけがどこにも無いので**推測として明示し確定させていない**。是正候補は 65 の出どころに依存せず「66 へ置き換える」を提案している。
