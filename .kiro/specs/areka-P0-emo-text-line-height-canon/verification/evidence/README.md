# 根拠画像と読み取り値（Requirement 1.3・6.1・2026-09-05）

> 開発者方針「SSP 実測主義は取らない。意味論は輸入する」（2026-09-05）に従い、SSP の画素実測は行わない。本ディレクトリは裁定の根拠として用いた表示画像 2 枚と、その目視読み取り値を保存する場所である。

**画像の到着を待つ必要はない**: 行送り・文字寸法の裁定は既に成立しており、正本は本仕様の `design.md` §4.1／§4.3 と `doc/COMPAT_ARCHITECTURE.md` §8 にある。画像は裁定の根拠を後から見返すための添え物であり、後続タスク（2.x 以降）は 2 枚が届くのを待たずに進める。届いたら本ファイルの「状態」欄を実ファイル名の参照へ書き替えるだけでよい。

## 画像

| ファイル | 内容 | 状態 |
|---|---|---|
| `ssp-emo2-200pct-2026-09-05.png` | SSP 2.8.83・ghost `emo`・balloon `emo2-kakukaku`・表示 200%（192 DPI の面）・本体側「昼間っから呼んでくれるん？／嬉しいわぁ！」・相方側「暇なだけだと／思うよ。」 | **未着（受領待ち）**——出どころは開発者。2026-09-05 の設計ディスカッションでチャットへ貼られた画面の写しであり、リポジトリには未取り込み。取り込みには開発者からの元ファイル提供が要る。下の読み取り値と裁定はこの画像を見て記録したものなので、ファイルが届かなくても裁定は成立している（正本は design §4.1・COMPAT §8）。届いたら同じファイル名で本ディレクトリへ置く |
| `areka-emo2-200pct-2026-09-05.png` | areka（本ブランチ HEAD `36d1c323` 時点の `target\debug\areka.exe`・2026-09-05 15:49 ビルド）・同 fixture・192 DPI の面・本体側「こんばんはー！夜やけど／元気やでー！」・相方側「その元気、どこから／湧くの。」 | **未着（受領待ち）**——出どころは同じく開発者がチャットへ貼った画面の写し。デスクトップ全体を写した画面写しなら撮れる（この 1 枚がそれ）が、窓を名指しして画素を読む道具はリポジトリに無く（本仕様 `research.md` の「画素を読む道具は無い」の行＝`PrintWindow`／`BitBlt` の利用実績はリポジトリに無い）、areka のゴースト窓は GPU 合成窓（`WS_EX_NOREDIRECTIONBITMAP`）なので窓単位のキャプチャでは中身が返らない（`.kiro/specs/completed/wintf-ulw-removal/design.md:407`）。加えて開発者の画面をこちらから操作できないため、この 1 枚を撮り直して差し替えることはできない。裁定への影響は SSP 側と同じくなし |

等倍の確認: どちらの画像もバルーン画像（本体側 400 image px 幅）が画面上 ≈ 800px で写っており、物理 px 等倍（k = 2）である。以下の読み取り値はすべてこの等倍（物理 px ＝ image px × 2）を前提に読んだ。

## 読み取り値（目視・±5 物理 px）

読み取り元の対応: 「SSP」列はすべて `ssp-emo2-200pct-2026-09-05.png`、「areka（改訂前）」列はすべて `areka-emo2-200pct-2026-09-05.png` から読んだ。行ごとに、どの字・どの行を測ったかを「読み取り元の字・行」欄に示す。

| 量 | 読み取り元の字・行（SSP／areka） | SSP | areka（改訂前） | image px 換算（÷2） |
|---|---|---|---|---|
| 1 文字の送り（本体側の 1 行の幅 ÷ 文字数） | 本体側 1 行目「昼間っから呼んでくれるん？」／本体側 1 行目「こんばんはー！夜やけど」 | 「昼間っから呼んでくれるん？」13 文字 ≈ 585px → ≈ 45／字 | 「こんばんはー！夜やけど」11 文字 ≈ 505px → ≈ 46／字 | ≈ 22.5〜23（Yu Gothic UI は仮名の送りが em より狭い） |
| 字のインクの丈 | 本体側 1 行目の「昼」／本体側 1 行目の「夜」 | 「昼」≈ 45px | 「夜」≈ 44px | ≈ 22 |
| 行送り（1 行目→2 行目の字の上端） | 本体側の 1 行目と 2 行目（上の「内容」欄に挙げた 2 行）の字の上端の差 | ≈ 58〜60px | ≈ 72px | SSP **29〜30**・areka **36**（実装値 35） |
| 相方側 1 文字の送り | 相方側 1 行目「暇なだけだと」／相方側 1 行目「その元気、どこから」 | 「暇なだけだと」6 文字 ≈ 275px → ≈ 46／字 | 「その元気、どこから」9 文字 ≈ 400px → ≈ 44／字 | ≈ 22〜23 |

上の数値は 2026-09-05 に 2 枚の画像を目視して読んだ値であり、本ファイルの改訂で読み直してはいない。

## 「閉じる」の右端の見え方（Requirement 6.1 の裁定の裏付け欄）

| 項目 | 記録 |
|---|---|
| 状態 | **未取得** |
| 理由 | 保存対象の 2 枚はどちらも通常のトークを表示した画面であり、ダブルクリックメニュー（`menu.pasta:15`＝「おしゃべり頻度」「エモの位置調整」「閉じる」）を開いた画面ではない。したがって、相方側バルーン `emo2-kakukaku` で「閉じる」の右端が SSP でどう見えるかは 2 枚のどちらにも写っていない |
| それでも裁定が成立する理由 | 折返し基準（`wordwrappoint`＝ここを超えたら折り返す）と描画範囲（`validrect`＝ここを超えてはならない絶対上限）の二段構えという裁定は、ukadoc の記述（下の「参照」の `validrect`「テキスト描画範囲」と `wordwrappoint.x`「自動改行で折り返すX座標」・未指定時は「validrect.right まで書けるものとして扱う」）から導いた意味論であり、SSP の画面を見て決めたものではない。加えて上の読み取り値で SSP と areka の字の大きさが一致している（＝どちらも同じ em で字を置いている）ので、「閉じる」3 文字の送りも両者で同じ見込みとして扱える。裁定本文は design `§4.3`、登記は `doc/COMPAT_ARCHITECTURE.md` §8 にある |
| 扱い | **開発者の裁定へ回す**。SSP でメニューを開いた画面が別途撮れれば、その 1 枚を本ディレクトリへ加えて本欄を実測へ差し替える。撮れないままでも、収まるかどうかは実フォントの文字送りから決定論テスト（`kero_menu_capacity_test.rs`）で確かめるので、後続タスクは本欄を待たない |

## 読み取りから導いた裁定（Requirement 1.1／1.2）

- 字の大きさ（送り・インク丈）は SSP と areka で一致 → `font.height` は **em**（areka の現行解釈のまま）。候補 α（セル丈＝em を 28 ÷ 1.33 へ縮める）は不採用。
- 行送りだけが 30 対 35 で異なる → 行送り ＝ `font.height + 行間`・行間の既定は **定数 2 image px**（里々 wiki「既定フォント 12 で 1 行 14px」とも一致）。
- インク丈 ≈ 22 image px < 行送り 30 なので隣接行のインクは重ならない。brief の「係数 1.0 はインクが重なる」は行ボックス（37.24）とインクの取り違えであった。

## 再測の手順

1. SSP で ghost `emo`・balloon `emo2-kakukaku` を表示 200%（192 DPI の面）にして、本体側・相方側に 2 行以上のトークを出した画面を 1 枚撮る（SSP の設定やバルーンの複製には手を入れない）。
2. 同じ fixture・同じ DPI で areka を起動し、同様に 2 行以上のトークが出た画面を 1 枚撮る。
3. どちらもバルーン画像（本体側 400 image px 幅）が画面上 ≈ 800px であることで等倍（k = 2）を確かめてから、上の表を読み直す。

## 参照

- ukadoc `font.height`: https://ssp.shillest.net/ukadoc/manual/descript_balloon.html#font.height_2c_6570_5024:1
- ukadoc `\_l[x,y]`（`XXem`／`XXlh`「1lh＝1em＋行間」）: https://ssp.shillest.net/ukadoc/manual/list_sakura_script.html#_5c_l_5bx_2cy_5d:1
- ukadoc `\f[height,数値]`（「スタイルシートのサイズ指定も可能」）: https://ssp.shillest.net/ukadoc/manual/list_sakura_script.html#_5cf_5bheight_2c_6570_5024_5d:1
- ukadoc `wordwrappoint.x`「自動改行で折り返すX座標。…未指定の場合はvalidrect.rightまで書けるものとして扱う」（「閉じる」の欄の根拠）: https://ssp.shillest.net/ukadoc/manual/descript_balloon.html#wordwrappoint.x_2c_5ea7_6a19_20_2a1:1
- ukadoc `validrect.right`「テキスト描画範囲の右位置」（同上）: https://ssp.shillest.net/ukadoc/manual/descript_balloon.html#validrect.right_2c_5ea7_6a19_20_2a1:1
- 里々 wiki「選択肢 › 2 段組メニュー」（「戻りたい行数×14」）: https://soliton.sub.jp/satori/?%E9%81%B8%E6%8A%9E%E8%82%A2#dc10653c
