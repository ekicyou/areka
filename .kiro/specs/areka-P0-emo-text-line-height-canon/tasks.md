# Implementation Plan

> 実装順は design §12（コミット順）に一致する。意味論（正典表）→ 式の実装 → 既存テストの再導出 → 二段折返し → 新規テスト → 登記と全体検証。正典表と実装がずれた中間状態を残さない（9.6）。実行環境は Yu Gothic UI が在る Windows（実フォントを使うテストの前提）。

- [x] 1. 基盤: 着手前提の確認と、意味論の確定（文書）
- [x] 1.1 着手時の前提確認と再導出台帳の確定
  - 本ブランチ以外の進行中 spec が `draw.rs` を触っていないことを `git log main..HEAD -- crates/areka-emo-text/src/draw.rs` で確認し、対象 6 ファイル（`draw.rs`・`layout.rs`・`state.rs`・`region.rs`・`actor.rs`・`choice.rs`）の現行行数が 1,000 行未満であることを記録する
  - 行送り・行ボックス丈・帯・拡大率に数値で依存する箇所を crate 全域（`src`／`tests`／`examples`）の検索で洗い出し、全ヒットを design の再導出台帳（32 ファイル）と突き合わせて、台帳に無いファイルを追記する
  - 旧調整値の識別子を名前で参照している箇所（製品コードと兄弟テストの構造体リテラル・値比較）を別欄として洗い出す。これはクレートのコンパイル可否に関わるため、期待値の再導出とは別の欄にする
  - 警告 1 件の追加でログ件数が変わり得るテスト（領域の in-file テスト・出荷 fixture の領域テスト・カーソルタグのテスト土台）を台帳へ 1 行として加える
  - 完了状態: 確定した台帳が `verification/derivation-ledger.md`（ファイル名・分類 A〜D・旧値と新値・改名追随の要否）として実在し、design の 32 ファイル一覧との差分が 0 件か、差分がその台帳へ追記済みであること。以降 2.x／3.x はこの 1 ファイルだけを参照先とする
  - _Requirements: 7.1, 9.5_

- [x] 1.2 正典表・裁量記録・アーカイブ注記の改訂
  - 互換アーキテクチャ文書の沈黙ルール対応表へ 2 行（`font.height` の意味・行送りの式・行間の既定／折返し基準と描画範囲の二段構え）を先例の書式で追加し、完了 spec の表を上書きした事実と根拠（ukadoc・表示画像・里々 wiki・裁定日）を書く
  - 選ばなかった案（供給面を折返し基準まで広げる／現状維持で欠けさせる／折返し基準を描画範囲へ丸め込むだけ）とその帰結が正典表に残っていることを確認する
  - 完了 spec `areka-P0-emo-text-layer` の design 2 箇所へ 1 行注記のみを添え、表の中身は書き換えない。同 research のリスク行へ消化注記を添える
  - `cursor-tag-canon` の `lh` 定義は改訂せず、本仕様の式がそれを実体化する旨を対応表の行に添える
  - 完了状態: 対応表に 2 行が入り、アーカイブ 3 箇所の差分が注記行の追加のみであること
  - _Requirements: 2.1, 2.2, 2.3, 2.5, 6.1, 6.8, 1.1, 1.2, 1.4, 1.5, 1.6, 1.7_

- [x] 1.3 根拠画像 2 枚と読み取り値の保存
  - `verification/evidence/` に SSP・areka の 200% 表示画像を PNG で置き、README の「未着」表記を実ファイル名へ差し替える。SSP 画像は開発者からの受領、areka 画像はコントローラの実機撮影が前提であり、未着のままなら README に理由と受領待ちを明記して 1.2 および 2.x 以降を止めない（裁定自体は 1.2 の正典表で既に成立している）
  - 「閉じる」の見え方（裁定の裏付け欄）は現行の 2 枚（通常トークの画面）には写っていないため、メニューを開いた画面を別撮りして添えるか、撮れない場合は「未取得・裁定は ukadoc の記述と字の大きさの一致で成立」と欄に明記して裁定へ回す
  - 読み取り値の表（1 文字の送り・インク丈・行送り・物理 px と image px 換算・等倍の確認）を画像と対応づける
  - 完了状態: PNG 2 枚が実在して README の表が両画像を参照しているか、未着・未取得の欄が理由つきで明記され、後続タスクが待たずに進める状態になっていること
  - _Requirements: 1.3, 1.1, 6.1_

- [x] 2. 行送りの式を 1 点へ寄せ、実装を追随させる
- [x] 2.1 行送りの式を差し替え、旧調整値の参照をすべて追随させる
  - 調整値の持ち物を旧係数から行間（既定 2・image px）へ差し替え、行送り＝フォント高さ＋行間（切り上げなし）を返す関数と、非有限・負の行間を警告つきで 0 へ縮退する関数を置く。製品コードに旧式の口（係数の乗算・切り上げ）を残さない
  - 決定論の代役（固定メトリクス）と実フォントのメトリクスの両方が、行送りを自前で計算せずこの式を呼ぶようにする。実フォント側が保持する調整値を係数から調整値そのものへ替え、フォント実寸を取得できないときの警告つき縮退経路と欄名を追随させる
  - 結線層の実行時状態を組み立てるときに調整値を 1 度だけ正規化し、以降はその値を配る（縮退ログは構築時の 1 件だけ）。結線層の調整値に関する記述も新しい語へ揃える
  - 改名した識別子を名前で参照している箇所（兄弟テストの構造体リテラル構築・値比較を含む）を機械的に追随させ、**クレートがコンパイルできる状態で着地する**（期待値の再導出そのものは 3.x が持つ。この時点で一部のテストは赤でよい）
  - 行高さ単位の遅延座標が受け取る係数がこの式に自動追随し、em 単位の係数（フォント高さの N 倍）と座標語彙・原点・書字方向ごとの解決規則は変えない。フォントサイズの受け渡し（値のまま＝em）・行ボックス丈・文字送りの計測・フォント名と高さ欠落時の既定値も変えない
  - 調整値のテストへ、行送りの値（28→30・12→14・10→12）と縮退（NaN／負 → 0＋警告 1 件）の検査を足す
  - 完了状態: `cargo build`／`cargo test --no-run` が対象クレートで通り、旧係数の識別子がクレート全体から消え、新規の調整値検査が緑であること
  - _Requirements: 1.2, 1.6, 3.1, 3.3, 3.5, 3.7, 3.9, 3.10, 4.1, 4.2, 4.3_
  - _Boundary: TextLayerConfig, GlyphMetrics 実装 2 種, 結線層の調整値_

- [x] 2.2 帯の防御式を保ったまま値と記述を追随させる
  - ハイライト帯・ヒット帯・ダーティ帯の拡張はいずれも式・呼び出しを変えず、行ボックス丈をピッチで頭打ちにする現行の防御をそのまま残す
  - 帯の実測例を述べる記述（旧ピッチ 35 前提）と、行送りを定数で注入しているテスト土台の定数・記述を新しい式の値へ揃える（定数の書き換えは本タスク・その定数を使うテストの期待値の再導出は 3.1）
  - 例（タイプライタのデモ）の調整値に関する注記も新しい語へ揃える
  - 完了状態: 帯の値が新しいピッチで頭打ちになることを既存の帯テストが示し、旧ピッチを正典として述べる記述が残っていないこと
  - _Requirements: 3.6, 7.5, 5.6_
  - _Depends: 2.1_
  - _Boundary: choice, canvas の doc とテスト土台_

- [x] 3. 既存の決定論テストを緩めずに再導出する
- [x] 3.1 (P) 配置・折返し・あふれ判定・カーソルタグ系の期待値を再導出する（台帳 A-1）
  - 固定メトリクスを使う折返し・分節・あふれ判定・カーソルタグ系のテスト群について、期待値を新しい式から計算して更新する
  - 「ちょうど 3 行が収まる」型の境界前提を持つテストは、境界値そのものを新しい格子へ導き直し、意図（境界ちょうど）が消えないようにする（旧格子のままだと緑でも意味を失う）
  - あふれ判定の分岐は増減させず、行矩形の位置と境界という入力の変化だけを反映する
  - 許容幅を広げず、等値判定を範囲判定へ置き換えず、無効化せず、本数と名前を減らさない。テスト土台の注入定数は 2.2 が書き換え済みである前提とする
  - 完了状態: 台帳 A-1 のファイル群がテスト名を保ったまま緑になり、更新差分が期待値と記述のみであること
  - _Requirements: 7.1, 7.2, 4.4, 9.1_
  - _Depends: 2.1, 2.2_
  - _Boundary: 配置・折返し・カーソルタグのテスト群_

- [x] 3.2 (P) 表示・結線・状態側の期待値を再導出する（台帳 A-2）
  - ダーティ帯・描画計画・選択肢の契約・状態適用・一連の経路テストについて、期待値を新しい式から計算して更新する
  - 縦書きの列数を前提にしている経路テストは、新しい列送りであふれが起きる列数へ導き直し、途中経過の行数の期待も列数に合わせて再計算する（旧列数のままだとあふれず検証が空振りになる）
  - 2.1 が改名追随のために触ったファイルについては、本タスクは期待値だけを担当する
  - 完了状態: 台帳 A-2 のファイル群が緑で、あふれを検証する経路テストが実際にあふれる入力になっていること
  - _Requirements: 7.1, 7.2, 9.1_
  - _Depends: 2.1, 2.2_
  - _Boundary: viewbox・actor・state のテスト群_

- [x] 3.3 (P) COM 層・実フォント側の期待値を再導出する（台帳 B）
  - 実フォント／既定フォントを使う描画・読み戻し・フレーム描画・ホバー・拡大率不変のテスト群の期待値を新しい式から計算して更新する
  - 文字の大きさが不変であることの帰結として、文字送り・折返し位置・グリフ描画の期待値は変えず、ピッチ由来の差だけを反映する
  - 拡大率が 1 以外でも行数の判定が変わらないことを、更新後の期待値で引き続き示す
  - 完了状態: 台帳 B のファイル群が緑で、文字送りに依存する座標の期待値に差分が無いこと
  - _Requirements: 7.1, 7.2, 3.11_
  - _Depends: 2.1_
  - _Boundary: COM 層テスト群_

- [x] 3.4 容量前提を持つテストと参照描画比較を導き直す
  - 「何行収まるか」を前提に書かれたテスト・例（相方側 3 行・本体側 3→4 行・縦書きの列数・スクロール発火の行数）の前提と根拠の式を新しい容量で書き直し、緑のまま意味を失う状態を防ぐ
  - 参照描画との画素等価比較が両側とも同じ寸法で動くことを確認し、差分を注入する負の対照が新しい式でも赤になることを示す
  - 台帳 §3.5 の **R-2**（`yugothic_real_fixture_matches_oracle_byte_for_byte` が、行の下端からはみ出したインクの切り落としで参照描画と食い違う）を本タスクが引き受ける。帯を広げて隠さず、テストも緩めず、再描画の対象範囲がはみ出しを覆えているかを `expand_overhang_for_band` から調べる。本タスクの境界の中で根本原因を直せない場合は、そこで止めて引受先を立て、登記してから先へ進む
  - 先行タスクが期待値を更新済みのファイルについては、本タスクは容量の式を述べる記述だけを担当し、同じ数値を二度書き換えない
  - 数値が偶然一致して前提が保たれる箇所も、その根拠の式を記述に残す
  - 完了状態: 容量前提の各テストが新しい容量の式を記述として持ち、負の対照が赤・本体は緑であること
  - _Requirements: 7.3, 7.4, 5.5_
  - _Depends: 3.1, 3.2, 3.3_

- [x] 3.5 退役するテストを同数の代替へ差し替える
  - 検証対象（切り上げの端数・係数の乗算）が裁定で存在しなくなった 2 件を、新しい式で「式が違えば赤になる」形の検査へ名前と本文ごと差し替える
  - 行ボックス丈・帯の頭打ち・ダーティ帯の拡張を検証するテストは退役させない
  - 退役の根拠（何が仕様判断で無くなったか）と代替の対応を 1 対 1 で記録する
  - 完了状態: テストの総本数が減っておらず、差し替え後の 2 件が新しい式以外の値で赤になること
  - _Requirements: 7.2_
  - _Depends: 3.1, 3.3_

- [x] 4. 折返し基準と描画範囲の二段構えを実装する
- [x] 4.1 描画範囲の行内軸の遠辺を保持し、粗いバルーンを警告する
  - 領域の解決時に、描画範囲の行内軸の遠辺（横書きは右・縦書きは下）を軸解決して保持し、読める形で公開する
  - 折返し基準がその遠辺の外に解決された場合、バルーン名・軸・両方の値を欄に持つ警告をバルーンの読み込み 1 回につき 1 回だけ出す（バルーン名が無いときはプレースホルダで記録し、ログ無しの経路を作らない）
  - 3 方向（横書き・縦書き 2 方向）の遠辺と、警告件数（相方側相当で 1 件・本体側相当で 0 件）を固定する兄弟テストを新設する
  - 警告 1 件の追加でログ件数が変わる既存テスト（領域の in-file テスト・出荷 fixture の領域テスト・カーソルタグのテスト土台）の期待件数を更新する。境界の外側に当たるこの 3 箇所は本タスクが所有する
  - 完了状態: 新設テストが緑で、相方側 fixture 相当の読み込みで警告がちょうど 1 件記録されること
  - _Requirements: 6.2, 6.3, 6.7_
  - _Boundary: TextRegion, 領域テスト群_

- [x] 4.2 配置層に無条件折返しを足す
  - 行内配置で、折返し基準を超えたときに加えて、文字の遠端が描画範囲の遠辺を超えそうなときも折り返すようにし、2 つの値と 2 つの判定を別に持つ（片方へ丸め込まない）
  - 折返し方式（1 文字ずつ／分節）のどちらでも、また塊の途中であっても、配置の直前に必ず描画範囲の判定を通す。塊の途中で発火した場合は理由が読める記録を残す
  - 行頭の 1 グリフだけは両方の判定を超えても配置する（無限折返しを構造で排除する）
  - 縦書きは軸の読み替えだけで同じ判定が効き、配置層に書字方向の分岐を足さない
  - 純粋層の固定寸法で、折返し基準が描画範囲の外にある領域に長い文字列を置くと基準に達する前に折り返され、どのグリフの遠端も描画範囲を超えないことを、横書き・縦書き 2 方向 × 折返し 2 方式で固定する兄弟テストを新設する
  - 折返し基準が描画範囲の内にある入力では、本仕様の前後で出力が一致することを同テストで示す
  - 完了状態: 新設テストが緑で、既存の折返しテスト群が無改変のまま緑であること
  - _Requirements: 6.2, 6.3, 6.4, 6.6, 3.8, 8.4_
  - _Depends: 4.1_
  - _Boundary: LayoutEngine_

- [x] 5. 症状と裁定値を固定する新規の決定論テスト
- [x] 5.1 実物バルーン × メニュー 3 台本の容量テストを新設する
  - 実物の共通記述とバルーン別記述を実寸で解決し、メニュー 3 台本の本文を実 parser → 実 compile → 実状態 → 実配置の経路で通す
  - 3 台本すべてで先頭可視行が 0（スクロールしない）こと、各選択肢の行矩形の下端が描画範囲の下端を超えないことを、折返し 2 方式で同一結果として固定する
  - 行高さ単位の遅延座標で置いた選択肢が改行で送った行と同じ高さに、em 単位の水平位置が従来と同じ座標に着地することを、同じ台本の実経路で確かめる
  - 「閉じる」「もどる」の全グリフの遠端が描画範囲の右端の内に収まり、行が増えないことを固定する
  - 本体側バルーンで、折返し位置が描画範囲の判定を持たない参照と一致することを固定する
  - 行送りを旧式へ戻すテスト専用のメトリクスを注入すると先頭可視行が 1 になる（判定が生きている）対照を置く
  - 実フォントが無い環境では文字送りが縮退値になるため、先頭で縮退を検出して赤で止める
  - 完了状態: 3 台本 × 2 方式のすべてが緑で、対照が旧式で赤になること
  - _Requirements: 8.1, 8.2, 8.4, 8.7, 5.1, 5.2, 5.3, 5.4, 6.5, 3.4, 4.1, 4.2_
  - _Depends: 2.1, 4.2_

- [x] 5.2 裁定値の実フォント読み戻しテストを新設する
  - 実フォント・実寸・拡大率 1 で、行送りの値・仮名 1 文字の送り・1 行のインクの縦範囲を定数として固定し、根拠画像の物理 px と日付をコメントで添える
  - 2 行を並べ、上の行のインク下端が下の行のインク上端より上にある（隣接行のインクが重ならない）ことを固定する
  - 選択肢行のホバー塗り帯が選択肢の文字のインクの上端を含み、下端は次の箇条書きの裁定の範囲に収まることを読み戻しで確かめる
  - 帯のインク包含は**裁定 2026-09-06（要件 3.6）**に従い「帯の下端からのはみ出しは **1 画素以内**」を固定する（実測は帯の下端 y21 に対しインクの下端 y22）。裁定による意味の変更であることと裁定日をテストのコメントに書き、許容幅を緩めたものではないことが読み手に分かるようにする
  - はみ出しが 2 画素以上になった場合は帯を広げず、はみ出しの画素数を数値で添えて開発者の裁定を待つ。その場合は本タスクを停止扱いとして記録し、裁定を得るまで 6.x へ進まない
  - **第 2 回裁定（2026-09-06・上の停止条項が発火）**: 正典 `font.height,28` の実測で「閉じる」が 2 画素はみ出したため停止 → 開発者裁定「2 画素を許容する」。上限を 2 画素へ導き直し（3 画素以上なら再裁定）、y21／y22 は font 20 の fixture の値だった旨を記録する。文字インクは帯の塗り色と分離して測り、文字が在ることも固定し、先頭行以外の帯（「もどる」）でも上端の包含を反証可能にする。同じ理由で緑のまま意味を失っている `tests/emo2_fixture_e2e_test.rs` の帯包含（走査窓が帯の下で終わる）も本タスクで導き直す（要件 7.3）
  - 完了状態: 読み戻しの 3 検査がすべて緑であること（はみ出しが 2 画素以上なら裁定待ちで停止し、差の画素数が報告として残っていること）
  - _Requirements: 8.3, 8.5, 3.2, 3.3, 5.6, 3.6, 1.1, 1.2_
  - _Depends: 2.1_

- [x] 6. 統合検証と下流への引き渡し
- [x] 6.1 旧式の残存検査と全体テスト
  - 旧係数を現行の行送りとして述べる記述が製品コード・テスト・例・正典表に残っていないことを、2 段の絞り込み検索で 0 件として示し、履歴・注記つき引用として残る行の一覧を証跡に添える
  - 新設した 4 ファイルが兄弟ファイル規約または統合テストの置き場に従い、いずれも 1,000 行以下で、行数の見張りの例外表に触れていないことを、同見張りのテストが緑であることで示す
  - 対象クレートとワークスペース全体のテストを Yu Gothic UI の在る Windows で実行し、終了コードで合否を判定する（出力を絞る処理で終了コードを隠さない）
  - 完了状態: 検索が 0 件、見張りが例外表無改変のまま緑、両テストが終了コード 0 であること
  - _Requirements: 2.4, 7.6, 8.6, 10.4_
  - _Depends: 3.4, 3.5, 5.1, 5.2, 7.1, 7.2_

- [x] 6.2 触っていないことの確認と着地条件
  - バルーン fixture・kanade・pasta・parser を改変していないことを差分で示す
  - あふれ判定の分岐・遅延座標の語彙・全消去・比率つき改行・reveal のペースに手が入っていないことを確認する
  - 対象 6 ファイルが 1,000 行以下で着地していること、コミット列が設計の順序どおりで正典表と実装がずれた中間状態を残していないことを確認する
  - 完了状態: 不改変の対象の差分が 0 件で、6 ファイルの行数と設計のコミット順との対応が記録されていること
  - _Requirements: 9.1, 9.2, 9.3, 9.5, 9.6_
  - _Depends: 6.1_

- [x] 6.3 下流 spec への登記と引き渡し
  - バルーン記述の粗さの台帳へ 2 件（折返し基準が描画範囲の外に解決されるバルーン定義／行末禁則文字のぶら下がりが未実装であること）を末尾採番で登記する
  - 文字装飾の spec の brief へ相互参照を置く（本仕様が引き受けなかった登記・フォント高さの意味論の継承・禁則の引受先が台帳側であること）
  - ロードマップの該当 2 行を完了へ更新し、e2e の受入記録の「引受先が実在することの確認」欄に本仕様のディレクトリと確認日を記入する
  - 利用者から見える変化（行送りの値・「閉じる」の表示・本体側の行容量が 3 行から 4 行へ）と変わらないもの（文字の大きさとベースライン・遅延座標の語彙・あふれ判定の式・折返し位置）を 1 箇所の引き渡し文書へ表としてまとめる
  - 完了状態: 引き渡し文書が実在し、登記先 4 箇所すべてに本仕様への参照が入っていること
  - _Requirements: 6.7, 6.9, 9.4, 10.1, 10.2, 10.3, 5.5_
  - _Depends: 6.2_

- [x] 7. 裁定 2026-09-06 の追補: スクロール描画量の削減と、観測用 example の前提の再導出（design §13）
- [x] 7.1 ダーティ矩形ごとに交差する行だけを描くようにする
  - 計画の値オブジェクトに「矩形 1 枚と、その矩形を復元するために描く行」の対を設け、純粋層の導出が矩形ごとの交差行を割り当てる（和集合は従来の描画対象行として残す）
  - 実描画のダーティ描画ループを「矩形ごとに、その矩形の行だけを描く」へ改め、不整合検査に「各矩形の行は描画対象行の部分集合」を足す
  - 純粋層の決定論テスト（2 枚以上の矩形に別々の行が交差する入力で、矩形ごとの行の並びと和集合）を追加し、実描画側の「描画呼び出しの増分は積」の検査を「和」へ改訂する
  - 参照描画との画素等価比較が両側とも緑のまま・注入差分の負の対照が赤のままであることを示す（描く行を減らしても画素が変わらない）
  - ハイライト帯の拡張・帯の式・面内 blit・露出帯・残滓の導出には手を入れない
  - 完了状態: viewbox 系のテストが緑で負の対照が赤、`viewbox.rs`・`viewbox_draw.rs` とも 1,000 行以下であること
  - _Requirements: 11.1, 11.2, 11.3, 11.4_
  - _Depends: 3.4_

- [x] 7.2 観測用 example の前提を改行遅延と新しい容量で導き直す
  - 「完成プラトー」を「次のプラトーで先頭可視行が 1 進むもの」として選び直し、比べる 2 段を「送り出される行がどちらも短行」のものに限る（長い行の本数は短行の流入前の配置行数から求める）
  - 短行の本数を 13 へ増やし、時刻表（チェック時刻・Clear の注入時刻・ゲートの項目数・プラトー走査の窓）を式で導き直す。短行は 2 文字のまま
  - 削減後の実走で `EXPOSURE_BAND_DRAW_BOUND` を横書き・縦書きとも採り直し、内訳を「矩形ごとの交差行数の和」として doc に残す（積の記述を残さない）
  - 旧来の選び方では生成増分 2 で落ちること（負の対照）と、製品の不変条件は破れていなかったこと（誤報）を台帳へ記録する
  - 完了状態: 横書き・縦書きとも `__COMPAT_LAYER=DPIUNAWARE`（k=1.0）で全検査を通過して PASS、C5 縦書きと C8 の全項目が緑であること
  - _Requirements: 11.5, 11.6, 11.7, 11.8, 11.9, 11.10, 7.3, 5.5_
  - _Depends: 7.1_

## Implementation Notes

- 1.1: 再導出の参照先は **`verification/derivation-ledger.md` 1 本**。design の再導出台帳は実測で 7 箇所ずれていた（一覧の実数えは 32 でなく 37・`canvas.rs` は不変ではない・4 ファイル欠落・警告 1 件の影響が過大・`choice_fixture_test.rs` と `emo2_fixture_e2e_test.rs` は帯の数値定数を持たず実行時導出）。以降のタスクは design の表ではなく台帳を読むこと。
- 1.1: 台帳の行は design から写さず、必ず当該ファイルを開いて定数の実在を確かめてから書く（写しで 1 度差し戻された）。
- 1.1: 改名追随（`line_pitch_factor` → 行間）で**コンパイルが壊れるのは 1 箇所だけ**（`draw_format_metrics_tests.rs` の構造体リテラル）。`crates/areka` 側 12 ファイルは `TextLayerConfig::default()` 経由なので影響を受けない。
- 1.2: アーカイブへの注記は**表の中に行を足さない**。GFM の表の中に注記行を入れると「表の中身」になり、表の直後に `>` を置くと表が壊れる。表の最終行の後に空行を挟んで引用行を置くのが正解（1 度差し戻された）。
- 1.2: 引用は行番号でなく「何の定義行か」で指す。自分が足した 1 行が、自分の書いた行番号をずらす（1 度差し戻された）。
- 1.2: この shell では `grep -c $'\r$'` が CRLF を過大に数える（CRLF 1 行のファイルで 2 と報告）。CRLF の確認は `tr -dc '\r' | wc -c` と `tr -dc '\n' | wc -c` の byte 数比較で行い、毎回既知ファイルで較正すること。
- 1.2 → 1.3 引き継ぎ: COMPAT §8 の 2 行目が `verification/evidence/README.md` の「閉じる」の右端の欄を引いているが、その欄はまだ無い。1.3 で作ること。
- 1.3: 根拠 PNG 2 枚は**未着のまま着地**（開発者からの受領待ち）。裁定は 1.2 で COMPAT §8 に着地済みなので 2.x 以降は待たない。「閉じる」のメニュー画面も未取得で、開発者の裁定へ回す欄として README に記録した。
- 1.3: 引用先の spec を「同仕様」で省略しない。省略した先に**実在する無関係な行**があると、誤りが誤りとして現れず読み手を欺く（1 度差し戻された）。
- 1.3: 「撮れない理由」は撮れている事実と矛盾させない。デスクトップ全体の画面写しは撮れる／窓を名指しして画素を読む道具が無い／GPU 合成窓は窓単位のキャプチャで中身が返らない、は別々の話。
- 2.1: 行送りの定義点は `TextLayerConfig::line_pitch`（`state.rs`）1 か所。`FixedMetrics`（`layout.rs`）と `DWriteMetrics`（`draw.rs`）は委譲のみで自前の演算を持たない。正規化は `TextLayerRuntime::new` で 1 度だけ。
- 2.1: 既存テスト 59 本が想定どおり赤（lib 53・pipeline 3・scale_invariance 2・choice_fixture 1）。期待値の再導出は 3.1／3.2／3.3 の担当。**弱体化・`#[ignore]`・許容幅の拡大は禁止**。
- 2.1 → 5.2 裁定案件（要件 3.6・design「帯の防御式を保つ」の残存リスクが的中）: 実フォントで**インクが帯から 1 画素はみ出す**。`tests/choice_fixture_test.rs::real_font_menu_hover_render_dumps_png`（インク y5..22 対 帯 y5..21）と `src/viewbox_draw_live_diff_tests.rs::yugothic_real_fixture_matches_oracle_byte_for_byte`（行下端の切り落とし）。**帯を広げて隠さない**。どちらも期待値の付け替えでは解けないので 3.x の対象ではない。
- 2.1: 縮退時の行ボックス比は `config.line_pitch(h) / h`（旧実装が係数 1.25 をそのまま比として使い「帯はピッチで頭打ち」を保っていた形の写し）。`highlight_band_extent` の clamp を通ると結局ピッチになるため、縮退時の見え方は変わらない。
- 2.1 → 3.3／3.5 申し送り: `draw_format_metrics_tests.rs` の非既定分岐は改名だけを当てたため `line_gap: 2.0`＝既定になり空振り。design どおり 5.0（`line_pitch(10) = 15`）へ導き直すこと。数値の付け替えだけだと緑のまま意味を失う。
- 2.2: 帯の式・呼び出し・`expand_overhang_for_band` はいずれも HEAD と byte 同一のまま着地（`actor.rs` と `viewbox_draw.rs` は差分ゼロ）。帯は広げていない。
- 2.2 → 3.1 申し送り: 注入定数 `LINE_PITCH` を 13.0 → 12.0 にしたので、`cursor_tag_resolve_tests.rs` の `shared_premises_match_the_design_document`（定数を黙って動かさないための前提検査）が緑→赤になった。これを導き直すのが 3.1。赤は計 60 本（2.1 引き渡し 59 ＋ この 1 本）。
- 2.2: 注入定数を変えるときは**倍数の合成値も一緒に**動かす。`glyph_resident((0.0, 26.0))`（旧ピッチの 2 行目）を置き忘れると 0／12／26 という新旧どちらの格子でもない並びが残り、テストは緑のまま意味を失う。
- 2.2: `derive_hit_rows` の 5 番目の引数は帯（`band_extent`）でピッチではない。`13.0` を見ただけで旧ピッチと決めつけないこと。`canvas.rs` の帯 doc は数値を持たないので無改変（design の指示より台帳が正しい）。
- 3.1: **既に緑のテストも読むこと**。「3 行ちょうど収まる」型は境界を動かさないと緑のまま 2px の余裕が生まれて意味を失う。実際に 4 本（visible_window 2 本・vertical 2 本）が該当した。
- 3.1: 縦書きの `\_l[±13,0]` は**実数値そのものが 1 列送り**。±12 へ動かさないと、自動列送りと突き合わせている canon 側が赤になり、兄弟ファイルは 387 を「2 列目」と述べ続ける。
- 3.1: 新ピッチが**偶数**になったので `ratio 0.5` の端数が消える。端数そのものを主題にしているテストは font を奇数（12 → 13・pitch 15）へ導き直して端数を保つ。台帳が指示する `(0,0),(0,14),(0,21)` をそのまま採ると緑のまま無意味になる（3.2 の `canvas.rs` も同じ罠）。
- 3.1: 台帳は網羅ではない。実走の赤と突き合わせること（visible_window の 2 本が未記載・`canvas.rs` の赤は 2 本でなく 3 本）。判明ぶんは台帳へ反映済み。
- 3.2: **一般則**（3.1／3.2 で 4 度踏んだ）。新しい行送りは偶数になりやすいので、端数を主題にするテストは値の付け替えだけだと緑のまま無意味になるか、負の対照が死んで赤になる。`ratio 0.5 × pitch` は**ピッチが奇数のときだけ**、`pitch × k(1.25)` は**ピッチが 4 の倍数でないときだけ**端数になる。台帳へ明文化済み。
- 3.2: 台帳と design の「pipeline_test の境界 36 は据え置き」は**誤り**だった。当該ファイルは期待表で「下端 36＝境界ちょうど」と明言しており、36 のままだと主張が偽になる。34 へ。両文書とも是正済み。
- 3.2: `viewbox_plan_commit_tests.rs` は台帳どおり一律 12 にすると**赤になる**。`12 × 1.25 = 15` が整数になり、ドリフトの負の対照が死ぬため。font 10 → 13（刻み 15）へ。
- 3.2 → 3.3 申し送り: `tests/scale_invariance_test.rs` は k = 1.25 で font 40（pitch 42・`42 × 1.25 = 52.5` で安全）と font 10（pitch 12・`12 × 1.25 = 15` で**整数化**）を使う。端数に依存する検査があれば上の一般則で確かめること。
- 3.2: `cargo test -p areka-emo-text` は `--no-fail-fast` を付けないと lib で止まり `tests/` 配下が走らない。赤の実数えには必ず付ける。
- 3.3: B 群 8 ファイルのうち**着手時に赤だったのは 4 本だけ**。残り 4 本は緑のまま旧ピッチを正典として述べていた（要件 7.5 の是正）。3.4 は「同じ数値を二度書き換えない」ためにこの区別が要る。
- 3.3 → 3.5 申し送り: D 群 2 行目（`dwrite_metrics_line_pitch_follows_config_canon` の分岐）は **3.3 が実施済み**。赤いテストの中の 1 アサートで分離できなかったため。代替は `line_gap: 5.0` → `line_pitch(10) == 15`（既定の 2 なら 12 で赤）。3.5 は再編集せず 1 対 1 の記録を書くだけでよい。残る退役は `fixed_metrics_line_pitch_ceils_fractional_values` 1 本。
- 3.3: 要件 3.11 は空振りしていない。横書きは 3 行目下端 170 > 168 の **2px 差**で発火が生きている（旧ピッチでは 186 > 168）。縦 rl 354 < 360・縦 lr 46 > 40 も同様。
- 3.3: `draw_readback_test.rs` の容量前提「8 列 × pitch 15 = 120」は**偶然の一致**で判定の根拠ではなかった。列端の式（108 − 14i）へ書き直し、結論（8 列が上限）は新旧どちらでも変わらないことを確かめた。
- 3.4: R-2 の真因は切り落としでなく**残滓**。行間 2px < はみ出し 3px で、スクロールアウトした行の下端インクが blit 後の面内へ 1px 残る。`derive_dirty_with_overhangs` が可視窓より前の行を描画対象から外し（参照描画の `skip(first_visible_line)` と同じ規律）、blit フレームに限りその行の矩形をダーティへ入れる。帯は広げていない。
- 3.4: **例の C2 は 2026-07-17（PR#60）から死んでいた**。`cue()` の `duration: 0.0` が原因で typewriter が進まず、C3 以降が一度も走っていなかった。DPI・行送りとは無関係。復旧で通過数は 3 → 横 23・縦 21。「実測値」を語る前に、その実測の道具が動いているかを疑うこと。
- 3.4: 定数を「外の実測」で付け替えない（3 → 9 で差し戻された）。実測は自分の手順（`__COMPAT_LAYER=DPIUNAWARE`・k=1.0・日付）を doc に残し、内訳（4 × 4 の 4 枚と 4 行）まで書く。旧値 3 は本 spec 以前から実態（対照実走 6）と合っていなかった。
- 3.4 → 7.1／7.2 申し送り: 例の残件 3 件は裁定 2026-09-06 で決着（design §13・台帳 §3.5.3）。`create=2` は**誤報**——完成プラトーの選び方が改行遅延（2026-07-18）前の前提のままで、可視グリフ数 37/39/41（奇数）＝末尾行が 1 文字の途中の点を選んでいた。製品の不変条件は破れていない。
- 3.4: 製品コードを直したら、それを「不変」と述べていた doc（`viewbox_draw.rs` 2 か所）と design の Modified Files／境界節も同じ手で追随させる（差し戻された）。
- 3.5: 退役の代替は「係数を殺す」だけでなく「丸めの足し戻しも殺す」値を含めること。12→14・10→12 だけだと `(h+2).ceil()` が緑で通ってしまう。端数の font（10.5 → 12.5・旧式 14・ceil 足し戻し 13）の 1 本が両方を締める。
- 3.5: 台帳の「退役させないもの」に挙げた `expand_overhang_for_band` はテスト名でなく製品コードの関数名。守るべき検査の実体は `choice_tests.rs` の `band_extent_*` 3 本と `draw_format_metrics_tests.rs` の行ボックス丈 1 本。
- 4.1: design が引いた `BalloonModel::name()` は**存在しない**（`model.rs` の `fn name` は `impl Font` のフォント名）。`descript.txt` の `name,` を `map_merged` が写像していない。警告の `balloon` 欄は定数 `BALLOON_NAME_PLACEHOLDER` で埋め、引受先は `ukadoc-survey-assets` の brief へ登記（台帳 §7 #10）。design の「X が在る」は着手時に `grep` で実在を確かめること。
- 4.1: 純粋層へ兄弟テストを足したら `lib.rs` の `PURE_SOURCES`（windows 依存の構造検査）へも登録する。境界外だが必須の配線。4.2 の `layout_hard_limit_tests.rs` も同じ。
- 4.1: 0 件を主張するログ検査は、捕捉窓の内側で対照の `error!` を 1 件出して数えられることを同時に確かめる（恒真化の防止）。
- 4.2: hard の判定は `match wrap` の**外側に 1 か所**（`over_hard`）。経路ごとに複製すると「塊内だけ抜けている」型の欠陥が入る。行頭例外のガードも `!current.is_empty()` 1 つだけを共有する。
- 4.2: 塊の容量を `min(soft, hard)` で先決すると、塊内 hard は送り幅の積み上がりでは**到達不能**になる。到達させるテストは `\_l` で塊の途中の行内位置を進める（台帳 §7 #11）。design の「粗いバルーンでだけ起きる」は誤り。
- 4.2: 6.2（描画範囲の外に置かない・無条件）と 6.4（折返し位置不変）が `\_l` の遠辺付近への跳躍で衝突する。裁定本文である 6.2 を優先（台帳 §7 #12）。
- 4.2: 「本仕様前後で不変」を同一コード内の自己比較（遠辺を 10000 へ押しやった対照）で示すのは golden ではない。真の前後不変は無改変の既存テスト群が担う——新設側の doc にその論拠を書くこと。
- 5.1: 実物の読み込み規約は 2 種——バルーン記述は Ansi 既定（本番 `load_scope_balloon_model` と同じ）・pasta 辞書は UTF-8（`charset` 宣言なし）。取り違えると 5 本が赤になるが、それは欠陥ではなく規約の誤り。
- 5.1: 要件の「行」は視覚上の段、配置層の「行」は `PositionedLine`。`\_l` で同じ段に置いた選択肢は別々の行矩形になる（おしゃべり頻度＝4 本・台帳 §7 #13）。
- 5.1: 実測値（「閉じる」遠端 233.22）は逐語固定せず、境で挟んで（(200, 240]）実測をコメントに残す。机上値 248 > 240 が全角＝em の仮定に由来することも assert で固定した。
- 5.2: 裁定の根拠にした実測は**正典の値で採り直す**こと。「1 画素」は font 20 の fixture の値で、正典 28 では 2 画素だった（第 2 回裁定＝2 画素を許容・3 画素以上で再裁定）。
- 5.2: 読み戻しの包含検査は 2 度差し戻された。(1) α ≥ 128 は**塗り帯そのもの**に当たるので文字と帯を同じ述語で拾うと上端包含が恒真になる——文字は帯の塗り色と分離して測り、非退化（文字が在ること）も固定する。(2) 走査窓の下限を帯の上端に置くと上端包含が恒真、上限を次行の起点に置くとはみ出しが窓の外へ落ちて緑のまま無意味になる——窓は帯の上下へ余白（4 画素）を開き、窓が文字を切っていないことを門で確かめる。先頭行は帯の上端が面の縁なので反証できない＝先頭でない行をホバーする。
- 5.2: `tests/emo2_fixture_e2e_test.rs` の帯包含は窓の取り方で恒真だった（要件 7.3 の「緑のまま意味を失う」の実例）。`choice_fixture_test.rs` は `ChoiceHitRow::rect`（窓物理 px）を validrect-local として使い 5 画素ずれていた。どちらも本タスクで是正。
- 7.1: 描画量の削減は `DirtyRect { rect, lines }` で「矩形とそれを復元する行」を対にするのが要。`draw_lines` は和集合として残し（Phase 1 の資源準備と不整合検査の対象）、実描画は矩形ごとに `lines` だけを描く。追いつきスクロールの実測は 6（積）→ 4（和）。
- 7.1: 既存の `FramePlan::Update` 分解を壊さないために `#[cfg(test)] impl PartialEq<PhysicalRect> for DirtyRect`（矩形だけを比べる）を置いた。矩形だけを見る既存 assert はそのまま、行の割当は新設テストが固定する。
- 7.1: hover 帯を持つ行が 2 枚の矩形に交差するとき、両方の矩形で塗りと文字を描き直す必要がある。`FillRectangle` は矩形ループの内側に置く（Phase 1 の `SetDrawingEffect` は行ごとに 1 回のまま）。
- 7.1: 「矩形 1 枚」の入力では和＝積なので積→和の書き換えは弁別力を持たない。弁別には矩形 ≥ 2 枚・和 < 積を前提として assert する入力（追いつきスクロール）が要る。
- 7.2: 「完成プラトー」は**次のプラトーで先頭可視行が 1 進むもの**（改行遅延では次の行が開くのは次の文字の到着時）。旧来の「先頭可視行が 1 進んだ直後」は末尾行が 1 文字の途中で、生成増分が 2 に見えた（誤報）。製品の不変条件は破れていない。
- 7.2: 描画量の一定性を比べる 2 段は「送り出される行がどちらも短行」に限る（統制された比較）。長い行の本数は短行の流入前（t=1.95）の実配置行数から求める（横 3・縦 7）。縦書きで両方を短行にするには短行 13 本が最小（等号ぎりぎり）。
- 7.2: 実測は横 5/5・縦 3/3（和）・生成 0/0。生成 0 は C8 が全行提示後に完成プラトーだけを再提示するため（index＋内容一致のキャッシュ）。`EXPOSURE_BAND_DRAW_BOUND` = 5（両モードの最大・水増しなし）。
- 7.2: 負の対照（旧選び方・7.1 後）は横 8/8・縦 6/7・生成 2/2 で赤。ただし短行 9 本の状態で採ったので 2 変数が同時に動いている（台帳 §3.5.3 の注記）。
- 6.1: 機械検査の第 3 段（除外語 `旧式|本仕様で改訂|履歴`）は、履歴・注記つき引用・改訂指示の行でも除外語を偶然含まないと残る（5 行）。決着は案 A＝その行に「旧式」の 1 語を添える（削除 0・定義不変・DPI の k や再生時刻の 1.25 には押さない）。真の門は design が定めるとおり「第 2 段の残り行を一覧にして目視分類し証跡へ」。証跡は `verification/task-6.1-sweep-and-tests.md`。
- 6.1: `rg` はこの環境に無い。`grep -rnI -E` で代替し対応表を証跡に残す。パイプの終了コードは `${PIPESTATUS[n]}` で該当段を取る（0 件のときは grep が exit 1 になるのを「0 件の証拠」として書く）。
- 6.1 → 完了時の申し送り: `draw.rs` は 988 行（残り 12）。次に触る spec（`text-decoration-canon`）は分割を先に。`crates/areka/src/emo2_boot/spine_conformance_script.rs` の走行 A の説明は旧式 35px を現在形で述べたまま（要件 2.4 の経路外・e2e 再走時に見直し）。
- 6.2: 「差分なし」の pathspec は実在を較正してから（追跡ファイル数を数える・実際に差分が出るコミットで空振りでないことを見る）。`Cargo.lock` は `.gitignore` で無視されており、その行だけは反証不能。
- 6.2: 「意味論に手が入っていない」は関数本体の抽出 diff（`visible_window`・`apply_pending_newline`）と、差分行に意味語（`RevealSchedule`・`parse_cursor_coord` など）が現れないこと＋その語が HEAD に実在すること（非空振り）の二本立てで示す。判定関数が呼ぶアクセサ（`TextRegion::bottom` など）が差分に現れないことも併せて見る。
- 6.2: design §12 の「次のコミットまでに緑にする」は字義どおりには満たされていない（緑化は 6 コミット後）。要件 9.6 が禁じるのは「正典表と実装がずれた中間状態」で、正典表（9bd274ec）が製品初変更（94ba66f3）に先行することで成立。赤の区間はハッシュ付きで開示する。
- 6.3: 他 spec の表へ書くときは「名指しされたセルだけ」を単一行のまま置き換え、追記は表の外（末尾の節・引用ブロック）へ。採番は既存の 📌 注が占有する番号（13）まで数えてから（14／15）。
- 6.3: 引き渡し文書は design の語を写さず事実へ寄せる（「本体側の表示位置は不変」は行送りが変わった以上は偽——折返し位置だけを不変と書く）。数値は証跡ファイル（6.1／6.2・台帳・テストの doc）から引き、再計算しない。
- 6.3: roadmap の陳腐化した語（「SSP 実測で正典化」）は登記のついでに正す（design §4.2＝SSP 実測は行わない）。`/kiro-complete` 時に W12 A′ 行の 3 列目（`/kiro-start …`）も「完了（PR#）」へ揃えること。
