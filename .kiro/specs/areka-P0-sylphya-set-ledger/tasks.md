# Implementation Plan

> 対象: `areka-P0-sylphya-set-ledger`（語彙台帳の追随のみ・値の導出なし）。
> 全編を通じた禁止事項: `crates/areka-sylphya/src` 配下のいかなるファイルの本文・コメント・失敗メッセージにも、先送り中のプロパティを指す探し語（`zorder`。追跡 spec 名 `areka-P0-zorder-property` もこの綴りを含む）を書かない。t_zpd40 が sylphya の全ソースを小文字部分一致で走査するため、書いた時点で赤になる。

- [ ] 1. 基準の採取と語彙台帳の追加
- [x] 1.1 変更前の実測値を採り、後で「動いた」と言える基準を作る
  - `cargo run -p ukadoc-survey -- evidence` を 1 回走らせ、property ドメインの「証拠あり」件数を控える（設計の見込みは 2）
  - `grep -c '^owner = ""\r\?$' doc/ukadoc-coverage/ledger/property.toml`（Git Bash。台帳は CRLF なので `\r` を許容する形）と、対照の `grep -c '^owner = "areka-P0-' …` を控える
  - `grep -nE 'len\(\), 21|; 21\]|has_21_|件数 21|全 21 項|= 21' crates/areka-sylphya/src/vocab/dotted.rs crates/areka-sylphya/src/ledger_key_determinism_tests.rs`（探し語の末項 `= 21` は説明文中の「menu 4 = 21」を拾うために要る。これが無いと写しが 1 本取り残される）と `grep -n '5 本\|8 本' crates/areka/src/placement/zorder_property_deferral_tests.rs` の現在の該当箇所を控える
  - 完了状態: 4 つの実測値（証拠件数・担当欄の空き 2 と対照の本数・21 の写しの箇所 7〔本体側 5・基準群 C 側 2〕・「5 本」8 箇所と「8 本」1 箇所）が記録され、タスク 5.2 がこれと突き合わせられる
  - _Requirements: 4.7, 6.5_

- [x] 1.2 正典が設定可能と定める 4 つの名前を設定可能語彙へ登記する
  - 既存 21 要素の綴り・順序・意味論を 1 文字も変えず、末尾にグループ見出しコメント付きで 4 要素を設計が定める順（`seriko.sticky-window`・`pause`・`playing`・`position`）で足す。意味論は 4 項とも運行コマンド
  - `seriko.sticky-window` の直上にのみ正典 URL の注記 1 行を置き、残り 3 つは見出しコメントで「URL は記録用の表の同名要素にある」と示す
  - 同ファイル内で 21 を固定していた **5 箇所**（語彙表の説明文・テストの説明文・テスト関数名・件数の主張・網羅の主張の固定長）を 25 へ追随させる。テストの説明文は群構成を「…menu 4 = 21」の形で書いており数え落としやすい。関数名の命名規約は変えない
  - 完了状態: `cargo test -p areka-sylphya --lib vocab::dotted` が緑で、件数の検査が 25 を、網羅の検査が 25 項を過不足なく判定する
  - _Requirements: 1.1, 1.2, 1.5, 1.6, 4.1, 4.7, 4.8_

- [ ] 1.3 サウンドプロパティ名 18 葉の記録用の語彙表を新設し、正典 URL の証跡を残す
  - 設定可能語彙の後・拡張イベント語彙の前に、公開のスライス定数として 18 要素を新設する。公開にするのは先送りの走査（t_zpd12）が公開定数をソースから抜き出すため
  - 要素は括弧を含まない 10 葉（SSP 2.8.72）と `meta.` を伴う 8 葉（SSP 2.8.73）。綴りは台帳 id・正典見出し・カタログ title と逐語一致させる
  - 各要素の直上に `// ukadoc: <アンカー付き項目 URL>` の単独行を置く（`ukadoc:` と URL の間は空白 1 つ・URL の後に説明を付けない）。アンカーの無いページ URL の単独行は置かない（証拠抽出器の第 2 段が起動して要素名が意図しない照合にかかる）
  - 汎用名の表からは `name`・`path` を引かない（正典で別項目・汎用名の件数 17 は動かさない）
  - このタスクの完了時点から、別クレートの先送りを守る名簿検査が「新しい公開定数が名簿に無い」として赤になる。これは設計が事前列挙した想定内の赤であり、タスク 3 で名簿へ登記するまで解消しない
  - 完了状態: 設定可能語彙と合わせて `// ukadoc: ` 単独行がファイル内でちょうど 19 行あり、`cargo run -p ukadoc-survey -- check` が URL の不一致を 1 件も報告しない
  - _Requirements: 3.1, 3.2, 3.3, 5.1, 5.2, 5.3, 5.4_

- [ ] 1.4 基準群 C の件数と網羅を新しい数へ追随させる
  - 設定可能語彙の網羅検査の説明文と固定長を 25 へ（末尾に 4 項を追加・先頭 21 要素は不変）
  - 件数の基準検査へ記録用の表が 18 であることの判定を足し、見出しにも「サウンド 18」を加える。汎用名 17 の記述は変えない
  - 完了状態: `cargo test -p areka-sylphya --lib ledger_key_determinism` が緑で、25 と 18 の双方を不一致なら失敗として判定する
  - _Requirements: 4.1, 4.6, 4.7, 4.8_

- [ ] 1.5 件数の導出と、正典に根拠のない仕分けの作りを説明文に書き残す
  - 設定可能語彙 25 の導出を 2 通り（登記の側＝既存 21＋本機能 4／照合元の側＝台帳 26 行 − 先送り 1 → 語彙表 23 項目 ＋ 先取り 2）で書き、2 通りが一致するのは相殺による偶然であり「26 から 1 を引く」だけでは根拠にならない旨の注記を添える
  - `.ext.拡張プロパティ名` 系 4 行を 26 に数えない理由と担い手、先取り 2 項が正典と食い違う既知の状態であり本機能では解消しない旨を書く
  - 記録用の表 18 の導出（括弧なし 10＋`meta.` 8・汎用名の表から引かない理由）を書く
  - 正典に根拠のない areka 固有の作り 2 点（葉の名前だけで正準語彙とみなす判定／設定可能語彙が末尾形で持たれ実キーと突き合わされないこと）と、利用者から見える結果（正典に無い名前が「保存して読み戻せる」から「受理して捨てる」へ変わり得る）、実在を確かめた引受先 spec を記録する
  - 先送り中のプロパティは名前で呼ばず「窓の重なり順のプロパティ（所有は互換記録 §8 の該当行）」と言い換える
  - 完了状態: 上記 4 項目が説明文として存在し、`cargo test -p areka-sylphya --lib` が全本緑（タスク 1.4 までで件数の写しが揃っているので、ここは説明文のみの変更として赤を出さない）
  - _Requirements: 4.2, 4.3, 4.4, 4.5, 4.6, 10.1, 10.2, 10.3_

- [ ] 2. 本機能が足した項目を項目ごとに判定する兄弟テストの新設
- [ ] 2.1 兄弟テストを接続し、件数・集合・既存分の順序を判定する
  - 語彙表の本体末尾に兄弟テストの接続宣言を置く（ファイル名は `<stem>_<モジュール名>.rs`・宣言は `mod <モジュール名>;` の規律に従う）
  - 記録用の表について、件数 18・literal 18 要素の集合と過不足なく一致・集合サイズが配列長と等しいこと（重複 0）・区切りを含まない要素がちょうど 10・`meta.` で始まる要素がちょうど 8 を判定する
  - 設定可能語彙にも載る 3 葉がちょうど 3 件であり、残り 15 葉は載らないことを判定する
  - 変更前の全キーを可変長の literal スライスとして持ち、設定可能語彙の先頭がそれと順序込みで一致することを判定する（固定長の数も関数名の数も使わない＝件数の写しを増やさない）
  - 完了状態: 3 本のテストが緑で、要素を 1 つ抜いても足しても赤になる
  - _Requirements: 1.6, 3.2, 4.6, 4.8, 4.9_

- [ ] 2.2 足した名前と記録用の葉の書込分類・参照結果を literal の期待値で判定する
  - 新規登記の 4 項それぞれについて、書込分類が運行コマンドであり、実際に書込を適用した効果列が「予約済みとして受理」ちょうど 1 件に等しいことを判定する
  - 記録用の 15 葉について、変更前と同じ分類（13 件は自由な名前としての保存・2 件は設定できない正準語彙）を literal の期待値表で判定する
  - 実キー形（親枝とセレクタを伴う形）3 つが根の判定で「設定できない正準語彙」になることを判定する。括弧の中に区切り文字を含む要素名は使わない（解釈に失敗して対照にならない）
  - 18 葉と実キー形の参照がすべて「見つからない」になること、対照として値を持つ既存キーが 1 つ返ることを同居させる
  - 完了状態: 22 行の literal 期待値がすべて緑で、期待値を 1 つ書き換えれば赤になる
  - _Requirements: 1.3, 1.4, 3.4, 3.5, 4.10, 7.1_

- [ ] 2.3 仕分けが記録用の表を読まないことと、URL 注記の本数を構造として判定する
  - 仕分けの実装ファイルとその兄弟テスト 3 本をソースとして読み込み、4 本すべてで記録用の表の名前が 0 回であること、較正として仕分けの実装ファイルで汎用名の表の名前が 1 回以上であることを判定する
  - 失敗メッセージに「仕分けが読む表へ変えるなら要件 3.3／7.2 の再検討と引受先 spec の裁定が要る」と書く
  - 語彙表の本体を読み込み、行頭を詰めて `// ukadoc: ` で始まる行がちょうど 19、うち正典ページを指す行が 19、アンカー無しのページ URL の行が 0 であることを判定する
  - 探し語の文字列をテストの行頭に書かない（行頭に置くと証拠抽出器がテストの行まで証拠として拾う）
  - 完了状態: 2 本のテストが緑で、仕分けの実装へ記録用の表への参照を 1 つ足すと赤になることを一度確かめてから元に戻す
  - _Requirements: 3.3, 3.7, 5.1, 5.4, 7.2_

- [ ] 3. 先送りを守る決定論テストの走査対象名簿へ記録用の表を登記する
  - 取り込み宣言に記録用の表を加え、名簿の固定長を 6 へ、末尾に表の名前を足す
  - 名簿と実際に読む表を対で動かす（表の一覧を返す関数の末尾に同じ順で 1 件追加する）
  - 名簿の本数を写している判定の literal 2 箇所を 6 へ更新し、較正行として新表が実際に要素を運んでいることを示す組を足す
  - 数を書いた説明文の写し 9 箇所（「5 本」8 箇所・「8 本」1 箇所）をすべて更新し、実装後に該当の探し語が 0 件であることを確かめる
  - 先送りを守る判定そのもの（語彙表走査・書込分類・ソース全走査）の本文・除外名簿・探し語は変更しない
  - タスク 1.3 の時点から赤になっていた名簿検査が、このタスクで緑へ戻る
  - 完了状態: `cargo test -p areka --lib placement::zorder_property_deferral_tests` が全本緑で、判定 3 本の本文の差分が 0 行
  - _Requirements: 2.3, 2.4, 3.6, 9.2_

- [ ] 4. 調査台帳と互換記録の所有の記録
- [ ] 4.1 (P) 調査台帳の担当欄の空き 2 行を埋め、「裁定待ち」を所有の記述へ差し替える
  - 先送り中のプロパティの行の担当欄へ追跡 spec を、設定可能語彙へ登記した `sticky-window` の行の担当欄へ本 spec を記入する
  - 両行の備考末尾の「裁定待ち」の記述を、決まった所有を記す文へ差し替える。転記元の行と状態欄は変えない
  - 他のエントリ（サウンド 18 行の備考を含む）は 1 文字も変えない。ファイルの改行コードを保つ
  - 完了状態: 担当欄が空の行が 0 件になり、`cargo test -p ukadoc-survey` が緑（TOML の構造崩れなし）で、差分が 2 エントリ・各 2 行の置換に閉じている
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5_
  - _Boundary: property.toml 2 行_

- [ ] 4.2 (P) 互換記録に所有の相互参照を 1 行足す
  - 先送り中のプロパティの読み書きを記す既存行の直後に、4 列の体裁を踏襲した 1 行を足す。既存行は 1 文字も変えない
  - 足す行には、先送り中のプロパティの語彙台帳の行も値の導出もともに追跡 spec が持つこと、`sticky-window` の語彙台帳の行とサウンド 18 葉の記録用の表は本 spec が持つこと、調査台帳のサウンド 18 行の備考の指し先は分割前のものであり本行が上書きすることを書く
  - 語彙そのものは書き写さない（綴りと URL の所在を指すにとどめる）。他の節は変更しない。ファイルの改行コードを保つ
  - 完了状態: 当該ファイルの差分が追加 1 行のみで、先送りを記す既存行と完了 spec の要件 13.5 の記述が無改訂
  - _Requirements: 2.5, 8.1, 8.2, 8.3, 8.4_
  - _Boundary: COMPAT §8 追加行_

- [ ] 5. 統合検証
- [ ] 5.1 決定論検査を全走させ、赤を「古い前提」と「壊した」へ切り分ける
  - `cargo test -p areka-sylphya --lib` と `cargo test -p areka --lib placement::zorder_property_deferral_tests` と `cargo test -p ukadoc-survey` を走らせる（名前を指定する走らせ方では母数 0 の緑を避ける指定を付ける）
  - 赤が出た場合、設計が事前列挙した「台帳が古い前提を固定していた」側のみを更新し、先送りを守る判定と語彙表走査の検査が赤になったら本機能の側の変更を取り下げる
  - `Running` 行の本数で全スイートが完走したことを確かめる（出力を先頭で切らない）
  - 完了状態: 3 つの走行がすべて緑で、先送りを守る判定の本文の差分が 0 行であることを差分で示せる
  - _Requirements: 2.1, 2.2, 3.6, 7.3, 7.4, 7.5_
  - _Depends: 1.5, 2.3, 3_

- [ ] 5.2 台帳・証跡の実測と編集範囲の固定を行い、完了報告の材料を揃える
  - `cargo run -p ukadoc-survey -- check` を 1 回走らせ、カタログに無い URL の報告が 0 件であることを確かめる
  - `cargo run -p ukadoc-survey -- evidence` を走らせ、property の証拠件数がタスク 1.1 で控えた値から 21 へ動いたことを記録する（報告書そのものは再生成しない）
  - 担当欄の空きを数え直して 0 であること、対照の探し語が 180 行以上を拾うことを確かめる（母数 0 の恒真を避ける）
  - 件数の写しの全数確認: タスク 1.1 と同じ探し語で 21 の側が **0 件**、較正として同じ探し語の 25 版が **5 件以上**であることを確かめる（較正が無いと、探し語そのものが壊れていても 0 件で緑に見える）
  - 変更が設計の定める 6 ファイルに閉じていること、範囲外に触れる必要が生じていないことを差分の要約で確かめる。生じていた場合は変更を行わず事実と理由を報告する
  - 利用者から見える唯一の挙動変化（登記した 4 つの末尾形そのものへの書き込みが「自由な名前として保存」から「予約済みとして受理し捨てる」へ変わること）と、範囲外の申し送り 4 件を完了報告の文面として用意する
  - 完了状態: 6 つの実測値（URL 不一致 0・証拠件数の前後・担当欄 0 と対照・21 の写し 0 件と 25 版の較正・変更ファイル 6・範囲外 0）が揃い、完了報告に転記できる
  - _Requirements: 4.7, 6.5, 7.2, 9.1, 9.3, 10.3_
  - _Depends: 4.1, 4.2, 5.1_

## Implementation Notes
- 1.1 基準（09-17 実測）: `evidence` の property 証拠 2 件（全体 263 件）／`owner = ""` 2 件・対照 `owner = "areka-P0-` 186 件／21 の写し 7 箇所（dotted.rs 57・188・190・191・209、ledger_key_determinism_tests.rs 210・211）／zorder_property_deferral_tests.rs の「5 本」8 箇所（19・111・113・116・127・150・252・281）・「8 本」1 箇所（115）
- ワークツリーは `vendors/pasta` submodule 未取得だった（`git submodule update --init vendors/pasta` で解消）
