# ギャップ分析: areka-P0-sakura-tag-word-boundary

> 作成: 2026-09-11（`/kiro-validate-gap`）。file:line はすべて本日の現物（ブランチ `claude/sakura-tag-word-boundary-e6d12b`・HEAD `67e0a4d3`）で再検証した。brief（09-03）の行番号は使っていない。

## 1. 分析の要約

- **欠陥は現存し、要件の全例が実測で再現した**（§3 の 24 件）。`\1青い[1]ノート…\e` → `[Raw("\1青い[1]"), Text("ノート…"), End]`、`\eテキスト[注です…` → `[Raw(全部)]`。影響しない 4 形と「変えない」形（`\e[注]です`・`\w2[x]`・`\n[half]`）も要件の記述どおり。
- **直す場所は 1 関数の 1 か所**: `crates/areka-parsers/src/sakura/lexer.rs` の `scan_tag`（132〜190 行）で、語走査ループ（158〜164 行）が既知タグでも本文へ伸びることが根因。角括弧経路（169〜178 行）・`bare_tag_len`（214〜227 行）・`decode.rs`・`compile.rs` は触らずに済む見込み。
- **本質的なギャップは「字句層が既知タグ集合を知らない」こと 1 点**。現状は `SHORTHAND_WORDS`（`lexer.rs:63`・3 語）だけが字句層にあり、既知 1 文字タグ 8 語は `decode.rs` の `decode_bare`（177〜197 行）の腕にしか無い。依存方向 `model ← lexer ← decode ← parse` のもとで、この 8 語を字句層へどう見せるかが設計の主論点。
- **有力案は「語走査を既知タグ自身の長さで頭打ちにする」（案 α）**: 差分は `scan_tag` 内 10〜15 行＋定数 1 本。結果が変わるのは「既知タグ＋本文 1 文字以上＋`[`」の形だけで、それは要件 1〜3 の対象そのもの。副作用候補は `\___x[1]`（§5 D2）のみ。
- **規模 S・リスク Low**。決定論テスト 2 ファイル新設（字句層・通し）と互換記録 1 行で閉じる。ワークスペース全体テストの前提（submodule・i686 成果物）は環境側の注意点（§7 D6）。

## 2. 現状調査（本日の現物）

### 2.1 字句層 `crates/areka-parsers/src/sakura/lexer.rs`（338 行）

| 箇所 | 行 | 内容 | 本仕様との関係 |
|---|---|---|---|
| `Token` enum | 33〜55 | `Tag{word,args}`／`Bare(String)`／`Shorthand{word,n}`／`SysVar`／`Text`／`Raw` | `Bare` の doc（36〜41 行）は「既知 1 文字タグ `e` `c` `-` `n` `0` `1` `h` `u`」を列挙しているが、これは説明であって集合の定義点ではない |
| `SHORTHAND_WORDS` | 63 | `&['w','b','p']` | 字句層が持つ唯一の「既知語」集合。群 (b) はこれで引ける |
| `scan_tag` 入口 | 132〜136 | 末尾の裸 `\` は `Bare("\\")` | 不変 |
| 短縮形判定 | 149〜156 | `first ∈ SHORTHAND_WORDS` ∧ 次が 1 桁 ∧ その次が `[` でない → `Shorthand` | 不変（要件 4.3）。`\w2[x]` はここを抜けて語走査へ落ちる |
| **語走査ループ** | **158〜164** | `[`／`\`／`%` に当たるまで前進（本文でも止まらない） | **根因**。既知タグでも本文を `word` に含める |
| `word` 確定 | 166 | `chars[word_start..j]` | |
| 角括弧経路 | 169〜178 | `chars[j]=='['` なら `scan_bracket_args`。閉じれば `Tag`、未閉じなら `\` から末尾まで `Raw`（174〜177） | 不変。本文中 `[` で「ここが選ばれてしまう」のが症状 |
| 角括弧なし経路 | 179〜189 | `take = bare_tag_len(&word)` で綴りを切り出し、残りは後続走査へ | 不変。先行 spec の固定長規律。`[` があると 169 行の分岐が先に取られるため届かない |
| `bare_tag_len` | 214〜227（doc 192〜213） | 先頭 `_` 以外→1、`_`＋非`_`→min(2,n)、`__`→min(3,n) | 不変（要件 4.8）。純粋関数なので案 α からそのまま再利用できる |
| テスト接続 | 336〜338 | `#[path = "lexer_bare_tag_tests.rs"] mod bare_tag_tests;` | 新規テストは同形で 1 本足す（§4） |

### 2.2 意味層 `decode.rs`（360 行）と下流 `compile.rs`（329 行）

- `decode_token`（140〜163 行）: `Shorthand{'w'|'b'|'p'}` → `Wait`／`BalloonSurface`／`SpeakerScope`、`Bare(word)` → `decode_bare`、`Tag` → `decode_tag`、`Raw` → 素通し。
- `decode_bare`（177〜197 行）: `"e"|"c"|"-"|"n"|"0"|"h"|"1"|"u"` の 8 腕が既知 1 文字タグの**唯一の意味定義点**。他は `decode_passthrough_bare`（343〜345 行）で `Raw("\\" + 綴り)`。
- `decode_tag`（201 行〜）: 未知 word は `decode_passthrough_tag`（320〜321 行）で `Raw`。`Tag{"1青い",["1"]}` はここで `Raw("\1青い[1]")` になる。
- `compile.rs` の catch-all（**202〜204 行**）: `Raw` を `tracing::debug!("M-boot 外タグを無視")` で捨てる。要件の記述（202〜204 行）と一致。
- 結論: **意味層・下流は本仕様で編集不要**。字句層が正しく切れば `Bare("1")` → `SpeakerScope{1}`、本文は `Text` として既存経路で届く。

### 2.3 既存テストの被覆（本仕様の穴を確認）

| ファイル | 該当 | 見ているもの | 本文中 `[` の形 |
|---|---|---|---|
| `lexer_bare_tag_tests.rs` T13（185〜194 行） | 既知 1 文字タグ 8 個＋本文 | `\eあ` 等 → `[Bare, Text]` | **0 本** |
| 同 T10（143〜147 行） | `\_a[Hint]`・`\__q[OnTest]`・`\_[x]` | 直後 `[` は角括弧経路 | 直後のみ |
| 同 T14（223〜226 行） | `\s[1000`・`\_a[` | 未閉じの `Raw` 吸収 | 直後のみ |
| `lexer_tests.rs` 415／428 行 | `\b2[x]`・`\w2[x]` | 数字＋`[` は角括弧経路（`Tag{"w2",["x"]}`） | 要件 4.3 の檻として**既に在る**。案 α の実装ミス（群 (b) を無条件に 1 文字で閉じる）はここが赤にする |
| `lexer_tests.rs` 357 行 | `あ\foo[a,b]い` | 未知語の角括弧形 | 要件 4.10 の一部（直後 `[`） |
| `parse_bare_tag_tests.rs` P9（168〜180 行） | 8 個の意味 | 単独入力のみ | 0 本 |
| `validation_tests.rs` | `include_str!` による構造検査 | `lexer.rs` を走査する構造テストは**無い**（grep 済み） | 定数追加は既存構造テストに触れない |

`areka-parsers` の現行テストは **415 本全緑**（本日実行・`cargo test -p areka-parsers`）。

### 2.4 周辺

- 互換記録 `doc/COMPAT_ARCHITECTURE.md` §8（122 行〜）: 4 欄表 `| 項目 | 裁量 | 根拠 | 出典 spec |`。先行 spec の行は **213 行**（角括弧なし `\_` タグの字句境界と意味）。本仕様の行はその直後が自然。
- 完了 spec `areka-P0-sakura-parse` 受入基準 13.1 は `.kiro/specs/completed/areka-P0-sakura-parse/requirements.md:159`（「後続文字をタグの一部に巻き込まない」）。改変しない（要件 7.4）。
- emo2 フィクスチャ: `dic/*.pasta` の `[` は **21 行**（`actors` 12・`boot` 1・`menu` 5・`skit` 1・`update` 2）で、すべて `\s[…]`／`\q[…]`／`\![move,…]`／`\_l[…]`／`\w[3000]`／`\_w[600]` の角括弧付きタグか `＃` コメント行（`menu.pasta:4` の `\q[]`・`update.pasta:16` の `reference[0]`）。台詞中の素の `[` は **0 件**（要件 4.11 の前提を再確認）。
- 同ウェーブ W13 の `text-decoration-canon` は `decode.rs`／`compile.rs` を触る（roadmap 124 行）。本仕様が **`lexer.rs` とテスト・互換記録だけ**に留まれば共有ファイル 0 を保てる。W14 の `property-query-channels` は `scan_sysvar`（322〜334 行）を拡張する予定で、本仕様の後に積む。
- ukadoc 網羅台帳（`doc/ukadoc-coverage/ledger/sakura-script.toml`）はソースの行番号を持たず、検査 `cargo test -p ukadoc-survey` はソース整理で壊れない設計（README 161〜167 行）。定数追加・注記変更で台帳側の検査は動かない見込み。

## 3. 実測（公開入口 `parse` の現行出力・2026-09-11）

scratchpad の小さな probe crate から `areka_parsers::sakura::parse` を呼んだ結果。要件の「直す」「直さない」の両側を同じ道具で固定した。

| 入力 | 現行出力 | 要件 | 判定 |
|---|---|---|---|
| `\1青い[1]ノートさんから交代したよ〜。\e` | `[Raw("\1青い[1]"), Text("ノートさんから交代したよ〜。"), End]` | 1.2／2.1 | **欠陥** |
| `\eテキスト[注です。まだ続く台詞` | `[Raw("\eテキスト[注です。まだ続く台詞")]` | 3.1 | **欠陥**（残り全部） |
| `\wテキスト[注]です` | `[Raw("\wテキスト[注]"), Text("です")]` | 1.3 | **欠陥** |
| `\pテキスト[注]です` | `[Raw("\pテキスト[注]"), Text("です")]` | 1.3 | **欠陥** |
| `\_q文字[注]を瞬間表示する。\_q` | `[Raw("\_q文字[注]"), Text("を瞬間表示する。"), Raw("\_q")]` | 1.4 | **欠陥** |
| `\__q文字[注]を\__q` | `[Raw("\__q文字[注]"), Text("を"), Raw("\__q")]` | 1.4／1.5 | **欠陥** |
| `\0file[1].txt` | `[Raw("\0file[1]"), Text(".txt")]` | 1.6 | **欠陥** |
| `\eあ[1]い[2]う` | `[Raw("\eあ[1]"), Text("い[2]う")]` | 1.7 | **欠陥** |
| `\eあ]い` | `[End, Text("あ]い")]` | 1.7 | 既に正しい（`]` 単独は発火しない） |
| `\1青い[1]ノート\nさん%username` | `[Raw("\1青い[1]"), Text("ノート"), NewLine(1.0), Text("さん"), SystemVar("username")]` | 1.8 | **欠陥**（後続タグ・sysvar は既に独立） |
| `\s[0]テキスト[注]です` | `[Surface("0"), Text("テキスト[注]です")]` | 4.9 | 影響しない（不変） |
| `\w9テキスト[注]です` | `[Wait(450ms), Text("テキスト[注]です")]` | 4.4／4.9 | 影響しない（不変） |
| `\eテキスト［注］です` | `[End, Text("テキスト［注］です")]` | 4.9 | 影響しない（不変） |
| `ふつうの台詞[1]です` | `[Text("ふつうの台詞[1]です")]` | 4.9 | 影響しない（不変） |
| `\4テキスト[注]` | `[Raw("\4テキスト[注]")]` | 4.10 | 未知語＝**現行維持**（本仕様後も消える） |
| `\sテキスト[注]` | `[Raw("\sテキスト[注]")]` | 4.10 | 未知語＝現行維持 |
| `\_zテキスト[注]` | `[Raw("\_zテキスト[注]")]` | 1.4 の読み方次第 | §7 D2 |
| `\4テキスト`（`[` 無し） | `[Raw("\4"), Text("テキスト")]` | — | 未知 1 文字語は `[` が無ければ既に 1 文字で閉じる（現行の非対称） |
| `\e[注]です` | `[Raw("\e[注]"), Text("です")]` | 4.1／3.2 | 直後 `[` は角括弧形＝不変 |
| `\e[注です` | `[Raw("\e[注です")]` | 3.2／4.7 | 直後の未閉じ＝不変 |
| `\n[half]` | `[NewLine(0.5)]` | 4.1 | 不変 |
| `\_a[ID]` | `[Raw("\_a[ID]")]` | 4.2 | 不変（anchor の前提） |
| `\w2[x]` | `[Raw("\w2[x]")]` | 4.3 | 不変（数字＋`[` は角括弧経路） |
| `\___x[1]` | `[Raw("\___x[1]")]` | 4.8／4.10 | **案 α では変わり得る**（§7 D2） |

## 4. 要件 → 資産の対応表

| 要件 | 既存資産 | ギャップ種別 | 備考 |
|---|---|---|---|
| 1.1／2.1〜2.5（既知 1 文字タグ 8 個） | `decode_bare` の 8 腕（意味）・`bare_tag_len` の「`_` 以外→1」（長さ） | **Missing**: 字句層に 8 語の集合が無い | 集合の置き場が主論点（§7 D1） |
| 1.3／2.6（`\w` `\b` `\p` 非数字後続） | `SHORTHAND_WORDS`（`lexer.rs:63`） | **Missing**: 短縮形判定に落ちた後の語走査に頭打ちが無い | 数字後続＋`[`（`\w2[x]`）は現行維持が要件 4.3（§7 D3） |
| 1.4／1.5（`\_X`・`\__X` 12 綴り） | `bare_tag_len`（純粋関数・再利用可） | **Missing**: `[` 検出より前に `bare_tag_len` が呼ばれない | 走査を 3 文字で頭打ちにして `bare_tag_len` を通せば解ける |
| 1.6〜1.8（半角英数・複数 `[`・後続タグ） | 語走査の停止条件 `\`／`%`（160 行） | Missing（1.1 と同根） | 後続タグ・sysvar の独立は既に成立（§3） |
| 3.1（未閉じでも残りを飲まない） | `scan_bracket_args::Unclosed`（174〜177 行） | Missing（1.1 と同根） | 既知タグの後ろでは角括弧経路に入らなくなるので自動的に解決 |
| 3.2／4.7（直後の未閉じは不変） | 同上 | Constraint | 「閉じた位置に `[` があれば従来どおり」で保たれる |
| 4.1〜4.6／4.8／4.9（不変群） | T10／T14／`lexer_tests` 415・428 行／P8・P9 | Constraint（檻は既存） | 新規テストでも重ねて固定（5.5／5.6） |
| 4.10（未知語は現行維持） | 語走査ループ（158〜164 行） | Constraint | 既知集合の**外**は現行ループを残す。案 δ（全語 1 文字）は採れない |
| 4.11（emo2 不変） | フィクスチャ実測（§2.4） | Constraint | 素の `[` 0 件ゆえ構造上不変。通しテストで代替証跡 |
| 5.1〜5.10（決定論テスト） | 兄弟ファイル規約（`structure.md` 146〜181 行）・既存の `bare()`/`text()` ヘルパ（各テストファイル内） | **Missing**: テストファイル 2 本 | 命名は §7 D4 |
| 5.11（ワークスペース全体） | — | **Unknown（環境）** | submodule `vendors/pasta` は本ワークツリーで本日 `git submodule update --init` 済み。i686 host-32 成果物の要否は実行時に確認（§7 D6） |
| 6.1〜6.6 | `lexer.rs` 338 行・注記（138〜140 行・36〜41 行・192〜199 行） | Constraint | 注記の更新箇所を列挙（§7 D7） |
| 7.1〜7.5（互換記録） | `COMPAT_ARCHITECTURE.md` §8・213 行の先行行 | **Missing**: 1 行 | 位置は §7 D5 |

## 5. 実装案の比較

### 案 α: 語走査を「既知タグ自身の長さ」で頭打ちにする（`scan_tag` 内・推奨候補）

短縮形判定（149〜156 行）の直後・語走査ループ（158〜164 行）の手前で分岐する。

- `first` が既知 1 文字タグ 8 語 **または** `SHORTHAND_WORDS`（＝短縮形判定を抜けた `w`/`b`/`p`）→ `j = word_start + 1` で語を閉じる。**ただし** `w`/`b`/`p` の直後が数字なら現行の語走査に落とす（`\w2[x]` → `Tag{"w2",["x"]}` を保つ・要件 4.3）。
- `first == '_'` → 語走査ループを **3 文字で頭打ち**にして（停止条件 `[`／`\`／`%` は同じ）、`bare_tag_len(word)` の長さで閉じる。`\_[x]` は走査が `[` で止まり `word="_"` → 1 → `[` が直後 → 角括弧経路（T10 不変）。`\_q文字[注]` は `word="_q文"` → 2 → 直後は `文` → 角括弧なし経路。
- それ以外（未知語）→ 現行ループのまま。
- 閉じた位置 `j` での `[` 判定（169 行）と両経路はそのまま。角括弧なし経路の `bare_tag_len(&word)`（183 行）は頭打ち後の `word` に対しても同じ値を返す（不変条件 `1 <= 返り値 <= min(3, n)`）。

| 観点 | 評価 |
|---|---|
| 差分 | `lexer.rs` に定数 1 本＋分岐 10〜15 行。`decode.rs`・`compile.rs`・`model.rs` 非接触 |
| 結果が変わる入力 | 「既知タグ＋本文 1 文字以上＋`[`」だけ（要件 1〜3 の対象そのもの）。例外候補は `\___x[…]`（D2） |
| 既存檻 | `lexer_tests` 415／428 行（数字＋`[`）が群 (b) の実装ミスを赤にする。T10／T14／P8／P9 が不変群を守る |
| 同ウェーブ干渉 | `lexer.rs` のみ → `text-decoration-canon`（`decode.rs`/`compile.rs`）と共有 0 |
| 弱点 | 既知 8 語が `lexer.rs` と `decode_bare` の 2 か所に並ぶ（D1 で檻を掛ける） |

### 案 β: 走査後の検証で引き戻す（post-scan validation）

現行の語走査を残し、角括弧経路に入る**前**に「`first` が既知語で、`word` が自分の規則の長さより長い」なら `take = 自分の規則の長さ` で角括弧なし経路へ流す。

- 結果は案 α と同一（同じ入力集合で同じトークン）。
- 差分は案 α よりやや多く（既知判定を 2 度書く形になりやすい）、「走査してから捨てる」ので読み手が追う経路が 1 本増える。案 α に対する利点は無い。退避案として記録。

### 案 γ: 角括弧経路の前提条件にする（bracket-path precondition）

169 行の `if chars[j]=='['` に「`word` が角括弧を取り得る形か」を足す。既知語について `word.len() == 自分の規則の長さ` を要求する。

- 案 β と実質同じ（判定の置き場が 1 行下がるだけ）。`word` が長い既知語は角括弧なし経路へ落ち、`bare_tag_len` が正しい長さを返すので結果も同じ。
- 「角括弧経路に入れない」条件が角括弧経路の側に書かれるため、`bare_tag_len` の doc（196〜199 行「ワード走査の結果の長さを消費長に使ってはならない」）と読み合わせたとき意図が分かりにくい。

### 案 δ: 全語を「`_` 以外は 1 文字」で閉じる（既知集合を持たない）

正典の角括弧なしタグは 1 文字か `\_` 系しか無いので、この規則は正典の模型に最も近く、差分も最小（既知集合が不要）。**しかし要件 4.10（未知語の走査規則を変えない）に反する**ため本仕様では採れない。`\sテキスト[注]` が `Raw("\s")`＋`Text("テキスト[注]")` に変わる（利用者には改善に見えるが、契約上は変更）。**残件として互換記録の残件欄（要件 7.3）と `ukadoc-coverage-roadmap` への申し送りに書く価値がある**（案 A の語彙表より軽い一般解の候補）。

### 既知集合の置き場（案 α〜γ に共通）

| 形 | 内容 | 利点 | 欠点 |
|---|---|---|---|
| (i) `lexer.rs` 内の `const`（`SHORTHAND_WORDS` の隣・8 文字） | 字句層に閉じる。依存方向を守る | 差分最小・`decode.rs` 非接触（W13 の干渉 0） | `decode_bare` の 8 腕と二重。**片方向の整合檻**（定数の各文字を `parse` に通して `Raw` にならないことを固定）で「字句層が知っている語は意味層も知っている」を守れる。逆方向（意味層が知っている語を字句層も知っている）は `decode_bare` の腕を列挙できないので檻にできない——doc 注記で相互参照する |
| (ii) 共有定義（`model.rs` か新 `vocab.rs`）を両層が参照 | 定義点 1 つ | 理想形 | `decode_bare` は `match` の腕なので定数を直接使えず、`if KNOWN.contains()` へ書き換えることになる＝`decode.rs` を触る（同ウェーブの `text-decoration-canon` と rebase 面が生じる）。本仕様の規模に対して過剰 |
| (iii) 字句層が意味層へ問い合わせる | — | — | 依存方向の逆転。却下 |

## 6. 規模とリスク

- **規模: S**（1 日以内）。本番差分は `scan_tag` の分岐と定数 1 本。テスト 2 ファイル（字句層・通し）は既存の T13／P9 の型をなぞって既知タグ全件（8＋3＋12）× 閉じた `[…]`・未閉じ・不変群を並べるだけ。互換記録 1 行。
- **リスク: Low**。純粋関数・決定論・公開 API 不変・意味層非接触。唯一の判断点は既知集合の置き場（D1）と `_` 始まりの範囲（D2）で、どちらも結果に影響する入力は狭い。
- 実装ミスの検出力: 群 (b) の数字後続を誤ると `lexer_tests` 415／428 行が赤、`\_[x]` を誤ると T10 が赤、既知語を取りこぼすと新規テストが赤。要件 5.7 の「是正を元へ戻すと失敗する対」は新規テストが自然に満たす（§3 の欠陥行がそのまま赤の証拠）。

## 7. 設計への申し送り（判断項目）

番号付きの項目は要件ディスカッションの議題候補。**A＝勝者が明白（設計で決めて報告のみ）**、**B＝答えで作業が変わる（開発者裁定が要る可能性）**。

1. **【B】既知集合の置き場と整合檻の形**（§5「既知集合の置き場」）。(i) `lexer.rs` 内 `const` ＋ 片方向の整合檻（定数の各文字が `parse` で `Raw` にならない）を推奨候補とするが、「定義点は 1 つ」を優先して (ii) を採るなら `decode.rs` を触る（W13 の `text-decoration-canon` と rebase 面）。利用者から見える結果は同じ。
2. **【B】群 (c) の範囲——`_` 始まりの全綴りか、正典 12 綴りに限るか。** 要件 1.4 は「先行 spec の固定長規律に従うすべて」と読めるので `\_z文字[注]`（正典外）も `\___x[1]` も自分の規則で閉じる（前者は `Raw("\_z")`＋`Text("文字[注]")`、後者は `Raw("\___")`＋`Text("x[1]")`）。ただし `\___x[1]` は現行 `Raw("\___x[1]")` から**変わる**ので、要件 4.10「未知語は不変」との境界を設計で明文化する（「`_` で始まる語は先行 spec の規律に従う＝既知扱い」とするのが最も単純で、12 綴りの表を字句層に持ち込まない）。利用者から見える差は `\___x[…]` という実用上あり得ない形だけ。
3. **【A】群 (b) の数字後続。** `\w2[x]`／`\b2[x]` は要件 4.3 で「角括弧経路のまま」と確定済み。案 α では「`w`/`b`/`p` の直後が数字なら頭打ちしない」の 1 条件で表す。既存檻（`lexer_tests` 415／428 行）が守る。
4. **【A】テストの配置と名前。** `structure.md` の兄弟ファイル規約に従い、`lexer.rs` 末尾へ `#[path = "lexer_word_boundary_tests.rs"] mod word_boundary_tests;`、`parse.rs` 末尾へ `#[path = "parse_word_boundary_tests.rs"] mod word_boundary_tests;` を足す（同ディレクトリの stem は `decode`／`lexer`／`model`／`parse` で衝突しない）。既存 T13／P9（`[` 無し）とは重複させず、新規側は「本文中 `[`」の形だけを持つ。既知タグ全件は `lexer_bare_tag_tests` 同様に 1 テスト内でループする（要件 5.1〜5.3）。
5. **【A】互換記録の登記位置と「触らないファイル」。** `COMPAT_ARCHITECTURE.md` §8 の 213 行（先行 spec の行）の直後へ 4 欄で 1 行。根拠欄に要件 7.2 の 3 点、残件欄相当に要件 7.3 の 2 点＋案 δ（全語 1 文字）を一般解候補として添える。`decode.rs`・`compile.rs`・完了 spec 文書は非接触。
6. **【A・環境】ワークスペース全体テストの前提。** 本ワークツリーは `vendors/pasta` submodule が未初期化で `cargo test` が解決に失敗した（本日 `git submodule update --init --recursive` で解消・`areka-parsers` 415 本緑）。ワークスペース全体（要件 5.11）は i686 host-32 成果物を要する既知の前提があるため、実装タスクの実行手順に前置する。
7. **【A】注記の更新箇所（要件 6.6）。** `lexer.rs` モジュール doc 6〜11 行（bare タグの説明）・`Token::Bare` doc 36〜41 行・`scan_tag` の 138〜140 行（「本文でも空白でも止まらない」は未知語に限る旨）・`bare_tag_len` doc 194 行（「`word` は … 走査結果」は頭打ち後も真だが、頭打ちの存在を一言足す）。`decode.rs` の注記は現状のままで実装と食い違わない（`Token::Bare` の載荷は不変）。

### Research Needed（設計で確認・小）

- **R1**: 正典の角括弧なしタグに `\_` 系以外の多文字綴りが無いことの再確認（`doc/ukadoc-coverage/ledger/sakura-script.toml` か ukadoc MCP）。無ければ互換記録の残件欄に「案 δ（`_` 以外は 1 文字）が正典の模型に一致する」と書け、`ukadoc-coverage-roadmap` の裁定材料になる。有れば残件の文言を狭める。本仕様の実装には影響しない。
- **R2**: 案 α の「`_` 始まりは 3 文字で頭打ち」が `\_` 単独末尾（T8）・`\__`（T8）・`\_%username`（T7 の変形）で現行と同じトークンを返すことを、設計の受入例に含めて実装前に机上で通す（§3 の probe は `[` 系のみで、これらは既存 T7／T8 が守る）。

## 8. 次のステップ

- `/kiro-requirements-discussion areka-P0-sakura-tag-word-boundary` で判断項目 1・2 を確認（3〜7 は設計で決めて報告）。
- その後 `/kiro-design areka-P0-sakura-tag-word-boundary`。設計は案 α を主線に、既知集合の置き場（D1）と `_` 始まりの範囲（D2）を決定ログに残す。
