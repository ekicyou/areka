# Implementation Plan

> 入力: `requirements.md`（10 要件・78 基準）・`design.md`（2026-09-24 設計ディスカッション反映済み）。新クレート `areka-update` に閉じ、既存ソースは 1 行も触らない。テストは各本番ファイルの隣の `*_tests.rs`（`#[cfg(test)] #[path]`）に置く。各タスクの完了条件は `cargo test -p areka-update` の緑を含む。モジュールの依存の向きは design「依存の向き」に従う（`paths` は `manifest` より左）。

- [ ] 1. 基盤: クレートの足場・契約の型・試験の道具
- [x] 1.1 新クレートの足場を建てる
  - ワークスペース配下に更新エンジンのクレートを新設し、本番の依存は承認済みの文字コード・誤り型・記録・Windows 束縛の 4 本だけにする
  - Windows 束縛の機能（Foundation・WinHTTP・暗号）はクレート自身の定義で上乗せし、根の定義は変えない
  - テスト専用の道具（記録の捕捉・検体の複製）は dev 依存にだけ置く
  - 完了: `cargo build -p areka-update` が通り、`git diff` で既存の `.rs`・根の `Cargo.toml` に差分が無い
  - _Requirements: 10.1, 10.4, 10.8_
- [x] 1.2 閉じた失敗の語彙と結果・進捗の型を定める
  - 失敗の理由 11 語を 1 宣言から生成し、種別名と全数一覧を同じ宣言から得る
  - 取得の失敗 8 種（複製と比較ができる形）・段 6 種（削除の段は無い）・警告 7 種・無効の理由 9 種・削除行の拒否 4 種を定める
  - 成功の 2 形（差分 0／n 件と 4 つの一覧）・進捗 6 種（番号は 0 始まり）・定義ファイル名 2 形を定める
  - 失敗の結果は段・理由・原因のファイル・戻せたか・残骸・作業場所を返せる
  - 完了: 語彙のテストで種別名が宣言順に並び、全数一覧が 11 件、表示文が期待値と実際値を両方含み、戻せたか・原因のファイルが理由ごとに正しい値を返す
  - _Requirements: 7.2, 7.3, 7.4, 7.6, 8.2_
- [x] 1.3 取得の境界と偽の取得口・試験の道具を用意する
  - 外界へ出る唯一の口を 1 つの trait として定める
  - 偽の取得口は URL → バイト列または失敗の固定表を持ち、呼ばれた URL を記録し、取得のたびに差し込みの関数を呼ぶ（表に無い URL は「無い」）
  - 木のバイト単位の写し・読み共有で開いたまま持つ保持・定義ファイル 2 形式の組み立て・Shift_JIS の符号化・対象の外を指すジャンクションの作成（作れなければ赤）を用意する
  - 完了: 道具の自己テストで、固定表どおりの戻りと呼出の記録・差し込みの呼出・保持中の `rename` の失敗・木の写しの一致が確かめられ、ネットワークに触れない
  - _Requirements: 3.1, 3.2, 9.6_

- [ ] 2. 定義ファイルの解釈と木との照合
- [x] 2.1 (P) パスのパーセント符号化の判定・復号・符号化を実装する
  - 全文字 ASCII かつ `%` が必ず 16 進 2 桁を伴うときだけ「符号化済み」と判定する
  - 符号化は UTF-8 のバイト列を大文字の `%XX` にし、未予約文字と `/` は残す
  - 完了: 判定（16 進 2 桁でない `%` は未符号化）・復号・符号化（非 ASCII・`/` の保持）の固定入力テストが緑
  - _Requirements: 1.15, 9.1_
  - _Boundary: urlpath_
- [x] 2.2 (P) 相対パスの解決・区切りの正規化・「実パスが対象フォルダの配下か」の判定を実装する
  - `\` を `/` に正規化する関数と、作業場所の名前（`.update-work`）で始まるかの大小無視の判定をここに置く（読み手・削除・作業場所が借りる）
  - 相対パスを対象フォルダ配下のローカルパスにし、最も深い実在する祖先を実パスにして対象フォルダの実パスから始まるかを返す（まだ無いパスにも使える）
  - 完了: 区切りの正規化・作業場所の判定・配下なら真／対象の外を指すジャンクションの下なら偽のテストが緑
  - _Requirements: 1.13, 5.6, 6.3_
  - _Boundary: paths_
- [x] 2.3 (P) OS の CNG で MD5 を計算する
  - 擬似ハンドルの MD5 を 1 呼出で使い、小文字 32 桁を返す。失敗は OS の暗号基盤の破損として致命扱いにする。入力は 4 GiB 以下を前提とする（守るのは呼び出し側）
  - unsafe はこの 1 呼出だけにする
  - 完了: RFC 1321 の既知のベクトル 3 本以上（空・`abc`・`message digest`）と一致するテストが緑
  - _Requirements: 2.1, 2.2, 10.2, 10.3_
  - _Boundary: md5_
- [x] 2.4 定義ファイルの文字コード解決・復号・行と欄の分解を実装する
  - `updates2.dau` は先頭エントリの最後の欄の `charset=` だけを先読みし、`updates.txt` は `charset,` 行を探す。解決できない名前は警告して既定の Shift_JIS に倒し、OS のロケールは読まない
  - BOM を優先して復号し、CRLF／LF の行に分け、`\x01` の欄を位置で読む（拡張欄は読み飛ばす）。`updates.txt` の `file,` 行・`charset,` 行・その他の行を正典どおりに扱う
  - 完了: 拡張欄あり／なし・Shift_JIS の日本語パス（指定あり／なしで同じ結果）・`charset=UTF-8`・CRLF／LF・`updates.txt` の 3 種の行・解決できない名前（警告 1 件と既定）・空ファイル（エントリ 0）の固定入力テストが緑
  - _Depends: 2.1_
  - _Requirements: 1.5, 1.6, 1.7, 1.8, 1.9, 1.10, 9.1_
- [x] 2.5 無効エントリ・重複・符号化の判定を定義ファイルの読み手に加える
  - 2.2 の正規化で `\` を `/` にしてから、無効 9 種を定めた順に検査し、最初に当たった理由 1 つを警告にする（作業場所の名前で始まるパスは 2.2 の判定を使う）
  - ローカルパスの小文字で重複を後勝ちにし、捨てた行を警告にする。MD5 は小文字に正規化する
  - 全エントリが符号化済みなら URL 側はそのまま・ローカル側は復号したバイト列を UTF-8 → 定義ファイルの文字コードの順で読み、そうでなければ URL 側だけを符号化する
  - 完了: 無効 9 種で各 1 件の警告・`..\` を区切りとして捕まえる・大小違いの重複の後勝ち・符号化済み（UTF-8／Shift_JIS のバイト列）と未符号化の固定入力テストが緑
  - _Depends: 2.2_
  - _Requirements: 1.11, 1.12, 1.13, 1.14, 1.15, 2.2, 9.1_
- [x] 2.6 差分を計算して要取得を選ぶ
  - 定義の側だけを定義の順に歩き、配下の検査 → 無い／在る → 在れば MD5 の比較、の順で要取得を決める
  - 在るのに読めない物と 4 GiB を超える物はハッシュの前に失敗にし、「要取得」に化けさせない（大きさはメタデータで見て、上限を引数に取る小さな判定で決める）。外へ解決される物も失敗にする
  - 完了: 無い／同じ／違う／定義に無いローカルのファイル、の 4 形で要取得が期待どおりになり、定義に無いファイルが前後で同一、読めない物（同名のフォルダ）で読めない失敗・大きさの判定が上限ちょうどで通り上限＋1 で失敗・ジャンクションの下のエントリで外へ解決される失敗になるテストが緑
  - _Depends: 2.3, 2.5_
  - _Requirements: 2.1, 2.3, 2.4, 2.6, 9.2_

- [x] 3. (P) WinHTTP による本物の取得口を実装する
  - セッションを 1 つ持ち回り、時間切れ 4 種と利用者エージェントを設定する。http／https を受け、転送は既定の方針（3xx 追随・https → http の降格拒否）に任せ、圧縮の自動伸長は使わない
  - 状態コードを数値で取り、404 は「無い」、2xx 以外はコード付きの失敗にする。本文は 0 バイトまで読み、上限を超えたら失敗にする
  - 名前解決・接続・時間切れ・TLS・その他を Win32 エラー番号の表 1 つで写し、ハンドルは成功・失敗とも必ず閉じる
  - unsafe と WinHTTP の綴りはこのファイルだけに置き、常時テストからは呼ばない
  - 完了: `cargo clippy -p areka-update` が警告なく通る。振る舞いは 6.3 の実機の一周で確かめる
  - _Depends: 1.3_
  - _Requirements: 3.3, 3.4, 3.5, 3.6_
  - _Boundary: winhttp_

- [ ] 4. 作業場所・確定と戻し・削除
- [x] 4.1 (P) 作業場所を作り、書き、片付ける
  - 対象フォルダ直下の棚にプロセス識別子と連番の名前で自分のフォルダを作り、落とした物（new）と退避した物（old）の 2 段を持つ
  - 作る前に他の走行の残骸を消し、消せない物は残骸に列挙する。`old/` に中身が残るフォルダ（戻せなかった走行の元の内容）は消さずに残骸に列挙する
  - 書き込みはバイト列を変換しない。片付けは自分のフォルダを消し、棚が空なら棚も消す。戻せなかったときは片付けずにパスを返す
  - 完了: 作成・書き込み・片付けで消える・他の走行の残骸を消す／消せなければ列挙・`old/` に中身の残るフォルダがもう 1 周の作成でも残り列挙されるテストが緑
  - _Depends: 2.2_
  - _Requirements: 4.1, 4.7, 4.8, 5.5_
  - _Boundary: work_
- [x] 4.2 ファイル単位の確定と逆順の戻しを実装する
  - 各ファイルを、配下の検査 → 親フォルダの作成 → 既存の退避 → 置く、の順に進め、各手の取り消しを積む。配下に無ければ外側に何も作らず失敗にする
  - 失敗（配下に無い場合を含む）で取り消しを逆順に全部試み、途中で失敗しても続ける。全部戻れば「戻せた」、1 つでも残れば戻せた一覧と戻せなかった一覧を返す
  - 定義に無いローカルのファイルには触らない
  - 最初に「戻しの削除を失敗させる」注入を現行の道具で較正する（読み取り専用の `remove_file` が失敗することを赤で確かめ、消せてしまうなら削除を拒む ACL 等へ替える）。選んだ手段は 5.2・6.1 の「戻せなかった」でも使う
  - テストの注入（保持・削除を拒む仕掛け・ジャンクションへの置き換え）は確定を直に呼ぶ前に仕込む（偽の取得口の差し込みは使わない）
  - 完了: 宛先の保持で退避が失敗 → 木がバイト単位で同一／無い親の下に置いた 1 件目が 2 件目の失敗で親ごと消える／削除を拒む注入で戻せなかった一覧が返る／ジャンクションに置き換えた親で外側に何も作られず 1 件目が戻る、の 4 本のテストが緑
  - _Depends: 4.1, 1.3_
  - _Requirements: 2.3, 5.2, 5.4, 5.5, 5.6, 5.7, 9.4_
- [x] 4.3 (P) `delete.txt` と `delete[数字].txt` を安全に適用する
  - 対象直下の `delete.txt` → `delete<N>.txt`（N の数値の昇順）を集め、定義ファイルの文字コードで行を読む。読めないファイルは警告して飛ばす
  - 絶対・`..`・外へ解決・作業場所の行を拒否して警告し、実体の種別を先に見て、行の種別と違えば取り除かず警告する
  - フォルダは中身ごと、ファイルは 1 つ取り除き、無い物は何もしない。取り除けない物は列挙し、一周を失敗にしない。読み手の定義行に ukadoc の印を 1 行置く
  - 完了: 拒否 4 種・フォルダの行・種別の食い違い 2 方向・無い物・`delete2.txt` が `delete10.txt` より先・Shift_JIS の行・読めない `delete.txt` の固定入力テストが緑
  - _Depends: 2.2_
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7, 9.5, 10.7_
  - _Boundary: delete_

- [ ] 5. 一周の進行を束ねる（統合）
- [x] 5.1 入口の検査から差分 0 の成功までと、記録の発火点・公開面を実装する
  - 入口で対象フォルダと更新先 URL を検査し、末尾 `/` を補って警告する（取得口はまだ呼ばない）。開始を `info!` で記録する
  - `updates2.dau` が「無い」ときだけ `updates.txt` に後退し、それ以外の失敗では後退しない。読み手の警告を 1 件 1 行の `warn!` に写す
  - 差分の段で要取得を通知し、0 件なら作業場所を作らず終了を記録して「差分 0」を返す
  - 失敗の全経路を 1 つの口に通し、`error!` を 1 回だけ出してから同じ内容を返す（失敗の終了に `info!` は重ねない）。スレッドを起こさない
  - 公開面（一周の関数・入力・取得の境界と本物の取得口・取得の失敗・進捗・成功と失敗の型）をクレートの根から出す
  - 完了: `run_tests.rs` を新設し、差分 0・有効 0 件・定義ファイルが無い・`updates.txt` への後退・通信失敗で後退しない・入口の失敗 2 種・末尾 `/` の各テストが緑。公開面は `use areka_update::{…}` の公開パスだけを使う doc テストで確かめる
  - _Depends: 1.2, 1.3, 2.6_
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.16, 1.17, 2.5, 2.6, 3.7, 7.1, 7.5, 8.1, 8.2, 8.3_
- [x] 5.2 取得・照合・確定・削除・片付けの段を一周に繋ぐ
  - 作業場所を作り、要取得を定義の順に 1 件ずつ取得 → 照合（不一致でも照合結果を通知してから失敗）→ 書き込みし、メモリに全件を溜めない。URL は更新先＋符号化済みパス
  - 全件揃ってから定義ファイルを最後の 1 件として確定し、確定の成功後にだけ `delete.txt` を適用する。取り除けない物と残骸を 1 件 1 行の `warn!` に写す
  - 失敗時は戻せなかった場合を除き作業場所を片付け、戻せなかったときは作業場所を残してそのパスを返す
  - 完了: `run_tests.rs` に、n 件の更新（置いた一覧が定義の順・定義ファイルが対象直下・`delete.txt` が適用・作業場所が消えている）・確定の途中失敗（戻せた・作業場所が消えている）・戻せなかった（作業場所のパスが返り、実在する）の各テストが加わり緑
  - _Depends: 4.2, 4.3_
  - _Requirements: 4.2, 4.3, 4.4, 4.5, 4.6, 4.8, 5.1, 5.3, 5.8, 6.1, 6.7, 7.1_

- [ ] 6. 検証: 一周の全経路・字面の見張り・実機
- [x] 6.1 偽の取得口で一周の残りの経路と横断の判定を加える
  - 5.1・5.2 が置いていない経路を加える: 取得失敗（2 件目・3 件目は呼ばれない）・MD5 不一致・作業場所を作れない・差分の段で配下の外（ジャンクション）・確定の段で配下の外（差し込みで 2 件目の取得時に親をジャンクションに置き換え → 確定の段で失敗・1 件目は戻される）
  - `run_tests.rs` の全経路について、戻り値の形・観測者が受けた進捗の列・レベル別の記録件数・木のバイト単位の同一性を同時に判定する
  - 完了: 全経路のテストが純 x64・ネット不使用で緑になり、失敗 1 回につき `error` 1 件・警告 1 件につき `warn` 1 件が件数で確かめられる
  - _Requirements: 5.6, 8.4, 9.3, 9.4, 9.6_
- [x] 6.2 語彙の全数対応・記録の欄・字面の見張りを検査にする
  - 11 語の固定入力を 1 本で回し、得た種別名の集合が全数一覧と完全一致し、失敗ごとに `error` が 1 件であることを判定する
  - `error` の欄（更新先・対象・段・理由・原因のファイル・戻せたか・作業場所）が戻り値と一致することを捕捉で判定する
  - 字面の走査は `*_tests.rs` と `testkit.rs` を除いた本番ソースだけを見る（`areka-nar/src/lib_tests.rs` の前例）。本番ソースで記録を綴るのは `lib.rs` だけ・`error!` は 1 か所・WinHTTP の API の綴り（公開型 `WinHttpFetch` を除く `WinHttp[A-Z]`、または `windows::Win32::Networking::WinHttp` の取り込み）は取得口の実装だけ・unsafe は取得口と MD5 だけ・dev 専用の道具が本番依存に無い、を字面で判定する
  - 完了: 見張りのテストが緑で、各見張りが既知の違反（別ファイルへ `tracing::` を 1 行・別モジュールへ `unsafe` ブロックを 1 つ足す等）で赤になることを 1 度確かめている
  - _Depends: 3_
  - _Requirements: 3.1, 3.3, 7.4, 8.1, 8.4, 10.8_
- [x] 6.3 本物の取得口で実機の一周を回す
  - 同じテストの中でローカルの TCP 待受を別スレッドの静的配信にし、パーセント復号したパスのファイルを 200 で、無ければ 404 で返し、転送用の前置パスには 302 を返す
  - 検体（emo2）の複製を対象に、辞書 1 つを書き換えた別の複製から定義ファイルと `delete.txt` を組んで配信する
  - 1 周目「差分あり → n 件更新・1 件取り除き」、2 周目「差分 0」を、記録の捕捉で段の順序と件数まで判定する。常時テストには入れない（`#[ignore]`）
  - 完了: `cargo test -p areka-update --release winhttp_real -- --ignored --nocapture` が緑で、実行したコマンド・段の順序・件数・2 周目の差分 0 を記録に残している
  - _Depends: 3, 5.2_
  - _Requirements: 3.3, 3.4, 3.5, 9.7_

- [ ] 7. 登記とワークスペース全体の関門
  - 先に `cargo about` と `cargo deny` が使えることを確かめ版を記録する。無ければ止めて報告し、第三者ライセンスの一覧を手で直さない
  - steering の技術方針に「HTTP と MD5 は OS の機能で賄い、HTTP クレートも MD5 クレートも足さない」の 1 行を足す
  - steering の構造にクレートの項（場所・目的・モジュール・依存・規律）を既存の項と同じ型で足す
  - 第三者ライセンスの一覧を生成器で作り直し、依存の関門を通す
  - 完了: `cargo test --workspace`（1,000 行の見張り・一時フォルダ・検体の置き場の見張りを含む）と `cargo clippy --workspace`・`cargo deny check` が緑で、既存の `.rs` と根の `Cargo.toml` の差分が 0
  - _Requirements: 10.1, 10.2, 10.5, 10.6, 10.8_

## Implementation Notes
- 1.2: `UpdateError::file()` は対象フォルダからの相対名だけを返す。パスを持つ失敗は `target.join(rel)`（`/` 区切りの相対名をそのまま join）から組むこと。`target_real` から組むと `None` になる。
- 1.3: `testkit::tree` はジャンクションを含む木で panic する（`fs::read` がジャンクションを読む）。ジャンクションを作るテストは、`tree` をジャンクションの無い木に対して取るか、ジャンクションを扱う分岐を足すこと。`FakeFetch` は `new`／`serve`／`fail`／`on_get`／`calls` で組む。
- 2.4: `Manifest.charset` は実際に使った文字コード（BOM が勝つ）で、`delete.txt` はこれを引き継ぐ。`manifest.rs` の `allow(dead_code)` は `run` が読み手を呼んだら外す。中身の無い `file,` 行は今は黙って飛ばされる（2.5 で無効エントリにするか決める）。
- 2.5: 中身の無い `file,` 行は `NoMd5` の無効エントリとして警告する。復号後のパスも同じ検査を通す（design に追記済み）。
- 4.2 較正: rustc 1.98.1 では読み取り専用のファイルも `fs::remove_file` で消える。ACL で削除を拒むと、置くための `rename` まで止まる。そこで「戻しの削除を失敗させる」注入は `testkit::pe_image()` の中身を書き、置く前の `new/` の写しに `testkit::pin(path)`（`LoadLibraryExW` AS_IMAGE_RESOURCE）を掛ける方式にした。5.2・6.1 の「戻せなかった」でもこれを使う。
- 4.3: Windows は部品の末尾の `.`・空白を黙って落とし、`.update-work` には 8.3 の短い名前（`UPDATE~1`）が付く。字面の検査だけでは消す経路が対象ごと・作業場所ごと消せる（実機で確認済み）。そこで `paths::unsafe_component`（空・`.`・末尾 `.`／空白・`:`）を読み手 2 つが共有し、`paths::resolves_under` は実パスが作業場所の中でも偽を返す（差分・確定・削除を 1 か所で守る）。短い名前は `testkit::short_name` で取る。
- 5.1: スタブを残さないため、`run` は n 件更新の経路（取得・照合・書き込み・確定・削除・片付け）まで配線済み。5.2 は主にその経路のテストを足す。定義ファイルを作業場所へ書けない失敗は `Stage::Commit`＋`WorkArea`、`warn!` の欄は `kind`＋`detail`（Debug）と design を揃えた。
- 6.2: 字面の見張りは `//` で始まる行を読み飛ばす（`lib.rs` の説明文が `tracing::error!` を綴るため。areka-nar の前例は生の字面を数える）。行末のコメントは走査される。
- 較正で本番ファイルを写し戻すと古い時刻のままになり、cargo が古いビルドを使って赤を出す。戻した後は時刻を更新してから走らせること。
- 6.3 実機の一周（2026-09-24）: `cargo test -p areka-update --release winhttp_real -- --ignored --nocapture` が緑（1 passed・1.4〜1.8 秒）。ローカル 127.0.0.1 の配信で、全要求が `/r/…` の 302 を経て取得された。404 は `NotFound`、500 は `Status{500}` になった。
  - 1 周目は `Updated`。`placed` は `delete.txt`・`ghost/master/dic/talk.pasta`・`ghost/master/日本 語.txt` の 3 件で、`readme.txt` を 1 件取り除いた。段は `ManifestFetched` → `DiffDecided`（3 件）→ (`DownloadBegin` → `Md5Compared`)×3 → `Committed` → `Deleted` の順。記録は info 2・warn 0・error 0。日本語と空白のパスは `%E6%97%A5%E6%9C%AC%20%E8%AA%9E.txt` で要求され、中身がバイト単位で一致した。
  - 2 周目は `Unchanged`。要求は定義ファイルだけで、記録は info 2。WinHTTP 側の欠陥は 0 件。
