# Implementation Plan

- [ ] 1. Foundation: dola cue 拡張と areka-sakura クレート雛形
- [x] 1.1 dola の cue コマンド集合へ改行コマンドを追加する
  - 改行比率を保持する新しい演出コマンド種別を dola の cue コマンド集合へ追加し、既存の derive（Clone/Debug/PartialEq/Serialize/Deserialize）が壊れないことを確認する
  - バリアント数を記述しているモジュールドキュメントと既存の網羅テストを新種別込みへ更新する
  - 新種別のシリアライズ往復テストを追加し、dola のテストスイートが全て成功することを確認する（observable: 新規テストと既存テストが green）
  - _Requirements: 4.2_
  - _Boundary: dola cue コマンド_

- [x] 1.2 (P) areka-sakura クレートの雛形を作成する
  - 新規クレートを作成し、dola・areka-actor・areka-parsers・tracing・thiserror を依存として宣言する
  - クレート冒頭のドキュメントに責務（さくらスクリプト再生）・三層構成（compile/drive/sink）・cue ドメイン写像の正本であることを記述する
  - 空実装状態でビルドが成功することを確認する（observable: 新規クレートの `cargo build` が成功）
  - _Boundary: crate scaffold_

- [ ] 2. Core: 契約型とエラー型
- [x] 2.1 talk の授受・出力・写像の契約型を定義する
  - kanade との授受メッセージ（起動・時刻前進・中断）、talk 起動契約、talk 相関 ID、reason 3 値を持つ終端信号、起動戻り値（投函端＋join ハンドル）を定義する
  - 両消費系統（surface 系／テキスト系）へ共通の発火表現（発火時刻・話者・演出コマンド）を定義し、演出コマンドを配送先スロット（surface 系／テキスト系）へ分類する関数を実装する
  - dola の cue ドメイン型と上流パーサの値型を二重定義せず再輸出する
  - 分類関数が全演出コマンド種別を正しい配送先へ分類することを検証する単体テストを書く（observable: 全 variant の分類テストが green）
  - _Requirements: 1.3, 3.1, 3.3, 4.1, 5.1, 6.1, 6.6, 7.4, 9.1_
  - _Boundary: contract_

- [x] 2.2 (P) 失敗種別を表すエラー型を定義する
  - 非有限な時刻前進・下流出力送出失敗を表すエラー種別を定義する
  - 各エラー種別の表示メッセージが原因を含むことを検証する単体テストを書く（observable: エラー表示の単体テストが green）
  - _Requirements: 11.1, 11.4_
  - _Boundary: error_

- [ ] 3. Core: talk コンパイラ（純粋層・単体テスト主戦場）
- [x] 3.1 (P) 待ち累積・話者スコープ転写・基本コマンド写像を実装する
  - 待ち命令を経過秒として単調非減少に累積し、以降の発火時刻へ反映する処理を実装する
  - 話者スコープ切替で現在スコープを更新し、テキスト・サーフェス切替・改行・クリアの各命令を対応する演出コマンドへ写像し、話者の転写と発火時刻を伴う発火列を構築する処理を実装する
  - サーフェス切替命令の引数を解釈・変換せず値のまま転写することを確認する単体テストを書く
  - 先頭に待ち命令がある場合でもその待ち時間が 0 に潰れず保存されることを確認する単体テストを書く（observable: 待ち累積列に対し発火時刻が単調に累積し、先頭待ちが保存されるテストが green）
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 3.1, 3.2, 4.1, 4.2, 4.3, 4.4, 5.1, 5.2, 5.3_
  - _Depends: 1.1, 2.1_
  - _Boundary: compile_

- [x] 3.2 終端検出・切詰めと M-boot 外タグの無視ログ・シームを実装する
  - 終端命令（通常終了／quit）と末尾到達を検出して終端理由を確定し、終端命令以降の命令を発火列へ含めず破棄する処理を実装する
  - M-boot で実挙動を持たない命令群と将来拡張用の未知命令を無視ログとして記録し、発火列を生成せず非パニックで継続する処理を実装する
  - 終端命令を含む列・M-boot 外タグを含む列のそれぞれで期待どおり切り詰め／無視されることを確認する単体テストを書く（observable: 終端切詰めテストと M-boot 外タグ無視テストが green）
  - _Requirements: 6.1, 6.2, 6.3, 6.5, 8.1, 8.2, 8.3, 11.2_
  - _Depends: 3.1_
  - _Boundary: compile_

- [ ] 3.3 talk コンパイラの決定性と不変条件を検証する
  - 同一の命令列を複数回コンパイルして常に同一の発火列・終端理由が得られることを確認するテストを書く
  - 生成される発火時刻が常に有限・非負・非減少であることを確認するテストを書く（observable: 決定性テストと不変条件テストが green で、talk コンパイラの単体テスト一式が成功する）
  - _Requirements: 2.5, 9.4, 11.2_
  - _Depends: 3.2_
  - _Boundary: compile_

- [ ] 4. (P) Core: 出力 sink とテスト用 mock を実装する
  - surface 系・テキスト系を型で分離した 2 つの出力先抽象を定義する
  - 発火を蓄積し、生成元スレッドとは別のスレッドから発火列と発火時刻を照合できるテスト用 mock を実装する（observable: 複数の発火を mock へ送った後、別スレッドから発火順どおりに全件取得できるテストが green）
  - _Requirements: 3.3, 4.1, 9.2_
  - _Depends: 2.1_
  - _Boundary: sink_

- [ ] 5. Integration: talk 運転手（駆動層・per-talk transient）
- [ ] 5.1 talk の起動・自己投函と発火列の駆動可能形への変換を実装する
  - talk ごとの名前付き実行単位を起動し、起動直後に起動要求メッセージを自身の受信口へ自己投函して、以降の時刻前進／中断の投函口と終了同期ハンドルを呼び出し元へ返す処理を実装する
  - 起動要求受領時に上流パーサでの変換とコンパイルを行い、発火列を時刻駆動可能な形へ変換する処理を実装する。変換にあたっては同一発火時刻の発火が記述順（先入れ先出し）で配信されることを保証する
  - 発火列が空の場合は時間軸駆動を行わず、コンパイル結果の終端理由を伴う終端信号を直ちに返すことを確認する統合テストを書く（observable: 空発火列の talk が即座に終端信号を返すテストが green）
  - _Requirements: 1.1, 1.2, 1.4_
  - _Depends: 2.1, 3.3, 4_
  - _Boundary: drive_

- [ ] 5.2 時刻前進の駆動ループと 2 系統への振り分け・自然終端を実装する
  - 時刻前進メッセージを受けるたびに、逆行・同値・非有限な値を安全に無視するガードを適用したうえで内部スケジュールを進め、到達済みの発火を配送先分類に従って surface 系／テキスト系の出力先へ振り分ける処理を実装する
  - 内部スケジュールが完了したことを検出したら、確定した終端理由を伴う終端信号を送出して当該 talk の実行単位を終了させる処理を実装する。安全ガードで検出した異常はエラー型として記録しつつ受信ループ自体は継続させる
  - fixture の script（テキスト・サーフェス切替・待ち・終端を含む一連の命令）を時刻前進列で駆動し、2 つの mock 出力先へ期待どおりの発火列・発火時刻が届き、正しい終端信号が返ることを確認する統合テストを書く（observable: fixture 駆動テストが期待発火列と終端信号を確認して green）
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.6, 9.1, 9.2, 9.3, 10.1, 10.2, 10.3, 11.3_
  - _Depends: 5.1, 2.2_
  - _Boundary: drive_

- [ ] 5.3 中断（Close）の即時停止とプロトコル異常のガードを実装する
  - 中断メッセージ受領時に進行中の再生状態を直ちに手放し、中断を終端理由とする終端信号を送出して即座に実行単位を終了させる処理を実装し、未発火の残り命令が処理されないことを保証する
  - 既に自然終端済みの talk への中断受領・起動要求の二重受領・非有限な時刻前進・想定外の内部エントリ遭遇を、パニックさせずログ記録のうえで安全に処理するガードを実装する
  - 再生途中の中断で中断を理由とする終端信号がちょうど 1 回返り、未発火分が出力先に届かないことを確認する統合テストを書く
  - 自然終端後に中断を受けても追加の終端信号が発生しないことを確認する統合テストを書く（observable: 中断テストと二重終端防止テストが green）
  - _Requirements: 6.4, 7.1, 7.2, 7.3, 7.4, 7.5, 9.1, 11.1, 11.2, 11.4_
  - _Depends: 5.2_
  - _Boundary: drive_

- [ ] 6. Validation: 統合シナリオと相関・再現性の検証
- [ ] 6.1 talk 相関 ID の一貫性と決定的再現性を検証する
  - 複数の talk を異なる相関 ID で起動し、各 talk の全出力（発火・終端信号）に起動時と同一の相関 ID が対応付けられることを確認する統合テストを書く
  - 同一の fixture script と同一の時刻前進列を複数回実行し、発火列・発火時刻・終端信号が毎回同一であることを確認する再現性テストを書く（observable: 相関 ID 一貫性テストと再現性テストが green）
  - _Requirements: 1.3, 6.6, 9.4_
  - _Depends: 5.3_
  - _Boundary: drive_

- [ ] 6.2 空 script と発火を伴わない終端のみの script の経路を検証する
  - 空の script、および M-boot 外タグのみで構成される script（発火列が空になるケース）を起動し、時間軸駆動なしで正しい終端理由の終端信号が返ることを確認する統合テストを書く
  - 発火を伴わない quit 相当のみの script が、通常終了ではなく quit を終端理由として終端信号を返すことを確認する統合テストを書く（observable: 空 script の終端テストと quit 単独 script の終端理由テストが green）
  - _Requirements: 1.4, 6.2_
  - _Depends: 5.3_
  - _Boundary: drive_
