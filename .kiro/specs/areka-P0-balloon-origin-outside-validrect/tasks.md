# Implementation Plan

> 実装は `crates/areka-emo-text` の中で完結する。新規ファイル・新規モジュール・依存 crate の追加は 0（design.md「File Structure Plan」）。
> 要件 7.1（`.kiro/steering/roadmap.md` 台帳行 #46 の完了更新）は完了手続き `/kiro-complete` の作業として意図的にタスク化していない（design.md「Out of Boundary」の明記による）。
> タスク 2.1 は既存 4 本を意図的に赤にする。赤の窓は 2.2 で閉じる——この 2 本は続けて実施し、間に別の作業を挟まない。

- [x] 1. 実物の検体で現行の欠陥を赤として固定する
  - `tests/shipped_fixture_region_test.rs` の**末尾**に、範囲外の `origin` を宣言した検体 `emo2-kakukaku-offsetdpi` を引くものを 2 つまとめて置く――共有の窓口をプロセス寿命で保つ値と、その根を返す関数（既存の複製検体と同形）。パスを自分で継ぎ足す書き方はしない。既存の根パス関数・既存 6 関数の近傍には手を入れない
  - 既存の `merged_model`（本番と同じ 2 層マージの経路）と `assert_region` を使い、sakura `(36, 46)`／kero `(24, 40)` を期待する。原本と同じ辺・開始点・折返しの定数を使い、原本との領域まるごとの `==` 比較はしない
  - 同じテストの中に前提の確認を 3 つ置く——マージ後のモデルが `origin` を**宣言している**こと・2 枚の PNG の IHDR が定数と一致すること・横書きへ解決されること
  - 冒頭 doc の禁則（宣言された生値を assert しない）が原本 `emo2-kakukaku` のテストに対するものである旨を、対象を明記して書き足す
  - 完了時に観測できること: 追加したテストが現行の実装で開始点 `(0, 0)` を得て**赤**になり、同ファイルの既存 6 関数は 1 つも書き換えずに緑のままである
  - _Requirements: 1.6, 2.3, 5.5_
  - _Boundary: FixtureTest_

- [ ] 2. 開始点の解決規則を改める（純粋層）

- [x] 2.1 範囲外に宣言された `origin` 成分を書字開始角へ落とし、無視した宣言値を領域に運ぶ
  - 成分の解決関数の返値を「開始点の成分」と「範囲外ゆえ無視した宣言の解決値」の対にし、範囲外の腕だけが後者を持つ形にする。範囲判定の式は現行のまま（両端を含む）1 か所に保ち、負値を絶対値化してから判定する順序も現行のまま
  - 文字領域に「無視した `origin` 宣言の解決値（成分ごと）」の欄 1 つと、crate 内からの読み口 1 つを足す。欄は非公開のまま・`Clone + Copy + Debug + PartialEq` を保つ
  - 解決のたびの DEBUG は残し、範囲外の腕の文言だけを「範囲の外にあるので宣言を使わず書き始めの角を用いる」旨の平易な語へ改める（プロジェクト内部の言い回しを持ち込まない）。純粋層は WARN を書かない
  - **このファイルの記述はすべてこのタスクが持つ**——冒頭と各定義の doc 6 か所を新しい規則へ書き直し、撤去→取り下げの経緯（2026-08-27 撤去・2026-09-18 取り下げ・理由＝撤去の前提が実機目視で反証された）と参照先を残す。「`range` は返す値に影響しない」の不変条件は削除する。ほかの 4 ファイルのコメントは 6.1 が持つ
  - 書字開始角の選択・`validrect` の解決・解析層・消費側（配置・`\_l`・描画）は一切触らない
  - 完了時に観測できること: タスク 1 の検体テストが緑に変わる。同時に、旧規則を期待値に固定している既存 4 本（純粋層の中の 1 本と兄弟ファイルの 3 本）が**赤になる**——これは想定内の赤であり、名前を控えて 2.2 で閉じる。`region.rs` は 1,000 行の見張りに触れず（見込み 965〜975 行）、純粋層の `windows` 非依存の検査は緑のままである
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.7, 2.1, 2.2, 2.4, 3.5, 6.4_
  - _Boundary: OriginResolution_

- [x] 2.2 撤去を固定している既存 4 本を新しい規則の期待値へ書き直し、赤の窓を閉じる
  - 純粋層の中の 1 本と兄弟ファイルの 3 本について、名前を新しい規則を語る名前へ改め、範囲外の場合の開始点を書字開始角へ更新する
  - `validrect` から独立であることを主張している 1 本は主張を反転させる——宣言が範囲内に留まる矩形では宣言どおり、宣言が範囲外になる矩形では書字開始角へ動く。退化した全 0 の矩形は規則の自然な帰結として期待値だけ更新し、保証としては語らない
  - 負値の 1 本は、範囲内の場合と非負の宣言との同値の対照を据え置き、範囲外の 2 場合だけを書字開始角へ更新する
  - 被覆を失わないこと（名前と期待値の更新であって、検査の削除ではないこと）を書き直しの単位で確かめる。旧名を参照している doc の 1 か所も同時に直す
  - 完了時に観測できること: `cargo test -p areka-emo-text` が赤 0 に戻り、4 本とも新しい名前で緑になる。無改変の 3 本（未宣言の対照・分岐 5・分岐 7）は触らないまま緑である
  - _Requirements: 5.4_
  - _Depends: 2.1_
  - _Boundary: OriginResolution, OriginRangeTests_

- [ ] 3. 装着 1 回につき成分 1 つ 1 件の WARN を登録口から書く（結線層）
  - 警告の関数本体を `actor_decoration.rs` に `pub(super)` で新設する（`actor.rs` は 1,000 行まで余白 30 行しかないため。書く**時点**は登録口のままで、関数の置き場所だけが子モジュール）
  - 登録口で前回の解決済み領域を上書きの**前**に 1 度だけ取り出し、既存の折返しの警告に続けて新しい警告を呼ぶ。前回と同値の領域なら何も書かない（先例と同じ件数規律）
  - 欄は `balloon`（既存のプレースホルダ定数を共有）・`key`（`origin.x`／`origin.y`）・`resolved`・`range_min`／`range_max`・`corner` の 6 つ。文言はバルーンの作者が読んで意味の取れる平易な 1 文にし、テスト側の定数と 1 文字単位で一致させる
  - 既存の折返しの警告の文言・欄・件数は 1 文字も変えない（実機走行の手順書がこの語を検索している）
  - 完了時に観測できること: 範囲外の `origin` を宣言したバルーンを装着すると成分 1 つにつき WARN が 1 件出て、範囲内の宣言と未宣言では 1 件も出ない。`actor.rs` の増分は 3〜7 行に収まり、crate のテストは赤 0 のままである
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - _Depends: 2.2_
  - _Boundary: IgnoredOriginWarning_

- [ ] 4. 規則を決定論テストで固定する

- [ ] 4.1 3 書字方向 × 2 成分 × 11 場合の全数の表を足す
  - `region_vertical_canon_tests.rs` に 66 行を**繰り返しで生成**する表のテストを 1 本足す（手で代表を選ばない）。場合は近い辺の 1 つ外／近い辺ちょうど／内／遠い辺ちょうど／遠い辺の 1 つ外／検体の形／負値の 5 場合
  - 各行の主張は 5 つ——検査する成分の開始点・他方の成分が宣言値のまま（成分の独立）・無視した宣言値の有無・DEBUG の件数（外 1・内 0）・WARN 0 件
  - 解決後の値と内外の別は表に数値で直書きする（実装と同じ式で計算し直さない）。書字開始角も方向ごとに直書きする
  - 検査しない側の成分は範囲内の固定値で宣言し、既存の補助（他所の DEBUG を封じる解決と、捕捉窓が生きていることの確認）を使う
  - 完了時に観測できること: 66 行すべてが緑で、遠い辺の 1 つ外の行が「最寄りの辺ではなく書字開始角へ落ちる」ことを固定している。同ファイルの行数は 1,000 行の見張りに触れない
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.7, 2.1, 5.1, 5.2_
  - _Depends: 2.2_
  - _Boundary: OriginRangeTests_

- [ ] 4.2 (P) WARN の件数と欄を数えるテスト群を足す
  - `actor_region_warn_tests.rs` に 4 つの場合を足す——両成分が範囲外のバルーンの装着でちょうど 2 件（片方だけなら 1 件）／値の変わらない再追従を 3 回で追加 0 件／binding だけが変わって領域が同値なら 0 件・原寸が変わって領域の値が変われば新しい値で 1 件ずつ／範囲内に宣言したバルーンの装着で WARN の**総数**が 0 件
  - 捕捉した WARN のうち本警告の文言と一致するものだけを数える（既存の折返しの警告と混在する捕捉窓から選り分ける）。ただし総数 0 件を主張する場合だけは文言で絞らない
  - 0 件を主張する場合は、同じ捕捉窓の中で対照の記録を 1 件発行し、それが数えられていることを併せて確かめる
  - 既存 6 本は `origin` を宣言しない入力なので無改変のまま成り立つことを確かめる
  - 完了時に観測できること: 件数の 4 つの場合がすべて緑で、0 件の主張が空振りしていないこと（対照が数えられていること）が同じテストの中で見える
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 5.6_
  - _Depends: 3_
  - _Boundary: WarnCountTests_

- [ ] 5. 判断を潰して赤が出ることを確かめ、元へ戻す
  - 範囲外の腕だけを一時的に「開始点に解決後の値を返す」形へ戻し、表の「外」の行・書き直した 4 本・検体のテストが**赤になる**ことを確かめてから元へ戻す
  - 続けて警告を何もしない形にして、件数のテストの装着の場合が赤になることを確かめてから元へ戻す
  - 走らせた差し替えは 1 つも残さない（戻した後に差分が無いことを確かめる）
  - 完了時に観測できること: どの檻がどの判断を潰したときに赤くなったかが、テスト名とともに記録として残り、差し替えを戻した後の差分が 0 である
  - _Requirements: 5.3_
  - _Depends: 4.1, 4.2_

- [ ] 6. 文書と記録の追随

- [ ] 6.1 旧規則を現在形で語っているソース内のコメントを言い直す（純粋層の外）
  - 旧規則「宣言された `origin` は範囲の内外を問わず字義どおり」を現在形で語っているコメント 4 か所——配置層の 2 か所・カーソル基点の doc・検体から宣言を消した是正の doc の 1 文——を「範囲内の宣言は宣言どおり・範囲外と未宣言は書字開始角」へ揃える
  - 最後の 1 つは修正後に偽になる予言（宣言が復活すれば赤くなる、の旨）なので事実へ言い直す。テスト本体は無改変
  - 配置層は 1,000 行の見張りまで余白が少ないので、語の置き換えだけで行を増やさない
  - 旧規則の語で全文検索し、漏れが 0 であることを確かめる（純粋層の記述は 2.1 が済ませている）
  - 完了時に観測できること: 旧規則の語での全文検索が、言い直した後の記述だけを返す（新しい規則と食い違う現在形の記述が 0 件である）。触った 4 ファイルはいずれも行数が増えていない
  - _Requirements: 6.4_
  - _Depends: 2.1_
  - _Boundary: SourceComments_

- [ ] 6.2 (P) 互換方針の対応表 §8 の 3 行を取り下げの事実へ改める
  - 撤去の行を「撤去の取り下げ」の行へ——判断欄に本仕様の規則（範囲外は宣言なしとして書字開始角へ・両端は範囲内・成分ごとに独立・負値は絶対値化してから判定・解決側 DEBUG ＋装着 1 回につき成分 1 つ WARN 1 件）、根拠欄に取り下げの理由と日付、出典欄に本仕様と上書きした完了 spec の要件番号を記す。撤去の経緯は履歴として残す
  - `\_l` の縦書き座標系の行にある理由の文を、撤去を前提にしない形へ言い直す（結論は不変）
  - `\_l[x,y]` の上書き行の「未宣言成分だけが書字開始角へ落ちる」を「未宣言または範囲外の成分が……」へ改める
  - 上の 3 行が既に抱えている陳腐化した行番号の引用を、「何の定義行か」で指す形へ改める。書く file:line・関数名・テスト名は書く前に実ファイルで裏取りする
  - 完了時に観測できること: §8 に「撤去」を現在の判断として語る行が残っておらず、3 行のいずれにも行番号の引用が無い
  - _Requirements: 6.1, 6.2, 6.3, 6.6_
  - _Boundary: CompatDoc_

- [ ] 6.3 (P) `\_l` の原点の全数確認を引き直して 0 を明示する
  - `\_l` の原点に宣言された `origin` を使う既存テストの全数確認を、記録の正本（research.md §3.4）の 3 手法で引き直す
  - 手法を 1 つ足す——`origin` の構築点を全数、ファイルごとの件数つきで列挙し、値を素通しする補助関数はその呼び手まで辿る（元の 3 手法では 6 ファイルが漏れることを設計検証が実測で示したため）
  - 範囲外の値を使うものが 0 本であるなら、その 0 を対象ファイル・方法・日付とともに明示して書く（表や引き算で導ける 0 は沈黙と同じ）。見つかったときは期待値を本仕様の規則へ見直す
  - 実装の変更は無い。`\_l` の既存テスト群が無改変のまま緑であることを併せて確かめる
  - 完了時に観測できること: 記録の正本に、調べた対象・方法・日付つきの件数が並び、`\_l` の既存テスト群が無改変で緑である
  - _Requirements: 4.1, 4.2, 4.3_
  - _Boundary: OriginRangeSurvey_

- [ ] 6.4 (P) 隣接する spec へ申し送りを 1 行ずつ置く（2 本）
  - プロパティの spec へ「基準点の値は解決後の文字描画開始点から導けば足りる」とだけ申し送る（本仕様では実装しない）
  - 検体パスの申し送りは**行わない**――引受先として見込んでいた `areka-P0-nar-install` は 2026-09-19 に完了しており（完了 spec は先送りを吸收できない）、共有の窓口への寄せは先方が済ませた。本仕様はその窓口を使うだけで要件 7.3 を満たす（タスク 1 で済む）
  - プレースホルダ定数を実名へ差し替える spec へ、参照が 3 ファイル・警告 2 種類に増えたことを申し送る
  - 申し送り先の brief が実在することを、書く前にパスで確かめる
  - 完了時に観測できること: 2 本の brief に 1 行ずつ申し送りが載り、いずれの行も実在しかつ**未完了の** spec を指している
  - _Requirements: 7.2, 7.3_
  - _Boundary: Handoffs_

- [ ] 7. crate とワークスペースの全数を回し、見張り 3 本と無改変の約束を確かめる
  - `cargo test -p areka-emo-text` を `--lib` と統合テストの両方が走る形で回し、続けてワークスペース全体を回す。完走は出力を途中で切らずに確かめる（走ったスイートの本数で数える）
  - 1 ファイル 1,000 行の見張り・純粋層の `windows` 非依存の字面検査・走査対象の登録の検査の 3 本が緑であることを個別に確かめる（新設ファイルが無いので走査対象の登録は無改変のはず）。タスク 6 のすべての編集（6.1〜6.4）を含めた状態で回す
  - 解析層・完了 spec のアーカイブ・検体のファイルに差分が無いことを、実在するパスを指していることを確かめたうえで見る
  - 検体を固定している既存テストの**既存の検査関数を 1 つも書き換えていない**ことを差分で確かめる
  - 完了時に観測できること: ワークスペースの全スイートが緑で赤 0、見張り 3 本も緑である旨を、走行の出力と差分の確認とともに記録できる
  - _Requirements: 2.3, 2.4, 5.7, 6.5_
  - _Depends: 5, 6.1, 6.2, 6.3, 6.4_

## Implementation Notes

- タスク 2.1 は `region.rs` の散文のうち `origin_components_resolve_literally_and_independently` に属する 3 か所を意図的に残した——doc（`region.rs:694-699`）・本体内のコメント（`:708`）・関数名そのもの。いずれも旧規則を現在形で語っており、**2.2 が名前と期待値を書き直すときに同時に言い直す**。書き直し後に `grep -n '字義' crates/areka-emo-text/src/region.rs` が 0 件になること、「旧クランプ」「寄せ先」の語が残っていないことを確かめる——6.1 の完了条件である全文検索の 0 件は、この 3 か所が閉じて初めて成立する。
- `TextRegion::ignored_origin()` はタスク 3 で呼び手が付くまで `never used` 警告 1 件を出す（既存の同種警告 9 件と同列・`deny(warnings)` は無い）。`#[allow(dead_code)]` は付けない。
- `cargo fmt -p areka-emo-text -- --check` を各タスクの完了前に回す。タスク 1 は 1 行の整形崩れを残し、別コミット `6fb5bdfa` で追随した。
