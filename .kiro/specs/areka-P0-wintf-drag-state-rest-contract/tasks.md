# Implementation Plan

> 設計: `design.md`（案 A・述語 `is_button_held`・C1・`exists()` 直書き）。触るのは既存 11 ファイル・新規 0。コードは「何の定義か」で指し、行番号では指さない。

- [x] 1. wintf: ドラッグの状態の契約を実装に合わせる
- [x] 1.1 状態の説明を出来事で書き直し、述語を足し、呼び手の無いリセット関数を撤去する
  - 5 つの状態それぞれに「入る出来事／出る出来事」を書き、`JustEnded` には「次の左押下まで休む・製品コードが `Idle` へ戻すことは無い」と書く。`JustStarted` の出口は「次の tick の `dispatch_drag_events`」（フレーム数の語は使わない）
  - 写しの型の説明に「各状態の意味は状態そのものと同じ」を 1 行足す
  - 「左ボタンを押している間か」を答える述語を状態そのものと写しの両方に置き（`Preparing`・`JustStarted`・`Dragging` で真）、押下の受け付け判定をその述語へ寄せる（判定結果は同じ）
  - 呼び手の無いリセット関数を定義と再輸出から消し、同じ変更の中で既存テストを追随させる: 陳腐化した 2 本を区切りコメントごと削除し、透過制御のテストの後片付けを状態の更新関数で `Idle` を代入する形へ置き換える（確かめる内容は変えない）。分けると中間状態で compile が落ちるので 1 タスクで行う
  - 完了時: `cargo test -p wintf` が緑で、`crates/wintf` にリセット関数の名前が 0 件、状態そのものと写しの説明で `rg "1 ?フレーム"` が 0 件、押下の受け付けの既存テスト 2 本（既に動作中なら無視・`JustEnded` から受け付け）が不変のまま緑
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 3.5, 6.1, 6.5_

- [x] 1.2 契約が後退したら赤くなる決定論テストを 3 本足す
  - 述語の真偽表: 製品と同じ遷移関数で 5 状態を順に組み、各段で状態そのものと写しの両方の述語を確かめる
  - 解放の契約: 押下 → 解放の直後に、写しが `JustEnded`・述語が偽（両型）・他の関数を呼ばずに別の entity の押下が受け付けられ `Preparing` になる
  - 中断の契約: 押下 → 閾値到達 → 中断で、解放と同じ 3 点を確かめる
  - 手で組んだ状態から始めず、期待値は書き下し、末尾で状態を待機へ戻す（既存ヘルパを使う）
  - 完了時: 3 本が緑で、兄弟テストファイルの総行数が 1,000 未満
  - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3, 3.4, 3.7_

- [x] 1.3 (P) 当たり判定の読み手を述語へ寄せる
  - `WM_NCHITTEST` のキャッシュの当たり判定で、状態を並べて聞いている箇所を述語の呼び出しへ置き換え、ローカル変数名を「押している間」の意味へ揃える
  - 定数の説明 1 か所と関数内のコメント 2 か所の「ドラッグ中」を「左ボタンを押している間」へ直す
  - 完了時: `HTCLIENT` を強制する条件の真偽が寄せる前と同じで、`cargo test -p wintf` が緑
  - _Requirements: 2.3, 2.5_
  - _Boundary: pointer nchittest_cache_
  - _Depends: 1.1_

- [x] 1.4 (P) 透過制御の説明の「直前 1 フレーム」2 か所を出来事の語へ直す
  - 透過制御の判定関数の説明（判定順序 1）と `docs/click_through.md` の「ドラッグ中の透過抑止」の段落で、`JustStarted` を「閾値到達から次の tick の `dispatch_drag_events` まで」と書く
  - `JustEnded` 再収束（R5.2）の文言とランタイムの配線の説明は変えない。判定式は触らない
  - 完了時: 判定関数の説明と文書の 2 か所で `JustStarted` を指す「1 フレーム」の語が 0 件、透過制御の既存テストが緑（状態の説明側の同じ確認は 1.1、3 ファイル通しの確認は 3 が持つ）
  - _Requirements: 1.5, 6.2_
  - _Boundary: clickthrough controller, docs_

- [ ] 2. areka: 右クリックの引き金と説明書を直す
- [x] 2.1 右解放の判定を述語へ寄せる
  - 右解放を無視する判定を写しの述語へ置き換え、使わなくなった写しの型の import を外し、コメントを「`JustEnded` は押していない」の意味へ書き換える。無視の理由文字列は変えない
  - 完了時: 左クリック後の右解放でメニューが開く既存テストと、ドラッグ中の右解放を無視する既存テストが不変のまま緑で、`cargo build -p areka` に未使用 import の警告が出ない
  - _Requirements: 2.4, 2.5, 3.6_
  - _Depends: 1.1_

- [x] 2.2 預かった右ダブルクリックを要求の窓で絞り、決定論テストを足す
  - 返事が決着した tick で、預かりを判断関数へ渡す前に窓番号を比べ、異なれば両方の窓番号を添えた `trace!`（新しい event 名）を 1 行出して預かりを捨てる。判断関数の引数・本文は変えない
  - フロー試験に窓番号を引数に取る預かりのヘルパを足し、既存の預かりヘルパはそれを窓 0 で呼ぶ形にする（既存テストの本文は不変）
  - 「窓 1 の預かり＋窓 0 の要求＋抑止の返事」のテストを 1 本足す: kanade へ何も届かない・預かりが残らない・照会中でない・新 event の `TRACE` 行が両方の窓番号付きで 1 本
  - 完了時: 新テストと、同じ窓＋抑止で 1 件届く既存テスト・同じ窓＋表示で捨てる既存テスト・判断関数の既存テスト 2 本がすべて緑で、フロー試験ファイルの総行数が 1,000 未満
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6, 6.3, 6.4_

- [ ] 2.3 (P) 説明書が無ければ OS を呼ばず `warn!` で終える
  - 説明書を開く World 経由の入口で、開く処理を呼ぶ前にファイルの実在を見て、無ければパス付きの `warn!`（新しい event 名）を要求 1 件につき 1 行出して戻る。メニューの灰色判定の関数は呼ばない（初回だけの `debug!` を奪わないため）。開く処理そのものの `error!` は変えない
  - ファイルの無い一時フォルダでこの入口を呼ぶテストを 1 本足す: 開けない失敗の行 0・開いた行 0・新 event の行が `WARN` かつパス付きで 1 本
  - 完了時: 新テストと、灰色判定の初回記録の既存テスト・開く処理が無いファイルを `error!` で記録する既存テストが緑
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 6.4_
  - _Boundary: readme_

- [ ] 3. 全体の回帰と静的な確認
  - `cargo test -p wintf` と `cargo test -p areka` が緑で、既存テストの本文の変更は要件 3.5 の除外 2 本と後片付け 1 か所だけ（`git diff` で確認。2.2 の預かりヘルパの委譲はテストの本文ではないので許容）
  - `rg reset_to_idle crates/ docs/` が 0 件
  - `rg "1 ?フレーム"` が状態の説明（`state/mod.rs`）・判定関数の説明（`controller.rs`）・`docs/click_through.md` の「ドラッグ中の透過抑止」の段落で 0 件（`runtime/mod.rs` の別の文脈は対象外）
  - 製品コードで `Preparing`／`JustStarted`／`Dragging` を並べる残りが、判定関数（「移動中か」）・値を取り出す 3 ファイル・述語の定義 2 か所・遷移関数の内側だけ
  - 触ったテストファイル 4 本の総行数がどれも 1,000 未満
  - 完了時: 上の 5 点がすべて満たされている
  - _Requirements: 1.1, 1.5, 2.5, 2.6, 3.7, 6.1, 6.2, 6.3, 6.4, 6.5_
