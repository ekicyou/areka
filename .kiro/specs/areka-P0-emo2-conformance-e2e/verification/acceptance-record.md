# emo2 適合走行 受入記録（様式）

> **本書は走行前に置く空欄の様式である。** 値の欄はすべて `（未記入）` で始まる。走行の前に埋める欄（§6 宣言）と、走行の最中・後に埋める欄（§7 以降）を分けてある。空欄のまま残っている欄は「まだ観測していない」を意味し、**合格でも不合格でもない**。
>
> - 対象仕様: `areka-P0-emo2-conformance-e2e`
> - 様式の根拠: design.md「Data Models」の**実機記録の欄**（13 区分）・D5（手順書と記録）・D7（読み分けと合否）・D8（既知症状の登記）・D9（欠陥の仕分け規律）
> - 手順の正本: `verification/lap-procedure.md`（走行の順序・読み分けの 4 行の表はそちらに置く）
> - 期待値の正本: design.md の**適合検証項目表（20 項目）**。**本記録は表を参照するだけで、期待する結果・観測の層・由来ユニット・期待値の出典の写しを持たない**（R4.1）

---

## 0. 書く前に

### 0.1 二層の合否は別の欄に書く（R1.3）

合否は 2 つある。**§11 合否（決定論層）**と**§12 合否（実機層）**である。

- 決定論層の合否は、常設テストの走行結果そのものである。相 1（決定論一周テスト）の走行の証跡が**そのまま**ここへ入る。実機走行の前に埋まる欄であり、実機走行を待たない。
- 実機層の合否は、**§8.1 の適合検証項目表 20 項目の実機サインオフ**である（R10.1 ⑶・R4.1）。人間の判断で確定する（R5.10）。**7 行の合否ブロックはこれではない**——ブロックは走行 D から採る**項目 16 の証跡**であり、§12 の中で総合判定とは別の欄に置く（design D5「層ごとに独立した合否」・D7・2026-09-04 の開発者裁定）。
- **片方の欄を他方で埋めない。** 決定論層が緑であることは §12 の欄を 1 文字も埋めない。実機層が合格であることは §11 の欄を 1 文字も埋めない。どちらか一方しか埋まっていない記録は、完成判定の根拠として不足である。

### 0.2 根拠は種別を区別して書く（R5.4）

| 種別 | 何を書くか | 書けないもの |
|---|---|---|
| 目視 | 誰が・いつ・何を見たか。段と操作を名指しする | 「たぶん出ていた」のような伝聞・推定 |
| ログの引用 | 生ログの該当行の逐語と、その行が在るファイルと行番号 | 要約した言い換え |
| 機械判定の出力 | 判定の走行の出力（§9 に全文を残し、ここには結論の行を引く） | 出力を読んだ人の解釈 |

3 種を混ぜて 1 つの欄に書かない。1 つの項目が 2 種以上の根拠を持つ場合は、種別ごとに行を分ける。

### 0.3 判断が縮退したら必ず理由を書く（R12.5）

観測が取れない・道具が動かない・条件が揃わない等で判断を縮退させた場合は、**縮退したという事実と理由を必ずこの記録に書く**。理由の書けない縮退は行わない。書く場所は縮退が起きた欄の直下（「縮退: （未記入）」の行）とし、書かないまま次の項目へ進まない。

### 0.4 消灯した観測点の沈黙を根拠にしない（R5.7）

出力が 0 件であることを「起きなかった」の根拠に用いない。使った観測点が実際に出力を出していることを §7 で先に確かめる。確かめられない場合は、その 1 語だけを目視の宣言で代替し、代替したことを §7 に書く。

---

## 1. 同定

| 欄 | 値 |
|---|---|
| 実施日時 | **2026-09-10 22:21:31 〜 2026-09-11 00:12:45**（走行 A → 走行 B → 走行 D → 走行 C の順・走行ごとの開始と終了は §5 と §5.1。機械判定は走行 D の後の 2026-09-10 23:38＝§9）。準備は 2026-09-10 の 1 回目（20:27〜20:30）に済ませ、**走行の直前 22:21:19 に事前確認をやり直した**（`preflight.txt`）。同じ日の 22:14:06 に始めた走行 A は 22:15:48 付近で起動元のセッションごと落ちて中断した（＝§13.3 の 6 件目・製品の症状ではない）ので、記録置き場は**同じ日の 2 回目** `emo2-conformance-2026-09-10-2` である。その前の走行 A は 2026-09-07 20:27 に走り、A3 で症状 F を見て中断した＝§13.3 の 5 件目 |
| 実施者 | **走行の操作と目視は開発者**（2026-09-10 22:21〜2026-09-11 00:12）。**準備者: AI（2026-09-10 20:30・22:21 に事前確認をやり直し）**。開発者の手元作業は走行の前に済んだ——⑴ 実拡大率は主モニタ **200%**（96 dpi でない水準・§2）、⑵ 重い別処理の扱いは §5 の並走の欄のとおり（SSP は開発者の指示で隣に起動したまま）、⑶ §6 の宣言と追記は 2026-09-10 22:2x に読み、**1 行も書き換えなかった**（§6 の末尾） |
| コミット | `6cbf7c59`（`6cbf7c591a2a6d7389577f3514447bad7e6d67c5`＝タスク 6.13 の修正〔あふれの送り量の原点を描画範囲の開始側へ〕・2026-09-10 20:20:54 +0900・件名「fix(areka-P0-emo2-conformance-e2e): task 6.13 - visible_window anchors the scroll at the region start instead of the first line …」（以下略）。解放の失敗を直したコミット `ce545350`〔タスク 6.11〕はこの祖先に含まれる。作業ツリー `C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56`・準備の時点で `git status --porcelain` が出すのは本記録と手順書と `tasks.md` の編集だけである）。**サインオフの対象になるコードは `6cbf7c59` のものである**——このコミットより後にブランチへ積んだのは文書だけのコミット（`4de3a1f2`〔タスク 6.12〕と、それ以後の文書コミット）で、`git diff --stat 6cbf7c59 HEAD -- crates/` は 2026-09-10 の時点で**空**である。したがって準備で作った実行体は、走行までにコミットが増えてもそのままサインオフの対象でよい（製品コードを触るコミットが積まれたら作り直す）。**走行時のブランチの HEAD は `434a4a82`**（文書だけのコミット）であり、実行体は `6cbf7c59` のものである。**走行の後に積んだ 2 件は本走行のビルドに入っていない**——⑴ `86d050de`（タスク 6.15＝括弧形 `\w[n]` の解釈を撤去した製品コードの改変・§13.4 の改変 6）、⑵ `1e90c743`（ゴースト資産 `pasta.dll` を `\_w[%d]` を出す上流のビルドへ差し替えたもの）。どちらも本走行の 20 項目の判定には効いていない（⑴⑵ を入れた後の確認の起動は §8.1 の行 13 に日付つきの注記として書いた） |
| ビルド構成 | `dev`（`cargo build -p areka`・2026-09-10 20:28 ビルド完了〔51.36 秒〕・`target\debug\areka.exe` 22,810,112 バイト・更新時刻 20:28:27）。32bit 橋渡しは `cargo build -p shiori-host32-helper --target i686-pc-windows-msvc` を**`cargo build -p areka` の後に**ビルドして隣へ複写した（手順書 §2.2 の順序。**本準備ではワークスペース全体のビルド／テストを走らせていない**——走らせると同じ名前の 64bit 版が上書きしてしまう）。タスク 6.11 が helper の原本を変えたので、今回の i686 のビルドは**実際に作り直した**（`shiori-host32-ipc` → `shiori-host32-helper` を再コンパイル・19.98 秒）。複写は 2026-09-10 20:28:52・直後に PE machine `014C` を確認 |
| OS（版・ビルド番号） | Windows 11 Pro 10.0.26200 |

---

## 2. 実機構成

| 欄 | 値 |
|---|---|
| モニタ台数 | **2 台**（走行 A の生ログ 2026-09-10T13:21:31.146697Z `[diag.monitor_snapshot] context=prepare_ghost_windows count=2`。以下の値は同 13:21:31.146721Z／`.146730Z` の `[diag.monitor]` 2 行から採った。時刻は UTC＝日本時間 −9 時間） |
| 各モニタの解像度 | 主 **2880×1800**（`index=0 bounds=0,0,2880,1800`）・副 **2560×1600**（`index=1 bounds=-2560,195,0,1795`＝主の左隣に置かれている） |
| 各モニタの拡大率 | 主 **192 dpi（200%）**・副 **144 dpi（150%）**（`index=0 dpi=192`／`index=1 dpi=144`）。どちらも 96 dpi でない水準である（R5.1）。走行 D はこの主モニタを **192 ↔ 144** で切り替えた（§5.1） |
| 各モニタの作業領域の下端 | 主 **1704**（`work_area=0,0,2880,1704`＝画面の下端 1800 との差 96 はタスクバー）・副 **1795**（`work_area=-2560,195,0,1795`＝画面の下端と同じ） |
| 主副の別 | `index=0` が**主**（`primary=true`）・`index=1` が副（`primary=false`） |
| ゴーストが載っているモニタ | **主モニタ**。起動時の拡大率 k₀ は主モニタの 192 dpi ÷ 作者基準 96 dpi ＝ **2.0** である（同 13:21:31.147685Z `placement: 起動時 k₀ を導出（primary モニタ DPI ÷ 作者基準 DPI・D7） primary_dpi=192 shell_author_dpi=96 balloon_author_dpi=96 k_shell=2.0 k_balloon=2.0`） |

> 拡大率は 96 dpi でない水準を含めること（R5.1）。実効の拡大率は、アプリ自身のログが記録した値を正とする。

---

## 3. 資産

| 欄 | 絶対パス |
|---|---|
| ゴースト | `C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\crates\pilot\examples\shiori-host-32\fixtures\emo2` |
| バルーン | `C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\crates\pilot\examples\shiori-host-32\fixtures\emo2\emo2-kakukaku` |
| 実 32bit の脳 | `C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\target\debug\shiori-host32-helper.exe`（PE machine `014C`＝32bit・**2026-09-10 20:28:52 に i686 版を複写**し、同時刻に PE machine を読んで `014C` を確認した。273,408 バイト。タスク 6.11 が helper の原本を変えたため、i686 の成果物は**このコミットで作り直された**（更新時刻 2026-09-10 20:28:52・置いた側と i686 の成果物で `Get-FileHash` の SHA-256 `4783B4D136FCD0A160707AAFFE6A9F7A380001F67002869ADF6A1F8A7B7DF854` が一致）。起動スクリプト `lap.ps1` が毎回この値を確かめ、`8664` なら起動を拒む） |
| 実バイナリ（起動する実行体） | `C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\target\debug\areka.exe` |

> すべて**絶対パス**で書く。相対パスでは脳の読み込みが成立しない（R5.1）。

---

## 4. 環境変数（R5.3）

指定しなかった変数は「**未指定（既定）**」と明記する。空欄のまま残さない——後から走行条件を復元できなくなるためである。

| 変数 | 既定 | 記録の書き方 | 実際の指定 |
|---|---|---|---|
| 画面更新の間引きの切替 | 既定は無効 | 指定しなければ「未指定（既定）」と明記する | **未指定（既定）**（走行 A・B・C・D すべて） |
| 記録用の出力水準 | — | 実際に与えた文字列を逐語で書く | `RUST_LOG=info,kanade=trace,wintf::transition=debug,areka::placement::diag=debug,areka_emo_present=debug`（走行 A・B・C・D すべて同一・起動スクリプト `lap.ps1` が与える） |
| 有界の自動終了 | — | 与えた値を書く。番犬が無いことも書く | `AREKA_APP_SMOKE_EXIT_MS`＝走行 A `1800000`（30 分）・走行 B／C／D `600000`（10 分）。番犬は付かない（下の明記）。**走行 A は自動終了で終わらせない**——A20 で Ctrl+左ダブルクリックし、終了挨拶が終わって窓が自分で閉じるところまでを見る（手順書 §5.2 の A20・R15.5） |
| 選択肢の見た目の反転の注入 | 既定は無効 | **本走行では点けない**。点ける場合は別走行として分け、その旨を記録する（処理系に 1 度だけ焼き付くため走行中に切り替えられない） | 走行 A・B・D は**未指定（既定）**。走行 C のみ `AREKA_CHOICE_HOVER_INJECT=cycle`（別プロセス・`lap.ps1 -Run C` が与え、他の走行では変数を消してから起動する） |
| 実機ログの機械判定の入力 | — | 判定の走行で与える絶対パス | `AREKA_TRANSITION_LOG=C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\lap-run-d.log`（走行 D の生ログ。置き場は**同じ日の 2 回目**なので末尾に `-2` が付く＝1 回目の走行 A が起動元のセッションごと落ちたため〔§13.3 の 6 件目〕。実際に与えた逐語は §9） |
| 実 32bit の脳の絶対パス | — | 絶対パスで与える | **変数は無い**——配置で決まる（実行体の隣の `shiori-host32-helper.exe`・§3 の行）。`areka.exe` はこの名を上書きする環境変数を読まない |

番犬が付かないことの明記: この直接起動には番犬（ハング判定の締切）が付かない。終わりを与えるのは `AREKA_APP_SMOKE_EXIT_MS` の有界自動終了だけである（手順書 §3.4）。

---

## 5. 走行

走行は **走行 A（初回起動の一周）→ 走行 B（再起動）→ 走行 D（拡大率遷移の専用採取）→ 走行 C（選択肢の見た目の反転）** の順に分かれる（手順書 `lap-procedure.md` §5.1 の制約 6・§5.4・design D5「走行の分割と走行 D」）。下の表には**走行 A・B・C** の値を、どの走行の値かを明記して書き分ける。**走行 D は §5.1 の専用の表**へ書く。

| 欄 | 値 |
|---|---|
| 起動コマンドの逐語 | 起動スクリプト `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\lap.ps1`（手順書 §3.2 の逐語を写したもの・本記録の §3／§4 の値を埋め込み済み）。走行 A＝`.\lap.ps1 -Run A`、走行 B＝`-Run B`、走行 C＝`-Run C`、走行 D＝`-Run D`。実体は `& $AREKA $GHOST $BALLOON 2>&1 \| Tee-Object -FilePath $LOG`。**写し元にできる前の置き場が 1 つも残っていなかった**ため（§13.3 の冒頭）、この写しは会話の記録から復元したものであり、`$LOGDIR` の 1 行だけを置き場へ向けてある。**実際に走らせたのは同じ日の 2 回目の置き場に置いた写し**（2026-09-10 22:21 に配置・3,517 バイト。1 回目の置き場の写しは 20:29:35・3,515 バイトで、差は `$LOGDIR` の 1 行だけである）。スクリプトは起動のたびに橋渡しの PE machine を読み、`014C` でなければ起動を拒む（`times.txt` の各行に `helper=014C` が残っている） |
| 採取の開始時刻 | 走行 A **2026-09-10 22:21:31**・走行 B（1 回目）**23:21:37**・走行 B（2 回目＝判定に用いる）**23:23:23**・走行 D **23:26:24**・走行 C **2026-09-11 00:11:51**（いずれも `times.txt` の `開始` 行） |
| 採取の終了時刻 | 走行 A **22:37:27**（exit 0）・走行 B 1 回目 **23:23:01**（exit 0）・走行 B 2 回目 **23:24:01**（exit 0）・走行 D **23:36:25**（exit 0）・走行 C **00:12:45**（exit 0）。**5 本とも exit 0 である**（`times.txt` の `終了` 行）。走行 B が 2 本あるのは、1 回目の起動で保存位置が 1 つも読み戻されず（下の行数の欄）、開発者がもう一度起動したためである |
| ログの保存先（絶対パス） | 置き場は `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\`（**同じ日の 2 回目**・手順書 §2.4 の 4）。走行 A `lap-run-a.log`・走行 B 1 回目 `lap-run-b-1.log`・走行 B 2 回目 `lap-run-b.log`・走行 D `lap-run-d.log`・走行 C `lap-run-c.log`。ほかに `times.txt`（走行ごとの開始・終了・行数）・`preflight.txt`（走行直前の確認）・`judge-output.txt`（§9 の機械判定の出力）・`lap.ps1` が在る。**1 回目の置き場 `…-2026-09-10\` も残してある**（中断した走行 A の生ログ 551 行・§13.3 の 6 件目）。**さらに前の置き場は 2026-09-10 の時点でディスクから消えていた**——残っているのは本記録へ写した値だけである＝§13.3 の冒頭・手順書 §9 |
| ログの行数 | 走行 A **8,031 行**・走行 B 1 回目 **3,332 行**・走行 B 2 回目 **436 行**・走行 D **5,110 行**・走行 C **5,911 行**（`times.txt` の `lines=`）。走行 B の 1 回目は、起動時に**保存位置を 1 つも読み戻していない**（`merge_scope restore scope=0 … saved_win_x=None saved_win_y=None`／`scope=1` も同じ・2026-09-10T14:21:40.616032Z と `.616092Z`）。同じ走行は `id=OnBoot` を 1 行出しており初回起動ではない（起動回数は残っていた）。読み戻しが空だった理由は生ログからは分からない——走行 A の終了（22:37）と走行 B 1 回目の起動（23:21）のあいだの 44 分に永続状態がどう変わったかは、ログの外の出来事である。開発者はもう一度起動し、**2 回目は保存位置を読み戻して位置が一致した**（`saved_win_x=Some(1912) saved_win_y=Some(904)`・14:23:23.862123Z）。項目 12 の判定はこの 2 回目で行った（§8.1 の行 12） |
| 有界の自動終了が効いて終わったか | 走行 A は**効いていない**（正しい姿）——A20 の Ctrl+左ダブルクリックで終了挨拶を通り、`event="ghost_quit"` の後に窓が自分で閉じた（22:37:27・§7 と §8.1 の行 13）。走行 B は 2 本とも開発者が閉じた（各 `ghost_quit` 1 行・`force_quit` 0 行）。**走行 D だけが有界の自動終了で終わった**（10 分・23:36:25。`event="force_quit" reason="user"` 1 行がその印で、これは走行 D の正規の終わり方である＝手順書 §5.4 の手順 7）。走行 C も終了挨拶を通って終わった（`ghost_quit` 1 行・`force_quit` 0 行） |
| 実測を目的とする他の仕様を並走させていないことの確認（R5.9） | 2026-09-10 の準備の時点: ⑴ **20:30 の確認で `cargo.exe` が 2 個動いていた**。`Get-CimInstance Win32_Process` で中身と親をたどると `cargo test --workspace --no-fail-fast`（20:27:00 開始）で、回しているのは**別のリポジトリ `C:\home\maz\git\evernote_export` の別セッション**である。areka のワークスペースではないので 32bit の橋渡しを上書きする形には当たらず、複写の直後に読んだ PE machine `014C` も変わっていないが、同じ機械の負荷にはなる。**止めていない**（自分が起こしたと確認できないプロセスは止めない）。⑵ 同時刻の CPU 上位は、その別リポジトリのテスト実行体（822 秒）・`msedge`・`claude`・`OneDrive.Sync.Service`（421 秒）である。⑶ `git worktree list` は 5 本——`C:/home/maz/git/areka`（`main`）・本作業ツリー（`6cbf7c59`）・`areka-p0-cursor-tag-canon-c24d2c`・`areka-p0-ukadoc-survey-33754d`・`sakura-bare-tag-lexer-bbdf8c`。いずれも文書調査か完了済みで、**この作業ツリーで実測を目的とする仕様は 1 本も並走していない**。⑷ **走行の前に重い別処理を止めてから起動すること**（手順書 §0.2 の 4）——描画の所要は機械の負荷でも動く量で、2026-09-07 の走行ではそれが「ガクガク」として目に見えた（§13.2 の行 9）。止めた後の `Get-Process` の CPU 上位をこの欄へ書き足し、**走行を始める直前と走行 D の直前に `Get-Process cargo` で 0 個**と PE machine `014C` をもう一度確かめる。走行時の確認結果: **⑴ 走行の直前（2026-09-10 22:21:19・`preflight.txt`）**——`cargo` のプロセス **0 個**・橋渡しの PE machine **`014C`**・`profile\areka` は**無い**・置き場には `lap.ps1` だけ・機械の CPU 負荷 **4%**・HEAD `434a4a82`（実行体は `6cbf7c59`）。CPU 上位 8 は `claude` 4 プロセス・`OneDrive.Sync.Service`・`msedge` 2・`SshRegister` で、ビルドやテストは動いていなかった。**⑵ SSP を隣で起動したまま走らせた**（開発者の指示・CPU 0.3% 程度）。**⑶ 走行 A の途中で別リポジトリ `C:\home\maz\git\evernote_export` の `cargo test` が走り出し**、22:22:30〜22:25:00 のあいだ 5〜6 コアを使って機械の CPU は 85〜97% になった（2026-09-11 00:17 の確認の起動でも同じ負荷が掛かっていた）。areka のワークスペースではないので橋渡しは無事だが、**描画の所要は機械の負荷でも動く**（§10 の起動挨拶の行・§13.2 の行 9）。**止めていない**（自分が起こしたと確認できないプロセスは止めない）。**⑷ 走行 D の直前の `Get-Process cargo` は記録に残っていない**（手順書 §0.2 の 4 が求める 2 回目の確認は、`preflight.txt` に走行の直前ぶんしか無い）。機械判定（§9）は走行 D が終わった後に回しており、走行中に `cargo` を起こしてはいない |

> **ゴーストの永続状態の消去（手順書 §2.4 の 2）**。走行 A を初回起動にするため、`crates\pilot\examples\shiori-host-32\fixtures\emo2\ghost\master\profile\areka\` を **2026-09-10 20:30:07 に消した**（前回〔2026-09-07 19:39:02〕の消去の後、中断した走行 A と決定論テストの走行がこのフォルダを作り直していた）。消す前に在ったのは `sylphya.toml` の 1 ファイルだけで、中身は起動回数（`[boot] count = "1"`）である。消した後に同フォルダが無いことと、隣の `profile\pasta\`（消さない対象）がそのまま在ることを確認した。**走行の直前にもう一度この 2 点を確かめること**——別セッションのテストがフォルダを作り直すと走行 A が初回起動にならない。このフォルダは版管理の対象外（`crates/pilot/examples/shiori-host-32/.gitignore:3`）であり、消去の前後で `git status --porcelain` は本記録・手順書・`tasks.md` の編集しか出さない。**走行 A と走行 B のあいだでは消さない**（項目 12 の後半が観測できなくなる）。

### 5.1 走行 D（拡大率遷移の専用採取）の同定

走行 D は **§9 機械判定**の入力になる生ログを採るためだけの短い専用採取である（手順書 §5.4）。**走行 B の後・走行 C の前**に、新しいプロセスとして行う。手順書 §2.4 の永続状態は消さない。

| 欄 | 値 |
|---|---|
| 起動コマンドの逐語（走行 A との差は `$LOG` と有界の自動終了の値だけである旨も書く） | `.\lap.ps1 -Run D`（同じスクリプト・走行 A との差は `$LOG`＝`lap-run-d.log` と `AREKA_APP_SMOKE_EXIT_MS=600000` の 2 点だけ。`RUST_LOG` と 3 つの絶対パスは同一） |
| ログの保存先（絶対パス・`lap-run-d.log`） | `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\lap-run-d.log` |
| ログの行数 | **5,110 行**（`times.txt` の `lines=5110`） |
| 切り替えた 2 つの拡大率水準（一方は §2 実機構成の 96 dpi でない水準） | 主モニタの **192 dpi（200%）↔ 144 dpi（150%）**。どちらも 96 dpi ではない（§2）。判定の出力も 8 遷移すべてを `dpi 192 -> 144` か `dpi 144 -> 192` と読んでいる（§9） |
| 往復の回数（低→高 何回・高→低 何回。**各 3 回以上**） | **低→高（144→192）4 回・高→低（192→144）4 回・計 8 遷移**（§9 の `transitions=8`。遷移 #1／#3／#5／#7 が 192→144、#2／#4／#6／#8 が 144→192）。**各方向 3 回以上**を満たす |
| **走行中にゴースト窓へ一切触れていないこと**（`ATOM-NO-DRAG` を代替する採取者の宣言・手順書 §5.4 の手順 8） | **触れていない**（採取者＝開発者の宣言）。採取中はゴースト窓を掴まず、ゴーストの上で左ボタンも押していない。拡大率の変更は OS の設定から行った。裏づけとして、掴みの準備に入った印 `[start_preparing]` は生ログに **0 行**である（§7） |
| **有界の自動終了で終えたこと**（自分で窓を閉じていないこと） | **終えた**。10 分の有界自動終了（`AREKA_APP_SMOKE_EXIT_MS=600000`）で 23:36:25 に終わっており、自分では閉じていない。印は `event="force_quit" reason="user"` の 1 行（2026-09-10T14:36:24.856669Z）で、これは走行 D の**正規の終わり方**である（手順書 §5.4 の手順 7）。同じ走行に `event="ghost_quit"` は 0 行、`event="unload_clean"` は 1 行である |
| 採取の開始時刻 | **2026-09-10 23:26:24** |
| 採取の終了時刻 | **2026-09-10 23:36:25**（exit 0） |
| 採り直した場合はその回数と理由（同じログへ追記していないこと） | **採り直していない**（1 回の採取で `ATOM-QUOTA` の各方向 3 回以上を満たした）。同じログへの追記も無い——`lap-run-d.log` はこの 1 回だけで作られている |

> 切替 1 回ごとの目視所見（向き・跳ねの有無・二体の隙間の有無・接地点の浮きの有無）は **§10 目視所見**の「拡大率の切替」の行へ、**遷移ごとの内訳**として書く。

---

## 6. 宣言（**結果を見る前に**記入する・R5.6）

走行を始める前に埋める欄である。走行の後にこの節を書き足したり書き換えたりしない。書き換えた場合は、書き換えたこと自体を §13.3 へ記す。

**狙う操作の順序**: 本手順書 §5 の順序どおり——走行 A は A1〜A20（項目 1→15→2→11→10→3→4→18 前半→16→19→18 後半→5→6→7 前半→8→9→14→17→12 前半→13）、走行 B は B1（項目 12 後半）、走行 D は拡大率を 2 水準で 3 往復（触らない）、走行 C は C1（項目 7 後半）。撫でる順（A13）は **本体の頭 → 本体の胸 → 相方の頭 → 相方の胸**。外れる箇所は無し。（下書き: 2026-09-05 08:20・走行前・準備者が手順書から写した。開発者は走行前に読み、自分の言葉と違えば直してから起動する）

**項目ごとの期待**: 期待の本体は design.md の適合検証項目表にある。ここには「その項目で自分が何を見るつもりか」を、走行前に自分の言葉で書く。

| # | 項目 | 走行前の宣言 |
|---|---|---|
| 1 | 起動と既定位置 | 起動しただけで本体が画面右下・相方がその左に出る。最初の面が届くまで何も描かれず、届いた後は実際の絵が出る。拡大率が 100% でないモニタでも窓が画面外へ出たり潰れたりしない |
| 2 | 起動挨拶の再生 | 挨拶が 1 文字ずつ流れ、待ち・改行・表情の切替が台本の順で起きる。文字が飛んだり順番が入れ替わったりしない |
| 3 | 着せ替えの表情 | 挨拶の途中で本体の表情が変わり、変わった後にその表情が貼り付いたまま残らない（次の指示で戻る） |
| 4 | まばたき 2 系統 | 会話が終わって放置していると、本体も相方もそれぞれまばたきする（本体は着せ替え込み） |
| 5 | 放置で自発会話・会話中は割り込まない | 何もしないでいると自発会話が始まる。その会話の最中に別の会話が割り込んで来ない |
| 6 | 撫で反応（本体側と相方側の両方） | 本体の頭・胸、相方の頭・胸の順に撫でると、それぞれ反応が返る。生ログの照会に当たり領域（Head／Bust）と話者（0／1）が載る |
| 7 | 二重クリックでメニュー | 二重クリックでメニューが出て、選択肢が字下げされて並ぶ。見た目の反転は走行 C で見る |
| 8 | 選択によるシーン遷移の一周 | 選択 → サブメニュー → もどる → 位置調整と辿れて途中で止まらない。生ログには選択肢と同名のイベント 1 段だけが出て、正典の選択確定イベントは出ない |
| 9 | 位置調整 | メニューの位置調整で相方が動く。動く量は拡大率倍（150% なら台本の 1.5 倍）で、生ログの移動量で確かめる |
| 10 | 二人立ち総合 | 相方の窓と相方用バルーンが出る。話者が本体と相方で替わる。別名指定で相方の表情が変わる。両方のバルーンがそれぞれのキャラに付いて動く |
| 11 | 利用者名の展開 | 挨拶の文面に利用者名が入っていて、`%username` のような生の記法が漏れていない |
| 12 | 位置の永続化 | A18 で動かした位置を控えておく。走行 B で起動すると同じ位置に戻る。初回起動限定の位置調整（A16）は走行 B では既定位置へ戻る——これを許容仕様として最終確定する |
| 13 | 終了 | 自分で閉じると、終了挨拶 → 終了 → 解放の順に進み、解放が 1 度だけ起きる。挨拶が途中で切れない。生ログに解放完了の行が 1 行 |
| 14 | 省略した機能の縮退 | 更新系とバルーン変更のイベントを送らなくても何も壊れない。メニューを出したまま放置すると時間切れで裁定どおりに閉じる。矢印・目印・通信系のバルーン資産が無くても壊れない |
| 15 | バルーンの表示ライフサイクル | 起動直後はバルーンが見えない。会話が始まると現れ、終わってしばらくして消える |
| 16 | 拡大率の切替 | 表示倍率を変えると、キャラの足元が新しい作業領域の下端に載り、バルーンが同時に付いて動く。窓が 1 枚ずつ順にずれていく形は無い。絵が先に大きくなって窓が 0.2〜0.3 秒遅れて動く跳ねは**登記済みの症状として見るだけ**で合否に載せない。機械判定は走行 D で別に採る |
| 17 | 掴んで動かしたときの追従 | 本体・相方を掴んで動かすと、窓がカーソルに 1 対 1 で付いてくる（遅れやずれが無い）。動かした先の位置を控える |
| 18 | 二体の隣接 | 何もしていない状態で二体が隙間 0 で隣り合う。拡大率を変えた後も一度だけ並び直して隙間 0 に戻る。動かした側はこの対象外 |
| 19 | 再表示直後の重なり順 | 拡大率を変えた直後（描き直しの直後）も、バルーンがキャラの手前に居続ける |
| 20 | 子プロセスへの受け渡し | 3 点で見る——生ログに接続失敗の行が 0 行、実 SHIORI 由来の挨拶が画面に出る（目視）、正規の解放完了の行が 1 行 |

> 上の 20 行は **2026-09-05 08:20（走行前）** に準備者が設計の項目表から「何を見るつもりか」へ言い換えた下書きである。開発者は走行前に読み、自分の言葉と違う行があれば直してから起動する。走行後に書き換えた場合は §13.3 へ記す。

### 追記（第 1〜4 回・2026-09-06〜2026-09-07・タスク 7.1 で 1 行ずつに圧縮）

> 第 1〜4 回の追記は、いずれも**採り直しの走行を始める前**に書かれ、走行の後には触っていない。1,000 行の見張りに収めるためタスク 7.1（2026-09-11）で 1 回 1 行へ圧縮した——事実は 1 つも落としていない。追 3・追 4 は第 2 回で追 3′・追 4′ に差し替わっており、採り直しで読むのは 追 3′・追 4′ である。

- **第 1 回（2026-09-06 19:52・採り直しの前）**——上の 20 行は 1 行も書き換えていない（§13.3 の約束）。上流 spec `areka-P0-emo-text-line-height-canon` が完了して本ブランチへ入った（PR #142・main `e3291fc1`・merge `3c2908e`）ため、準備者が引き渡し文書（`.kiro/specs/completed/areka-P0-emo-text-line-height-canon/verification/handoff.md`）から**新たに期待するものだけ**を 4 行書き起こした（開発者は走行前に読み、自分の言葉と違う行があれば直してから起動する）。**追 1**（項目 7 前半・A14）＝相方側バルーンのメニューで先頭の「おしゃべり頻度」を含む 3 つの選択肢がすべて見え、字下げされた「閉じる」の右端が欠けていない（走行 A 2026-09-05 はこの 2 点が成立せず中断＝§13.2 の #1・#2。採り直しで最初に見るのはここ）。**追 2**（項目 2・項目 15）＝本体側バルーンは**最大 4 行**が見えてから送られる（以前の前提は 3 行）。収まる行数が増えたぶん送りの起き方が走行 A と違って見えることがあるが、**項目 2 の期待そのもの（1 文字ずつ流れ、待ちと改行と表情の切替が台本どおり）は変わらない**。**追 3**（項目 7 後半・C1・→ 第 2 回で 追 3′ へ差し替え）＝反転帯から文字のインクが下へはみ出すのは 2 画素までが正常で、3 画素以上も不合格とはせず画素数を添えて再裁定を求める（§13.1 の行 5）。**追 4**（項目 14・→ 第 2 回で 追 4′ へ差し替え）＝相方側バルーンの読み込み時に「折返し基準が描画範囲の外に解決された」警告が 1 件出るのが正常で、バルーン定義側の粗さを知らせる記録ゆえ項目 14 の不合格理由にしない（手順書 §5.7）。
- **第 2 回（2026-09-06 22:08・直した後の採り直しの前）**——上の 20 行も 追 1〜追 4 の 4 行も 1 行も書き換えていない。走行 A（2026-09-06 20:41〜20:48）で開発者が 2 つの症状を許容できないと裁定し、本仕様の中で直した（§13.2 の行 5・行 6、改変の台帳は §13.4）。準備者が要件の「改訂（2026-09-06・第 2 回）」の 2 と 4、および Requirement 13・14 から**期待が変わった 2 行だけ**を書き起こした。**追 3′**（追 3 に代わる・項目 7 後半・C1）＝反転帯から文字のインクが下へはみ出すのは **0 画素**であり、**1 画素でも見えたら不合格**として画素数を §13.2 の行 5 へ書く。帯そのものは正典 `font.height,28` で **4 画素下がる**（帯 y4..33・文字 y7..31・上の余白 3・下のはみ出し 0）。追 3 が拠っていた上流の「2 画素まで正常」は**覆された**（§13.1 の行 5・R7.7 廃止）。**追 4′**（追 4 に代わる・項目 14）＝「折返し基準が描画範囲の外に解決された」の警告は相方側バルーンで**装着ごとに 1 件**（本体側バルーンは 0 件＝折返し基準が描画範囲の内に収まるため）。**2 行以上出たら退行**として項目 14 の不合格に数え、1 件そのものは症状ではないので不合格理由にしない（手順書 §5.7）。
- **第 3 回（2026-09-07 19:39・終了指示を直した後の採り直しの前）**——上の 20 行も 追 1〜追 4 も 追 3′・追 4′ も 1 行も書き換えていない。走行 A（2026-09-07 18:30〜18:33）で項目 13「終了」が終了挨拶を通らず即座に終わることが分かり、本仕様の中で直した（§13.2 の行 7、台帳は §13.4 の改変 3）。準備者が要件の「改訂（2026-09-07・第 3 回）」の 2・3 と Requirement 15 から**期待が変わった 1 行だけ**を書き起こした。**追 5**（項目 13・A20。上の 20 行の「13 終了」の行に代わる）＝**Ctrl+左ダブルクリックを 1 回**すると、まず**終了挨拶がバルーンに流れ**、挨拶が終わってから**窓が自分で閉じる**。押した瞬間に窓が消えるのは不合格。生ログには `method=GET id=OnClose`・`event="close_talk_start"`・`event="ghost_quit"`・`event="unload_clean"` が**それぞれ 1 行**並び、`event="force_quit"` は **0 行**（0 行の読み方は §7 の点灯確認）。**強制退避は Ctrl+Shift+左ダブルクリック**という別の操作へ分かれた——起動に失敗したゴーストから抜けるための口であり、**走行では使わない**。使ってしまったら §8.1 の行 13 は不合格とし、使った旨を §13.3 へ書く。
- **第 4 回（書き起こしは 2026-09-10 20:4x・解放の失敗を直した後の採り直しの前）**——上の 20 行も 追 1〜追 4 も 追 3′・追 4′ も 追 5 も 1 行も書き換えていない。走行 A（2026-09-07 20:01〜20:04）で終了挨拶は流れたのに**解放（Unload）が脳との通信で失敗**することが分かり、本仕様の中で直した（症状 D・§13.2 の行 8、台帳は §13.4 の改変 4）。同じ走行で見つかった**描画の重さ**は別の仕様が引き取り、本走行の合否には載せない（症状 E・§13.2 の行 9）。準備者が要件の「改訂（2026-09-07・第 4 回）」と Requirement 16.4／16.5 から**新しく見るもの 3 行**を書き起こした。**追 6**（項目 13・追 5 に足す）＝終了挨拶の後に生ログへ `event="unload_clean"` が **1 行**あり `event="unload_failed"` は **0 行**である。直す前（2026-09-07 20:01 の走行）は逆で `unload_failed` 1 行・`unload_clean` 0 行だった——挨拶は流れたのに解放が成立しておらず**項目 13 は不合格**。1 行でも `unload_failed` が出たらその走行の項目 13 は不合格として §13.2 へ書く（読み方は §7・手順書 §5.7）。**追 7**（走行の前の段取り・項目の期待ではない）＝起動の前にこの機械で走っている**重い別処理**（別プロジェクトのビルドやテスト・クラウドの同期）を止め（手順書 §0.2 の 4）、止めた後の `Get-Process` の CPU 上位を記録 §5 の並走の欄へ写す。描画の所要は機械の負荷でも動く量なので、負荷の状態を書き残しておかないと後から読めない。**追 8**（項目 16 の周辺の計測・合否には載せない）＝走行 A の生ログで `perf(apply_show)` の `t_total_us`（1 コマ適用の総所要）の中央値を数えて記録 §7 へ写す（数え方は手順書 §5.7）。2026-09-07 20:01 の走行は **78 ms**・その前の走行は 41 ms だった。**この値は合否に載せない**——絵が変わるコマ 1 回の費用が 61〜84 ms かかること自体は別の仕様が引き取っている（§13.2 の行 9）。

### 追記（2026-09-07・第 5 回・送りの起点を直した後の採り直しの前）

**上の 20 行も、追 1〜追 4 の 4 行も、追 3′・追 4′ の 2 行も、追 5 の 1 行も、追 6〜追 8 の 3 行も、1 行も書き換えていない。** 2026-09-07 の診断のための走行と、それに続けて始めた走行 A（20:22〜20:27）で、**相方側バルーンが最新の 1 行しか見せない**ことが分かり、本仕様の中で直した（症状 F・§13.2 の行 11、改変の台帳は §13.4 の改変 5）。新しく見るもの 2 行だけをここへ足す。この追記も**採り直しの走行を始める前**に書いたものであり、走行の後には触らない。

| # | どの宣言行に掛かるか | 採り直しで新たに見ること |
|---|---|---|
| 追 9 | **項目 2**（起動挨拶の再生・走行 A の A3）。上の 20 行の「2 起動挨拶の再生」に足す | 相方側バルーンで、**台詞の前半が最新の台詞の上に残って見える**。起動挨拶の 4 往復目では「僕はエモ。クール系の可愛い娘。」（幅 216 の描画範囲では 2 行に折り返す）が「イイジャン！…」の**上に見えている**のが正しい。最新の 1 行だけになっていたら止めて、記録 §13.2 と §13.3 へ書く（2026-09-07 20:27 の走行はここで止めた＝§13.3 の中断 5 件目） |
| 追 10 | **項目 2**（同上）。追 9 に添える読み方 | 相方側バルーンの**一番上に 1.5 行ぶんの空きが見えてよい**。ゴーストの台本が話者交替のたびに 1.5 行ぶんの改行を出し、それがトークの最初は**まだ空の相方側**へ落ちるためで、正典どおりの見え方である（§13.2 の行 13）。**3 行目が来るまでは見えていてよく、空きが見えること自体を不合格の理由にしない。** 3 行目が来て収まらなくなったら、この空きから先に送られて消える |

> この 2 行は **2026-09-10（送りの起点を直した後の採り直しの走行前）** に準備者が要件の「改訂（2026-09-07・第 5 回）」の 6・7 と Requirement 17.5 から書き起こした。開発者は走行前に読み、自分の言葉と違えば直してから起動する。

**開発者の手元作業の記録（走行の前・2026-09-10 22:2x）**: 開発者は上の宣言 20 行と 追 1〜追 10 を走行の前に読み、**1 行も書き換えなかった**（＝自分の言葉と違う行が無かった）。実拡大率は主モニタ 200%（96 dpi でない水準・§2）に設定済みであった。この 1 段は走行の前に書いた宣言ではなく、**開発者が手元作業を済ませた事実の記録**であり、上の宣言と追記は 1 文字も触っていない（§13.3 の約束）。

---

## 7. 点灯確認（R5.7）

判定に使う観測点が、この走行で実際に出力を出していたことを先に確かめる。**消灯している観測点の沈黙は「起きなかったこと」の根拠にならない。**

| 観測点（用途） | 点灯を確かめた方法 | 点灯していたか | 消灯時に代替した 1 語と、その旨の記載 |
|---|---|---|---|
| `event=connect_failed`（項目 20 ⑴ 接続失敗の印・**期待値は 0 行**） | 5 本の生ログ全体をこの語で数える（出力側は `crates/areka-kanade/src/shiori/real.rs:275-280` の `error!`・target `shiori-actor`。`RUST_LOG` の先頭が `info` なので §3.2 の水準で点いている） | **走行 A 0 行／走行 B 1 回目 0／走行 B 2 回目 0／走行 D 0／走行 C 0**。点灯の裏づけは、同じファイル・同じ target の `event=unload_clean`（同 `:204-208` の `info!`）が **5 本とも 1 行ずつ**出ていること——沈黙ではなく、点いている観測点が 0 行を返したという読みである | 代替していない（点灯している） |
| `method=GET id=OnClose`／`event="close_talk_start"`／`event=unload_clean`（項目 13 の残り 3 語・**期待値は各 1 行**） | 走行 A の生ログ全体を 3 語で数える（出所は手順書 §5.7 の語の表——順に kanade の SHIORI 送出点の `trace!`〔`kanade=trace` が要る〕・`crates/areka-kanade/src/schedule/close.rs:66` の `info!`・`crates/areka-kanade/src/shiori/real.rs:204-208` の `info!`） | **走行 A は 3 語とも 1 行ずつ**（22:37:06.652366 の GET → `.666166` の `close_talk_start` → 22:37:27.019568 の `unload_clean`。時刻は UTC の `13:37:…` を日本時間へ直したもの）。走行 B は 2 本とも同じ 3 語が 1 行ずつ、走行 C も 1 行ずつ。走行 D は自動終了なので GET と `close_talk_start` は 0 行・`unload_clean` だけ 1 行 | 代替していない |
| `[transition]`（項目 16 の機械判定の入力・**走行 D の点灯確認**＝手順書 §5.4 の手順 4）と `整合待ちの札がある窓へ窓書込が到達した`（`ATOM-HOLD-BREACH` に当たる語・**ブロックの欄ではない**） | 走行 D の生ログを 2 語で数える（出力側は `crates/wintf/src/ecs/window/transition_diag.rs:57`・target は同 `:54` の `wintf::transition`／`crates/areka/src/placement/follow/window_move.rs:691` の `warn!`） | `[transition]` は**走行 D で 581 行**（走行 A 1,053・B 1 回目 747・B 2 回目 60・C 104）＝点いている。`ATOM-HOLD-BREACH` の語は**走行 D で 0 行**（判定の出力の `holds=0` と一致する）。**この語はブロックへ足さない**（R6.1・手順書 §5.4 の読み替え 8） | 代替していない |
| `event=boot_input_ignored`（起動系列に無関係な入力を捨てた印・**2026-09-06 追加**） | 走行 A の生ログ全体を `event=boot_input_ignored` で数える（出力側は `crates/areka-kanade/src/schedule/boot.rs:34` の `warn!`・`RUST_LOG` の先頭が `info` なので §3.2 の水準で点いている） | **走行 A 0 行**（期待どおり）。走行 B 1 回目 0／2 回目 0／走行 D 0／走行 C 0。よって §13.2 の行 4 の欠陥は**本走行で発現していない**——沈黙ではなく、点いている観測点の 0 行として読む（§0.4）。同じ走行で終了の握手が最後まで通っている（`ghost_quit` 1 行）ことが、欠陥が起きていないことと突き合う | 代替していない |
| `折返し基準が描画範囲の外に解決された`（バルーン定義の粗さの警告・**2026-09-06 第 2 回改訂で追加**） | 走行 A の生ログ全体をこの語で数える（出力側は `crates/areka-emo-text/src/actor.rs:199-211` の `warn_coarse_wrap_threshold`——`warn!` は同 `:204-210`・文言そのものは同 `:209`・欄は同 `:205-208` の `balloon`／`axis`／`wrap_threshold`／`inline_limit`。呼ぶのは `TextLayerRuntime::register_actor`〔同 `:327`〕で、装着 1 回につき 1 度だけ通る。**2026-09-06 のタスク 6.7 まで出所は `region.rs` の `TextRegion::resolve` の中に在り、毎フレーム出ていた**） | **5 本とも 1 行ずつ**（走行 A 1／B 1 回目 1／B 2 回目 1／D 1／C 1）＝期待どおり**装着ごとに 1 件**である（相方側バルーン 1 件・本体側 0 件）。逐語は走行 D の 2026-09-10T14:26:24.984827Z `WARN actor{actor=emo-text}: areka_emo_text::actor: 折返し基準が描画範囲の外に解決された——実効の折返し位置は描画範囲の辺になる（バルーン定義側の粗さ） balloon="(名前なし)" …`。**2 行以上なら退行**という読み（手順書 §5.7）に照らして退行は無い。直す前の走行（2026-09-06 1 回目）は 30,837 行のうち 27,908 行がこの 1 行だった | 代替していない |
| `event="force_quit"`（終了指示が正規の握手を通らず強制終了系列へ直行した印・**2026-09-07 第 3 回改訂で追加**） | 走行 A の生ログ全体を `force_quit` で数える（出力側は `crates/areka-kanade/src/schedule/mod.rs` の `force_quit` 関数の `warn!`——文言は「強制終了指示——終了系列（Forced）へ直行」・欄は `event` と `reason`・target `kanade`。`RUST_LOG` の先頭が `info` なので §3.2 の水準で点いている） | **走行 A 0 行**（期待どおり＝終了指示は正規の握手を通った）。走行 B 1 回目 0／2 回目 0／走行 C 0。**走行 D だけ 1 行**（14:36:24.856669Z `reason="user"`）だが、これは**有界の自動終了の印**であり、走行 D の正規の終わり方である（§5.1・手順書 §5.4 の手順 7）。この語が点くことは走行 D の 1 行そのものが示している | 代替していない |
| `event="ghost_quit"`（kanade の終了系列の完了を受けて全ゴースト窓を閉じた印・**2026-09-07 第 3 回改訂で追加**） | 走行 A の生ログ全体を `ghost_quit` で数える（出力側は `crates/areka/src/emo2_boot/frame.rs` の `run_ghost_quit_phase` の `info!`——文言は「kanade の終了系列が完了した: 全ゴースト窓を閉じる」・欄は `event` と `cause`。同関数は相順の先頭に置かれている） | **走行 A 1 行**（期待どおり・22:37:27.024311 `cause=Quit`）。走行 B 1 回目 1／2 回目 1／走行 C 1。**走行 D は 0 行**（自動終了で終えたため・上の行と対になる） | 代替していない |
| `event="unload_failed"`（解放の要求が脳との通信で失敗した印・**2026-09-07 第 4 回改訂で追加**） | 走行 A の生ログ全体を `unload_failed` で数える（出力側は `crates/areka-kanade/src/shiori/real.rs:224` の `error!`——target `shiori-actor`・文言「正規 clean shutdown に失敗」・欄は `event` と `error`。同じ走行で対になる `event=unload_clean`〔同 `:206`〕が 1 行出ていることが、この観測点が点いていることを示す） | **5 本とも 0 行**（期待どおり）。対になる `event=unload_clean` は**5 本とも 1 行**なので、観測点が消えているのではなく 0 行を返している。2026-09-07 20:01 の走行はこれが逆（`unload_failed` 1・`unload_clean` 0）で項目 13 が成立しなかった＝§13.2 の行 8 | 代替していない |
| `perf(apply_show)` の `t_total_us`（1 コマ適用の総所要・**2026-09-07 第 4 回改訂で追加・合否には載せない**） | 走行 A の生ログの `perf(apply_show)` の行から `t_total_us=` の値を全部拾い、並べ替えて中央の値を採る（手順は手順書 §5.7。出力側は `crates/areka-emo-present/src/presenter/timing.rs` の `debug!`・文言は同 `:56` の `perf(apply_show): 段階別計時`・`RUST_LOG` の `areka_emo_present=debug` が要る） | **走行 A は 904 個・中央値 1,304 µs＝約 1.3 ms**（手順書 §5.7 の台本をそのまま回した。中央の位置は `[math]::Floor` で採っている）。参考として 2026-09-07 20:01 の走行は 78 ms・その前は 41 ms・2026-09-10 22:14 の中断した走行は**外れ（`cache_hit=false`）だけの中央値**が 306 ms（全数の中央値は 207 ms・比べるなら本走行の外れ中央値 97 ms と）だった。**この値は合否に載せない**（§13.2 の行 9・引受先は別仕様）。**中央値だけを前回と並べると読み違える**ので内訳も書く——904 個の内訳は**絵を作り直したコマ（`cache_hit=false`）が 299 個・中央値 97,086 µs（約 97 ms・最小 26 ms・最大 743 ms）**、**作り置きが効いたコマ（`cache_hit=true`）が 605 個・中央値 745 µs**である。全数の中央値が 1.3 ms なのは後者が過半を占めるためであり、**絵を作り直す 1 コマの費用は 97 ms で、2026-09-07 の 78 ms より重い**（機械の負荷は §5 の並走の欄のとおり） | 代替していない |

> **`event="force_quit"` の 0 行の読み方**（手順書 §5.7・R15.5）。この語は「終了指示が正規の握手を通らなかった」ときにだけ出る。**0 行は、点灯している観測点が 0 行を返したという読みである**——同じ走行で `event="ghost_quit"` が 1 行出ていることが、終了経路そのものは通ったことを示すので、沈黙との区別が付く。**1 行でも出たら、その走行の項目 13 は不合格**であり、押した操作（Ctrl+左ダブルクリックか、Ctrl+Shift+左ダブルクリックか）を §13.3 へ書く。

> **`event=boot_input_ignored` の 0 行の読み方**（手順書 §5.7）。0 行は「§13.2 の行 4 の欠陥が本走行で発現しなかった」ことの根拠になる——沈黙ではなく、点灯している観測点が 0 行を返したという読みである。**1 行でも出たら発現した証拠**なので、§13.2 の行 4 の「本走行では発現しない」を「発現した」へ改め、項目 13（終了）の結果と突き合わせる。

縮退: **無し**。上の 9 つの観測点はすべて点いており（0 行を書いた語はいずれも、同じ走行の対になる語が出ていることで点灯を裏づけてある）、目視の宣言で代替した語は 1 つも無い。

---

## 8. 判定

### 8.1 項目ごとの結果（20 項目）

**期待する結果・観測の層・由来ユニット・期待値の出典は design.md の適合検証項目表にのみ在る。** 本表はその表を参照する行の見出しだけを持ち、写しを作らない（R4.1）。

結果は **合格 / 不合格 / 中断** の 3 値で書く。**中断は合格にも不合格にも数えない**（§13.3・R5.11）。1 段でも不成立なら走行そのものを不合格として扱い、後段の結果で埋め合わせない（R1.6）。

| # | 項目 | 結果 | 根拠 | 根拠の種別 |
|---|---|---|---|---|
| 1 | 起動と既定位置 | **合格** | 目視: 走行 A の A1。起動しただけで本体が画面右下・相方がその左に出て、窓が画面外へ出たり潰れたりしなかった（開発者。2026-09-11 00:2x に「ここまでの目視項目はすべて確認完了」と述べた A1〜A7 の 1 つ）。ログ: 主モニタの 192 dpi から起動時の拡大率 2.0 を導き（13:21:31.147685Z）、scope ごとの既定位置の調整量を決めている（同 `.388831Z`／`.389268Z`） | 目視／ログの引用 |
| 2 | 起動挨拶の再生 | **合格** | 目視: 走行 A の A3。挨拶が 1 文字ずつ流れ、待ち・改行・表情の切替が台本の順で起きた。**相方側バルーンでは前の台詞（2 行）が最新の台詞の上に残って見えた**（§6 の 追 9 のとおり＝症状 F は再現していない）。冒頭の 1.5 行ぶんの空きは 追 10 が「見えてよい」と定めたものである。ログ: `id=OnFirstBoot` の照会が 1 行（13:21:31.838034Z）＝初回起動の挨拶が実 SHIORI から返っている | 目視／ログの引用 |
| 3 | 着せ替えの表情 | **合格** | 目視: 走行 A の A6。挨拶の途中で本体の表情が変わり、変わった後にその表情が貼り付いたまま残らなかった（開発者・A1〜A7 の 1 つ） | 目視 |
| 4 | まばたき 2 系統 | **合格** | 目視: 走行 A の A7。会話が終わって放置しているあいだ、本体も相方もそれぞれまばたきした（本体は着せ替え込み。開発者・A1〜A7 の 1 つ） | 目視 |
| 5 | 放置で自発会話・会話中は割り込まない | **合格** | 目視: 走行 A の A12。何もしないでいると自発会話が始まり、その最中に別の会話が割り込んで来なかった（開発者「A8〜A10 とあわせて問題なし」） | 目視 |
| 6 | 撫で反応（本体側と相方側の両方） | **合格** | 目視: 走行 A の A13。本体・相方とも撫でると反応が返った（開発者「問題なし」）。ログ: 撫での照会 `id=OnMouseMove` が 74 行あり、当たり領域の名が載ったのは**本体の胸（`region: Some("Bust")` scope 0）3 件と相方の頭（`region: Some("Head")` scope 1）1 件**である（13:35:24.431553Z／13:34:34.737656Z）。残りは領域名が空の照会で、カーソルが当たり領域の外を通ったものである——**本体側と相方側の両方で領域つきの反応が出ている**ので項目の条件は満たすが、狙った 4 か所のうち領域名が載ったのは 2 か所である | 目視／ログの引用 |
| 7 | 二重クリックでメニュー | **合格** | 目視（前半・走行 A の A14）: 二重クリックでメニューが出て、選択肢が字下げされて並び、**先頭の「おしゃべり頻度」を含む 3 つがすべて見え、「閉じる」の右端も欠けていなかった**。目視（後半・**走行 C＝別走行**の C1）: 選択肢の見た目が順に反転し、帯は行の中で中央に寄っていて、**反転帯から文字のインクが下へはみ出した画素は 0**、「閉じる」の右端も欠けていない（開発者「OK」）。ログ: 走行 C は反転の注入が効いている（`hover_inject` 3,696 行・環境変数は §4）。走行 A の `id=OnMouseDoubleClick` は 3 行 | 目視／ログの引用 |
| 8 | 選択によるシーン遷移の一周 | **合格** | 目視: 走行 A の A15。選択 → サブメニュー → もどる → 位置調整と辿れて途中で止まらなかった（開発者「OK」）。ログ: `choice_count=3` が 12 行・`event="choice_selected"` が 11 行で、選ばれた ID は選択肢と同名の 6 種（`Onおしゃべり頻度メニュー`・`Onしゃべくりおしゃべり`・`Onメインメニュー`・`Onエモの位置調整メニュー`・`Onエモの位置調整選択`・`Onメニュー閉じる`）である。**正典の選択確定イベント（`OnChoiceSelect`）は 0 行**＝選択肢と同名のイベントが 1 段だけ出ている | 目視／ログの引用 |
| 9 | 位置調整 | **合格** | 目視: 走行 A の A16。メニューの位置調整で相方が動いた（開発者「OK」）。ログ: 選択「調整」（13:34:47.978683Z）の 44 ms 後に `apply_move_directive: move 適用完了（scope→物理px移動） scope=1 base_scope=0 from_x=1049 … to_x=1404`（同 `.022175Z`）。同じ語は走行 A に 3 行ある。**移動量が拡大率倍であること自体は本走行の生ログからは読めない**（到達先が絶対座標で記録されるため）——その点の根拠は目視である | 目視／ログの引用 |
| 10 | 二人立ち総合 | **合格** | 目視: 走行 A の A5（A3 のあいだ）。相方の窓と相方用バルーンが出て、話者が本体と相方で替わり、別名指定で相方の表情が変わり、両方のバルーンがそれぞれのキャラに付いて動いた（開発者・A1〜A7 の 1 つ。窓を単独で動かす側は A18 のドラッグで見た＝行 17） | 目視 |
| 11 | 利用者名の展開 | **合格** | 目視: 走行 A の A4（A3 と同じ場面）。挨拶の文面に利用者名が入っており、`%username` のような生の記法は漏れていなかった（開発者・A1〜A7 の 1 つ） | 目視 |
| 12 | 位置の永続化 | **合格** | 目視: 走行 A の A19 で動かした位置を控え、**走行 B（2 回目）の起動直後に位置が一致した**（開発者「位置一致 OK」）。ログ: 走行 A は終了の直前に位置を保存し（13:37:06.577150Z `char DragEnd 保存 scope=1 … saved_x=1901 saved_y=904`）、走行 A の終わりに `ghost-shutdown: persist flush confirmed`（13:37:27.200205Z）。走行 B（2 回目）は起動時に読み戻している（14:23:23.862123Z `merge_scope restore scope=1 … saved_win_x=Some(1912) saved_win_y=Some(904)`）。**走行 B の 1 回目は読み戻しが空だった**（`saved_win_x=None`・14:21:40.616032Z／`.616092Z`）。原因は製品ではなく準備者の手順違反である——走行 A（22:37 終了）と走行 B（23:21）のあいだに、タスク 6.15 の実装とレビューがこの作業ツリーで `cargo test -p areka --bin areka` を回し（試験実行体 `target\debug\deps\areka-6913f2adddc1d0a2.exe` は 23:17:20 生成）、その決定論一周テスト（`crates/areka/src/emo2_boot/spine.rs` の `remove_dir_all(&persist_dir)`）が fixture の `profile\areka\` を消した（手順書 §0.2・§2.4 の 2 が禁じる形）。ゆえに A で保存した位置（scope 1 は `saved_x=1901`）は B の 1 回目には無く、開発者は B の 1 回目で二体を掴み直して（14:22:19〜14:22:43Z の DragEnd 保存・scope 1 の最終値 `saved_x=1912`）閉じ、B の 2 回目がその 1912 を読み戻した。1901 と 1912 の 11 px の差はこの掴み直しによる。項目 12 の根拠は**B の 1 回目（再起動・`OnBoot`）で保存した位置を B の 2 回目が読み戻した**ことであり、再起動どうしの観測として成立する。A → B の 1 回目の証跡は準備者の過失で失われた（§13.3 に中断ではなく過失として注記）。開発者がもう一度起動しており、判定はその 2 回目で行った（事実と経緯は §5 の行数の欄） | 目視／ログの引用 |
| 13 | 終了 | **合格** | 目視: 走行 A の A20。Ctrl+左ダブルクリック 1 回で**終了挨拶がバルーンに流れ、挨拶が終わってから窓が自分で閉じた**（開発者）。ログ: `method=GET id=OnClose`（13:37:06.652366Z）→ `event="close_talk_start"`（`.666166Z`）→ `event="talk_done_quit"`（13:37:26.924959Z）→ `event="unload_clean"`（`27.019568Z`）→ `event="ghost_quit" cause=Quit`（`27.024311Z`）が**各 1 行**、`event="force_quit"` と `event="unload_failed"` は **0 行**（§7）。**挨拶が終わってから窓が閉じるまでは 0.1 秒**である。開発者は「終了挨拶が流れた後、窓が閉じるまで 10 秒以上待った」とも述べているが、その待ちは**台本の再生がまだ終わっていない時間**である——文字が出終わった後にゴーストの台本が 15 秒の待ち（`\w[300]`）を置いており、再生の開始から完了までが 20.3 秒（13:37:06.666166Z → 13:37:26.924959Z）かかっている＝§13.2 の行 12 の症状 G。引受先は上流であり、**areka 側の項目 13 は成立している**（再生の完了から窓が閉じるまでは 0.1 秒）。**日付つきの注記（走行ではない）**: この走行の後にゴースト資産 `pasta.dll` を上流の新しいビルドへ差し替え（コミット `1e90c743`）、areka 側も括弧形の読みを撤去した（`86d050de`）。2026-09-11 00:17:35〜00:18:09 に置き場 `emo2-close-check-2026-09-11` で確かめの起動を 1 度行ったところ、終了挨拶の終わり（15:18:08.131396Z `talk_done_quit`）から `ghost_quit`（`.761531Z`）まで **0.6 秒**で、15 秒の待ちは消えていた。**この起動は本走行の一部ではない**ので判定には用いない | 目視／ログの引用 |
| 14 | 省略した機能の縮退 | **合格** | 目視: 走行 A の A17。メニューを開いたまま放置すると選択待ちが時間切れで閉じ、矢印・目印・通信系のバルーン資産が無くても壊れなかった（開発者「OK」）。ログ: `event="choice_timeout_fired"` の直後に `method=GET id=OnChoiceTimeout` が 1 行（13:35:55.883419Z／`.883592Z`）。選択待ちの開始（13:35:25.367181Z `event="choice_waiting_established" talk_id=30 choice_count=3`）から **30.5 秒**で、kanade の既定の 30 秒と合う（開発者の体感は「2 分ほど」であった＝時間の見積りは目視では当てにならない）。「折返し基準が描画範囲の外に解決された」の警告 1 件はバルーン定義側の粗さの記録であり、**この項目の不合格理由にしない**（§6 の 追 4′・手順書 §5.7）。送っていないイベントの側は決定論層が固定しており §11 に入る | 目視／ログの引用 |
| 15 | バルーンの表示ライフサイクル | **合格** | 目視: 走行 A の A2 と、その続き。起動直後はバルーンが見えず、会話が始まると現れ、終わってしばらくして消えた（開発者・A1〜A7 の 1 つ） | 目視 |
| 16 | 拡大率の切替 | **合格** | 目視: 走行 A の A9（200% → 150% → 200%。ログの `k_old=2.0 k_new=1.5`＝13:31:49.025645Z と `k_old=1.5 k_new=2.0`＝13:32:08.728956Z）と走行 D の 8 回の切替。接地点が新しい作業領域の下端に載り、バルーンが同時に付いて動き、**窓が 1 枚ずつ順にずれていく形は無かった**。機械判定の出力（§9・走行 D の 8 遷移）: `balloon_same_frame=true(pairs=2)`・`ground_diff_max=0`・`path_a_writes=0`・`sync_stage_writes=0`・`holds=0` が 8 遷移すべてで成立し、`ATOM-QUOTA` と `ATOM-NO-DRAG` は PASS、決定論系統は既知の限界に当たる 1 件を除いて PASS である（§12）。**絵が先に大きくなって窓が遅れて動く跳ねは登記済みの症状として見るだけで合否に載せない**（§13.1 の行 1・R7.1）——開発者は 150→200 で跳ねを見ており、2026-09-10 に「DPI 切替は頻繁に起こらないため許容」と裁定した | 目視／機械判定の出力 |
| 17 | 掴んで動かしたときの追従 | **合格** | 目視: 走行 A の A18。本体・相方を掴んで動かすと窓がカーソルに 1 対 1 で付いてきた（遅れやずれ無し・開発者「OK」）。動かした先の位置は行 12 で使った | 目視 |
| 18 | 二体の隣接 | **合格（縮退あり）** | 目視: 走行 A の A8（前半・まだ何も動かしていない状態）で二体が隙間 0 で隣り合っていた。A11（後半・拡大率を変えた後）では**二体の間が開いた**。開発者は 2026-09-11 に「DPI を 200→150% にしたとき二体間の距離が開くが、**これは許容する**」と裁定した。**縮退した項目と理由**は下の欄に書いた（項目表 行 18 の「拡大率遷移の後も一度だけ解き直されて隣接に戻る」は成立していない）。ログ: 走行 D の 8 遷移はいずれも `chain_realigned=1`＝解き直し自体は 1 回だけ走っている（§9） | 目視／機械判定の出力 |
| 19 | 再表示直後の重なり順 | **合格** | 目視: 走行 A の A10（A9 の直後）。拡大率を変えて描き直された直後も、バルーンがキャラの手前に居続けた（開発者「問題なし」） | 目視 |
| 20 | 子プロセスへの受け渡し | **合格** | 3 つの証跡が同時に揃った（内訳と根拠は §8.2）——⑴ `event=connect_failed` が走行 A で **0 行**、⑵ 実 SHIORI 由来の起動挨拶が画面に出た（A3 の目視・`id=OnFirstBoot` 1 行）、⑶ `event=unload_clean` が **1 行** | ログの引用／目視 |

縮退した項目と理由（R12.5）: **1 件ある。行 18（二体の隣接）の後半**——拡大率を 200% → 150% へ変えた後、二体の間が開いたまま隣接へ戻らない。項目表 行 18 が期待する「拡大率遷移の後も一度だけ解き直されて隣接に戻る」は成立していないが、**開発者が 2026-09-11 に「これは許容する」と裁定した**ため、この 1 点を判断から外して合格としている。外した事実と理由をここに残す（理由の書けない縮退は行わない＝§0.3）。なお **20 項目の表の行は項目表と 1 対 1 で対応させるため増やしていない**——1 つの項目が 2 種以上の根拠を持つ場合は、行を分けず 1 つの欄の中で「目視:」「ログ:」と種別を書き分けた（§0.2 が言う混ぜ書きを避けるための書き方である）。

### 8.2 子プロセスへの受け渡し（項目 20・R5.8）

「1 度通った」の意味は、**引数・環境変数・作業ディレクトリの 3 経路が同時に成立したこと**である。3 つのどれが欠けても脳の読み込みが成立しないため、経路を選り分けて確かめる必要はない——**3 つの証跡が同時に揃ったことが、3 経路の同時成立そのものである**。

| 経路 | 与えた値 |
|---|---|
| 引数 | 3 本を同時に渡す——`arg1`＝親窓の番号（10 進・走行ごとに変わる実行時の値）・`arg2`＝**`C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\crates\pilot\examples\shiori-host-32\fixtures\emo2\ghost\master`**・`arg3`＝**`pasta.dll`**（`ghost\master\descript.txt` の `shiori,pasta.dll`）。渡す側は `crates/shiori-host32-host/src/process_host.rs` の `spawn`（引数 3 本・同値の環境変数 3 本・作業ディレクトリを 1 つの `Command` に組み立てる）、呼ぶ側は `crates/areka-ghost/src/shiori_wiring.rs:59` |
| 環境変数 | 同じ 3 つの値を同じ名前で二重に渡す——`HOST32_PARENT_HWND`／`HOST32_LOAD_DIR`／`HOST32_SHIORI_NAME`（名前の定義は `process_host.rs:36`／`:42`／`:48`）。引数が読めない場合の控えである |
| 作業ディレクトリ | 上の `arg2` と同じ **`…\fixtures\emo2\ghost\master`**（伺かの慣習・`process_host.rs` の `current_dir(load_dir)`） |

| # | 証跡 | 観測結果 | 根拠 | 根拠の種別 |
|---|---|---|---|---|
| ⑴ | 接続失敗のログが **0 行** | **0 行**（成立） | 走行 A の生ログ全体を `event=connect_failed` で数えて 0 行。**この観測点が点いていることは §7 で先に確かめた**（同じファイル・同じ target の `event=unload_clean` が 1 行出ている） | ログの引用 |
| ⑵ | **実 SHIORI 由来の起動挨拶**が画面に出た | **出た**（成立） | 走行 A の A3 で、開発者が起動挨拶の再生を最後まで見た（§8.1 の行 2）。台本は実 32bit の脳 `pasta.dll` が返したものであり、生ログでは `method=GET id=OnFirstBoot` の照会が 1 行（13:21:31.838034Z）出ている | 目視 |
| ⑶ | 正規の**解放完了のログが 1 行** | **1 行**（成立） | 走行 A の 13:37:27.019568Z `INFO actor{actor=shiori}: shiori-actor: 正規 clean shutdown 完了（unload → helper 正常終了 exit(0)） event="unload_clean"`。対になる `event="unload_failed"` は 0 行 | ログの引用 |

3 経路の同時成立の可否: **成立した**。3 つの証跡が同じ走行 A で同時に揃っている（⑴ 0 行・⑵ 目視と 1 行・⑶ 1 行）。3 つのどれが欠けても脳の読み込みは成立しないので、これが 3 経路の同時成立そのものである。

> ⑴ は「0 行であること」を主張する欄である。**その観測点が点灯していることを §7 で先に確かめてから書く**（消灯した観測点の沈黙は根拠にならない・R5.7）。
> 実走テストの改変は行わない（当該ファイルは編集集合の外）。

---

## 9. 機械判定

| 欄 | 値 |
|---|---|
| 入力に与えた絶対パス（**走行 D の生ログ**＝§5.1 の `lap-run-d.log`。一周＝走行 A のログではない） | `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\lap-run-d.log`（走行 D の生ログ・5,110 行） |
| 走行のコマンド | `$env:AREKA_TRANSITION_LOG = "C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\lap-run-d.log"` に続けて `cargo test -p areka transition_signoff -- --ignored --nocapture`（手順書 §6.1 の逐語・置き換えたのは絶対パスの 1 か所だけ） |
| 走行日時 | **2026-09-10 23:38**（走行 D が終わった後・走行 C の前。出力は同じ置き場の `judge-output.txt` に全文が残っている） |

**走行の出力（全文）**:

```
== transition signoff: C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\lap-run-d.log ==
[judge] records=581 transitions=8 unassigned=48 (malformed 0)
  transition #1 frame=1554 dpi 192 -> 144 records=66
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=64324 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=291345 last_write_t_us=291468 sum_call_us=4988
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=237769
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=141561
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=214103
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=99982
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 237769µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 141561µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 214103µs > 上限 16667µs（scope=1 win_kind=balloon）
      - 可視化から書込まで 99982µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 64324µs > 上限 16667µs
  transition #2 frame=2197 dpi 144 -> 192 records=66
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=147749 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=231154 last_write_t_us=231379 sum_call_us=14572
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=213083
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=198618
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=207062
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=189354
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 213083µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 198618µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 207062µs > 上限 16667µs（scope=1 win_kind=balloon）
      - 可視化から書込まで 189354µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 147749µs > 上限 16667µs
  transition #3 frame=2544 dpi 192 -> 144 records=66
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=161713 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=263165 last_write_t_us=263260 sum_call_us=9500
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=229815
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=214655
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=222741
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=207700
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 229815µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 214655µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 222741µs > 上限 16667µs（scope=1 win_kind=balloon）
      - 可視化から書込まで 207700µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 161713µs > 上限 16667µs
  transition #4 frame=2977 dpi 144 -> 192 records=66
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=147871 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=252100 last_write_t_us=252218 sum_call_us=15420
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=230262
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=216818
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=225192
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=208787
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 230262µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 216818µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 225192µs > 上限 16667µs（scope=1 win_kind=balloon）
      - 可視化から書込まで 208787µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 147871µs > 上限 16667µs
  transition #5 frame=3324 dpi 192 -> 144 records=66
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=202465 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=296146 last_write_t_us=296372 sum_call_us=19730
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=266843
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=253852
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=261145
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=246278
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 266843µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 253852µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 261145µs > 上限 16667µs（scope=1 win_kind=balloon）
      - 可視化から書込まで 246278µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 202465µs > 上限 16667µs
  transition #6 frame=3640 dpi 144 -> 192 records=68
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=136674 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=218517 last_write_t_us=218634 sum_call_us=13965
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=198560
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=183354
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=191125
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=175556
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 198560µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 183354µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 191125µs > 上限 16667µs（scope=1 win_kind=balloon）
      - 可視化から書込まで 175556µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 136674µs > 上限 16667µs
  transition #7 frame=4789 dpi 192 -> 144 records=66
    量: frames_to_last_write=0 writes=4 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=159872 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=234073 last_write_t_us=234207 sum_call_us=15022
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=213229
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=205996
    量(窓): scope=1 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=190369
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=198857
    量(見送り窓): なし
    deterministic: PASS
    signoff: 5 件の違反
      - 可視化から書込まで 213229µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 205996µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 190369µs > 上限 16667µs（scope=1 win_kind=balloon）

      - 可視化から書込まで 198857µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 159872µs > 上限 16667µs
  transition #8 frame=5390 dpi 144 -> 192 records=69
    量: frames_to_last_write=0 writes=5 path_a_writes=0 sync_stage_writes=0 balloon_same_frame=true(pairs=2) holds=0 chain_realigned=1 ground_diff_max=0 flush_total_us_max=193579 malformed=0 frames_indeterminate=false
    量(参考): first_write_t_us=245828 last_write_t_us=21257 sum_call_us=9205
    量(窓): scope=0 win_kind=balloon writes=1 mismatch_frames=0 visualize_to_write_us=224000
    量(窓): scope=0 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=215360
    量(窓): scope=1 win_kind=balloon writes=2 mismatch_frames=- visualize_to_write_us=-
    量(窓): scope=1 win_kind=char writes=1 mismatch_frames=0 visualize_to_write_us=207603
    量(見送り窓): [scope=1 win_kind=balloon]
    deterministic: 1 件の違反
      - 窓ごとの書込回数 2 > 上限 1（scope=1 win_kind=balloon）
    signoff: 4 件の違反
      - 可視化から書込まで 224000µs > 上限 16667µs（scope=0 win_kind=balloon）
      - 可視化から書込まで 215360µs > 上限 16667µs（scope=0 win_kind=char）
      - 可視化から書込まで 207603µs > 上限 16667µs（scope=1 win_kind=char）
      - 一括書込の総所要 193579µs > 上限 16667µs
```

> 環境変数の未設定・空・空白のみ、パスが読めない、観測行が 0 行——いずれも失敗として落ちる。無言の成功を作らない（R6.5）。
> 上の引用は、判定が落ちたときに標準エラーへ混ざった 3 行（`thread '…' panicked at crates\areka\src\placement\transition_signoff_tests.rs:71:5:` と、その次の行の「実機ログが上限を満たさない（上の列挙が違反の全件・決定論／実機専用の 2 系統ぶん）」、および `stack backtrace:`）と、その後ろのスタックトレース、前置きのビルドの行を省いたものである。省いた行は判定の中身を 1 文字も含まない。落ちた事実そのものは `test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1571 filtered out; finished in 7.01s` であり、**実機専用系統の上限超過（下の §12 の `SIGNOFF-BOUNDS`）によって落ちるのは想定どおりである**（§12 の読み方 4）。
> 窓ごとの書込回数の違反が現れた場合は、当該窓が見送りの一覧に居るかを先に確かめる（R6.6）。確かめた結果: **確かめた。当たっているのは判定器の既知の限界であり、製品の欠陥ではない。** 違反は遷移 #8 の `窓ごとの書込回数 2 > 上限 1（scope=1 win_kind=balloon）` 1 件で、同じ遷移の `量(見送り窓): [scope=1 win_kind=balloon]` に**当該窓がそのまま挙がっている**（手順書 §6.2 の 1〜3）。中身は、相方側バルーンが遷移の 1.8 秒前に始まった会話でいったん消えており（走行 D の 2026-09-10T14:27:09.937619Z `[balloon-visibility] バルーンの可視状態が遷移した scope=1 trigger="clear" visible=false`）、そのため frame=5390 の寸法を伴わない追従の書込（14:27:11.980755Z `origin=BalloonFollow`）と frame=5435 の寸法変更の書込（14:27:12.414707Z `origin=KeepPositionResize`）が同じ 1 遷移に入ったものである。**見送り窓を数えない規則が `writes_per_window` の検査へ広げられていない**という上流の申し送りどおりの形であり、ほかの 7 遷移は `deterministic: PASS` である。

---

## 10. 目視所見（段ごと）

| 段 | 所見 | 見た者・時刻 |
|---|---|---|
| 起動 | 本体が画面右下・相方がその左に出た。窓が画面外へ出たり潰れたりせず、最初の面が届いた後は実際の絵が出た。**SSP を隣で起動したまま走らせている**（開発者の指示・CPU 0.3% 程度）ので、同じ画面に SSP のゴーストも居る | 開発者・2026-09-10 22:21〜 |
| 起動挨拶 | 1 文字ずつ流れ、待ち・改行・表情の切替が台本の順で起きた。相方側バルーンでは前の台詞（2 行）が最新の台詞の上に残って見えた。**最初の 2〜3 トークだけ「描画とび（10 文字くらいが急にまとめて出る）」があり**、その後は「そこまでひどくない・SSP より重く感じる・たまにまばたきが遅延する」程度であった（開発者）。同じ時間帯に別リポジトリのテストが 5〜6 コアを使っている（§5 の並走の欄）。**この重さは合否に載せない**（§13.2 の行 9・引受先は別仕様） | 開発者・22:21〜22:23 ころ |
| 自発会話 | 放置していると自発会話が始まり、その最中に別の会話が割り込んで来なかった（「問題なし」） | 開発者・22:2x〜 |
| 撫で反応 | 本体・相方とも撫でると反応が返った（「問題なし」）。生ログで領域名が載ったのは本体の胸と相方の頭である（§8.1 の行 6） | 開発者・22:34〜22:35 |
| メニュー表示 | 二重クリックでメニューが出て、選択肢が字下げされて並んだ。**先頭の「おしゃべり頻度」を含む 3 つがすべて見え、「閉じる」の右端も欠けていない**。**見た目の反転は別走行の走行 C で見た**（C1・2026-09-11 00:11:51〜00:12:45）——選択肢の見た目が順に反転し、帯は行の中で中央に寄っていて、**反転帯から文字のインクが下へはみ出した画素は 0**、「閉じる」の右端も欠けていなかった（開発者「OK」） | 開発者・22:34 と 2026-09-11 00:12 |
| 選択によるシーン遷移 | 選択 → サブメニュー → もどる → 位置調整と辿れて途中で止まらなかった（「OK」）。選ばれた選択肢は §8.1 の行 8 に 6 種挙げてある | 開発者・22:34〜22:35 |
| 位置調整 | メニューの「調整」で相方が動いた（「OK」）。生ログでは選択の 44 ms 後に移動が適用されている | 開発者・22:34:48 |
| 拡大率の切替 | 走行 A の A9（200% → 150% → 200%）と走行 D の 8 回。**遷移ごとの内訳**——開発者の所見は 8 回とも同じで、⑴ 向きは低→高 4 回・高→低 4 回、⑵ **跳ねはある**（「最初のティックでウィンドウ位置が動いた後、次のティックでシェルサイズが修正される。150→200 で跳ねる」）、⑶ 二体の隙間は **200→150% で開く**（開発者「これは許容する」＝§8.1 の縮退の欄）、⑷ 接地点の浮きは無い（機械判定も 8 遷移すべて `ground_diff_max=0`）。跳ねについて開発者は「**DPI 切替は頻繁に起こらないため許容**」と裁定した（2026-09-10）。跳ねは §13.1 の行 1 の登記済みの症状であり合否に載せない | 開発者・走行 A 22:31:49／22:32:08、走行 D 23:26〜23:36 |
| 再表示直後の重なり順 | 拡大率を変えて描き直された直後も、バルーンがキャラの手前に居続けた（「問題なし」） | 開発者・22:32 ころ |
| 終了挨拶と正常終了 | Ctrl+左ダブルクリック 1 回で終了挨拶が流れ、**挨拶が終わってから窓が自分で閉じた**。ただし文字が出終わってから閉じるまで「10 秒以上待った」と見えた——その待ちは**台本の再生がまだ終わっていない時間**で、ゴーストの台本が文字の後に 15 秒の待ちを置いている（§13.2 の行 12 の症状 G・引受先は上流）。areka 側は再生の完了から 0.1 秒で閉じている | 開発者・22:37:06〜22:37:27 |

---

## 11. 合否（決定論層）

**この欄は実機層の欄とは別である。ここが緑であることは §12 を 1 文字も埋めない**（R1.3）。相 1（決定論一周テスト）の走行の証跡がそのまま入る欄であり、実機走行を待たずに埋まる。

### 11.0 基準値（編集前・タスク 2.1 で採取）

**これは合否ではなく事実の計測である**。`spine.rs` へ記録の第 2 系統を追補する前の値であり、追補後に「前」を採り直すことはできないため、編集に先立って控えた（R12.2・R12.6 の増分判定の基準）。合否の宣告はタスク 3.4 が §11 の表へ書く。

| 欄 | 値 |
|---|---|
| 採取日時 | 2026-09-03 21:02 |
| コミット | `a86edf69`（ブランチ `claude/areka-p0-emo2-conformance-7b2e56`） |
| コマンド | `cargo test -p areka --bin areka emo2_boot::spine::` |
| 結果 | `test result: ok. 19 passed; 0 failed; 0 ignored; 0 measured; 1534 filtered out; finished in 3.97s`（終了コード 0） |
| テスト本数 | 19 本（8 兄弟ファイル） |
| テスト走行時間 | 3.97 秒 |
| `cargo` 呼出全体の実時間 | 18.67 秒（ビルド確認を含む） |
| `crates/areka/src/emo2_boot/spine.rs` の行数 | 930 行（1,000 行の見張りの例外表に無い＝余白 70 行） |

**設計が引く 9.24 秒との差について**: design.md は 2026-09-02 実測の 19 本 9.24 秒を引くが、本日同一ツリーで測ると 3.97 秒であった。機械の状態（並行走行・再スキャン）が違えば秒数はこの幅で動く。R12.6 が縛るのは**増分**であるため、追補後の判定は設計の 9.24 秒ではなく本節の 3.97 秒との対で行う。

**合否（決定論層）の宣告**（タスク 3.4・根拠の全文は §11.2）

| 欄 | 値 |
|---|---|
| 走行日時 | 2026-09-04 |
| コミット | `f170fd19`（ブランチ `claude/areka-p0-emo2-conformance-7b2e56`・走行の前後とも `git status --porcelain` は本記録のほかに 1 件も出さない） |
| 走行のコマンド | `cargo test -p areka --bin areka emo2_boot::spine::`（12 回）・同コマンド ＋ `-- --skip conformance`（12 回）・`cargo test -p areka`・`cargo test -p log-capture-kit`・`cargo fmt -p areka -- --check` |
| 結果（成否） | **合格**——R12.2（非回帰）・R2.11（分量）・R12.6（時間）の 3 点をいずれも満たす。判定に用いた検証コマンドの終了コードは §11.2 ⑹ のとおり——`emo2_boot::spine::` の 12 回の走行のうち **1 回が 101**、ほかはすべて 0。**留保**: その 1 回は本仕様が新設した檻 1 本が高負荷で間欠的に赤くなったもの（§11.2 ⑺）。既存 19 本の赤ではないので非回帰の宣告は変わらないが、R9 の隔離裁定（タスク 4.2〜4.4）へ申し送る |
| 一周テストの本数と結果 | 新設 12 本（一周 2 本＋支援層の檻 10 本）。単独走行 3 回はいずれも `12 passed; 0 failed`（1.98〜2.08 秒） |
| 既存の兄弟テストの本数と結果（追補の前後で不変であること） | 19 本（`a86edf69` の 19 本と**名前が完全一致**・改名 0 件・差し替え 0 件）。24 走行すべてで 19/19 が `ok`。既存 8 ファイルは `a86edf69`↔`HEAD` で差分 0 行、期待値の緩和も 0 件（§11.2 ⑴⑵） |
| 走行時間（基準値と増分） | 基準は §11.0 の 3.97 秒（`a86edf69`・19 本）。同一セッションの対で測った増分は中央値 **+0.54 秒**・平均 **+0.86 秒**・範囲 −0.40〜+2.56 秒（11 対・§11.2 ⑸）。設計の「1 秒前後」に収まる |

### 11.1 判定が生きていることの対照（タスク 3.3）

**これは合否ではない。**〈段名・表示指令〉の照合（R2.4・R2.5）が空振りでないことの証拠であり、§11 の合否欄はタスク 3.4 が書く。ここには 2 つの走行の出力（変異で赤・復帰で緑）と、変異が跡形なく戻っていることの証跡だけを置く。

| 欄 | 値 |
|---|---|
| 実施日 | 2026-09-04 |
| ブランチ | `claude/areka-p0-emo2-conformance-7b2e56`（走行の前後とも製品コードに差分なし） |
| 対象の判定 | `crates/areka/src/emo2_boot/spine_conformance_judge.rs:102` の `assert_sequence_eq("〈段名・表示指令〉の列", …)` |
| 走行のコマンド | `cargo test -p areka --bin areka emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion` |

#### 変異 ①: 段の上限を 1 段ずらす（tasks.md 3.3 の字面どおり）——**空振りだった**

`crates/areka/src/emo2_boot/spine_conformance_script.rs:102-158`（`LAP_STAGES`）の `limit_ms` を、各段が**次の段の上限**を持つように 1 段ぶんずらした（最終段は外挿）。`begin_ms` は 1 つも触っていない。

| 段 | 変異前の `limit_ms` | 変異後の `limit_ms` | 行 |
|---|---|---|---|
| 起動 | `0` | `20_000` | `:106` |
| 装着 | `20_000` | `23_000` | `:111` |
| 自発会話 | `23_000` | `26_000` | `:116` |
| 会話中の抑止 | `26_000` | `48_000` | `:121` |
| 撫で | `48_000` | `70_000` | `:126` |
| メニュー | `70_000` | `92_000` | `:131` |
| 選択確定 | `92_000` | `114_000` | `:136` |
| サブメニューと戻り（前半） | `114_000` | `136_000` | `:141` |
| サブメニューと戻り（後半） | `136_000` | `158_000` | `:146` |
| 位置調整 | `158_000` | `180_000` | `:151` |
| 終了 | `180_000` | `200_000` | `:156` |

走行結果（終了コード 0・**緑のまま**）:

```
   Compiling areka v0.0.1 (C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\crates\areka)
    Finished `test` profile [unoptimized + debuginfo] target(s) in 6.47s
     Running unittests src\main.rs (target\debug\deps\areka-6913f2adddc1d0a2.exe)

running 1 test
test emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1564 filtered out; finished in 1.39s
```

**なぜ空振りなのか（設計の想定が実装と食い違っている）。** 〈段名・表示指令〉の列は一周で **2 行**しかなく（装着＝本体の面 0・撫で＝相方の面 10）、その 2 行がどの段名に付くかは**段の完了条件が決めており、段の上限は 1 ミリ秒も関与しない**。

- 装着段の完了条件は `!progress.collected.is_empty()` を含む（`crates/areka/src/emo2_boot/spine_conformance_lap_tests.rs:697-702`）——起動挨拶の表示指令を**採るまで段が終わらない**ので、この行の段名は構造的に「装着」に固定される。前段（起動）は注入を 1 件も行わず呼出 5 件で完了し、表示指令が出るのはその後の `run_attach_phase` 以降なので、前の段へ漏れることもない。
- 撫で段の完了条件も同じく `!progress.collected.is_empty()` を含む（同 `:782-786`）——相方の面切替を採るまで終わらない。

上限を動かして変わるのは**注入の予算**だけである。予算を削れば「段名を名指しした待ち切れ」（`StageFailure::Timeout`）に、増やせば何も変わらない。どちらも〈段名・表示指令〉の**照合**が赤になる形ではない。ゆえに設計 D3 の「段の上限を意図的に 1 段ずらすと段名の照合が赤になる」という想定は、**この実装では成立しない**。上へずらせば緑のまま（本節の実測）、下へずらせば別の失敗の形（待ち切れ）になる。

#### 変異 ②: 表示指令の宛先を 1 つずらす——**照合が赤になった**

変異 ① が判定に届かないため、**同じ列を狙う別の変異**を設計した。段の上限ではなく、**段が実際に出す表示指令の宛先**を 1 つずらす——撫での相方側の応答が指す話者を、相方（`\1`）から本体（`\0`）へ移す。

`crates/areka/src/emo2_boot/spine_conformance_script.rs:393`

```diff
-pub(super) const STROKE_KERO_TALK: &str = r"\1\s[10]……なに。\e";
+pub(super) const STROKE_KERO_TALK: &str = r"\0\s[10]……なに。\e";
```

**値をずらすのではなく regime を越える変異である。** 面番号を 1 つ足すような平行移動は下流の解決や飽和が吸収しうるが、この変異は表示指令の**届く窓そのもの**を相方のキャラ窓（`TargetId(2)`）から本体のキャラ窓（`TargetId(0)`）へ移す。副次的に、以後の `\0\s[0]` の応答が本体の面を戻す**新しい表示指令を 1 件生む**ため、列は長さも内容も変わる。期待列（`expected_display()`）は 1 文字も触っていない。

走行結果（終了コード 101・**赤**）:

```
   Compiling areka v0.0.1 (C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\crates\areka)
    Finished `test` profile [unoptimized + debuginfo] target(s) in 6.46s
     Running unittests src\main.rs (target\debug\deps\areka-6913f2adddc1d0a2.exe)

running 1 test
test emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion ... FAILED

failures:

---- emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion stdout ----

thread 'emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion' (5476) panicked at crates\areka\src\emo2_boot\spine_conformance_judge.rs:150:5:
〈段名・表示指令〉の列が期待と一致しない（等値照合・部分一致は用いない＝R2.5）
  件数: 期待 2 件・実測 3 件
  最初に食い違う位置: [1]
  [1] 期待: Some(("撫で", Show { target: 2, surface: 10 }))
       実測: Some(("撫で", Show { target: 0, surface: 10 }))
  [2] 期待: None
       実測: Some(("メニュー", Show { target: 0, surface: 0 }))

failures:
    emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1564 filtered out; finished in 2.04s

error: test failed, to rerun pass `-p areka --bin areka`
```

（上の引用はスタックトレースの行だけを省いた。省いた行は判定の呼出経路——`assert_sequence_eq` ← `judge_lap` ← 一周テスト本体——を示すもので、判定の内容を 1 文字も含まない。）

この赤が示すもの:

1. 落ちたのは**〈段名・表示指令〉の照合**そのものである（`spine_conformance_judge.rs:150` の `assert_sequence_eq`・ラベルが列名を名乗っている）。
2. **交信の列の照合はこの直前に通っている**。`judge_lap` は交信の列 → 表示指令の列 → 進行状態の列の順に照合するので、表示指令の列で落ちたことは交信の列が緑だったことを意味する——この変異を捕まえたのは表示指令の列**だけ**であり、他の列の巻き添えではない。
3. 差分は列名・両方の件数・最初に食い違う位置・食い違う各行の期待と実測を出しており、退行の形が読める。

#### 復帰（緑）

変異 ② を戻し、既存の spine 兄弟テスト全数で緑に復した（終了コード 0）。**`Compiling areka` が実際に走っていることを出力で確かめてある**——復元でファイルの更新時刻が巻き戻ると cargo が変異版の実行体を配り続けるため（tasks.md「Implementation Notes」の道具の罠）、再ビルドの実在は緑の前提である。

```
   Compiling areka v0.0.1 (C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\crates\areka)
    Finished `test` profile [unoptimized + debuginfo] target(s) in 6.77s
     Running unittests src\main.rs (target\debug\deps\areka-6913f2adddc1d0a2.exe)

running 31 tests
（31 本の個別の ok 行は省略・全数 ok）
test emo2_boot::spine::conformance_lap_tests::conformance_lap_walks_every_stage_to_its_completion ... ok

test result: ok. 31 passed; 0 failed; 0 ignored; 0 measured; 1534 filtered out; finished in 2.13s
```

| 走行 | コマンド | 終了コード | 結果 |
|---|---|---|---|
| 復帰後（spine 兄弟テスト全数） | `cargo test -p areka --bin areka emo2_boot::spine::` | 0 | `31 passed; 0 failed` |
| 復帰後（クレート全数） | `cargo test -p areka` | 0 | `1563 passed; 0 failed; 2 ignored` ほか 2 本・1 本の走行も全数 ok |
| 書式 | `cargo fmt -p areka -- --check` | 0 | 出力なし |

#### 変異が 1 バイトも残っていないことの証跡

変異の前後で SHA-256 が全ファイル一致する。`git diff --numstat -- crates/` は空、`git status --porcelain` に `crates/` 配下のファイルは 1 件も現れない。

| ファイル | 変異前の SHA-256 | 復帰後の SHA-256 |
|---|---|---|
| `crates/areka/src/emo2_boot/spine.rs` | `37705e4c9b645e6a03e725d1a74fb72adee69e7c97704d83e116a1475fc70d21` | 同左 |
| `crates/areka/src/emo2_boot/spine_conformance_script.rs` | `106fe610386a9805b1b55c76f99392d420dcc98b7ca709a53f10320edf6a5f08` | 同左 |
| `crates/areka/src/emo2_boot/spine_conformance_support.rs` | `25587518ec76c8525791822071c37ac6e63cef5c24c7cafceafa0b439b96abaa` | 同左 |
| `crates/areka/src/emo2_boot/spine_conformance_support_tests.rs` | `ca75ca3eb281deea704f22d718b61fe1f7c8e7d7a2ea691f73dd807dc896878c` | 同左 |
| `crates/areka/src/emo2_boot/spine_conformance_lap_tests.rs` | `06e599144dc6b14e9dd5946b3e30280bd15f7f7a4f1554a0e25b63e5e530c472` | 同左 |
| `crates/areka/src/emo2_boot/spine_conformance_judge.rs` | `6720a53b5657275e5a8952cef54e5531b9df309cb0e0b64ff099a05cf6e8c58c` | 同左 |

#### この対照が示すこと

〈段名・表示指令〉の照合は**空振りではない**。走行が実際に出す表示指令の宛先が 1 つでも期待と違えば、その位置を名指しして赤になる（R2.4・R2.5）。ただし本節が示したのは**この列が退行を捕まえること**までであり、列が 2 行しかないという射程の狭さ（R12.5・`expected_display()` の doc が記す事実）は本節では変わらない。

縮退: **設計 D3 が名指しする変異（段の上限を 1 段ずらす）では対照が作れず、同じ列を狙う別の変異へ差し替えた。** 理由は変異 ① の節に記したとおり、2 行の段名は段の完了条件が固定しており段の上限は関与しないためである。差し替えた変異は同じ判定（`spine_conformance_judge.rs:102`）を同じ列で赤にしているので、対照の目的（判定が生きていることを示す）は満たしている。⇒ design D3 の当該記述（「段の上限を意図的に 1 段ずらして駆動すると、段名の照合が赤になる」）は**実装と食い違うので、正本更新（タスク 7.3・D12）で引き直すこと**。

### 11.2 非回帰と分量・時間の規律（タスク 3.4）

**§11 の合否を宣告する節はここである**（R12.2・R2.11・R12.6）。§11.0 の基準値（編集前・`a86edf69`）と本節の実測を対にして判定した。宣告そのものは §11.0 の直後の「合否（決定論層）の宣告」の表に置いてある。

| 欄 | 値 |
|---|---|
| 実施日 | 2026-09-04 |
| コミット | `f170fd19`（`claude/areka-p0-emo2-conformance-7b2e56`） |
| 基準コミット | `a86edf69`（§11.0 を採った編集前のコミット） |
| 機械の状態 | **同一機で他セッションの `cargo` が並行して走っている**。各走行の直後に `cargo`／`rustc` の実プロセス数を数えたところ 2〜8 の間で動いた。秒数はこの幅で揺れる——増分は 1 本の走行の対ではなく分布で読むこと |

#### ⑴ 既存 19 本が名前ごと生き残っていること

件数の一致は改名や差し替えを隠す。よって**名前の集合**で確かめた。方法は次の 4 手（いずれも再実行できる）。

1. `cargo test -p areka --bin areka emo2_boot::spine:: -- --list` で HEAD 側の全テスト名を採る → **31 本**。
2. 名前に `conformance` を含む 12 本が本仕様の新設、含まない **19 本**が既存。
3. 基準側は `git show a86edf69:<既存の 7 テストファイル>` の中身から `#[test]` の直後の `fn` 名を抜き、モジュール名を冠して 19 本の名前を作る → **19 本**。
4. 2 と 3 を `diff` する。

**出力: 差分なし（`IDENTICAL`）。改名 0 件・差し替え 0 件・消失 0 件。**

| モジュール | 本数 | テスト名（`emo2_boot::spine::` を冠する） |
|---|---|---|
| `boot_smoke_tests` | 1 | `spine_harness_boots_scripted_ghost_and_reaches_attach_ready` |
| `display_tests` | 4 | `spine_boot_leaves_all_balloons_invisible_with_established_slot_and_surface`・`spine_s1_boot_to_display_attaches_all_targets_with_opaque_readback`・`spine_s3_balloon_face_cue_delivers_hide_then_show_in_order`・`spine_s4_balloon_free_onboot_completes_without_balloon_face_switch` |
| `move_cue_tests` | 1 | `spine_move_cue_drives_window_move_end_to_end` |
| `seriko_loop_tests` | 5 | `spine_blink_smoke_send_tick_drives_loop_pattern_command`・`spine_dpi_change_during_live_seriko_loop_keeps_loop_progressing`・`spine_e2e_kero_blink_one_cycle_golden`・`spine_e2e_sakura_blink_after_bind_one_cycle_golden`・`spine_e2e_sakura_blink_default_off_emits_nothing` |
| `settle_tests` | 4 | `settle_bounded_does_not_return_before_the_consecutive_quiet_rounds`・`settle_bounded_does_not_return_before_the_minimum_duration`・`settle_bounded_drives_the_real_clock_without_panicking`・`settle_bounded_returns_at_the_upper_bound_even_when_never_quiet` |
| `talk_close_tests` | 2 | `spine_s2_talk_drives_surface_switch_and_typewriter_reveal`・`spine_s5_close_handshake_consumes_onclose_and_joins_all_handles_bounded` |
| `text_scale_tests` | 2 | `spine_dpi_change_refreshes_balloon_text_scale_on_real_attach`・`spine_dpi_change_while_balloon_hidden_lands_on_next_show` |

**全数緑の確認**: 本節で採った 24 走行（`emo2_boot::spine::` 12 回・`-- --skip conformance` 12 回）の出力を 1 本ずつ「`test <名前> ... ok`」の逐語で照合したところ、**24 走行すべてで 19/19 が `ok`**。⑺ で述べる赤が出た走行（対 4）でも既存 19 本は 19/19 で `ok` であった。

#### ⑵ 既存の期待値を 1 つも緩めていないこと（R12.2）

**「差分なし」を書く前に pathspec が実在に解決することを証明する**（実在しないパスへ `git diff` を当てると空出力になり、「差分なし」と区別が付かない）。

| 検査 | コマンド | 出力 |
|---|---|---|
| pathspec の実在（陽性） | `git ls-files -- <既存 8 ファイル>` | **8 行**（8 ファイルすべてが解決する） |
| pathspec の空振り（陰性の対照） | `git ls-files -- crates/areka/src/emo2_boot/NO_SUCH_FILE.rs` | **0 行**（実在しなければ 0 行になることの確認） |
| 既存 8 ファイルの差分 | `git diff --numstat a86edf69 HEAD -- <既存 8 ファイル>` | **0 行**（解決する 8 本に当てて差分が無い） |
| 赤を出せる対照 | `git diff --numstat a86edf69 HEAD -- crates/areka/src/emo2_boot/spine.rs` | `32  3  crates/areka/src/emo2_boot/spine.rs`（**非空**——同じ道具が差分を検出できることの確認） |

対象の 8 ファイル＝`spine_boot_smoke_tests.rs`・`spine_display_tests.rs`・`spine_move_cue_tests.rs`・`spine_seriko_loop_tests.rs`・`spine_settle_tests.rs`・`spine_talk_close_tests.rs`・`spine_test_support.rs`・`spine_text_scale_tests.rs`。

**唯一触れた既存ファイル `spine.rs` の中身**（`git diff a86edf69 HEAD -- crates/areka/src/emo2_boot/spine.rs` の全項目）:

| 変更 | 種別 | 既存の期待値への影響 |
|---|---|---|
| 兄弟ファイルからの `use`（`RecordedStatus`・`StatusLedger`・`record_status`・`snapshot_status_calls`）の追加 | 追加 | なし |
| `ScriptedShioriBackend`／`ScriptedShioriHandle` へフィールド `status_calls` を 1 本追加 | 追加 | なし（既存フィールド `calls` は無改変） |
| `request`／`notify` の第 3 引数を `_status` → `status` へ改名し、`record_status(...)` の 1 行を先頭へ足す | 追加（書き込みのみ） | なし（返り値・既存の `calls` への push は 1 字も変わらない） |
| 取り出し口 `status_calls()` を 1 本追加 | 追加 | なし（既存の取り出し口 `calls()` は無改変） |
| 接続宣言 3 本（`conformance_lap_tests`・`conformance_script`・`conformance_support`） | 追加 | なし |

**削除も緩和も 0 件**（32 行追加・3 行削除で、削除 3 行はすべて上表の「追加のために書き直した行」＝ `_status` の 2 行と `let handle = …` の 1 行）。design D3「追補の形（挙動を変えない）」の言う「追加は書き込みのみで、既存の読み手が増えない」が実測で成立している。追補の分量は差分で **29 行**（予算 40 行以内）。

#### ⑶ 例外表に触れていないこと

| 検査 | 出力 |
|---|---|
| `git rev-parse a86edf69:crates/log-capture-kit/tests/file_length_guard_test.rs` | `d76995d89b824fad55c78121ac2e4dc9db4f599f` |
| `git rev-parse HEAD:…`（同ファイル） | `d76995d89b824fad55c78121ac2e4dc9db4f599f` |
| `git hash-object …`（作業ツリーの実体） | `d76995d89b824fad55c78121ac2e4dc9db4f599f` |
| `OVER_LIMIT_ALLOWED` の項目数（作業ツリー） | 11 件 |
| `OVER_LIMIT_ALLOWED_COUNT`（`crates/log-capture-kit/tests/file_length_guard_test.rs:109`） | `11` |

3 つの blob が同一＝**基準コミット・HEAD・作業ツリーで 1 バイトも違わない**。分割の代わりに例外表へ 1 行足して逃げた形跡は無い。

（注: 素の `sha256sum` は `git show` の出力が LF・作業ツリーの実体が CRLF のため一致しない。ここでは改行の差に影響されない git の blob ハッシュで比べている。）

#### ⑷ 分量規律（R2.11）

**新規ファイルは 3 本ではなく 5 本である。** タスク 2.3 と 3.2 がそれぞれ R2.11 の「超える場合は主題単位に分けて接続する」に従って分けたため（tasks.md「Implementation Notes」）。本節は 5 本すべてを数えた。

| ファイル | 行数 | 上限 1,000 に対する余白 |
|---|---|---|
| `crates/areka/src/emo2_boot/spine_conformance_lap_tests.rs` | 966 | 34 |
| `crates/areka/src/emo2_boot/spine_conformance_script.rs` | 717 | 283 |
| `crates/areka/src/emo2_boot/spine_conformance_support.rs` | 743 | 257 |
| `crates/areka/src/emo2_boot/spine_conformance_support_tests.rs` | 669 | 331 |
| `crates/areka/src/emo2_boot/spine_conformance_judge.rs` | 186 | 814 |
| `crates/areka/src/emo2_boot/spine.rs`（追補後の組立ファイル） | 959 | **41**（基準 930 行＋29 行） |

6 本すべてが上限を下回る。接続は `spine.rs` から 3 本（design が固定した本数のまま）、残る 2 本は `spine_conformance_support.rs:742` と `spine_conformance_lap_tests.rs:965` の末尾から。

**上限の見張りが常設で走ることの確認**: `cargo test -p log-capture-kit` は終了コード **0**。`tests/file_length_guard_test.rs` を含む 7 つのテストバイナリが走り、いずれも `0 failed`（`30 passed`／`1 passed; 2 ignored`／`6 passed`／`16 passed`／`24 passed`／`18 passed`／`4 passed`）。門（`#[ignore]`・環境変数）は付けていない。

#### ⑸ 走行時間の増分（R12.6）

**測り方**: 同一機・同一セッションで、`cargo test -p areka --bin areka emo2_boot::spine::`（新規込み 31 本）と、同じコマンドに `-- --skip conformance` を付けた走行（既存 19 本）を**交互に**12 対走らせ、`test result:` 行の `finished in` を採った。1 対だけを選ばず分布で読む。

| 対 | 新規込み 31 本 | 既存 19 本のみ | 増分 |
|---|---|---|---|
| 1 | 6.96 秒 | 4.40 秒 | +2.56 |
| 2 | 7.57 秒 | 5.08 秒 | +2.49 |
| 3 | 7.49 秒 | 4.94 秒 | +2.55 |
| 4 | **35.69 秒（赤・⑺ 参照）** | 3.95 秒 | （赤のため増分の算出から除外） |
| 5 | 3.93 秒 | 3.35 秒 | +0.58 |
| 6 | 3.26 秒 | 3.20 秒 | +0.06 |
| 7 | 3.64 秒 | 3.43 秒 | +0.21 |
| 8 | 3.27 秒 | 2.73 秒 | +0.54 |
| 9 | 2.97 秒 | 2.19 秒 | +0.78 |
| 10 | 2.80 秒 | 2.75 秒 | +0.05 |
| 11 | 2.61 秒 | 2.58 秒 | +0.03 |
| 12 | 2.52 秒 | 2.92 秒 | **−0.40** |

| 量 | 値 |
|---|---|
| 新規込み（緑 11 本）の中央値／最小／最大 | 3.27 ／ 2.52 ／ 7.57 秒 |
| 既存のみ（12 本）の中央値／最小／最大 | 3.28 ／ 2.19 ／ 5.08 秒 |
| 増分（11 対）の中央値 | **+0.54 秒** |
| 増分（11 対）の平均 | **+0.86 秒** |
| 増分の範囲 | −0.40 〜 +2.56 秒 |
| 新規 12 本だけの単独走行（3 回） | 2.01 ／ 1.98 ／ 2.08 秒 |
| 一周テスト 2 本だけの単独走行（3 回） | 2.07 ／ 3.63 ／ 2.29 秒 |

**判定: 設計の「1 秒前後」に収まる**（中央値 +0.54 秒・平均 +0.86 秒）。**ただし雑音は正直に書く**——対 1〜3 では増分が +2.5 秒に達した。同じ 3 対では既存のみの側も 4.4〜5.1 秒（後半の 2.2〜2.9 秒に対し倍近く）で、他セッションの `cargo` が重かった時間帯に両側が揃って伸びている。新規の 12 本は既存 19 本より重い（一周テストは実ゴーストの起動を伴う）ため、機械が混むほど増分も伸びる。**中央値が 1 秒を下回る一方、最悪の対では 2.5 秒だった**——これが本日の実測の全体像である。

§11.0 の基準値 3.97 秒（`a86edf69`・19 本）は本節の「既存のみ」の分布（中央値 3.28 秒・範囲 2.19〜5.08 秒）の中に収まっており、基準値の側にも機械由来の揺れがあることを裏づける。**設計が引く 9.24 秒は別の機械状態の値であり、判定には用いていない**（§11.0 の注記と同じ扱い）。

#### ⑹ 判定に用いた検証コマンド

| コマンド | 回数 | 終了コード | 結果 |
|---|---|---|---|
| `cargo test -p areka --bin areka emo2_boot::spine::` | 12 | 11 回 0 ／ 1 回 101 | 11 回 `31 passed; 0 failed`・1 回 `30 passed; 1 failed`（⑺） |
| 同上 ＋ `-- --skip conformance` | 12 | すべて 0 | すべて `19 passed; 0 failed` |
| `cargo test -p areka --bin areka emo2_boot::spine::conformance` | 3 | すべて 0 | すべて `12 passed; 0 failed` |
| `cargo test -p areka` | 1 | 0 | `1563 passed; 0 failed; 2 ignored` ほか `1 passed`・`2 passed` |
| `cargo test -p log-capture-kit` | 1 | 0 | 7 バイナリすべて `0 failed`（⑷） |
| `cargo fmt -p areka -- --check` | 1 | 0 | 出力なし |
| `git diff --numstat a86edf69 HEAD -- crates/` | 1 | 0 | 本仕様の 6 ファイルのみ（`spine.rs` ＋ 新規 5 本）。pathspec の実在は ⑵ の表で確認済み |
| `git status --porcelain` | 1 | 0 | 本記録（`verification/acceptance-record.md`）以外は 1 件も出ない |

本タスクは計測と記録だけを行い、`crates/**` を 1 行も編集していない。

#### ⑺ 観測した間欠的な赤（非回帰の判定は変えない・R9 へ申し送り）

12 回の走行のうち **1 回**（対 4）が赤くなった。**既存 19 本ではなく、本仕様が新設した檻である。**

```
test emo2_boot::spine::conformance_support::driver_tests::kanade_probe_raises_no_shiori_call_and_observes_the_close ... FAILED

panicked at crates\areka\src\emo2_boot\spine_conformance_support_tests.rs:571:5:
起動系列 5 呼出が有界内に揃わない

test result: FAILED. 30 passed; 1 failed; 0 ignored; 0 measured; 1534 filtered out; finished in 35.69s
```

- 所要 35.69 秒は `SPIN_WAIT`（30 秒）の期限切れと一致する＝ゴースト起動の飢餓。steering の「協調テストループの飢餓」と同じ形であり、tasks.md「Implementation Notes」が 18 走行中 4 回で記録した系統と**同じテスト・同じ主張・同じ行**である。
- **この赤は非回帰ではない**。落ちたのは本仕様がタスク 2.3 で新設した檻であり、`a86edf69` には存在しない。同じ走行でも既存 19 本は 19/19 が `ok`（⑴）。よって §11 の非回帰の宣告は変わらない。
- **既存 19 本の側は、本日の 24 走行では 1 本も赤くならなかった。** ただし tasks.md「Implementation Notes」は高負荷下で `boot_smoke_tests::spine_harness_boots_scripted_ghost_and_reaches_attach_ready`・`talk_close_tests::spine_s2_talk_drives_surface_switch_and_typewriter_reveal`・`talk_close_tests::spine_s5_close_handshake_consumes_onclose_and_joins_all_handles_bounded`・`text_scale_tests` の 2 本・`seriko_loop_tests` の 2 本・`display_tests::spine_s4_balloon_free_onboot_completes_without_balloon_face_switch` が赤くなることを名指しで記録している。**本節の緑は「この 24 走行では出なかった」以上のことを言わない**——既存の間欠赤が消えたという主張ではない。
- **引受先**: R9（間欠的な赤の隔離裁定）＝タスク 4.2〜4.4。design D11 ⑷ が「触らない」と裁定しているのは既存 2 本だけだが、実測される集合はそれより広く、`driver_tests::kanade_probe_raises_no_shiori_call_and_observes_the_close` は本仕様自身の檻である。`verification/isolation-decision.md` へ書き足すこと（R9.1・R9.4）。

#### 縮退（R12.5）

- **タスク 3.4 の完了条件が言う「新規 3 ファイル」は実装と食い違う。** タスク 2.3 と 3.2 が R2.11 に従って主題単位に分けたため実数は **5 ファイル**である。本節は 5 本すべてを数えた（3 本だけ数えると 2 本が検査から漏れる）。tasks.md・design.md の当該語（`design.md:145-161` の File Structure Plan・Testing Strategy の「既存テストへの影響の確認」項目 2＝`design.md:848`・タスク 3.4 の完了条件）の引き直しは**正本更新（タスク 7.3・D12）の担当**であり、本タスクは文書を書き換えていない。
- **走行時間の判定は「1 対の比較」ではなく「分布の中央値」で行った。** 同一機で他セッションの `cargo` が走っているため 1 対の差は機械の混み具合を測ってしまう。中央値・平均・範囲の 3 つを併記し、都合のよい対を選んでいないことを表で示してある（⑸）。
- **§12（実機層）は 1 文字も埋めていない。** 決定論層が合格であることは実機層を埋めない（§0.1・R1.3）。

---

## 12. 合否（実機層）

**この欄は決定論層の欄とは別である。実機層が合格であることは §11 を 1 文字も埋めない**（R1.3）。合否は人間の判断で確定する。AI 単独で宣言しない（R5.10・R10.4）。

読み分け（機械判定と目視が食い違ったときに 3 問を上から当てて 1 行に定める手順）は手順書 `verification/lap-procedure.md` に置く。ここへ書くのは**当てた結果の行番号と、7 行の合否ブロックと、そこから導いた項目 16 の合否**である。

**ブロックの読み方**（手順書 §7.1・design D7「ブロックの位置づけと FAIL の読み方」・2026-09-04 の開発者裁定）。**走行の前から決まっている規則であり、走行の結果を見てから変えない。**

1. **7 行のブロックは実機層の総合判定ではない。** 走行 D（§5.1）から採る**項目 16 の証跡**である。**実機層の総合判定は §8.1 の適合検証項目表 20 項目の実機サインオフ**であり（R10.1 ⑶・R4.1）、下の「実機層の総合判定」欄にはそちらの結果を書く。**ブロックの `ATOM-SIGNOFF` をその欄へ写さない。**
2. **項目 16 の合否は `ATOM-QUOTA`・`ATOM-NO-DRAG`・`DETERMINISTIC` の 3 欄だけで決める。** 3 欄がすべて `PASS` なら項目 16 は項目表 行 16 の条件で判定し、どれかが `FAIL` なら項目 16 は不合格である。`ATOM-QUOTA`・`ATOM-NO-DRAG` の `FAIL` は判定の前に**走行 D を採り直して**解消する（`.kiro/specs/completed/areka-P0-dpi-transition-atomicity/signoff-procedure.md:342`）。採り直したときは新しいブロックを書き、古いブロックを消さずに並べる。
3. **`SIGNOFF-BOUNDS: FAIL`・`VISUAL: 跳ねあり`・`AGREEMENT: 食い違い` の 3 つは項目 16 の不合格理由にしない。** これは §13.1 の症状 #1（**引受先なし**）が再現したときに先行仕様の 3 問が必ず 3 行目へ落とし（同 `:456`）、その行の `AGREEMENT` を `食い違い` と書かせる（同 `:463`）ためであり、独立した 3 つの兆候ではなく**症状 #1 の 1 つの現れ方**である。`AGREEMENT` はそもそも `PASS|FAIL` を採る欄ではなく分類の欄（`一致|食い違い`）である。決定論系統の違反は `DETERMINISTIC` が捕まえるので、この除外で見落としは生じない。
4. **`ATOM-SIGNOFF: FAIL` は想定どおりの結果である**（症状 #1 による。先行仕様自身がこの FAIL を抱えたまま完了している＝同 `:465`）。ブロックの文法（`ATOM-SIGNOFF: PASS` は上の 6 項目がすべて PASS ／ 跳ねなし ／ 一致のときに限る＝同 `:479`）は**1 文字も変えない**。

当てた行（1〜4 のいずれか・食い違いが無いなら「該当なし」）: **3 行目**。手順書 §6.3 の 3 つの問いを上から当てた結果である——⑴ **目視で跳ねはある**（§10 の拡大率の切替の行・開発者「150→200 で跳ねる」）ので 2 行目ではない。⑵ **決定論系統は違反を立てていない**——遷移 #8 の 1 件は当該窓が `量(見送り窓)` に挙がっており判定器の既知の限界に当たると §9 で確かめた（手順書 §6.2）ので、製品の欠陥としての違反は 0 件であり 4 行目ではない。⑶ **症状に対応する量（絵と窓のずれの時間）で違反を立てているのは実機専用系統だけ**である（`可視化から書込まで` 100〜267 ms・`一括書込の総所要` 64〜202 ms がいずれも上限 16.667 ms 超）。よって 3 行目である。
その行が指示する行動を履行したか: **履行した**。3 行目の行動は「判定器にも上限にも手を入れない／症状に対応する量を持つ系統と量の名前を記録で名指しし、その系統の未達として引受先へ渡す／あわせて『判定に載らない症状』として登記する」である。⑴ 判定器・上限値・量の名前は 1 文字も変えていない（本走行は証明に徹する・R6.2／R6.3）。⑵ 名指し——**系統は実機専用系統、量の名前は `可視化から書込まで` と `一括書込の総所要`**（上限はどちらも 16667µs＝確定値・台帳 L9）。⑶ 登記——この症状は **§13.1 の行 1**（絵が新しい寸法で描かれてから窓がその寸へ動くまでの 0.2〜0.3 秒級の遅れ・**判定に載せない**・**引受先なし**〔上流は見送りと登記で閉じた〕）に既に登記済みであり、本走行はそれが再現したことを確かめた。**引受先が無いままなので、再着手には新規の起票が要る**（R7.5・R10.6）。あわせて開発者は 2026-09-10 に「**DPI 切替は頻繁に起こらないため許容**」と裁定している。

**7 行の合否ブロック**（欄・量の名前・上限値は先行仕様の逐語をそのまま引く。R6.7）:

```
ATOM-SIGNOFF: FAIL
  ATOM-QUOTA:        PASS (transitions=8 low2high=4 high2low=4 scopes=0,1)
  ATOM-NO-DRAG:      PASS
  DETERMINISTIC:     PASS（違反 0 件・判定の出力の 1 件は §9 で既知の限界と確認）
  SIGNOFF-BOUNDS:    FAIL（違反 39 件・上限 16667µs＝確定値・台帳 L9）
  VISUAL:            跳ねあり
  AGREEMENT:         食い違い
```

**全ての欄が満たされたときにのみ合格とする。** 空欄が 1 つでも残っているブロックは合格ではない（R6.7 ＝ **7 行のすべてが記入され、未記入が 1 行も無いこと**）。**各欄の値の出どころ**——`transitions`／`low2high`／`high2low` は §9 の出力の遷移の見出し 8 本を数えたもの、`scopes` は判定器が単独の欄として刷らないので**各遷移の `量(窓)` の行から採った**（8 遷移すべてに `scope=0` と `scope=1` が揃っている）。`ATOM-NO-DRAG` は採取者の宣言による（§5.1・手順書 §5.4 の読み替え 8）。`SIGNOFF-BOUNDS` の 39 件は、遷移 #1〜#7 の各 5 件と #8 の 4 件の合計である。

項目 16 の合否（3 欄による・上の読み方 2）: **合格**。`ATOM-QUOTA`・`ATOM-NO-DRAG`・`DETERMINISTIC` の 3 欄がすべて PASS なので、項目 16 は適合検証項目表 行 16 の条件で判定する——接地点が新しい作業領域の下端に載る（`ground_diff_max=0` が 8 遷移とも）・随伴バルーンが同一フレームで追従する（`balloon_same_frame=true(pairs=2)` が 8 遷移とも）・遷移の途中で中間の矩形が提示されない（`path_a_writes=0`・`sync_stage_writes=0`）・窓が 1 枚ずつ順にずれていく形が観測されない（目視・§10）。いずれも成立しているので**合格**である。**見た目の跳ねは合否条件に含めない**（R7.1・§13.1 の行 1）。
実機層の総合判定＝**§8.1 の 20 項目の実機サインオフ**（合格 / 不合格 / 中断）: **合格**（20 項目すべてが合格。うち行 18 は開発者の裁定による縮退を 1 件含む＝§8.1 の縮退の欄）。**このブロックの `ATOM-SIGNOFF: FAIL` はここへ写していない**（読み方 1）。
判定した人間: **開発者**（2026-09-11 00:2x に「ここまでの目視項目はすべて確認完了」と述べ、A11 の隙間と A9／D3 の跳ねについてそれぞれ「許容する」「DPI 切替は頻繁に起こらないため許容」と裁定した）。AI は記録の作成のみを行った。

縮退: **1 件**（§8.1 と同じ 1 件）——行 18 の後半（拡大率遷移の後に二体が隣接へ戻らない）を、開発者の「これは許容する」という裁定によって判断から外した。ほかに縮退は無い。**判定器・上限値・量の名前・判定のコードは 1 文字も変えていない**（R6.2・R6.3・R8.1）。

---

## 13. 登記

### 13.1 判定に載せない既知の症状（R7.5）

下表の**行 1〜行 4 の 4 件**は**判定に載せない**と決まっている症状である。走行で見えても不合格の理由にせず、かつ忘れないようにここへ残す。症状・扱い・引受先は設計で確定済みであり、走行で埋めるのは**走行での確認結果**の欄だけである。

**行 5 は 2026-09-06 に追加したが、同日中に覆された**（走行 A の目視・R7.7 廃止・Requirement 13 で直した）。行そのものは履歴として残すが、**判定に載せない症状として数えない**——現在の期待は「はみ出し 0 画素・1 画素でも不合格」である。

| # | 症状 | 扱い | 引受先 | 走行での確認結果 |
|---|---|---|---|---|
| 1 | 絵が新しい寸法で描かれてから窓がその寸へ動くまでの 0.2〜0.3 秒級の遅れ | **判定に載せない**。拡大率遷移の見た目を合否条件に含めない | **別仕様 `areka-P0-dpi-transition-two-tick-bounce`**（2026-09-11 起票・タスク 6.5。ロードマップの W13 裁定枠 G＝枠 D の後に着手する。中身は「窓の位置と絵の寸法を同じ画面更新で動かす」「判定器の見送り窓を書込回数から除く」であり、上限そのものは動かさない）。**扱いは変えない——判定に載せない** | **再現した**（走行 D・2026-09-10 23:26〜23:36）。機械判定の実機専用系統が **8 遷移すべてで FAIL**——`可視化から書込まで` **100〜267 ms**・`一括書込の総所要` 64〜202 ms（上限はどちらも 16.667 ms・§9）。開発者の目視も同じものを見ている（走行 D の D3・「最初のティックでウィンドウ位置が動いた後、次のティックでシェルサイズが修正される。150→200 で跳ねる」・§10 の拡大率の切替の行）。§12 の読み分けは**この症状が対応する量を持つ系統が実機専用系統だけである**ことから 3 行目に落ち、その行の指示（判定器にも上限にも手を入れず、系統と量の名前を名指しして登記する）を履行してある。開発者は 2026-09-10 に「**DPI 切替は頻繁に起こらないため許容**」と裁定した。**扱いは変えない**——判定には載せない。変わったのは引受先だけである: 2026-09-11 に別仕様 `areka-P0-dpi-transition-two-tick-bounce` を起こしたので、「引受先なし」ではなくなった（再着手の宛先が決まっただけで、本走行の判定には引き続き載せない） |
| 2 | 話し始めの冒頭の空行 | **判定に載せない**。走行時に確認して登記する。M1 完成を妨げない | 上流のゴースト側（areka の範囲外） | **確認した**（走行 A・2026-09-10）。話し始めに相方側バルーンの一番上へ **1.5 行ぶん（行送り 30 で 45 画素）の空き**が見えた。原因はゴースト pasta が話者交替の改行 `\n[150]` を、まだ 1 文字も出ていない相方側へ出すことであり（§13.2 の行 13・引受先は上流のリポジトリ `ekicyou/pasta`）、areka はその空きを ukadoc の `\n[パーセント]` どおりに保っている。**不合格の理由にしていない**——§6 の追記 追 10 が走行の前に「見えてよい」と定めており、開発者はこれを承知の上で項目 2 を合格とした（§8.1 の行 2）。3 行目が来て収まらなくなったときは、この空きから先に送られて消える |
| 3 | 初回起動限定の位置調整が 2 回目以降の起動で既定配置へ戻る | **判定に載せない**。許容仕様とする既存の裁定を実機で確かめ、開発者の判断で最終確定して登記する | 違和感があればその時点で個別仕様を切る | **未観測**。**本走行では確かめられなかった（準備者の過失・§8.1 行 12）**——この行を見るには「走行 A（初回起動）で位置を調整 → 走行 B（2 回目の起動）でその位置がどうなるか」を見る必要があるが、走行 A と走行 B のあいだに `cargo test -p areka --bin areka` が回り、決定論一周テストが fixture の `profile\areka\` を消したため、走行 B の 1 回目は保存された位置を 1 つも読み戻せなかった（§13.3 の冒頭の過失の注記・手順書 §0.2／§2.4 の 2 が禁じている形である）。判定に用いた走行 B の 2 回目は、B の 1 回目で掴み直した位置を読み戻したもので、**初回起動限定の調整が 2 回目以降で既定配置へ戻るかどうかは試していない**。**既存の裁定（許容仕様）を維持し、最終確定は次の一周または開発者の個別確認へ持ち越す** |
| 4 | 重なりの手前・奥の規約が参照実装と逆向き | **判定に載せない**。適合対象の資産では現れないため本走行では検出されない旨を明記する | なし（意図的な差） | **本走行では検出されなかった**（行の扱いのとおり）。適合対象の資産 `emo2` には手前・奥の規約の差が見える重なりが無く、観測した場面（走行 A の A10＝項目 19）では重なり順の異常は見えていない（走行 B・C では重なり順を見る段を置いていない）——拡大率を変えて描き直された直後もバルーンがキャラの手前に居続けた（走行 A の A10＝項目 19・開発者「問題なし」・§8.1 の行 19・§10 の再表示直後の重なり順の行）。**意図的な差であり、扱いは変えない** |
| 5 | **選択肢の反転帯から文字のインクが下へ最大 2 画素はみ出す**（走行 C・項目 7 後半・**2026-09-06 追加**・R7.7）。帯の丈は行送りと同じ 30 で、正典の `font.height,28` の読み戻しでは「閉」「も」「調」「頻」「度」のインク下端が帯の下端 y29 に対し y31＝2 画素、「は」「い」「じ」「る」「ど」「整」が y30＝1 画素になる。**文字は欠けない** | **覆された**（2026-09-06・走行 A の目視・R7.7 廃止・Requirement 13 で直した＝§13.2 の行 5）。**この行はもう「判定に載せない症状」ではない**——はみ出しは 0 画素が期待であり、1 画素でも見えたら項目 7 の不合格である（§6 の追記 追 3′・手順書 §5.5 の C1）。以下は覆される前の扱いであり、履歴として残す: 〔**判定に載せない**。項目 7 の不合格理由にせず、機械判定と目視の食い違いにも数えない。**3 画素以上を観測した場合も不合格とせず、はみ出した画素数を添えて再裁定を求める**（帯を広げる是正は上流が却下済み＝隣の行の帯と重なり、どの選択肢を指しているかが一意でなくなるため）〕 | 完了 spec `areka-P0-emo-text-line-height-canon` の design「帯の防御式を保つ」節の裁定（**第 2 回**・2026-09-06）→ **本仕様が引き取った**（Requirement 13・D13・§13.4 の改変 1。完了置き場の文書は改変しない） | **覆された裁定の追跡行**であり、判定に載せない症状としては数えない（2026-09-06・走行 A の目視。帯を下げる是正で 0 画素になった＝§13.2 の行 5）。**本走行でも 0 画素を確かめた**——実拡大率 200% の走行 C（2026-09-11 00:11〜00:12・C1）で、選択肢の見た目が順に反転し、帯は行の中で中央に寄っていて、**反転帯から文字のインクが下へはみ出した画素は 0**、「閉じる」の右端も欠けていない（開発者「OK」・§8.1 の行 7・§10 のメニュー表示の行）。帯を行ボックスの中央へ寄せた改変は §13.4 の行 1 である |

#3 の最終確定（開発者の判断・R7.3）: **本走行では確定していない**（2026-09-11）。行 3 の欄のとおり、初回起動の位置調整が 2 回目以降の起動で既定配置へ戻るかどうかは**本走行では確かめられなかった**（準備者の過失で走行 A → 走行 B の 1 回目の証跡が失われた・§8.1 の行 12・§13.3 冒頭の過失の注記）。よって**既存の裁定（許容仕様とする）を維持し、最終確定は次の一周または開発者の個別確認へ持ち越す**。持ち越しても行 3 は開いたままであり、「判定に載せない」扱いは変わらない。

**引受先を新たに作る場合は、引受先が実在することを確かめてから登記する**（R7.6）。仕様名を書くだけでは登記にならない。

| 新たに作った引受先 | 実在することの確認（見たファイルと日付） |
|---|---|
| **本走行では新設なし**（2026-09-11・タスク 6.4）。上の 4 行のうち、行 1 は 6.4 の時点では引受先なし（**タスク 6.5 で新しく起こした**——下の行）・行 2 は上流のゴースト側・行 3 は「違和感があればその時点で個別仕様を切る」で据え置き・行 4 は意図的な差であり、いずれも新しい引受先を要していない | **該当なし**。引受先の新設は §13.2 の仕分け（タスク 6.5）が行う。本行に書くことがあるとすれば 6.5 が新設したときであり、その場合も**実在を確かめてから**ここへ書く（R7.6） |
| 行 5 の引受先＝完了 spec `areka-P0-emo-text-line-height-canon` の design（「帯の防御式を保つ」節の裁定・第 2 回・2026-09-06） | `.kiro/specs/completed/areka-P0-emo-text-line-height-canon/design.md` を **2026-09-06 に開いて確認**した。見出し「帯の防御式を保つ」の節が同ファイルに実在する（見出しには対象の 3 ファイル `choice.rs`・`actor.rs`・`viewbox_draw.rs` が併記されている）。同ファイルの決定の表に「第 2 回裁定（同日・タスク 5.2）＝ 正典 `font.height,28` では 2 画素 → 2 画素を許容」（1 画素は font 20 の fixture の値・3 画素以上なら再裁定）とあり、読み戻しの固定先は `crates/areka-emo-text/tests/line_pitch_readback_test.rs`（実在を同日に確認）である。**ただしこの裁定は覆された**（2026-09-06・走行 A の目視）——引受先は完了置き場の design ではなく**本仕様**へ移り、`BAND_OVERHANG_MAX` は 3 か所とも 2 から **0** へ締めた（§13.2 の行 5・§13.4 の改変 1）。完了置き場の文書は改変していない |
| 行 1 の引受先＝新しく起こした仕様 `areka-P0-dpi-transition-two-tick-bounce`（2026-09-11・タスク 6.5。開発者の「起票の候補はすべて起票せよ」という指示による） | `.kiro/specs/areka-P0-dpi-transition-two-tick-bounce/brief.md` を **2026-09-11 に開いて確認**した（4,363 バイト・コミット `05a957ff` で作られたもの）。`.kiro/steering/roadmap.md` の W13 裁定枠 G の行にも、本記録の §13.1 行 1 の引受先として載っている |

### 13.2 走行で見つかった症状の仕分け（R8.4・R8.5・R8.7）

**規則**

1. 症状は**その場で直せる小さなもの**と**構造的なもの**に仕分ける（R8.2）。
2. 構造的なら、個別の仕様を**その時点で切って先に完遂**し、完遂の後に一周走行をやり直す（R8.3）。
3. **引受先の欄を空のまま閉じない**（R8.5）。「いずれ誰かが直す」は引受先ではない。引受先が書けないなら、その症状はまだ閉じられていない。
4. **1 つの症状に 1 つの引受先**という粒度を保つ（R8.7）。発見の都度に仕様を量産しない。1 つの引受先が複数の症状を抱えるなら、症状の側を分けずに引受先の側の記述を厚くする。
5. 引受先は**実在を確かめてから**書く。仕様名の記載だけでは仕分けが済んだことにならない。

| # | 症状 | 分類（その場で直せる小さなもの／構造的なもの） | 引受先 | 引受先が実在することの確認 |
|---|---|---|---|---|
| 1 | **メニューの先頭の選択肢が描かれない**（走行 A・2026-09-05・A14 で発見）。相方側バルーン `emo2-kakukaku` の文字描画範囲は高さ 93px（`balloonk0s.txt:4-7` の `validrect.top,40`／`bottom,-70` を 288×203 へ解決）だが、areka の行送りは `ceil(font.height × 1.25)`＝35px（`crates/areka-emo-text/src/state.rs:64-66`・`draw.rs:477-479`）のため、`menu.pasta:15` の 3 行目（`\_l[5em,2lh]`＝y110..138）が下端 133 を 5px あふれる。`LayoutEngine::visible_window`（`layout.rs:532-546`）が 1 行スクロールを返し、`draw.rs:766-774` の `.skip(first_visible_line)` が**先頭行**を描画対象から外す。SSP は同じ `font.height,28` で 3 行を 93px に収める（≈31px/行）。3 本のメニュー全部で先頭の選択肢（おしゃべり頻度／しゃべくり／調整）が消える。脳は 3 件返し kanade も 3 件登録（生ログ `choice_count=3`）＝欠けるのは描画のみ。決定論の再現（実 parser→compile→state→region→layout）で確認済み | **構造的**（R8.2）。`font.height` の意味（箱の丈 vs em）・行送りの式・`1lh` の定義が連動する `areka-emo-text` の正典変更で、既存の檻多数が 1.25 前提。R8.3 に従い**別 spec を切って先に完遂し、その後に一周を採り直す**（開発者裁定・2026-09-05） | **新規起票**（案: `areka-P0-emo-text-line-height-canon`。SSP と同じバルーンで文字を並べて撮り `font.height` の意味と行送りの式を確定→`draw.rs` の format 生成・`DWriteMetrics`・`TextLayerConfig`・`visible_window` の呼び側を正典へ。檻: 実物 `emo2-kakukaku` で `menu.pasta` の 3 台本すべて `first_visible_line == 0`）。**着手は `areka-P0-cursor-tag-canon` の main マージ後に rebase してから**（同 spec は `layout.rs`・`state.rs`・`region.rs` を全面改変・`visible_window`／`draw.rs`／1.25 は無改変＝別セッション回答 2026-09-05）。相互参照: `areka-P0-text-decoration-canon` brief「追加登記 4」（後戻り行のあふれ挙動） | **確認済み**（`.kiro/specs/completed/areka-P0-emo-text-line-height-canon/`・2026-09-06・実装完了〔タスク 1.1〜7.2〕・`tests/kero_menu_capacity_test.rs` で 3 台本 `first_visible_line == 0` を固定・引き渡し文書 `verification/handoff.md`） |
| 2 | **「閉じる」の右端が欠ける**（同上・関連症状）。`wordwrappoint.x,-34` が画像幅 288 基準で 254 に解決され `validrect.right`（240）の外に出るため、`\_l[5em,…]` 後の 3 文字（x164..248）が文字供給面（validrect ちょうど・`actor.rs:662-669`）で 8px 切れる。バルーン定義の粗さと areka の供給面寸法の両方に跨る | **構造的**（同上・供給面と折返し閾値の関係は #1 と同じ正典の問題） | #1 と同じ新規 spec（1 症状 1 引受先の粒度は保ち、引受先の側の記述を厚くする） | **確認済み**（`.kiro/specs/completed/areka-P0-emo-text-line-height-canon/`・2026-09-06・#1 と同じ spec。『閉じる』は実フォントの文字送りで x164..≈233 ≤ 240 に収まる＝要件 6.5・`kero_menu_capacity_test.rs`） |
| 3 | **生ログの日本語が化ける**（走行 A）。PowerShell が areka の UTF-8 標準出力を CP932 で受け、`Tee-Object` が化けたまま UTF-8 で書くため復元不能。ログの ASCII 欄（`event=`・`id=`・数値）は無傷 | **その場で直せる小さなもの**（道具の罠・製品外） | 起動スクリプト `lap.ps1` に `[Console]::OutputEncoding = UTF8` を追加して是正済み（2026-09-05）。手順書 §3.2 の逐語にも同じ 1 行を入れた（2026-09-06） | 該当なし（製品コード非接触） |
| 4 | **起動系列の途中に届いた再生完了の通知が捨てられる**（**2026-09-06 追加**・走行ではなく決定論層の作業〔タスク 5.5〕が構造から見つけたもの）。kanade が起動系列の `BootVersion` に滞在している間に再生完了（`TalkDone{Ended}`）が届くと、`crates/areka-kanade/src/schedule/mod.rs:543` が枝ごとに委譲した先の `dispatch_phase`（同 `:646` が `BootVersion` を `boot::step` へ渡す）で、`crates/areka-kanade/src/schedule/boot.rs:33-36` の「上記以外」の分岐が**通知を捨てる**（捨てるときに `event=boot_input_ignored` を `warn!` で書く＝同 `:34`）。突合の側（同 `mod.rs:681-694` の `current_talk_id`）は `BootVersion{Some}` を突合対象に含めて防御しているのに、委譲先が捨てるためトーク枠が定常相へ漏れ、**以後、終了の握手が二度と始まらない** | **構造的**。ただし**走行で見つかった症状ではない**ので、R8.3 の「その時点で切って先に完遂し、完遂の後に一周走行をやり直す」は掛からない（同項は走行で見つかった症状に掛かる条項である）。**本走行では発現しない**——発現には起動挨拶の再生完了が `basewareversion` の応答より先に届く必要があるが、`crates/areka-kanade/src/schedule/boot.rs:241` が再生の起動を応答の要求より先に積むうえ、実機の起動挨拶は非空で数秒かかり（`crates/pilot/examples/shiori-host-32/fixtures/emo2/ghost/master/dic/boot.pasta:78` 以降＝`＊OnFirstBoot` の自己紹介 8 往復）、応答は同じ往復で数ミリ秒で戻るため、窓が開かない。**R8.5（引受先の無い症状を閉じない）は掛かる**。**本走行でも発現していない**——`event=boot_input_ignored` は走行 A・走行 B の 1 回目・走行 B の 2 回目・走行 D・走行 C の**どれも 0 行**であった（§7 の点灯確認の表のその行。観測点は点いているので、沈黙ではなく 0 行として読む） | **S 規模の個別仕様**（起動系列に届いた再生完了の通知を取り落とさないようにする）。**2026-09-11 に起こした**（開発者の「起票の候補はすべて起票せよ」という裁定による）＝別仕様 `areka-P0-kanade-boot-talkdone-drop`（ロードマップの W13 裁定枠 E。`boot::step` の「上記以外」の分岐から再生完了の通知を分け、起動系列の外へ渡す）。**本仕様では直さない**（上の分類のとおり、走行で見つかった症状ではない） | **確認済み**。`.kiro/specs/areka-P0-kanade-boot-talkdone-drop/brief.md` を **2026-09-11 に開いて確認**した（4,030 バイト・コミット `05a957ff`）。`.kiro/steering/roadmap.md` の W13 裁定枠 E の行にも、本記録の §13.2 行 4 の引受先として載っている |
| 5 | **選択肢の反転帯の縦位置が高すぎる**（症状 A・走行 A・2026-09-06・A14 の目視で開発者が「色反転位置が 2 ドット程上すぎる」と判じた）。**この裁定は上流 spec の「はみ出し 2 画素は許容」を覆すもの**である（§13.1 の行 5・R7.7 廃止）。読み戻しで裏づけた（実フォント Yu Gothic UI・正典 `font.height,28`）——帯 y0..29（丈 30）に対し文字のインクは y7..31 で、**上に 7 画素の余白を残したまま下で 2 画素を切っていた**。機序は、DirectWrite が行ボックス（28 で 37.24）の中でインクを中央寄りに置くのに、帯は行矩形のブロック軸の近端（横書き＝上端）に揃えて置かれていたこと。帯を広げると隣の行の帯と重なってどの選択肢を指しているかが一意でなくなるので、**広げずに行ボックスの中央へ寄せた**（`band_offset = round(max(0, 行ボックス丈 − 帯の丈) / 2)`＝28 で 4・fixture の font 20 で 2・行ボックス丈が em に等しい既定フォントで 0） | **その場で直した**（R8.1・2026-09-06 第 2 回改訂）。本仕様の 1 ウェーブで着地できる規模——純関数を 1 本足し、その値を描画とヒットの両方へ配る配線だけである | **本仕様**（タスク 6.6・コミット `6450ccad`）。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 1** | **確認済み**（`git log -1 6450ccad` を **2026-09-06 22:10:44** に実行し、`6450ccad1a9a8bf450d1e93f5febad30bcfd5e36`「task 6.6 - center the choice highlight band in the line box (band_offset) so no ink is cut」が実在することを見た）。直した後の読み戻しの値: 正典 28 は帯 y4..33／字 y7..31（上 3・**下 0**）、emo2 fixture は塗り y34..63／字 y37..61（上 3・**下 0**。直す前は塗り y30..59／字 y37..61 で上 7・下 2）、font 20 は帯 y24..45／字 y28..44（上 4・**下 0**） |
| 6 | **折返しの警告が配置のたびに出る**（症状 B・走行 A・2026-09-06）。生ログ **30,837 行のうち 27,908 行**が同じ 1 行——「折返し基準が描画範囲の外に解決された」（`wrap_threshold=254`／`inline_limit=240`）である。読み込み時に 1 件のはずが毎フレーム出ていて、ログが読める分量でなくなり、件数が意味を持たなくなっていた。機序は、毎フレーム走る再追従が churn ガードの判定キーを得るために解決関数を呼び、警告がその**解決関数の中**に置かれていたこと。**あるべき姿は、純粋な解決関数はログを書かず、「装着 1 回につき 1 件」という意味を持つ層＝actor の登録口が、解決済み領域の値が新しく決まったときだけ記録すること**である | **その場で直した**（R8.1・2026-09-06 第 2 回改訂）。本仕様の 1 ウェーブで着地できる規模——警告を書く場所を移すだけで、文言・4 つの欄・出る条件は変えない | **本仕様**（タスク 6.7・コミット `a9aef152`）。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 2** | **確認済み**（`git log -1 a9aef152` を **2026-09-06 22:10:44** に実行し、`a9aef15277ec7e077db8a192ff5fb9442fe96ed1`「task 6.7 - emit the coarse-wrap warning once per attach at register_actor, not on every resolve」が実在することを見た）。新しい出所は `crates/areka-emo-text/src/actor.rs:199-211` の `warn_coarse_wrap_threshold`（`warn!` は同 `:204-210`・文言は同 `:209`・4 欄は同 `:205-208`）で、呼ぶのは `TextLayerRuntime::register_actor`（同 `:327`）である。**手順書 §5.7 と本記録 §7 の出所はこの新しい場所へ改めた** |
| 7 | **終了指示が強制終了系列へ入り終了挨拶を通らない**（症状 C・走行 A・2026-09-07・A20）。開発者所見「Ctrl+ダブルクリックしか終了方法がなく、即時終了。挨拶は出てこなかった」。生ログ（`C:\home\maz\lap-records\emo2-conformance-2026-09-06-2\lap-run-a.log`・6,912 行）の終端 5 行がそのまま機序である——`event="mouse_escape_close"`（1 行）→ `[App] Last window closed.`（3 ms 後）→ `event="force_quit" reason="user"`（さらに 17 ms 後）→ `method=NOTIFY id=OnClose references=["user"]`（同時刻）→ `event="unload_clean"`（50 ms 後）。`method=GET id=OnClose`・`event="close_talk_start"`・`event="ghost_quit"` は**いずれも 0 行**で、辞書の終了挨拶は 1 度も照会されていない。機序は、唯一の終了操作が「暫定退避」＝全ゴースト窓の破棄のままで、kanade に実装済みの正規の握手（`OnClose` GET → 終了挨拶 → `\-` → 解放）へ入る道と、握手が終わってから窓を閉じる者の、**2 つが欠けていた**こと | **その場で直した**（R8.1・2026-09-06 第 2 回改訂）。本仕様の 1 ウェーブで着地できる規模——終了操作の分岐を 2 つに割り、kanade の停止を UI へ 1 件知らせ、その通知で窓を閉じる相を足すだけである | **本仕様**（タスク 6.9・コミット `9ba2515f`）。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 3** | **確認済み**（`git log -1 9ba2515f` を **2026-09-07 19:39:26** に実行し、`9ba2515f760d9ee344a7a95d320af15bcea35fad`・2026-09-07 19:30:59 +0900・件名「feat(areka-P0-emo2-conformance-e2e): task 6.9 - wire the close gesture to kanade's close handshake and close the windows when kanade stops」が実在することを見た） |
| 8 | **終了挨拶の後の解放が脳との通信で失敗する**（症状 D・走行 A・2026-09-07 20:01〜20:04・A20 の直後）。開発者所見は「終了挨拶は流れた——合格」だったが、生ログは `event="talk_done_quit"` の 150 ms 後に `[request_clean_shutdown] unload send failed error=Ipc(Timeout)` を、同時刻に helper 側の `[helper] unload-ack 送出失敗（観測）: SendFailed` を残していた（`event="unload_failed"` 1 行・`event="unload_clean"` 0 行）。機序＝脳との往復は窓あてのメッセージで行い、helper は要求を処理している最中から応答を再入でホスト窓へ送る。その送出には**「相手が応答なしなら待たずに打ち切る」旗**（`SMTO_ABORTIFHUNG`）が両方向とも付いていた。ホスト窓が載っているスレッドは要求の往復以外でメッセージを取り出さず、終了挨拶の再生中は 17〜19 秒の空白ができるため、OS はこのスレッドを「応答なし」と見なして応答の送出を即座に失敗させ（戻り 0・`GetLastError` も 0）、ホストは応答が来ないまま時間切れと読んだ。**実測の条件は「プロセス生存 20〜30 秒を過ぎた後で、対象スレッドが 14 秒以上メッセージを取り出していないこと」**——起動直後には猶予期間があり発現しない（2026-09-07 の調査係が較正し、要件側の数値をコミット `c215f6ae` で訂正した）。定常運転では毎秒の往復が空白を作らないので露呈しなかった | **その場で直した**（R8.1）。本仕様の 1 ウェーブで着地できる規模——応答方向の送出だけをこの旗から外し（時間上限 5 秒は保つ）、どちらの向きにどの旗を付けるかを名前で読める形に分けるだけである | **本仕様**（タスク 6.11・コミット `ce545350`）。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 4** | **確認済み**（`git log -1 ce545350` を **2026-09-10** に実行し、`ce545350bf7f2fe699ea8fd7eeedaf3a4250f1e9`「task 6.11 - send helper responses without SMTO_ABORTIFHUNG (SendFlavor::Response) so the unload after a long farewell is acknowledged」が実在することを見た） |
| 9 | **拡大率 200% で描画が重く、ガクガクする**（症状 E・走行 A・2026-09-07・開発者所見「描画が異常に重い・ガクガク・遅延が目に見える」）。1 コマの適用（`perf(apply_show)`）の段階別の計時が 1 コマ目から一様に前の走行の約 2.2 倍で、中央値 41 → **78 ms**・p90 65 → 227 ms（compose 3.0→6.7・resample 22→49・mask 4.6→11・upload 8.4→19 ms）。当初は機械の負荷（別プロジェクトの `python.exe` が 1 コア分を占有・クラウド同期・別リポジトリのテスト）を疑ったが、その後の計測で**絵が変わるコマ 1 回ぶんの費用そのものが 61〜84 ms**（compose 5〜7＋resample 44〜61＋mask 11・いずれも画面のスレッド上・作り置きは容量 3 の入れ替え）と分かり、環境の負荷で説明が付くのは **1.35 倍まで**であった。まばたきのように 60 ms 間隔で絵が変わる場面では 1 コマが間に合わない | **構造的**（性能の設計に掛かる。本仕様の 1 ウェーブでは着地しない）。**本走行の合否には載せない**（開発者裁定・2026-09-07「E は別 spec」）——採り直しでは `perf(apply_show)` の中央値を数えて記録に残すだけにする（§7・§6 の追記 追 8） | **別仕様 `areka-P0-present-gpu-transform-scale（旧名 present-resample-budget）`**（2026-09-07 起票・ロードマップの W12 裁定枠 A″。目標は 1 回 16 ms 以下・許容 30 ms 以下。2026-09-11 に**ロードマップの W13 裁定枠 D**へ移り、4 本のうち最優先になった） | **確認済み**（`.kiro/specs/areka-P0-present-gpu-transform-scale/brief.md` を **2026-09-11 に開いて確認**した〔7,216 バイト〕。2026-09-11 の改名で旧名 `present-resample-budget` から差し替わり、開発者裁定「絵は原寸のまま持ち、拡大は常に D2D の変換で行う」を受けて中身も書き換わっている。`.kiro/steering/roadmap.md` の **W13 裁定枠 D** の行にも、本仕様の症状 E の引受先として登記されている） |
| 10 | **ホスト窓のスレッドが待機中にメッセージを取り出さない**（構造・2026-09-07 の症状 D の調査で分かったもので、走行で見えた症状ではない）。脳と話す窓は SHIORI アクターのスレッドに在るが、そのスレッドは入力待ち（`crates/areka-kanade/src/shiori/real.rs:179` の `rx.recv()`）で止まり、要求の往復と起動時の握手以外ではメッセージを取り出さない。あるべき姿は、入力と窓のメッセージを**同時に**待つ形（`MsgWaitForMultipleObjectsEx`）である | **構造的**。ただし**本走行では発現しない**——症状 D の直し（応答方向から旗を外す・§13.4 の改変 4）で、待機が長くても応答が届くようになり、この構造が表に出る経路は塞がっている。R8.3 の「その時点で切って先に完遂する」は**走行で見つかった症状**に掛かる条項なので、この行には掛からない | **別仕様 `areka-P0-host32-window-thread-pump`**（2026-09-11 起票・タスク 6.5。ロードマップの W13 裁定枠 F。中身は「SHIORI アクターの入力待ちを、窓のメッセージと同時に待つ形へ改める」）。隣接する `areka-P0-zorder-chain-residue` の A 系（wintf／host32 の常設の飢餓と間欠的な赤）へ寄せず、単独で切ることに決まった。**この行はこれで閉じた**（R8.5） | **確認済み**。`.kiro/specs/areka-P0-host32-window-thread-pump/brief.md` を **2026-09-11 に開いて確認**した（4,714 バイト・コミット `05a957ff`）。`.kiro/steering/roadmap.md` の W13 裁定枠 F の行にも、本記録の §13.2 行 10 の引受先として載っている |
| 11 | **相方側バルーンが最新の 1 行しか見せない**（症状 F・2026-09-07 20:22〜20:27 の診断のための走行と、それに続けて始めた採り直しの走行 A・**A3**〔項目 2 起動挨拶の再生〕。開発者のスクリーンショットは 20:27:58＝`OnFirstBoot` の 4 往復目）。相方側バルーンには「イイジャン！」だけが描画範囲の上から 45 px の位置に見え、直前の台詞「僕はエモ。クール系の可愛い娘。」（幅 216 の描画範囲では 2 行に折り返す）が見えない。**機序**——あふれたときに何行ぶん送るかを決める起点を、`crates/areka-emo-text/src/layout.rs` の `visible_window` が**最初の行の開始側**に取っていた。相方側の台詞の冒頭には 1.5 行ぶん（行送り 30 で 45）の空きがある（行 13。台本が話者交替の前に出す `\n[150]` が、まだ 1 文字も出ていない相方側へ落ちてできる）が、この空きは起点より前にあるので送る候補に入らず、「1 行送る」しか選べない。その結果、収まるはずの上の行が描画範囲から押し出されていた。起点を**描画範囲の開始側**に取れば「0 行送って空きだけを詰める（−45）」が選べ、3 行とも 40／70／100..130 に収まる。**2026-09-07 に本命と疑った「PR #142 が入れた差分描画（矩形ごとに交差する行だけを描く）」は外れである**——外した根拠は 2 つで、⑴ 同日に書いた再現の試験 5 本は台本に冒頭の改行を含んでおらず、直す前のコードでそのまま緑だった（症状を再現していなかった）、⑵ 診断のための走行のログの `first_visible_line` が 1・2 と動いており、上の行は「描かれていない」のではなく「送られて可視窓の外にある」ことを示していた | **その場で直した**（R8.1）。本仕様の 1 ウェーブで着地できる規模——送り量の起点 1 か所だけを変えるもので、あふれの判定・行単位の送り・最新行への飽和は変えない | **本仕様**（タスク 6.13・コミット `6cbf7c59`）。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 5** | **確認済み**（`git log -1 6cbf7c59` を **2026-09-10** に実行し、`6cbf7c591a2a6d7389577f3514447bad7e6d67c5`「task 6.13 - visible_window anchors the scroll at the region start instead of the first line, so a leading pending newline (pasta's \n[150] in the empty kero scope) is scrolled away first and every fitting line stays visible (symptom F)」が実在することを見た） |
| 12 | **終了挨拶が終わってから窓が閉じるまで長く待つ**（症状 G・2026-09-07 の診断のための走行で症状 F と一緒に見つけたもの。同じ日の 20:01 の走行で「終了挨拶の再生中に 17〜19 秒メッセージを取り出さない空白があった」（§13.2 の行 8）と数えた空白は、その大半がこの待ちで説明が付く）。ゴーストの辞書は話の終わりに「ゴースト終了（３００）」を呼び（`crates/pilot/examples/shiori-host-32/fixtures/emo2/ghost/master/dic/boot.pasta:95`・同 `:102`・同 `:109`）、pasta の `act:wait(ms)` はこれを **`\w[300]`** という綴りで出す（上流 `ekicyou/pasta` v0.3.3 の `crates/pasta_lua/pasta_scripts/pasta/shiori/sakura_builder.lua:73`。ゴーストに同梱された写しでは同名ファイルの `:85`。待ちを積む側は上流・写しとも `pasta_scripts/pasta/act.lua:229` の `ACT_IMPL.wait`）。ところが ukadoc の `\w時間` は**書いた数 × 50 ms** であり（https://ssp.shillest.net/ukadoc/manual/list_sakura_script.html#_5cw_6642_9593:1 「時間x50ms分の時間ウェイト」）、ミリ秒で待たせたいときに使う綴りは `\_w[時間]`（同 `#_5c_w_5b_6642_9593_5d:1` 「精密ウエイト。[時間]ms」）である。**待ちが 15 秒になったのは areka の側の読みである**——ukadoc に括弧形 `\w[n]` は無いのに、areka の復号（`crates/areka-parsers/src/sakura/decode.rs`）が短縮形と同じ「書いた数 × 50 ms」で括弧形を受理していた（正典に無い綴りを発明して読んでいた）。原因は 2 つ重なっている: ⑴ 台本が正典に無い綴りを出す（上流）、⑵ areka がその綴りを待ちとして読む（areka 側） | **2 つに分かれる**。⑵ areka 側の非正典読みは**その場で直した**（R8.1・2026-09-10 第 6 回改訂の裁定「`\w[xxx]` 系は不要・除去が正しい・この場で修正」）——復号から括弧形の腕を削るだけで、他の未知タグと同じ素通し（待ち無し）になる。⑴ 台本側の綴りは**上流**のままである（areka からは直せない）。**本走行の判定には載せない**: 終了挨拶が流れて `event="unload_clean"` が 1 行出れば項目 13 は成立であり、挨拶の後の待ちの長さ自体は不合格の理由にしない | **2 つ**。⑵ は**本仕様**（タスク 6.15・コミット`86d050de`。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 6**）。⑴ は**上流のリポジトリ `ekicyou/pasta`**（areka の repo ではない。台本〔`ゴースト終了（３００）`〕と、それを綴り `\w[300]` へ変換する側の両方が上流にある。正しい綴りは `\_w[300]`） | **⑴ は確認済み**（上流の作業ツリー `C:\home\maz\git\pasta` が実在し、`git -C C:\home\maz\git\pasta log --oneline -1` が **2026-09-10** に `edde4b37 release: v0.3.3` を返した。同ツリーの `Cargo.toml:9` が `version = "0.3.3"` であることも見た）。**⑵ は本仕様の中で完結**（タスク 6.15。檻は `crates/areka-parsers/src/sakura/decode_tests.rs` の `wait_bracket_form_is_raw_not_wait`＝`\w[2]` が `Raw` になることを固定し、`\w2`＝100 ms・`\_w[450]`＝450 ms を同じ試験で対照する）。**⑴ も上流で直り、その版を取り込んだ**（2026-09-11 に確認）——適合対象の資産の `pasta.dll` を、上流が直した版へ差し替えた（コミット `1e90c743`・**3,821,568 バイト**。前の版は 3,450,880 バイト）。差し替えた DLL が起動時に自分で書き出す写し `crates\pilot\examples\shiori-host-32\fixtures\emo2\ghost\master\profile\pasta\pasta_scripts\pasta\shiori\sakura_builder.lua` の 73 行目が、待ちを**正典どおりの `\_w[ミリ秒]`** で出している（この写しの更新時刻は下の確認の走行と同じ 2026-09-11 00:17）。上流の作業ツリー `C:\home\maz\git\pasta` の HEAD も **`8e063286`「会話ウェイトを UKADOC 準拠の `\_w[ms]` で出力する」（2026-09-10）** へ進んでおり、同ツリーの同じファイルの 73 行目も `\_w[%d]` である（v0.3.3＝`edde4b37` の 1 つ後）。**確認の走行**（2026-09-11 00:17:35〜00:18:09・記録 `C:\home\maz\lap-records\emo2-close-check-2026-09-11\lap-run-a.log`・597 行）では、終了挨拶の再生が終わってから（`event="talk_done_quit"`）窓が閉じるまで（`event="ghost_quit"`）が **0.6 秒**であり、15 秒の待ちは消えている（生ログの時刻は協定世界時で `15:18:08.131396Z` → `15:18:08.761531Z`＝現地の 00:18:08.131 → 00:18:08.761。同じ走行で `event="unload_clean"` も 1 行出ている） |
| 13 | **話者が切り替わる前の改行が、まだ空の相手側へ落ちて冒頭に 1.5 行ぶんの空きを作る**（**症状 F の引き金であって、症状 F そのものではない**）。pasta は話者が変わるとき、変わる**前**の相手の側へ `\n[150]`（1.5 行ぶんの改行）を出す（上流 `ekicyou/pasta` v0.3.3 `crates/pasta_lua/pasta_scripts/pasta/shiori/sakura_builder.lua:160`。ゴーストに同梱された写しでは同名ファイルの `:63`＝話者交替の出力点で、既定の 1.5 は同 `:115`）。トークの最初の話者交替では、その改行が**まだ 1 文字も出ていない相方側**へ落ちるため、相方側バルーンの一番上に 1.5 行ぶん（行送り 30 で 45）の空きができる。areka はこの空きを捨てない——ukadoc の `\n[パーセント]`（https://ssp.shillest.net/ukadoc/manual/list_sakura_script.html#_5cn_5b_30d1_30fc_30bb_30f3_30c8_5d:1 「通常のパーセント分改行する」）はカーソルの移動なので、**収まっている間は空きが見えるのが正典どおり**であり、あふれたときはこの空きから先に送られて消える。その「先に送られる」が成り立っていなかったのが症状 F（行 11）である | **構造的（上流）**。**本仕様では直さない**——areka 側は正典どおりで直すところが無い。**本走行の判定にも載せない**: 冒頭の空きが見えることを項目 2 の不合格の理由にしない（§6 の追記 追 10・手順書 §5.2 の A3） | **上流のリポジトリ `ekicyou/pasta`**（行 12 と同じ引受先である。1 つの引受先が 2 つの症状を抱える形で、症状の側は分けない＝R8.7） | **確認済み**（行 12 と同じ——`C:\home\maz\git\pasta` が実在し、HEAD は **2026-09-10** 時点で `edde4b37`〔v0.3.3〕である）。**ただし、行 12 で取り込んだ上流の新しい版では、この空きはもう出ないと読める**（2026-09-11 に写しを読んで確認）——差し替えた `pasta.dll` が書き出した写し（行 12 に挙げたのと同じファイル）は、話者が変わった時点で改行を出すのをやめ、**変わった先の側にすでに文字が出ているときだけ**改行を出す形に変わっている（`:142` で「切替先に文字が出ているか」を控え、`:160` でそのときにだけ `\n[150]` を出す。無条件に出していたのは同梱されていた古い lua のほうで、この repo に取り込んである写し `vendors/pasta/crates/pasta_lua/scripts/pasta/shiori/sakura_builder.lua` の切替の分岐がその形である。`:142`／`:160` の「文字が出ているときだけ」の分岐は **v0.3.3（`edde4b37`）にも同じ形で在り**、`edde4b37` → `8e063286` の差は `:73` の `\w[%d]` → `\_w[%d]` の **1 行だけ**である（`git diff` で確認）。ゆえに前提を明記する——一周のときにゴーストに入っていた `pasta.dll`（3,450,880 バイト）は**自身の版表示 v0.3.3 より古い lua を抱えていた**（同梱の写しで待ちの出力が `:85`・話者交替の出力点が `:63` と、v0.3.3 の `:73`／`:160` と一致しなかったことが根拠）。この前提の上で、差し替えで振る舞いが変わると読む——つまり左の「変わる前の相手の側へ出す」という記述は、**一周のときにゴーストに入っていた DLL（3,450,880 バイト）の振る舞い**を指している）。よって**まだ 1 文字も出ていない側へ落ちる冒頭の空きは、新しい版では出ないはず**である。**目で確かめてはいない**——確認の走行は終了の待ちだけを見る短いもので、生ログに台本の本文は載らない（`\n[` を 1 行も含まない）。引受先は上流のまま据え置き、次に一周を回すときに目視で確かめる |
| 14 | **会話中の普通の応答が「想定外」の警告として数えられる**（症状 H・2026-09-10 の走行 A の生ログを読み直して見つけたもの。走行 A に `event="steady_unexpected_reply" phase="Steady{Some}"` が **19 行**・走行 B の 1 回目 7 行・走行 C 4 行）。どの行の直前にも `GET id=OnMouseMove status=Some("talking")` があり、**会話中の撫でに台本が無かった（204）**だけである。機序＝`crates/areka-kanade/src/schedule/steady.rs` の `Phase::Steady{talk: Some}` の腕が `Notified` と `Value` しか扱わず、204 が「想定外」の防御アーム（`steady_reply_unexpected`）へ落ちていた。`Steady{talk: None}` の腕には 204 の扱いがある。**見える挙動は何も変わらない**（どちらの経路も状態を保って何もしない）——欠陥は記録の上だけで、生ログの警告の数が異常の数と一致しなくなる | **その場で直した**（R8.1）。本仕様の 1 ウェーブで着地できる規模——`match` の腕を 1 つ足すだけで、他の相・他の応答の扱いは変えない | **本仕様**（タスク 6.16・コミット ``e6c210e5``）。改変の範囲・理由・走行前後で見える挙動の差は **§13.4 の改変 7** | **確認済み**（本仕様の中で完結。檻は `crates/areka-kanade/src/schedule/steady_flow_tests.rs` の `steady_some_mouse_no_content_is_silent_and_keeps_state`＝会話中に `OnMouseMove` 由来の 204 が届いたとき、再生中の talk が差し替わらず `Action` が空で、`steady_unexpected_reply` が 1 行も出ないことを固定する。直す前の HEAD では同じ檻が警告 1 行を捕まえて赤だった） |

**行 4 の発現の有無は、走行ごとに確かめる**（2026-09-06 追加）。確かめ方は `event=boot_input_ignored` の**行数を数える**ことであり、**0 行なら発現していない**（§7 の点灯確認の表に 1 行として書く。沈黙ではなく、点灯している観測点の 0 行として読む＝§0.1）。1 行でも出たら、行 4 の「本走行では発現しない」を「発現した」へ改め、その走行の**項目 13（終了）の結果と突き合わせる**。

**製品コードの改変（R8.6・2026-09-06 第 2 回改訂）**

**この節はもう「例外」ではない。** 2026-09-06 の第 2 回改訂で R8.1 が「走行で見つかった症状のうち、本仕様の 1 ウェーブで着地できるものは本仕様の中で直す（製品コードの改変を含む）」へ改まった。改変ごとに、範囲・理由・走行前後で見える挙動の差（直した症状そのものを除き差が無いこと）を残す。**台帳は §13.4 に置く**（改変 7 件・行 5・6・7・8・11・12・14 に対応）。改訂の前に書いてあった「埋める欄が無いことが正常である」は、もう当てはまらない。 **引受先の空欄は 0 件である**（2026-09-11・タスク 6.5）。14 行の内訳（行 12 は ⑴⑵ で引受先が 2 つに分かれるため、割り当ての合計は 15 件＝14 行を 1 上回る）は、本仕様の中で直した **7 行**（行 5・6・7・8・11・12 の areka 側・14＝§13.4 の台帳の 7 件と同じ組）、完了済みの別仕様 `areka-P0-emo-text-line-height-canon` が直した 2 行（行 1・2）、走行の道具の直し 1 行（行 3）、別仕様へ渡した 3 行（行 4＝`areka-P0-kanade-boot-talkdone-drop`・行 9＝`areka-P0-present-gpu-transform-scale`・行 10＝`areka-P0-host32-window-thread-pump`。いずれも 2026-09-11 に実在を確かめてある）、上流のゴーストの側 2 行（行 12 の ⑴・行 13。引受先は 1 つ＝`ekicyou/pasta` で、症状の側は分けない＝R8.7）である。 **一周走行のやり直しは要らない**（2026-09-11・タスク 6.5）。一周（2026-09-10 22:21〜2026-09-11 00:12）より後に入った直しは 3 つあり、どれも 6.2〜6.4 の結果を揺るがさない: ⑴ タスク 6.15（正典に無い `\w[n]` を待ちとして読むのをやめた・コミット `86d050de`）は、終了挨拶の後の待ちが 15 秒 → 0 秒になるだけで、その待ちの長さは初めから判定に載せていない（§13.2 の行 12）。項目 13 が要る「終了挨拶が流れて `event="unload_clean"` が 1 行出る」ことは、2026-09-11 00:17 の確認の走行で改めて成り立っている。⑵ タスク 6.16（会話中の 204 を警告から外した・コミット `e6c210e5`）は、見える挙動の差が無く、変わるのは生ログの警告 1 行が出なくなることだけである（§13.4 の改変 7）。⑶ 資産の `pasta.dll` の差し替え（`1e90c743`）も、同じ確認の走行で終了挨拶と解放が通ることを見てある。よって **6.2〜6.4 の結果はそのまま生きており**、R8.4 の「やり直しが必要になった場合は結果を無効として明記する」は発動しない。

### 13.3 中断（R5.11）

走行が途中で成立しなくなった場合に埋める。**中断は合格にも不合格にも数えない。部分的な結果を合格として扱わない。**

**記録置き場が消えている（2026-09-10 に判明）。** 中断した走行の生ログを置いていた親フォルダ `C:\home\maz\lap-records\` が、2026-09-10 の時点で**ディスクから消えていた**——`emo2-conformance-2026-09-05`・同 `-2026-09-06`・同 `-2026-09-06-2`・同 `-2026-09-07` の 4 つと、症状 F を調べた走行の置き場 `emo2-diag-2026-09-07-f2` が、生ログごと無くなっている（消えた経緯は分かっていない）。**以下の中断記録について今も残っているのは、本書へ写した値だけである**——行数・時刻・数えた語の件数・逐語で引いた行は本書の中に在るが、生ログへ戻って数え直すことはできない。採り直しは新しい置き場 `C:\home\maz\lap-records\emo2-conformance-2026-09-10`（2026-09-10 に親フォルダごと作り直した）で行い——**ただしそこで始めた走行 A は起動元のセッションごと落ちたため（下の 6 件目）、記録に載っている走行はすべて同じ日の 2 回目の置き場 `…\emo2-conformance-2026-09-10-2` で採った**——、**「前の置き場を 1 つも消さない」という約束は、この置き場から先に掛かる**（手順書 §2.4 の 4・§9）。

**中断は 6 件ある。** 1 件目は 2026-09-05 の走行 A、2 件目は 2026-09-06 の走行 A、3 件目は 2026-09-07 18:30 の走行 A、4 件目は 2026-09-07 20:01 の走行 A、5 件目は同じ日の 20:27 の走行 A、6 件目は 2026-09-10 22:14 の走行 A である（下に古い順で表を置く）。**6 件とも合格にも不合格にも数えない。** 6 件目だけは製品とは無関係の中断であり、その直後に採り直したのが本記録の走行である。 **中断ではない過失が 1 件ある**（2026-09-10・§8.1 の行 12）: 走行 A（22:37 終了）と走行 B（23:21）のあいだに準備者がこの作業ツリーで `cargo test -p areka --bin areka` を回し（試験実行体 23:17:20 生成）、決定論一周テスト（`crates/areka/src/emo2_boot/spine.rs` の `emo2_root()` が `remove_dir_all` で `profile\areka\` を消す）が走行 A の永続状態を消した。走行は成立しているので中断ではなく、A → B の 1 回目の位置の証跡だけが失われた（手順書 §0.2・§2.4 の 2 の禁止事項）。

#### 中断 1 件目（走行 A・2026-09-05）

| 欄 | 値 |
|---|---|
| 中断点（どの段・どの項目で止まったか） | 走行 A・A14（項目 7 二重クリックでメニュー）。2026-09-05 08:24 開始・08:27 終了（開発者が閉じた・exit 0・生ログ 3,515 行＝`lap-run-a.log`・日本語は化けているが ASCII 欄は無傷） |
| 中断の理由 | 構造的な製品欠陥 #1（§13.2）を発見。R8.3「構造的なら個別の仕様を切って先に完遂し、完遂の後に一周走行をやり直す」と開発者裁定（2026-09-05・別 spec を切って先に直す）による |
| 観測できたところまで（成立した段と項目） | A1〜A13 を通過（開発者の目視メモは未転記＝**この中断走行の結果は合否に用いない**）。生ログの機械可読な事実: 起動系列成立・`k_new=2.0`（200%）・二重クリック 3 回で毎回 `choice_count=3`・選択確定 3 回（`choice_selected`）・撫で照会あり |
| 観測できていない項目（合否を付けない項目） | 20 項目すべて（中断走行は合格にも不合格にも数えない） |
| やり直しの予定（条件を整えて再走するか） | **再走する**。引受先 spec（emo-text の行送り正典化・`cursor-tag-canon` マージ後に起票）の完遂後、§2 から準備をやり直し（永続状態の消去を含む）、走行 A〜D を新品のログ置き場で採り直す。§6 の宣言は書き換えない（再走時に読み直す）。<br>**採り直しの前提（2026-09-06 追記）**: 引受先 spec `areka-P0-emo-text-line-height-canon` は**完了した**（PR #142・main `e3291fc1`・本ブランチへの取り込みは merge `3c2908e`・引き渡し文書は `.kiro/specs/completed/areka-P0-emo-text-line-height-canon/verification/handoff.md`）。採り直しの記録置き場は**新しい日付のディレクトリ**（`C:\home\maz\lap-records\emo2-conformance-<実施日>`・実施日はタスク 6.1 で確定する）とし、走行 A（2026-09-05）の `lap-run-a.log` は中断の記録として**残す**（追記も上書きもしない・手順書 §2.4 の 3・同 §9）。新たに期待するものは §6 の**追記（2026-09-06・採り直しの前）**に書いた（既存の 20 行は 1 行も書き換えていない）。改訂の正本は requirements.md の「改訂（2026-09-06・上流 spec の着地・実機走行の採り直しの前）」であり、上流から引き取った 23 行の消化は **§14** にある |

#### 中断 2 件目（走行 A・2026-09-06・直す前）

| 欄 | 値 |
|---|---|
| 中断点（どの段・どの項目で止まったか） | 走行 A・**A14 までは成立**（項目 7 前半の目視で開発者が症状を判じた地点で止めた）。2026-09-06 **20:41:50 開始・20:48:03 終了**（exit 0・生ログ `C:\home\maz\lap-records\emo2-conformance-2026-09-06\lap-run-a.log`・**30,837 行**）。準備のコミットは `8eff0c60` |
| 中断の理由 | **開発者裁定「症状 A／B は許容できない・その場で直す」**（2026-09-06）。A14 で反転帯の縦位置が高すぎること（症状 A・§13.2 の行 5）と、生ログの 9 割が同じ警告で埋まっていること（症状 B・同行 6）が見つかった。あわせて本仕様のスコープが「総合試験を行い、問題をその場でつぶす」へ改まり（requirements.md「改訂（2026-09-06・第 2 回）」）、R8.1 の「証明に徹し製品コードを改変しない」が覆った。**直してから採り直す**という R8.3 後段の形で止めた |
| 観測できたところまで（成立した段と項目） | **A1〜A14 を通過**。A14 では**先頭の「おしゃべり頻度」を含む 3 つの選択肢がすべて見え、字下げされた「閉じる」の右端も欠けていなかった**（開発者のスクリーンショット）——1 件目の中断理由（§13.2 の #1・#2）は解消していた。**A15〜A20 の目視所見は転記していない**（中断走行の結果は合否に用いないため、無いものは無いと書く）。生ログの機械可読な事実: `boot_input_ignored` **0 行**・`connect_failed` **0 行**・`unload_clean` **1 行**・`choice_count=3` が **12 回**・`choice_selected` が **6 回**。実効の拡大率は主モニタ **192**（200%）・副モニタ **144** |
| 観測できていない項目（合否を付けない項目） | 20 項目すべて（中断走行は合格にも不合格にも数えない）。ただし上の 3 つのログの数え（`boot_input_ignored` 0 行・`connect_failed` 0 行・`unload_clean` 1 行）は、**5.5 の欠陥が発現しなかったこと**と**項目 20 の ⑴・⑶ の証拠**としては読める（requirements.md 改訂 5）——合否には載せない |
| やり直しの予定（条件を整えて再走するか） | **再走する**。2 つの症状を**直したコミット** `a9aef152`（6.6 が `6450ccad`・6.7 が `a9aef152`）で **§2 の準備からやり直す**（永続状態の消去を含む）。記録置き場は**同日 2 回目**なので `C:\home\maz\lap-records\emo2-conformance-2026-09-06-2` とし（手順書 §2.4 の 4・§9）、2026-09-05 と同日 1 回目の置き場はどちらも中断の記録として**残す**（追記も上書きもしない）。§6 の宣言の 20 行と追 1〜追 4 は書き換えず、期待が変わった 2 行だけを **§6 の追記（2026-09-06・第 2 回・直した後の採り直しの前）** に足した。改変の台帳は **§13.4** |

#### 中断 3 件目（走行 A・2026-09-07・終了指示を直す前）

| 欄 | 値 |
|---|---|
| 中断点（どの走行の何番目の段か・何が成立しなかったか） | 走行 A・**A20（項目 13 終了）**。**A1〜A19 は成立**した。2026-09-07 **18:30:30 開始・18:33:37 終了**（exit 0・生ログ `C:\home\maz\lap-records\emo2-conformance-2026-09-06-2\lap-run-a.log`・**6,912 行**）。準備のコミットは `a9aef152` |
| 中断の理由 | **開発者裁定「その場で直す」**（2026-09-07）。A20 で終了指示が強制終了系列へ入り、終了挨拶を通らずに窓が消えた（症状 C・§13.2 の行 7）。R8.1（1 ウェーブで着地できるものは本仕様の中で直す）に当たると仕分け、**直してから §2 の準備をやり直す**という R8.3 後段の形で止めた |
| 観測できたところまで（成立した段と項目） | **A1〜A19 を通過**。開発者は目視で ①〜⑪（項目 1・15・2・11・10・3・4・18・16・19・5・6・7 前半・8・9・14・17・12 前半）が OK であると述べており、**帯の縦位置（症状 A）と警告の回数（症状 B）の 2 つの直しも実機で解消**していた（反転帯で文字が欠けず、警告は 1 行）。ただし**この所見は参考であり合否に用いない**（中断走行の結果は合格にも不合格にも数えない）。生ログの機械可読な事実: `unload_clean` **1 行**・`connect_failed` **0 行**・`boot_input_ignored` **0 行**・`折返し基準が描画範囲の外` **1 行**・`OnMouseDoubleClick` **4 行**・`choice_count=3` **10 行**・`choice_selected` **11 行**（選択肢 ID は `Onメインメニュー`・`Onメニュー閉じる`・`Onエモの位置調整メニュー`・`Onエモの位置調整選択`・`Onしゃべくりおしゃべり`・`Onおしゃべり頻度メニュー`）・`apply_move_directive` **3 行**・`k_new` **4 行**（2.0→1.5 と 1.5→2.0・話者 0 と 1 の両方）。実効の拡大率は主モニタ **192**（作業領域 0,0,2880,1704）・副モニタ **144**。**A20 の終端 5 行**は §13.2 の行 7 に逐語で写した |
| 観測できていない項目（合否を付けない項目） | 20 項目すべて（中断走行は合格にも不合格にも数えない）。ただし `connect_failed` 0 行・`unload_clean` 1 行は項目 20 の ⑴・⑶ の証拠として、`boot_input_ignored` 0 行は §13.2 の行 4 の欠陥が発現しなかったことの根拠としては読める（requirements.md 改訂 5）——合否には載せない |
| やり直しの予定（条件を整えて再走するか） | **再走する**。終了指示を**直したコミット** `9ba2515f`（タスク 6.9）で **§2 の準備からやり直す**（永続状態の消去を含む）。記録置き場は当日 1 回目なので `C:\home\maz\lap-records\emo2-conformance-2026-09-07` とし（手順書 §2.4 の 4・§9）、2026-09-05・2026-09-06・同 `-2` の 3 つの置き場はいずれも中断の記録として**残す**（追記も上書きもしない）。§6 の宣言の 20 行と追 1〜追 4・追 3′・追 4′ は書き換えず、期待が変わった 1 行だけを **§6 の追記（2026-09-07・第 3 回・終了指示を直した後の採り直しの前）** に足した。改変の台帳は **§13.4 の改変 3** |

#### 中断 4 件目（走行 A・2026-09-07 20:01・解放の失敗を直す前）

| 欄 | 値 |
|---|---|
| 中断点（どの走行の何番目の段か・何が成立しなかったか） | 走行 A・**A20（項目 13 終了）の直後**。終了挨拶そのものは流れて目視では合格だったが、**解放（Unload）が脳との通信で失敗した**（症状 D・§13.2 の行 8）。2026-09-07 **20:01 開始・20:04 終了**。生ログは `C:\home\maz\lap-records\emo2-conformance-2026-09-07\lap-run-a.log` に在ったが、**2026-09-10 の時点でディスクから消えている**（本節の冒頭）。準備のコミットは `9ba2515f` |
| 中断の理由 | **開発者裁定「その場で直す」**（2026-09-07）。項目 13 の証跡は `event="unload_clean"` が 1 行のはずが、`event="unload_failed"` 1 行・`unload_clean` 0 行だった。R8.1（1 ウェーブで着地できるものは本仕様の中で直す）に当たると仕分け、**直してから §2 の準備をやり直す**という R8.3 後段の形で止めた |
| 観測できたところまで（成立した段と項目） | **終了挨拶は流れた**（開発者所見「合格」）。生ログの機械可読な事実: `method=GET id=OnClose`・`event="close_talk_start"`・`event="talk_done_quit"`・`event="ghost_quit"` が出ており、`event="force_quit"` は **0 行**——2026-09-07 18:30 の走行を止めた症状 C（終了指示が終了挨拶を通らない）は解消していた。**この所見は参考であり合否に用いない**（中断走行の結果は合格にも不合格にも数えない）。同じ走行で**描画の重さ**も見えた——`perf(apply_show)` の中央値 78 ms（その前の走行は 41 ms）で、開発者所見は「異常に重い・ガクガク」である（症状 E・§13.2 の行 9） |
| 観測できていない項目（合否を付けない項目） | 20 項目すべて。項目 13 は**成立していない**（解放が失敗した）が、中断走行なので不合格としても数えない |
| **構造の申し送り**（R16.5） | 症状 D を調べる中で、**ホスト窓のスレッドが待機中にメッセージを取り出さない**構造が分かった。本仕様では直さず、**引受先は起票待ち**として §13.2 の行 10 に登記した（応答方向の直しで、本走行では発現しない） |
| やり直しの予定（条件を整えて再走するか） | **再走する**。解放の失敗を**直したコミット** `ce545350`（タスク 6.11）を含む `6cbf7c59`（あふれの送り量まで直した HEAD）で **§2 の準備からやり直す**（永続状態の消去を含む）。記録置き場は準備した日で `C:\home\maz\lap-records\emo2-conformance-2026-09-10` とする（手順書 §2.4 の 4・§9。**2026-09-07 の置き場は消えているので、同じ名前の続きは作れない**）。§6 の宣言の 20 行と追 1〜追 5 は書き換えず、新しく見るもの 3 行だけを **§6 の追記（2026-09-07・第 4 回・解放の失敗を直した後の採り直しの前）** に足した。改変の台帳は **§13.4 の改変 4** |

#### 中断 5 件目（走行 A・2026-09-07 20:27・バルーンの送りの起点を直す前）

| 欄 | 値 |
|---|---|
| 中断点（どの走行の何番目の段か・何が成立しなかったか） | 走行 A・**A3（項目 2 起動挨拶の再生）**。**A1〜A2 は成立**した。2026-09-07 **20:27 開始**——同じ日の 20:22〜20:27 に SSP と横に並べて見る診断のための走行を挟み、その直後に始めた採り直しの走行である。A3 の途中、起動挨拶の 4 往復目で**相方側バルーンが最新の 1 行しか見せていない**ことを開発者がスクリーンショット（20:27:58）で確かめ、そこで止めた（症状 F・§13.2 の行 11）。使ったビルドは **2026-09-07 20:01 の走行（4 件目）と同じもの**である——この日の製品コードの改変は 19:30 の `9ba2515f`（タスク 6.9）が最後で、そこから 20:27 までのコミットはいずれも文書だけの改変だった（`git log` で 2026-09-10 に確認）。したがって症状 D の直し（`ce545350`・同日 21:36）も症状 F の直し（`6cbf7c59`）も、この走行には入っていない |
| 中断の理由 | **開発者裁定「この不具合はこの spec で直す」**（2026-09-07）。所見は「emo 側のバルーンが 1 行しか表示されないことがある」から、スクリーンショットで「常に一番下の行しか表示されない。2 行以上のバルーン表示のはずなのに」へ確定した。R8.1（1 ウェーブで着地できるものは本仕様の中で直す）に当たると仕分け、**直してから §2 の準備をやり直す**という R8.3 後段の形で止めた |
| 観測できたところまで（成立した段と項目） | **A1〜A2 を通過**（起動と既定位置・バルーンの表示ライフサイクル）。A3 は途中で止めたので**成立していない**。スクリーンショットから読めた事実は 2 つ——本体側バルーンは「はじめましてや！！うちは、むらさき。」（2 行）＋段落の空き＋「そこ自分でいう‥‥。」の 3 行が正しく残っていたこと、相方側バルーンは「イイジャン！」だけが描画範囲の上から 45 px の位置に見え、直前の台詞「僕はエモ。クール系の可愛い娘。」（2 行）が見えなかったことである。**どちらも合否には数えない**（中断走行の結果は合格にも不合格にも数えない）。**生ログは置き場ごと消えている**（本節の冒頭）ので、行数や語の件数を数え直すことはできない——直前の診断のための走行（20:22〜20:27・出力水準に `areka_emo_text=debug` を足して採ったもの）で読んだ値、すなわち相方側で保留改行 1.5 倍が先頭に来ていたことと、あふれの判定が `first_visible_line=1 total_lines=2` → `=2/3` と動いていたことも、本書へ写したこの値だけが残っている（§13.2 の行 11 の根拠） |
| 観測できていない項目（合否を付けない項目） | 20 項目すべて（中断走行は合格にも不合格にも数えない）。項目 2 は**成立していない**が、中断走行なので不合格としても数えない |
| やり直しの予定（条件を整えて再走するか） | **再走する**。送りの起点を**直したコミット** `6cbf7c59`（タスク 6.13）で **§2 の準備からやり直す**（永続状態の消去を含む）。準備はタスク 6.12 が 2026-09-10 に行い、記録置き場は `C:\home\maz\lap-records\emo2-conformance-2026-09-10` である（手順書 §2.4 の 4・§9）。§6 の宣言の 20 行と追 1〜追 8 は書き換えず、新しく見るもの 2 行だけを **§6 の追記（2026-09-07・第 5 回・送りの起点を直した後の採り直しの前）** に足した。改変の台帳は **§13.4 の改変 5** |


#### 中断 6 件目（走行 A・2026-09-10 22:14・**製品の症状ではない**）

**中断点**: 走行 A の A2〜A3 のあたり（項目を 1 つも確定していない）。2026-09-10 **22:14:06 開始・22:15:48 付近で途切れ**（生ログ `C:\home\maz\lap-records\emo2-conformance-2026-09-10\lap-run-a.log`・**551 行**・`times.txt` に `exit` の記録が無い）。準備のコミットは `6cbf7c59`（実行体は本走行と同じもの）。**中断の理由**: 起動元の Claude セッションが再起動し、背景で走っていた `lap.ps1` ごと落ちた——**製品の症状ではなく準備者の過失**である。同じ時刻の機械の負荷は CPU 90%（別リポジトリ `evernote_export` の `cargo test`・`OneDrive.Sync`・`claude` 2 プロセス）で、`perf(apply_show)` の外れ（`cache_hit=false`）の中央値は 306 ms（全段が一様に約 2.5 倍＝CPU の取り合いの型）であった。**観測できたところまで**: 起動と最初の挨拶だけで、**20 項目とも合格にも不合格にも数えない**。**やり直し**: 同じ日のうちに置き場を `emo2-conformance-2026-09-10-2`（同じ日の 2 回目）へ新しく作り、22:21:19 に事前確認をやり直してから 22:21:31 に走り直した＝**本記録の走行 A** である。1 回目の置き場も消さずに残してある（手順書 §2.4 の 4）。§6 の宣言は書き換えていない。

> **走行ではない起動が 1 件ある（記録の混同を避けるための注記）。** 2026-09-06 22:26:48〜22:56:52 に同じ置き場（`…-2026-09-06-2`）で `lap.ps1 -Run A` を 1 度回したが、**操作を 1 つも行わず 30 分の有界自動終了で終えた**もので、項目を 1 つも観測していない。生ログは同じ置き場の `lap-run-a.idle-2226.log` へ退避してあり（`times.txt` に退避の 1 行が残る・7,399 行・`OnMouseMove` 0 件）、**走行にも中断にも数えない**。上の 3 件目が指すのは 2026-09-07 18:30 の走行である。

§6 の宣言を走行後に書き換えた場合の記載: **該当なし**。開発者は 2026-09-10 22:2x に §6 を読み、宣言 20 行と 追 1〜追 10 を **1 行も書き換えなかった**（§6 の末尾に、手元作業を済ませた事実の記録として 1 段だけ足してある）。走行の後に宣言や追記へ手を入れてもいない。

### 13.4 製品コードの改変の台帳（R8.6）

2026-09-06 の第 2 回改訂で、本仕様は「走行で見つかった症状のうち 1 ウェーブで着地できるものはその場で直す」ことになった（R8.1）。**直した 7 件を下表に登記する**（改変 1・2 は 2026-09-06・改変 3・4 は 2026-09-07・改変 5・6 は 2026-09-10・改変 7 は 2026-09-11）。走行前後で見える挙動の差は、**直した症状そのものだけ**である。

| # | 改変 | 範囲（ファイル） | 理由 | 走行の前後で見える挙動の差 | コミット |
|---|---|---|---|---|---|
| 1 | **反転帯を行ボックスの中央へ寄せる**（症状 A・§13.2 の行 5・R13） | `crates/areka-emo-text/src/choice.rs`（純関数 `highlight_band_offset` を新設し、`derive_hit_rows`／`decorate_canvas` に引数を 1 つ足す）・`canvas.rs`（`ChoiceLineContent` に `band_offset`）・`viewbox_draw.rs`（`highlight_rect` の起点と、ダーティ帯の超過分に寄せを加算）・`actor.rs`（帯の丈を決めた直後に寄せを 1 度だけ決め、描画とヒットへ配る）。試験は兄弟 5 本と `tests/` の読み戻し 3 本（許容量の定数 `BAND_OVERHANG_MAX` を 2 → **0**）。`draw.rs`・`region.rs`・バルーン fixture は**未接触** | 走行 A で開発者が「反転位置が上すぎる」と判じ、上流 spec の「はみ出し 2 画素は許容」という裁定を覆した。機序は、DirectWrite が行ボックス（28 で 37.24）の中でインクを中央寄りに置くのに、帯（丈 30）を行の近端に揃えて置いていたこと。**帯を広げると隣の行の帯と重なる**ので広げず、中央へ寄せた（寄せ＝(37.24−30)/2 → 4・font 20 では 2・既定フォントでは 0） | **直した症状だけ**。帯が 4 画素下がり、上の余白 7 → 3・下の切れ 2 → **0**（読み戻し: 28 は帯 y4..33／字 y7..31、emo2 fixture は塗り y34..63／字 y37..61、font 20 は帯 y24..45／字 y28..44）。帯の丈と当たり範囲は帯と一緒に下がって一致したままで、隣の行とは接するが重ならない。行ボックス丈が em に等しい既定フォントでは寄せが 0 になるので、既定フォントの byte 等価検査 9 本は **1 バイトも動かない**。レビューでは 3 通りの摂動（寄せを 0 にする／ダーティ帯の寄せを外す／縦書きの寄せを外す）で各検査が赤になることを確かめた | `6450ccad` |
| 2 | **折返しの警告を装着の口へ移す**（症状 B・§13.2 の行 6・R14） | `crates/areka-emo-text/src/region.rs`（粗さの `warn!` を撤去・`inline_axis_name` を新設・`BALLOON_NAME_PLACEHOLDER` を `pub(crate)` へ）・`actor.rs`（`warn_coarse_wrap_threshold` を新設し、`register_actor` が解決済み領域を上書きする前に 1 度呼ぶ）・`region_inline_limit_tests.rs`（解決関数は 0 件へ引き直し）・新規 `actor_region_warn_tests.rs`（6 本・各本が対照の記録を 1 件数える） | 再追従が churn ガードの**前**に毎フレーム解決関数を呼び、警告がその解決関数の中に在ったため、走行 A の生ログ 30,837 行のうち **27,908 行**が同じ 1 行になっていた。「装着 1 回につき 1 件」という意味を持つ**登録口**へ移す | **直した症状だけ**。同じ警告が毎フレーム繰り返されなくなる。**文言と 4 つの欄はバイト同一**（SHA-256 一致）で、出る条件（折返し基準が描画範囲の遠辺より外）も不変。画面の見た目と当たり判定は 1 画素も変わらない。手順書 §5.7 と本記録 §7・§14 の**出所の記載だけ**を新しい場所（`actor.rs` の `warn_coarse_wrap_threshold`）へ改めた | `a9aef152` |
| 3 | **終了指示を kanade の正規の握手へつなぎ、握手が終わったら窓を閉じる**（症状 C・§13.2 の行 7・R15） | `crates/areka/src/input_events/mod.rs`（終了操作の分岐を 2 つに割る・`MouseWiring::send_close_request` を新設）・`placement/spawn.rs`（`despawn_ghost_windows` を新設し、強制退避と終了相で共通に使う）・`emo2_boot/mod.rs`（`wire_emo2_boot` が停止通知の通り道を 1 本作る）・`emo2_boot/frame.rs`（終了相 `run_ghost_quit_phase` を新設し、相順の**先頭**へ置く）・`emo2_boot/frame/wiring.rs`（受信端を受け取る口 `set_kanade_stop`）・`emo2_boot/spine.rs`（接続宣言と結線で 968 行・1 ファイル 1,000 行の見張りの内）・`crates/areka-kanade/src/msg.rs`（停止通知の型 `KanadeStopCause`／`KanadeStopped`）・同 `actor.rs`（停止通知つきの派生 spawn `spawn_kanade_with_stop_sink`・`Action::StopSelf` の実行点で `notify_stop`）・同 `lib.rs`・`crates/areka-ghost/src/runtime.rs`（派生関数 `boot_with_kanade_stop`）・同 `lib.rs`。試験は 4 群 15 本を新設（入力層 2・kanade 単体 5・kanade 統合 3・終了相 4・spine 1。ほかに既存 2 本の意味を強めた）。`main.rs`・`boot_config.rs`・一周テストの 3 台帳・バルーン fixture・行数の見張りの例外表は**未接触** | 終了操作が「全ゴースト窓を消す」暫定の退避のままで、kanade に実装済みの正規の握手（`OnClose` GET → 辞書の終了挨拶 → `\-` で解放）へ入る道が無く、握手が終わっても窓を閉じる者がいなかった。**欠けていたのは 2 か所**であり、どちらも配線である | **直した症状だけ**——項目 13 のみが変わる。Ctrl+左ダブルクリックで終了挨拶が流れ、終わってから窓が自分で閉じる。生ログは `method=GET id=OnClose`・`event="close_talk_start"`・`event="ghost_quit"`・`event="unload_clean"` が**それぞれ 1 行**で、`event="force_quit"` は **0 行**である（改変前は `event="force_quit"` → `method=NOTIFY id=OnClose` の順で、GET も終了挨拶も無かった）。**起動に失敗して結線前のときは従来どおり Ctrl+左ダブルクリックが強制退避**であり、起動後の強制退避は Ctrl+**Shift**+左ダブルクリックへ分かれた。辞書が終了を拒む（応答が `\-` で終わらない）ときは停止通知が出ず窓が残るのは正典どおりである。レビューでは 3 通りの摂動（分岐を退避へ戻す／停止通知を外す／終了相を無操作にする）で各検査が赤になることを確かめた | `9ba2515f` |
| 4 | **応答方向の送出から「応答なしなら打ち切る」旗を外す**（症状 D・§13.2 の行 8・R16） | `crates/shiori-host32-ipc/src/lib.rs`（向きを表す `SendFlavor`〔`Request`／`Response`〕と、旗を返す純関数 `send_flags` を新設。`send_copydata` は要求方向の薄い包みとして署名を変えず、応答方向は `send_copydata_response` を新設し、共通の本体 `send_copydata_with` へ寄せた。構造の試験 4 本）・`crates/shiori-host32-helper/src/main.rs`（応答の 3 か所＝要求への応答・load-ack・unload-ack を応答方向へ。送出に失敗したときの記録に `GetLastError` を併記。HELLO は要求方向のまま）・新規 `main_response_flavor_hung_cage_tests.rs`（結合の試験・所要約 40 秒・上限 90 秒）。`shiori-host32-host`・既存の loopback 試験・要求方向の呼び手は**未接触** | 走行 A で終了挨拶の後の解放が失敗した（§13.2 の行 8）。応答の宛先は**自分の送出の中で必ず待っている相手**なので、「応答なし」判定でこの向きの配送を打ち切るのは誤検知にしかならない。**較正の経緯**——最初は起動直後に 20 秒待って「届く」と反証されたが、それは猶予期間の内だった。調査係が「20 秒待つ → 往復① → さらに 20 秒待つ → 往復②」で直す前の赤（往復②の応答が届かない）を作り、5.5 秒／10 秒の待ちでは試験が恒真の緑になることも示した | **直した症状だけ**。終了挨拶の後に `event="unload_clean"` が 1 行出て、`event="unload_failed"` が 0 行になる（項目 13）。要求方向の旗・HELLO・LOAD・毎秒の往復は 1 文字も変えていない。レビューは応答方向へ旗を戻す変異で、構造の試験 3 本と結合の試験が赤になることを再現して確かめた。⚠ `cargo test -p shiori-host32-helper` の既定の所要が 40 秒になる（常設の遅い試験・設計どおり） | `ce545350` |
| 5 | **あふれたときの送り量の起点を、最初の行の開始側から描画範囲の開始側へ移す**（症状 F・§13.2 の行 11・R17） | `crates/areka-emo-text/src/layout.rs`（`LayoutEngine::visible_window` の起点を `near(&lines[0])`〔最初の行の開始側〕から**描画範囲の開始側**へ。軸の読み替えは `layout` と同じ表を使う——横書きは `region.start()` の縦座標、縦書き右流しはその横座標の符号を反転したもの、縦書き左流しは横座標そのもの。`visible_window` と `VisibleWindow::block_offset` の説明文も「スキップした行の位置差そのもの」から「先頭可視行の開始側と描画範囲の開始側の差」へ直した）。試験は `layout_visible_window_tests.rs` に 2 本（`leading_gap_scrolls_from_region_start_not_from_first_line`＝直す前は 2 行スキップ／−60、直した後は 0 行／−45。`vertical_rl_leading_gap_scrolls_from_region_start`＝2／+60 → 0／+45。どちらも**冒頭の空きが無い対照を同じ試験の中に置いて**、値が直す前後で動かないことを一緒に固定した）と、`actor_scroll_retain_tests.rs` に 2 本（`present_actor_leading_gap_keeps_earlier_lines_visible_k1`／`_k2`＝台本の先頭が 1.5 行ぶんの改行で、直す前は最初の台詞の帯にインクが無い）。2026-09-07 に書いた 5 本（冒頭の改行を含まない台本）は主張を変えずに残した——差分描画と再生の相という 2 つの疑いを外した証拠だからである。`draw.rs`・`viewbox.rs`・バルーン fixture は**未接触** | 相方側バルーンが最新の 1 行しか見せなかった（症状 F）。台詞の冒頭にある 1.5 行ぶんの空きは起点より前にあるため送る候補に入らず、「1 行送る」しか選べないので、収まるはずの行が描画範囲の外へ押し出されていた。起点を描画範囲の開始側に取れば「0 行送って空きだけを詰める」が選べる | **直した症状だけ**。値が変わるのは**冒頭に空きのある台本のときだけ**で、相方側の起動挨拶では 3 行が 40／70／100..130 に収まって見える。**空きの無い入力では起点が最初の行の開始側と一致する**ので、送り量は 1 バイトも変わらない——レビュー係が式から独立に導き直して確かめ、`layout_visible_window_tests.rs` の既存 10 本と読み戻し検査（`live_diff`・`oracle_regression`・`line_pitch_readback`・`kero_menu_capacity`）は無改変で緑だった（`cargo test -p areka-emo-text` は単体 538 本と統合が全緑・`cargo test -p areka --bin areka` は 1,570 本が緑） | `6cbf7c59` |
| 6 | **正典に無い括弧形 `\w[n]` を待ちとして読むのをやめる**（症状 G の areka 側・§13.2 の行 12・R18） | `crates/areka-parsers/src/sakura/decode.rs`（`decode_tag` の `"w"` の腕と `wait_from_arg` を**削るだけ**——括弧形は既定の `decode_passthrough_tag` へ落ちて `Raw` になる。`\wN` 用の `wait_units`／`WAIT_UNIT_MS` と `\_w[ms]` の腕は無改変。モジュール doc・`WAIT_UNIT_MS`・`wait_units` の説明文から括弧形の記述を消した）・`decode_tests.rs`（`wait_bracket_n_times_50ms` を `wait_bracket_form_is_raw_not_wait` へ書き換え。`\w[2]` → `Raw(r"\w[2]")` を固定し、正典 2 形〔`\w2`＝100 ms・`\_w[450]`＝450 ms〕を同じ檻で対照）・`model.rs` と `parse_tests.rs`・`validation_tests.rs`（括弧形を使っていた台本と説明文を正典形へ）・括弧形を台本に使っていた試験 8 ファイル（`crates/areka-sakura/src/{compile_sheet,drive_choice,drive_delivery,drive_lifecycle}_tests.rs`・`crates/areka-ghost/src/{dispatcher_choice,dispatcher_slot}_tests.rs`・`crates/areka-ghost/tests/ghost/spine_e2e_test_s5_close_deadline.rs`・`crates/areka/src/emo2_boot/spine_talk_close_tests.rs`）で `\w[n]` → **同じ待ち時間**の `\_w[n×50]`（`\w[2]`→`\_w[100]`・`\w[999999]`→`\_w[49999950]` など。主張と期待値は 1 つも変えていない）・`crates/areka/src/emo2_boot/talk_lifecycle_tests.rs`（説明文のみ）・`doc/emo2-conformance-scope.md`・`doc/PASTA_PROFILE.md`（注記を追加）。**lexer は未接触**（`\w[2]` を正準タグとして切る構造は残る＝`Raw` の中身が元の綴りのまま復元できる） | 走行 A の A20 で終了挨拶の後に 15 秒待った（§13.2 の行 12）。ukadoc のウェイトは `\w時間`（1〜9・n × 50 ms）と `\_w[時間]`（絶対 ms）の 2 形だけで、括弧形 `\w[n]` は**正典に無い**。areka が正典に無い綴りを発明して読むと、台本側の綴りの誤りが areka でだけ別の意味（15 秒の待ち）になる。**削除が正しい直しである**（開発者裁定・2026-09-10） | **直した症状だけ**。pasta が `\w[300]` を出す間、終了挨拶の後の待ちが **15 秒 → 0 秒**になる（上流が `\_w[300]` へ直せば 0.3 秒）。ほかに変わるものは無い——正典の `\wN`（短縮形）と `\_w[ms]` の解釈は 1 文字も触っておらず、括弧形を使っていた試験台本はすべて**同じ待ち時間**の `\_w` 形へ書き換えたので、期待値・発火時刻・horizon はどれも同じ値のままである。`cargo test -p areka-parsers -p areka-sakura -p areka-ghost` と `cargo test -p areka --bin areka`（1,570 本）が緑 | `86d050de` |
| 7 | **会話中の 204 を「想定外」の警告から外す**（症状 H・§13.2 の行 14・R19） | `crates/areka-kanade/src/schedule/steady.rs`（`Phase::Steady{talk: Some}` の `match outcome` に `ShioriOutcome::NoContent => (state, Vec::new())` の 1 腕を足すだけ。`Notified`／`Value`／防御アームと `Steady{talk: None}` の腕は無改変）・`steady_flow_tests.rs`（檻 1 本を新設）。ほかのファイルは**未接触** | 会話中（`status="talking"`）の撫でで発行した `OnMouseMove` 照会に台本が無い（204）のは**普通の応答**なのに、`Steady{Some}` の腕がそれを扱わず防御アームへ落として警告にしていた。生ログの警告の数がそのまま異常の数になるようにする | **見える挙動の差は無い**（直した症状は記録の上だけのものである）。204 を受けたときの結果は直す前後とも「状態を保って何もしない」で同じであり、変わるのは警告 1 行が出なくなることだけである。`cargo test -p areka-kanade`（285＋50 本）と `cargo test -p areka --bin areka` が緑 | ``e6c210e5`` |

> **7 件とも、直した症状を除けば走行の前後で見えるものは変わらない。** 1 は既定フォントで寄せが 0 になること（byte 等価検査が 1 バイトも動かない）が、2 は文言と 4 欄がバイト同一で出る条件も変わらないことが、3 は一周テストの 3 台帳の期待が 1 バイトも動かないこと（注入は元から終了指示を送っている）が、その証拠である。編集集合に `crates/areka-emo-text/` を加えたのは第 2 回改訂の 1（R12.1）、`crates/areka/src/input_events/`・`crates/areka/src/emo2_boot/`・`crates/areka-kanade/src/actor.rs`・`crates/areka-ghost/src/runtime.rs` を加えたのは第 3 回改訂の 5 による。4 は、要求方向の旗・HELLO・LOAD・毎秒の往復が 1 文字も変わっていないこと（応答方向へ旗を戻す変異で試験が赤くなることも確かめてある）がその証拠であり、編集集合に `crates/shiori-host32-ipc/`・`crates/shiori-host32-helper/`・`crates/shiori-host32-host/` を加えたのは第 4 回改訂の 4 による。5 は、**冒頭に空きの無い入力では起点が最初の行の開始側と一致して送り量が同じ値になる**こと——だから既存の読み戻し検査と `layout_visible_window_tests.rs` の既存の期待値が 1 つも動かず無改変で緑である——がその証拠であり、編集集合は第 2 回改訂で加えた `crates/areka-emo-text/` の中で足りている（第 5 回改訂の 5・追加なし）。6 は、**正典の `\wN`／`\_w[ms]` の解釈を 1 文字も触っておらず、括弧形を使っていた試験台本をすべて同じ待ち時間の `\_w` 形へ書き換えた**こと——だから期待値・発火時刻・horizon が 1 つも動かない——がその証拠であり、編集集合に `crates/areka-parsers/`・括弧形を台本に使っていた試験ファイル・`doc/emo2-conformance-scope.md`・`doc/PASTA_PROFILE.md` を加えたのは第 6 回改訂による。 7 は、**204 を受けたときの結果が直す前後とも「状態を保って何もしない」で同じ**であること——変わるのは警告 1 行が出なくなることだけである——がその証拠であり、編集集合に `crates/areka-kanade/src/schedule/` を加えたのは第 7 回改訂による。

---

## 14. 上流の引き渡し文書の消化（2026-09-06）と、旧前提の点検（履歴）

上流 spec `areka-P0-emo-text-line-height-canon` の引き渡し文書（`.kiro/specs/completed/areka-P0-emo-text-line-height-canon/verification/handoff.md`）は 3 つの表——表 1「変わったもの」9 行・表 2「変わらないもの」8 行・表 3「注意」6 行——の計 **23 行**から成る。**1 行ずつ当たり**、本仕様に関わる行は写した先を、関わらない行はその理由を書いた。**本節は 2026-09-11 に短くまとめ直した**（タスク 6.2——§9 へ機械判定の出力を全文貼るため、23 行の表と 4 回ぶんの確認の表を、行・件数・素性を落とさずに箇条書きへ畳んだ。数え直しはしておらず、値はいずれも当時数えたものである）。

- **関わる 12 行と写した先**。**表 1 #1**（行送り 35 → **30**・式は `font.height + 2`＝`crates/areka-emo-text/src/state.rs:78` の `line_pitch`・既定の行間 2 は同 `:68`）＝以下 3 行の前提であり §13.2 の #1 の解消の根拠。写した先は §6 の 追 1・追 2 と §13.2 の #1／#2 の「引受先が実在することの確認」欄。**#2**（相方側バルーンのメニューで先頭の選択肢が落ちない）＝§6 の 追 1・手順書 §5.2 の A14（§13.2 の #1 はこれで解消）。**#3**（「閉じる」の右端が欠けない・実フォントの文字送りで x164..≈233 ≤ 240）＝同じ 2 か所（§13.2 の #2 はこれで解消）。**#4**（本体側バルーン `emo2` の行容量 3 → **4 行**）＝§6 の 追 2（項目 2 の期待そのものは変えていない）。**#6**（折返し基準が描画範囲の外のバルーンで読み込み時に警告 1 件）＝手順書 §5.7 の語の表と §6 の 追 4 → **2026-09-06 第 2 回で直した**（§13.2 の行 6・§13.4 の改変 2。「1 件」の意味は変わらないが上流の実装では毎フレーム出ていた。読み方は「装着ごとに 1 件・2 行以上は退行」＝追 4′、出所は `actor.rs` の `warn_coarse_wrap_threshold` へ改めた）。**#7**（反転帯からのインクのはみ出し最大 2 画素を許容・第 2 回裁定）＝§13.1 の行 5・手順書 §5.5 の C1・§6 の 追 3 → **同日に覆された**（§13.2 の行 5・§13.4 の改変 1。R7.7 は廃止し、帯を中央へ寄せてはみ出し 0 にした。写した先 3 か所はいずれも新しい期待へ改めてある）。**表 2 #4**（本体側バルーンの折返し位置は不変）と **#5**（`\c` の全消去・比率つき改行の意味・reveal のペースは不変）＝§6 の 追 2 の但し書き「項目 2 の期待そのもの——1 文字ずつ流れ、待ちと改行と表情の切替が台本どおり——は変わらない」の根拠。**#7**（ハイライト帯の式・面内 blit・露出帯と残滓の導出は不変）＝§13.1 の行 5 の「帯を広げる是正は上流が却下済み」の裏づけ → 第 2 回で直した後も**式そのものは不変**で、変えたのは帯を置く起点だけである（ダーティ帯は寄せたぶん遠端側へ伸ばした＝§13.4 の改変 1）。**表 3 #2**（警告の `balloon` 欄はバルーン名ではなく定数の代替値・引受先は `areka-P0-balloon-canon-residue` の項目 14）＝手順書 §5.7 の読み方（名前で絞り込まない・項目 14 の不合格理由にしない）→ 定数の定義は `region.rs` に残したまま `pub(crate)` にして `actor.rs` の警告が使う形になった。**#3**（要件の「行」は視覚上の段・配置層の「行」は行矩形）＝§6 の 追 1・追 2 が数える「3 つの選択肢」「4 行」は**段**の数であり、生ログや決定論層の行矩形の本数とは一致しない前提で突き合わせる。**#6**（`spine_conformance_script.rs` の説明文が行送り 35px を現在形で述べている＝再走時の申し送り）＝**消化済み**（タスク 5.8・コミット `6a2343a4`・期待値は 1 つも動かしていない）。
- **関わらない 11 行とその理由**。表 1 **#5**（描画範囲の遠辺で無条件に折り返す絶対上限の新設）と表 3 **#1**（`\_l` が遠辺付近へ跳ぶと折り返す）＝20 項目に折返しの位置そのものを判定する項目が無い（本走行で見える差は表 1 #2・#3・#6 として追記へ写してある）。表 1 **#8**（スクロール 1 段あたりの描画呼び出し 16 → 5・画素は不変）＝本仕様の 3 つの台帳がこの量を運ばず、20 項目にも描画呼び出し数を見る項目が無い。表 1 **#9**（観測用 example `emo-text-layer` が横書き・縦書きとも `PASS`）と表 3 **#5**（その example の実走手順）＝上流 spec 側の手動確認であり走行 A〜D の手順に含まれない。表 2 **#1**（文字の大きさとベースラインは不変）・**#2**（`\_l` の語彙・原点・書字方向ごとの解決規則は不変＝`lh` の値だけが 30 へ追随）・**#3**（あふれ判定の式は不変）＝見える差が無く、§6 の既存 20 行を書き換える理由にならない。表 2 **#6**（バルーン fixture・kanade・pasta・sakura は無改変）＝本仕様の編集集合の外で、決定論層の期待列が動かないことは実測で確定済み。表 2 **#8**（行数の見張りの例外表は 1 行も変えていない）＝製品コード側の見張りであり本仕様の 2 文書の 1,000 行以下という制約とは別のもの。表 3 **#4**（`draw.rs` は 988 行・上限まで残り 12 行）＝本仕様はこの余白を消費しない。
- **旧前提が現在形で残っていないことの確認（5 回ぶんの履歴・毎回とも両文書が 1,000 行以下であることも確かめた）**。⑴ **2026-09-06**（`35px`・`1.25`・「3 行」）＝現在形の旧前提 **0 件**。残ったのは履歴として残すと決めた §13.2 の #1 の本文・本節 表 3 #6・別の意味の語（「3 行目」「3 行削除」「3 行から 4 行へ」）の 3 種だけである。⑵ **2026-09-06 第 2 回**（22:1x・4 語）＝旧い出所（`region.rs` を行番号つきで指す書き方）**0 件**・`1 件出るのが正常` **3 件**（§6 の 追 4／その旧行が当てはまらないと述べる但し書き／確認表の行。差し替えは同じ節の 追 4′）・`2 画素` **11 件**（記録 10・手順書 1。内訳は 書き換えない約束のある旧行 1／覆されたと述べる新しい記述 2／覆されたと注記した消化の表 3／直す前の観測と直す前後の実測 3／手順書 §5.5 の新本文 1／確認表 1）・`emo2-conformance-2026-09-06` に `-2` が続かないもの **6 件**（うち 5 件は同日 1 回目を中断の記録として残すと述べる文・当時の走行に使うパスはすべて `-2`）。**現在の規則として 2 画素のはみ出しを許す記述は 0 件**。⑶ **2026-09-07 第 3 回**（19:4x・3 語）＝`暫定退避` **2 件**（§13.2 の行 7 の過去形の説明と確認表の行。手順書は 0 件）・`emo2-conformance-2026-09-06-2` **5 件**（写し元・中断した走行の生ログの在り処 2・当時の記述・確認表。当時の走行に使うパスは `emo2-conformance-2026-09-07`）・`窓を閉じ、終了の手順` **0 件**。⑷ **2026-09-10**（20:3x・3 語）＝`emo2-conformance-2026-09-07` を**走行に使うパスとして**述べる記述は両文書とも **0 件**（残る 6 件は当時の在り処 1・当時の記述 1・確認表 4 で、前 2 者は §13.3 の冒頭が「消えている」と上書きしている）・「前の回の置き場は 1 つも消さない」を現在の状態として述べる記述 **0 件**（約束はこの置き場から先に掛かると書き改めた）・`unload_failed` の現在形 **0 件**（期待値 0 行として書いてある）。⑸ **2026-09-10・症状 F**（3 語）＝`最新の 1 行` の現在形 **0 行**（残り 8 行は過去形 4／走行でこうなっていたら止めるという条件付きの確認 3／確認表 1）・`差分描画` **3 行**（すべて「本命と疑ったが外れである」と述べる側で、原因として述べているものは 0 行）・`一番下の行` **2 行**（日付つきで引いた 2026-09-07 の所見と確認表）。各回の「新しい記述が在ることの確認」は、記録では §13.2 の行 5〜13・§13.4 の改変 1〜5・§13.3 の中断 2〜5 件目・§6 の追記 5 本（第 2〜第 5 回）・§7 の観測点の行・§1 の同定の 1 文で、手順書では §3.5・§5.2 の A3・§5.5 の C1・§5.7 の語の表と読み方で満たしている。⑹ **2026-09-11（本走行の記録・タスク 6.2）**＝⑸ の時点で両文書が指していた記録置き場 `emo2-conformance-2026-09-10` は、**走行に使うパスとしては本記録に 0 件**になった（1 回目の置き場は §13.3 の 6 件目と §5 が「中断した走行の生ログの在り処」として指すだけである）。走行・機械判定・起動スクリプトに使うパスは**すべて `emo2-conformance-2026-09-10-2`** である（§4・§5・§5.1・§9 の 9 か所）。手順書の側の置き場の記述は本タスクでは触っていない（手順書は本タスクの編集の対象外）。

---

## 15. 編集集合の非回帰（タスク 7.1・2026-09-11）

**測り方**: `git merge-base origin/main HEAD` ＝ `6a6ea1b5`。`git diff --name-only 6a6ea1b5 HEAD` と `git diff --name-only origin/main...HEAD` は**同一の 72 パス**（差 0 行・タスク 7.1 の測定時点。以後の実数えは追補を見よ）。本ブランチは 2026-09-05 に PR #135（squash `b2ac9a35`）でいったん main へ入り、その後 main を数回取り込んでいるため、**三点差分が本ブランチの未統合の編集集合**である。PR #135 で入った分（事前登記済みの例外 2 件を含む）は既に main に在るので、この 72 パスには現れない。1,000 行の見張りに収めるため、表は**宣言の出所ごと**に束ねた（パスは 1 つも省いていない・分類は R12.1 と各改訂・タスクの Boundary で裏取りした）。

| パス | 分類 | 宣言の出所（要件の改訂 n／例外／討議） |
|---|---|---|
| `.kiro/specs/areka-P0-emo2-conformance-e2e/{requirements,design,tasks,brief}.md`・同 `verification/{acceptance-record,lap-procedure,m1-completion}.md`（**7**・タスク 7.1 の測定時点では 5） | 本仕様の記録・文書 | R12.1 本文「本仕様の記録」。**うち 2 つは 7.1 の測定の後に加わった**——`verification/m1-completion.md` は**タスク 7.2 が新規作成**したもので、設計が最初から名指ししている（`design.md:163` のファイル木「新規: 完成判定と宣言」・同 `:889` の D10 の置き場の欄）。`brief.md` は**タスク 7.3 が追記**したもので、宣言の出所は design D12 ⑷（引き受けた 3 件の登記先を本仕様の brief と定めた——送り元は完了置き場に在り、本仕様の編集集合の外だからである） |
| `.kiro/specs/areka-P0-{dpi-transition-two-tick-bounce,host32-window-thread-pump,kanade-boot-talkdone-drop,present-gpu-transform-scale}/brief.md`（4） | 引受先の起票（ディスカバリ出力・2026-09-07／09-11） | R8.5（引受先の無い症状を閉じない）・ロードマップ追記(94) |
| `.kiro/steering/roadmap.md`（1） | 正本の更新 | R11.2・R11.4／design「Modified Files」 |
| `crates/areka-emo-text/src/{choice,actor,canvas,region,layout,viewbox_draw}.rs` と兄弟試験 `{actor_choice_contract,actor_region_warn,actor_scroll_retain,choice_decorate,choice,layout_visible_window,region_inline_limit,viewbox_choice_marker,viewbox_draw_choice_hover,viewbox_draw_scroll_retain}_tests.rs`・`tests/{choice_fixture,emo2_fixture_e2e,line_pitch_readback}_test.rs`（19） | 宣言済みの編集集合 | 改訂 2（`crates/areka-emo-text/` を追加・R13／R14）・改訂 5 の 5（R17 は追加なしと明記） |
| `crates/areka/src/emo2_boot/{mod,frame,frame/wiring,spine,spine_conformance_script}.rs`・`{frame_ghost_quit,spine_close_wiring,spine_talk_close,talk_lifecycle}_tests.rs`（9） | 宣言済みの編集集合 | R12.1 本文（兄弟テスト＋`spine.rs`）・改訂 3・改訂 6／タスク 6.9・6.15 の Boundary |
| `crates/areka/src/input_events/{mod.rs,input_events_tests.rs}`・`crates/areka/src/placement/spawn.rs`（3） | 宣言済みの編集集合 | 改訂 3（R15 の終了指示の配線）／タスク 6.9 の Boundary |
| `crates/areka-kanade/src/{actor,msg,lib}.rs`・`{actor,actor_stop_notify}_tests.rs`・`tests/kanade/{close_test,close_test_stop_notify_tests,common/common_harness,common/mod}.rs`（9） | 宣言済みの編集集合 | 改訂 3（R15）／design「Modified Files」（`msg.rs` の `KanadeStopped`）／タスク 6.9 の Boundary「`{actor.rs, msg.rs, lib.rs}` と同 crate の試験」 |
| `crates/areka-kanade/src/schedule/{steady.rs,steady_flow_tests.rs}`（2） | 宣言済みの編集集合 | 改訂 7（`crates/areka-kanade/src/schedule/` を追加・R19）／タスク 6.16 の Boundary |
| `crates/areka-ghost/src/{runtime,lib}.rs`（2） | 宣言済みの編集集合 | 改訂 3（`runtime.rs`）／タスク 6.9 の Boundary と §13.4 の改変 3 の範囲（`lib.rs` は `boot_with_kanade_stop` の再輸出） |
| `crates/areka-ghost/src/{dispatcher_choice,dispatcher_slot}_tests.rs`・`tests/ghost/spine_e2e_test_s5_close_deadline.rs`・`crates/areka-sakura/src/{compile_sheet,drive_choice,drive_delivery,drive_lifecycle}_tests.rs`（7） | 宣言済みの編集集合（台本の書き換え） | 改訂 6（`crates/areka-sakura/`・`crates/areka-ghost/` の試験ファイル）／タスク 6.15 の Boundary「8 試験ファイル」 |
| `crates/areka-parsers/src/sakura/{decode,decode_tests,model,parse_tests,validation_tests}.rs`（5） | 宣言済みの編集集合 | 改訂 6（`crates/areka-parsers/` を追加・R18） |
| `crates/shiori-host32-ipc/src/lib.rs`・`crates/shiori-host32-helper/src/{main.rs,main_response_flavor_hung_cage_tests.rs}`（3） | 宣言済みの編集集合 | 改訂 4（R16・応答方向の送出とその試験）／design「Modified Files」 |
| `doc/emo2-conformance-scope.md`・`doc/PASTA_PROFILE.md`（2） | 実物定義の文書 | R12.1 本文「実物定義の文書」・R11.1／R11.2・改訂 6 |
| `crates/pilot/examples/shiori-host-32/fixtures/emo2/ghost/master/pasta.dll`（1） | fixture の差し替え（開発者提供） | 症状 G の上流側＝`act:wait` が `\_w[ms]` を出す上流ビルドへ更新（`1e90c743`・§13.2 の行 12） |
| 例外 R9.2（S3・S1 の 2 ファイル）・例外 R9.8（`wintf` の 2 ファイル）（0） | 事前登記済みの例外 | **本測定には 0 パス**——4 ファイルとも PR #135（`b2ac9a35`）で既に main に在り、三点差分には現れない |
| **宣言外**（0） | — | **0 件** |

**タスク 7.2・7.3 の追補（2026-09-11）**: 上の表はタスク 7.1 の測定（72 パス）のものであり、その後 **2 パスが加わった**——タスク 7.2 が `verification/m1-completion.md` を新規作成し、タスク 7.3 が `brief.md` へ追記した（どちらも分類は「本仕様の記録・文書」で、宣言の出所は上表の当該行に書いた）。タスク 7.3 が触ったほかの 3 パス（`.kiro/steering/roadmap.md`・`doc/emo2-conformance-scope.md`・`verification/lap-procedure.md`）は上表に既に在る。**よって編集集合は 74 パス＝宣言済み 74／宣言外 0 である。** **この 74 は引き算や足し算で導いたものではなく、実際に数えた値である**——`git diff --name-only $(git merge-base origin/main HEAD)`（**第 2 の参照を与えない**＝作業ツリーの変更を含む形）の出力行数が **74**、`… HEAD` を付けた形（作業ツリーを含まない）が **73** で、差は `brief.md` の 1 本だけである（`comm` で突き合わせて確かめた）。**完了置き場の文書は 1 バイトも触っていない。**

**合計**: **74 パス＝宣言済み 74／宣言外 0**（実数えの値・上の追補のとおり。タスク 7.1 の測定時点では 72 で、7.2 と 7.3 が 1 本ずつ加えた）。`.claude/` の変更は本ブランチの編集ではない（`.claude/skills/ponytail` の最終コミットは分岐点そのもの＝`6a6ea1b5`・main から取り込んだもの）。

**節構造の非回帰（R12.4・R12.5）**: 決定論層の合否は §11、実機層の合否は §12 と**独立した節**に分かれており、§11 の末尾は「§12（実機層）は 1 文字も埋めていない」、§12 の冒頭は「この欄は決定論層の欄とは別である」と互いに明記している（R12.4 の節構造で相 1／相 2 の境界を表す）。縮退は記録の 5 か所すべてに理由が付いている——§7「無し」（対になる語の点灯で裏取り）・§8.1 行 18 後半（拡大率遷移の後に隣接へ戻らない・開発者裁定で判断から外した）・§11.1（設計 D3 が名指しする変異では対照が作れず別の変異へ差し替えた）・§11 末尾の 3 件（新規ファイル数の食い違い・走行時間を中央値で判定・§12 は未記入）・§12（§8.1 と同じ 1 件）。記録の無い縮退は 0 件（R12.5）。
