# M1 完成判定

本書は M1 の完成判定を 1 か所に束ねる文書である（`design.md` の D10 完成判定・Requirement 10）。

完成判定は 3 つで構成する（R10.1）。

- ⑴ ワークスペース全体のテストが成功で終わること（§2）
- ⑵ 依存の許諾の検査と第三者告知の生成が通ること（§3）
- ⑶ 適合検証項目表 20 項目の実機サインオフ（§4）

3 つの根拠はいずれも本書の §2〜§4 から辿れる（R10.5）。**⑶ は人間の判断であり、AI 単独で完成を宣言しない**（R10.4・R5.10）。日付と時刻はすべて日本時間で書く。

---

## 0. 前提——32bit の橋渡し実行体（R10.3）

全体テストを回す前に、**32bit の橋渡し実行体を作って実バイナリの隣へ置く**必要がある。これが無いと全体テストは赤くなる。完了手続きの検査項目には無い前提なので、判定手順の側であるここに書く。

| 欄 | 値 |
|---|---|
| 置き場 | `C:\home\maz\git\areka\.claude\worktrees\areka-p0-emo2-conformance-7b2e56\target\debug\shiori-host32-helper.exe` |
| 走行の準備で置いた版 | 2026-09-10 20:28:52 に i686 版を作り直して複写し、同時刻に PE machine を読んで `014C`（32bit）を確認した（受入記録 `acceptance-record.md` §3） |
| 全体テストの後に確かめ直した版 | 全体テストは**同じ名前の x64 版で上書きする**ため、テストの後に i686 版（`target/i686-pc-windows-msvc/debug/` の成果物・再コンパイル無し）を複写し直し、PE machine が `014C` であることを読み直した（2026-09-11 10:1x） |

**この前提は 2 度満たす必要がある**——テストを回す前（テストを緑にするため）と、テストの後（実機走行に使えるものを隣へ戻すため）である。

---

## 1. 隔離裁定の状態（R10.7・D11）

間欠的な赤が残ったまま「全通過」と記録しないため、**隔離裁定を先に済ませてある**。

- タスク 4.1〜4.4（2026-09-04〜05）で、`wintf` の 2 系統を明示的に指定したときだけ走る形へ隔離し、S1・S3 の 2 本を「数える前に待つ」形へ更新した。裁定の一覧と理由は `verification/isolation-decision.md` に在る。
- **今回の全体テスト（§2）では間欠的な赤が 1 件も出ていない**。したがって「同一コミットで再走 1 回まで」の規定は発動しておらず、再走はしていない。D11 の裁定への差し戻しも生じていない。

---

## 2. ⑴ ワークスペース全体のテスト

| 欄 | 値 |
|---|---|
| コマンド | `cargo test --workspace --no-fail-fast` |
| 回したコミット | `dedf1f23`（タスク 6.5）。その後のコミット（タスク 7.1・7.2）は**文書だけ**であり、製品コードは `e6c210e5`（タスク 6.16）以降 1 行も変わっていない |
| 日時 | 2026-09-11 09:59:52 〜 10:03:36 |
| 結果 | **103 スイート・7,166 passed・0 failed・40 ignored・終了コード 0** |
| 生ログ | `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\workspace-test-2026-09-11.log`（7,860 行。末尾に `EXIT=0 end 2026-09-11T10:03:36` が在る） |
| 再走 | **無し**（赤が出なかったため・§1） |

**完了置き場へ移した後の再実行は本判定の範囲ではない。** 正の証跡は移した後の再実行であるが、それを回すのは完了手続き（`/kiro-complete`）の工程であり、本仕様の作業単位が持つのは**移す前の緑まで**である（D10 の手順 2・タスク 7.2 の但し書き）。

---

## 3. ⑵ 許諾の検査と第三者告知

**本リポジトリには設定ファイルが実在するため、実際に走らせた。**「設定不在により省略」（R10.2）は発動しない。

| 欄 | 値 |
|---|---|
| 設定ファイル | `deny.toml`・`about.toml`・`about.hbs`（いずれもリポジトリ直下に実在） |
| 許諾の検査 | `cargo deny check`（cargo-deny 0.20.2）→ **`advisories ok, bans ok, licenses ok, sources ok`** |
| その出力 | `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\cargo-deny-2026-09-11.txt` |
| 第三者告知の生成 | `cargo about generate about.hbs`（cargo-about v0.9.2・`--features cli` で導入）→ 終了コード 0 |
| 生成物 | `C:\home\maz\lap-records\emo2-conformance-2026-09-10-2\third-party-2026-09-11.html`（99,239 バイト） |

---

## 4. ⑶ 適合検証項目表 20 項目の実機サインオフ

判定の中身は受入記録 `verification/acceptance-record.md` に在る。本書はそこを指すだけで写しを作らない。

| 欄 | 参照先と内容 |
|---|---|
| 項目ごとの結果 | 記録 §8.1——**20 項目すべて合格**。縮退は 1 件（項目 18「二体の隣接」の後半。拡大率を 200% → 150% へ変えた後に二体の間が開いたまま隣接へ戻らない点を、開発者が 2026-09-11 に「これは許容する」と裁定して判断から外した。理由は同節に書いてある） |
| 実機層の総合判定 | 記録 §12——読み分けは **3 行目**、7 行の合否ブロックは記入済み（`ATOM-QUOTA: PASS`・`ATOM-NO-DRAG: PASS`・`DETERMINISTIC: PASS`）、項目 16 は合格、**実機層の総合判定は合格**（20 項目の実機サインオフ） |
| 判定した人間 | **開発者**（記録 §12）。AI は記録の作成のみを行った |

### サインオフ（人間の記入欄）

下の 1 文を開発者が確認し、署名と日付を入れた時点で ⑶ が確定する。**この欄は AI が埋めない**（R10.4・R5.10）。

> 適合検証項目表 20 項目の実機サインオフを確定し、M1 完成を宣言する。

サインオフ: **ekicyou 2026-09-11**（開発者が上の 1 文をそのまま返信して確定。転記のみ AI が行った）

---

## 5. 宣言

3 つの根拠は本書の §2（全体テスト）・§3（許諾の検査）・§4（実機サインオフ）に在り、**この 1 か所から辿れる**（R10.5）。

**M1 完成の宣言は、§4 のサインオフ欄に開発者の署名が入った時点で成立する。** ⑴ と ⑵ は本書の時点で満たされており（§2・§3）、⑶ は 2026-09-11 に開発者の署名が入った（§4）。**よって M1 完成をここに宣言する。** AI は根拠を揃えるところまでを行い、宣言そのものは人間の判断に委ねた。

なお**決定論層の合否は本判定とは別の層であり、記録 §11 に独立して置いてある**（R1.3）。§11 が合格であることは §4 の実機層を 1 文字も埋めない。逆も同じである。

---

## 6. 未達と引受先（R10.6）

宣言を妨げる未達は無い。下の 8 件は**持ち越し**であり、**いずれも判定結果（20 項目すべて合格）を書き換えない**。

| 持ち越した事項 | 理由 | 引受先 |
|---|---|---|
| 記録 §13.1 行 3——初回起動限定の位置調整が 2 回目以降の起動で既定配置へ戻るか | **未観測**。走行 A と走行 B のあいだに決定論のテストが走って保存された位置を消したため、本走行では確かめられなかった（記録 §8.1 行 12・§13.3 冒頭） | 既存の裁定（許容仕様）を維持し、**次の一周または開発者の個別確認**へ持ち越す。違和感があればその時点で個別の仕様を切る |
| 記録 §13.1 行 1——絵が新しい寸法になってから窓がその寸へ動くまでの遅れ | 判定に載せないと決まっている症状。走行 D で再現したが、開発者が 2026-09-10 に「拡大率の切替は頻繁に起こらないため許容」と裁定した | 別仕様 `areka-P0-dpi-transition-two-tick-bounce`（2026-09-11 起票） |
| 症状 E——拡大率 200% で 1 コマの適用が重い | 判定に載せない（走行の条件の差ではなく、絵が変わるコマの費用そのものの問題であると分かったため） | 別仕様 `areka-P0-present-gpu-transform-scale`（2026-09-11 起票） |
| 記録 §13.2 行 4——起動系列の途中に届いた再生完了の通知が捨てられる | 構造的だが**本走行では発現していない**（`event=boot_input_ignored` が 0 行・記録 §7） | 別仕様 `areka-P0-kanade-boot-talkdone-drop`（2026-09-11 起票） |
| 記録 §13.2 行 10——脳と話す窓のスレッドが待機中にメッセージを取り出さない | 構造的だが**本走行では発現しない**（症状 D を直した後の形では往復が成立している） | 別仕様 `areka-P0-host32-window-thread-pump`（2026-09-11 起票） |
| 記録 §13.2 行 13——話し始めに相方側バルーンの冒頭へ 1.5 行ぶんの空きが出る | 上流のゴーストが話者交替の改行をまだ空の相手側へ出すため。areka は正典どおりに保っている。上流の新しい DLL で解消する見込み | **上流のリポジトリ `ekicyou/pasta`**。解消したかどうかは次の一周で目視する |
| 隔離裁定 `verification/isolation-decision.md` §4.5.1 の留保——「**壁時計期限（`SPIN_WAIT`＝30 秒）で待つ常設の決定論テストの族——task 3.1 の負荷走行が赤を観測した `crates/areka/src/emo2_boot/spine_talk_close_tests.rs`（371 行）・同 `spine_text_scale_tests.rs`（169 行）・同 `spine_seriko_loop_tests.rs`（610 行）・同 `spine_display_tests.rs`（440 行）——は、高負荷で低確率に期限切れになりうる。A-2（`areka-P0-zorder-chain-residue/brief.md:16`）が名指しするのは 3 件だけで族の残りは名指しの外に居る。裁定は §2 の ⑷（触らない）に準じるが、表に行を持つのは `spine_talk_close_tests.rs` だけで残り 3 本は行を持たない。族の引受先は未定（§3.3.1）**」 | **高負荷で低確率に期限切れになりうる**が、今回の全体テスト（§2・7,166 passed）では赤 0 だった。門を付けて既定から外すのは裁定の無い隔離になるため行っていない（同 §4.5.1「推奨しない形 ⑴」） | 既存の台帳仕様 `areka-P0-zorder-chain-residue` の **A-2**。同仕様の `brief.md:16` へ本日（2026-09-11）族の 4 本のうち名指しの外に居た 3 本（`spine_text_scale_tests.rs`・`spine_seriko_loop_tests.rs`・`spine_display_tests.rs`）を追記した |
| R8.3 後半の未履行——タスク 6.15（`86d050de`＝括弧形 `\w[n]` の撤去）と 6.16（`e6c210e5`＝会話中の 204 の警告を外す）の**後に一周走行を採り直していない**（記録 §13.4 の行 6・行 7） | 6.16 は**記録の上だけの修正**で見える挙動の差が無く（同行 7）、6.15 の見える差（終了挨拶の後の 15 秒待ちが消える）は 2026-09-11 00:17 の終了確認の起動で 0.6 秒として目視済み（記録 §8.1 の行 13 の日付つき注記）。製品コードは `e6c210e5` 以降 1 行も動いていない。**ただしサインオフの根拠になった実行体は `6cbf7c59`** で、HEAD の製品コードより 2 コミット古い | **開発者の裁定待ち**——⑴ HEAD のビルドで走行 A〜C を採り直す、⑵ 上の理由で受容して本行を確定する、のいずれか。**本行は §4（サインオフ）と §5（3 点の根拠）を書き換えない**（20 項目の判定は `6cbf7c59` の走行に基づき、その走行の記録は §13.4 の 2 行で縮退として登記済みである） |

---

## 7. 次の段階の起点

**次の段階（実物を見て組み直す）の起点は本宣言である**（R11.6）。

完了手続き（`/kiro-complete`）は、本仕様を完了置き場へ移した後に全体テストを回し直す。**その再実行が正の証跡**であり、§2 の緑はその前段である（D10 の手順 2）。移した後の再実行で赤が出た場合は、本書の §2 を書き換えるのではなく、完了手続きの側の記録に残す。
