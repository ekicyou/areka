# emo2 適合走行 手順書（実機一周）

> **本書は「同じ条件の走行を、本書だけを読んで再現する」ための手順である。** 期待する結果は 1 行も持たない——期待値の正本は design.md の**適合検証項目表（20 項目）**であり、本書はその表を **項目番号と短い呼び名だけ**で参照する（R4.1）。
>
> - 対象仕様: `areka-P0-emo2-conformance-e2e`
> - 記録の様式: `verification/acceptance-record.md`（本書の各段で埋める欄をそのつど名指しする）
> - 手順の骨格: design.md **D5 実機走行の手順書と記録**「手順書の構成（先例の合成・research 決定 G）」の 8 段
> - 合否は**人間が確定する**。AI 単独で完成を宣言しない（R5.10）

---

## 0. 大前提（走行の前に読む）

### 0.1 消灯した観測点の沈黙を「起きなかったこと」の根拠に用いない（R5.7）

出力が 0 件であることは、それだけでは「起きなかった」を意味しない。**その観測点が本当に出力を出す状態だったのか**を先に確かめていなければ、0 件は「観測点が消えていた」と区別できない。

手順は次の 3 段で固定する。

1. **先に点灯を確かめる。** 判定に使うつもりの語ごとに、この走行の生ログの中に**その観測点が出した別の行**が 1 行でもあることを確かめ、記録の **§7 点灯確認**へ「観測点／点灯を確かめた方法／点灯していたか」を書く。
2. **確かめられたときだけ、0 件を根拠に使ってよい。**
3. **確かめられないときは、その 1 語だけを目視の宣言で代替し、代替したことを §7 に書く。** 代替を黙って行わない。代替を書かないまま次の項目へ進まない。

この規律が直に効くのは項目 20 の証跡 ⑴（接続失敗のログが 0 行）である。0 行を主張する欄なので、**同じ観測点が出す別の行**（後述 §5.6 の語の表）を先に見つけてから書く。

### 0.2 実測を目的とする他の仕様を並走させない（R5.9）

走行の間、この機械で**実測を目的とする別の仕様の走行を動かさない**。「動かしていないつもり」ではなく、**確かめる**。

この機械では複数の作業ツリーが同居しており（`git worktree list` で 10 本前後が並ぶ）、別のセッションがその一つで `cargo` を回していることがある。負荷は隣の作業ツリーからでも届く——本仕様の作業記録そのものが、高負荷のときに既存テストが名前つきで赤くなることを実測している（tasks.md「Implementation Notes」の負荷走行の項）。

確かめ方と記録:

1. `git worktree list` で作業ツリーを列挙し、
2. 走っている `cargo`／`rustc`／テスト実行体が 1 つも無いことを確かめ（**推測で `kill` しない**。自分が起こしたと確認できないプロセスは止めない）、
3. 記録の **§5 走行**の行「実測を目的とする他の仕様を並走させていないことの確認（R5.9）」へ、**確かめた方法と時刻**を書く。

**この作業ツリーで `cargo test` を回さない**ことも同じ理由で守る。決定論テストのゴースト検体は呼ばれるたびにゴースト側の永続状態を消す（`crates/areka/src/emo2_boot/spine.rs:483-489`）ため、走行 A と走行 B のあいだにテストを回すと**項目 12 の観測材料が消える**。

---

## 1. 証跡の分類（R5.4）

証跡は 3 種類あり、**混ぜて 1 つの欄に書かない**。記録 §8.1 の「根拠の種別」欄はこの 3 語のいずれかで埋める。

| 種別 | 何を書くか | 書いてはならないもの |
|---|---|---|
| **目視** | 誰が・いつ・何を見たか。段と操作を名指しする | 「たぶん出ていた」のような伝聞・推定 |
| **ログの引用** | 生ログの該当行の逐語と、その行が在るファイルと行番号 | 要約した言い換え |
| **機械判定の出力** | 判定の走行の出力（記録 §9 に全文を残し、§8.1 には結論の行を引く） | 出力を読んだ人の解釈 |

1 つの項目が 2 種以上の根拠を持つ場合は、**種別ごとに行を分ける**。3 語は記録 §0.2 の表と同じ語であり、**言い換えない**（言い換えると記録と手順書が静かにずれる）。

---

## 2. 準備

### 2.1 実行体を作り直す

サインオフの対象は**このコミットのビルド**である。古い実行体を黙って測ると「直っていないものを直った」と読む。走行の前に必ずビルドし直す。

```powershell
cargo build -p areka
```

ビルドしたコミットは記録 **§1 同定**の「コミット」へ書く。

### 2.2 32bit の橋渡し実行体を実バイナリの隣へ置く（**既知の罠・順序が効く**）

areka はゴーストの脳を、**自分の実行体と同じフォルダに置かれた** `shiori-host32-helper.exe` 経由で読み込む（`crates/areka/src/boot_config.rs:77-82`——`current_exe()` の親へこのファイル名を結合するだけであり、**別の場所を指させる環境変数は無い**。先例も同じことを書いている＝`crates/areka/src/placement/transition_judge_offset_signoff_tests.rs:62-65`）。この実行体は **32bit（i686）でなければならない**。

罠の姿はこうである。`shiori-host32-helper` はワークスペースの構成員なので、`cargo build --workspace` や `cargo test --workspace` を回すと **同じ名前の 64bit 版**が `target/debug/shiori-host32-helper.exe` へ上書きされる。64bit の実行体は 32bit の `pasta.dll` を読み込めないため、**ゴーストは起動するが一言も喋らない**。名前もパスも正しいまま中身だけが入れ替わるので、更新時刻やファイル名では気づけない。

ゆえに順序を固定する——**ワークスペースのビルド／テストを済ませた後に**、32bit 版を作って上書きする。

```powershell
cargo build -p shiori-host32-helper --target i686-pc-windows-msvc
Copy-Item "<リポジトリの絶対パス>\target\i686-pc-windows-msvc\debug\shiori-host32-helper.exe" "<リポジトリの絶対パス>\target\debug\shiori-host32-helper.exe" -Force
```

> **クロスターゲットのビルドは PowerShell で行う。** Git Bash から回すと GNU の `link.exe` が MSVC のリンカを覆い隠してリンクに失敗する。

置いた実行体の**中身**（PE ヘッダの machine 欄）で上書きを確かめる。**`014C` なら 32bit（i386）＝正しい。`8664` なら 64bit＝そのまま採取してはならない。**

```powershell
$HELPER = "<リポジトリの絶対パス>\target\debug\shiori-host32-helper.exe"
$fs = [IO.File]::OpenRead($HELPER); $br = New-Object IO.BinaryReader($fs)
$fs.Position = 0x3C; $pe = $br.ReadInt32(); $fs.Position = $pe + 4
'{0:X4}' -f $br.ReadUInt16()
$br.Close(); $fs.Close()
```

（手順と数値の出所: `crates/areka/src/placement/transition_judge_offset_signoff_tests.rs:79-94`）

確かめた値と、置いた実行体の絶対パスを記録 **§3 資産**の「実 32bit の脳」へ書く。

### 2.3 実拡大率を 96 dpi でない水準にする（R5.1）

ゴーストを載せるモニタの表示倍率を、**96 dpi でない水準**（125% / 150% / 200% など）に設定する。

**実効 DPI はアプリ自身のログで確かめる。** DPI 非対応のプロセスから読むと全モニタが 96 へ丸められ、200% の環境を 100% と読み違える（`crates/areka/src/placement/transition_judge_offset_signoff_tests.rs:24-25`）。走行後の生ログに現れる実効値を、記録 **§2 実機構成**の「各モニタの拡大率」へ書く。

モニタ台数・各モニタの解像度・作業領域の下端・主副の別・ゴーストが載っているモニタも同じ §2 へ書く。

### 2.4 記録置き場を新品にし、ゴーストの永続状態を消す

1. **ログの保存先**を新しい空のファイルにする（前回の採取が混ざると行数も引用も当てにならない）。絶対パスを記録 **§5 走行**へ書く。
2. **ゴーストスコープの永続状態を消す。** 消す対象は

   ```
   <ghost_root>\ghost\master\profile\areka\
   ```

   このフォルダには起動回数と窓位置が残る。残ったまま走ると boot が「2 回目起動」と判定して初回起動の系列が出ない（`crates/areka/src/emo2_boot/spine.rs:483-489`）。**項目 12 は「初回起動 → 再起動」の対比で観測する**ので、走行 A を初回起動にするためにここで消す。

   > **走行 A と走行 B のあいだでは消さない。** 消すと項目 12 の後半が観測できなくなる。§0.2 の「この作業ツリーで `cargo test` を回さない」も同じ理由である。

---

## 3. 起動（R5.1・R5.5）

### 3.1 手順の出所

起動の正典手順は **`crates/areka/tests/emo2_real_run.rs` 冒頭のモジュール doc** に在る。本節はそこから導いたものであり、勝手に組み立てていない。

- 実行体の隣へ 32bit の脳を置く前提: 同 `:34-42`
- 位置引数（argv[1]=ghost_root・argv[2]=balloon_root）と検体の場所: 同 `:18-19`
- 目視のための寛大な有界自動終了と、その起動形の逐語: 同 `:102-116`

### 3.2 起動コマンド（逐語・そのまま貼れる形）

```powershell
$AREKA   = "<リポジトリの絶対パス>\target\debug\areka.exe"
$GHOST   = "<リポジトリの絶対パス>\crates\pilot\examples\shiori-host-32\fixtures\emo2"
$BALLOON = "<リポジトリの絶対パス>\crates\pilot\examples\shiori-host-32\fixtures\emo2\emo2-kakukaku"
$LOG     = "<記録置き場の絶対パス>\lap-run-a.log"

$env:RUST_LOG                = "info,kanade=trace,wintf::transition=debug,areka::placement::diag=debug,areka_emo_present=debug"
$env:AREKA_APP_SMOKE_EXIT_MS = "1800000"

& $AREKA $GHOST $BALLOON 2>&1 | Tee-Object -FilePath $LOG
```

**置き換える値**は 4 つだけである。

| 置き換える箇所 | 何を入れるか |
|---|---|
| `<リポジトリの絶対パス>` | サインオフ対象のコミットを持つ作業ツリーの絶対パス |
| `<記録置き場の絶対パス>` | §2.4 で新品にした保存先 |
| `$env:AREKA_APP_SMOKE_EXIT_MS` | 目視に要る時間。§5 の 20 段を一人で辿るには寛大に取る（例では 30 分） |
| `$env:RUST_LOG` | §3.3 の内訳を減らさない範囲で調整してよい。**実際に与えた文字列を逐語で**記録 §4 へ書く |

**すべて絶対パスで与える。** 相対パスだと脳の読み込みが `0x8007007E` で落ちる。ゴースト・バルーン・実バイナリの絶対パスは記録 **§3 資産**へ、起動コマンドの逐語は記録 **§5 走行**へ書く。

### 3.3 記録用の出力水準の内訳（なぜこの 5 語か）

| 指定 | 何を点灯させるか | 出所 |
|---|---|---|
| `info` | 既定の水準。接続失敗（`error!`）と正規の解放完了（`info!`）を捕らえる | `crates/areka/src/main.rs:136-141`（`RUST_LOG` 未設定時は `info` へ落ちる） |
| `kanade=trace` | 起動系列の成立と装着完了の印 | `crates/areka/tests/emo2_real_run.rs:21-22` |
| `wintf::transition=debug` | 拡大率の切替の**機械判定の入力**になる観測チャネル | `crates/wintf/src/ecs/window/transition_diag.rs:51-54` |
| `areka::placement::diag=debug`／`areka_emo_present=debug` | 同チャネルへ発行する残り 2 crate ぶん | 先例の起動コマンド `.kiro/specs/completed/areka-P0-dpi-transition-atomicity/signoff-procedure.md:148` |

`wintf::transition` の指定を落とすと、§6 の機械判定が「観測行 0 行」で落ちる（`crates/areka/src/placement/transition_signoff_tests.rs:34-55`）。**落ちるのが正しい**——無言の成功を作らないためにそうしてある。

### 3.4 番犬は付かない

この直接起動には**番犬（ハング判定の締切）が付かない**。番犬を持つのは `crates/areka/tests/emo2_real_run.rs` の `#[test]` の側であり、本走行はそれとは別物である（同 `:114-116`）。走行が終わらないときに自動で殺してくれるものは無く、終わりを与えるのは `AREKA_APP_SMOKE_EXIT_MS` の有界自動終了だけである。

記録 **§4 環境変数**の「番犬が付かないことの明記」へ、その旨を書く。

---

## 4. 宣言（R5.6）

**結果を 1 つも見る前に**、記録 **§6 宣言**を埋める。

1. **狙う操作の順序**を書く。本書 §5 の順序をそのまま採るなら「本手順書 §5 の順序どおり」と書き、外れる箇所があるならその箇所を名指しする。
2. **項目ごとの期待**を、20 行すべてについて自分の言葉で書く。期待の本体は design.md の適合検証項目表にある——ここに書くのは「その項目で**自分が何を見るつもりか**」である。

> **走行の後に書いた宣言は宣言ではない。** 結果を見てから書いた期待は結果と一致するに決まっており、証跡としての値が 0 になる。§6 を走行後に書き足したり書き換えたりした場合は、**書き換えたこと自体**を記録 §13.3 へ記す。

---

## 5. 走行——20 項目の確認順

### 5.1 順序を決めている制約

順序を縛っているのは次の 5 つの制約である。ただし **5 つだけでは並びは一意に閉じない**——最終的に順序を一意に決めるのは下の A1〜A20 の表であり、各行の操作欄に理由を併記してある（理由を書けない隣り合わせが 2 組ある: A7 と A8、A12 と A13。どちらも入れ替えても制約に反しないが、表が並びを固定するので読み手が選ぶ余地は残らない）。

1. **項目 16 の直後に項目 19 を確かめる。** 拡大率の切替は再表示を伴うので、そこが再表示直後の重なり順を見る好機である（design D5 手順の 6・適合検証項目表 行 19 の末尾）。
2. **項目 18 は、こちらから窓を動かす前に見る。** 項目 18 の期待値には「操作で動かした窓を除く」但し書きが付いている（design.md の項目表 行 18 を見よ）。ゆえに項目 9（台本による位置調整）と項目 17（掴んで動かす）より**前**に置く。項目 18 は制約 1 の前後で 2 度見るので確認は 2 か所に分かれ、記録 §8.1 の行は**両方を見終えてから 1 度だけ**書く。
3. **項目 12 は再起動を跨ぐ。** 位置の永続化は「動かして終了 → 起動し直す」でしか観測できないので、**走行 A の終わり**と**走行 B**に分かれる。
4. **項目 7 の後半（選択肢の見た目の反転）は別走行にする。** 反転の注入は処理系の寿命で 1 度だけ焼き付く（`crates/areka/src/emo2_boot/hover_inject.rs:184-187` の `OnceLock`）ため、走行の途中で点けたり消したりできない。**走行 A・B では点けない**（design D5 環境変数の表）。点ける走行を**走行 C**として分ける。
5. **項目 20 は一点では成立しない。** 3 経路の同時成立を 3 つの証跡で示すので、走行 A 全体に跨がる（§5.5）。

### 5.2 走行 A（1 回目の起動＝初回起動）

上から順に確かめる。各段で記録 **§8.1** の当該行（結果・根拠・根拠の種別）を埋める。**1 段でも成立しなければ、そこで走行を不合格として扱い、後段の結果で埋め合わせない**（R1.6）。

| 順 | 項目 | 何をするか（操作） |
|---|---|---|
| A1 | **項目 1** 起動と既定位置 | §3.2 のコマンドで起動する。窓が出た直後に見る |
| A2 | **項目 15** バルーンの表示ライフサイクル | 起動直後の相を、起動挨拶が流れ始める**前**に見る。会話の始まりと終わりの相は A3 と続きで見る |
| A3 | **項目 2** 起動挨拶の再生 | 何も操作せず最後まで見る |
| A4 | **項目 11** 利用者名の展開 | A3 と同じ場面。バルーンの文面を見る |
| A5 | **項目 10** 二人立ち総合 | 項目表 行 10 が束ねている見どころを、A3 の間に順に見る。窓を単独で動かす側の確認は A18 のドラッグに含めてよい（含めたなら §8.1 の根拠にそう書く） |
| A6 | **項目 3** 着せ替えの表情 | A3 の中で表情が切り替わる箇所を見る。切替の後もしばらく見続ける |
| A7 | **項目 4** まばたき 2 系統 | 会話が終わった後、両体をしばらく見る |
| A8 | **項目 18**（前半） 二体の隣接 | **まだ何も動かしていない状態**で二体の間を見る |
| A9 | **項目 16** 拡大率の切替 | **表示設定の拡大率を変える**（例: 125% → 200%）。窓を別のモニタへ掴んで移す方法は取らない——制約 2 の但し書きに掛かり、同じ走行で A11 が観測できなくなる。機材の都合でモニタ間移動しか取れない場合は、A11 を縮退として扱い、その事実と理由を記録 §8.1 の「縮退した項目と理由」へ書く（R12.5） |
| A10 | **項目 19** 再表示直後の重なり順 | **A9 の直後**。切替で描き直された直後の重なりを見る |
| A11 | **項目 18**（後半） 二体の隣接 | A9 を経た後の二体の間を見る。A8 と併せて §8.1 の行 18 を 1 度だけ書く |
| A12 | **項目 5** 放置で自発会話・会話中は割り込まない | 何も操作せずに放置する。会話が始まったら、その会話の最中も放置し続ける |
| A13 | **項目 6** 撫で反応 | 本体の頭・本体の胸・相方の頭・相方の胸の **4 か所**を順に撫でる。狙った順序を記録 §6 の「狙う操作の順序」に**先に**書いておき、生ログの並びと突き合わせる（§5.6 も見よ） |
| A14 | **項目 7**（前半） 二重クリックでメニュー | 二重クリックしてメニューを出す。**見た目の反転は走行 C で見る**（制約 4） |
| A15 | **項目 8** 選択によるシーン遷移の一周 | メニューの選択肢を辿って一周させる |
| A16 | **項目 9** 位置調整 | メニューから位置調整を選び、動いた先を見る。**ここで初めて窓が明示的に動く** |
| A17 | **項目 14** 省略した機能の縮退 | メニューをもう一度開き、何も選ばずに放置して選択待ちが時間で切れるところまで見る。バルーン側の資産については項目表 行 14 が挙げるものを画面で確かめる。**送っていないイベントの側の証跡は生ログに対する判定で採る**（§6・目視では採れない） |
| A18 | **項目 17** 掴んで動かしたときの追従 | 本体・相方をそれぞれ掴んで動かす。**動かした先の位置を控える**——A19 で使う |
| A19 | **項目 12**（前半） 位置の永続化 | A18 で動かした位置を控えたまま、窓を閉じる操作へ進む |
| A20 | **項目 13** 終了 | 窓を閉じ、終了の手順が最後まで進むところを見る。**有界自動終了で終わらせない**——自分で閉じて終了系列を通す（自動終了で終えた場合はその旨を §8.1 の行 13 に書く） |

走行 A が終わったら、生ログの行数を数えて記録 **§5 走行**へ書く。**§2.4 の永続状態は消さない。**

### 5.3 走行 B（再起動）

§3.2 と**同じコマンド**でもう一度起動する（ログの保存先だけ `lap-run-b.log` のように分ける）。

| 順 | 項目 | 何をするか |
|---|---|---|
| B1 | **項目 12**（後半） 位置の永続化 | 起動直後の二体の位置を、A19 で控えた位置と突き合わせる。項目表 行 12 が「2 回目以降の起動」について書いている側も、この 1 回で見る |

B1 を見終えたら、記録 §8.1 の行 12 を A19 と B1 の両方を根拠として 1 度だけ書く。窓を閉じて終える。

> 項目 12 の後半のうち初回起動限定の振る舞いに関わる部分は、**既存の裁定を実機で確かめて開発者が最終確定する**ものである（R7.3）。確定した内容は記録 **§13.1**（判定に載せない既知の症状）へ書く。

### 5.4 走行 C（別走行・選択肢の見た目の反転）

**制約 4 により、これは走行 A・B とは別のプロセスである。**

```powershell
$env:AREKA_CHOICE_HOVER_INJECT = "cycle"
```

を加えて §3.2 のコマンドを回し（ログの保存先は `lap-run-c.log` のように分ける）、メニューを出して選択肢の見た目を見る。

| 順 | 項目 | 何をするか |
|---|---|---|
| C1 | **項目 7**（後半） 選択肢の見た目の反転 | 二重クリックでメニューを出し、選択肢の見た目が変わるところを見る |

記録 §8.1 の行 7 は A14 と C1 の両方を根拠として 1 度だけ書き、**走行 C が別走行であること**を根拠欄に明記する。記録 §4 の「選択肢の見た目の反転の注入」の行にも、別走行として分けた旨を書く。

### 5.5 項目 20 の証跡をいつ採るか（R5.8）

項目 20 は「1 度通った」＝**引数・環境変数・作業ディレクトリの 3 経路の同時成立**である。3 つのどれが欠けても脳の読み込みが成立しないので経路を選り分ける必要はなく、**3 つの証跡が同時に揃ったこと**がそのまま 3 経路の同時成立である（research 決定 F）。証跡を採る時点は次のとおり。

| 証跡 | いつ採るか | 種別 |
|---|---|---|
| ⑴ 接続失敗のログが **0 行** | **走行 A の生ログ全体**に対して、走行が終わってから数える。**§0.1 の点灯確認を先に済ませてから**書く | ログの引用 |
| ⑵ **実 SHIORI 由来の起動挨拶**が画面に出たこと | **A3**（起動挨拶の再生）の場面で目視する | 目視 |
| ⑶ 正規の**解放完了のログが 1 行** | **A20**（終了）の後、走行 A の生ログの末尾側で数える | ログの引用 |

3 経路へ与えられた値（引数・環境変数・作業ディレクトリ）は記録 **§8.2** の上段の表へ書く。受け渡しの実装は `crates/shiori-host32-host/src/process_host.rs:241-251` が引数 3 本・同値の環境変数 3 本・作業ディレクトリを**同時に**渡す形である。**実走テストは改変しない**（当該ファイルは本仕様の編集集合の外）。

### 5.6 判定に使う語（R11.5）

生ログを grep するときは、**実際に出力される語**を使う。言い換えた語を grep すると 0 件が返り、それを「起きなかった」と読んでしまう（§0.1 が禁じている形そのもの）。

| 用途 | grep する語 | 出力側の定義 |
|---|---|---|
| 項目 20 ⑴ 接続失敗 | `event=connect_failed` | `crates/areka-kanade/src/shiori/real.rs:275-280`（`error!`・target `shiori-actor`） |
| 項目 20 ⑶ 正規の解放完了 | `event=unload_clean` | `crates/areka-kanade/src/shiori/real.rs:204-208`（`info!`・同 target） |
| 項目 16 の機械判定の入力 | `[transition]` | `crates/wintf/src/ecs/window/transition_diag.rs:57`（target は同 `:54` の `wintf::transition`） |
| 折返しが有効に解決された証跡 | `wrap=BudouxWordWrap` | `crates/areka/tests/emo2_real_run.rs:31` |

**項目 6 の注意。** 適合検証項目表の項目 6 に現れる `Head0`／`Bust0`／`Head1`／`Bust1` は**撫でる 4 か所の呼び名**であって、ログに出る当たり領域の名ではない。実検体の領域名は `Head`／`Bust` の 2 種だけで（`crates/pilot/examples/shiori-host-32/fixtures/emo2/shell/master/surfaces.txt:23-24`・`:417-418`）、本体か相方かは話者（Ref3）が運ぶ。**`Head0` を grep すると 0 件が返る。**

点灯を確かめた語と方法は、記録 **§7 点灯確認**の表へ 1 行ずつ書く。

---

## 6. 判定（読み分け）

> **本節はタスク 5.2 が書く。** 空欄ではなく**未着手**である。
>
> 入るもの: 機械判定の走行（実機ログを既存の明示実行の走行へ食わせ、出力を全文残す）と、機械と目視が食い違ったときに**決まった順に 3 問を当てる 4 行の表**（design D7・先行仕様 `.kiro/specs/completed/areka-P0-dpi-transition-atomicity/signoff-procedure.md` §6.5 をそのまま引く）。

---

## 7. 合否

> **本節はタスク 5.2 が書く。** 空欄ではなく**未着手**である。
>
> 入るもの: **7 行の定型の合否ブロック**（design D7・先行仕様 同 §6.6 をそのまま引く）。全ての欄が満たされたときにのみ合格とする。

---

## 8. 環境変数の表（R5.3）

**指定しなかった変数は「未指定（既定）」と明記する。空欄のまま残さない**——後から走行条件を復元できなくなる。記録 **§4 環境変数**の「実際の指定」欄に、この表と同じ行順で書く。

変数名は**実際にコードが読む名前**を書く。言い換えた名前で記録すると、後から条件を復元できない。

| # | 役割 | 変数（実名） | 既定 | 記録の書き方 | 名前の出所（`file:line`） |
|---|---|---|---|---|---|
| 1 | 画面更新の間引きの切替 | `AREKA_TICK_GATE` | **既定は無効**（未設定・空・表に無い綴りは既定を動かさない） | 指定しなければ「未指定（既定）」と明記する | `crates/areka/src/tick_gate_config.rs:25`（既定が無効であることは同 `:6-7`、読み方は `:12-16`） |
| 2 | 記録用の出力水準 | `RUST_LOG` | 未設定・非 UTF-8・不正な構文なら `info` へ落ちる | **実際に与えた文字列を逐語で書く**（§3.2 から 1 語でも変えたなら必ず明記） | `crates/areka/src/main.rs:136-141` |
| 3 | 有界の自動終了 | `AREKA_APP_SMOKE_EXIT_MS` | 未設定・空・非数値・負値なら門が閉じる（自動終了しない） | 与えた値を書く。**番犬が付かないことも書く**（§3.4） | `crates/areka/src/main.rs:854`（番犬が付かないことは `crates/areka/tests/emo2_real_run.rs:114-116`） |
| 4 | 選択肢の見た目の反転の注入 | `AREKA_CHOICE_HOVER_INJECT` | **既定は無効**（未設定・空・不正なら完全に効かない） | **走行 A・B では点けない**。点ける走行は**別走行**として分け、その旨を記録する（処理系の寿命で 1 度だけ焼き付くため走行中に切り替えられない＝§5.4） | `crates/areka/src/emo2_boot/hover_inject.rs:29`（既定が無効であることは同 `:37-38`、1 度だけ焼き付くことは `:184-187`） |
| 5 | 実機ログの機械判定の入力 | `AREKA_TRANSITION_LOG` | — | 判定の走行で与える**絶対パス**を書く。未設定・空・空白のみ・読めない・観測行 0 行は**いずれも失敗**になる | `crates/areka/src/placement/transition_judge_verdict.rs:102`（失敗の形は `crates/areka/src/placement/transition_signoff_tests.rs:34-55`） |
| 6 | 実 32bit の脳の絶対パス | **環境変数は無い** | — | 変数では与えられない。**実バイナリの隣へ置いた実行体の絶対パス**を記録 §3 資産へ書き、§2.2 で確かめた PE の machine 欄の値（`014C`）も併記する | `crates/areka/src/boot_config.rs:77-82`（別の場所を指させる変数が無いことは `crates/areka/src/placement/transition_judge_offset_signoff_tests.rs:62-65`） |

> **行 6 について。** design D5 の表はこの行を「変数」の列に並べているが、**実装には該当する環境変数が存在しない**。名前を発明して書くと、後から誰かがその名前で条件を復元しようとして失敗する。ゆえに行そのものは残し、**与え方が「配置」であること**を明記した（R11.5——判定に用いる語を実際の実装と一致させ、古びたときに検出できる形を保つ）。

### 8.1 この表に載せない 2 つの変数（載せない理由）

`AREKA_WINTF_REAL_WINDOW_ZORDER`（`crates/wintf/src/ecs/window/zorder_pair_maintain_always_on_top_tests.rs:75`）と `AREKA_WINTF_VBLANK_DEADLINE`（`crates/wintf/src/runtime/tick_bridge.rs:307`）は、**この表に載せない**。

理由: どちらも**決定論テストを明示実行へ限る門**であり、製品の実行体は読まない。後者は `#[cfg(test)]` のモジュール（同 `:290`）の内側に定義があり、前者はテスト専用のファイルそのものに在る。実機走行は `areka.exe` を起動するので、これらを与えても走行の条件は 1 ミリも変わらない。**表に載せると「走行に効く変数」と読まれ、条件の復元をかえって誤らせる。**

これらを与えるのは `wintf` の該当テストを狙って走らせるときだけであり、その走行の記録先は本記録ではなく `verification/isolation-decision.md` である。**忘れたのではなく、意図して外した。**

---

## 9. 中断したとき（R5.11）

走行が途中で成立しなくなったら、**そこで止める**。

1. **中断点**（どの走行の何番目の段か・何が成立しなかったか）を記録 **§13.3** へ書く。
2. **観測できたところまで**を §8.1 に残す。まだ見ていない項目の欄は `（未記入）` のままにする。
3. **中断した項目の結果は「中断」と書く。合格にも不合格にも数えない。**
4. **部分的な結果を合格として扱わない。** 1 段でも成立しなければ、その走行そのものが不合格である（R1.6）。

やり直すときは §2 の準備からもう一度行い、**新しいログの保存先**を使う。前の走行のログへ追記しない。
