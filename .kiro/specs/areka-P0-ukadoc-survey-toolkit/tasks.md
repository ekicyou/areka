# Implementation Plan

- [x] 1. 基盤: 調査クレートの骨組みと共有の値
- [x] 1.1 調査クレートを新設し、全モジュールの空宣言・依存・誤りの型を先に置く
  - ワークスペースに調査用クレートを 1 本追加する。版や共通設定はワークスペースから継承し、公開しない設定と実行ファイルの明示を持たせる。依存は TOML 読み取り・誤りの型・JSON 読み取りの 3 本をこのタスクで書き切る（後続タスクが同じ設定ファイルを取り合わないため）
  - 設計のファイル構成にある全モジュールを、中身が空のまま宣言だけ置く（値の型・割り当て・ハッシュ・TOML 組み立て・カタログ・台帳・証拠・検査・報告・差分・入出力・振り分け）。共用の見本データを置くファイルも空のまま接続しておく。以後のタスクは自分のファイルだけを書き、集約ファイルには触らない
  - 失敗を表す誤りの型を 1 つ用意し、「探した絶対パス」「読めない理由」「形が違う場所」を本文に載せられるようにする。黙って失敗する経路を作らない
  - 実行ファイルは引数を受け取って振り分け側へ渡すだけの入口に留める（中身は後続タスク）
  - 既存クレートからは参照されず、既存ファイルは 1 つも変更しない
  - 完了時: `cargo test -p ukadoc-survey` と `cargo test --workspace` が緑になり、ワークスペースのファイル行数上限テストが新クレートを走査しても赤にならない
  - _Requirements: 9.1, 9.6, 9.7_

- [x] 1.2 (P) 値の型と語彙を 1 か所で定義する
  - 項目 id の型を作り、「ページ全体の形」と「アンカー付きの形」の 2 形だけを受け付ける。区切りはコロンで、下線では割らない
  - 状態 7 種・関連の種別 6 種・テーマ 8 種・ドメイン 4 種を列挙で持ち、文字列からの変換を失敗しうる操作にする。既定の腕を置かず網羅を守る
  - 状態には台帳へ書く英字の綴りと、報告に出す平易な日本語の呼び名の 2 つの読み出しを持たせる
  - 完了時: 2 形の id が通り、コロン以外で割った文字列や語彙外の綴りが誤りとして返ることを在中テストが確かめる
  - _Depends: 1.1_
  - _Requirements: 1.9, 2.2, 4.3, 4.4, 7.8_
  - _Boundary: model_

- [x] 1.3 (P) 本文ハッシュを自前で用意する
  - 本文の変更検出だけに使う 64 ビットのハッシュを実装し、16 桁の 16 進小文字を返す。算法の名前を定数で持ち、カタログの冒頭に記録できるようにする
  - 外部の暗号ライブラリを足さない（依存が増えると第三者表記の更新が要るため）
  - 完了時: 公表テストベクタ 3 本と逐語一致する在中テストが緑になる
  - _Depends: 1.1_
  - _Requirements: 1.3, 8.2_
  - _Boundary: hash_

- [x] 1.4 (P) 決定的な TOML 本文の組み立て部品を用意する
  - 二重引用符で囲む文字列・文字列の配列・インラインテーブル・キー付きテーブルの見出しを、与えた順のまま組み立てる関数を用意する
  - 逆斜線・二重引用符・制御文字・改行を逃がし、出力が 1 行に収まるようにする
  - 完了時: 組み立てた本文を TOML の読み取りで読み戻すと元の値に一致することを、逆斜線と改行を含む見本で在中テストが確かめる
  - _Depends: 1.1_
  - _Requirements: 1.5, 2.1_
  - _Boundary: tomlout_

- [x] 1.5 ページからドメインへの割り当て表を機械可読な正本として持つ
  - 38 ページの割り当てを表として持ち、ページからドメインを引く・ドメインの担当ページを名前順で返す・割り当ての無いページを挙げる、の 3 つの操作を用意する
  - 完了時: 割り当てが 38 ページ、内訳が shiori 12・assets 24・sakura-script 1・property 1 であることを逐語で確かめる在中テストが緑になる
  - _Depends: 1.2_
  - _Requirements: 3.1, 3.2, 3.5_

- [x] 1.6 入出力層のうち、場所の解決・読み書き・ソース走査を作る
  - ワークスペース根と、カタログ・台帳・報告・テーマ定義それぞれの場所を求める関数を用意する
  - 読み込みでは復帰文字を落とし、書き出しでは改行だけを書く（環境で本文が変わらないため）
  - ソース走査はワークスペース配下の Rust ファイルを名前順・重複なしで返し、成果物・外部取り込み・版管理のディレクトリと**調査クレート自身**を除く。返すパスはワークスペース根からの相対で区切りを揃える
  - 完了時: ソース走査が調査クレート由来のファイルを 1 つも含まないこと、復帰文字付きの本文が復帰文字なしで読めることを在中テストが確かめる
  - _Depends: 1.2_
  - _Requirements: 9.7_

- [x] 1.7 入出力層のうち、スナップショットの読み込みを検査経路から切り離して作る
  - 既定の場所より環境変数の指定を優先してスナップショットの場所を決め、JSON を読んで版・生成日時・提供パッケージとその版・全 entry を持つ値にする
  - 読めない（存在しない・壊れている）ときは、探した絶対パスと理由を本文に載せて失敗し、既存のカタログには一切触れない
  - 提供パッケージの版は、スナップショットを配るパッケージの定義から読む
  - 完了時: 既定の場所か環境変数の指定でスナップショットが実在することを 1 度確かめたうえで、この部品が振り分け側からしか参照されないこと（検査経路のどこからも呼ばれないこと）を確かめる
  - _Depends: 1.6_
  - _Requirements: 1.7, 1.8, 6.2_

- [x] 2. カタログと台帳の読み書き
- [x] 2.1 スナップショットからカタログを組み立てる
  - 正典由来の項目だけを残し、それ以外の出典の項目は落とす
  - 各項目について、項目 id・ページ名・見出し・カテゴリ・本文に現れる版番号のすべて（重複を除き昇順・無ければ空）・本文のハッシュ・正典 URL を持たせる。本文そのものは持たせない
  - スナップショットの版・生成日時・提供パッケージの版・全件数・正典の件数を冒頭の情報として持たせる
  - 割り当ての無いページが現れたらページ名を挙げて失敗する
  - 完了時: 見本のスナップショットから、出典違いが落ち、版番号が 2 つ以上ある項目でも 1 つに絞られないことを在中テストが確かめる
  - _Depends: 1.3, 1.5, 1.7_
  - _Requirements: 1.2, 1.3, 1.4, 1.6, 1.9, 3.5, 9.4_

- [x] 2.2 カタログの本文を書き出し、同じ形で読み戻せるようにする
  - 冒頭の情報を先頭に置き、続けて項目を 1 項目 1 行のインラインテーブルとして id の byte 昇順で書く
  - 同じ本文を読み戻して同じ値になる（丸めが無い）ようにする
  - URL から項目 id を引く逆引き、フラグメントを外したページ URL の一覧、ページごとの見出し一覧の 3 つの読み出しを用意する
  - 完了時: 読んで書き戻すと元の本文に 1 バイトも違わないこと、続けて 2 回書き出しても同じ本文になることを在中テストが確かめる
  - _Depends: 1.4, 2.1_
  - _Requirements: 1.1, 1.5, 2.7_

- [x] 2.3 (P) 台帳の本文を項目ごとの塊に切り分ける
  - 本文を「前置き」と「項目ごとの塊」に分け、各塊の本文中の位置（始まりと終わり）を返す
  - 切り分けは行単位で行い、複数行の備考の中に現れる見出しらしき行に引っかからないようにする
  - 完了時: 切り分けが返す id の集合が、同じ本文を TOML の読み取りで読んだときの鍵の集合と一致することを在中テストが確かめ、食い違えば誤りとして失敗する
  - _Depends: 1.2_
  - _Requirements: 2.1_
  - _Boundary: ledger_

- [x] 2.4 台帳を読み取り、欄と語彙を検証する
  - 前置きのドメイン名と担当ページ、および各項目の欄（状態・登場した版・別名の参照先・後継の参照先・担当 spec・優先度・テーマ・関連・備考）を型のある値として読む
  - 証拠は台帳の欄として読まない（人手が書く欄だけを持つ）。未分類の件数を宣言する欄も持たせない（数は報告側の分布を正とする）
  - 本文に現れた順を別に保持し、並び順の検査に使えるようにする
  - 完了時: 状態の綴りを 1 文字変えると読み取りが失敗し、どの id のどの欄かが本文に載ることを在中テストが確かめる
  - _Depends: 2.3_
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 6.9_

- [x] 2.5 初期台帳を生成し、既存の台帳を壊さずに欠けた項目だけを差し込む
  - カタログの id をドメインごとに選び、状態を未分類・担当 spec を未設定とした項目を凍結済みの形式・id の文字順で書き出す
  - 既存の本文があるときは既存の塊を 1 バイトも変えずに写し、欠けている id の塊だけを id 順の位置へ差し込む
  - 既存の塊が id 順に並んでいなければ、順序を破る id を告げて失敗する
  - 完了時: 既存の塊を 1 つ落として差し込み直すと、その 1 つだけが増え、他の塊のバイト列が変わらないことを在中テストが確かめる
  - _Depends: 2.2, 2.4_
  - _Requirements: 3.3, 3.3a, 3.4_

- [x] 3. 証拠の収集と報告の組み立て
- [x] 3.1 ソースのコメントから正典 URL を取り出し、証拠の索引の型を置く
  - 行頭の空白を除いて 3 種のコメント記号のいずれかで始まり、正典 URL 1 語だけが続いて行末に達する行だけを拾う。説明文が続く行は拾わない
  - 行番号や内部 ID を証拠に使わず、ファイルパスと URL の対だけを返す
  - 証拠の索引（id ごとのファイルパス・解決できなかった URL・対応が付かなかった名前）と候補の型を、この段でまとめて置く（後続の 2 本が同じ集約ファイルを取り合わないため）
  - 完了時: 「ukadoc」の語だけで URL を伴わないコメント行が 1 件も拾われず、URL 付きの行だけが拾われることを在中テストが確かめる
  - _Depends: 1.1_
  - _Requirements: 5.1, 5.3, 5.6, 5.7_

- [x] 3.2 (P) 取り出した URL を項目 id へ解決し、語彙表の名前と突き合わせる
  - 項目 URL は完全一致でカタログの id へ解決し、同じ id が複数のファイルに現れる場合は重複を除いた名前順の一覧として持つ。カタログに無い URL は証拠に混ぜず別の一覧へ回す
  - ページ URL は語彙表の目印として扱い、その直後に始まる最初のスライス定数の要素ごとの最初の文字列リテラルを要素とみなして、カタログの見出しと名前で突き合わせる。名前の照合は互換の正規化と空白の畳み込みを行った完全一致とし、1 件に定まるときだけ証拠として採る。対応が付かなかった要素も別の一覧へ回す
  - 既存の 2 系統の機械可読資産（契約カタログと語彙台帳）は置き換えず、カタログ id からその entry 名へ結ぶ対応だけを持つ
  - 完了時: 見本のソース文で、同じ id が 2 ファイルに現れると名前順 2 件にまとまり、カタログに無い URL が証拠 0 件・別一覧 1 件になり、スライス定数の 3 形（文字列だけ・タプル・構造体リテラル）から要素ごとの最初の文字列リテラルだけが拾われる（2 番目以降の文字列や定数の外の文字列は拾われない）ことを在中テストが確かめる
  - _Depends: 2.2, 3.1_
  - _Requirements: 2.3, 5.4, 5.5, 9.2, 9.3_
  - _Boundary: evidence/resolve_

- [x] 3.3 (P) 正典 URL を置く作業の手掛かり候補を、証拠とは別に集める
  - 許可表の要素文字列・命令の消費側の登録名・設定キーの表・「縮退」「無視」「未知」などを含むログ行を候補として拾う
  - 候補は証拠の索引に混ぜず、別の値として返す（状態の判定は人手に委ねる）
  - 完了時: 見本のソース文から候補が種類つきで並び、同じ文から作った証拠の索引には 1 件も混ざらないことを在中テストが確かめる
  - _Depends: 3.1_
  - _Requirements: 5.8, 5.9_
  - _Boundary: evidence/candidates_

- [x] 3.4 (P) 台帳の集計と、関連で繋がった束の算出を作る
  - 状態の分布（全体・ページ別）・世代別（登場した版の先頭 2 節。空は「世代不明」）・テーマ別の分布を集計する
  - 関連で繋がった id の連結成分を求め、各束に構成 id だけから決まる安定した束 id（構成 id のうち文字順で最小のもの）を付ける
  - 完了時: 見本の台帳で束が連結成分ごとに 1 つずつでき、構成 id の順番を入れ替えても束 id と並びが変わらないことを在中テストが確かめる
  - _Depends: 2.4_
  - _Requirements: 7.9_
  - _Boundary: report/tally, report/bundle_

- [x] 3.5 ドメイン別報告の本文を組み立てる
  - 入力はその台帳 1 本とテーマ名だけとし、状態の分布（ドメイン全体とページ別）・世代別の対応表・別名の一覧・テーマ別の状態分布・ドメイン内で関連が閉じている束の一覧を載せる
  - ページ別の分布を未分類件数の正とする（同じ数を台帳側に宣言値として持たせない）
  - 状態の呼び名は平易な日本語で表示し、並びは状態は語彙の順、ページ・テーマ・束 id は名前順に固定する
  - 完了時: 同じ台帳から 2 回作ると同一の本文になり、未分類の残りがどのページに何件あるかが本文から読めることを在中テストが確かめる
  - _Depends: 3.4_
  - _Requirements: 6.9, 7.1, 7.3, 7.8_

- [x] 3.6 全体報告の本文を組み立てる
  - 冒頭にカタログのスナップショット生成日時と、各台帳の項目数・未分類件数を載せる。壁時計の時刻は載せない
  - 状態の分布（全体・ドメイン別）・ドメインを跨いで繋がった束の一覧・テーマ別の状態分布（全体）・ドメインごとの証拠あり件数（有無だけ）を載せる
  - 完了時: 同じ入力から 2 回作ると同一の本文になり、時刻を含む行がスナップショット生成日時の 1 行だけであることを在中テストが確かめる
  - _Depends: 3.2, 3.5_
  - _Requirements: 7.2, 7.3_

- [x] 4. 整合検査の判定
- [x] 4.1 検査の入力・所見・出力の骨組みと、共用の見本データを作り切る
  - カタログ・台帳 4 本・割り当て・テーマ名・証拠の索引・ドメイン別報告の本文を受け取り、所見の一覧と走査の件数を返す入口を作る
  - 所見の種類を設計の一覧どおり全種そろえて置き、種類・該当 id・場所・詳細を持たせて、種類ごとにまとめた本文へ整形できるようにする。ファイルには一切触れない
  - 構造・内容・新しさの 3 つの判定を、所見 0 件を返す空の実装として先に置き、入口から呼ぶ配線までここで済ませる（後続の 3 本は自分のファイルだけを書く）
  - 共用の見本データ（小さなカタログ・台帳 4 本・ソース文・ドメイン別報告）を正常な形で作り切る。後続の 3 本は見本を読むだけとし、壊し方はそれぞれのテストファイルの中に閉じる
  - 完了時: 所見が 0 件なら空の本文、所見があれば種類ごとにまとまった本文が返ることを在中テストが確かめ、見本データが 3 つの判定すべてで所見 0 件になる
  - _Depends: 1.5, 2.4, 3.2, 3.5_
  - _Requirements: 6.12_

- [x] 4.2 (P) 構造の検査（台帳とカタログの対応・割り当て・並び順）
  - 台帳の id がカタログに実在するか、カタログの id がちょうど 1 つの台帳に 1 回だけ現れるかを判定する
  - id のページがその台帳の担当かを、割り当て表を正本として判定する。前置きのドメイン名と担当ページが割り当て表と食い違う場合も所見にする
  - 台帳の項目が id の byte 厳密昇順でない場合（同じ id が 2 回現れる場合を含む）と、カタログにあるページに割り当てが無い場合を所見にする
  - 完了時: 見本データでその項目だけを壊すと該当する所見が 1 件だけ出て、壊さなければ 0 件になることを対で確かめる在中テストが緑になる
  - _Depends: 4.1_
  - _Requirements: 3.1, 3.2, 6.3, 6.4, 6.13_
  - _Boundary: check/structure_

- [x] 4.3 (P) 内容の検査（URL・証拠・関連・版・テーマ）
  - ソース中の正典 URL がカタログの項目 URL にもページ URL にも一致しない場合を所見にする
  - 状態が実装済みの項目に証拠が 1 件も無い場合を所見にする。証拠は台帳には書かず、検査の出力に id ごとのファイルパスとして列挙する
  - 関連の相手 id・別名の参照先・後継の参照先がカタログに無い場合、別名の指す先が別名である場合、カタログの版番号が 1 つ以上あるのに台帳の登場版がその中に無い場合を所見にする
  - 台帳のテーマ名がテーマ 8 つに無い場合を所見にする
  - 完了時: 見本データで URL を 1 文字変える・証拠を消す・別名を連鎖させる・相手 id を消す・登場版を版番号の外へ動かす・テーマ名を 1 文字変える、のそれぞれで該当する所見だけが出ることを対で確かめる在中テストが緑になる
  - _Depends: 4.1_
  - _Requirements: 6.5, 6.6, 6.7, 6.8, 6.10, 6.11, 6.13_
  - _Boundary: check/content_

- [x] 4.4 (P) 新しさの検査（ドメイン別報告と台帳の突き合わせ）
  - ドメイン別報告 4 本それぞれについて、対応する台帳 1 本から作り直した本文と一致するかを判定し、食い違えばどのドメインの再生成が要るかを所見に載せる
  - 全体報告は検査の入力に取らない
  - 完了時: 見本の報告の 1 行を書き換えるとそのドメインの所見が 1 件出て、復帰文字付きの本文でも緑になることを在中テストが確かめる
  - _Depends: 4.1_
  - _Requirements: 6.13, 7.4, 7.5, 7.6_
  - _Boundary: check/freshness_

- [x] 5. (P) スナップショット更新時の差分を出す
  - 追加・削除・本文が変わった項目を id 付きで列挙する。本文の変更判定はハッシュの比較だけで行う
  - 削除された id が台帳に現れる場合は「台帳の見直しが要る項目」として別に挙げる
  - 常時走るテストからは呼ばない（スナップショットが要る作業として扱う）
  - 完了時: 見本の 2 つのカタログで追加・削除・変更・見直しが要る項目がそれぞれ正しく分かれることを在中テストが確かめる
  - _Depends: 2.2, 2.4_
  - _Requirements: 8.1, 8.2, 8.3, 8.4_
  - _Boundary: diff_

- [x] 6. 実行ファイルの配線
- [x] 6.1 副手続きの振り分けと使い方の表示を作る
  - 引数の解析を自前で行い、8 つの副手続きへ振り分ける。未知の副手続きは使い方を出して終了コード 2 で終わる
  - 失敗は標準エラーへ誤りの本文を出して終了コード 1 で終わり、標準出力には結果だけを出す
  - 完了時: 引数なしと未知の副手続きで使い方が出て終了コード 2 になり、`cargo run -p ukadoc-survey` が実行できることを確かめる
  - _Depends: 1.1_
  - _Requirements: 6.12_

- [x] 6.2 生成の副手続き（カタログ・初期台帳・ドメイン別報告・全体報告）を繋ぐ
  - カタログ再生成はスナップショットを読んでカタログ本文を丸ごと書き換え、失敗時は 1 バイトも書かない
  - 初期台帳の生成は既存の台帳を壊さず差し込み、ドメイン別報告は 4 本、全体報告は 1 本を書き出す
  - 完了時: スナップショットが実在する環境で 4 つの副手続きがいずれも成功し、同じスナップショットと同じ台帳に対して 2 回続けて実行しても書き出されたファイルが 1 バイトも変わらない。スナップショットの場所を存在しない場所に指すと、探した絶対パスを本文に出して失敗し、既存のカタログが変わらない
  - _Depends: 2.5, 3.6, 6.1_
  - _Requirements: 1.1, 1.5, 1.7, 1.8, 3.3, 3.3a, 7.1, 7.2, 7.3_

- [x] 6.3 調べる副手続き（整合検査・証拠・候補・差分）を繋ぐ
  - 整合検査は所見の本文と、id ごとの証拠のファイルパスを並べて出す
  - 証拠だけ・候補だけ・差分だけを出す副手続きをそれぞれ用意する
  - 完了時: 所見が 0 件なら終了コード 0、所見があれば本文つきで終了コード 1 になることを確かめる
  - _Depends: 4.1, 5, 6.1_
  - _Requirements: 5.5, 5.8, 5.9, 6.12, 8.1_

- [x] 7. 資産と規約文書の生成
- [x] 7.1 (P) 伺からしさのテーマ定義を書く
  - テーマ 8 つ（気配・触れ合い・掛け合い・装い・記憶・交わり・気配り・更新）を、値の型が持つ定数と同じ順序の見出しで書く
  - 各テーマに「1 行の定義」「その項目が無いと利用者がゴーストの何を失うか」「代表となる項目 2〜3 件」を書く
  - 付与規則を 1 つだけ書く（答えられるテーマだけを付け、答えられなければ何も付けない）
  - 完了時: 見出しが 8 つで、値の型が持つ定数と順序まで一致する
  - _Depends: 1.2_
  - _Requirements: 4.4, 4.5, 4.6_
  - _Boundary: doc/ukadoc-coverage/values.md_

- [x] 7.2 正典カタログを実データから生成する
  - カタログ再生成を実行し、正典由来 1,749 項目のカタログを書き出す
  - 完了時: 書き出されたカタログの項目数が 1,749、ページが 38 種、カテゴリが 6 種、URL が相異なり 1,749 件であることを確かめ、続けてもう一度実行しても 1 バイトも変わらない
  - _Depends: 6.2_
  - _Requirements: 1.1, 1.2, 1.4, 1.5, 1.6, 1.9_

- [x] 7.3 初期台帳 4 本を生成する
  - 初期台帳の生成を実行し、shiori・assets・sakura-script・property の 4 本を書き出す
  - 既に手書きの台帳が置かれている場合は既存の項目を 1 バイトも変えず、欠けた id だけが差し込まれることを確かめる
  - 完了時: 4 本の項目数がそれぞれ 677・542・342・188、合計 1,749 になり、同じ id が 2 つ以上の台帳に現れない
  - _Depends: 7.2_
  - _Requirements: 3.1, 3.2, 3.3, 3.3a, 3.4_

- [x] 7.4 ドメイン別報告 4 本と全体報告を生成する
  - ドメイン別報告の再生成と全体報告の再生成を実行し、5 本を書き出す
  - 完了時: 5 本がいずれも空でなく、続けてもう一度実行しても 1 バイトも変わらない
  - _Depends: 7.1, 7.3_
  - _Requirements: 7.1, 7.2, 7.3_

- [x] 8. 常時走る整合検査と最終検証
- [x] 8.1 実データを読み込んで整合検査を走らせる入口を作る
  - 入出力層で repo のカタログ・台帳 4 本・テーマ定義・ソース・ドメイン別報告 4 本を読み、純粋層の判定へ渡す
  - 全体報告は読まず、スナップショットの場所を組み立てる関数を 1 度も呼ばない（構造でスナップショット非依存を保証し、環境変数を書き換えるテストは置かない）
  - ネットワークにも実機にも触れず、ファイルを 1 つも作らず、一時ディレクトリも使わない
  - 完了時: 所見が 0 件で緑になり、所見があれば整形した本文がそのまま失敗メッセージになる
  - _Depends: 4.4, 7.4_
  - _Requirements: 6.1, 6.2, 6.12_

- [x] 8.2 実データに対する整合検査を常時テストに載せる
  - 台帳とカタログの対応・割り当て・並び順・URL・証拠・関連・版・テーマ・ドメイン別報告の一致を実データで走らせる
  - 完了時: `cargo test -p ukadoc-survey` で所見 0 件の緑になり、台帳の 1 行をわざと壊すと該当 id と場所つきで赤になることを 1 度確かめて元に戻す
  - _Depends: 8.1_
  - _Requirements: 6.3, 6.4, 6.5, 6.6, 6.7, 6.8, 6.10, 6.11, 7.4, 7.5_

- [x] 8.3 検査対象が 0 件でないことを実データで確かめる
  - カタログの項目数が 1,749・ページ 38 種・カテゴリ 6 種、台帳が 4 本で項目数がそれぞれ 677・542・342・188（合計 1,749）、割り当てが 38 ページで内訳 12・24・1・1 であることを確かめる
  - ソース走査が返すファイル数が 0 でなく、調査クレート由来のファイルを 1 つも含まないことを確かめる
  - ドメイン別報告 4 本が実在し、いずれも空でないことを確かめる（新しさの検査が空回りしていないこと）
  - 完了時: 初期台帳が全行未分類でも、これらの件数が 0 でないことが常時テストで守られる
  - _Depends: 8.2_
  - _Requirements: 6.13_

- [x] 8.4 自前の道具を実データで較正する
  - テーマ定義の見出しが値の型の定数 8 つと順序まで一致することを確かめる
  - 自前の TOML 書き出しが、実データの見出し 1,749 件すべてについて読み戻しで元の文字列に一致することを確かめる
  - 語彙表経路の較正: 既存の語彙台帳にある 159 要素を取り出し規則で拾い、対応するページの見出しへ 159 件すべてが 1 件に定まって対応付くことを逐語で確かめる。あわせて実データの解決で語彙表経路により解決した件数が 0 でないことを確かめる
  - 完了時: 3 つの較正がいずれも緑になり、取り出し規則や書き出しが壊れれば赤になる
  - _Depends: 8.3_
  - _Requirements: 4.4, 5.4, 6.8, 6.13_

- [x] 9. 台帳の形式と仕訳の規則をまとめた案内を書く
  - 台帳の項目形式・状態語彙 7 つ・各欄の意味と、カタログ（機械生成のみ）と台帳（人手記入・機械検査）を別責務に保つ旨を書く
  - 仕訳の規則を書く: 新旧の書式の向きの決め方（正典本文の注記→版番号→人手の判断）・版番号が無い項目を最も古いと決めつけないこと・関連の種別 6 つ・優先度の根拠 4 つと固定された序列・段階の最終決定は本 spec の外に置くこと・壊れ方の段の根拠を備考に書けること
  - 正典 URL の書き方の規約を書く: 定義箇所だけに置き呼び出し側には置かない・1 項目 1 行で説明文を伴わない・語彙表は先頭にページ URL を 1 つ置く
  - 報告の扱いを書く: 手で編集しない・食い違いは再生成で解消する・全体報告の再生成は統合担当の仕事・束 id は人手の文書から引用する
  - 調査 spec と本 spec の着地順が前後する場合の合流の手順を、どちらが先でも成り立つ形で書く
  - 用語は平易な語に限り、プロジェクト内でしか通じない言い回しを持ち込まない
  - 完了時: 上の 5 つの見出しが揃い、形式の変更には要件の改訂が要る旨と、id は符号化済みなのでカタログから写す注意が書かれている（着手時は「要件の記入例にある id が実データに存在しない」注意だったが、12.1 で記入例を実在 id へ訂正したので節を書き換えた）
  - _Depends: 7.4_
  - _Requirements: 2.5, 2.6, 4.1, 4.2, 4.7, 4.8, 4.9, 5.2, 7.7, 9.5_

- [x] 10. ワークスペース全体で緑になることと非接触を確かめる
  - `cargo test --workspace` が緑になることを確かめる
  - 新設した Rust ファイルがいずれも 1,000 行未満であること（ファイル行数上限テストが緑）を確かめる
  - 既存の実行時コード・既存クレート・ロードマップ本文に差分が 1 行も無いことを、実在するパスに対する差分で確かめる（パスが解決することを先に示す）
  - 完了時: ワークスペースのテストが緑で、変更されたファイルが新設クレートと `doc/ukadoc-coverage/` 配下だけである
  - _Depends: 8.4, 9_
  - _Requirements: 9.1, 9.6, 9.8_

- [x] 11. 最終検証で見つかった見張りの穴を塞ぐ（検証ゲートの差し戻し）
  - 要件 3.3a: `ledger-init` が既存本文を渡していることを常時テストで守る。純粋層に継ぎ目を置き（既存本文の供給を引数で受け、書き出す `(パス, 本文)` の組を返す計画関数へ `ledger_init` を委譲する）、手書きの本文を渡したときにその本文が生き残ることを単体テストで確かめる。ファイルも一時ディレクトリも作らない
  - 要件 1.8 の後半: 失敗した走行が既存のカタログを書き換えないことを確かめる。置き場所の規約に収まらないなら、収まらない理由を書いて報告する（作り話の檻を置かない）
  - 完了時: `merge_initial` へ `None` を渡す配線に変える摂動が赤になり、`cargo test --workspace` が緑のまま
  - _Depends: 10_
  - _Requirements: 3.3a, 1.8_

- [ ] 12. 完了報告の残件を片付ける（開発者指示 2026-09-05「残件は対応が必要なら深掘りして対応せよ」）
- [x] 12.1 要件 付録 A の記入例を実在 id へ訂正し、記入例がカタログに実在して読めることを常時テストで守る
  - 親が要件 A.1／A.3 と設計 D-10 の注記を訂正済み（未コミット・この task の差分に含める）。実装役は ⑴ `doc/ukadoc-coverage/README.md` の「⚠ 要件の記入例にある id は実データに存在しない」節を訂正後の姿（記入例は実在 id・id は符号化済みなので写す・逆斜線を書くのは `note` の中だけ）に書き換える ⑵ `crates/ukadoc-survey/tests/consistency/examples.rs` を新設し、要件 A.1 の TOML ブロックと README の TOML ブロックに現れる `ukadoc:` 形の id が**すべて**カタログに実在すること、A.1 のブロックが道具の台帳読み手（`ledger::read::read`）で property の台帳として読めて前置きの pages が割り当て表と一致し項目が id 順であること、`alias_of` の指す先が実台帳で `alias` でないこと、を主張する。空振り防止に各文書の id 件数の下限（A.1 は 4）を釘付けする
  - 読むだけ。ファイルも一時ディレクトリも作らない。`tests/consistency/checks.rs`（985 行）には触れない
  - 要件文書のパスは定数 1 つに集め、`/kiro-complete` のアーカイブ移動（`.kiro/specs/completed/` へ）で書き換える対象であることをコメントに書く
  - 完了時: A.1 の id を 1 文字壊す摂動・README の id を壊す摂動がそれぞれ赤、`cargo test -p ukadoc-survey` と `cargo test --workspace` が緑
  - _Depends: 11_
  - _Boundary: requirements.md（親）・design.md（親）・doc/ukadoc-coverage/README.md・crates/ukadoc-survey/tests/consistency/{mod.rs,examples.rs}_
  - _Requirements: 2.1, 2.6, 6.3, 付録 A.1, 付録 A.3, 付録 B.5_
- [ ] 12.2 `ledger_init` の書き出しを注入可能にし、実 repo に対する不動点と「4 本決めてから書く」を常時テストで守る
  - `cli/generate.rs`: `ledger_init()` を 1 行の配線（実際の取り寄せ `read_if_present` と実際の書き手＝`ensure_parent`＋`files::write_lf`）にし、判断と順番は `pub(crate) fn ledger_init_with(read_existing, write)` へ移す。`write` は `&mut dyn FnMut(&Path, &str) -> Result<(), SurveyError>`。標準出力の 1 行（`announce`）は今までどおり
  - `cli/generate_tests.rs`: ⑴ 実 repo のカタログ・実際の `read_if_present`・記録するだけの書き手で `ledger_init_with` を呼び、記録された `(パス, 本文)` が `Domain::ALL` 順の台帳 4 本で、本文が**コミット済みの台帳とバイト一致**すること（不動点＝`ledger-init` の再実行で台帳が 1 バイトも変わらない・要件 3.3a）⑵ 4 本目（property）の取り寄せで失敗する関数を渡すと書き手が 1 度も呼ばれないこと（全部決めてから書く）⑶ 書き手の失敗がそのまま返ること
  - 読むだけ。ファイルも一時ディレクトリも作らない。`ledger_init()` 自身はテストから呼ばない（呼べば 4 本書く）
  - `ledger_init` の doc コメント「常時テストが届かない 2 か所」を現状に合わせて書き直す（残るのは `ledger_init()` の 1 行の配線だけ、と明記）
  - 完了時: 書き出す本文を空にする摂動・書き出す先を取り違える摂動・4 本決める前に書き始める摂動がそれぞれ赤、`cargo test --workspace` が緑
  - _Depends: 11_
  - _Boundary: crates/ukadoc-survey/src/cli/{generate.rs,generate_tests.rs}・design.md（親）_
  - _Requirements: 3.3, 3.3a, 1.8_
- [ ] 12.3 `tests/consistency/checks.rs` の摂動の道具を `consistency/perturb.rs` へ分け、行数上限の余白を取り戻す
  - 写し `Perturbed` と補助関数（`anchor_id`・`id_of`・`fabricated_entry`・`seen`・`expect_exactly`・`evidence_with_source_line`・摂動用の綴り定数など）を `tests/consistency/perturb.rs` へ移し、`checks.rs` は `use super::perturb::…` で引く。テストの本体・名前・件数は 1 つも変えない（挙動不変の移動）。`checks.rs` 冒頭の doc コメントの「このファイルの後半」等の自己言及は移動後の姿に直す
  - 完了時: `cargo test -p ukadoc-survey` の件数が移動前と同数、`checks.rs` が 800 行以下、新設ファイルも 1,000 行未満、`cargo test --workspace` が緑
  - _Depends: 12.1_
  - _Boundary: crates/ukadoc-survey/tests/consistency/{mod.rs,checks.rs,perturb.rs}・design.md（親）_
  - _Requirements: 9.6, 6.1_

## Implementation Notes

- 1.1: `cargo test --workspace` は先に i686 成果物を作らないと `shiori-host32-host` で赤になる（環境の前提であって欠陥ではない）。`cargo build -p shiori-host32-helper --target i686-pc-windows-msvc` と `cargo build -p shiori-host32-testdll --target i686-pc-windows-msvc` を一度回せば緑（99 suites・6,376 passed を確認）。
- 1.1: 新しい worktree では `vendors/pasta` サブモジュールが未展開で cargo がワークスペースを解決できない。`git submodule update --init --recursive` を一度回す（作業ツリーの展開だけで repo の差分は出ない）。
- 1.1: design.md「File Structure Plan」の本文は「`.rs` は 48 本」と書くが、同じ節の Directory Structure の木は `src/` 62 本＋`tests/` を数える。木の方が数え上げられる正本なので、木に合わせて 62 本を作った（`tests/consistency*` はタスク 8.x の持ち物）。
- 1.2: 語彙の変換は `Result<_, UnknownVocabulary>` を返す（`SurveyError::BadVocabulary` は file と id を持つが、素の `&str` を受ける parse はそれを知らないため）。`ledger::read`（タスク 2.4）は**必ず** `err.at(file, id)` で包むこと。包み忘れると要件 6.10 の「該当 id と場所を示す」がこの継ぎ目で失われる。
- 1.2: `as_key` の綴りを入れ替える摂動は往復テストを素通りする（往復は入れ替えても自己整合）。要件 2.2／4.3／7.8 の綴りを逐語で確かめるテストだけが捕まえるので、後の整理で往復テストに畳まないこと。
- 1.2: このワークスペースは `cargo fmt --all -- --check` が緑（外れ値 0 件）。新設ファイルも fmt に通すこと。
- 1.3: **差分だけの主張（`assert_ne!`）と形だけの主張（長さ・字種）は「入力の単射関数であること」しか言えず、どの関数かを 1 つも固定しない。** 公表テストベクタが ASCII だけだったため、入力を `as_bytes()` でなく `chars()` で読む取り違え（符号位置読み）がテスト 36 本すべて緑のまま通った。日本語が大半の本文では全行が黙って別の値になる。純粋関数の檻には必ず逐語の期待値を 1 本以上置くこと。多バイト 3 本を追加して是正済み。
- 1.3: 形の一般テスト（固定サンプル列）は 16 桁 0 詰めの欠落も捕まえない。捕まえるのは上位桁 0 の入力を決定的に探す専用テスト 1 本だけ。
- 1.4: 制御文字の逃がし形は一様に `\u00XX`（大文字 16 進）に決めた。design.md の D-10（規則・正）と `tomlout` 節の事前条件の括弧書き（`\n` に逃がす）が食い違って読めるが、括弧書きは入力側の文字の呼び名と読む。実データ 1,749 件の id と見出しに制御文字は 0 件なので `catalog.toml` には現れず、どちらを採っても 1 バイトも動かない。
- 1.4: 読み戻し一致（round-trip）は「単射であること」しか言えない——余計に逃がす書き出しでも素通りする。逐語一致の主張と対で置くこと（1.3 の教訓と同じ形）。区切りの空白 1 個の違いは読み戻しでは絶対に捕まらないが、要件 1.5 のバイト安定性はそこに載っている。
- 1.5: 割り当て表は要件 3.1・実データ（スナップショット 1,749 件の 38 ページ）・実装の 3 者を機械で突き合わせて一致を確認済み。ページごとの件数も要件 3.1 の表と実データが全件一致。以後この表を触るときは同じ 3 者比較をやり直すこと。
- 1.5: 逐語のページ一覧テストは実装の const 配列を参照せず、独立した文字列リテラルで書く。const を参照すると表を表自身と比べるだけになり、転記の誤りを 1 件も捕まえられない。件数だけのテストは 12 個すべてが綴り違いでも緑になる（実証済み：1 文字入れ替えの摂動は件数テストを素通りし逐語テストだけが赤）。
- 1.6: **`write_lf` は親ディレクトリを作らない。** 新しい checkout に `doc/ukadoc-coverage/` は存在しないので、生成の副手続き（タスク 6.2）が自分でディレクトリを作ること。作らないと書き出しがパス付きで失敗する（黙って落ちはしない）。
- 1.6: design.md の Directory Structure は `src/io/` に `_tests.rs` の兄弟を 1 つも載せていないが、タスクの完了条件は在中テストを要求し、steering の規約はテスト本体を兄弟ファイルへ置くことを義務づける。兄弟 3 本を新設した（1.1 の 48 対 62 と同じ、木の数え落とし）。
- 1.6: `EXCLUDED_DIRS`（target・vendors・.git）は現在の配置では 1 度も効かない——走査は `crates/` から始まり、その下にこの 3 つは無い（`vendors/pasta` は repo 根）。`vendors` を外す摂動は緑のまま通る。既存の走査器に倣った防御であって欠陥ではないが、この 3 つについては「違反 0 件」が何も証明していない。
- 1.6: 全称の主張（`for ... assert!`）だけのテストは走査が空だと無条件に真になる。非空の主張と必ず対で置くこと（1 本欠けていたのでレビュー後に追加した）。
- 1.7: `io::snapshot` は `pub(crate)` にした。`tests/consistency` は統合テスト＝別クレートなので、この形なら型検査がスナップショットへの到達を禁じる（要件 6.2 を規約でなくコンパイラが守る）。design.md の `io` 節も追随させた。負の対照（E0603）と正の対照（`pub` に戻すと通る）の両方で確認済み。
- 1.7: **タスク 6.2 で `cli::generate` が `io::snapshot` を呼ぶまで、`dead_code` の警告が 22 件出る。** `pub(crate)` が唯一の原因（`pub` に戻すと 0 件）で、ワークスペースの他クレートは無警告。`#[allow]` で蓋をしていない——本当に配線し忘れたコードを隠すため。6.2 で消えることを確かめること。
- 1.7: `SurveyError::MissingEnv` の本文が D-7 の後半（`AREKA_UKADOC_SNAPSHOT` で場所を指定してほしい）を欠いていた。design.md の Error Handling の enum も同じ欠落だったので、コードと設計の両方を直した（片方だけ直すと矛盾が残る）。
- 1.7: 部品を単体で釘付けしても、**入口がその部品を呼んでいるか**は別に守る必要がある。`load` が `absolutize` を呼ばず渡された綴りをそのまま載せる摂動は、`absolutize` の単体テスト 2 本を素通りして全 140 本緑だった。入口 1 本のテストを足して赤にした。
- 2.1: 版番号の抽出規則で**塞ぐのは数字と小数点だけ。英字は塞がない**。`SSP2.3.00以降` は正しく `2.3.00` を拾う（`ukadoc:dev_nar`・`ukadoc:dev_update`・`OnMouseWheel`・`OnMouseGesture` の 4 件が該当）。英字も塞ぐ規則にすると 406/23 が 403/22 に落ち、要件 1.2 の「23 件」に届かない。実装係とレビュアーが独立に実データで 406 件（≥1）／23 件（≥2）／最大 7 種を再現し、design.md の実測と一致。
- 2.1: 並びは**文字列の昇順**（`2.10.0` が `2.9.0` より前）。数値順ではない。
- 2.1: `total_entries`（全 2,983）と `ukadoc_entries`（正典 1,749）は別の欄。両者が一致する見本では 2 つの欄を区別できないので、テストは必ず値の違う見本で書くこと。
- 2.1: `BTreeMap` への挿入は同じ id を黙って畳む（消えた 1 件は `ukadoc_entries` が減るだけで何も言わない）。実データに重複 id は 0 件だが、黙って壊れる経路を残さないため重複を誤りとして落とすようにした。
- 2.2: カタログの 3 つの読み出し（`by_url`・`page_urls`・`titles_of_page`）は `read.rs` の `impl Catalog` に置いた。`mod.rs` には設計の木が `_tests.rs` の兄弟を与えていないので、そこに置くとテストがコードから離れる。
- 2.2: `read` は**繕わない**。`page` 欄が id と食い違う本文・知らない欄を持つ本文はどちらも落とす。繕うと `write(read(t)) == t` が破れる（書き戻した本文が入力と別物になる）。将来 `catalog_format = 2` が欄を足したときは「知らない欄」で落ちるので、版番号を名指しする文言は形式検査を入れるタスクで足すこと。
- 2.2: 設計の見本（Data Models のカタログ本文）を逐語の錨に使うときは、**design.md から機械で抜いて突き合わせる**こと。手で写した近似を期待値にしたテストは何も守らない。
- 2.2: doc コメントが防御を主張したら、その主張を釘付けするテストを置く。「最初の `#` で切る」「表の鍵で書く」はどちらも主張だけで檻が無く、摂動が全緑を通った（レビュー後に 2 本追加）。
- 2.3→2.5 の申し送り（D-12 の穴を閉じるのは 2.5 側）: 切り分けの較正は `toml` の鍵**集合**との一致で行うが、備考の中に**既存の id と同じ綴りの見出し行**が現れた場合は集合が一致してしまい見抜けない。design.md:274 はこの穴を閉じる役を「**厳密な**昇順（前の id と同じでも失敗）」に割り当てている。タスク 2.5 の本文は「id 順に並んでいなければ」としか書いていないが、**等しい隣り合わせも落とすこと**。`ledger-init` は検査（タスク 4.2）より先に走るので、2.5 が緩いとこの穴は永久に開いたままになる。
- 2.3: **摂動は「制約を緩める向き」を必ず走らせる。** 制約を足す摂動（等値比較に条件を 1 つ足す）と緩める摂動（等値比較を件数比較に落とす）は別の問いを検証する。前者は「比較が緩いことに何かが依存しているか」を聞き、後者は「比較が中身を見ていることに何かが依存しているか」を聞く。檻の強さを守るのは後者だけ。実際、較正を `cut == read` から `cut.len() == read.len()` に落とす摂動は全 208 本を素通りした。等値比較・集合比較・並び順の判定には、緩める向きの摂動（等値→件数・等値→常に真）を必ず当てること。
- 2.4: `ledger::read` は**必ず** `err.at(file, id)` で語彙の失敗を包む（1.2 の申し送り）。包む箇所は 4 つ＝状態・テーマ・関連の種別・前置きのドメイン名。前置きには id が無いので場所は `[ledger]` と書く。レビューは 4 箇所とも id 側・file 側の両方向で摂動して赤を確認した。
- 2.4→4.2 の申し送り（`LedgerDomainMismatch` は読み取り段へ移った）: `[ledger].domain` とファイル名の食い違いは `ledger::read` が落とす。`Ledger` は `domain` を 1 つしか持たずその値はファイル名から来るので、宣言された綴りが残るのは `read` の中だけ。`CheckInput` は台帳ごとのファイル名を受け取らないから、検査層はこの判定を作り直せない。**タスク 4.2 に「台帳ファイルからこの所見を出すテスト」を求めてはいけない（書けないのが正しい）。** 対の `LedgerPagesMismatch` は逆で検査層が持ち続ける——`read` は割り当て表を受け取らないため。design.md の判定表と注記も同時に直した。
- 2.4: 「この欄は存在しない」（証拠＝要件 2.3・未分類件数＝要件 6.9）は、型に欄が無いだけでは守れない。**知らない欄を落とす**ことで初めて守られる。手書きの `evidence = [...]` や `unclassified_count = 42` が黙って無視されると、書いた人は書けたつもりのまま数が合わなくなる。
- 2.4: `file_order`（本文の順）と `entries`（id 順）は**食い違える見本で確かめる**こと。text 順と id 順が同じ見本では、`file_order` が並べ替えた鍵の写しでも緑になる。
- 2.5: **1 要素の見本は並び順を 1 つも守らない。** 前置きの担当ページ一覧を 1 ページのドメイン（property・sakura-script）だけで試していたため、一覧を逆順にする摂動が 250 本すべてを素通りした。実台帳 4 本のうち 2 本（shiori 12 ページ・assets 24 ページ）がその経路を通り、要件 3.3a により前置きは以後バイト列のまま写されるので、誤った並びは永久に焼き付く。下流も助けにならない——`LedgerPagesMismatch` は集合の比較なので並べ替えを見ない。**並び順を主張する箇所は、2 要素以上・かつ入力を意図的に非整列にした見本で釘付けすること**（整列済みの見本では「整列する」摂動が素通りする）。
- 2.5: D-12 の穴は閉じた。`check_strictly_ascending` は `pair[1].id <= pair[0].id` で落とすので等しい隣り合わせも失敗する。備考の中に既存 id の見出しを隠した見本で確かめてあり、その見本は `blocks::split` が**受理する**ことをテスト内で先に主張している（受理されなければ並び順の判定に到達せず、別のことを試したことになる）。`<=` を `<` に緩める摂動が赤にするのはこの 1 本だけ＝申し送りの守りはこのテストが全部背負っている。
- 2.5: 生成済みの台帳では既存の塊が `render_initial_entry` の出力そのものなので、**「写す」を「作り直す」に変えた摂動と区別が付かない**。区別できるのは手書きの見本だけ（奇妙な空白・複数行の備考・埋まった status など、生成物と何通りにも違う形）。バイト列を写す契約を守るには手書きの見本が要る。
- 2.5→6.2 の申し送り（前置きに渡すページの並び）: `PageAssignment::pages_of` は名前順で返し、`canonical_pages`（私有）は要件 3.1 の転記順を持つ。`merge_initial` の契約は「渡された順をそのまま保つ」なので、**どちらを渡すかは 6.2 が決める**。要件 3.3a により最初に生成した前置きは以後バイト列のまま写されるため、後から変えられない。設計の `assignment` 節は `pages_of` を名前順と定めており、検査側の `LedgerPagesMismatch` は集合比較で並びを見ないので、名前順（`pages_of`）が素直な選択。
- 3.1: 取り出しの行の形は実データで較正済み——`crates/**/*.rs`（調査クレート自身を除く 1,089 ファイル）で「ukadoc」に触れるコメント行は 152 件（doc 129・行 23）、取り出される URL は 0 件。design.md の実測と一致。`ukadoc:` を字面に含む行は 4 件あり、3 件は語が文の途中、残る 1 件（`sakura/decode_tests.rs:438`）は説明文が 5 語続くので落ちる——**要件 5.3 の説明文の規則が実データで効いているのはこの 1 行だけ**。
- 3.1: コメント記号は `///`・`//!`・`//` の**長い順**に照合する。短い順にすると `///` の行が `/` を残した形で解釈され、`ukadoc:` に届かず取りこぼす。並び自体が優先順位なので、並べ替える摂動は 10 本を赤にする。
- 3.1: `extract` は行単位で走るので、**複数行の生文字列やブロックコメントの中で行頭から始まる `// ukadoc:` は拾われる**。要件 5.2 の「定義箇所だけに置く」と同じく人が守る規約の側（設計 D-4）。作業ツリーに該当行は 0 件。README にも書くこと。
- 3.1→3.2 の申し送り（`UnmatchedName` の形）: いまは `{ name: Option<String>, reason: NameMatchFailure }` で、D-5 の「表が続かない」場合に `name` が `None` になる。`None` の処理を誰も強制されないので、`enum NameMatchFailure { NoMatch(String), Ambiguous(String), TableMissing }` のようにデータ付きの列挙へ寄せると対応が構造で保たれる。**まだ参照は 1 件も無いのでタスク 3.2 が作り替えてよい。** 置き場（4 つ目の欄を足さず `unmatched_names` に相乗り）は D-5 に忠実なので変えない。
- 3.2: **NFKC は依存を足さずに解けた。** 実コーパス 1,955 文字列（スナップショット見出し 1,749＋実物の語彙表 3 本の要素 206）で NFKC が変える文字は 2 つだけ（`U+3000`→半角空白・`U+FF5E`→`~`）。実装は `U+3000` と `U+FF01..=U+FF5E`（`cp − 0xFEE0`）を写す。レビューが全 94 文字を確認し、これは NFKC の**近似ではなくその文字帯への制限**だと確定した——ゆえに誤って一致することは原理的に無く、一致し損ねる方向にしか外れない。設計 D-5 の実測 4 つ（変わる見出し 5・新たに重複するページ 0・相異なる見出し 1,657・159 件が 158→159）を独立に再現。
- 3.2: 射程の外（半角カタカナ・丸数字・合字・単位記号）を持つ見出しが将来増えると、赤にならず `NoMatch` へ黙って落ちる。**ただし `implemented` の行なら要件 6.6 が赤にする**ので、静かに失われるのは「まだ実装済みと申告していない行」だけ。見張りはタスク 8.4 の 159 要素較正で、効くのは `list_shiori_resource` の 1 ページのみ。
- 3.2: D-4 の「完全一致」は摂動 `starts_with` で 1 度素通りした（実データに前方一致の URL 対が 0 件のため、素朴な見本では噛まない）。塞いだのは `a_url_that_merely_extends_a_catalog_url_is_not_resolved` の 1 本だけで、解決の 2 段（項目 URL・ページ URL）両方をこれが背負っている。整理で削らないこと。
- 3.2: 語彙表の要素の取り出しは実物 3 本で 159／21／26 件を確認（各クレート自身の件数表明とも一致）。規則を触ったらこの 3 つの数で較正し直すこと。
- 3.3: 候補は 4 種で実データ 381 件（許可表 254・命令の登録 4・設定キー 24・ログ行 99）。設計が引用する 3 か所の件数（11・1・4）を実装とレビューが独立に再現。許可表 254 の内訳は 231 件が sylphya の語彙表 5 つ（`shiori_resource`159・`dotted`44・`flat`28 ほか）——目印の行を表 1 つにつき 1 行置けば 231 件がまるごと証拠へ変わる。**ノイズではなく、候補の仕組みが狙いどおり働いている印**。
- 3.3: 設計に無い判断を 4 つ置いた（いずれもレビュー承認）。⑴ 直上のコメントの塊に `ukadoc:` の行があれば候補にしない（要件 5.8 の「まだ置かれていない」の実装・署名がカタログを取らないので字面でしか判定できない）⑵ 設定キーは点付き小文字で末尾が拡張子でないもの（引用箇所が設計に無い・拡張子表は正典由来でない。外すと `descript.txt` 90 件と `*.png` 665 件が流れ込む）⑶ テスト本文を除く（根拠は要件 5.2 の「定義箇所だけ」であって件数ではない）⑷ ログの語 7 つはすべて実データに出現を確認（縮退 58・未知 18・無視 16・未対応 5・非対応 2・対象外 2・未実装 1）。
- 3.3: **走査部品が 2 本になり、すでにずれている**（担当タスクが無いので申し送りとして記録）。`resolve.rs` の `slice_element_names` は文字リテラル（`b'['`）とブロックコメントを扱わないが、`candidates.rs` の写しは扱う。いまは潜在——目印の抑止により両者が同じ表を読むことが無いため。`resolve.rs` の部品が私有で兄弟から引けず、3.3 の境界が `resolve.rs` を含まなかった結果。`evidence/` 直下に私的な共有モジュールを置いて 1 本化するのが筋だが、残りのタスクに `evidence/` を境界に持つものは無い（3.3 が最後）ので、**引受先を捏造せず**ここに記録するに留める。
- 3.3: 釘付けされていない規則の境目が 3 つある（いずれも現在の木では無害）。登録名は `register(` で終わる識別子のみ（`register_default("x")` は拾わない）／許可表は宣言行に `= &[` があるもののみ（複数行の宣言は拾わない）／`has_marker_above` は項目 URL も目印と見なすので、159 要素の表の直上に項目 URL を 1 本置くと 159 件の候補がまとめて消える（要件 6.6 は未分類の行を赤にしないので気付けない）。
- 3.4: **穴を 1 つ塞いでも、そのすぐ隣に同じ族の穴が残る。** 連結成分の檻で「1 歩しか辿らない `find`」を塞いだ直後、「2 歩しか辿らない `find`」が全 332 本を素通りした（4 id の連鎖では早く平らになるため）。深さに依存する実装を守るときは、**有界な深さすべてを破る**見本を選び、なぜその深さかをコメントに書くこと。書かないと次の人が縮める。
- 3.4: **「入力で反証できない」と諦める前に、境界の文字を疑うこと。** ページ名は id の 2 番目の区切りなので「id 順とページ名順は原理的に一致する」と結論しかけたが、`0`（0x30）は `:`（0x3A）より小さいので `ukadoc:a0:x:1` と `ukadoc:a:y:1` は id 順 `a0, a`・名前順 `a, a0` で割れる。実データ 38 ページには今のところ該当が無いが、`spec_shiori3` があるので `spec_shiori` が 1 つ増えれば即座に現れる。
- 3.4: 束の並びの見本は**辺の向き**も散らすこと。すべての辺を頭尾で繋いだ見本だけだと、辺を有向として扱う実装が素通りする。実台帳では 2 つの旧名がそれぞれ同じ正典 id を `alias_of` する形（1 つの id へ 2 辺が集まる）が日常的に出る。
- 3.4→3.5 の申し送り（関連の集め方）: 束のテスト `two_links_leaving_the_same_id_merge` は「同じ id から出る 2 辺」を守る。これが噛むのは、関連の一覧を `BTreeMap<from, to>` のような「元 id ごとに 1 本」の入れ物で集める実装——`alias_of` が高々 1 本という直感を `links` へ持ち込むと起きる。**3.5 が辺を組むときは 1 つの id から複数の関連が出られる形にすること。**
- 3.4: テーマの並びは**名前順**（design.md の事後条件「ページとテーマと束 id は名前順」）。要件 4.4 は 8 つの綴りを凍結するだけで表示順を定めない。「名前順」は UTF-8 のバイト順であって五十音順ではない（交わり・掛け合い・更新・気配・気配り・装い・触れ合い・記憶）。状態だけは要件 2.2 の語彙順。
- 3.4: 道具が嘘をついた 2 件——`grep -c $''` はコマンド置換の中で空パターンに退化し「全行に CR」と報告する。`od -c | grep` も偽陽性を出す。CR の有無は素直にバイトを数えて決めること（[[subagent-tooling-can-be-wrong-calibrate-it]]）。
- 3.5: **報告の本文そのものが契約**。タスク 4.4 の新しさの検査は本文をバイト単位で突き合わせるので、版面を変えると 4 本の報告が一斉に古くなる。`domain_tests.rs` の `the_body_is_pinned_verbatim` が版面の正本で、期待値は実装の定数を 1 つも参照しない独立したリテラル。版面を変えるときはここから引き直すこと（安易に焼き直さない）。
- 3.5: **仕様側の穴（3.6・タスク 9・調査 spec 4 本へ）**: 束の辺は `links` だけから作る（設計 D-11 が明記）。ところが付録 A.2 は `alias_of`／`supersedes` を専用の欄だけに書いてよいと定めている。`alias_of` だけの対は別名の一覧（項目 ⑶）に出るので見えるが、**`supersedes` だけの対はドメイン別報告のどこにも現れない**。README（タスク 9）に「関連として束に出したい繋がりは `links` にも書く」と明記するか、設計の裁定が要る。
- 3.5: 壁時計の不在は「4 桁続く数字が無い」で守る。二重描画の一致テストでは代用にならない——2 回の描画はマイクロ秒差なので、秒単位の時刻を焼き込んでも同じ値になり素通りする。
- 3.6→7.4・8 の申し送り（時刻の見張りは見本に閉じる）: 全体報告の「時刻を含む行は 1 行だけ」の判定は「4 桁続く数字」で時刻を見つける。**実データの全体報告には `| 合計 | 1749 |` が出るので、この判定を実データへ持ち上げると偽の赤になる。** 見本に閉じている限り安全で、7.4 の完了条件は「空でない・2 回で同一」だけ、8.1 は要件 7.6 により `summary.md` を読まないので、どちらもこの見張りを引き継がない。
- 3.6: 二重描画の一致テストは壁時計の不在を守れない（2 回の描画はマイクロ秒差なので秒単位の時刻を焼き込んでも一致する）。時刻の不在は別の判定で守り、その判定自体を両方向（時刻らしい行を見つける・表の行を見つけない）で較正すること。
- 3.6: 証拠のパスが本文へ漏れていないことの主張は、**見本に現れるパスを 1 つでも数え落とすとその分だけ穴が開く**。`unresolved` と `unmatched_names` のパスも含めること（1 本抜けていて、漏らす摂動を逐語の錨だけが捕まえていた）。
- **4.1→4.2・4.3・4.4 の必須の申し送り（入口の配線）**: レビュアーが「判定を 1 つも呼ばず `judgements_run` を手で書いた `run`」を実際に作り、全 411 本を素通りさせた。3 判定が所見 0 件の間は 4.1 側でこれを閉じられない。**後続 3 本は破壊の主張を `check::run(&world.input())` を通して書くこと。** `structure::check` / `content::check` / `freshness::check` を直接呼ぶと、入口の配線は永久に釘付けされないまま残る。
- 4.1→4.2 の申し送り（単独では出せない所見が 2 つある）: `CatalogIdInMultipleLedgers` は必ず `LedgerIdPageMismatch` を道連れにする（id を 2 本目の台帳へ複製すると、その台帳が担当しないページに載るため——1 ページ 1 ドメインは仕様の不変条件なのでどんな見本でも避けられない）。`PageNotAssigned` も `CatalogIdMissingFromLedgers` を道連れにする。**「所見が 1 件だけ」ではなく種類ごとの件数で主張すること。**
- 4.1: 共用の見本は `lib_test_support.rs` の `World::normal()`。ドメイン別報告は**正常時の台帳から生成して固定**してあり、読むたびに作り直さない。4.4 は報告の文字列を壊し、4.2／4.3 は台帳を壊してから `refresh_reports()` を呼ぶ——呼び忘れても黙って隠れるのではなく所見が 2 種類出て赤くなる。
- 4.1: 見本が 3 判定すべてで所見 0 件になるという主張は、判定が空実装の間は無条件に真。中身を数える**肯定的なテスト 13 本**（id・件数を逐語で）が支えている。見本を痩せさせる摂動（証拠の経路 1 本を削る）はその 13 本のうち 4 本を赤にする。
- 4.2→4.3・4.4 の申し送り（見本を 1 件だけ壊しても「全部を挙げる」は守れない）: 要件が「**全** id を確かめる」と言う判定は、違反を 1 件だけ置いた見本では「全部挙げる」と「最初の 1 件だけ挙げる」を区別できない。違反を **2 件**置いて件数まで主張するテストを 1 本ずつ置くこと。
- 4.2→4.3・4.4 の申し送り（並びに依存しないという主張の釘付け方）: `World::normal()` は台帳を `Domain::ALL` の順で作り、`Domain` の導出 `Ord` は宣言順なので**同じ並び**になる。だから「渡された順に左右されない」という防御は、同じ入力を 2 回流すテストでは原理的に見えない。**台帳の並びを逆にした入力**を作って結果が等しいことを主張すること（`CheckInput { ledgers: &..., ..world.input() }` で 8 行）。
- 4.2: **入口の配線の穴は閉じた。** 判定を 1 つも呼ばない `run` は構造の検査だけで 17 本を赤にする。4.3・4.4 も同じく `check::run` 経由で主張すること（直接 `content::check` / `freshness::check` を呼ぶとこの守りに寄与しない）。
- 4.2→4.4 の申し送り（消すべきテスト）: `finding_tests.rs::the_three_judgements_are_still_empty` は 4.4 の完了時に**意図して削除**すること。3 判定が埋まっても正常な見本では所見 0 件なので**通り続ける**——赤くなって気付くことはない。4.3 の時点ではまだ名前が半分正しいので、消すのは 4.4。
- 4.2: 「全部を挙げる」の檻は、違反 2 件の**間に正しい項目を挟む**と二重に効く（「最初の違反で止まる」と「最初の正しい項目で止まる」の両方を捕まえる）。
- 4.3: **登場版の突き合わせは完全一致**。前方一致に緩める摂動が全 460 本を素通りした。`2.3` は現実に起こる打ち間違いで——報告が世代を「先頭 2 節」で作る（要件 7.1）ので、調べる人が `introduced` にそう書きやすい。テーマ側は同じ罠を塞いでいたのに版側が空いていた。**対になる判定は対で檻を置く**こと。
- 4.3: 別名の連鎖を「1 段だけ辿る」ことは、**正しい台帳のデータでは原理的に区別できない**（付録 A.2 が `alias_of` を別名の行にしか許さず、`ledger::read` がそれを強制するので、非別名の節に `alias_of` が付いた形は作れない）。段数の檻は「読み取りが弾く形」の `Ledger` 値でしか試せない——`LedgerDomainMismatch` と同じ役割分担。
- 4.3: **輪のテストは輪を捕まえない。** 見本の 2 行がどちらも別名なので「この先に別名があるか」を問う実装は 1 段目で答えが出て輪に入らない。実際に連鎖を辿る実装を当てても緑のまま通り、ぶら下がりもしなかった（実装係の主張・レビュアーの実験・コントローラの追試で一致）。**返ってこない性質はどんな見本でも「きれいな赤」にできない**——`check_alias_chain` を書き換えるときは 1 段で答えを出す形を保つこと、という記録として残す。
- 4.3: doc の主張は**書くのではなく走らせてから**書く。「輪を辿る実装はここで終わらない」は 2 度目の差し戻し理由になった（1 度目は「1 段だけを見ることも固定する」）。同じファイルで同じ欠陥類型を 2 度踏んでいる。
- 4.3: 在中テストが 1,000 行に触れたので 3 本に割った（`content_tests.rs` / `content_link_tests.rs` / `content_test_support.rs`）。steering の逆引き規則（最長 stem 優先・前向きの衝突禁止）は成立する——中間の stem がファイルとして実在しないため。design.md の木も追随させ、あわせて本文の「`.rs` は 48 本」を撤去した（数を書けば必ず陳腐化する・木が正本）。
- 4.4: **報告の突き合わせはバイト単位で、正規化は保存側の復帰文字 1 回だけ。** 両側に `trim_end()` を挟む摂動も `trim_start()` を挟む摂動も、それぞれの檻を足すまで全 474〜476 本を素通りした。空の本文で飛ばす摂動も同様。「X を両側で正規化する」形の摂動は無限にあるので全部は守れないが、**対になるもの（先頭と末尾）は対で置く**こと。
- 4.4: 空の報告の檻は `ScanStats::non_empty_domain_reports` の件数も同時に主張する。見本が実際に空になっていなければ檻は何も守らないまま緑になるし、この主張が「鍵ごと無い」場合のテストとの違いを読む人に示す（そちらは `domain_reports` の件数が減る）。
- 4.4: 版面への逐語の結び付きは 1 か所だけ（非空を確かめる見出し 4 行）。末尾の空行の檻は「保存側＝再生成＋改行」という**相対的な差**に載っているので、版面を変えても空回りにならない——実際に `render_domain` の版面を変える摂動を当てて確かめた。
- 4.4: `純粋層` は**効果の境界**であって module の境界ではない（design.md:93 が「ファイルにもスナップショットにも触らない」と行いで定義し、図も `FileIo --> CheckMod` を描く）。`check` から `io::files::strip_cr` を引くのは適法で、`catalog/build.rs` が `io::snapshot` の型を引く前例が既にある。
- 5→6.3 の申し送り（ハッシュ算法の食い違いは差分では言えない）: `SnapshotMeta.hash_algorithm` は D-1 が「将来の切り替えを検出可能にする」ために置いた欄だが、`CatalogDiff` は 4 欄に凍結されていて `diff` は `Result` も返さないので、算法の食い違いを差分側では申し立てられない。食い違ったまま流すと**共通の id がほぼ全部 `changed` に落ちて出力が読めなくなる**（多めに出る向きなので見落としは起きない）。**タスク 6.3 の副手続きが、印刷の前に現行カタログと新しく組んだカタログの `hash_algorithm`（と `catalog_format`）を突き合わせて、違えばそう言うこと。** 6.3 の `_Requirements:` にはこの項目が無いので、ここに記録しておく。
- 5: 「ハッシュだけを見る」という主張は**両方向に檻が要る**。多めに出る向き（項目全体を比べる摂動）だけを守っていると、少なく出る向き——「ハッシュが違い、かつ見出しが同じときだけ変更とする」——が全 491 本を素通りする。これは本文と見出しが同時に変わった実際の改訂を黙って落とす向きで、より危うい。見本の行は**ハッシュ以外の欄も全部違う**ものを 1 つ用意して初めて両方向が分かれる。
- 5: `diff` と `CatalogDiff` は `pub(crate)`。要件 8.4 の「常時走るテストからは呼ばない」を規約でなくコンパイラに守らせる（タスク 1.7 が `io::snapshot` で採った形）。統合テストからの到達不能は E0603 で、`pub` に戻すと同じ試験が通ることまで両向きで確認済み。`cli::inspect` は lib の中なのでタスク 6.3 は届く。design.md の `diff` 節も同期した。**この変更で `dead_code` が 20→22 件に増える**（`diff.rs` の 2 件・6.3 の配線で消える）。
- 5: 大小を無視するハッシュ比較を赤にする檻は `hashes_differing_only_in_letter_case_are_not_the_same_hash` の 1 本だけ。到達しうる入力である（`catalog::read` は `hash` の綴りを大小そのまま持ち、crate 内に 16 進や長さの検査は 1 つも無い）。**整理で隣のテストへ畳まないこと。**
- 6.1→6.2・6.3 の申し送り（足場の撤去）: `SurveyError::NotWired` は 8 つの副手続きの空実装だけが作る足場で、中身を入れたら消すこと。design.md の Error Handling の列挙には**同期していない**（2 タスク後に消えるものを載せない判断）。⚠**未構築の `pub` 変種は警告を 1 件も出さない**（実測）ので、宣言だけ残しても黙って腐る。気付けるのは振る舞いの側で、`cli_tests.rs::every_name_reaches_its_own_not_wired_body` が 6.2 の配線と同時に赤くなる（成功すれば `expect_err` が落ち、別の失敗なら本文に名前が入らない）。このテストも足場なので同時に退役させる。
- 6.1: `main.rs` の出口（終了コードと標準出力／標準エラーの分け）は**在中テストからは一切見えない**。`eprintln!` を `println!` にしても `ExitCode::from(2)` を `0` にしても、全 506 本が緑のまま通った。`tests/cli_streams.rs` が実行ファイルを起こして出口を檻に入れる。**どの事例も副手続きが成功することに寄りかからない**ので、ファイルもスナップショットも触らない（失敗の腕は `catalog` に実在しない場所を環境変数で渡して守る——今は `NotWired`、6.2 以降は `SnapshotUnreadable` で、どちらも終了コード 1・標準出力は空なので主張は書き換え不要）。design.md の木にも登記した。
- 6.1→6.2 の申し送り（書き出しの順序）: `tests/cli_streams.rs` の失敗の腕の事例は `catalog` に実在しない場所を渡して終了コード 1 を確かめる。**6.2 は `doc/ukadoc-coverage/` を作る前にスナップショットの読み取りで落ちること。** Implementation Note 1.6 が「6.2 がディレクトリを作る」と言い、要件 1.8 が「失敗時は 1 バイトも書かない」と言うので、順序を逆にすると失敗経路が repo にディレクトリを掘る（この事例の主張はそれを見ない）。
- 6.1→6.2・6.3 の申し送り（成功の腕はまだ檻に入っていない）: `Outcome::Done` → 終了コード 0 の腕は摂動が緑のまま通る。8 副手続きすべてが `Err` を返す今は**どんな入力からも到達できない**ので 6.1 では閉じられない（先送りではなく不可能）。6.3 の完了条件「所見が 0 件なら終了コード 0」が同じ腕を通るので、そこで `tests/cli_streams.rs` に事例を足すこと。
- **6.2→7.3 の申し送り（最も危うい未検査）**: `ledger-init` が既存本文を `None` として渡す摂動は**全 521 本が緑・生成物もバイト一致**のまま通る。台帳が今すべて未分類なので痕跡が残らないためで、実際には手書きの `status`／`introduced`／`owner` を静かに初期値へ巻き戻す。要件 3.3a は調査 spec 4 本が寄りかかる保証そのもの。実装は正しい（レビュアーが手書きの行を保存し、消した id を正しい位置へ再挿入することを実測）が、**檻が無い**。タスク 7.3 の 2 番目の箇条書き（既に手書きの台帳が置かれている場合は既存を 1 バイトも変えない）が手作業の確認としてこれを持つ。
- 6.2: 檻に入れられない摂動が 3 つある（いずれも根が `env!("CARGO_MANIFEST_DIR")` で固定＝テストから書き先を逸らせないため）。⑴ 置き場作りをスナップショット読み込みより前へ動かす——**タスク 7.2 が `doc/ukadoc-coverage/` を commit した後は挙動そのものが無効化される**ので、7.2 より前だけの一度きりの性質。⑵ カタログの部分更新——7.2 の完了条件（1,749 項目・2 回で一致）は摂動下でも緑になるので**別の性質**。守っているのは「`catalog()` が自分の書き先を読まない」という事実だけ。⑶ 上記の `ledger-init`。
- 6.2→6.3・8.x の申し送り: 要件 1.8 の**持続する側**（失敗しても既存のカタログを書き換えない）にも檻が無い。`tests/cli_streams.rs` に「失敗の走行の後で木がバイト単位に不変」を主張する事例を足せば**タスク 7.2 の後も生き残り**、より悪い摂動（失敗する前に途中まで書く）も捕まえる。あの файл はタスク 6.1 の持ち物。
- 6.2: 台帳の前置きに渡すページの並びは **`pages_of`（名前順）**に決めた。生成済みの `shiori.toml` は `list_plugin_event` から始まる（要件 3.1 の転記順の先頭 `list_shiori_event` ではない）。要件 3.3a により以後この並びは焼き付く。検査側の `LedgerPagesMismatch` は集合で比べるので下流は再び開かない。
- 6.2: **証拠の経路は実データでは今のところ空回りしている。** ソース走査は調査クレート自身を除き（設計 D-3）、`crates/` の他のどこにも ukadoc の URL は 1 本も無いので、全体報告の「証拠あり」は全ドメイン 0 件。要件 6.13 が想定している状態で、非空の見張りはタスク 8.x の持ち物。
- 6.3: **継ぎ目を 1 か所に作ったら、対称なもう 1 か所にも当てること。** `check` の入口を純関数 `examine` へ切り出して檻に入れた同じファイルで、対称な `diff` の入口が素通りのまま残った。しかも `diff` の自己比較（現行の代わりに新しいカタログ同士を比べる）は**今の製品では出力が変わらない**——全部 0 件のまま清潔に見え、実際に使う場面（新しいスナップショット）で初めて間違う。静かに間違う向きなので、赤くなって気付くことは無い。
- 6.3: `values.md` の綴りの正本は**要件 4.4**（要件 6.8 は「台帳のテーマ名が定義に実在するか」を検査に求めるだけ）。CLI の検査が `model::THEMES` を使うのは正しい——報告を描く側も同じ定数を使っており、新しさの検査は本文をバイトで比べるので、**片方だけ `values.md` を読むと食い違いが `DomainReportStale` として誤った理由で赤くなる**。タスク 8.1 が `values.md` の読み手を置くときは、報告を描く側も同時に移すこと。`paths::values_path()` は既にある。
- 6.3: **`dead_code` は 22 → 0**。全モジュールが到達可能になった＝配線が本物である信号。以後この数が増えたら、繋ぎ忘れか撤去し忘れを疑うこと。
- 6.3: 檻に入れられない摂動は最終的に 3 つ、すべて `check` の読み込みの前段（`examine` の継ぎ目より手前）にある。⑴ 本文を標準エラーへ出す（**静か**——終了コード 0・標準出力は空）⑵ 台帳を 1 本しか渡さない（**騒がしい**——終了コード 1・所見 1,072 件）⑶ 報告を 1 本も渡さない（騒がしい——所見 4 件）。根が `env!("CARGO_MANIFEST_DIR")` で固定なので `check` を別の場所へ向けられず、ファイルが無ければ `print!` に届く前に落ちる。静かなのは ⑴ だけ。
- 6.3: 実データでの `check` は**緑（所見 0 件・証拠 0 件）**。これは正しい観測で、配線の失敗ではない——`candidates` が同じ走査で 381 件を見つけているので走査は動いており、`crates/` にまだ ukadoc の URL が 1 本も無いだけ（調査クレート自身は D-3 で除外）。「何も読まずに何も見つけない」検査と見分けが付かないので、レビューは probe を刺して 4 台帳・4 報告・1,749 項目を実際に読んでいることを確かめた。
- 7.1→8.4 の申し送り: `values.md` は `##` をテーマ名専用にしてあるので、抽出は `^## (.+)$` で足りる（文書自身にもその旨を書いた）。余裕があれば**見出しの段の形**（`# ` が 1 本・`## ` が 8 本・`###` 以上が 0 本）も主張すると、将来の書き換えが「一覧が食い違う」という読みにくい失敗ではなく、破った約束を名指しして落ちる。
- 7.1: 代表項目は**正典を引いて中身を確かめてから**選ぶこと。見出しの字面だけで選ぶと外す——記憶の代表に `OnNotifySelfInfo` を挙げかけたが、正典を読むとシェルとバルーンの名前を運ぶだけで「覚えている」話ではなかった。24 件すべての id・見出し・URL をカタログと逐語で突き合わせて確認済み。
- 7.1: 「無いと利用者が何を失うか」は**観測できる形**で書く。`OnBalloonTimeout` について「吹き出しが出しっぱなしになる」と書いていたが、これは誤り——吹き出しは基盤側が閉じる。失われるのは「読まれずに消えたとゴーストが気付くこと」の方。
- ⚠道具の罠: **この機械の `/tmp` は他セッションと共有されている。** 一時ファイルが別セッションの出力で上書きされた実例あり。作業用の一時ファイルはセッションのスクラッチパッドへ置くこと。
- 7.2: カタログはスナップショットから**独立に再構築して照合済み**（レビュアーが自前の FNV-1a 64 と版番号抽出を書き、1,749 件のハッシュと 1,749 件の版一覧が全件一致・id の集合もスナップショットの正典 1,749 件と完全一致・本文の漏れ 0）。実測 12 項目はすべて設計の測定値と一致（1,749／38／6／1,749・2 欄 19・4 欄 1,730・版 ≥1 が 406・≥2 が 23・最大 7 種・非 ASCII の id 0・逆斜線の見出し 316）。
- **7.2→8.2・8.3 の申し送り（単位の罠）**: カタログの最長行は **579 文字だが 619 バイト**。Rust の `str::len()` はバイト長を返すので、`line.len() == 579` と書くと**永久に間違った常時テスト**になる。`chars().count()` を使うこと。最長行は文字数でもバイト数でも同じ 1 行なので、危ういのは単位だけ。
- 7.2: 生成して確かめるだけの課題には**本来の赤→緑が無い**（作り直した結果が既に置かれていたものと 1 バイトも違わない）。空振りの緑を避けるため、カタログを退避して `check` が終了コード 1 で落ちることを対照として立てた。なお別セッション・別プロセスをまたいでバイト一致したことは、続けて 2 回走らせるより強い要件 1.5 の証跡である。
- 7.3: 要件 3.3a を実データで実証した（実装係とレビュアーが**別々の手書き見本**で独立に）。生成器が書かない形——鍵の並べ替え・不規則な空白・タブ・`alias_of`・複数行の `supersedes`・`"""…"""` の備考・連続空行・末尾空行なし——を混ぜた台帳に `ledger-init` を当てて**バイト単位で不変**、そこから先頭・中央・末尾の項目を消して当て直すと**消した塊だけが id 順の位置へ初期値で戻り、他は 1 バイトも動かない**。期待値は手で組み立ててビット比較した。件数は 677／542／342／188＝1,749、ページごとの内訳も要件 3.1 の表と全 38 ページ一致、id の重複 0、和集合はカタログと過不足なし。
- **⚠ 7.3 が残した未解決（開発者の判断が要る）**: `cli/generate.rs:80` の `merge_initial(existing.as_deref(), …)` を `None` に変える摂動は**全テストが緑・生成物もバイト一致**のまま通り、手書きの調査成果を静かに初期値へ巻き戻す。要件 3.3a は調査 spec 4 本が寄りかかる保証で、壊れ方は「黙って壊れる」——この repo の最悪の類型である。純関数側（`merge_initial`）は 18 本の在中テストで十分に守られており、抜けているのは**入口の配線 1 行**だけ。残るタスク（7.4・8.1〜8.4・9・10）に引受先は無い（8.1 はファイル作成と一時ディレクトリを禁じ、要件 6.2 は在中テストのファイル接触を禁じ、`tests/cli_streams.rs` は repo の状態への依存を禁じ、`io::paths` は根を `env!("CARGO_MANIFEST_DIR")` で固定している）。
  - レビュアーの是正案（コスト見積りも修正済み）: 一時ディレクトリを使う `ledger_init_at(root)` は **design.md:390 がクレート全域で一時ディレクトリを禁じている**ため不可（`log-capture-kit` の例外表への追記が要り要件 9.1 を破る）。代わりに**純粋層の継ぎ目**——既存本文の供給を引数で受け取り `(パス, 本文)` の組を返す計画関数に `ledger_init` を委譲する——ならファイルに一切触れずに配線を守れ、設計の 2 層構成の中に収まる。
- 7.3: `ledger-init` の「N 項目」は書き出した件数ではなくカタログの束の件数（`ids.len()`）を出す。塊を落とす書き出しがあっても数字は変わらない。`report` は正しく `ledger.entries.len()` を使っており、`ledger-init` だけが例外。
- 7.4: 報告 5 本を生成。実装係とレビュアーが**別々に描画器を書き直し、5 本ともバイト一致**で再現した。件数は台帳から数え直して 677／542／342／188＝1,749、ページ別は要件 3.1 の表と全 38 ページ一致、全体報告はドメイン別 4 本と突き合う。本文の漏れ 0、語彙の生の綴り 0。鮮度検査が**バイト単位**であることは「件数を変えない摂動」2 本で確かめた（行の入れ替え・見出しの改称——どちらも `[DomainReportStale]` で赤）。全体報告を壊しても緑＝要件 7.6 のとおり。
- **7.4 が task 9（README）に残した宿題 4 件**:
  1. **世代別の表が今は全部「世代不明」なのは正しい空の状態**（不具合ではない）。表は台帳の手書きの登場版だけを見る。候補はカタログの版番号にあり **1,749 件中 406 件が 1 つ以上・うち 23 件は 2 つ以上で人が選ぶしかない**（要件 1.2 が意図的に絞らない）。残り 1,343 件は版番号を持たず、古い世代と決めつけてはいけない（要件 4.2）。手書きの値は要件 6.7 の検査が守る。——なぜカタログから取らないか: 取ると**カタログを作り直すたびにドメイン別 4 本が一斉に古くなり**、並走する調査 spec 4 本の検査が同時に赤くなる（D-11・要件 3.4／7.4／7.6）。それに要件 6.7 は「手書きの登場版がカタログの版番号と矛盾しないか」の検査なので、カタログから取ったら同語反復になる。
  2. 報告の**テーマの並びは名前順（UTF-8 のバイト順）で、`values.md` の見出し順でも五十音順でもない**。要件 4.4 が凍結するのは「どの 8 つか」であって描画順ではなく、順序が効く `values.md` 側では 4.4 の順が守られている（task 8.4 が釘付けする）。状態の表だけが要件 2.2 の順なのは、状態が「済み→…→不明」という並びのある列だから。
  3. **全体報告はソース木を歩く**ので `doc/ukadoc-coverage/` だけの関数ではない。証拠あり件数の 5 行だけが対象で、ソースが変わると**黙って古くなり、何も赤くならない**（要件 7.6 が構造的に検査の外に置いている＝`CheckInput` に欄が無い）。作り直しは `ukadoc-coverage-roadmap` の持ち物。
  4. D-11 の落とし穴——`supersedes` だけで結んだ対はどの報告にも出ない（束は `links` から組む）——は**今は 0 件だが、それは台帳が空だから**。「0 件なので問題ない」と書いてはいけない。これから繋がりを書く調査担当への前向きな指示として「束に出したい繋がりは `links` にも書く」と書く。
- 8.1: 実データを読む常時検査の入口。`tests/consistency.rs`（宣言だけ 13 行）＋ `tests/consistency/{mod,checks}.rs`。**スナップショットに触れないことは型で保証されている**——`io::snapshot` が `pub(crate)` なので別クレートのテストからは名前すら書けず、書き足すと `E0603` で落ちる（実装とレビューが別々に確かめた）。環境変数を書き換えるテストは 1 本も無い。要件 6.12 のとおり**整形した本文がそのまま失敗メッセージ**になる（包み紙なし・報告を 1 行壊して逐語確認）。
  - ⚠ **差し戻し 1 件＝置き場所の規約違反**。テスト本体を入口ファイルに書いていた。`.kiro/steering/structure.md:129` は「入口は `#[path]` の `mod` 宣言だけ」と定め、設計の File Structure Plan と Testing Strategy 18／19 も `tests/consistency/checks.rs` を指名している。この workspace の他の入口 13 本はすべてテスト本体 0 件。子は素の `mod checks;` で引ける（`#[path]` で読まれたファイルは自分のディレクトリを基準に子を解決する＝`structure.md:141`）。
  - 読み込みの失敗は包まない。`SurveyError::Io` が既に絶対パスと理由を出すので、役目の札を足すとパスも役目も二重に出る。解析の失敗（`read_catalog`／`read_ledger`）だけは包む——こちらはパスを持たないため。
- **⚠ 8.1 が 8.2／8.4 へ送った 2 件**:
  1. **`CheckInput::themes` は報告の行を減らせない**。`report/tally.rs:134` が `by_theme` を `model::THEMES` から直接埋め、`report/domain.rs:139-149` はそれと引数を合わせる（和）だけ。だから `themes: &[]` を渡しても報告はバイト一致で再現され、「所見 0 件」のテストは緑のまま通る。ただし要件 6.8 の判定本体（`check/content.rs:263`）は `input.themes` を読むので繋がりは成立している。鎖は 3 段——台帳のテーマ ∈ `input.themes`（6.8）∧ `input.themes` == `THEMES`（8.1 で釘付け済み）∧ `THEMES` == `values.md` の見出し（**8.4 が釘付けする**）。**8.4 は見出しを順序・個数（8 つ）・見出しの形まで逐語で釘付けすること**——鎖はここ 1 点で吊られている。
  2. **証拠の索引は実データで完全に空**（`by_id` 0・`unresolved` 0・`unmatched_names` 0）。ソースに正典 URL がまだ 1 つも無いため、要件 6.5／6.6 は実データでは対象が 0 件。**8.2 は証拠について実データから非空虚を主張できない**。非空虚の唯一の経路は設計 Testing Strategy 17a（`extract` を `SHIORI_RESOURCE_IDS` の 159 件に直接当てる）で、これは 8.4 の持ち物。
- 8.2: 判定 10 本を実データで走らせる。**要となったのは「対象の国勢調査」**——10 の要件それぞれについて、今の実データに判定の対象が何件あるかを数えて表にし、0 件のものは 0 件だと**表明として書く**。だから対象が 1 件でも現れた瞬間にテストが赤くなり、読む人に分類のし直しを迫る。「所見 0 件です」だけの緑は、対象が 0 件でも通ってしまう。
  - 今の内訳（実装とレビューが別々に数え、12 行すべて一致）: **対象あり**＝6.3（台帳 1,749 行）・6.4（カタログ 1,749 id）・7.4／7.5（報告 4 本）・6.10 の状態語彙（1,749 行）。**対象 0 件**＝6.5（ソースの正典 URL 0）・6.6（`implemented` 0 行）・6.7 の 3 側面（関連／別名／登場版 いずれも 0・ただしカタログ側の版番号は 406 件ある）・6.8（テーマ値 0）・6.11（証拠つき項目 0）。
  - 摂動は実装 16 本・レビュー 11 本（うち 3 本はレビュアーの自作）で**repo 水準の生存 0**。レビュアーは**較正器そのものを較正した**——実データの複製を作る仕組みから台帳 1 本を黙って落とす摂動を打ち、14 本が赤くなることを確かめている。国勢調査 12 行も 1 行ずつ両方向に反転して全て赤。
- **⚠ 8.2 が調査 spec 4 本と roadmap へ送った 1 件（重要）**: **ドメインを跨ぐ関連は、常時検査のどこでも検証されない**。`links` の相手がカタログには在るが別の台帳の持ち物である場合、所見は**0 件**になる——`LinkEndpointMissing` は「カタログに在る」ので鳴らず、`report/domain.rs:203-213` の束の組み立ては構成員が全員その台帳に居ない束を落とすので報告も古くならない。全体報告は要件 7.6 で常時検査の外。これは D-11 と 09-02 の裁定から出る帰結であって不具合ではないが、調査担当は必ずここに踏み込む。
- 8.2 の小さな申し送り 3 件: ⑴ 国勢調査の報告 4 本の行は `Domain::ALL` を回して作るので構造的に常に 4＝データを読んでいない（別のテストが本文の非空を守っている）。⑵ 内容層の摂動が全て shiori 対象なので、`content.rs::ledger_file` を shiori 決め打ちにする摂動は常時検査を素通りする（クレート内の 11 本が捕まえる）。⑶ 別名の鎖の摂動は `status = "alias"` かつ `alias_of` 無しという、読み込み側が拒む形を作っている（層の境界としては正当だが、3 行の鎖なら到達可能な形で同じことを示せる）。
- **⚠ `tests/consistency/checks.rs` は 985 行で上限 1,000 に対し余白 15 行**。8.3 は `non_vacuity.rs`、8.4 は `values_md.rs` と置き場所が別なので当面は困らないが、**このファイルを触るなら先に分ける**こと（摂動の道具一式を `consistency/perturb.rs` へ出せば約 700 行になる）。
- 8.3: 検査対象が 0 件でないことを実データで確かめる（`tests/consistency/non_vacuity.rs`・5 本）。**件数を書き写すだけでは檻にならない**ので、成り立つはずの関係も一緒に表明した——冒頭の宣言件数と数えた行数が一致、台帳 4 本の合計＝カタログ件数かつ和集合の大きさ＝合計（＝重なり無しで覆っている）、ドメイン別件数を**カタログ側からページ→ドメインの表で導き直す**（台帳とは別の出所）、12＋24＋1＋1＝38＝カタログの実ページ数かつ各ドメインの数は**台帳ファイル自身が宣言するページ一覧**と結ぶ、報告の「合計」行がその台帳の件数と一致。摂動 7 本＋レビュー 6 本で全て捕捉。
  - 8.2 の申し送り⑴（報告 4 本の行が構造的に常に 4）はここで解消。報告の検査はディスク上の実在・先頭行が**自分の**ドメインを名乗る・4 本の本文が互いに異なる・合計行の一致で、すべてディスクに依存する。
  - ⚠ **差し戻し 1 件＝実測値の誤記**（走査に現れるクレート数を 23 と書いたが実際は 24＝25 ディレクトリ − 調査クレート 1・ファイルは 1,089）。挙動には影響しないが、下限値をこの先較正する人を誤らせる。
  - ソース走査の下限は 500 本・20 クレート・報告 500 字（実測 1,089・24・最小 1,126）。**正確な数を凍結しない**——workspace のどこかに `.rs` が 1 本増えるたびに赤くなるのはこの spec の仕事ではない。代わりに**この下限が捕まえないものをファイル自身に書いた**: 走査が 1 本おきに取りこぼしても（約 545 本）下限も錨も並び順も素通りする。守っているのは「ほぼ全滅していない」であって「取りこぼしが無い」ではない。
  - 調査クレートの接頭辞は**わざと書き写している**（実装側の定数を輸入すると、テストが実装と定義により一致してしまい独立の主張にならない。それに別クレートからは `pub(crate)` では届かない）。書き写しが実在を指し続けることは `src/lib.rs` の実在検査で守る——クレート名が変わったら**赤くなる**。これが無いと除外の主張が黙って空虚になる。
- 8.4: 自前の道具を実データで較正する（`tests/consistency/values_md.rs`・8 本）。⑴ テーマ定義の見出しが `model::THEMES` と**順序・個数（8）・見出しの形まで**一致（要件 6.8 の鎖はこの 1 点で吊られている）。⑵ 自前の TOML 書き出しが実データの見出し 1,749 件すべてで読み戻して一致。⑶ 語彙表の 159 要素を取り出し規則で拾い、カタログの見出しへ 159 件すべてが**1 対 1 に定まる**（対応の付かない名前 0・`by_id` が 159）。
  - ⚠ **「日本語を全部 `\uXXXX` に逃がす書き出し」は 1,749 件の往復試験を素通りする**。往復は成立するのにカタログが読めなくなる。だから較正 ⑵ は 2 本立て——往復の一致と、**逃がすのは逃がすべきものだけ**という形の検査。「何を書けばこのテストを通ってしまうか」を問うたから見つかった。
  - 逐語の対応で**1 件だけ生の綴りが違う**（要素は半角空白・正典の見出しは全角空白）。だから生の一致は 158 件だと別に釘付けした。正規化が要らなくなったら赤くなる。
- **⚠ 8.4 の完了条件の後半は今の実データでは充足不能**——「語彙表経路で解決した件数が 0 でない」。この経路はソースに置かれた**ページ URL** のコメントが入口だが、ソース 1,089 本に正典 URL は 1 つも無い（唯一ある 2 行は調査クレート内で走査の対象外＝D-3）。**緑を捏造せず、0 であること自体を表明**にした（正典 URL が置かれた瞬間に赤くなり、何に書き換えるべきかを名指しする）。設計 Testing Strategy 17a が「URL が置かれる前は取り出し関数を直接呼ぶ」と定めており、満たせる側は完全に実装した。**設計は最初からこの状態を見込んでいて、tasks.md がその但し書きを落としていた**（spec と現実の衝突ではない）。
  - ⚠ **差し戻し 1 件＝「等価な摂動」の根拠そのものが偽だった**。「注記に括弧は 1 つも無い」と書いたが、`shiori_resource.rs:114` に実在する（`[変更不可]`）。等価なのは**釣り合っていて `[` が先に来る**からで、無いからではない。さらに再レビューで列挙の漏れも 1 つ（釣り合わない `(` や `{` は別の壊れ方をする——表を読み切れず 159 件が 0 件になる）。**檻の根拠を書くときは、書く前に実測すること**。
- 9: `doc/ukadoc-coverage/README.md`（570 行）。5 つの見出し＋所見から直し方を引く表＋書式変更には要件改訂が要る旨。この走行で溜まった 6 件の宿題をすべて収めた。レビュアーが**全ての数・欄名・並び・ファイル名・コードの主張を独立に導き直し**、152 行の数え直し・所見 15 種・逃がし方の 0／316・実在しない記入例 3 件まで一致。
  - 親が直した不正確 4 件: ⑴ 全体報告の証拠の表は 4 行（5 行と書いていた）⑵ 見出し「ドメインを跨ぐ関連は検証されない」が本文より広い——**`alias_of` はドメインを跨いでも検査される**（`check/content.rs` が 4 本の台帳ぜんぶから指す先の状態を引く。ソース自身のコメントが「旧名と正典名がドメインをまたぐことはある」と書いている）。検証されないのは `links` と `supersedes` の側だけ ⑶ 束が落ちる条件の書き方が両義的 ⑷ 例示の項目の但し書きが、そこに無い欄を名指ししていた。
- **⚠ 9 が開発者へ上げる 1 件（要件の訂正）**: **要件 付録 A.1 の記入例 id は 4 件中 3 件が実カタログに存在しない**。`balloon.scope(ID).width` で始まる id は 1 件も無く、実物は `currentghost.balloon.scope_28ID_29.validwidth` のように**記号と日本語が符号化されている**。付録 A.3 の「逆斜線は二重にする」という助言も id 側では空振りする——1,749 件の id に生の逆斜線も生の `(` も**0 件**、生の逆斜線を含むのは**見出しの側 316 件**。しかも同じ文書の付録 B.5 は「錨は符号化済みなのでそのまま写す」と正しく書いており、A と B が食い違っている。
  - **今すぐ効く罠である**——要件 2.1 が調査 spec 4 本に「道具の完成を待たず付録 A から台帳を書き始めよ」と指示しているため。付録 A だけを見て書いた人は全行で「カタログに無い id」と言われる。
  - 直し方は id 3 件の綴りの差し替えと A.3 の一文の書き換えだけで、**書式の変更ではない**（要件 2.6 の凍結には触れない）。README には既に警告表を置いてあるので読む人は救われるが、要件の側も直すのが筋。task 9 は要件を書き換えず、README に記録するに留めた。
- 10: `cargo test --workspace` が緑（**101 スイート・6,952 passed・0 failed・37 ignored**）。実装とレビューが別々に走らせて同数。spec 着手前の基準（99 スイート・6,376 passed）との差は +2 スイート／+576 本で、増えたのは `tests/cli_streams.rs` と `tests/consistency.rs` の 2 本＝勘定が合う。前提として i686 の 2 本のビルドが要る（サブモジュールは展開済みだった）。
  - **非接触の証明は「空の grep」ではなく算術で立てた**。ブランチが変えたファイルは 95 本、内訳が `crates/ukadoc-survey/` 78＋`doc/ukadoc-coverage/` 12＋この spec の文書 5 で**ちょうど 95**。余りが無いことが引き算で出る。パスを指定しない `git diff --name-only` も 95 本。
  - パスの実在は使う前に毎回示した（`crates/areka/` は 323 本に解決して差分 0 行——**解決すると分かっているパスで 0 行**が要る形）。対照も両向きに置いた: 差分の出るパス（`crates/` 78 本）と、実在しないパス（`crates/arekaa/` は 0 本・0 行・終了コード 0＝「差分なし」と見分けが付かない罠の実演）。
  - ルートの `Cargo.toml` は**1 バイトも変えていない**（merge-base・HEAD・main・作業ツリーの 4 点で同じ blob）。`members = ["crates/*"]` なので新クレートは総当たりで加わる。新クレート側で `serde_json` を持つのはこのためである。`.kiro/steering/roadmap.md` も 3 点で同一（要件 9.8）。
  - 行数上限は新設 77 本すべてが 1,000 行未満（最大は `tests/consistency/checks.rs` の 985）。**上限の見張り役は編集せずに緑**——`crates/log-capture-kit/` の差分 0 行（18 本に解決するパスで確認）、例外表は 11 件のまま、`ukadoc` の綴りは 1 つも無い。要件 9.1 の一番鋭い所である。
  - 親が直した 2 件: ⑴ 群 7・8 の見出しの印が付いていなかった（子は全部済み・完了判定が読む）⑵ `error_tests.rs` の例示パスに人名めいた利用者名が入っていた（兄弟ファイルに合わせて中立な綴りへ）。
- 11: **最終検証ゲートが NO-GO を出したので塞いだ 2 つの穴**。要件 77 節中 75 が covered・0 が uncovered、境界違反 0、実起動 PASS——それでも 2 節が「実装は正しいが自動の見張りが無い」形で残っていた。
  - **要件 3.3a**: `ledger_init` を純粋層の計画関数へ委譲した。既存本文の取り寄せを**引数**で受け、書き出す `(パス, 本文)` の組を返す。これで手書きの本文を渡す単体テストが書けるようになり、ファイルも一時ディレクトリも要らない。手書きの見本は生成器が書かない形（鍵の並べ替え・`=` の桁揃え・`"""…"""` の備考・末尾改行なし）で、欠けている塊は**中央**と**末尾**の 2 通り。副産物として、4 本の本文を全部決めてから書き出すようになったので、途中で失敗しても先頭の台帳だけ書き換わることが無くなった。
  - **要件 1.8 の後半**: 失敗した走行の前後で木が変わらないことを確かめる。`tests/cli_streams.rs` の規約（repo の状態に依存しない）に触れないのは、これが**関係だけを主張する**から——`doc/ukadoc-coverage/` が存在しない木でも両辺が空で成立する。スナップショット読み込みの前に書き込みを挿す探りで赤くなり、**既存の終了コードの檻は緑のまま**であることも確かめた（＝これまで誰も捕まえていなかったものを捕まえている）。
- **⚠⚠ 11 が暴いた誤読——この走行を通して受け入れられていた前提が間違っていた**。「実 repo のファイルを読む檻は置けない（要件 6.2 と一時ディレクトリ禁止が禁じる）」と何度も書いてきたが、**両方とも誤読**である。要件 6.2 は「スナップショットが無くても赤くなるな・**repo 内のカタログを正本として検査する**」であって読み取りを禁じていない。設計 File Structure Plan が禁じるのは**作ること**で、「**読むだけ**」と明示している。前例も既にあった（`io/files_tests.rs:129` が在中テストから実 repo ファイルを読んでいる）。この誤読が使える直し方を封じていた。**制約を根拠に「できない」と結論する前に、その制約の原文を引き当てること。**
  - 結果、`read_if_present` の分岐（実在すれば本文・無ければ `None`）が**可視性を 1 つも広げずに**釘付けできた。`Ok(true)` の枝を `Ok(None)` へ潰す摂動が赤 1 本になる。整合検査の側に置かなかったのは、あちらの保証が「`cli` を名指ししない」ことなので、置けば `read_if_present` を公開する羽目になり保証を壊すため。
  - 親が追加した檻: 読む先が**引数で決まる**ことを 2 本目の台帳で確かめる（レビュアーが「置き場の実在だけ見て本文はいつも property から読む」摂動の生存を報告したため。追加後に同じ摂動を打って赤 1 本を確認）。
- **⚠ 11 の後に残る未検証 2 か所（どちらも `ledger_init` を実際に呼ばないと現れず、呼べば台帳 4 本を書く）**: ⑴ 取り寄せの引数そのもの——`&read_if_present` を「いつも `None`」へ差し替えても緑。**台帳が今すべて未分類なので実データの通し確認でも捕まらない**（実測）。調査 spec が手で記入を始めた日から通し確認が受け持つようになる。⑵ 書き出しの繰り返し——`files::write_lf` へ渡す本文を空にしても緑（`ledger_init` を呼ぶテストが 1 本も無いことによる、11 以前からある穴）。
- 11: 設計文書の追随 6 件（親）。`cli/` の在中テスト 2 本を木へ・`values_md.rs` の注記を実体へ・D-5 の「NFKC」に**帯を限った実装である**旨・`tomlout` の事前条件と D-10 の食い違い・Components の依存欄に `io` の型のみ／`strip_cr` のみ・Testing Strategy 17a の後半が現状では成立しないこと。
- 12.1: 要件 付録 A.1 の記入例を実在 id へ訂正（親）・A.3 の逆斜線の一文を id の符号化に合わせて書き直し（親）・README の「記入例の id は実在しない」節を「id は符号化済み——カタログから写す」へ書き換え・`tests/consistency/examples.rs`（6 本）で要件と README の TOML の囲みに現れる id が全部カタログに実在すること、A.1 の囲みが道具の台帳読み手で property の台帳として読めて id 順であること（並びは `Ledger::file_order` を使い自前で数えない）、`alias_of` の先が実台帳で `alias` でないこと、を主張。取り出しの空振りは件数の下限（要件 4・README 3）と較正テスト（囲みの中の id 2 件だけを拾い、囲みの外は拾わない）で塞ぐ。レビュアーが 4 id の実在・1,749／0／0／316 の数・alias 例の根拠（ukadoc 本文「index指定との互換用の記述であり、特に意味はない」）をスナップショットまで遡って独立に確かめ、摂動 3 本（id の 1 字違い・README の id・id 順の破壊）を自分でも打って赤を確認。
  - ⚠ **差し戻し 1 件＝書き直した節に新しい誤りを混ぜた**。`validwidth` を「当たり判定の幅」と語釈していたが、正典は「現在のバルーンの文字描画に使える幅」（当たり判定は `mouse????list(当たり判定名)` 族で別物）。**偽の記述を消すために書き直した段落に偽の記述を足した**——id の実在は檻が守るが、語釈は檻が守らない。プロパティの説明を書くときは正典本文を引いてから書くこと。
  - property ドメインには ukadoc が「旧称」と呼ぶ真の別名の対が無い（旧名は見出しとして残らないので、カタログの項目にならない。真の別名は assets の `overlayfast`→`overlay-fast` 等）。A.1 の alias 例は「index 指定との互換用の記述」と正典が注記する`mouse????list(当たり判定名).name` を使い、仕分けは見本である旨を TOML のコメントに書いた。要件のパスは `examples.rs` の定数 1 つ（`/kiro-complete` のステップ 5-2 がアーカイブ移動で書き換える対象・コメントに明記）。残る取り残し（範囲外）: `tests/consistency.rs:9` の文が兄弟ファイルを `checks.rs` しか挙げていない——12.3 で直す。
