# Implementation Plan

- [x] 1. 基盤: 調査クレートの骨組みと共有の値
- [x] 1.1 調査クレートを新設し、全モジュールの空宣言・依存・誤りの型を先に置く
  - ワークスペースに調査用クレートを 1 本追加する。版や共通設定はワークスペースから継承し、公開しない設定と実行ファイルの明示を持たせる。依存は TOML 読み取り・誤りの型・JSON 読み取りの 3 本をこのタスクで書き切る（後続タスクが同じ設定ファイルを取り合わないため）
  - 設計のファイル構成にある全モジュールを、中身が空のまま宣言だけ置く（値の型・割り当て・ハッシュ・TOML 組み立て・カタログ・台帳・証拠・検査・報告・差分・入出力・振り分け）。共用の見本データを置くファイルも空のまま接続しておく。以後のタスクは自分のファイルだけを書き、集約ファイルには触らない
  - 失敗を表す誤りの型を 1 つ用意し、「探した絶対パス」「読めない理由」「形が違う場所」を本文に載せられるようにする。黙って失敗する経路を作らない
  - 実行ファイルは引数を受け取って振り分け側へ渡すだけの入口に留める（中身は後続タスク）
  - 既存クレートからは参照されず、既存ファイルは 1 つも変更しない
  - 完了時: `cargo test -p ukadoc-survey` と `cargo test --workspace` が緑になり、ワークスペースのファイル行数上限テストが新クレートを走査しても赤にならない
  - _Requirements: 9.1, 9.6, 9.7_

- [x] 1.2 (P) 値の型と語彙を 1 か所で定義する
  - 項目 id の型を作り、「ページ全体の形」と「アンカー付きの形」の 2 形だけを受け付ける。区切りはコロンで、下線では割らない
  - 状態 7 種・関連の種別 6 種・テーマ 8 種・ドメイン 4 種を列挙で持ち、文字列からの変換を失敗しうる操作にする。既定の腕を置かず網羅を守る
  - 状態には台帳へ書く英字の綴りと、報告に出す平易な日本語の呼び名の 2 つの読み出しを持たせる
  - 完了時: 2 形の id が通り、コロン以外で割った文字列や語彙外の綴りが誤りとして返ることを在中テストが確かめる
  - _Depends: 1.1_
  - _Requirements: 1.9, 2.2, 4.3, 4.4, 7.8_
  - _Boundary: model_

- [x] 1.3 (P) 本文ハッシュを自前で用意する
  - 本文の変更検出だけに使う 64 ビットのハッシュを実装し、16 桁の 16 進小文字を返す。算法の名前を定数で持ち、カタログの冒頭に記録できるようにする
  - 外部の暗号ライブラリを足さない（依存が増えると第三者表記の更新が要るため）
  - 完了時: 公表テストベクタ 3 本と逐語一致する在中テストが緑になる
  - _Depends: 1.1_
  - _Requirements: 1.3, 8.2_
  - _Boundary: hash_

- [x] 1.4 (P) 決定的な TOML 本文の組み立て部品を用意する
  - 二重引用符で囲む文字列・文字列の配列・インラインテーブル・キー付きテーブルの見出しを、与えた順のまま組み立てる関数を用意する
  - 逆斜線・二重引用符・制御文字・改行を逃がし、出力が 1 行に収まるようにする
  - 完了時: 組み立てた本文を TOML の読み取りで読み戻すと元の値に一致することを、逆斜線と改行を含む見本で在中テストが確かめる
  - _Depends: 1.1_
  - _Requirements: 1.5, 2.1_
  - _Boundary: tomlout_

- [x] 1.5 ページからドメインへの割り当て表を機械可読な正本として持つ
  - 38 ページの割り当てを表として持ち、ページからドメインを引く・ドメインの担当ページを名前順で返す・割り当ての無いページを挙げる、の 3 つの操作を用意する
  - 完了時: 割り当てが 38 ページ、内訳が shiori 12・assets 24・sakura-script 1・property 1 であることを逐語で確かめる在中テストが緑になる
  - _Depends: 1.2_
  - _Requirements: 3.1, 3.2, 3.5_

- [x] 1.6 入出力層のうち、場所の解決・読み書き・ソース走査を作る
  - ワークスペース根と、カタログ・台帳・報告・テーマ定義それぞれの場所を求める関数を用意する
  - 読み込みでは復帰文字を落とし、書き出しでは改行だけを書く（環境で本文が変わらないため）
  - ソース走査はワークスペース配下の Rust ファイルを名前順・重複なしで返し、成果物・外部取り込み・版管理のディレクトリと**調査クレート自身**を除く。返すパスはワークスペース根からの相対で区切りを揃える
  - 完了時: ソース走査が調査クレート由来のファイルを 1 つも含まないこと、復帰文字付きの本文が復帰文字なしで読めることを在中テストが確かめる
  - _Depends: 1.2_
  - _Requirements: 9.7_

- [x] 1.7 入出力層のうち、スナップショットの読み込みを検査経路から切り離して作る
  - 既定の場所より環境変数の指定を優先してスナップショットの場所を決め、JSON を読んで版・生成日時・提供パッケージとその版・全 entry を持つ値にする
  - 読めない（存在しない・壊れている）ときは、探した絶対パスと理由を本文に載せて失敗し、既存のカタログには一切触れない
  - 提供パッケージの版は、スナップショットを配るパッケージの定義から読む
  - 完了時: 既定の場所か環境変数の指定でスナップショットが実在することを 1 度確かめたうえで、この部品が振り分け側からしか参照されないこと（検査経路のどこからも呼ばれないこと）を確かめる
  - _Depends: 1.6_
  - _Requirements: 1.7, 1.8, 6.2_

- [x] 2. カタログと台帳の読み書き
- [x] 2.1 スナップショットからカタログを組み立てる
  - 正典由来の項目だけを残し、それ以外の出典の項目は落とす
  - 各項目について、項目 id・ページ名・見出し・カテゴリ・本文に現れる版番号のすべて（重複を除き昇順・無ければ空）・本文のハッシュ・正典 URL を持たせる。本文そのものは持たせない
  - スナップショットの版・生成日時・提供パッケージの版・全件数・正典の件数を冒頭の情報として持たせる
  - 割り当ての無いページが現れたらページ名を挙げて失敗する
  - 完了時: 見本のスナップショットから、出典違いが落ち、版番号が 2 つ以上ある項目でも 1 つに絞られないことを在中テストが確かめる
  - _Depends: 1.3, 1.5, 1.7_
  - _Requirements: 1.2, 1.3, 1.4, 1.6, 1.9, 3.5, 9.4_

- [x] 2.2 カタログの本文を書き出し、同じ形で読み戻せるようにする
  - 冒頭の情報を先頭に置き、続けて項目を 1 項目 1 行のインラインテーブルとして id の byte 昇順で書く
  - 同じ本文を読み戻して同じ値になる（丸めが無い）ようにする
  - URL から項目 id を引く逆引き、フラグメントを外したページ URL の一覧、ページごとの見出し一覧の 3 つの読み出しを用意する
  - 完了時: 読んで書き戻すと元の本文に 1 バイトも違わないこと、続けて 2 回書き出しても同じ本文になることを在中テストが確かめる
  - _Depends: 1.4, 2.1_
  - _Requirements: 1.1, 1.5, 2.7_

- [x] 2.3 (P) 台帳の本文を項目ごとの塊に切り分ける
  - 本文を「前置き」と「項目ごとの塊」に分け、各塊の本文中の位置（始まりと終わり）を返す
  - 切り分けは行単位で行い、複数行の備考の中に現れる見出しらしき行に引っかからないようにする
  - 完了時: 切り分けが返す id の集合が、同じ本文を TOML の読み取りで読んだときの鍵の集合と一致することを在中テストが確かめ、食い違えば誤りとして失敗する
  - _Depends: 1.2_
  - _Requirements: 2.1_
  - _Boundary: ledger_

- [x] 2.4 台帳を読み取り、欄と語彙を検証する
  - 前置きのドメイン名と担当ページ、および各項目の欄（状態・登場した版・別名の参照先・後継の参照先・担当 spec・優先度・テーマ・関連・備考）を型のある値として読む
  - 証拠は台帳の欄として読まない（人手が書く欄だけを持つ）。未分類の件数を宣言する欄も持たせない（数は報告側の分布を正とする）
  - 本文に現れた順を別に保持し、並び順の検査に使えるようにする
  - 完了時: 状態の綴りを 1 文字変えると読み取りが失敗し、どの id のどの欄かが本文に載ることを在中テストが確かめる
  - _Depends: 2.3_
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 6.9_

- [x] 2.5 初期台帳を生成し、既存の台帳を壊さずに欠けた項目だけを差し込む
  - カタログの id をドメインごとに選び、状態を未分類・担当 spec を未設定とした項目を凍結済みの形式・id の文字順で書き出す
  - 既存の本文があるときは既存の塊を 1 バイトも変えずに写し、欠けている id の塊だけを id 順の位置へ差し込む
  - 既存の塊が id 順に並んでいなければ、順序を破る id を告げて失敗する
  - 完了時: 既存の塊を 1 つ落として差し込み直すと、その 1 つだけが増え、他の塊のバイト列が変わらないことを在中テストが確かめる
  - _Depends: 2.2, 2.4_
  - _Requirements: 3.3, 3.3a, 3.4_

- [x] 3. 証拠の収集と報告の組み立て
- [x] 3.1 ソースのコメントから正典 URL を取り出し、証拠の索引の型を置く
  - 行頭の空白を除いて 3 種のコメント記号のいずれかで始まり、正典 URL 1 語だけが続いて行末に達する行だけを拾う。説明文が続く行は拾わない
  - 行番号や内部 ID を証拠に使わず、ファイルパスと URL の対だけを返す
  - 証拠の索引（id ごとのファイルパス・解決できなかった URL・対応が付かなかった名前）と候補の型を、この段でまとめて置く（後続の 2 本が同じ集約ファイルを取り合わないため）
  - 完了時: 「ukadoc」の語だけで URL を伴わないコメント行が 1 件も拾われず、URL 付きの行だけが拾われることを在中テストが確かめる
  - _Depends: 1.1_
  - _Requirements: 5.1, 5.3, 5.6, 5.7_

- [x] 3.2 (P) 取り出した URL を項目 id へ解決し、語彙表の名前と突き合わせる
  - 項目 URL は完全一致でカタログの id へ解決し、同じ id が複数のファイルに現れる場合は重複を除いた名前順の一覧として持つ。カタログに無い URL は証拠に混ぜず別の一覧へ回す
  - ページ URL は語彙表の目印として扱い、その直後に始まる最初のスライス定数の要素ごとの最初の文字列リテラルを要素とみなして、カタログの見出しと名前で突き合わせる。名前の照合は互換の正規化と空白の畳み込みを行った完全一致とし、1 件に定まるときだけ証拠として採る。対応が付かなかった要素も別の一覧へ回す
  - 既存の 2 系統の機械可読資産（契約カタログと語彙台帳）は置き換えず、カタログ id からその entry 名へ結ぶ対応だけを持つ
  - 完了時: 見本のソース文で、同じ id が 2 ファイルに現れると名前順 2 件にまとまり、カタログに無い URL が証拠 0 件・別一覧 1 件になり、スライス定数の 3 形（文字列だけ・タプル・構造体リテラル）から要素ごとの最初の文字列リテラルだけが拾われる（2 番目以降の文字列や定数の外の文字列は拾われない）ことを在中テストが確かめる
  - _Depends: 2.2, 3.1_
  - _Requirements: 2.3, 5.4, 5.5, 9.2, 9.3_
  - _Boundary: evidence/resolve_

- [x] 3.3 (P) 正典 URL を置く作業の手掛かり候補を、証拠とは別に集める
  - 許可表の要素文字列・命令の消費側の登録名・設定キーの表・「縮退」「無視」「未知」などを含むログ行を候補として拾う
  - 候補は証拠の索引に混ぜず、別の値として返す（状態の判定は人手に委ねる）
  - 完了時: 見本のソース文から候補が種類つきで並び、同じ文から作った証拠の索引には 1 件も混ざらないことを在中テストが確かめる
  - _Depends: 3.1_
  - _Requirements: 5.8, 5.9_
  - _Boundary: evidence/candidates_

- [x] 3.4 (P) 台帳の集計と、関連で繋がった束の算出を作る
  - 状態の分布（全体・ページ別）・世代別（登場した版の先頭 2 節。空は「世代不明」）・テーマ別の分布を集計する
  - 関連で繋がった id の連結成分を求め、各束に構成 id だけから決まる安定した束 id（構成 id のうち文字順で最小のもの）を付ける
  - 完了時: 見本の台帳で束が連結成分ごとに 1 つずつでき、構成 id の順番を入れ替えても束 id と並びが変わらないことを在中テストが確かめる
  - _Depends: 2.4_
  - _Requirements: 7.9_
  - _Boundary: report/tally, report/bundle_

- [x] 3.5 ドメイン別報告の本文を組み立てる
  - 入力はその台帳 1 本とテーマ名だけとし、状態の分布（ドメイン全体とページ別）・世代別の対応表・別名の一覧・テーマ別の状態分布・ドメイン内で関連が閉じている束の一覧を載せる
  - ページ別の分布を未分類件数の正とする（同じ数を台帳側に宣言値として持たせない）
  - 状態の呼び名は平易な日本語で表示し、並びは状態は語彙の順、ページ・テーマ・束 id は名前順に固定する
  - 完了時: 同じ台帳から 2 回作ると同一の本文になり、未分類の残りがどのページに何件あるかが本文から読めることを在中テストが確かめる
  - _Depends: 3.4_
  - _Requirements: 6.9, 7.1, 7.3, 7.8_

- [x] 3.6 全体報告の本文を組み立てる
  - 冒頭にカタログのスナップショット生成日時と、各台帳の項目数・未分類件数を載せる。壁時計の時刻は載せない
  - 状態の分布（全体・ドメイン別）・ドメインを跨いで繋がった束の一覧・テーマ別の状態分布（全体）・ドメインごとの証拠あり件数（有無だけ）を載せる
  - 完了時: 同じ入力から 2 回作ると同一の本文になり、時刻を含む行がスナップショット生成日時の 1 行だけであることを在中テストが確かめる
  - _Depends: 3.2, 3.5_
  - _Requirements: 7.2, 7.3_

- [ ] 4. 整合検査の判定
- [ ] 4.1 検査の入力・所見・出力の骨組みと、共用の見本データを作り切る
  - カタログ・台帳 4 本・割り当て・テーマ名・証拠の索引・ドメイン別報告の本文を受け取り、所見の一覧と走査の件数を返す入口を作る
  - 所見の種類を設計の一覧どおり全種そろえて置き、種類・該当 id・場所・詳細を持たせて、種類ごとにまとめた本文へ整形できるようにする。ファイルには一切触れない
  - 構造・内容・新しさの 3 つの判定を、所見 0 件を返す空の実装として先に置き、入口から呼ぶ配線までここで済ませる（後続の 3 本は自分のファイルだけを書く）
  - 共用の見本データ（小さなカタログ・台帳 4 本・ソース文・ドメイン別報告）を正常な形で作り切る。後続の 3 本は見本を読むだけとし、壊し方はそれぞれのテストファイルの中に閉じる
  - 完了時: 所見が 0 件なら空の本文、所見があれば種類ごとにまとまった本文が返ることを在中テストが確かめ、見本データが 3 つの判定すべてで所見 0 件になる
  - _Depends: 1.5, 2.4, 3.2, 3.5_
  - _Requirements: 6.12_

- [ ] 4.2 (P) 構造の検査（台帳とカタログの対応・割り当て・並び順）
  - 台帳の id がカタログに実在するか、カタログの id がちょうど 1 つの台帳に 1 回だけ現れるかを判定する
  - id のページがその台帳の担当かを、割り当て表を正本として判定する。前置きのドメイン名と担当ページが割り当て表と食い違う場合も所見にする
  - 台帳の項目が id の byte 厳密昇順でない場合（同じ id が 2 回現れる場合を含む）と、カタログにあるページに割り当てが無い場合を所見にする
  - 完了時: 見本データでその項目だけを壊すと該当する所見が 1 件だけ出て、壊さなければ 0 件になることを対で確かめる在中テストが緑になる
  - _Depends: 4.1_
  - _Requirements: 3.1, 3.2, 6.3, 6.4, 6.13_
  - _Boundary: check/structure_

- [ ] 4.3 (P) 内容の検査（URL・証拠・関連・版・テーマ）
  - ソース中の正典 URL がカタログの項目 URL にもページ URL にも一致しない場合を所見にする
  - 状態が実装済みの項目に証拠が 1 件も無い場合を所見にする。証拠は台帳には書かず、検査の出力に id ごとのファイルパスとして列挙する
  - 関連の相手 id・別名の参照先・後継の参照先がカタログに無い場合、別名の指す先が別名である場合、カタログの版番号が 1 つ以上あるのに台帳の登場版がその中に無い場合を所見にする
  - 台帳のテーマ名がテーマ 8 つに無い場合を所見にする
  - 完了時: 見本データで URL を 1 文字変える・証拠を消す・別名を連鎖させる・相手 id を消す・登場版を版番号の外へ動かす・テーマ名を 1 文字変える、のそれぞれで該当する所見だけが出ることを対で確かめる在中テストが緑になる
  - _Depends: 4.1_
  - _Requirements: 6.5, 6.6, 6.7, 6.8, 6.10, 6.11, 6.13_
  - _Boundary: check/content_

- [ ] 4.4 (P) 新しさの検査（ドメイン別報告と台帳の突き合わせ）
  - ドメイン別報告 4 本それぞれについて、対応する台帳 1 本から作り直した本文と一致するかを判定し、食い違えばどのドメインの再生成が要るかを所見に載せる
  - 全体報告は検査の入力に取らない
  - 完了時: 見本の報告の 1 行を書き換えるとそのドメインの所見が 1 件出て、復帰文字付きの本文でも緑になることを在中テストが確かめる
  - _Depends: 4.1_
  - _Requirements: 6.13, 7.4, 7.5, 7.6_
  - _Boundary: check/freshness_

- [ ] 5. (P) スナップショット更新時の差分を出す
  - 追加・削除・本文が変わった項目を id 付きで列挙する。本文の変更判定はハッシュの比較だけで行う
  - 削除された id が台帳に現れる場合は「台帳の見直しが要る項目」として別に挙げる
  - 常時走るテストからは呼ばない（スナップショットが要る作業として扱う）
  - 完了時: 見本の 2 つのカタログで追加・削除・変更・見直しが要る項目がそれぞれ正しく分かれることを在中テストが確かめる
  - _Depends: 2.2, 2.4_
  - _Requirements: 8.1, 8.2, 8.3, 8.4_
  - _Boundary: diff_

- [ ] 6. 実行ファイルの配線
- [ ] 6.1 副手続きの振り分けと使い方の表示を作る
  - 引数の解析を自前で行い、8 つの副手続きへ振り分ける。未知の副手続きは使い方を出して終了コード 2 で終わる
  - 失敗は標準エラーへ誤りの本文を出して終了コード 1 で終わり、標準出力には結果だけを出す
  - 完了時: 引数なしと未知の副手続きで使い方が出て終了コード 2 になり、`cargo run -p ukadoc-survey` が実行できることを確かめる
  - _Depends: 1.1_
  - _Requirements: 6.12_

- [ ] 6.2 生成の副手続き（カタログ・初期台帳・ドメイン別報告・全体報告）を繋ぐ
  - カタログ再生成はスナップショットを読んでカタログ本文を丸ごと書き換え、失敗時は 1 バイトも書かない
  - 初期台帳の生成は既存の台帳を壊さず差し込み、ドメイン別報告は 4 本、全体報告は 1 本を書き出す
  - 完了時: スナップショットが実在する環境で 4 つの副手続きがいずれも成功し、同じスナップショットと同じ台帳に対して 2 回続けて実行しても書き出されたファイルが 1 バイトも変わらない。スナップショットの場所を存在しない場所に指すと、探した絶対パスを本文に出して失敗し、既存のカタログが変わらない
  - _Depends: 2.5, 3.6, 6.1_
  - _Requirements: 1.1, 1.5, 1.7, 1.8, 3.3, 3.3a, 7.1, 7.2, 7.3_

- [ ] 6.3 調べる副手続き（整合検査・証拠・候補・差分）を繋ぐ
  - 整合検査は所見の本文と、id ごとの証拠のファイルパスを並べて出す
  - 証拠だけ・候補だけ・差分だけを出す副手続きをそれぞれ用意する
  - 完了時: 所見が 0 件なら終了コード 0、所見があれば本文つきで終了コード 1 になることを確かめる
  - _Depends: 4.1, 5, 6.1_
  - _Requirements: 5.5, 5.8, 5.9, 6.12, 8.1_

- [ ] 7. 資産と規約文書の生成
- [ ] 7.1 (P) 伺からしさのテーマ定義を書く
  - テーマ 8 つ（気配・触れ合い・掛け合い・装い・記憶・交わり・気配り・更新）を、値の型が持つ定数と同じ順序の見出しで書く
  - 各テーマに「1 行の定義」「その項目が無いと利用者がゴーストの何を失うか」「代表となる項目 2〜3 件」を書く
  - 付与規則を 1 つだけ書く（答えられるテーマだけを付け、答えられなければ何も付けない）
  - 完了時: 見出しが 8 つで、値の型が持つ定数と順序まで一致する
  - _Depends: 1.2_
  - _Requirements: 4.4, 4.5, 4.6_
  - _Boundary: doc/ukadoc-coverage/values.md_

- [ ] 7.2 正典カタログを実データから生成する
  - カタログ再生成を実行し、正典由来 1,749 項目のカタログを書き出す
  - 完了時: 書き出されたカタログの項目数が 1,749、ページが 38 種、カテゴリが 6 種、URL が相異なり 1,749 件であることを確かめ、続けてもう一度実行しても 1 バイトも変わらない
  - _Depends: 6.2_
  - _Requirements: 1.1, 1.2, 1.4, 1.5, 1.6, 1.9_

- [ ] 7.3 初期台帳 4 本を生成する
  - 初期台帳の生成を実行し、shiori・assets・sakura-script・property の 4 本を書き出す
  - 既に手書きの台帳が置かれている場合は既存の項目を 1 バイトも変えず、欠けた id だけが差し込まれることを確かめる
  - 完了時: 4 本の項目数がそれぞれ 677・542・342・188、合計 1,749 になり、同じ id が 2 つ以上の台帳に現れない
  - _Depends: 7.2_
  - _Requirements: 3.1, 3.2, 3.3, 3.3a, 3.4_

- [ ] 7.4 ドメイン別報告 4 本と全体報告を生成する
  - ドメイン別報告の再生成と全体報告の再生成を実行し、5 本を書き出す
  - 完了時: 5 本がいずれも空でなく、続けてもう一度実行しても 1 バイトも変わらない
  - _Depends: 7.1, 7.3_
  - _Requirements: 7.1, 7.2, 7.3_

- [ ] 8. 常時走る整合検査と最終検証
- [ ] 8.1 実データを読み込んで整合検査を走らせる入口を作る
  - 入出力層で repo のカタログ・台帳 4 本・テーマ定義・ソース・ドメイン別報告 4 本を読み、純粋層の判定へ渡す
  - 全体報告は読まず、スナップショットの場所を組み立てる関数を 1 度も呼ばない（構造でスナップショット非依存を保証し、環境変数を書き換えるテストは置かない）
  - ネットワークにも実機にも触れず、ファイルを 1 つも作らず、一時ディレクトリも使わない
  - 完了時: 所見が 0 件で緑になり、所見があれば整形した本文がそのまま失敗メッセージになる
  - _Depends: 4.4, 7.4_
  - _Requirements: 6.1, 6.2, 6.12_

- [ ] 8.2 実データに対する整合検査を常時テストに載せる
  - 台帳とカタログの対応・割り当て・並び順・URL・証拠・関連・版・テーマ・ドメイン別報告の一致を実データで走らせる
  - 完了時: `cargo test -p ukadoc-survey` で所見 0 件の緑になり、台帳の 1 行をわざと壊すと該当 id と場所つきで赤になることを 1 度確かめて元に戻す
  - _Depends: 8.1_
  - _Requirements: 6.3, 6.4, 6.5, 6.6, 6.7, 6.8, 6.10, 6.11, 7.4, 7.5_

- [ ] 8.3 検査対象が 0 件でないことを実データで確かめる
  - カタログの項目数が 1,749・ページ 38 種・カテゴリ 6 種、台帳が 4 本で項目数がそれぞれ 677・542・342・188（合計 1,749）、割り当てが 38 ページで内訳 12・24・1・1 であることを確かめる
  - ソース走査が返すファイル数が 0 でなく、調査クレート由来のファイルを 1 つも含まないことを確かめる
  - ドメイン別報告 4 本が実在し、いずれも空でないことを確かめる（新しさの検査が空回りしていないこと）
  - 完了時: 初期台帳が全行未分類でも、これらの件数が 0 でないことが常時テストで守られる
  - _Depends: 8.2_
  - _Requirements: 6.13_

- [ ] 8.4 自前の道具を実データで較正する
  - テーマ定義の見出しが値の型の定数 8 つと順序まで一致することを確かめる
  - 自前の TOML 書き出しが、実データの見出し 1,749 件すべてについて読み戻しで元の文字列に一致することを確かめる
  - 語彙表経路の較正: 既存の語彙台帳にある 159 要素を取り出し規則で拾い、対応するページの見出しへ 159 件すべてが 1 件に定まって対応付くことを逐語で確かめる。あわせて実データの解決で語彙表経路により解決した件数が 0 でないことを確かめる
  - 完了時: 3 つの較正がいずれも緑になり、取り出し規則や書き出しが壊れれば赤になる
  - _Depends: 8.3_
  - _Requirements: 4.4, 5.4, 6.8, 6.13_

- [ ] 9. 台帳の形式と仕訳の規則をまとめた案内を書く
  - 台帳の項目形式・状態語彙 7 つ・各欄の意味と、カタログ（機械生成のみ）と台帳（人手記入・機械検査）を別責務に保つ旨を書く
  - 仕訳の規則を書く: 新旧の書式の向きの決め方（正典本文の注記→版番号→人手の判断）・版番号が無い項目を最も古いと決めつけないこと・関連の種別 6 つ・優先度の根拠 4 つと固定された序列・段階の最終決定は本 spec の外に置くこと・壊れ方の段の根拠を備考に書けること
  - 正典 URL の書き方の規約を書く: 定義箇所だけに置き呼び出し側には置かない・1 項目 1 行で説明文を伴わない・語彙表は先頭にページ URL を 1 つ置く
  - 報告の扱いを書く: 手で編集しない・食い違いは再生成で解消する・全体報告の再生成は統合担当の仕事・束 id は人手の文書から引用する
  - 調査 spec と本 spec の着地順が前後する場合の合流の手順を、どちらが先でも成り立つ形で書く
  - 用語は平易な語に限り、プロジェクト内でしか通じない言い回しを持ち込まない
  - 完了時: 上の 5 つの見出しが揃い、形式の変更には要件の改訂が要る旨と、要件の記入例にある id が実データに存在しない注意が書かれている
  - _Depends: 7.4_
  - _Requirements: 2.5, 2.6, 4.1, 4.2, 4.7, 4.8, 4.9, 5.2, 7.7, 9.5_

- [ ] 10. ワークスペース全体で緑になることと非接触を確かめる
  - `cargo test --workspace` が緑になることを確かめる
  - 新設した Rust ファイルがいずれも 1,000 行未満であること（ファイル行数上限テストが緑）を確かめる
  - 既存の実行時コード・既存クレート・ロードマップ本文に差分が 1 行も無いことを、実在するパスに対する差分で確かめる（パスが解決することを先に示す）
  - 完了時: ワークスペースのテストが緑で、変更されたファイルが新設クレートと `doc/ukadoc-coverage/` 配下だけである
  - _Depends: 8.4, 9_
  - _Requirements: 9.1, 9.6, 9.8_

## Implementation Notes

- 1.1: `cargo test --workspace` は先に i686 成果物を作らないと `shiori-host32-host` で赤になる（環境の前提であって欠陥ではない）。`cargo build -p shiori-host32-helper --target i686-pc-windows-msvc` と `cargo build -p shiori-host32-testdll --target i686-pc-windows-msvc` を一度回せば緑（99 suites・6,376 passed を確認）。
- 1.1: 新しい worktree では `vendors/pasta` サブモジュールが未展開で cargo がワークスペースを解決できない。`git submodule update --init --recursive` を一度回す（作業ツリーの展開だけで repo の差分は出ない）。
- 1.1: design.md「File Structure Plan」の本文は「`.rs` は 48 本」と書くが、同じ節の Directory Structure の木は `src/` 62 本＋`tests/` を数える。木の方が数え上げられる正本なので、木に合わせて 62 本を作った（`tests/consistency*` はタスク 8.x の持ち物）。
- 1.2: 語彙の変換は `Result<_, UnknownVocabulary>` を返す（`SurveyError::BadVocabulary` は file と id を持つが、素の `&str` を受ける parse はそれを知らないため）。`ledger::read`（タスク 2.4）は**必ず** `err.at(file, id)` で包むこと。包み忘れると要件 6.10 の「該当 id と場所を示す」がこの継ぎ目で失われる。
- 1.2: `as_key` の綴りを入れ替える摂動は往復テストを素通りする（往復は入れ替えても自己整合）。要件 2.2／4.3／7.8 の綴りを逐語で確かめるテストだけが捕まえるので、後の整理で往復テストに畳まないこと。
- 1.2: このワークスペースは `cargo fmt --all -- --check` が緑（外れ値 0 件）。新設ファイルも fmt に通すこと。
- 1.3: **差分だけの主張（`assert_ne!`）と形だけの主張（長さ・字種）は「入力の単射関数であること」しか言えず、どの関数かを 1 つも固定しない。** 公表テストベクタが ASCII だけだったため、入力を `as_bytes()` でなく `chars()` で読む取り違え（符号位置読み）がテスト 36 本すべて緑のまま通った。日本語が大半の本文では全行が黙って別の値になる。純粋関数の檻には必ず逐語の期待値を 1 本以上置くこと。多バイト 3 本を追加して是正済み。
- 1.3: 形の一般テスト（固定サンプル列）は 16 桁 0 詰めの欠落も捕まえない。捕まえるのは上位桁 0 の入力を決定的に探す専用テスト 1 本だけ。
- 1.4: 制御文字の逃がし形は一様に `\u00XX`（大文字 16 進）に決めた。design.md の D-10（規則・正）と `tomlout` 節の事前条件の括弧書き（`\n` に逃がす）が食い違って読めるが、括弧書きは入力側の文字の呼び名と読む。実データ 1,749 件の id と見出しに制御文字は 0 件なので `catalog.toml` には現れず、どちらを採っても 1 バイトも動かない。
- 1.4: 読み戻し一致（round-trip）は「単射であること」しか言えない——余計に逃がす書き出しでも素通りする。逐語一致の主張と対で置くこと（1.3 の教訓と同じ形）。区切りの空白 1 個の違いは読み戻しでは絶対に捕まらないが、要件 1.5 のバイト安定性はそこに載っている。
- 1.5: 割り当て表は要件 3.1・実データ（スナップショット 1,749 件の 38 ページ）・実装の 3 者を機械で突き合わせて一致を確認済み。ページごとの件数も要件 3.1 の表と実データが全件一致。以後この表を触るときは同じ 3 者比較をやり直すこと。
- 1.5: 逐語のページ一覧テストは実装の const 配列を参照せず、独立した文字列リテラルで書く。const を参照すると表を表自身と比べるだけになり、転記の誤りを 1 件も捕まえられない。件数だけのテストは 12 個すべてが綴り違いでも緑になる（実証済み：1 文字入れ替えの摂動は件数テストを素通りし逐語テストだけが赤）。
- 1.6: **`write_lf` は親ディレクトリを作らない。** 新しい checkout に `doc/ukadoc-coverage/` は存在しないので、生成の副手続き（タスク 6.2）が自分でディレクトリを作ること。作らないと書き出しがパス付きで失敗する（黙って落ちはしない）。
- 1.6: design.md の Directory Structure は `src/io/` に `_tests.rs` の兄弟を 1 つも載せていないが、タスクの完了条件は在中テストを要求し、steering の規約はテスト本体を兄弟ファイルへ置くことを義務づける。兄弟 3 本を新設した（1.1 の 48 対 62 と同じ、木の数え落とし）。
- 1.6: `EXCLUDED_DIRS`（target・vendors・.git）は現在の配置では 1 度も効かない——走査は `crates/` から始まり、その下にこの 3 つは無い（`vendors/pasta` は repo 根）。`vendors` を外す摂動は緑のまま通る。既存の走査器に倣った防御であって欠陥ではないが、この 3 つについては「違反 0 件」が何も証明していない。
- 1.6: 全称の主張（`for ... assert!`）だけのテストは走査が空だと無条件に真になる。非空の主張と必ず対で置くこと（1 本欠けていたのでレビュー後に追加した）。
- 1.7: `io::snapshot` は `pub(crate)` にした。`tests/consistency` は統合テスト＝別クレートなので、この形なら型検査がスナップショットへの到達を禁じる（要件 6.2 を規約でなくコンパイラが守る）。design.md の `io` 節も追随させた。負の対照（E0603）と正の対照（`pub` に戻すと通る）の両方で確認済み。
- 1.7: **タスク 6.2 で `cli::generate` が `io::snapshot` を呼ぶまで、`dead_code` の警告が 22 件出る。** `pub(crate)` が唯一の原因（`pub` に戻すと 0 件）で、ワークスペースの他クレートは無警告。`#[allow]` で蓋をしていない——本当に配線し忘れたコードを隠すため。6.2 で消えることを確かめること。
- 1.7: `SurveyError::MissingEnv` の本文が D-7 の後半（`AREKA_UKADOC_SNAPSHOT` で場所を指定してほしい）を欠いていた。design.md の Error Handling の enum も同じ欠落だったので、コードと設計の両方を直した（片方だけ直すと矛盾が残る）。
- 1.7: 部品を単体で釘付けしても、**入口がその部品を呼んでいるか**は別に守る必要がある。`load` が `absolutize` を呼ばず渡された綴りをそのまま載せる摂動は、`absolutize` の単体テスト 2 本を素通りして全 140 本緑だった。入口 1 本のテストを足して赤にした。
- 2.1: 版番号の抽出規則で**塞ぐのは数字と小数点だけ。英字は塞がない**。`SSP2.3.00以降` は正しく `2.3.00` を拾う（`ukadoc:dev_nar`・`ukadoc:dev_update`・`OnMouseWheel`・`OnMouseGesture` の 4 件が該当）。英字も塞ぐ規則にすると 406/23 が 403/22 に落ち、要件 1.2 の「23 件」に届かない。実装係とレビュアーが独立に実データで 406 件（≥1）／23 件（≥2）／最大 7 種を再現し、design.md の実測と一致。
- 2.1: 並びは**文字列の昇順**（`2.10.0` が `2.9.0` より前）。数値順ではない。
- 2.1: `total_entries`（全 2,983）と `ukadoc_entries`（正典 1,749）は別の欄。両者が一致する見本では 2 つの欄を区別できないので、テストは必ず値の違う見本で書くこと。
- 2.1: `BTreeMap` への挿入は同じ id を黙って畳む（消えた 1 件は `ukadoc_entries` が減るだけで何も言わない）。実データに重複 id は 0 件だが、黙って壊れる経路を残さないため重複を誤りとして落とすようにした。
- 2.2: カタログの 3 つの読み出し（`by_url`・`page_urls`・`titles_of_page`）は `read.rs` の `impl Catalog` に置いた。`mod.rs` には設計の木が `_tests.rs` の兄弟を与えていないので、そこに置くとテストがコードから離れる。
- 2.2: `read` は**繕わない**。`page` 欄が id と食い違う本文・知らない欄を持つ本文はどちらも落とす。繕うと `write(read(t)) == t` が破れる（書き戻した本文が入力と別物になる）。将来 `catalog_format = 2` が欄を足したときは「知らない欄」で落ちるので、版番号を名指しする文言は形式検査を入れるタスクで足すこと。
- 2.2: 設計の見本（Data Models のカタログ本文）を逐語の錨に使うときは、**design.md から機械で抜いて突き合わせる**こと。手で写した近似を期待値にしたテストは何も守らない。
- 2.2: doc コメントが防御を主張したら、その主張を釘付けするテストを置く。「最初の `#` で切る」「表の鍵で書く」はどちらも主張だけで檻が無く、摂動が全緑を通った（レビュー後に 2 本追加）。
- 2.3→2.5 の申し送り（D-12 の穴を閉じるのは 2.5 側）: 切り分けの較正は `toml` の鍵**集合**との一致で行うが、備考の中に**既存の id と同じ綴りの見出し行**が現れた場合は集合が一致してしまい見抜けない。design.md:274 はこの穴を閉じる役を「**厳密な**昇順（前の id と同じでも失敗）」に割り当てている。タスク 2.5 の本文は「id 順に並んでいなければ」としか書いていないが、**等しい隣り合わせも落とすこと**。`ledger-init` は検査（タスク 4.2）より先に走るので、2.5 が緩いとこの穴は永久に開いたままになる。
- 2.3: **摂動は「制約を緩める向き」を必ず走らせる。** 制約を足す摂動（等値比較に条件を 1 つ足す）と緩める摂動（等値比較を件数比較に落とす）は別の問いを検証する。前者は「比較が緩いことに何かが依存しているか」を聞き、後者は「比較が中身を見ていることに何かが依存しているか」を聞く。檻の強さを守るのは後者だけ。実際、較正を `cut == read` から `cut.len() == read.len()` に落とす摂動は全 208 本を素通りした。等値比較・集合比較・並び順の判定には、緩める向きの摂動（等値→件数・等値→常に真）を必ず当てること。
- 2.4: `ledger::read` は**必ず** `err.at(file, id)` で語彙の失敗を包む（1.2 の申し送り）。包む箇所は 4 つ＝状態・テーマ・関連の種別・前置きのドメイン名。前置きには id が無いので場所は `[ledger]` と書く。レビューは 4 箇所とも id 側・file 側の両方向で摂動して赤を確認した。
- 2.4→4.2 の申し送り（`LedgerDomainMismatch` は読み取り段へ移った）: `[ledger].domain` とファイル名の食い違いは `ledger::read` が落とす。`Ledger` は `domain` を 1 つしか持たずその値はファイル名から来るので、宣言された綴りが残るのは `read` の中だけ。`CheckInput` は台帳ごとのファイル名を受け取らないから、検査層はこの判定を作り直せない。**タスク 4.2 に「台帳ファイルからこの所見を出すテスト」を求めてはいけない（書けないのが正しい）。** 対の `LedgerPagesMismatch` は逆で検査層が持ち続ける——`read` は割り当て表を受け取らないため。design.md の判定表と注記も同時に直した。
- 2.4: 「この欄は存在しない」（証拠＝要件 2.3・未分類件数＝要件 6.9）は、型に欄が無いだけでは守れない。**知らない欄を落とす**ことで初めて守られる。手書きの `evidence = [...]` や `unclassified_count = 42` が黙って無視されると、書いた人は書けたつもりのまま数が合わなくなる。
- 2.4: `file_order`（本文の順）と `entries`（id 順）は**食い違える見本で確かめる**こと。text 順と id 順が同じ見本では、`file_order` が並べ替えた鍵の写しでも緑になる。
- 2.5: **1 要素の見本は並び順を 1 つも守らない。** 前置きの担当ページ一覧を 1 ページのドメイン（property・sakura-script）だけで試していたため、一覧を逆順にする摂動が 250 本すべてを素通りした。実台帳 4 本のうち 2 本（shiori 12 ページ・assets 24 ページ）がその経路を通り、要件 3.3a により前置きは以後バイト列のまま写されるので、誤った並びは永久に焼き付く。下流も助けにならない——`LedgerPagesMismatch` は集合の比較なので並べ替えを見ない。**並び順を主張する箇所は、2 要素以上・かつ入力を意図的に非整列にした見本で釘付けすること**（整列済みの見本では「整列する」摂動が素通りする）。
- 2.5: D-12 の穴は閉じた。`check_strictly_ascending` は `pair[1].id <= pair[0].id` で落とすので等しい隣り合わせも失敗する。備考の中に既存 id の見出しを隠した見本で確かめてあり、その見本は `blocks::split` が**受理する**ことをテスト内で先に主張している（受理されなければ並び順の判定に到達せず、別のことを試したことになる）。`<=` を `<` に緩める摂動が赤にするのはこの 1 本だけ＝申し送りの守りはこのテストが全部背負っている。
- 2.5: 生成済みの台帳では既存の塊が `render_initial_entry` の出力そのものなので、**「写す」を「作り直す」に変えた摂動と区別が付かない**。区別できるのは手書きの見本だけ（奇妙な空白・複数行の備考・埋まった status など、生成物と何通りにも違う形）。バイト列を写す契約を守るには手書きの見本が要る。
- 2.5→6.2 の申し送り（前置きに渡すページの並び）: `PageAssignment::pages_of` は名前順で返し、`canonical_pages`（私有）は要件 3.1 の転記順を持つ。`merge_initial` の契約は「渡された順をそのまま保つ」なので、**どちらを渡すかは 6.2 が決める**。要件 3.3a により最初に生成した前置きは以後バイト列のまま写されるため、後から変えられない。設計の `assignment` 節は `pages_of` を名前順と定めており、検査側の `LedgerPagesMismatch` は集合比較で並びを見ないので、名前順（`pages_of`）が素直な選択。
- 3.1: 取り出しの行の形は実データで較正済み——`crates/**/*.rs`（調査クレート自身を除く 1,089 ファイル）で「ukadoc」に触れるコメント行は 152 件（doc 129・行 23）、取り出される URL は 0 件。design.md の実測と一致。`ukadoc:` を字面に含む行は 4 件あり、3 件は語が文の途中、残る 1 件（`sakura/decode_tests.rs:438`）は説明文が 5 語続くので落ちる——**要件 5.3 の説明文の規則が実データで効いているのはこの 1 行だけ**。
- 3.1: コメント記号は `///`・`//!`・`//` の**長い順**に照合する。短い順にすると `///` の行が `/` を残した形で解釈され、`ukadoc:` に届かず取りこぼす。並び自体が優先順位なので、並べ替える摂動は 10 本を赤にする。
- 3.1: `extract` は行単位で走るので、**複数行の生文字列やブロックコメントの中で行頭から始まる `// ukadoc:` は拾われる**。要件 5.2 の「定義箇所だけに置く」と同じく人が守る規約の側（設計 D-4）。作業ツリーに該当行は 0 件。README にも書くこと。
- 3.1→3.2 の申し送り（`UnmatchedName` の形）: いまは `{ name: Option<String>, reason: NameMatchFailure }` で、D-5 の「表が続かない」場合に `name` が `None` になる。`None` の処理を誰も強制されないので、`enum NameMatchFailure { NoMatch(String), Ambiguous(String), TableMissing }` のようにデータ付きの列挙へ寄せると対応が構造で保たれる。**まだ参照は 1 件も無いのでタスク 3.2 が作り替えてよい。** 置き場（4 つ目の欄を足さず `unmatched_names` に相乗り）は D-5 に忠実なので変えない。
- 3.2: **NFKC は依存を足さずに解けた。** 実コーパス 1,955 文字列（スナップショット見出し 1,749＋実物の語彙表 3 本の要素 206）で NFKC が変える文字は 2 つだけ（`U+3000`→半角空白・`U+FF5E`→`~`）。実装は `U+3000` と `U+FF01..=U+FF5E`（`cp − 0xFEE0`）を写す。レビューが全 94 文字を確認し、これは NFKC の**近似ではなくその文字帯への制限**だと確定した——ゆえに誤って一致することは原理的に無く、一致し損ねる方向にしか外れない。設計 D-5 の実測 4 つ（変わる見出し 5・新たに重複するページ 0・相異なる見出し 1,657・159 件が 158→159）を独立に再現。
- 3.2: 射程の外（半角カタカナ・丸数字・合字・単位記号）を持つ見出しが将来増えると、赤にならず `NoMatch` へ黙って落ちる。**ただし `implemented` の行なら要件 6.6 が赤にする**ので、静かに失われるのは「まだ実装済みと申告していない行」だけ。見張りはタスク 8.4 の 159 要素較正で、効くのは `list_shiori_resource` の 1 ページのみ。
- 3.2: D-4 の「完全一致」は摂動 `starts_with` で 1 度素通りした（実データに前方一致の URL 対が 0 件のため、素朴な見本では噛まない）。塞いだのは `a_url_that_merely_extends_a_catalog_url_is_not_resolved` の 1 本だけで、解決の 2 段（項目 URL・ページ URL）両方をこれが背負っている。整理で削らないこと。
- 3.2: 語彙表の要素の取り出しは実物 3 本で 159／21／26 件を確認（各クレート自身の件数表明とも一致）。規則を触ったらこの 3 つの数で較正し直すこと。
- 3.3: 候補は 4 種で実データ 381 件（許可表 254・命令の登録 4・設定キー 24・ログ行 99）。設計が引用する 3 か所の件数（11・1・4）を実装とレビューが独立に再現。許可表 254 の内訳は 231 件が sylphya の語彙表 5 つ（`shiori_resource`159・`dotted`44・`flat`28 ほか）——目印の行を表 1 つにつき 1 行置けば 231 件がまるごと証拠へ変わる。**ノイズではなく、候補の仕組みが狙いどおり働いている印**。
- 3.3: 設計に無い判断を 4 つ置いた（いずれもレビュー承認）。⑴ 直上のコメントの塊に `ukadoc:` の行があれば候補にしない（要件 5.8 の「まだ置かれていない」の実装・署名がカタログを取らないので字面でしか判定できない）⑵ 設定キーは点付き小文字で末尾が拡張子でないもの（引用箇所が設計に無い・拡張子表は正典由来でない。外すと `descript.txt` 90 件と `*.png` 665 件が流れ込む）⑶ テスト本文を除く（根拠は要件 5.2 の「定義箇所だけ」であって件数ではない）⑷ ログの語 7 つはすべて実データに出現を確認（縮退 58・未知 18・無視 16・未対応 5・非対応 2・対象外 2・未実装 1）。
- 3.3: **走査部品が 2 本になり、すでにずれている**（担当タスクが無いので申し送りとして記録）。`resolve.rs` の `slice_element_names` は文字リテラル（`b'['`）とブロックコメントを扱わないが、`candidates.rs` の写しは扱う。いまは潜在——目印の抑止により両者が同じ表を読むことが無いため。`resolve.rs` の部品が私有で兄弟から引けず、3.3 の境界が `resolve.rs` を含まなかった結果。`evidence/` 直下に私的な共有モジュールを置いて 1 本化するのが筋だが、残りのタスクに `evidence/` を境界に持つものは無い（3.3 が最後）ので、**引受先を捏造せず**ここに記録するに留める。
- 3.3: 釘付けされていない規則の境目が 3 つある（いずれも現在の木では無害）。登録名は `register(` で終わる識別子のみ（`register_default("x")` は拾わない）／許可表は宣言行に `= &[` があるもののみ（複数行の宣言は拾わない）／`has_marker_above` は項目 URL も目印と見なすので、159 要素の表の直上に項目 URL を 1 本置くと 159 件の候補がまとめて消える（要件 6.6 は未分類の行を赤にしないので気付けない）。
- 3.4: **穴を 1 つ塞いでも、そのすぐ隣に同じ族の穴が残る。** 連結成分の檻で「1 歩しか辿らない `find`」を塞いだ直後、「2 歩しか辿らない `find`」が全 332 本を素通りした（4 id の連鎖では早く平らになるため）。深さに依存する実装を守るときは、**有界な深さすべてを破る**見本を選び、なぜその深さかをコメントに書くこと。書かないと次の人が縮める。
- 3.4: **「入力で反証できない」と諦める前に、境界の文字を疑うこと。** ページ名は id の 2 番目の区切りなので「id 順とページ名順は原理的に一致する」と結論しかけたが、`0`（0x30）は `:`（0x3A）より小さいので `ukadoc:a0:x:1` と `ukadoc:a:y:1` は id 順 `a0, a`・名前順 `a, a0` で割れる。実データ 38 ページには今のところ該当が無いが、`spec_shiori3` があるので `spec_shiori` が 1 つ増えれば即座に現れる。
- 3.4: 束の並びの見本は**辺の向き**も散らすこと。すべての辺を頭尾で繋いだ見本だけだと、辺を有向として扱う実装が素通りする。実台帳では 2 つの旧名がそれぞれ同じ正典 id を `alias_of` する形（1 つの id へ 2 辺が集まる）が日常的に出る。
- 3.4→3.5 の申し送り（関連の集め方）: 束のテスト `two_links_leaving_the_same_id_merge` は「同じ id から出る 2 辺」を守る。これが噛むのは、関連の一覧を `BTreeMap<from, to>` のような「元 id ごとに 1 本」の入れ物で集める実装——`alias_of` が高々 1 本という直感を `links` へ持ち込むと起きる。**3.5 が辺を組むときは 1 つの id から複数の関連が出られる形にすること。**
- 3.4: テーマの並びは**名前順**（design.md の事後条件「ページとテーマと束 id は名前順」）。要件 4.4 は 8 つの綴りを凍結するだけで表示順を定めない。「名前順」は UTF-8 のバイト順であって五十音順ではない（交わり・掛け合い・更新・気配・気配り・装い・触れ合い・記憶）。状態だけは要件 2.2 の語彙順。
- 3.4: 道具が嘘をついた 2 件——`grep -c $''` はコマンド置換の中で空パターンに退化し「全行に CR」と報告する。`od -c | grep` も偽陽性を出す。CR の有無は素直にバイトを数えて決めること（[[subagent-tooling-can-be-wrong-calibrate-it]]）。
- 3.5: **報告の本文そのものが契約**。タスク 4.4 の新しさの検査は本文をバイト単位で突き合わせるので、版面を変えると 4 本の報告が一斉に古くなる。`domain_tests.rs` の `the_body_is_pinned_verbatim` が版面の正本で、期待値は実装の定数を 1 つも参照しない独立したリテラル。版面を変えるときはここから引き直すこと（安易に焼き直さない）。
- 3.5: **仕様側の穴（3.6・タスク 9・調査 spec 4 本へ）**: 束の辺は `links` だけから作る（設計 D-11 が明記）。ところが付録 A.2 は `alias_of`／`supersedes` を専用の欄だけに書いてよいと定めている。`alias_of` だけの対は別名の一覧（項目 ⑶）に出るので見えるが、**`supersedes` だけの対はドメイン別報告のどこにも現れない**。README（タスク 9）に「関連として束に出したい繋がりは `links` にも書く」と明記するか、設計の裁定が要る。
- 3.5: 壁時計の不在は「4 桁続く数字が無い」で守る。二重描画の一致テストでは代用にならない——2 回の描画はマイクロ秒差なので、秒単位の時刻を焼き込んでも同じ値になり素通りする。
- 3.6→7.4・8 の申し送り（時刻の見張りは見本に閉じる）: 全体報告の「時刻を含む行は 1 行だけ」の判定は「4 桁続く数字」で時刻を見つける。**実データの全体報告には `| 合計 | 1749 |` が出るので、この判定を実データへ持ち上げると偽の赤になる。** 見本に閉じている限り安全で、7.4 の完了条件は「空でない・2 回で同一」だけ、8.1 は要件 7.6 により `summary.md` を読まないので、どちらもこの見張りを引き継がない。
- 3.6: 二重描画の一致テストは壁時計の不在を守れない（2 回の描画はマイクロ秒差なので秒単位の時刻を焼き込んでも一致する）。時刻の不在は別の判定で守り、その判定自体を両方向（時刻らしい行を見つける・表の行を見つけない）で較正すること。
- 3.6: 証拠のパスが本文へ漏れていないことの主張は、**見本に現れるパスを 1 つでも数え落とすとその分だけ穴が開く**。`unresolved` と `unmatched_names` のパスも含めること（1 本抜けていて、漏らす摂動を逐語の錨だけが捕まえていた）。
