---
inclusion: manual
updated_at: 2026-08-27
---

# Roadmap — areka M1（最小 SSP 互換ベースウェア）

> **このロードマップは M1 のみを扱う。** M2 以降は **M1 完成後に実物を見て組み直す**（憶測で先に書かない）。
> 正本配置: 本ファイルが M1 ロードマップ正本（`.kiro/steering/roadmap.md`）。`focus.md`（`inclusion: always`）から辿る。設計判断の正本は [doc/COMPAT_ARCHITECTURE.md](../../doc/COMPAT_ARCHITECTURE.md)。M1 実物スコープは [doc/emo2-conformance-scope.md](../../doc/emo2-conformance-scope.md)。
> **履歴**: 追記①〜(51)・M-boot 完了詳報等は 2026-07-31 棚卸④で、W5 完了詳報・旧ウェーブ表・旧干渉台帳・追記(53)(54)(56)(57) 全文は 2026-08-01 棚卸⑤で、**W6 行全文・W6 完了詳報バレット（col/vis/scg）・追記(58)(60)(61)(62)(63)(64) 全文は 2026-08-14 棚卸⑧で**、**W6 完了詳報バレット全文・旧 W6/W6.5 行全文・旧干渉台帳・追記(65)(66)(67)(68) 全文は 2026-08-15 棚卸⑨で**、**旧 W6.75 行全文・旧ゴール表 atom 行・追記(69)(75) 全文は 2026-08-22 棚卸⑩で**、**W6.9 行全文（cage/dlp）・追記(79)(80)(81) 全文は 2026-08-27 棚卸⑪で**、いずれも [roadmap-history.md](roadmap-history.md) へ退避（history が全文正本・既知の記録欠陥〔㊻番号衝突・**(55) 欠番**〕も history 冒頭と棚卸⑤節に注記）。完了ユニットの実装詳細は各 `completed/` spec が正本。

## M1 ゴール

areka（**x64**）が最小 SSP 互換ベースウェアとして、適合対象ゴースト **emo2**（作者自作・脳=`pasta.dll`・**32bit SHIORI**）を「**そのまま**」起動→会話→撫で→メニュー→終了まで E2E 実走させる。

- emo2 が動く＝同じ汎用 32bit ブリッジで里々/YAYA も動く土台（互換＝普及の入口）。
- 「伺かっぽいマスコット」ではなく「**伺か互換系**」であること自体が長期ロードマップの起点。
- M1 スコープは emo2 が実際に使う機能で**実物定義**。完全網羅・予測実装はしない。

## 実装規律（balloon-system の失敗から得た正）

- **実装ファースト**: 各作業ユニットの成果物は「emo2 が実際に動く」検証済みコード。
- **spec 工場の禁止**: 成果物が子 spec になる構造を作らない。1ユニット＝1かたまりの動く振る舞い。
- **最小実装＋薄い拡張シーム**: emo2 が使う分だけ実装し、拡張は型/レジストリの口だけ残す。抽象は「2例目の実物」が要求してから。
- **動く資産から建てる**: ゼロから再アーキテクトしない。
- **粒度基準**: 1ユニット＝単一 pass/fail の独立観測。純粋層は fixture/mock 直入力で切る。**UI 位置決め・座標系は逆＝本番ゴースト（実 emo2）＋実 DPI（≠96）が観測条件**（dpi=96 の自己整合が欠陥を隠す・記憶 areka-placement-real-ghost-first）。
- **⚠️ 語彙完備・配線ゼロの追跡**: 語彙先送りの規律は効いているが配線の追跡が弱い（実例 5 件: `compose_into`・`ZOrder`・`PlacementRoute::SpawnInitial`/`Restore`・`capture_logs` 複製増殖）。先送りシームには狭い `#[allow(dead_code)]`＋実在理由の doc を義務付け、消費者ゼロの検出は棚卸の定期監査項目とする。

## アーキテクチャ横断原則（要約・詳細は history＋記憶＋completed spec）

- **シェル/バルーン統一**: 描画エンジンはシェルとバルーンを区別しない。バルーン＝surface 上の文字レンダリング層。element は他サーフェス参照可（入れ子・再帰合成）・配置は D2D 変換行列が内部表現。
- **アニメエンジンは2つ**: ①さくらスクリプト再生（talk timeline・sakura）＋②SERIKO ループ（seriko）。両者とも dola（絶対時刻台本・duration 権威・CuePlayer/CueSink）上。テキストは①から emo-text へ直接。
- **並行モデル**: 各エンジン＝チャンネル通信のアクター＋独立スレッド。**render/window は UI スレッド固定**。機構=areka-actor／経路=kanade／結線=ghost の責務三分。
- **emo 合成**: 自前コンポジタ（アトラス→1枚物ビットマップ合成→wintf へ完成品のみ）。emo=UI 層全般（合成・マウス/さわり・バルーン文字・選択肢）＝「見える・触れる」の窓口。
- **DPI 追従が基本設計**: k=monitorDPI÷author_dpi で全表示経路がスケール（W4 で実装済み・SSP と別思想・k=1.0 は途中状態）。**キャラ窓の原点は下端中央（足元の中心）**——**キャラ窓位置の保存/復元/resize の三層**で統一済み（Bottom 限定）・左上基準で計算しないこと。**⚠️ バルーン追従は例外＝「窓（char 左上）相対」**（2026-07-31 実機裁定・`kero-balloon`）——全アンカーで `balloon_pos − char_pos ≡ offset` 不変・リサイズで offset を補正しない・保存も生 offset。旧記述「四層で統一」は誤り（詳細は history）。

**エンジン固有名**（コード/spec/会話の参照はこの名で統一・詳細は記憶 areka-engine-names）:

| # | エンジン | 固有名 | # | エンジン | 固有名 |
|---|---|---|---|---|---|
| ⓪ | ゴーストエンジン（最上位 owner） | `ghost` | ④ | さくらスクリプト再生 | `sakura` |
| ① | SHIORI 通信層 host-32 | `shiori` | ⑤ | SERIKO アニメ | `seriko` |
| ② | parser/loader | `parsers` | ⑥ | render（surface 合成＋UI 層） | `emo` |
| ③ | conductor（SHIORI イベント循環） | `kanade` | | | |

## 完了サマリ（2026-08-27 時点・詳細は completed spec ＋ history）

- **耐力壁突破**（2026-07-01 `pilot-shiori-host-32`）: x64→32bit pasta.dll 駆動 GO。
- **M-boot 23/23 完了**（2026-07-13 `emo2-boot`）: 起動→表示→talk→close の可視一周。①shiori・②parsers トラック全完了。
- **増分ウェーブ W1〜W4＋割込 全完走**: W1〜W3（idle-talk／collision-geometry／sakura-dialogue-tags／input-events／mayuna-compose／sylphya／seriko-loop／choice-render）→ 割込（wintf-gpu-test-crash）→ W4（position-persist ∥ choice-interact ∥ emo-dpi-scaling＝DPI 追従 k の全表示経路適用）。横断: cue-playback-duration・surface-resize-resnap・balloon-face-cue・emo-text-viewbox ✅。
- **W5 完了 4/4**（col は W6 へ編入）: `choice-select-events` ✅（07-31・**M-dialogue 4/4 完走**・実 DPI 120 人間サインオフ）・`kero-balloon` ✅（08-01・PR#97・**SSP 裁定 2 件**＝`windowposition.x` 符号非反転／**バルーン追従は窓相対**）・`dpi-window-vanish` ✅（08-01・PR#98・S1〜S4 是正・実機全判定 PASS・確定台帳の裁定付き訂正運用が実例確立）。詳報バレット全文は history。
- **W5.95 完了**: `file-slimming` ✅（08-10・PR#103）＝ソース肥大の全域是正。**最大 8,472→986 行・1,000 行超 54→0 本・ファイル内テスト 500 行超 49→0 本**・三不変量（テスト総数 4,790 不変・本文一致 61/61・対応表全単射）成立。一度 GO を撤回して群 8 を追加した経緯は `completed/areka-P0-file-slimming/verification/notes.md` §39/§54。
- **W6 完了 5/5**（2026-08-05〜08-13・**完走**）: `collision-dpi-hittest` ✅（PR#100・当たり判定 ÷k＝丸め権威 `unscale_coord`・実 DPI 2 水準サインオフ）・`balloon-visibility` ✅（PR#106・可視性を presenter 第一級概念へ＝空バルーン根治・残件は `balloon-canon-residue` へ送付済み）・`bindoption-exclusivity` ✅（PR#105・表情固着根治＝bindoption 3 値正典＋保持コマ掃除 3 段・`mayuna-compose` R4.5/D11 覆し）・`ghost-window-zorder` ✅（PR#107・案 A Win32 owner 構造保証・topmost 帯引き込みは `HWND_TOP` へ是正）・`scope-chain-gap` ✅（PR#108・P2 幅差隙間根治＝SSP 実測 8 本正典＋一度きりの `finalize_chain_once`）。詳報バレット全文は history（棚卸⑨退避）。
- **W6.5 完了 3/3**（**完走**・2026-08-14〜08-15）:
  - `recompose-budget` ✅（08-15）＝**常駐アイドルの CPU 消費の決着**。要件 3.1「定常アロケーション完全ゼロ」を実機で成立（4 発生点すべて 0・長時間 1503 適用/1425 秒でも 0）。**1 コマ適用 22,210 → 1,240µs（18 分の 1）**・p95 77,880 → 7,152µs・**CPU 24.9% → 約 11%**。四段＝定常アロケーション 0／冗長ゼロ埋め除去／リサンプル計算の作り直し（乗算 24→12・範囲検査 16→4・恒等性は横方向 42.9 億組の**全数検査**で確定）／**キャッシュ容量 1 → 3・LRU（開発者裁定・上流 R4.1 まで追随改訂）**。**メモリリークと負荷の単調上昇はいずれも実測で消滅**（Private 12→24 分で +0.1MB・回帰の傾き +0.84 → −0.045 %/分）。**ビルド設定は裁定済＝`opt-level='z'` 据え置きが正しい**（是正後は O3 が 26〜33% *遅い*）。⚠**最大の成果は「どこを削っても無駄か」を確定させたこと**——`apply_show` は着手前でも CPU の 10.4%・現在 3.3% しか占めず、真の最大項は `try_tick_world` が 13 スケジュールを 120回/秒 全部回していること（1 tick 578µs・**tick の 98% は表示に変化なし**）。**SSP との同一手順比較を初実施**（areka 10.97% 対 SSP 3.05%）し、要件 4.4 の出所不明だった較正値に初めて裏を取った。残る未達（⑵ 進行境界スキップ・⑷a CPU）は **`draw-load-parity`（W8・優先度低）へ引受け済み**。
  - `scale-exact-rational` ✅（08-14・PR#110）＝**f32 供給面寸の +1 を許容する裁定の登記**。厳密化（`ScaleRatio` num/den の文字層配管）は**却下**され、着地物は⑴既知欠陥登記を裁定済みへ書き換え⑵「寸法演算に f32 を使わない」絶対規則の 4 箇所へ唯一の例外を明記⑶前提の決定論テスト⑷下流申し送り。**実行時の挙動は不変**——製品コード 3 ファイルから doc 行を除くと main と byte 一致（式・署名・use・属性の不変が構造として成立）。裁定の土台を檻に入れた実測: 到達 23 比 × 寸 1..=1200 ＝ 27,600 組で差は常に 0 か 1（**−1 は 1 件も出ない**＝文字が切れる方向に転ばない）・差 1 は 162 件＝**6/5 と 12/5 で各 81 件**・残る 21 比は 0 件。正体は「1.2 の f32 表現」一点。**下流の宿題**: 適合 e2e は供給面寸の判定に **+1 許容**が要る（窓 client 寸は丸め権威経由ゆえ従来どおり絶対値・両 brief へ追記(68) で申し送り済み）。
- **W6.75 `dpi-transition-atomicity` ✅**（08-22・**PR#114**・**残 `balloon-offset-dpi`**）＝**DPI 遷移中の窓の跳ねの是正と、その機序の確定**。逐次適用は消えた——1 遷移で 4 窓の書込が散らばる幅が **93〜158ms → 40〜101µs**（約 1,500〜2,000 分の 1・B-2a 合流＋B-2b `DeferWindowPos` 一括）。決定論 8 遷移すべて PASS（同一フレーム・窓ごと 1 回・経路 A 0・接地点差 0・連鎖 1 回・随伴の同一フレーム性）。**実機サインオフは `ATOM-SIGNOFF: FAIL` のまま開発者裁定 GO で閉じた**——合否は書き換えていないので「FAIL だが GO」と読むこと。未達は µs 2 系統（`visualize_to_write_us` 210,329〜306,301µs＝上限 16,667µs の 12.6〜18.4 倍）に限られ、引受先は `present-write-coherence`（W8）。**37 タスク**（うち **6.5 と 7.5 は検証・最終ゲートが本番の欠陥を掘り当てて新規起票**——どちらも「整合ゲートの守備範囲の列挙に穴がある」同型で、当日どのテストも赤にしていなかった）。恒久資産＝既定 OFF の観測チャネル `wintf::transition`（10 種のレコード）・実機ログの機械判定ランナー・サインオフ手順書。**マージ時に main の Bevy 0.19 が `ExecutorKind` を撤去しており、本 spec の檻 5 本を実行器 API の移行で追随させた**（`get_executor_kind` に後継が無いため、空振り防止の番人は構築側の字面検査へ移した）。
- **W6.9 `draw-load-parity` ✅**（08-23）＝**「どこが重いか」を機械が自分で測り直せる形の着地**。仕組み＝スレッド名簿＋4 段の帰属（過程/スレッド/関数/相）・tick の相別観測・**tick の門**（`AREKA_TICK_GATE`・既定 OFF）・追随チェックの自動化・台帳駆動の自走ループ（道具 10 本＋エージェント 4＋スキル）。**実走 3 周は全て不採用**（周 1 は道具の穴で結論撤回・周 2 門 ON は Δ−3.67 が ばらつき 6.99 に埋没・周 3 単スレッド実行器は allocs/catch-up 悪化）→ 頭打ちで STOPPED・**実行時の挙動は着手前と同じ**（`Cargo.toml` 非接触）。**最終判定 release 22.3%（目標 3.0% 未満は未達＝⑵⑶⑷a）**・残る最大項は段② 名簿外 51.8%。**合否外の最大の手がかり＝門 ON＋点灯 7 分で定常 3.30%（tick の 87.6% を省略）対 門 OFF 17.04%**——門の本採用（夜間・25 分・n≥3・catch-up 規則の再定義・昇格で段③）は**`tick-gate-adoption` が引受け済み**（2026-08-27 棚卸⑪起票・M2 解禁ゲート＝dlp requirements.md 改訂欄が申し送り正本）。
- **W6.9 `test-cage-determinism` ✅**（08-27・**PR#119**・**W6.9 完走＝dlp と 2 本**）＝**テストスイートから非決定を取り除く**。共有 crate `log-capture-kit` への全 8 crate 移行（`placement/test_support.rs` 等は委譲 shim 化）・待機の有界化・失敗注入・見張り 4 本・錮の退役と定義削除（要件 7.4 改訂）。**開発者裁定で範囲が広がり要件 12／13 を新設**——① 一時パスのプロセス間衝突の解消（窓口 crate **`temp-path-kit`** 新設＋22 ファイル移行＋迂回の見張り）② 常時化の費用の測定（結論＝「この測り方では検出できない」・符号反転・幅は差の 28 倍＝**差が出たことにしない**）。**A/B 完環**＝同一条件で 緑 27・赤 3 → **緑 30・赤 0**・`areka-sylphya` は 160 走行中 48 赤（30%）→ **160/160 緑**。ワークスペース **5,894 passed / 0 failed**（95 バイナリ）。⚠ 教訓 2 種＝対象選定のファイル単位絞り込みが混在ファイルを丸ごと落とす（20→22・同じ穴が見張りにも残る＝限界をコード内に明記）・**「全緑」は十分性の証拠にならない**（2 度実証）。残作業＝`/kiro-steering`（structure.md の crate 一覧欠落 12 本・本 spec が作った欠落ではない・2026-08-27 開発者裁定＝spec を立てず steering 同期で解消）。
- **W6.95 `present-write-coherence` ✅**（08-28・**見送り＋登記で完了＝是正コード 0 行**）＝**「直さない」という裁定を、仕様の内側から見える形で閉じた**。4 候補すべて却下——B-3 は刻印位置の構造から上限 16,667µs に**届かない**（隙間の下限＝flush 総所要 143,231µs 以上）／届く唯一の形 B-3′ は要件 1.2⑵ 該当＝大改造で除外／B-4 は合否量を **1µs も動かせず**当たり判定原点への接触リスクだけが残る／観測のみ先行は「いずれ直す前提」が無い。**⚠ゴールは未達のまま**——`visualize_to_write_us` 32 件（上限の 12.6〜18.4 倍）＋`flush_total_us` 8 件＝**計 40 件**が残り、**引受先は存在しない**（`tick-gate-adoption` は門の本採用が担当で本量は担当しない）。将来直すには**新規仕様の起票が要る**。既存サインオフ判定がこの 2 量の違反を報告し続けるのは**正常な状態**であり、未達の解消と読んではならない。「絵と窓のどちらが先に画面へ出るか」は**未特定のまま**（提示側の観測機構がリポジトリに 1 つも無い）。**差分は仕様文書のみ・`crates/` と `Cargo.toml` の差分 0 行・実行時の挙動は着手前と同一**（実在証明つきの項目別検査＋赤を出せる対照で証跡化）。ワークスペース全体テストは **5,894 passed / 95 ターゲット全 ok**（＝要件 7.7 の充足であって合否量の充足ではない）。**⚠検証が道具の穴を 4 件掘った**——最大は**実在しない pathspec に `git diff` を当てると空出力・exit 0 が返る**（設計の接触禁止集合が実在しないパスを指しており、項目別検査が 1 項目まるごと空振りしていた）。他に「素朴な文字列一致が偽の一致を作る」「`grep -n` の出力から行が落ちても気づかない（4 回）」「空振り確認に依存関係の無いテストが混ざる」。**⚠完了時の注意**: `crates/` に残る 2 行の `//!` コメント（移動前の spec パス）を「ついでに直す」と非接触の証跡が偽になる——**触らないこと**（詳細は pwc `verification/notes.md`「完了時の注意」）。**W6.95 残 3 本＝bod ∥ zsp ∥ bvc**
- **W6.95 `balloon-vertical-canon` ✅**（08-29・**バルーン縦書きの SSP 正典化**）＝**SSP 正典キー `vertical,0/1` を第一級で受け、areka 独自の「origin クランプ正準」を撤去した**。受口は転記層へ additive（`new()` 7 引数のまま＝30 呼出へ波及 0）、共存規則は `WritingDirectionDecision` という**単一の決定点**へ畳んだ（`WritingMode::resolve` は薄い委譲＝14 呼出へ波及 0・`writing_mode` が `vertical` に勝ち、矛盾併記は警告でなく DEBUG 1 件）。**撤去の本体**＝宣言された `origin` は validrect の内外を問わず字義どおり用い、範囲外は DEBUG 記録のみ（`range` が返値に影響しないことを不変条件として檻で固定）。⚠**意味論の棚卸しが設計の誤判定を掘り当てた**——`emo2-kakukaku-wplimit` を「範囲内で不変」としたのは**基層だけを見た判定**で、面別上書き層を重ねると範囲外（是正対象は 4→5 件）。design のどの行にも無かった in-code 1 件（撤去後も緑のまま＝全緑では検出不能）と、宣言された生値を継承の証拠に使う**第 3 類**テストも同時に発見した。**「檻を編集より先に」が証跡として機能**——実ゴーストの開始点を固定する檻をクランプ現存下で作り、フィクスチャ是正とクランプ撤去を**跨いで 1 バイトも変えずに緑**であることが挙動不変の直接証拠になった（2 方向の変異注入で 2 本の檻が両向きを分担して閉じることも実測）。`\_l`・プロパティ・`\f` 装飾は**一切実装せず**語彙登記＋追跡先の双方向登記へ縮小（COMPAT §8 へ 13 行）。ワークスペース 5,949 passed / 0 failed・実機は実 emo2 ゴーストで `vertical,1` の縦書き実表示を目視確認（ERROR 0・正規終了）。残件は受け皿 `balloon-canon-residue` へ項目 11・12 として起票済み（縦書き字形の観測点・未知値 warn の文言）
- **実機サインオフ発見 7件中 #1〜#6 解決済み**。**#7（冒頭空行）のみ pasta 上流（`ekicyou/pasta` 起票済み）＝areka スコープ外・未解決**。
- 完了 spec 直下エントリ = **166**（`.kiro/specs/completed/` 直下・2026-09-02 実測＝`scope-zorder-pinning` のマージ後の値。内訳の根拠: マージ直前の作業ツリーが 165 エントリ〔`balloon-offset-dpi` まで反映済み〕・本仕様の archive で +1）。計数は**直下エントリ数**で行うこと（ディレクトリ数だけ数えると 1 ずれる）。
- **W6.95 `balloon-offset-dpi` ✅**（08-29・**W6.95 の 2 本目**）＝**バルーン位置オフセットの単位空間を 1 つに畳み、拡大率遷移で基準から引き直す形の確立**。合流欄と追従オフセットが持つ値を「現在の表示 DPI における物理 px」の単一空間と定め（権威＝`placement/follow/offset_space.rs` のモジュール doc）、供給元ごとの換算軸を確定（`descript` の `balloon.offsetx/offsety`＝シェル軸〔**本仕様の新規決定**〕・`windowposition` 由来＝バルーン軸〔既存温存〕）。遷移の追随は**基準対から表示 DPI 比で毎回引き直す**ため誤差が連鎖せず、同じ DPI へ戻れば **bit 同一**。残差は 2,880 組の全数列挙で**実測最大 2px**（上限 3px）。**永続値は明示の例外**＝物理 px の生値のみ・復元は未係留で最初の観測時に係留（2026-08-14 の裁定を、費用の前提が変わったことを開発者へ提示したうえで改めて継承）。キーワード由来の基本位置とは**排他**（素材未消費のあいだは `verdict=keyword-pending` で見送る）。COMPAT §8 へ 3 行登記。**26 タスク**。恒久資産＝観測種別 `kind=offset`（判定語 6 語）＋起点 `kind=windpi`＋追随レコードの機械判定器＋実機サインオフ手順書。**実機サインオフ合格**（08-28・遷移 15 本・offset 行 14 件・違反 0 件。往復は合成入力でゴーストを掴めないため**人の手でドラッグ**）。⚠ 教訓 3 種＝⑴ **裁定で射程が広がったら design の境界節まで追随させる**（wintf の起点語彙の所有と完了仕様 `window-placement` U5／DD1 の緩和が design から辿れない状態で GO 目前まで来た）⑵ **判定器は経路を見分けない**ので外から窓を動かしたログでも緑が出る＝人が守る規律 ⑶ サインオフ検体は scope 0 を `windowposition.x,center` にしてあり、`center` の正典どおり重なりゼロで高く出る＝**見栄えの観測点にならない**（実機で誤読しかけた・検体 readme と手順書に登記）。
- **W6.95 `scope-zorder-pinning` ✅**（09-02・**W6.95 の 4 本目＝W6.95 完走**・**改訂第 2 版で着地**）＝**`\![set,zorder]`／`\![reset,zorder]`／descript `seriko.zorder` によるスコープ窓の重なり指定を、「毎巡の観測と是正」ではなく Windows の所有関係（owner）の一直線の鎖という構造で保証する形へ作り替えた**。初版は完成検証まで到達しながら実機で落ちた——所有の書込はすべて成功を返すのに重なりが動かない。そこで要件フェーズへ差し戻し、**正典 SSP と同じ「所有の鎖」を要件 14 で固定**した（WHAT でなく HOW を要件に置く意図的な逸脱・理由は「所有された窓は必ず所有者より手前」という OS の不変量が分岐なしの鎖に全順序を与えるから）。要件 15＝どれか 1 群でも有効なあいだは**全ゴースト窓が 1 本の鎖**（群は登記順・未指定スコープは `[バルーン, キャラ]` の塊で後方参加）。既存のペア機構の 2 窓は**この鎖の部分列**であり、新設は跨ぎの辺だけ（実走で 1 link＋2 pair＝4 窓で 3 連という算術が一致）。**⚠ 実測が 2 度、前提を覆した**——⑴ **Windows が鎖を並べ直すのは「所有している窓」が動いたときだけ**（鎖の頭は何も所有しないので頭を動かしても並ばない）⑵ **既定の不可視 IME 窓はスレッドが最初に作った窓に所有される**ため、囮を 1 枚先に立てないと実窓の檻が汚染される。⑵ は research §12.2 の測定を汚染しており、**出荷しかけていた後押しの形は隔離環境で 0/24** だった（task 2.3 で作り直し）。退役＝旧維持系 10 ファイル 5,966 行を削除。COMPAT §8 へ 16 行登記（裁量 14／訂正 1／先送り参照 1・`seriko.zorder`＝「SERIKO のレイヤ順」という既存記述の訂正を含む）。**検証**: `main` 合流後のワークスペース **96 対象 / 6,371 passed / 0 failed**・実機サインオフ PASS（8 走行・判定器の較正 25 ケース）・**目視サインオフ合格（開発者承認・09-02）**——アプリのログ／判定器／外部プロセスからの z 順走査／開発者の目の**四点が一致**。⚠**裁定待ちの残件 9 件**（research §13.8＝2 件・§13.9＝7 件）。うち 3 件は「**檻が浸食に対して片側しか守っていない**」形で、本仕様が繰り返し掘り当てた欠陥類型と同根。⚠ 申し送り＝`zorder-property` と `currentghost-property-tree` の二重所有裁定（M2 ゲート節に登記）。

## M1 残工程ゴール表（2026-08-27 棚卸⑪・完了行は完了サマリへ集約済み）

| 種別 | ゴール（単一文） | ユニット | ウェーブ |
|---|---|---|---|
| 見た目 | 遷移中に絵と窓が同じ提示フレームで揃う（要件 4.2 の実機側・可視化→書込の隙間 0.21〜0.31 秒） | **引受先なし**（`present-write-coherence` ✅完了・**見送り＋登記**＝ゴールは未達のまま・再着手には新規仕様の起票が要る） | **W6.95**（4 本並走の 1 本目・「cage の後」＝W6.9 完走で充足・棚卸⑪申し送りは pwc brief）・**要件討議裁定 2026-08-27＝見送り＋登記で確定**（B-3/B-4 とも不採用・実行時挙動不変・是正は行わずゴールは未達のまま登記して閉じる） |
| 挙動バグ | DPI 遷移時の `BalloonFollow.offset` スケール意味論確定（キーワード中央揃えの遷移後ずれ解消を含む＝atom D10 裁定の実装側） | `balloon-offset-dpi` ✅ | **W6.95 完了（08-29・完了サマリ参照）**（**開発者指示 2026-08-22＝優先度低**・4 本並走の 2 本目・**e2e より後ろへは送れない**〔適合 #1 が前提〕・追記(70)＋棚卸⑪ブロックが申し送り正本） |
| 互換機能 | `\![set,zorder,ID,...]`（b/s 記法＝バルーン込み厳密制御）によるスコープ窓 Z 順ピン留め＋`\![reset,zorder]`＋descript `seriko.zorder` 読み口 | `scope-zorder-pinning`（略称 zsp） ✅ | **W6.95 完了（09-02・完了サマリ参照）＝W6.95 完走**（4 本目・初版は実機で落ちて要件フェーズへ差し戻し、**改訂第 2 版＝所有の鎖**で着地） |
| 互換機能 | バルーン縦書きの SSP 正典化＝`vertical,0/1`（2.8.80/2.8.83）を第一級で受け自前拡張 `writing_mode` と共存（プロパティ `.vertical` と `\_l` は語彙登記＝実装は M2 ゲートの `currentghost-property-tree`／`cursor-tag-canon` が所有・追記(85)(86)） | `balloon-vertical-canon`（略称 bvc）**✅完了**（08-29） | **W6.95**（4 本目として並走・2026-08-27 起票＝追記(83)・ファイル素は実測済み・**討議 #4 で sylphya 語彙表への行追加が消滅＝zsp との要ウォッチ解消**） |
| M-e2e | 適合14項目一周＋DoD＝**M1 完成宣言** | `emo2-conformance-e2e` | **W7**（最終） |

> 完了済みマイルストーンのゴール表は history 参照。M-dual は退役＝e2e 適合 #10 へ吸収。

## ウェーブ編成（着手順の正本・2026-08-27 棚卸⑪改訂）

> 各ウェーブは**フルライフサイクル**（要件→設計→タスク→実装→`/kiro-complete`＝PR squash マージ）を完走してから次へ。並走はウェーブ内のみ（1 spec = 1 worktree = 1 PR）。同居は**実測で共有ファイル 0**が原則。文書フェーズ（要件・設計）は先行可＝先行 spec はウェーブ開始時に settled main へ再突合。優先順位: **挙動バグ → 依存ツリーが長く早期着手が効くもの → その他**。詳細な申し送りは各 brief の追記ブロックが正本（roadmap は編成と条件のみ持つ）。

| Wave | ユニット | 開始コマンド | 編成根拠・条件 |
|---|---|---|---|
| W1〜W6.9 ✅ | （W5=4/4・W5.95=slimming・W6=5本・W6.5=budget ∥ exact ∥ wpl・W6.75=atom・W6.9=cage ∥ dlp。完了サマリ参照） | — | 詳細は history（旧 W6/W6.5 行全文は棚卸⑨・旧 W6.75 行全文は棚卸⑩・旧 W6.9 行全文は棚卸⑪退避）。**生存する申し送り**: ⑴**⚠ vis は zorder の再表示シーム `ReassertZOrder` を消費せずに着地**（再表示直後のバルーン隣接は実機未確認）——**e2e／cage で拾うこと** ⑵配置系 spec は `window-placement` R2.9 を正典として引用しない（正典は COMPAT §8 経由で scg へ一意に辿る） ⑶**atom の実機未達 2 系統（µs）は pwc（W6.95 へ前倒し）が引受け済み**・atom→後続の申し送りは追記(70)〜(78) として各 brief に登記済み（台帳参照） |
| **W6.95**（4 本並走） | `present-write-coherence` **✅完了**（08-28・見送り＋登記・是正コード 0 行・**ゴールは未達／引受先なし**） | （完了・`.kiro/specs/completed/areka-P0-present-write-coherence/`） | **bod と並走（2026-08-22 開発者指示で W8 から前倒し・「cage の後」条件は W6.9 完走で充足**——「大改造が必要なら無理に治さなくて良い」裁定は不変＝要件段階でまず規模を見積もり、tick 大改造に及ぶなら見送りも正**）**。bod とはファイル素（本 spec＝`presenter/show.rs` 可視化の段・bod＝`placement/follow` 系）——B-4 を採る場合のみ `mount.rs` 配置契約で bod の関心事に意味論上近接（要ウォッチ・下記台帳）。atom が要件 4.2 を**決定論では満たし実機では満たさずに**閉じたぶんの引受先。出発点は atom 設計 **C8 の B-3（可視化の 2 相化・第一候補）／B-4（窓内下端中央補償・緩和）** で、候補表の外へは広げない（atom 要件 3.4 を継承）。**実測の起点**（`atom-73-signoff-1`・8 遷移・全遷移を走査した値）: `visualize_to_write_us` **210,329〜306,301µs**（上限 16,667µs の **12.6〜18.4 倍**・違反 32 件）／`flush_total_us` 143,231〜231,910µs（同 8 件）＝**実機専用系統の違反は計 40 件**。**B-2b は隙間を縮めなかった**（`flush_total` 平均 192,247→**188,711**µs＝**−1.8%**・OS 側が過半＝L7）。**窓ごとの隙間はむしろ +27% 伸びた**（全窓の書込がバッチ末尾へ揃った帰結・台帳 §11.6）。接触面は `presenter/show.rs` の可視化の段（`apply_show`:46 の末尾＝`set_visible`:375／`set_bounds`:381／`Visualize` 発行:392）。**cage（W6.9）の後**に置く（同じ `apply_show` 鎖を触るため）。B-4 を採る場合は当たり判定の原点（`collision-dpi-hittest`）と `mount.rs` の配置契約に触れるので atom 要件 10.1 の再確認が要る。**tick 構造の大改造に及ぶなら atom 要件 9.3 に従い分割を再裁定する**（要件段階でまず規模を見積もること）。判定器・観測語彙・サインオフ手順書は atom の着地物を流用（新設不要）。**→ 要件討議裁定 2026-08-27＝見送り＋登記で確定**——⑴ 文言どおりの B-3（可視化を窓書込の直前へ）は刻印位置の構造から上限 16,667µs に届かない（隙間の下限＝`flush_total_us` 143,231µs 以上）⑵ 届く唯一の形 B-3′（flush 完了後に可視化を駆動）は pwc 要件 1.2⑵「tick と flush の駆動関係の変更」該当＝大改造で除外 ⑶ B-4 は合否量に効かず当たり判定原点への接触リスクが費用に見合わず却下。**実行時挙動不変・未達 40 件と引受先なしの登記が正本＝pwc requirements.md「未達の登記」節**（規模見積りの素材は pwc research.md） |
| **W6.95**（4 本並走） | `balloon-offset-dpi` **✅ 完了（08-29）** | — | **pwc と並走（2026-08-22 開発者指示＝優先度低のため W6.8 単独案を撤回し本ウェーブへ後送。ただし e2e の前提〔適合 #1 DPI 検証は追従込み〕なので e2e より後ろへは送れない）**。atom D10／要件 6.5 の裁定に従う実装側: キーワード基本位置は DPI 遷移で再導出しない・`BalloonFollow.offset` を k 倍で追随（両者排他・**申し送り正本＝bod brief 追記(70)＋棚卸⑩ブロック**）。決定論行列に「キーワード由来 offset が k 倍で中央揃えを保つ」ケース必達。rebase 必達: U4 doc＝**`follow/window_move.rs:24`**・`windowposition.rs` 単位混在 doc＝**:191-194**（2026-08-22 実測）。**cage 後着の利得**: 檻は一本化済み共有ハーネスで書ける（import 追随の手戻りが消える）。SSP オラクル観測（モニタ跨ぎバルーン挙動）は要件 research——**無ければ areka 設計原則から導出し COMPAT へ「areka 裁量」登記**（裁定密度の頂） |
| **W6.95**（4 本並走） | `scope-zorder-pinning` **✅ 完了（09-02）** | — | **新規起票（2026-08-27 追記(82)・開発者要望＝`\![set,zorder]` バルーン込み対応）**。ukadoc 正典＝左から順に手前・`b1,s1,b0,s0` 明示記法・複数グループ・再ペアはエラー・ゴースト終了まで有効＋`\![reset,zorder]`（descript 既定へ戻す）＋descript `seriko.zorder`（タグの descript 版・SSP 2.4）。**土台は揃っている**: `ghost-window-zorder` のペア機構 5 ファイル（`zorder_pair*.rs`・現行はペア 2 窓専用）を N 窓グループ語彙へ拡張・`\!` 汎用キャリアは転写済み（parsers/sakura/dola **無改変**＝sink 新設のみ・`move_cue.rs:486-556` 同型）・descript は `config.rs:103` `zorder_raw` へ**転記済み＝読む口を足すだけ**。**⚠正典衝突の裁定が要件の核**＝完了 spec 要件 3「スコープ間の上下関係を強制しない」→「既定＝非強制／タグ実行後＝当該グループのみピン留め」の二状態化＋COMPAT §8 登記（現在 z-order の行ゼロ）＋`seriko.zorder`「SERIKO レイヤ順」誤記の訂正。consumer_ledger の `"set"` 粒度（サブコマンド分配）も要件裁定。プロパティ `currentghost.seriko.zorder` は M2 縮退シーム登記（defer-canon 4 点セット）。pwc/bod とファイル集合は実測で交差なし（要ウォッチ 2 点は台帳・正本＝zsp brief） |
| **W6.95**（4 本並走） | `balloon-vertical-canon` **✅完了**（08-29・`vertical,0/1` 受口＋origin クランプ撤去・`\_l`／プロパティ／`\f` は語彙登記のみ） | （完了・`.kiro/specs/completed/areka-P0-balloon-vertical-canon/`） | **新規起票（2026-08-27 追記(83)・開発者要望＝SSP が縦書き対応したためバルーン定義を SSP 互換へ）**。SSP 2.8.80 が `vertical,0/1`＋座標再解釈（origin.x＝1 列目右端・折返し `wordwrappoint.y`・`.x` 無視）＋プロパティ `currentghost.balloon.scope(ID).vertical` を確立、**2.8.83（2026-08-26）で validwidth/validheight/lines が画面向き基準へ改訂**。areka の縦書きは自前拡張 `writing_mode`（CSS 語彙）で実装済み＝**機構は流用・受口と意味論突合だけが仕事**（`vertical,1`→`VerticalRl` 写像・`parse.rs:110` 隣に受口・sylphya へ `.vertical` 最小追加）。**⚠ ukadoc-mcp スナップショットは 2.8.80 時点＝プロパティ意味論が旧い**——本 spec の正典参照はライブ ukadoc＋changelog 2.8.83（brief に採取済み）。裁定の核＝⑴ 両キー併記時の優先順位 ⑵ `\_l` 縦書き座標系（現行の軸スワップは**非互換の疑い**・2.8.83 はバルーン画像基準＋負 X 列送り）⑶ プロパティ族の範囲（推奨＝`.vertical` 単独＋族は縮退シーム）。emo2 は縦書き痕跡なし（適合 14 項目へ非干渉）＝e2e をブロックしない。pwc/bod/zsp とファイル素（正本＝bvc brief） |
| **W7** | `emo2-conformance-e2e`（最終） | `/kiro-start areka-P0-emo2-conformance-e2e` | 全ユニット完了後＝適合14項目（#1 DPI 検証は追従込み・バルーン表示ライフサイクル追補・#3 は bind・#10 は ker が前提充足）の一周走行→**M1 完成宣言**。着手時義務: brief 全面再監査・㉘(E) の実機判断・#7（pasta 上流）は M1 完成を妨げない扱いの確認・**W6 申し送り＝再表示直後のバルーン隣接（`ReassertZOrder` 未消費）の実機確認** |

**干渉台帳（生存ペアのみ・2026-08-27 棚卸⑪で W6.9 完走後の実形へ再解決・旧全文は history）**:
- **pwc⇄bod（W6.95 同居ペア）**〔ファイル素（pwc＝`presenter/show.rs`／`mount.rs`・bod＝`placement/follow` 系＋`windowposition.rs`＋`persist.rs`）。**要ウォッチ→解消（2026-08-27）**: pwc が B-4 を採る場合のみ `mount.rs` 配置契約・当たり判定原点で bod の関心事に意味論上近接する条件付きウォッチだったが、pwc 要件討議で B-4 却下（見送り＋登記）が確定したため解消——bod 側は pwc の登記のみ参照でよい〕
- **zsp⇄pwc（W6.95 同居ペア）**〔ファイル素。弱接触 2 点＝⑴ pwc brief:161 の `zorder_pair_maintain.rs`／`command.rs`／`move_cue.rs` 言及は **dlp が着地させた起床旗 1 行の報告（pwc は読むだけ・改変予定なし）**と 2026-08-27 同時開始再確認で判別済み。**裏返しの義務は zsp 側**＝`zorder_pair_maintain.rs` を N 窓グループへ改組するとき dlp の起床旗 1 行と観測語彙（相名）を保存すること（pwc と dlp 観測資産が読む前提） ⑵ COMPAT §8 へ両者追記（行が別・機械衝突は軽微）〕
- **zsp⇄bod（W6.95 同居ペア）**〔ファイル素。**唯一の実質リスク**＝bod が `enqueue_window_set_pos`（`follow/window_move.rs:452-544`・現在 `SWP_NOZORDER`＋`hwnd_insert_after: None` ハードコード）の署名を変える場合のみ同一関数衝突。**zsp は同 funnel を触らない**（維持系は既存どおり `SetWindowPosCommand` を直接発行して z を通す）を design の不変条件とする——両 spec の design 時に相互確認のこと〕
- **bvc⇄zsp（W6.95 同居ペア）**〔ファイル素（bvc＝`areka-parsers/balloon/*`＋`areka-emo-text`・zsp＝`zorder_pair*.rs`＋ghost config＋sakura sink）。**要ウォッチは 2026-08-27 bvc 討議 #4 で解消**——bvc の `.vertical` 実追加はプロパティ系 3 spec（M2 ゲート）へ移管され、bvc は sylphya の語彙表に触れない（追記(86)）。残る共有追記先は COMPAT §8 の行隣接のみ〕
- **bvc⇄pwc／bvc⇄bod（W6.95 同居ペア）**〔ファイル素（pwc＝`presenter/show.rs`・bod＝`placement/follow` 系——bvc はどちらにも非接触）。ウォッチ事項なし。bod brief:24 の `region.rs` 言及は exact 裁定 doc の**参照のみ**（シンボル名参照ゆえ bvc の同ファイル改変でも壊れない）〕
- **W6.95 同時開始の再確認（2026-08-27・開発者要請）**: 4 brief の言及ファイルを機械抽出して全 6 ペア突合＝**改変集合の交差 0 を再確認**（重なり 6 件は全て「参照・観測・報告」の側と判別）。コア接触 crate も別居（zsp=`wintf/ecs/window`・bod=`areka/placement`・pwc=`areka-emo-present`・bvc=`areka-parsers`＋`areka-emo-text`）。**共有の追記先は 3 つだけ**＝doc/COMPAT_ARCHITECTURE.md（zsp §8・bvc balloon 節・bod「areka 裁量」の可能性＝**各自の節のみ追記・後着が rebase 吸収**）・sylphya 語彙表（zsp/bvc 行隣接・既登記）・`file_length_guard_test.rs` の例外表（**4 spec とも触らない**＝新規ファイルは 1,000 行未満で作る。2 spec が同時に表を編集すると衝突かつ規律違反）。4 worktree 並走のテスト実行は cage の決定論化（`temp-path-kit`＝プロセス間一時パス衝突の根治・sylphya 30% flake 根治）で以前より安全＝**同時開始 GO**
- **show.rs アンカー（2026-08-27 棚卸⑪で実測再確認・ドリフトなし）**: `apply_show` :46 起点・budget 域（compose/resample/mask/insert）:97-173・可視化の段（pwc）`set_visible` :375／`set_bounds` :381／`Visualize` 発行 :392
- **退役（2026-08-27 棚卸⑪・W6.9 完走により消滅または rebase 義務へ転化）**: cage⇄dlp（両者完了・`command.rs` 同居裁定は成立のまま完走）・bod⇄cage（cage 完了＝bod は一本化済みハーネス〔`log-capture-kit`＋委譲 shim・実測確認済み〕で檻を書ける）・cage⇄pwc（直列条件は W6.9 完走で充足）・dlp⇄pwc（dlp は**実行時挙動不変**で完了＝pwc の tick 実形 rebase は実質ゼロ・dlp の観測資産〔4 段帰属・tick 相別・perf-loop 道具〕は pwc が流用可）。棚卸⑩退役分（atom⇄bod・atom⇄dlp）・棚卸⑨退役分は history 参照
- `status-execution-states`=台帳 spec（着手しない・源着地時に just-in-time）・`surfaces-basepos`／`sakura-time-directives`／`balloon-canon-residue`／**`tick-gate-adoption`（2026-08-27 棚卸⑪起票）**／**`zorder-property`（2026-08-27 zsp 要件ディスカッション議題 3 起票＝`currentghost.seriko.zorder` の実導出・upstream の zsp は 2026-09-02 完了済み）**／**`cursor-tag-canon`（2026-08-27 bvc 討議起票）**／**プロパティ系 3 本＝`property-query-channels`→`currentghost-property-tree`→`property-catalog-lists`（2026-08-27 bvc 討議起票・この順が依存順）**／**文字装飾系 3 本＝`text-decoration-canon`→`anchor-tag-canon`＋`choice-marker-styling`（2026-08-27 bvc 討議起票・decoration が先頭ゲート）**=**M2 解禁ゲート**（M1 では着手しない・tick-gate は開発者裁定で前倒し可＝夜間/25 分/n≥3 の実測環境が前提・cursor-tag/プロパティ系/装飾系は bvc 完了後が自然・**anchor の lexer バグ〔素の `\_a` の文字漏れ〕のみ M1 前倒し修正の裁定候補**・**⚠未決＝`zorder-property` と `currentghost-property-tree` の二重所有裁定〔`currentghost.seriko.zorder` を専用 spec が持つのか、プロパティ樹 spec が一括で持つのか〕は着手前に 1 度で決める（2026-09-02 zsp マージ時の持ち越し）**）

## 着手手順

> **brief 全数完備体制**: M1 残ユニット 1 本（e2e。pwc・bod・bvc は 2026-08-29 完了、zsp は 2026-09-02 完了）＋M2 ゲート 13 本（`zorder-property` 合流で 12→13）＝全 14 本 brief 済み＝着手は該当 brief を読んで `/kiro-start <unit>` へ直行。新規課題の起票は `/kiro-discovery`（再入）で brief just-in-time 生成。`/kiro-spec-batch` は使わない（一括＝工場化）。ウェーブ跨ぎの合流判断は別セッションで一括（記憶 portfolio-convergence-decided-in-separate-session）。

## 制約

- Rust 2024・マルチクレート（wintf/dola/areka ＋ `areka-parsers` ＋最小依存 `shiori-abi` ＋ host-32 3クレート `shiori-host32-ipc`/`-host`/`-helper`）。
- **32bit 可搬性の適用範囲＝host-32 系（`shiori-host32-*`／`shiori-abi`）のみ**。wintf/areka 本体は x64＋arm64 ネイティブ（i686 検証を本体 spec に課さない）。
- 透過は WUC/DComp GPU 合成上のクリックスルー機構（`WS_EX_TRANSPARENT` 動的トグル＋αマスク）で成立（ULW は撤去済み）。SHIORI 内部唯一 ABI=`IShiori`(COM, HSTRING/UTF-16)。過去互換は 32bit Rust ホスト。
- 設計判断の変更は [doc/COMPAT_ARCHITECTURE.md](../../doc/COMPAT_ARCHITECTURE.md) を正本として更新。
- 実機運転の定石: 絶対パス起動（相対は pasta.dll LOAD 失敗）・i686 helper を先ビルド・`AREKA_APP_SMOKE_EXIT_MS` 有界自動終了＋`RUST_LOG` grep（記憶 areka-real-machine-signoff-bounded-auto-exit）。

## M2 以降

**M1 完成後に、実物を見て組み直す。** 本ロードマップでは扱わない（pasta の native x64・`IShiori` in-proc 化、ベクトル描画・AI、**owner-draw 右クリック system メニュー（ゴースト管理 chrome）**、互換面拡大＝Shift_JIS/SAORI/里々・YAYA 網羅/NAR 等はその時に）。

**アプリ層の M2 予約（2026-07-05 ukadoc 裏付け・全て任意＝emo2 単体起動に不要）**: SSTP ポート（9801）ホスティング・FMO・DirectSSTP・Plugin/HEADLINE/SAORI ホスティング・ネットワーク更新（`\![update,platform]`・OnBasewareUpdating/Updated 系）・ゴースト/バルーン選択 UI（OnGhostChanging 系）・多重ゴースト運用。

**emo テキスト進化の予約（M2 候補）**: ①回転テキストの実挙動（M1 で行列変換領域を内部表現として持ち込み済み）②ポップアート級の文字装飾（text effects——1枚物自前合成ゆえ文字も合成レイヤ）。

**バルーン美観配置政策の予約（M2・2026-08-01 `dpi-window-vanish` task 6.2 が先送り登記）**: 画面端でのバルーン**左右反転**をはじめとする SSP 互換の美観配置政策。M1 が持つのは遷移ガードによる「完全不可視への遷移を防ぐ安全網」まで（*見えない会話*より*重なった会話*を優先する裁定）。**縮退シーム＝`[visibility-guard] ClampX` の `warn!`**（`route=BalloonFollow` 行）——安全網の発火回数が M2 着手時の優先度根拠。**M1 では起票しない**。

**M2 解禁ゲートの spec（brief 済・M1 では着手しない）**: `areka-P0-surfaces-basepos`・`areka-P0-sakura-time-directives`（互換拡充時に解禁）／`areka-P0-status-execution-states`（残状態の源サブシステム着地時に just-in-time・台帳）／`areka-P0-balloon-canon-residue`（balloon 正典残語彙 10 項目の受け皿＝kero-balloon 由来 6 件＋balloon-visibility 由来 4 件）／**`areka-P0-tick-gate-adoption`**（tick の門の本採用＝アイドル CPU 3.0% 未満への再挑戦・M1 完成後または開発者裁定で前倒し・夜間/25 分/n≥3 の実測環境が前提・申し送り正本＝dlp requirements.md 改訂欄）／**`areka-P0-cursor-tag-canon`**（`\_l[x,y]` 全仕様の一括正典化＝%・@ 相対・負値・3 書字方向・完了 spec `emo-text-layer` 縮退表の改訂込み。**つまみ食い実装禁止が起票理由**〔bvc 討議 #3 開発者裁定 2026-08-27〕・bvc は縦書き `\_l` 非互換を既知登記して閉じる・bvc 完了後が自然・申し送り正本＝brief＋bvc research §3.5）／**プロパティ系 3 本（bvc 討議 #4 開発者裁定 2026-08-27・分割の要は「照会経路 vs 値の木」・正典全体 ≈180 項目・SET 有効 ≈26）**: **`areka-P0-property-query-channels`**（経路 6 本＝`\![get,property]` イベント発生・`\![set,property]`・`%property[…]` 埋め込み・`\![embed]`・`.ext` 逆方向・非スクリプト同期読み〔輸送路は snapshot 無記載＝ライブ実測必須〕＋kanade 参照付きイベント発生型の新設＋sylphya SET 台帳追随 21→26・**3 本の先頭ゲート**）→ **`areka-P0-currentghost-property-tree`**（`currentghost.*` ≈65 項目の初実導出・**bvc 縮退登記の balloon.scope(ID) 族 19 項目〔`.vertical` 込み・2.8.83 意味論〕の指名受け皿**・⚠並走 zsp ブランチの `zorder-property` と `seriko.zorder` が二重所有＝**マージ時にクロス spec 合流セッションで吸収 or 切り出しを裁定**）→ **`areka-P0-property-catalog-lists`**（`system.*` 25＋カタログ/履歴 8 根＋汎用 17 葉＋sound 族＋`.ext` 発火・大半が M2 基盤待ち＝島単位で前倒し可）／**文字装飾系 3 本（bvc 討議 #5 開発者裁定 2026-08-27・`\f` 族全 43 項目＝どの spec も未所有だったものの全域所有・分割の要は「何が既に存在するか」）**: **`areka-P0-text-decoration-canon`**（核 17 項目＝font10＋影3＋align/valign＋default/disable・descript `font.*` 基底 13 キー・per-run 属性基盤〔decode の `"f"` 腕・装飾 CueCommand・`DWRITE_TEXT_RANGE` 適用——現在 repo に 0 件〕・縦書き写像は bvc SC1 裁定を継承・**着手前提＝`draw.rs` 分割〔974/1,000〕**・roadmap M2 予約「ポップアート級文字装飾」の具体着地先・**3 本の先頭ゲート**）→ **`areka-P0-anchor-tag-canon`**（`\_a` 3 形＋`OnAnchorSelect(Ex)`＋アンカー装飾 16 項目＋`anchor.*.font.*` 族。**⚠実バグ同梱＝素の `\_a` で後続の `a` が本文へ漏れる**〔`lexer.rs:172-177`・M1 前倒し修正の裁定候補〕）＋**`areka-P0-choice-marker-styling`**（`\f[cursor*]` 10 項目＝完了 spec `choice-render` の descript 解決に実行時層を重ねる・`underline`→`SquareFill` 縮退の解除・S）。矢印（`arrow0/1` 縦書き=右/左スクロール）は装飾 spec ではなく **`balloon-canon-residue` 項目 1 へ第 3 軸として追記済み**・`\x`/`\x[noclear]` は residue 項目 9 と decoration の相互登記済み（単独着地不可）。

---

**追記台帳（要約・全文は history）**: (51) W6.5 残件 2 spec 起票（07-30）／(52) 棚卸④＝roadmap 履歴分離・W6 に bind 編入・brief 補正 8 本（07-31）／(53) `recompose-budget` 起票＝`compose_into` 本番未配線・アイドル 1 コア 13〜22%（07-31）／(54) `ghost-window-zorder` 起票＝owner 無し・全 `SWP_NOZORDER`・「語彙完備・配線ゼロ」3 例目（07-31）／**(55) 欠番**（atom 起票の本体がマージで脱落・内容は atom brief＋(57)⑴ が正本）／(56) 未登記先送り棚卸＝孤児 17 件・新規 brief 4 本起票（08-01）／(57) van 完了＝S5 担当が atom で確定・確定台帳の裁定付き訂正運用が確立・檻の空虚性通算 9 例（08-01）／(58) 棚卸⑤＝W5 3本マージ後の全面棚卸・全配置確定・干渉行列 55 ペア再実測（08-01）／(59) col 実装中の申し送り＝cage brief ①インベントリを 95 呼出/12 モジュールへ拡大（08-05・PR#101・cage brief 内が全文正本）／(60) 棚卸⑥＝col マージ後の軽量アンカー監査・実害ドリフト 2 ファイルのみ（08-06）／(61) `file-slimming` 起票＝W5.95 新設・肥大の 7〜8 割は in-file 檻（08-06）／(62) 棚卸⑦＝slimming 後の干渉台帳全面再解決・緩和 3 ペア（08-11）／(63) `bindoption-exclusivity` 完了＝根因 2 層・mayuna R4.5/D11 覆し・25 分実機 PASS（08-11）／(64) `ghost-window-zorder` 完了＝案 A owner 確定・topmost 帯引き込み是正 `HWND_TOP`（08-13）／(65) 棚卸⑧＝W6 完走後の全面再解決・退役 5 ペア・W6 行/詳報/追記(58)(60)-(64) を history 退避（08-14）／(66) 開発者裁定＝性能最優先・budget を W6.5 へ前倒し（08-14）／(67) 開発者裁定＝atom 文書併走不採用・W6.5 着地後の単独直列へ（08-14）／(68) `draw-load-parity` 起票＝W8 新設・優先度低・SSP 同一手順比較初実施（areka 10.97% 対 SSP 3.05%・**⚠SSP は 100% 描画→引き伸ばしの疑い＝目標を絶対値か画素あたり効率かは要件裁定事項**）・真の最大項＝`try_tick_world` 120回/秒 全走・budget 判定式⑵（catch-up）も引受け（08-15）／(69) 棚卸⑨＝W6.5 完走後の全面再解決・W6.75 単独直列確定・退役 5 ペア（08-15・全文は history）／**(70)〜(78) は atom（W6.75）走行中に各 brief へ直接登記された申し送り群＝各 brief が全文正本・roadmap には一行のみ**: (70) atom→bod＝キーワード基本位置は遷移で再導出しない・offset k 倍は bod の責務（D10／要件 6.5・統合候補失効・08-21）／(71) atom→dlp・e2e＝窓書込指令の形と一括 flush の中身の変化（08-21）／(72) atom→cage＝観測チャネル `wintf::transition` 新設・窓書込檻の前提変化（08-21）／(73) atom→dlp・e2e＝一周走行で使える遷移観測チャネル増（08-21）／(74) atom→dlp＝一括 flush を `DeferWindowPos` 1 バッチへ移す確定（08-21）／(75) `present-write-coherence` 起票＝W8 に 2 本目・atom の実機未達 µs 2 系統の引受先（08-22・全文は history・pwc brief が正本）／(76) atom→cage＝(72) 続き・`in_batch` 追加・`SELF_INITIATED_DEPTH` 是正の登記（08-22）／(77) atom→e2e＝(73) 続き・「FAIL だが GO」の読み方（08-22）／(78) atom→dlp＝(74) 着地後実測・B-2b は隙間を縮めず −1.8%（08-22）。／(80) 棚卸⑩＝atom 完走後の全面再解決・show.rs 全アンカードリフト実測・cage 毒化インベントリ両方向ドリフト・dlp 全性能数値の Bevy 0.19 前失効を登記（08-22・全文は history）／(81) 棚卸⑩補＝開発者指示〔bod 優先度低・並行数最大化〕で W6.9=cage ∥ dlp・W6.95=pwc ∥ bod へ改訂・W8 解散・bod は e2e より後ろへ送れない（08-22・全文は history）。

**2026-08-22 追記(79)（1 ファイル 1,000 行の目安の漂流——✅ 解決済み・2026-08-27 棚卸⑪で確認・旧全文〔9 本表・経緯〕は history）**: 棚卸⑩が「番人ゼロ・9 本超過」と報告し 3 択を提示した件は、**`test-cage-determinism` が ⒜（機械の番人）を採って解決済み**——行数番人 `crates/log-capture-kit/tests/file_length_guard_test.rs`（cage 要件 10.2〜10.4）が常設され、例外表 11 件（着手時実測＝cage `verification/remeasure.md` §6 と一致・2026-08-27 の実測 11 本とも完全一致）は ⑴件数を逐語の定数で二重宣言 ⑵理由必須・ファイル逐語指定 ⑶「今はもう超過していない」項目を赤にする ⑷実測超過集合と表の完全一致検査、により**暗黙増加が構造的に不可能**（新規超過は即赤・既存は漸減のみが自然な方向）。登記は `structure.md:317` にも実在。**⚠ 棚卸⑪の初回登記（同日・コミット 808d2de9）はこれを見落とし「番人ゼロ・候補失効・3 択再提示」と誤認**——同日中に structure.md の記載から番人を発見し実物 grep で確認・本追記で是正した（doc 主張は file:line で裏取りの規律を、行数だけ測って番人を grep しなかった形で踏んだ実例）。残る任意課題は既存例外 11 本の漸減のみ（急がない・`file-slimming` 第 2 期はその時の選択肢）。

**2026-08-27 追記(82)（`scope-zorder-pinning` 起票＝`\![set,zorder]` バルーン込み対応・W6.95 を 3 本並走へ）**: `/kiro-discovery` 再入（Path C・開発者要望「ukadoc を調査し `\![set,zorder,スコープID,...]` にバルーン込みで対応して欲しい」）。①**ukadoc 正典 4 点を採取**——タグ（2.3.77・s,b 記法 2.7.34＝左から手前・複数グループ・再ペアエラー・ゴースト終了まで）・`\![reset,zorder]`（2.3.77・descript 既定へ）・descript `seriko.zorder`（2.4・「タグの descript 版」）・プロパティ `currentghost.seriko.zorder`（2.8.78・明示/数値モード排他・書込完全置換）。②**`set,zorder`／`reset,zorder` はリポジトリ全体 0 件＝完全新規**。ただし土台は実在: ペア機構（`zorder_pair*.rs` 5 本・ペア 2 窓専用）・`\!` 汎用キャリア転写済み（parsers/sakura/dola 無改変で sink まで届く）・`config.rs:103` `zorder_raw` 生転記済み。なお roadmap:27 の「配線ゼロ 5 例」中 `ZOrder` は PR#107 で実消費に転じており歴史的記録である。③**正典衝突を確認**——完了 spec `ghost-window-zorder` 要件 3（スコープ間の上下関係を強制しない）と正面衝突。ukadoc 原文「ユーザの操作次第」はスクリプト明示指定を禁じていないため「既定＝非強制／タグ後＝当該グループのみピン留め」の**二状態化**を要件裁定へ（COMPAT §8 は現在 z-order の行ゼロ＝登記義務）。④**解釈誤りの訂正を登記**——`ghost-window-zorder` brief の「`zorder_raw`＝SERIKO レイヤ順」注記は誤り（ukadoc・`window-placement` design.md:67 とも「タグの descript 版」＝窓 Z 順）。⑤**consumer_ledger の粒度問題**——台帳はコマンド名のみで `"set"` 登記は全サブコマンドの分配点化＝`sakura-time-directives`（compile 側 allowlist・層違い）と非衝突だが要件で明文化。⑥**編成＝W6.95 へ 3 本目として編入**（pwc ∥ bod ∥ zsp）——ファイル集合は実測で交差なし（zsp⇄pwc は観測名指しのみ・zsp⇄bod は `enqueue_window_set_pos` 署名変更時のみの条件付きリスク＝台帳へ登記）。追記(81) の「幅 2-2-1 が上限」は本起票で「3-1」へ改訂。⑦**一石二鳥候補**——W6 申し送り⑴（vis が `ReassertZOrder` を消費せず着地）の供給者に zsp の張り直し機構がなれる。⑧プロパティ実導出は M2 縮退シーム登記（defer-canon 4 点セット・sylphya SET 有効群 21→22 の要否は要件段階で裁定）。正本＝`areka-P0-scope-zorder-pinning/brief.md`。

**2026-08-27 追記(83)（`balloon-vertical-canon` 起票＝バルーン縦書きの SSP 正典化・W6.95 を 4 本並走へ）**: `/kiro-discovery` 再入（Path C・開発者要望「SSP が縦書き対応したのでバルーン定義を SSP 互換に変更・プロパティシステムの `vertical` も対応」）。①**正典を 2 系統で採取**——ukadoc-mcp（`vertical,0/1`＝2.8.80・`wordwrappoint.y`・座標再解釈・プロパティ `currentghost.balloon.scope(ID).vertical`）＋開発者提示の changelog URL（**SSP 2.8.83・2026-08-26**＝`sstpmessage.yb`/`number.x`/`number.yb` 追加・**validwidth/validheight/lines の意味論を画面向き基準へ改訂**・`\_l` は縦書きでもバルーン画像基準＋負 X 列送り）。②**ukadoc-mcp の陳腐化を実証**——スナップショットのプロパティ記述は 2.8.80 の旧意味論（validwidth と validheight の役割が現行ライブと**逆**）＝縦書き関連はライブ ukadoc で裏取り必須（記憶 ukadoc-mcp-preferred-source へ注記済み）。③**実装踏査**——areka の縦書きは自前拡張 `writing_mode`（CSS 語彙・`parse.rs:110`・`writing.rs:45` の 3 値軸リマップ）で**機構は完備**・`vertical` キーは未解析＝SSP バルーンが黙って横書きになる・sylphya に `balloon.scope` 系プロパティは族ごと 0 件。④**裁定の核 3 点を brief へ登記**——両キー併記時の優先順位／`\_l` 現行軸スワップ（`layout.rs:462-465`＝(y,x) 入替）は 2.8.83 正典と**非互換の疑い**／プロパティ族の範囲（推奨＝`.vertical` 単独＋族は縮退シーム）。⑤**編成＝W6.95 へ 4 本目として編入**（pwc ∥ bod ∥ zsp ∥ bvc）——ファイル集合は実測で交差なし（唯一の要ウォッチ＝zsp と sylphya 語彙表の行隣接・台帳へ登記）。追記(82) の「3-1」は本起票で「4-1」へ改訂。⑥ emo2 は縦書き痕跡なし（`emo2-conformance-scope.md:61`）＝適合 14 項目へ非干渉・e2e をブロックしない開発者要望駆動の互換機能（zsp と同種）。正本＝`areka-P0-balloon-vertical-canon/brief.md`。

**2026-08-27 追記(84)（棚卸⑪＝W6.9 完走後の全面再解決・`tick-gate-adoption` 起票・W6.95 は 4 本並走のまま確定）**: `/kiro-discovery` 再入。前回⑩以降の main 差分＝W6.9 完走（dlp PR#118〔08-23〕・cage PR#119〔08-27〕）＋zsp/bvc 起票（PR#120/#121・追記(82)(83)）＋wintf 直接コミット（geometry visual clips・visual content bounds）。①**引受先ゼロ案件を 1 件解消**——dlp requirements.md 改訂欄の「門の本採用は新規 spec が要る」へ `areka-P0-tick-gate-adoption` を起票（M2 解禁ゲート 5 本目・M1 完成を妨げない優先度の継承・着手ゲート＝M1 完成後または開発者裁定・夜間/25 分/n≥3 環境が前提・brief に dlp 改訂欄の要件候補 3 点＋未計測 4 項目を転記）。②**W6.95 アンカー総点検＝全て現存**（`apply_show`:46・可視化の段 :375/:381/:392・`window_move.rs:24`・`windowposition.rs:191-194`＝2026-08-27 実測・ドリフトなし）。cage③ の一本化も実形確認——`placement/test_support.rs` は `log-capture-kit` への委譲 shim 化済み＝bod は最初から共有ハーネスで檻を書ける。③**干渉台帳 4 ペア退役**（cage⇄dlp・bod⇄cage・cage⇄pwc・dlp⇄pwc＝W6.9 完走で消滅または充足。dlp は実行時挙動不変で完了＝pwc の tick 実形 rebase は実質ゼロ・dlp 観測資産は pwc が流用可）・生存は W6.95 同居 5 ペアのみ。④**追記(79) は解決済みと確認**——cage が ⒜ を採り行数番人を実装済み（`file_length_guard_test.rs`・例外表 11 件・暗黙増加不可）。棚卸⑪の初回登記は番人不在と誤認し同日是正（経緯は本文(79)）。⑤**cage 残作業＝`/kiro-steering` は同日実施済み**（structure.md へ欠落 12 crate の節を追加・開発者裁定 08-27 どおり spec を立てず steering 同期で解消）。⑥**roadmap 減量**: W6.9 完了 2 行全文・追記(79) 旧全文・(80)(81) 全文を history へ退避・ゴール表完了行の集約・「3 本並走」残存ラベルの是正。⑦ ウェーブ編成は追記(83) の 4-1（W6.95=pwc ∥ bod ∥ zsp ∥ bvc → W7=e2e）を**変更なしで確定**——厳しめ精査でも 5 本目は組めない（e2e は全ユニット後・M2 ゲート 5 本は解禁条件未成立・tick-gate は e2e と干渉）。

**2026-08-27 追記(85)（`cursor-tag-canon` 起票＝`\_l[x,y]` 全仕様の一括正典化・M2 解禁ゲート 6 本目）**: `/kiro-discovery` 再入（Path C・bvc 要件ディスカッション議題 3 の開発者裁定「`\_l` はやるなら同じタイミングで全部実装しないといけない。全仕様 spec を作り bvc では一切実装しない」による即日起票）。①**ukadoc 全語彙を採取**——数値（文字描画範囲左上 px）／省略／em／lh（1em＋行間）／%／@ 相対（負＝左/上・em/% 共存可）＋副作用（`\_l` 直後の左寄せリセット・`\f[align]` インデント相互作用・`\c[line]` の行分割単位）。**縦書きでは負の絶対 X が 2 列目以降の正規指定手段**（SSP 2.8.83・原点＝描画範囲右上）＝部分解禁は互換を名乗れない。②**bvc ギャップ分析の実測を brief へ転記**——非負ゲート `layout.rs:656-670`・`vertical_rl` の原点符号不一致（`\_l[0,0]` が描画範囲外左方へ着地）・`vertical_lr` は既に一致・%・@ 未実装・縦書きテスト被覆 0（22 箇所全て HorizontalTb）・縮退表の正典＝完了 spec `emo-text-layer` R2.4/6.5（改訂＋追随義務）。③**bvc R4 は語彙登記へ縮小**（実装 0・既知非互換の登記＋本 spec を追跡先とする defer-canon 4 点セット）。④配置＝**M2 解禁ゲート**（emo2 非依存で e2e を妨げない・bvc 完了後が自然・開発者裁定で前倒し可）。⑤並走 zsp ブランチの `zorder-property`（同日起票）と M2 ゲート計数が行隣接＝マージ時に後着が再計数。正本＝`areka-P0-cursor-tag-canon/brief.md`。

**2026-08-27 追記(86)（プロパティ系 3 spec 起票＝プロパティシステム正典の全域所有・M2 解禁ゲート 7〜9 本目）**: `/kiro-discovery` 再入（bvc 要件ディスカッション議題 4 の開発者指示「`currentghost` スコープの実装が無い。ukadoc で規模感を確認し 1〜n 個の spec を立ち上げよ」）。①**規模実測**——プロパティ木 ≈180 項目（`currentghost` ≈65〔balloon.scope 族 19・scope 幾何 17・seriko 14・mousecursor 6 他〕・`system` 25・カタログ/履歴 8 根・汎用 17 葉・サウンド語彙 ≈18 葉）・SET 有効 ≈26・照会経路 6 本（`\![get,property]` イベント発生型・`\![set,property]`・`%property[…]`・`\![embed]` 対・`.ext` 逆方向 `property.get/set`・非スクリプト同期読み〔里々 `get_property`＝**輸送路が snapshot 無記載・ライブ実測必須**〕）。②**areka 側の実測**——sylphya 機構は完備（ByIndex/ByName セレクタ・NotFound/NotSettable 縮退）だが `currentghost.*` 実導出 0・`\![get,property,…]` は汎用キャリアで全 sink に届いて全員に無視されている・kanade に参照付きイベント発生型が無い・`%property[…]` は lexer が `[` で止まり字句解析不能・host32 IPC はプロパティ 0 件・SET 台帳が正典比 5 件古い（21→26）。③**分割の要は「照会経路 vs 値の木」**——どの枝も「どう読むか」を再審議させない。3 spec＝**channels（L・先頭ゲート）→ currentghost-tree（M・bvc 縮退登記 balloon.scope 族 19 項目〔`.vertical` 込み〕の指名受け皿・全項目列挙済み）→ catalog-lists（M/L・大半 M2 基盤待ち・島単位前倒し可）**。④**bvc R7/R8 は語彙登記へ縮小**（実装 0・R4 と同形の defer-canon 4 点セット）＝bvc は sylphya に触れなくなり **bvc⇄zsp の唯一の要ウォッチ（語彙表行隣接）が解消**（台帳更新済み）。⑤⚠**二重所有の登記**——並走 zsp ブランチの `zorder-property` は `currentghost-property-tree` の `seriko.*` 範囲の真部分集合＝マージ時にクロス spec 合流セッションで吸収 or 切り出しを裁定（brief 双方に登記済み）。正本＝3 spec の brief。

**2026-08-27 追記(87)（文字装飾系 3 spec 起票＝`\f` 族全 43 項目の全域所有・M2 解禁ゲート 10〜12 本目）**: `/kiro-discovery` 再入（bvc 要件ディスカッション議題 5 の開発者指示「別 spec が必要では。ukadoc を調べて仕様を立ち上げよ」）。①**規模実測**——`\f[...]` 全 43 項目（版番号付きは 4 つだけ＝shadowstyle 2.5.27・align 2.5.31・disable 2.5.51・valign 2.6.19）＋descript `font.*` 基底 13 キー（うち解析済み 5）＋接頭辞付き font 族 9 系統（descript の font 掃引 96 項目）。**`\f` は 1 項目も解読されていない**（`decode_tag` に `"f"` 腕なし→`Instruction::Raw`→compile で黙って破棄・`TextEffects` はフィールド 0 の空シーム・repo に `SetUnderline`/`SetFontWeight` 等 0 件・align/valign は全書字方向で `draw.rs:271-272` 固定）。どの spec も `\f` を未所有だった。②**分割の要は「何が既に存在するか」**——per-run 属性だけで立つ核 17（decoration・L）／アンカー機能ごと不在の 16（anchor・M-L・`\_a` 3 形＋イベントごと所有）／既存選択肢レンダラ上の 10（choice-marker・S）。③**⚠実バグ発見**——素の `\_a` は lexer が `'_'` のみ消費し**後続の `a` が可視本文へ漏れる**（`lexer.rs:172-177`・emo2 非使用で M1 適合には無害・anchor brief が修正を所有・M1 前倒しは開発者裁定候補）。④**矢印は装飾ではない**——`arrow0/1` の縦書き=右/左スクロール再解釈は `balloon-canon-residue` 項目 1 へ**第 3 軸として追記**（同 brief の「列挙なき所有者なし」規律を追記で満たす）。`\x`/`\x[noclear]`（residue 項目 9）と decoration のリセット意味論は**相互登記＝単独着地不可**を双方に明記。⑤ bvc R5 は語彙登記＋追跡先確定へ縮小（align/valign/underline→decoration・arrow→residue 項目 1）。SC1（valign 写像の 2 ページ矛盾）は bvc 裁定をdecoration が継承し再審議しない。正本＝3 spec の brief＋residue brief 追記。
