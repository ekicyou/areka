# perf-compare.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 測っていない副指標（NA）は採用を止めない
goal = goal.toml
a1 = ../runs/idle_930_p95_150
a2 = ../runs/idle_940_p95_150
b1 = ../runs/idle_610_p95_150
b2 = ../runs/idle_620_p95_150
exit = 0
verdict = ADOPTED

before_idle_cpu_pct = 9.35
after_idle_cpu_pct = 6.15
delta_pct = -3.20
noise_pct = 0.30
secondary = p95_ms=150.000/150.000, talk_peak=-/-, allocs=0/0

note = adopted と同じ 4 本の走行を、副指標に talk_peak_cpu_pct を足した目標定義で読む。
note = 素材の走行は発話区間を持たないので、この指標は前後とも `-` で出る。
note = 期待は ADOPTED——`-` は「測っていない」であって「悪化していない」ではないから
note = 悪化と断じられず、かといって観測が無いことを悪化として扱うと、既定 OFF の観測を
note = 点け忘れた走行が必ず WORSE になり、採否が点灯忘れで決まってしまう。
note = 代わりに表の verdict 列へ NA と書き、脚注で「見落とさないよう名前を出す」ことを
note = 期待出力が逐語で固定する。worse_secondaries は空のままである。
note = この決めを逆にする改変（NA も悪化として数える）は、このケースが赤にする。
