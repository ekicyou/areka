# judge-perf.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 指定されたファイルが無い（引数の誤り）
mode = verdict
build = release
exit = 3

note = cpu.csv が置かれていないディレクトリ。終了コードは 3（引数の誤り・
note = 読み取り不能）でなければならない。
note = 「測ったが判定できない（2）」と「そもそも指定が間違っている（3）」は
note = 直し方が違うので、終了コードで分けてある。
