# judge-perf.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 別 scope のアニメが重なっても判定対象の間隔は落ちない
mode = verdict
build = release
exit = 1

note = 判定対象の系列（キャラ・scope="0"）に、上限を大きく超えるコマ間隔
note = 400.000ms が 10 本ある走行。上限は 172.500ms なので不合格（終了コード 1）で
note = なければならない。
note = その 10 本の区間に重ねて、**別の scope（ケロ・scope="1"）のアニメ** の
note = 発火と停止を置いてある。ケロのアニメは別の対象へコマを出すので、キャラの
note = 記録の列には 1 行も混ざらない。キャラのコマ送りが健全かどうかについて
note = 何も語らない情報である。
note = 【この fixture が押さえている経路】判定の窓の条件 4（同時に走っている
note = アニメの推定本数が 1 本を超える区間の間隔は使わない）を、scope をまたいで
note = 数えると、ケロが動いているというだけでキャラの間隔が落ちる。
note = 落ちたのが上限超過の 10 本ちょうどなら、残るのは健全な 140 本だけになり、
note = 上位 5% は 150.000ms＝**確定的な不合格が合格（終了コード 0）へ反転する**。
note = 数える範囲を判定対象の系列の scope に限れば 150 本すべてが残り、
note = 上位 5% は 400.000ms＝不合格である。
note = 終了コードが 1 と 0 に分かれるので、この fixture だけで 2 つの数え方を
note = 区別できる。条件 4 の数える範囲を全 scope へ戻すと、ここが赤になる。
note = 【なぜ足したか】この形は実機のベースラインで実際に起きていた。長時間 release で
note = 42 本の間隔が落ちており、その 42 本は 1 本残らず「キャラ 1 本＋ケロ 1 本」で、
note = キャラの 2 本重なりは 1 件も無かった。落ちた 42 本には上限超過が 1 本
note = （173.997ms）含まれ、上限超過が 9 件から 8 件へ減っていた。
note = corpus には scope をまたぐ形が 1 つも無かったので（P12・P13 はどちらも
note = 2 本とも scope="0"）、この誤りは 3 回のレビューを素通りした。
note = 陽性対照の無い経路は静かに壊れる——本 spec が三度踏んだ形である。
