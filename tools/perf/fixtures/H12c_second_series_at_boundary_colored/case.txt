# judge-perf.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 色付きログ（色を落とす経路の較正）
mode = verdict
build = release
exit = 0
twin = H12_second_series_at_boundary

note = H12 と中身が同じで、run.log にだけ色（画面を彩る制御文字）が入っている走行。
note = 本物の run.log はこの形で出る——ログの出力先がファイルであっても、色を止める
note = 設定が無いかぎり色が付くためである。judge-perf.py は読み込み時に色を落として
note = から解析する。その経路を踏む fixture がここにしかない。
note = 期待終了コードは H12 と同じ 0 である。色を落とす処理を外すと、行頭の時刻が
note = 読めなくなって判定不能（2）へ落ちるので、この 1 本が赤になる。
note = さらに --selftest は、この fixture のレポートが色なしの双子（H12）の
note = レポートと（入力パスの違いを除いて）一字一句同じであることまで確かめる。
note = 終了コードが同じでも中身がずれていたら、それも捕まえるためである。
