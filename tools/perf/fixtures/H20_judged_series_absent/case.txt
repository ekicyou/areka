# judge-perf.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 判定対象に指定した系列が観測されていない（判定不能）
mode = verdict
build = release
exit = 2

note = 判定対象に指定した系列（TargetId(0) / surface_id=1000）が、定常状態の記録に
note = 1 度も現れない走行。別の系列（surface_id=1005）だけが健全に動いている。
note = 判定不能（終了コード 2）でなければならない。
note = 【なぜ黙って通りやすいか】指定した系列は記録に現れないので、判定対象の
note = 一覧にも除外の一覧にも出てこない。数え直す元の表にすら現れないため、
note = 「残った系列が上限内だった＝合格」に化ける。指定と実測を直接突き合わせる
note = 関門がないと、この形は静かに合格する。
note = ログの絞り込み設定を誤って片方のゴーストの詳細ログが落ちた走行、
note = 面の呼称や対象の番号が変わった走行が、実際にこの形になる。
note = 指定は全部か無しかなので、指定した系列が観測されていることまで含めて
note = 初めて合格が意味を持つ。
