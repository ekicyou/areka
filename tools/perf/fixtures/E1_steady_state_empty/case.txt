# judge-perf.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 定常状態に観測がない（判定不能・沈黙を合格にしない）
mode = verdict
build = release
exit = 2

note = 起動直後の適用しかなく、定常状態（起動から 60 秒より後）には適用が
note = 1 回も無い走行。判定不能（終了コード 2）でなければならない。
note = ここを合格にしてはいけない理由がこの corpus の背骨である——
note = 「定常状態での取りこぼし 0 件」「定常状態での新規確保 0 件」という 2 つの
note = 判定は、観測が 1 つも無ければ「0 件だから合格」と自動的に成立してしまう。
note = 何も測っていない走行と、測って問題が無かった走行を、終了コードで
note = 区別できなければ、この道具は沈黙を合格として売ることになる。
