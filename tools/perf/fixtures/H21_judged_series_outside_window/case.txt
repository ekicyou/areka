# judge-perf.py --selftest の期待値台帳（人も機械もこの 1 ファイルを読む）
title = 判定対象の系列が判定の窓に 1 本も残らない（判定不能）
mode = verdict
build = release
exit = 2

note = 判定対象の系列が定常状態で 400 回も適用されているのに、アニメの発火の記録が
note = 1 本も無い走行。判定の窓は発火を起点に切り出すので、間隔が 1 本も残らない。
note = 判定不能（終了コード 2）でなければならない。
note = 【なぜ黙って通りやすいか】間隔が 1 本も残らない系列は間隔の表に鍵が立たず、
note = 判定対象にも除外にも現れない。しかも間隔が 150ms と健全なので、
note = 窓の外を見れば「問題ない走行」に見える。ここを合格にすると、
note = 「窓の設定が壊れていて何も測れていない」と「測って問題が無かった」が
note = 区別できなくなる。
note = 発火の記録が出ない走行（ログ水準の絞り込み・アニメ定義の変更）と、
note = 適用が発火から離れすぎている走行（＝本 spec が追っている鈍化そのもの）が、
note = 同じ経路でここへ来る。どちらも判定不能である。
