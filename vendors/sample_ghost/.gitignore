# リポジトリ直下の .gitignore はデバッグ用一時ログを避けるために `*_test.txt` と
# `*_dump.txt` を除外している。Windows の git は大小を区別しないため、この規則が
# 里々テンプレートの辞書 `dic09_Test.txt` を巻き込んで追跡対象から落とす。
# 辞書が 1 つ欠けた検体は配布物ではないので、この木の中だけ打ち消す。
!*_test.txt
!*_dump.txt
