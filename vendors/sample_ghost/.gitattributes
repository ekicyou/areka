# 第三者が配布した検体ゴースト置き場。取り出したバイト列が配布時と 1 バイトも
# 違ってはならない。このリポジトリは core.autocrlf=true なので、何もしないと
# CRLF が LF に変換されて登録され、Windows 以外で取り出したときに中身が変わる
# （里々テンプレートは辞書も surfaces.txt も descript.txt も全行 CRLF）。
# 将来この木を .nar へ畳んでハッシュで陳腐化を見る予定であり、環境ごとに
# バイトが揺れると検出そのものが成り立たなくなる。変換をやめさせる。
* -text
